{
  "name": "@vecells/hub-desk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Phase 5 hub desk shell and same-shell start-of-day workspace.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4303",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview --host 127.0.0.1 --port 4403"
  },
  "dependencies": {
    "react": "19.2.5",
    "react-dom": "19.2.5",
    "@vecells/api-contracts": "workspace:*",
    "@vecells/design-system": "workspace:*",
    "@vecells/observability": "workspace:*",
    "@vecells/persistent-shell": "workspace:*",
    "@vecells/release-controls": "workspace:*"
  }
}
