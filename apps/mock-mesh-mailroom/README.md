# mock-mesh-mailroom

Operational mailroom console for `seq_028`.

- Visual mode: `Signal_Post_Room`
- Default dev host: `127.0.0.1`
- Default dev port: `4179`
- App URL: `http://127.0.0.1:4179`
- Expected mock transport base URL: `http://127.0.0.1:4178`

The app is intentionally built as a premium internal operational surface, not a generic admin grid. It keeps mailbox ownership, workflow validation, transport acceptance, business proof, replay review, and live-later mailbox-application gating visible on one same-shell surface.
