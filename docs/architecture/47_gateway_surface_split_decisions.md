# 47 Gateway Surface Split Decisions

        These decisions explain why gateway surfaces remain split, or where a route family has an explicit secondary variant instead of two primary owners.

        | Decision | State | Surfaces | Drivers | Summary |
        | --- | --- | --- | --- | --- |
        | `DEC_047_PATIENT_PUBLIC_WEB_VS_PHONE` | `split_required` | `gws_patient_intake_web; gws_patient_intake_phone` | `session_policy; ingress_channel; cache_policy` | Public patient web and telephony intake remain separate because session evidence and capture-channel recovery posture differ. |
| `DEC_047_GRANT_RECOVERY_VS_PATIENT_PORTAL` | `split_required` | `gws_patient_secure_link_recovery; gws_patient_home` | `tenant_isolation; session_policy; recovery_profile` | Grant-scoped recovery cannot merge into the authenticated patient portal because route intent and tenant scope are materially narrower. |
| `DEC_047_PATIENT_HOME_VS_REQUESTS` | `split_required` | `gws_patient_home; gws_patient_requests` | `downstream_workload; mutating_contexts; recovery_profile` | Patient home remains projection-only while request detail can launch request-bound mutations across several domains. |
| `DEC_047_APPOINTMENTS_VS_REQUESTS` | `split_required` | `gws_patient_appointments; gws_patient_requests` | `recovery_profile; served_contexts; cache_policy` | Appointments keep booking and external-confirmation posture distinct from general request detail. |
| `DEC_047_MESSAGES_VS_REQUESTS` | `split_required` | `gws_patient_messages; gws_patient_requests` | `mutating_contexts; recovery_profile` | Messages keep communication reply and callback-repair authority explicit instead of hiding it inside the general request surface. |
| `DEC_047_RECORD_VS_PORTAL_HOME` | `split_required` | `gws_patient_health_record; gws_patient_home` | `cache_policy; artifact_posture` | Record and artifact viewing stays read-only and parity-governed, so it cannot merge into the general patient portal home. |
| `DEC_047_EMBEDDED_CHANNEL_SPLIT` | `split_required` | `gws_patient_embedded_shell; gws_patient_home` | `channel_freeze; release_posture; cache_policy` | Embedded patient delivery stays explicit because channel-freeze and host-capability posture differ from browser mode. |
| `DEC_047_CLINICAL_VS_PRACTICE_OPS` | `explicit_variant` | `gws_clinician_workspace; gws_practice_ops_workspace` | `audience; mutating_contexts; purpose_of_use` | Clinical and practice-operations work reuse one route family but stay as separate audience variants with different minimum-necessary mutation scope. |
| `DEC_047_HUB_QUEUE_VS_CASE` | `split_required` | `gws_hub_queue; gws_hub_case_management` | `mutating_contexts; recovery_profile` | Hub queue and hub case management stay separate because active cross-organisation mutation is legal only in the case surface. |
| `DEC_047_SUPPORT_TICKET_VS_REPLAY` | `split_required` | `gws_support_ticket_workspace; gws_support_replay_observe` | `trust_zone_boundary; session_policy; recovery_profile` | Support replay crosses the assurance boundary and cannot share the primary support ticket surface. |
| `DEC_047_SUPPORT_TICKET_VS_ASSISTED_CAPTURE` | `explicit_variant` | `gws_support_ticket_workspace; gws_support_assisted_capture` | `ingress_channel; mutating_contexts` | Support-assisted capture keeps a separate support-owned ingress and recovery contract while reusing the main ticket route family. |
| `DEC_047_OPS_BOARD_VS_DRILLDOWN` | `split_required` | `gws_operations_board; gws_operations_drilldown` | `mutating_contexts; recovery_profile; trust_zone_boundary` | Operations drill-down is split from the board because intervention and restore controls carry stronger authority than watch-only diagnostics. |
| `DEC_047_OPS_VS_GOVERNANCE` | `split_required` | `gws_operations_board; gws_governance_shell` | `tenant_isolation; assurance_trust; recovery_profile` | Governance remains a separate control-plane shell because watch-tuple, blast-radius, and approval-freeze posture differ from operations work. |
| `DEC_047_ASSISTIVE_NOT_STANDALONE` | `explicit_variant` | `gws_clinician_workspace_child; gws_assistive_sidecar` | `tenant_isolation; bounded_secondary; recovery_profile` | Assistive remains a bounded secondary surface inside the owning staff shell rather than a standalone published BFF. |
