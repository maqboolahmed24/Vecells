# 94 Release Publication Parity Rules

        ## Exactness rules

        - Bundle, matrix, route, frontend, projection, live-channel, cache, recovery, and continuity members are grouped into one parity tuple.
        - `parityState = exact` only when the runtime bundle is publishable and no group has drift or refusal reasons.
        - `routeExposureState = constrained` when parity is exact but browser posture, accessibility coverage, or design lint ceilings still suppress calm live publication.
        - `routeExposureState = frozen` when parity is stale or conflict.
        - `routeExposureState = withdrawn` when provenance or publication is revoked.

        ## Current parity records

        - `rpp::local::authoritative` for `local`: parity `exact`, route exposure `constrained`.
- `rpp::ci-preview::authoritative` for `ci-preview`: parity `stale`, route exposure `frozen`.
- `rpp::integration::authoritative` for `integration`: parity `conflict`, route exposure `frozen`.
- `rpp::preprod::authoritative` for `preprod`: parity `withdrawn`, route exposure `withdrawn`.
- `rpp::production::authoritative` for `production`: parity `stale`, route exposure `frozen`.

        ## Refusal reasons

        - `BINDING_CEILING_1`: Browser posture is read_only for Clinical workspace.
- `BINDING_CEILING_2`: Accessibility coverage is degraded.
- `BINDING_CEILING_3`: Design lint verdict is dclv::clinical_workspace::pending.
- `BUILD_PROVENANCE_CONFLICT`: Fail-closed publication reason generated by the runtime publication authority.
- `BUILD_PROVENANCE_STALE`: Fail-closed publication reason generated by the runtime publication authority.
- `BUILD_PROVENANCE_WITHDRAWN`: Fail-closed publication reason generated by the runtime publication authority.
- `SURFACE_DEFERRED_CHANNEL_MIXED`: Fail-closed publication reason generated by the runtime publication authority.
- `SURFACE_DESIGN_PUBLICATION_PENDING`: Fail-closed publication reason generated by the runtime publication authority.
- `SURFACE_PLANNED_EXACTNESS_GAP`: Fail-closed publication reason generated by the runtime publication authority.
- `SURFACE_PLANNED_PUBLICATION_GAP`: Fail-closed publication reason generated by the runtime publication authority.
- `SURFACE_RELEASE_PARITY_PENDING`: Fail-closed publication reason generated by the runtime publication authority.
- `SURFACE_REPLAY_RESTORE_GUARDED`: Fail-closed publication reason generated by the runtime publication authority.
