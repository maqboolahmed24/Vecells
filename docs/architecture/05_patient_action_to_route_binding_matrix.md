# Patient Action To Route Binding Matrix

Every post-submit patient action is route-bound, freshness-checked, and safety-aware. The shell must recover in place when the route tuple, reachability, visibility, or binding posture drifts.

| Patient Action | Route Context | Required RouteIntentBinding | Command Semantics | Freshness Check | Safety Preemption | Recovery Behavior | Source Refs |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Reply to more-info | Requests detail or secure-link respond-to-request mode in the same patient shell. | RouteIntentBinding(actionScope = respond_more_info, governingObject = MoreInfoCycle, lineageFenceEpoch, responseRouteIntentBindingRef, requestReturnBundleRef). | One patient reply command settles MoreInfoResponseDisposition; only accepted_in_window or accepted_late_review may mint EvidenceAssimilationRecord and MaterialDeltaAssessment. | Current MoreInfoReplyWindowCheckpoint must be open, reminder_due, or late_review; the cycle must still be current and unsuperseded. | Any potentially_clinical or contact_safety_relevant delta reruns canonical safety before routine triage resumes. | expired_rejected, superseded_duplicate, blocked_repair, or stale tuple opens same-shell recovery or ContactRouteRepairJourney; no detached link-expired success page. | phase-3-the-human-checkpoint.md#Build MoreInfoCycle as a first-class workflow object<br>phase-0-the-foundation-protocol.md#RouteIntentBinding |
| Message reply | Messages cluster / thread child route inside the patient shell. | RouteIntentBinding(actionScope = message_reply, governingObject = ClinicianMessageThread, thread version tuple, visibility tuple, lineage fence). | Patient reply appends to the current thread, then settles EvidenceAssimilationRecord and any ThreadResolutionGate update under the current thread tuple. | ThreadExpectationEnvelope.replyCapabilityState must be live and the current RouteIntentBinding, release tuple, and continuity evidence must still match. | Clinically material or contact-safety-relevant replies trigger MaterialDeltaAssessment and SafetyPreemptionRecord before routine review continues. | Delivery dispute, blocked visibility, stale thread version, or route repair keeps the same cluster shell open in repair or read-only posture. | callback-and-clinician-messaging-loop.md#Clinician message domain<br>patient-portal-experience-architecture-blueprint.md#Messages list and cluster shell |
| Callback response | Messages cluster callback child route or bounded callback-response entry in the same patient shell. | RouteIntentBinding(actionScope = callback_response, governingObject = CallbackCase, callback fence, current expectation envelope tuple). | The response settles against the active CallbackCase fence and updates CallbackOutcomeEvidenceBundle or CallbackResolutionGate rather than free-text local state. | CallbackExpectationEnvelope must still be current, writable, and bound to the same reachability and continuity tuple. | Any callback outcome that introduces clinically material evidence or contact-risk delta reruns canonical safety / reachability classification. | Missed window, route invalidation, or stale callback tuple falls to same-shell repair, retry, or read-only provenance. | callback-and-clinician-messaging-loop.md#Callback domain<br>patient-portal-experience-architecture-blueprint.md#Messages and callback continuity rules |
| Manage appointment | Appointments manage route inside the patient shell. | RouteIntentBinding(actionScope = appointment_manage_mutation, governingObject = AppointmentRecord or BookingCase manage tuple, confirmation truth projection, capability lease). | Cancel, reschedule, callback request, or details update must settle through CommandActionRecord and current manage capability lease; local acknowledgement is never authoritative. | BookingConfirmationTruthProjection must still permit manage posture and the capability set must be live, not stale, blocked, or expired. | Material symptom change, identity drift, or contact-risk change blocks ordinary manage posture and routes to review or repair. | pending_confirmation, read-only manage, or ContactRouteRepairJourney keeps the same appointment anchor pinned in shell. | phase-4-the-booking-engine.md#Manage appointment<br>patient-account-and-communications-blueprint.md#Core projections |
| Accept waitlist offer | Appointments waitlist offer card inside the same patient shell. | RouteIntentBinding(actionScope = waitlist_offer_response, governingObject = WaitlistOffer, continuationFenceEpoch, reservation truth tuple, waitlist truth tuple). | Offer acceptance mutates the current WaitlistOffer and BookingCase only if the same continuation fence, reservation truth, and deadline evaluation still match. | Current WaitlistContinuationTruthProjection must still show offer_available and the offer must not be expired or superseded. | If new evidence or contact-risk change threatens the live dependency, acceptance is blocked and the case reroutes to repair or review. | Expired, superseded, or fallback_due waitlist posture switches the same shell to callback or hub fallback instead of pretending the offer is still live. | phase-4-the-booking-engine.md#Waitlist continuation<br>phase-0-the-foundation-protocol.md#Waitlist continuity algorithm |
| Accept or decline network alternative | Patient appointments / request-detail hub alternative card, still inside the same patient shell. | RouteIntentBinding(actionScope = network_offer_response, governingObject = AlternativeOfferSession, offerFenceEpoch, truthTupleHash, visibleOfferSetHash). | Accept, decline, or callback-request settles against the live AlternativeOfferSession and HubOfferToConfirmationTruthProjection, not browser-local ranking state. | Access grant, subject binding, release tuple, offerFenceEpoch, and current truthTupleHash must all still match. | If clinical slack or newer evidence invalidates the current offer set, mutation freezes and the case regenerates or falls back before acceptance can continue. | Expired or superseded offer sets stay visible as read-only provenance while callback-only recovery or regenerated alternatives take over in shell. | phase-5-the-network-horizon.md#Alternative offers and patient choice<br>phase-5-the-network-horizon.md#No-slot handling, urgent bounce-back, callback fallback, and reopen mechanics |
| Pharmacy choice | Patient pharmacy child route (/pharmacy/:pharmacyCaseId/choose) inside the same patient shell. | RouteIntentBinding(actionScope = pharmacy_choice, governingObject = PharmacyChoiceSession, choice proof tuple, provider selection binding, lineage fence). | Patient selection commits against the current PharmacyChoiceSession, PharmacyChoiceProof, and selectionBindingHash; stale recommendations cannot mutate current provider choice. | Choice session, visible choice set hash, provider capability snapshot, and continuity tuple must still be current and unsuperseded. | Timing guardrail drift, new symptoms, or wrong-patient hold block selection and return the case to repair or clinician review. | Expired or superseded choice sets reopen same-shell re-selection or recovery; suppressed unsafe providers never become silently selectable. | phase-6-the-pharmacy-loop.md#Pharmacy contract, case model, and state machine<br>phase-0-the-foundation-protocol.md#Pharmacy choice, consent, dispatch, and reconciliation algorithm |
| Pharmacy consent | Patient pharmacy child route (/pharmacy/:pharmacyCaseId/instructions or /status) inside the same patient shell. | RouteIntentBinding(actionScope = pharmacy_consent, governingObject = PharmacyConsentCheckpoint, selected provider tuple, pathway, scope, package fingerprint, lineage fence). | Consent capture or renewal creates PharmacyConsentRecord and refreshes PharmacyConsentCheckpoint; dispatch is illegal until the refreshed checkpoint is satisfied. | Current provider, pathway, scope, package fingerprint, and selection binding must still match the latest consent checkpoint and choice proof. | Material evidence drift, identity repair, or reachability failure freezes calm instructions and reroutes the shell to renewal or repair. | renewal_required, withdrawn, revoked_post_dispatch, or recovery_required posture stays same-shell and blocks dispatch until revalidated. | phase-6-the-pharmacy-loop.md#Pharmacy contract, case model, and state machine<br>phase-0-the-foundation-protocol.md#Pharmacy consent checkpoint |
| Contact-route repair | Same current patient shell through ContactRouteRepairJourney. | RouteIntentBinding(actionScope = contact_route_repair, governingObject = ContactRouteRepairJourney, bound ReachabilityDependency, reachabilityEpoch, parent route tuple). | Repair updates the governed contact route and verification checkpoint for the live dependency instead of writing profile state as if the blocked action had already succeeded. | The current ReachabilityAssessmentRecord must still show blocked, disputed, or stale posture for the same dependency and reachabilityEpoch. | A dependency-triggered contact change is contact_safety_relevant whenever it threatens callback, message, waitlist, pharmacy, or other active continuity promises. | The blocked action summary remains pinned in shell until ContactRouteVerificationCheckpoint.rebindState = rebound and reachability clears. | phase-0-the-foundation-protocol.md#Reachability repair algorithm<br>patient-portal-experience-architecture-blueprint.md#Messages and callback continuity rules |
| Identity correction or recovery | Same current patient shell via PatientIdentityHoldProjection, or secure-link recovery when the current shell can no longer remain writable. | RouteIntentBinding(actionScope = identity_correction_or_claim_resume, governingObject = IdentityRepairCase, frozenIdentityBindingRef, subjectBindingVersion, lineage fence). | Correction, claim, or release proceeds only through IdentityRepairCase, IdentityBinding, and IdentityRepairReleaseSettlement; patientRef is never overwritten in place. | The current active IdentityRepairCase and binding tuple must still match; stale grant redemption cannot bypass identity hold. | While identity repair is active, PHI-bearing actions remain blocked and any new evidence is classified against the frozen binding until release or compensation. | Same-shell identity-hold messaging, grant rotation, and bounded recovery persist until IdentityRepairReleaseSettlement attests the corrected lineage. | phase-0-the-foundation-protocol.md#1.5 IdentityRepairCase<br>blueprint-init.md#10. Identity, consent, security, and policy |
