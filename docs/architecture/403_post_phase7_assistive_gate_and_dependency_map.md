# 403 Post-Phase-7 Assistive Gate And Dependency Map

## Hard Verdict

`open_phase8_now`

The Phase 8 assistive foundation wave may start now for tracks `404` through `409` only. This is not broad visible rollout approval. It opens contract freeze, protected evaluation, transcript normalization, documentation composer, and bounded suggestion backend work under local non-production and shadow-first assumptions.

The verdict consumes the approved Phase 7 exit gate in `data/contracts/402_phase7_exit_verdict.json`. Phase 7 has no blocking refs and no carry-forward refs. Its six Phase 8 launch conditions are interpreted as hard inherited controls rather than optional context.

## Dependency Spine

| Source | Phase 8 inheritance |
| --- | --- |
| `LC402_001` one-portal shell and accessibility coverage | Assistive work must not fork patient shells or lose semantic coverage. |
| `LC402_002` release controls | Assistive work remains under route freezes, cohort gates, kill switches, and rollback posture. |
| `LC402_003` identity and route continuity | Assistive context may bind only through current route, return, SSO, and deep-link law. |
| `LC402_004` artifact law | Transcript, draft, export, and replay artifacts stay summary-first and handoff-governed. |
| `LC402_005` telemetry disclosure | Shadow, evaluation, and assistive telemetry must be redacted and privacy-minimized. |
| `LC402_006` support and governance traceability | Operator surfaces must link to support, audit, and what-the-patient-saw truth. |

## Immediate Open Tracks

| Track | Owner lane | Boundary |
| --- | --- | --- |
| `par_404` | `assistive_evaluation_contracts` | Freeze evaluation corpus, label, adjudication, shadow evidence, and feedback eligibility law. |
| `par_405` | `assistive_release_safety_and_regulatory_control` | Freeze release-candidate, safety-case, regulatory-trigger, approval, rollback, and freeze law. |
| `par_406` | `evaluation_runtime` | Build protected evaluation, replay, label, adjudication, and shadow dataset runtime. |
| `par_407` | `audio_transcript_artifact_pipeline` | Build conservative transcript and artifact normalization pipeline. |
| `par_408` | `documentation_composer` | Build typed, evidence-mapped, abstention-capable draft artifact backend. |
| `par_409` | `bounded_suggestion_orchestrator` | Build review-only risk, question, endpoint hypothesis, and abstention backend. |

These tracks may create contracts, local non-production backend implementations, shadow evidence paths, and internal artifacts. They may not open patient-visible AI output, autonomous writeback, visible staff assistive controls, or regulatory submission.

## Withheld Tracks

Tracks `410`, `411`, `412`, `413`, `416`, `417`, `418`, `419`, and `421` are blocked. Tracks `414`, `415`, and `420` are deferred. The most important blockers are:

- `par_410` needs the concrete 404-409 object families before invocation grants and composition law can be executable.
- `par_411` needs `410` before trust-envelope projection can become authoritative.
- `par_412`, `par_418`, and `par_419` need trust envelopes and leases before visible staff controls can exist.
- `par_417` needs release-candidate law, replayable inference evidence, and freeze disposition before assurance evidence can be automated.

## Interface Map

The canonical machine-readable interface map is `data/contracts/403_phase8_dependency_interface_map.yaml`. It identifies each track's owner lane, produced interfaces, consumers, blockers, and Phase 9 reserve refs. Later tasks must use that file rather than rediscovering boundaries from prose.

## Phase 9 Reserve

Phase 9 remains reserved, not open. The reserve ledger is `data/contracts/403_phase9_dependency_reserve.json`. Its rows require Phase 8 outputs for trust envelopes, rollout ladders, human approval proof, telemetry evidence, artifact handoff, freeze posture, clinical safety evidence, and replay or restore readiness.

## Boundary Rules

- A model response is never enough to render visible assistive output.
- Human-authored final artifacts remain the only authoritative closure, endpoint, booking, pharmacy, escalation, or record truth.
- Feedback cannot become training-eligible until final human settlement, exclusion checks, and revocation law are implemented.
- DTAC, DCB0129, DCB0160, IM1 RFC, DPIA, and medical-device posture remain evidence gates, not release decoration.
