# 261 Changed Review Resume Accessibility Notes

- Keep the route inside the existing semantic shell: header, navigation, main, and stable section headings.
- The changed lane, delta summary, compare block, and resume gate must all be keyboard reachable in one tab flow.
- Visible focus remains required on filter chips, changed rows, marker jumps, compare toggle, and dominant gate action.
- `data-delta-class` styling may not rely on colour alone; each changed state also includes words and grouped content.
- Superseded context remains collapsed by default, but the expand control is explicit and operable.
- Reduced motion keeps the same layout truth and markers while suppressing non-essential animation.
- Long changed lists and filter sets still need sensible focus order and visible state.
