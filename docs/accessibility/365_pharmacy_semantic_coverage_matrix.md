# 365 Pharmacy Semantic Coverage Matrix

| Surface | Landmark / semantic contract | Announcement posture | Focus return |
| --- | --- | --- | --- |
| Patient pharmacy shell | `banner`, same-shell `main`, anchor `aside`, route `nav` | polite route summary, assertive only on repair/recovery | route buttons remain in order |
| Patient chooser warned choice | inline acknowledgement group adjacent to selected provider | polite acknowledgement status | returns to warned choice controls |
| Pharmacy console shell | `banner`, same-shell `main`, queue/support/decision regions | polite route summary, assertive only on blocked recovery | route toggles remain in shell order |
| Queue peek drawer | modal dialog semantics inside mission stack | polite entry only | Escape returns to queue toggle |
| Referral confirmation drawer | modal dialog semantics on dispatch proof route | polite entry only | Escape returns to drawer trigger |
| Status and anchor badges | context-labeled badges instead of anonymous pills | no live output | n/a |
