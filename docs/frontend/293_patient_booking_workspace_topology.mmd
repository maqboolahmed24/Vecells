flowchart LR
  home["/home"]
  requests["/requests"]
  appointments["/appointments"]
  booking["/bookings/:bookingCaseId"]
  select["/bookings/:bookingCaseId/select"]
  confirm["/bookings/:bookingCaseId/confirm"]
  manage["Future manage host"]
  waitlist["Future waitlist host"]
  return["PatientNavReturnContract"]
  continuity["PatientPortalContinuityEvidenceBundle"]
  capability["BookingCapabilityProjection"]

  home --> booking
  requests --> booking
  appointments --> booking

  booking --> select
  select --> confirm
  confirm --> manage
  confirm --> waitlist

  return -.preserves origin and anchor.-> booking
  continuity -.preserves shell or freezes in place.-> booking
  continuity -.selected anchor survives child-route travel.-> select
  continuity -.selected anchor survives child-route travel.-> confirm
  capability -.dominant action authority.-> booking
  capability -.dominant action authority.-> select
  capability -.dominant action authority.-> confirm
