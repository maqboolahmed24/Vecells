# Project Audit

Audit date: 2026-04-28.

Scope: internal Render deployment readiness, GitHub branch plan, local database/state review, security for nontechnical testers, and validation cleanup.

## Repository State

- Current working branch: `dev`
- Remote: `https://github.com/maqboolahmed24/Vecells.git`
- `dev` tracks `origin/dev` at `f488ecd`
- local `main` is behind `origin/main` by 3 commits
- `origin/main...origin/dev` divergence: `1 4`
- local `main...dev` divergence: `0 6`
- Dirty worktree at the time of this audit: 101 modified/staged tracked files and 3372 untracked paths

Deployment implication: do not switch branches, merge, or deploy from `main` until the current working tree is preserved. The deployment branch is supposed to be `main`, but the usable work is currently on `dev` plus a large dirty workspace.

## Monorepo Shape

Root project:

- Name: `vecells-phase0-foundation`
- Package manager: `pnpm@10.23.0`
- Build system: Nx
- Root scripts:
  - `build`: `nx run-many -t build --all`
  - `typecheck`: `nx run-many -t typecheck --all`
  - `test`: `nx run-many -t test --all`
  - `dev`: runs seven apps plus five services in parallel
- Root Node engine: not set in `package.json`
- CI workflows use Node 24 and pnpm 10.23.0

No deployment files currently exist at the repo root:

- No `render.yaml`
- No root `Dockerfile`
- No `.nvmrc`
- No `.node-version`

## Frontend Apps

All primary apps are Vite/React apps with `build`, `dev`, `preview`, `test`, and `typecheck` scripts.

| App | Nx project | Dev port | Preview port | Context |
| --- | --- | ---: | ---: | --- |
| `apps/patient-web` | `app-patient-web` | 4300 | 4400 | patient experience |
| `apps/clinical-workspace` | `app-clinical-workspace` | 4301 | 4401 | clinical workspace |
| `apps/ops-console` | `app-ops-console` | 4302 | 4402 | operations |
| `apps/hub-desk` | `app-hub-desk` | 4303 | 4403 | hub coordination |
| `apps/pharmacy-console` | `app-pharmacy-console` | 4304 | 4404 | pharmacy |
| `apps/support-workspace` | `app-support-workspace` | 4305 | 4405 | support |
| `apps/governance-console` | `app-governance-console` | 4306 | 4406 | governance |

Current Vite configs bind local dev/preview to `127.0.0.1`. Static builds are fine for static hosting, but Vite preview as a Render service would need host/port changes.

## Runtime Services

| Service | Nx project | Default service port | Default admin port | Current bind concern |
| --- | --- | ---: | ---: | --- |
| `services/api-gateway` | `service-api-gateway` | 7100 | 7200 | binds `127.0.0.1` |
| `services/command-api` | `service-command-api` | 7101 | 7201 | binds `127.0.0.1` |
| `services/projection-worker` | `service-projection-worker` | 7102 | 7202 | binds `127.0.0.1` |
| `services/notification-worker` | `service-notification-worker` | 7103 | 7203 | binds `127.0.0.1` |
| `services/adapter-simulators` | `service-adapter-simulators` | 7104 | 7204 in env example | code binds `127.0.0.1`; compose sets `HOST=0.0.0.0` but code does not read it |

Render web services must bind public HTTP to `0.0.0.0` and should use `PORT`. The current service bind code is a real deployment blocker.

## Local State And Emulators

Local emulators exist under `infra/`:

- `infra/data-storage/local/data-storage-emulator.compose.yaml`
  - Postgres 16 domain store: `vecells_domain`, exposed locally as `6543:5432`
  - Postgres 16 FHIR store: `vecells_fhir`, exposed locally as `6544:5432`
- `infra/event-spine/local/event-spine-emulator.compose.yaml`
  - NATS JetStream on `4222`, monitoring on `8222`
- `infra/cache-live-transport/local/cache-live-transport-emulator.compose.yaml`
  - Valkey on `16379`
  - NATS on `14222`
- `infra/object-storage/local/object-storage-emulator.compose.yaml`
  - MinIO on `9000`, console on `9001`
- `services/adapter-simulators/manifests/docker-compose.yaml`
  - Adapter simulator service only

The current app/service runtime is still heavily fixture/in-memory/local-emulator oriented. I did not find a production-ready `DATABASE_URL` wiring path that would make the whole product durable simply by creating a Render database.

## Environment Inventory

`.env.example` includes:

- `NODE_ENV`
- `VECELLS_ENVIRONMENT`
- app dev/preview ports
- service/admin ports for API gateway, command API, projection worker, notification worker, adapter simulators
- `ENABLE_ASSISTIVE_CONTROL_LAB`
- `RELEASE_RING`
- `OBSERVABILITY_SAMPLE_RATE`

Additional secret reference names appear in source and generated manifests, including:

- `AUTH_EDGE_SESSION_SECRET_REF`
- `AUTH_EDGE_SIGNING_KEY_REF`
- `COMMAND_IDEMPOTENCY_STORE_REF`
- `COMMAND_MUTATION_GATE_SECRET_REF`
- `TELEPHONY_WEBHOOK_SECRET_REF`
- `NOTIFICATION_WEBHOOK_SECRET_REF`
- `EVIDENCE_WEBHOOK_SECRET_REF`

User instruction says no secrets are available now. Therefore every real secret must be generated by Render or entered manually in the Render Dashboard later; none should be committed.

## CI/GitHub

Workflow files:

- `.github/workflows/build-provenance-ci.yml`
- `.github/workflows/nonprod-provenance-promotion.yml`

CI uses:

- `actions/checkout@v4`
- `pnpm/action-setup@v4` with pnpm `10.23.0`
- `actions/setup-node@v4` with Node `24`
- `pnpm install --frozen-lockfile`

Important script chain:

- `ci:foundation-gates` runs `pnpm build && pnpm check`
- `check` expands into lint, format check, typecheck, test, and many validation scripts

Deployment implication: local fast gates pass, but the full GitHub workflow is heavier than the gates run in this audit.

## Validation Performed

Passed after this pass:

- `pnpm install --frozen-lockfile`
- `pnpm --dir apps/patient-web typecheck`
- `pnpm --dir tests/playwright build`
- `node tests/playwright/155_patient_intake_mission_frame.spec.js --run`
- `pnpm --dir tests/playwright test`
- `NX_TUI=false pnpm test`
- `NX_TUI=false pnpm typecheck`
- `NX_TUI=false pnpm build`

Build warnings observed:

- Vite reports large chunks over 500 kB for multiple apps. This is not a functional blocker for internal testing, but it should be tracked before a public launch.

## Blockers Fixed During This Pass

The repo had real validation failures before the documentation pass. Fixes made:

- Resolved conflicting exports in `packages/domains/assistive_evaluation/src/index.ts`.
- Fixed empty-data fallbacks in governance evidence vault models/views.
- Fixed mutable projection handling in ops go-live and wave promotion models.
- Updated event contract active schema count from 192 to 193 in code and generated data.
- Stabilized command API appointment replacement tests by using explicit timestamps.
- Preserved pharmacy confirmation gates across outcome capture so close behavior remains blocked while gates are open.
- Stabilized staff shell Playwright focus behavior.
- Added an opt-in patient shell seed harness for old seed route smoke tests without changing normal patient `/home` behavior.
- Made patient mission-frame keyboard focus deterministic and updated the mission-frame spec to follow the real progressive details flow.

These are code/test fixes, not Render deployment implementation.

## Deployment Readiness Summary

Ready:

- Dependency install is healthy.
- Typecheck, build, and tests pass after fixes.
- CI structure exists.
- Git remote exists and is GitHub-backed.
- Internal deployment can use synthetic/local-mode behavior if the team accepts limited persistence.

Not ready:

- `main` is not yet updated with `dev`.
- Worktree is dirty and must be preserved before branch work.
- No Render Blueprint exists yet.
- Runtime services bind `127.0.0.1`.
- Root Node version is not pinned for Render.
- Durable database/storage wiring is not deployment-complete.
- Internal access gate does not yet exist.

