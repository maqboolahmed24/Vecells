{
  "name": "@vecells/hub-supplier-mirror",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Hub supplier mirror worker surface for monotone observation ingestion and drift checkpoints.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vecells/domain-hub-coordination": "workspace:*"
  }
}
