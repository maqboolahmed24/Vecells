{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "service-notification-worker",
  "sourceRoot": "services/notification-worker/src",
  "projectType": "application",
  "tags": ["artifact:service", "context:platform_integration", "class:service_runtime"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} lint"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} typecheck"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} dev"
      }
    }
  }
}
