{
  "name": "@vecells/hub-exception-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Hub exception worker surface for typed retry, escalation, and suppression state.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vecells/domain-hub-coordination": "workspace:*"
  }
}
