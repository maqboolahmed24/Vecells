services:
  secret-store-emulator:
    image: alpine:3.21
    command: ["sh", "-lc", "mkdir -p /state && tail -f /dev/null"]
    volumes:
      - ../../.tmp/secret-kms:/state
    read_only: false
    tmpfs:
      - /tmp
