# Webview-Safe Artifact Preview Flows

Use this prompt to direct an autonomous AI agent:

## Prompt
You are a principal systems architect and autonomous remediation agent responsible for fully resolving `Webview-Safe Artifact Preview Flows`.

Map this domain to `ArtifactPresentationContract`, `ArtifactSurfaceContext`, `ArtifactFallbackDisposition`, `ArtifactParityDigest`, `BinaryArtifactDelivery`, `ArtifactByteGrant`, `ArtifactStage`, `OutboundNavigationGrant`, `BridgeCapabilityMatrix`, `PatientRecordArtifactProjection`, `GovernanceEvidencePackArtifact`, and `RecoveryEvidenceArtifact`. Your mission is to fully resolve this failure class. Identify and eliminate every place where artifact preview, print, download, and handoff flows still assume a full browser, bypass summary-first rendering, or lose same-shell continuity under constrained channels such as NHS App webview.

Execution mandate:
- Do not stop at diagnosis, observation, or recommendation.
- Read the full algorithm, end-to-end lifecycle, and governing contracts before changing the system.
- Then synthesize the strongest viable remediation and implement it directly when you have write access, using refactoring, schema changes, migration work, and contract hardening where needed.
- When you have write access, deliver patches, refactors, migrations, and contract changes rather than stopping at a change proposal.
- Prefer durable elimination of the failure class over local patches, UI workarounds, extra flags, or operator vigilance.
- Carry the remediation through implementation, verification, rollout or backfill handling, and proof that the unsafe path is closed.

Work method:
- Read the artifact, presentation, parity, NHS App webview, and external-handoff rules before making changes.
- Distinguish summary-first artifact posture, verified preview parity, governed download or print posture, external handoff posture, and in-place fallback or recovery.
- Trace artifact access from source object through preview, parity validation, byte delivery, overlay or browser handoff, and return-safe re-entry.
- Inspect how grants, parity state, visibility tier, summary safety tier, channel capability, and current continuity evidence constrain what artifact mode may remain live.
- Move from diagnosis into implementation as soon as the governing model is clear; do not leave the task as analysis-only commentary.

Required remediation work:
- Convert the discovered failure modes into a concrete target design and implement that design across the affected layers.
- Remove or refactor the unsafe paths so the system no longer depends on operator vigilance, local convention, or lucky sequencing.
- For each unsafe condition, carry the fix through the owning algorithm, contracts, storage, integrations, and user-facing state until the failure is removed or explicitly governed.
- Find artifact routes that still expose raw bytes, detached print pages, or direct browser or overlay URLs without one active `ArtifactPresentationContract` and `OutboundNavigationGrant`.
- Detect preview surfaces that continue to imply full-fidelity artifact truth after parity, grant validity, publication, or channel viability has drifted.
- Surface constrained-channel flows that attempt unsupported print, oversized download, or browser-only preview instead of falling back to same-shell summary or governed recovery.
- Examine whether patient, governance, audit, and resilience artifact surfaces all preserve their selected anchor, return intent, and masking or redaction posture consistently during handoff.
- Identify where summary-first rendering is bypassed and the user is forced into export- or download-first journeys that break continuity or fail poorly inside webview constraints.

Implementation posture:
- Choose the remediation that definitively closes the failure class, even if that requires non-trivial refactoring.
- Implement the end-state architecture and algorithm, not just the analysis of it.
- Prefer deleting, consolidating, or hardening weak paths over documenting them or layering more conditional logic on top.
- Do not treat this as a file-download compatibility bug class.
- Prefer one artifact-mode system where preview, parity, transfer, and fallback all resolve from the same typed artifact surface context.
- If constrained channels currently handle artifacts through route-local workarounds, redesign them around summary-first presentation and governed transfer posture.
- Ensure every failed or unsupported artifact action degrades in place with the last safe summary still visible.

Produce:
1. A concise description of the current failure mode, its blast radius, and the target end-state.
2. The exact algorithmic, architectural, schema, contract, runtime, and interface changes required to eliminate the issue.
3. The concrete code, migration, configuration, and data-shape changes you will apply, or an exact executable patch plan if the environment is truly read-only.
4. The invariants, state-transition rules, guardrails, and recovery semantics enforced by the new design.
5. The verification suite needed to prove the remediation, including unit, integration, contract, replay, recovery, concurrency, and end-to-end coverage as applicable.
6. The rollout, backfill, reindex, compatibility, and operational validation plan required to land the fix safely.
7. Residual risks only if they remain after implementation, plus the exact evidence required to close them.

Optimize for definitive elimination of `Webview-Safe Artifact Preview Flows` through coherent algorithmic and architectural remediation, with durable invariants, safe rollout, and proof-backed correctness. The task is not complete at diagnosis; finish only when the strongest viable remediation is implemented and proven, or reduced to an exact executable change set for a truly read-only environment.
