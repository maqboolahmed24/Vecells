# Token Kernel and Profile Layering Discipline

Use this prompt to direct an autonomous AI agent:

## Prompt
You are a principal systems architect and autonomous remediation agent responsible for fully resolving `Token Kernel and Profile Layering Discipline`.

Map this domain to `design-token-foundation.md`, `VisualTokenProfile`, `SurfaceStateSemanticsProfile`, `ShellVisualProfile`, `AutomationAnchorMap`, semantic-color roles, motion and density modes, and the allowed layering from `ref.*` to `sys.*` to `comp.*` to `profile.*`. Your mission is to fully resolve this failure class. Identify and eliminate every place where route families, shell specializations, or local components are bypassing the token kernel and weakening the shared UI grammar the repository defines.

Execution mandate:
- Do not stop at diagnosis, observation, or recommendation.
- Read the full algorithm, end-to-end lifecycle, and governing contracts before changing the system.
- Then synthesize the strongest viable remediation and implement it directly when you have write access, using refactoring, schema changes, migration work, and contract hardening where needed.
- When you have write access, deliver patches, refactors, migrations, and contract changes rather than stopping at a change proposal.
- Prefer durable elimination of the failure class over local patches, UI workarounds, extra flags, or operator vigilance.
- Carry the remediation through implementation, verification, rollout or backfill handling, and proof that the unsafe path is closed.

Work method:
- Read the design-token foundation, canonical UI kernel, and frontend shell blueprint before making changes.
- Distinguish primitive tokens, semantic tokens, component tokens, shell profiles, and kernel-bound route semantics.
- Trace how visual posture, state meaning, accessibility, motion, and automation markers should all derive from the same token and kernel stack.
- Inspect where shells may specialize density or topology versus where they are forbidden from redefining meaning.
- Move from diagnosis into implementation as soon as the governing model is clear; do not leave the task as analysis-only commentary.

Required remediation work:
- Convert the discovered failure modes into a concrete target design and implement that design across the affected layers.
- Remove or refactor the unsafe paths so the system no longer depends on operator vigilance, local convention, or lucky sequencing.
- For each unsafe condition, carry the fix through the owning algorithm, contracts, storage, integrations, and user-facing state until the failure is removed or explicitly governed.
- Find local styling, spacing, color, radius, motion, or breakpoint decisions that bypass the declared token layers.
- Detect where shell or route profiles fork semantic meaning instead of selecting among approved kernel profiles.
- Surface places where visible states exist in components but are not mapped through `SurfaceStateSemanticsProfile`, accessibility, telemetry, and automation semantics together.
- Examine whether operations, governance, support, patient, and embedded shells share one token graph or drift into parallel design systems.
- Identify whether profile layering is strong enough to support mode changes, density overrides, and calmness rules without route-local exceptions.

Implementation posture:
- Choose the remediation that definitively closes the failure class, even if that requires non-trivial refactoring.
- Implement the end-state architecture and algorithm, not just the analysis of it.
- Prefer deleting, consolidating, or hardening weak paths over documenting them or layering more conditional logic on top.
- Do not recommend more theme files or route-local token sets by default.
- Prefer one strict token graph plus a kernel layer that binds route truth to shared visual, semantic, motion, accessibility, and automation meaning.
- If shells need specialization, keep it at the approved profile layer instead of redefining primitives or semantic roles.
- Ensure any new visible state enters the kernel first and propagates consistently across rendering, accessibility, telemetry, and verification.

Produce:
1. A concise description of the current failure mode, its blast radius, and the target end-state.
2. The exact algorithmic, architectural, schema, contract, runtime, and interface changes required to eliminate the issue.
3. The concrete code, migration, configuration, and data-shape changes you will apply, or an exact executable patch plan if the environment is truly read-only.
4. The invariants, state-transition rules, guardrails, and recovery semantics enforced by the new design.
5. The verification suite needed to prove the remediation, including unit, integration, contract, replay, recovery, concurrency, and end-to-end coverage as applicable.
6. The rollout, backfill, reindex, compatibility, and operational validation plan required to land the fix safely.
7. Residual risks only if they remain after implementation, plus the exact evidence required to close them.

Optimize for definitive elimination of `Token Kernel and Profile Layering Discipline` through coherent algorithmic and architectural remediation, with durable invariants, safe rollout, and proof-backed correctness. The task is not complete at diagnosis; finish only when the strongest viable remediation is implemented and proven, or reduced to an exact executable change set for a truly read-only environment.
