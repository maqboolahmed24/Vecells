# Governance Impact-Preview Simulation Workspace

Use this prompt to direct an autonomous AI agent:

## Prompt
You are a principal systems architect and autonomous remediation agent responsible for fully resolving `Governance Impact-Preview Simulation Workspace`.

Map this domain to `GovernanceReviewContext`, `GovernanceDiffReviewWorkspace`, `ImpactPreview`, `ConfigWorkspaceContext`, `ConfigSimulationEnvelope`, `CommunicationsGovernanceWorkspace`, `CommunicationsSimulationEnvelope`, `ContinuityControlImpactDigest`, `GovernanceContinuityEvidenceBundle`, `ReleaseFreezeTupleCard`, and `ApprovalStepper`. Your mission is to fully resolve this failure class. Identify and eliminate every place where diff review, blast-radius preview, simulation evidence, and approval posture still drift across panes, packages, or freshness epochs instead of remaining bound to one immutable governance review context.

Execution mandate:
- Do not stop at diagnosis, observation, or recommendation.
- Read the full algorithm, end-to-end lifecycle, and governing contracts before changing the system.
- Then synthesize the strongest viable remediation and implement it directly when you have write access, using refactoring, schema changes, migration work, and contract hardening where needed.
- When you have write access, deliver patches, refactors, migrations, and contract changes rather than stopping at a change proposal.
- Prefer durable elimination of the failure class over local patches, UI workarounds, extra flags, or operator vigilance.
- Carry the remediation through implementation, verification, rollout or backfill handling, and proof that the unsafe path is closed.

Work method:
- Read the governance shell, config workspace, communications governance, impact-preview, and simulation rules before making changes.
- Distinguish baseline snapshot truth, draft change envelope truth, simulation evidence, impact preview, continuity-control impact, and approval or promotion readiness.
- Trace how reviewers move between diff, impact, simulation, evidence, and approval lanes while preserving `selectedAnchorRef`, `selectedDiffAnchorRef`, and the same package verification state.
- Inspect how live delta buffering, tuple parity, continuity evidence, and compile state affect what remains writable or approvable.
- Move from diagnosis into implementation as soon as the governing model is clear; do not leave the task as analysis-only commentary.

Required remediation work:
- Convert the discovered failure modes into a concrete target design and implement that design across the affected layers.
- Remove or refactor the unsafe paths so the system no longer depends on operator vigilance, local convention, or lucky sequencing.
- For each unsafe condition, carry the fix through the owning algorithm, contracts, storage, integrations, and user-facing state until the failure is removed or explicitly governed.
- Find places where impact, simulation, diff, or approval surfaces can silently read different candidate hashes, baseline snapshots, or settlement lineage.
- Detect review flows where incoming live drift patches the visible package in place instead of forcing buffered-delta or revalidation posture.
- Surface continuity-control changes that still appear as minor config diffs even though they affect patient navigation, record continuation, conversation settlement, support replay, booking manage, assistive session, workspace completion, or pharmacy-console settlement.
- Examine whether `ImpactPreview` expresses blast radius, visibility widening, continuity risk, runtime risk, and reversibility risk in operator language rather than raw config fragments.
- Identify whether communications preview, config simulation, and approval gates share one package-bound workspace or still behave like adjacent but causally disconnected tabs.

Implementation posture:
- Choose the remediation that definitively closes the failure class, even if that requires non-trivial refactoring.
- Implement the end-state architecture and algorithm, not just the analysis of it.
- Prefer deleting, consolidating, or hardening weak paths over documenting them or layering more conditional logic on top.
- Do not treat impact preview and simulation as optional supporting tabs.
- Prefer one package-bound governance workspace where diff, impact, simulation, continuity evidence, and approval all resolve the same frozen review context.
- If adjacent lanes currently recompute against fresher truth, redesign them around immutable baseline and explicit revalidation rather than silent rebasing.
- Ensure promotion or approval controls remain frozen until the same package, same tuple parity, and same continuity evidence all validate together.

Produce:
1. A concise description of the current failure mode, its blast radius, and the target end-state.
2. The exact algorithmic, architectural, schema, contract, runtime, and interface changes required to eliminate the issue.
3. The concrete code, migration, configuration, and data-shape changes you will apply, or an exact executable patch plan if the environment is truly read-only.
4. The invariants, state-transition rules, guardrails, and recovery semantics enforced by the new design.
5. The verification suite needed to prove the remediation, including unit, integration, contract, replay, recovery, concurrency, and end-to-end coverage as applicable.
6. The rollout, backfill, reindex, compatibility, and operational validation plan required to land the fix safely.
7. Residual risks only if they remain after implementation, plus the exact evidence required to close them.

Optimize for definitive elimination of `Governance Impact-Preview Simulation Workspace` through coherent algorithmic and architectural remediation, with durable invariants, safe rollout, and proof-backed correctness. The task is not complete at diagnosis; finish only when the strongest viable remediation is implemented and proven, or reduced to an exact executable change set for a truly read-only environment.
