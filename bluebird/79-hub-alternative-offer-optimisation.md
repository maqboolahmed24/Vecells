# Hub Alternative Offer Optimisation

Use this prompt to direct an autonomous AI agent:

## Prompt
You are a principal systems architect and autonomous remediation agent responsible for fully resolving `Hub Alternative Offer Optimisation`.

Map this domain to `AlternativeOfferSession`, `HubCoordinationCase`, `CrossSiteDecisionPlan`, `NetworkSlotCandidate`, `robustFit`, `coverage(A_c)`, `redundancy(A_c)`, `AccessGrant`, `TransitionEnvelope`, `releaseRecoveryDispositionRef`, and the governed callback fallback path. Your mission is to fully resolve this failure class. Identify and eliminate every place where hub alternative offers are still assembled as naive top-`K` lists, funnel patients toward one path, or lose same-shell truth under expiry, drift, or regenerated supply.

Execution mandate:
- Do not stop at diagnosis, observation, or recommendation.
- Read the full algorithm, end-to-end lifecycle, and governing contracts before changing the system.
- Then synthesize the strongest viable remediation and implement it directly when you have write access, using refactoring, schema changes, migration work, and contract hardening where needed.
- When you have write access, deliver patches, refactors, migrations, and contract changes rather than stopping at a change proposal.
- Prefer durable elimination of the failure class over local patches, UI workarounds, extra flags, or operator vigilance.
- Carry the remediation through implementation, verification, rollout or backfill handling, and proof that the unsafe path is closed.

Work method:
- Read the network horizon, alternative-offer, patient-choice, secure-link, and same-shell recovery rules before making changes.
- Distinguish the clinically acceptable frontier, the optimized diverse offer set `A_c`, the stable patient-visible order, the separate callback fallback card, and the current recovery posture of the offer session.
- Trace how offer sets are solved, sessionized, delivered, opened, accepted, declined, expired, and regenerated.
- Inspect how access grants, lineage fences, selected anchors, embedded posture, and continuity evidence are validated before live patient action remains available.
- Move from diagnosis into implementation as soon as the governing model is clear; do not leave the task as analysis-only commentary.

Required remediation work:
- Convert the discovered failure modes into a concrete target design and implement that design across the affected layers.
- Remove or refactor the unsafe paths so the system no longer depends on operator vigilance, local convention, or lucky sequencing.
- For each unsafe condition, carry the fix through the owning algorithm, contracts, storage, integrations, and user-facing state until the failure is removed or explicitly governed.
- Find offer flows that still package the next raw ranked candidates instead of a clinically safe and meaningfully diverse option set.
- Detect duplicate or redundant offer sets that over-represent one site, one capacity unit, or one bucket while hiding other safe alternatives.
- Surface recommendation behavior that auto-selects, renumbers, or visually funnels the patient away from the full offerable set.
- Examine whether callback is exposed as a separate fallback card rather than occupying ranked slot ordinals or suppressing slot choice visibility.
- Identify expiry, subject-binding drift, publication drift, embedded-rollout drift, or regenerated-offer cases that leave stale accept, decline, or callback actions live.

Implementation posture:
- Choose the remediation that definitively closes the failure class, even if that requires non-trivial refactoring.
- Implement the end-state architecture and algorithm, not just the analysis of it.
- Prefer deleting, consolidating, or hardening weak paths over documenting them or layering more conditional logic on top.
- Do not treat hub alternatives as a simple booking-results page.
- Prefer `AlternativeOfferSession` as a first-class recovery and coordination object with one optimized open-choice set and one explicit callback fallback path.
- If offer optimization, session recovery, and page rendering currently operate independently, redesign them around a single stable session contract.
- Ensure patient choice stays same-shell, mobile-first, and openly ranked without collapsing into a covert single-path funnel.

Produce:
1. A concise description of the current failure mode, its blast radius, and the target end-state.
2. The exact algorithmic, architectural, schema, contract, runtime, and interface changes required to eliminate the issue.
3. The concrete code, migration, configuration, and data-shape changes you will apply, or an exact executable patch plan if the environment is truly read-only.
4. The invariants, state-transition rules, guardrails, and recovery semantics enforced by the new design.
5. The verification suite needed to prove the remediation, including unit, integration, contract, replay, recovery, concurrency, and end-to-end coverage as applicable.
6. The rollout, backfill, reindex, compatibility, and operational validation plan required to land the fix safely.
7. Residual risks only if they remain after implementation, plus the exact evidence required to close them.

Optimize for definitive elimination of `Hub Alternative Offer Optimisation` through coherent algorithmic and architectural remediation, with durable invariants, safe rollout, and proof-backed correctness. The task is not complete at diagnosis; finish only when the strongest viable remediation is implemented and proven, or reduced to an exact executable change set for a truly read-only environment.
