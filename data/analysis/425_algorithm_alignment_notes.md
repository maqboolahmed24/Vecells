# 425 Algorithm Alignment Notes

## Phase 8 Alignment

The 425 setup keeps the assistive layer non-authoritative. Project and key readiness cannot prove clinical completion, patient reassurance, route closure, or release widening. It only proves that model-vendor references, service identities, scopes, and evidence handles are governed enough for dry-run, rehearsal, and verify modes.

## Existing Output Alignment

- 404 through 417 establish Phase 8 evaluation, release change control, transcript runtime, trust-envelope projection, monitoring, freeze disposition, and change-control evidence.
- 418 through 424 establish the assistive rail, note draft, confidence provenance, override reason surface, trust posture, stale recovery, and same-shell stage.
- 425 binds those controls to model-vendor onboarding without letting provider health or model output outrank `AssistiveCapabilityTrustEnvelope`, `AssistiveCapabilityRolloutVerdict`, or supplier-assurance freshness.

## Readiness Algorithm

1. Detect the configured provider from repository evidence before browser automation.
2. Resolve the environment registry and reject production labels.
3. Validate provider selection is unambiguous.
4. Validate project identity posture and block live external placeholders.
5. Validate service identity member bindings, owner roles, and non-wildcard scopes.
6. Validate key references use managed locators only.
7. Recompute masked fingerprints from the reference handle.
8. Emit machine-readable readiness rows.
9. Block apply unless a future selected-provider artifact carries named approval, intended-use review, subprocessor review, rollback rehearsal, and mutation approval.

## Interface Gap

No new interface gap file is required for this task because the existing dependency inventory already names the assistive model-vendor family, secret capture checklist row, watch-only posture, and simulator replacement posture. The missing live-provider choice is intentional scope, not a repo seam failure.
