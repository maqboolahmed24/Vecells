# 424 Algorithm Alignment Notes

Task `par_424` integrates the assistive layer as `Assistive_Same_Shell_Stage`.

The implementation follows the local source-of-truth order:

- Phase 8 requires visible assistive artifacts to bind to the active shell, current `WorkspaceTrustEnvelope`, current `AssistiveCapabilityTrustEnvelope`, provenance, confidence, feedback, and freeze posture before they render beyond shadow awareness.
- The staff workspace architecture defines `AssistiveWorkspaceStage`, `AssistiveWorkspaceStageBinding`, and `AssistiveSummaryStub` as same-shell support regions that must not outrank the case canvas or `DecisionDock`.
- The platform frontend blueprint's `AttentionBudget` rule is represented by `AssistiveAttentionBudgetCoordinator`; the stage records the support-region slot, promotion state, primary canvas floor, and promotion blocker.
- Phase 3 and Phase 0 continuity law are represented by the anchor sync bridge, queue context pocket, and DecisionDock coexistence frame.

Implemented closures:

1. The stage is routed through `staff-shell-seed.tsx` and only replaces the fourth-column assistive rail when `assistiveStage` fixtures are active.
2. The active task shell, shared status strip, task canvas, selected anchor, and `DecisionDock` remain in the canonical `PersistentShell`.
3. The promoted stage is a labelled complementary support region, not a modal and not a second workspace.
4. The folded state uses the same stage binding and renders as a tabbed support panel below the split threshold.
5. Pinning is disabled when trust downgrades; pinned state cannot widen actionability.
6. Existing 418 to 423 assistive components remain available through their validated contracts and are summarized as provenance, draft, override, trust, and stale-recovery rows inside the stage.

The static 403 readiness registry does not yet list `par_424`, so `data/analysis/PHASE8_BATCH_420_427_INTERFACE_GAP_SAME_SHELL_ASSISTIVE_STAGE.json` records the narrow launch-packet gap.
