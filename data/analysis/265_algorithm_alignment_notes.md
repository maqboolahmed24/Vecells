# 265 Algorithm Alignment Notes

- `SelfCareBoundaryDecision` is the top-level mode switch. The UI never reclassifies work by wording, channel, or transport status.
- `AdviceRenderSettlement` controls self-care issue posture. Only `renderable` keeps the self-care action live.
- `AdviceAdminDependencySet` is the blocker authority. Hidden-dependency-blocker gap is closed by making dominant blockers primary content.
- `AdminResolutionSettlement` governs waiting, completed, reopened, blocked, and stale-recoverable admin states.
- `PatientExpectationTemplate` stays tuple-aligned with the current boundary tuple and delivery mode.
- wording-reclassifies-work gap: closed by separate self-care and admin stage layouts, accents, and copy rules.
- hidden-dependency-blocker gap: closed by `AdminDependencyPanel` and dominant blocker rendering.
- stale-boundary-send gap: closed by `BoundaryDriftRecovery` and frozen action controls with preserved draft or subtype context.
- patient-staff-mismatch gap: closed by keeping the patient expectation preview derived from the same boundary tuple and settlement.
- detached-success-gap: closed by leaving completion and waiting settlement inside the same shell instead of a separate success page.
- same-shell stale-freeze law from the workspace architecture remains authoritative for reopen, trust degradation, and replacement tuple recovery.
