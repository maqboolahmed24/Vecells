# 270 Algorithm Alignment Notes

## Resolved

- queue preview no longer invents callback or admin posture from `StaffQueueCase` alone
- callback repair can launch the callback desk in the repair stage from the queue preview
- bounded admin waiting now gates `CompletionContinuityStage` and `NextTaskPostureCard`

## Remaining

- live query consumption gap: the frontend helper mirrors the 270 contract, but it is not yet backed by a runtime fetch
- message-family scope gap: clinician-message repair stays outside the 270 dominant-family set by design
