{
  "schemaVersion": "379.phase7.sso-bridge.fixture.v1",
  "examples": [
    {
      "scenarioId": "silent_success_same_subject",
      "expected": {
        "grantState": "consumed",
        "transactionState": "verified",
        "bindingState": "matched",
        "mergeDecision": "reuse",
        "returnOutcome": "silent_success"
      }
    },
    {
      "scenarioId": "consent_denied",
      "expected": {
        "grantState": "denied",
        "transactionState": "denied",
        "returnOutcome": "consent_denied",
        "allowRetry": true
      }
    },
    {
      "scenarioId": "entry_grant_replay",
      "expected": {
        "returnOutcome": "safe_reentry_required",
        "evidenceRef": "SSOEntryGrant:already-consumed"
      }
    },
    {
      "scenarioId": "subject_mismatch_session_conflict",
      "expected": {
        "bindingState": "mismatched",
        "mergeDecision": "terminate_and_reenter",
        "returnOutcome": "session_conflict"
      }
    },
    {
      "scenarioId": "manifest_drift_recovery",
      "expected": {
        "returnOutcome": "manifest_drift",
        "patientRouteRef": "/nhs-app/recovery/manifest-drift"
      }
    }
  ]
}
