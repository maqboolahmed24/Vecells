{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vecells://contracts/BookingConfirmationTruthProjection",
  "title": "BookingConfirmationTruthProjection",
  "description": "Sole audience-safe authority for booking outcome posture.",
  "type": "object",
  "additionalProperties": false,
  "x-taskId": "seq_280",
  "x-contractVersion": "280.phase4.booking-flow-freeze.v1",
  "required": [
    "bookingConfirmationTruthProjectionId",
    "bookingCaseRef",
    "bookingTransactionRef",
    "selectedSlotRef",
    "appointmentRecordRef",
    "externalConfirmationGateRef",
    "commandSettlementRecordRef",
    "latestReceiptCheckpointRef",
    "authoritativeProofClass",
    "confirmationTruthState",
    "patientVisibilityState",
    "manageExposureState",
    "artifactExposureState",
    "reminderExposureState",
    "continuityEvidenceRef",
    "truthBasisHash",
    "projectionFreshnessEnvelopeRef",
    "settlementRevision",
    "generatedAt"
  ],
  "properties": {
    "bookingConfirmationTruthProjectionId": {
      "type": "string",
      "minLength": 1,
      "description": "Stable confirmation-truth projection id."
    },
    "bookingCaseRef": {
      "type": "string",
      "minLength": 1,
      "description": "Owning BookingCase ref."
    },
    "bookingTransactionRef": {
      "type": "string",
      "minLength": 1,
      "description": "Owning BookingTransaction ref."
    },
    "selectedSlotRef": {
      "type": "string",
      "minLength": 1,
      "description": "Selected slot ref."
    },
    "appointmentRecordRef": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "AppointmentRecord ref."
    },
    "externalConfirmationGateRef": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "Linked ExternalConfirmationGate ref."
    },
    "commandSettlementRecordRef": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "Current CommandSettlementRecord ref."
    },
    "latestReceiptCheckpointRef": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "Current AdapterReceiptCheckpoint ref."
    },
    "providerReference": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "Durable provider reference when present."
    },
    "authoritativeProofClass": {
      "type": "string",
      "enum": [
        "none",
        "durable_provider_reference",
        "same_commit_read_after_write",
        "reconciled_confirmation"
      ],
      "description": "Proof class for confirmed booking truth."
    },
    "confirmationTruthState": {
      "type": "string",
      "enum": [
        "booking_in_progress",
        "confirmation_pending",
        "reconciliation_required",
        "confirmed",
        "failed",
        "expired",
        "superseded"
      ],
      "description": "Authoritative confirmation truth state."
    },
    "patientVisibilityState": {
      "type": "string",
      "enum": [
        "selected_slot_pending",
        "provisional_receipt",
        "booked_summary",
        "recovery_required"
      ],
      "description": "Patient-safe visibility posture."
    },
    "manageExposureState": {
      "type": "string",
      "enum": [
        "hidden",
        "summary_only",
        "writable",
        "recovery_only"
      ],
      "description": "Manage exposure state."
    },
    "artifactExposureState": {
      "type": "string",
      "enum": [
        "hidden",
        "summary_only",
        "handoff_ready",
        "recovery_only"
      ],
      "description": "Artifact exposure state."
    },
    "reminderExposureState": {
      "type": "string",
      "enum": [
        "blocked",
        "pending_schedule",
        "scheduled",
        "suppressed"
      ],
      "description": "Reminder exposure state."
    },
    "continuityEvidenceRef": {
      "type": "string",
      "minLength": 1,
      "description": "Linked BookingContinuityEvidenceProjection ref."
    },
    "truthBasisHash": {
      "type": "string",
      "minLength": 1,
      "description": "Truth-basis hash."
    },
    "projectionFreshnessEnvelopeRef": {
      "type": "string",
      "minLength": 1,
      "description": "Projection freshness envelope ref."
    },
    "settlementRevision": {
      "type": "integer",
      "minimum": 1
    },
    "generatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Generation timestamp."
    }
  },
  "x-laws": [
    "BookingConfirmationTruthProjection is the sole audience-safe authority for booking commit outcome posture across patient booking shells, request-detail booking cards, appointment-manage surfaces, reminder and artifact affordances, staff-assisted booking panels, and support or operations booking summaries.",
    "confirmationTruthState = confirmed is legal only when the linked BookingTransaction.authoritativeOutcomeState = booked, the current CommandSettlementRecord agrees, and either a durable provider reference or same-commit read-after-write proof binds to that same transaction lineage.",
    "Local acknowledgement, accepted-for-processing, provider acceptance, callback arrival without a hard match, detached appointment rows, or stale AppointmentRecord presence may widen pending explanation but they may not produce booked_summary, writable manage, handoff_ready artifacts, or scheduled reminders.",
    "Duplicate confirm clicks, refresh re-entry, outbox replay, and callback replay must monotonically advance the same current projection for the active transaction chain."
  ],
  "x-sourceRefs": [
    "blueprint/phase-0-the-foundation-protocol.md",
    "blueprint/phase-4-the-booking-engine.md",
    "blueprint/patient-portal-experience-architecture-blueprint.md"
  ]
}
