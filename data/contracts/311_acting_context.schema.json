{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vecells.local/contracts/311_acting_context.schema.json",
  "title": "311 ActingContext",
  "description": "Current acting scope for hub commands and projections. This is the multi-factor command envelope that closes the 'cross-org access is a role check' gap.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "actingContextId",
    "staffIdentityContextRef",
    "staffUserId",
    "homePracticeOds",
    "activeOrganisationRef",
    "activePcnId",
    "activeHubSiteId",
    "tenantScopeMode",
    "tenantScopeRefs",
    "purposeOfUse",
    "actingRoleRef",
    "audienceTierRef",
    "visibilityCoverageRef",
    "minimumNecessaryContractRef",
    "elevationState",
    "breakGlassState",
    "contextState",
    "scopeTupleHash",
    "switchGeneration",
    "issuedAt",
    "expiresAt"
  ],
  "properties": {
    "actingContextId": {
      "type": "string",
      "minLength": 1,
      "description": "Stable ActingContext identifier."
    },
    "staffIdentityContextRef": {
      "type": "string",
      "minLength": 1,
      "description": "Owning StaffIdentityContext ref."
    },
    "staffUserId": {
      "type": "string",
      "minLength": 1,
      "description": "Staff user ref repeated for command and audit convenience."
    },
    "homePracticeOds": {
      "type": "string",
      "minLength": 1,
      "description": "Home-practice ODS code or equivalent organisation code for the acting user."
    },
    "activeOrganisationRef": {
      "type": "string",
      "minLength": 1,
      "description": "Current acting organisation or site ref."
    },
    "activePcnId": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "Current acting PCN ref when the case is coordinated at PCN level."
    },
    "activeHubSiteId": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "Current acting hub site ref when coordination is site-bound."
    },
    "tenantScopeMode": {
      "type": "string",
      "enum": [
        "single_tenant",
        "organisation_group",
        "multi_tenant",
        "platform"
      ],
      "description": "Breadth of tenant scope currently granted to the acting context."
    },
    "tenantScopeRefs": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "description": "Tenant or organisation refs that define the active scope."
    },
    "purposeOfUse": {
      "type": "string",
      "enum": [
        "direct_care_network_coordination",
        "direct_care_site_delivery",
        "practice_continuity",
        "supervisor_recovery",
        "break_glass_patient_safety"
      ],
      "description": "Purpose of use for this acting scope."
    },
    "actingRoleRef": {
      "type": "string",
      "minLength": 1,
      "description": "Resolved acting role ref for this context. Role names alone never authorize access."
    },
    "audienceTierRef": {
      "type": "string",
      "enum": [
        "origin_practice_visibility",
        "hub_desk_visibility",
        "servicing_site_visibility"
      ],
      "description": "Visibility audience tier for projections and commands."
    },
    "visibilityCoverageRef": {
      "type": "string",
      "minLength": 1,
      "description": "Coverage row set or compiled visibility coverage ref for the acting scope."
    },
    "minimumNecessaryContractRef": {
      "type": "string",
      "minLength": 1,
      "description": "MinimumNecessaryContract applied to this context."
    },
    "elevationState": {
      "type": "string",
      "enum": [
        "none",
        "requested",
        "active",
        "expiring",
        "revoked"
      ],
      "description": "Elevation or supervisor override posture."
    },
    "breakGlassState": {
      "type": "string",
      "enum": [
        "none",
        "requested",
        "active",
        "revoked"
      ],
      "description": "Break-glass posture for the acting context."
    },
    "contextState": {
      "type": "string",
      "enum": [
        "current",
        "stale",
        "blocked",
        "superseded"
      ],
      "description": "Current validity state of the acting context."
    },
    "scopeTupleHash": {
      "type": "string",
      "minLength": 1,
      "description": "Hash of the exact acting-scope tuple used for command and projection checks."
    },
    "switchGeneration": {
      "type": "integer",
      "minimum": 0,
      "description": "Monotonic generation that increments when organisation or scope switching occurs."
    },
    "issuedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Acting-context issue time."
    },
    "expiresAt": {
      "type": "string",
      "format": "date-time",
      "description": "Acting-context expiry time."
    }
  }
}
