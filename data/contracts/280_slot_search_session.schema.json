{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vecells://contracts/SlotSearchSession",
  "title": "SlotSearchSession",
  "description": "Governed search session contract for Phase 4 booking snapshot production.",
  "type": "object",
  "additionalProperties": false,
  "x-taskId": "seq_280",
  "x-contractVersion": "280.phase4.booking-flow-freeze.v1",
  "required": [
    "slotSearchSessionId",
    "bookingCaseId",
    "caseVersionRef",
    "searchPolicyRef",
    "policyBundleHash",
    "providerAdapterBindingRef",
    "providerAdapterBindingHash",
    "capabilityResolutionRef",
    "capabilityTupleHash",
    "selectionAudience",
    "requestedActionScope",
    "routeIntentBindingRef",
    "routeIntentTupleHash",
    "providerSearchSliceRefs",
    "slotSetSnapshotRef",
    "coverageState",
    "createdAt",
    "expiresAt"
  ],
  "properties": {
    "slotSearchSessionId": {
      "type": "string",
      "minLength": 1,
      "description": "Stable search-session identifier."
    },
    "bookingCaseId": {
      "type": "string",
      "minLength": 1,
      "description": "Owning BookingCase identifier."
    },
    "caseVersionRef": {
      "type": "string",
      "minLength": 1,
      "description": "Active BookingCase version at session creation."
    },
    "searchPolicyRef": {
      "type": "string",
      "minLength": 1,
      "description": "Compiled SearchPolicy reference from 278."
    },
    "policyBundleHash": {
      "type": "string",
      "minLength": 1,
      "description": "Hash of the active compiled policy bundle."
    },
    "providerAdapterBindingRef": {
      "type": "string",
      "minLength": 1,
      "description": "Current BookingProviderAdapterBinding reference from 279."
    },
    "providerAdapterBindingHash": {
      "type": "string",
      "minLength": 1,
      "description": "Current BookingProviderAdapterBinding hash from 279."
    },
    "capabilityResolutionRef": {
      "type": "string",
      "minLength": 1,
      "description": "Current BookingCapabilityResolution reference from 279."
    },
    "capabilityTupleHash": {
      "type": "string",
      "minLength": 1,
      "description": "Current BookingCapabilityResolution capabilityTupleHash."
    },
    "selectionAudience": {
      "type": "string",
      "enum": [
        "patient",
        "staff"
      ],
      "description": "Audience that owns the search shell."
    },
    "requestedActionScope": {
      "type": "string",
      "enum": [
        "search_slots",
        "book_slot",
        "view_appointment",
        "request_staff_assist"
      ],
      "description": "Requested booking action scope."
    },
    "routeIntentBindingRef": {
      "type": "string",
      "minLength": 1,
      "description": "Current route-intent binding ref."
    },
    "surfaceRouteContractRef": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "Current surface route contract ref."
    },
    "surfacePublicationRef": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "Current surface publication ref."
    },
    "runtimePublicationBundleRef": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "Current runtime publication bundle ref."
    },
    "routeIntentTupleHash": {
      "type": "string",
      "minLength": 1,
      "description": "Hash of the current route-intent tuple."
    },
    "queryEnvelope": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "requestedStartAt",
        "requestedEndAt",
        "timeZone",
        "modality",
        "sitePreferences",
        "sameBandReorderSlackMinutesByWindow"
      ],
      "properties": {
        "requestedStartAt": {
          "type": "string",
          "format": "date-time",
          "description": "Requested search start time."
        },
        "requestedEndAt": {
          "type": "string",
          "format": "date-time",
          "description": "Requested search end time."
        },
        "timeZone": {
          "type": "string",
          "minLength": 1,
          "description": "Search-time display timezone."
        },
        "modality": {
          "type": "string",
          "enum": [
            "in_person",
            "remote",
            "either"
          ],
          "description": "Requested modality posture."
        },
        "sitePreferences": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "sameBandReorderSlackMinutesByWindow": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "preferred",
            "acceptable"
          ],
          "properties": {
            "preferred": {
              "type": "integer",
              "minimum": 0
            },
            "acceptable": {
              "type": "integer",
              "minimum": 0
            }
          }
        }
      }
    },
    "temporalNormalizationEnvelopeRef": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 0,
      "description": "Linked TemporalNormalizationEnvelope ref."
    },
    "providerSearchSliceRefs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "slotSetSnapshotRef": {
      "type": "string",
      "minLength": 1,
      "description": "Current SlotSetSnapshot ref."
    },
    "coverageState": {
      "type": "string",
      "enum": [
        "complete",
        "partial_coverage",
        "timeout",
        "degraded",
        "failed"
      ],
      "description": "Aggregated provider coverage state."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Search-session creation time."
    },
    "expiresAt": {
      "type": "string",
      "format": "date-time",
      "description": "Snapshot selectability expiry time."
    }
  },
  "x-formulas": {
    "SnapshotSelectable": {
      "name": "SnapshotSelectable(q,t)",
      "expression": "SnapshotSelectable(q,t) = 1[t <= q.expiresAt] * 1[q.caseVersionRef matches the active BookingCase version] * 1[q.policyBundleHash matches the active compiled policy bundle] * 1[q.providerAdapterBindingHash matches the current BookingCapabilityResolution.providerAdapterBindingHash] * 1[q.capabilityTupleHash matches the current BookingCapabilityResolution.capabilityTupleHash] * 1[q.coverageState in {complete, partial_coverage}] * 1[recoveryState(q).viewState != stale_refresh_required]",
      "machineFields": [
        "expiresAt",
        "caseVersionRef",
        "policyBundleHash",
        "providerAdapterBindingHash",
        "capabilityTupleHash",
        "coverageState",
        "recoveryState.viewState"
      ],
      "inputObjects": [
        "SlotSetSnapshot",
        "SlotSnapshotRecoveryState",
        "BookingCase",
        "BookingCapabilityResolution"
      ],
      "failurePosture": "selection may preserve provenance and day anchor, but confirm-path mutation must fail closed and require refresh or governed recovery before a slot can be committed"
    }
  },
  "x-sourceRefs": [
    "blueprint/phase-4-the-booking-engine.md",
    "docs/architecture/278_phase4_booking_case_contract_and_state_machine.md",
    "docs/architecture/279_phase4_provider_capability_matrix_and_adapter_seam.md"
  ]
}
