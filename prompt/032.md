# Prompt 032 For 032_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_create_telephony_vendor_accounts_and_test_numbers

Use the shared operating contract first:
[shared_operating_contract_026_to_035.md](/Users/test/Code/V/prompt/shared_operating_contract_026_to_035.md)

```text
Execute roadmap task 032_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_create_telephony_vendor_accounts_and_test_numbers.md.

Inherit the Shared_operating_contract_for_prompts_026_to_035 and consume the validated outputs from tasks `001-031`, especially tasks `021`, `022`, `023`, `031`, and the telephony architecture from Phase 2.

Mission:
Create the telephony account-and-number provisioning pack in a way that respects the blueprint’s telephony law:
- call sessions, IVR, recording, transcript readiness, SMS continuation, and urgent-live preemption are all governed product behaviors
- real telephony accounts and numbers are later, gated, and potentially billable
- the programme still needs a high-fidelity local telephony lab now

This task therefore has two equally important jobs:
1. build a high-fidelity local telephony lab, local number namespace, and provider-twin admin surface now
2. build a gated real telephony account / workspace / test-number creation strategy later

Algorithm grounding you must honor:
- `phase-2-the-identity-and-echoes.md`, especially:
  - `2E. Telephony edge, IVR choreography, and call-session persistence`
  - `2F. Caller verification, voice capture, transcript stub, and SMS continuation`
  - eventing and degraded-mode expectations for telephony
- `phase-0-the-foundation-protocol.md`, especially:
  - safety preemption, evidence readiness, artifact quarantine, and grant law
- `patient-account-and-communications-blueprint.md`
- `callback-and-clinician-messaging-loop.md`
- `platform-runtime-and-release-blueprint.md`, especially adapter and degradation profiles
- `forensic-audit-findings.md`, especially findings on audio quarantine, transcript degradation, wrong-recipient risk, and continuity fragmentation

Current commercial-provider mechanics you must respect in the `Actual_provider_strategy_later` section:
Use current official vendor docs from the shortlisted telephony providers in task `031` as the external-process truth for later execution. At minimum support:
- account / organisation / workspace creation
- test vs live environment setup
- webhook endpoint configuration
- number search / reservation / purchase / release mechanics
- recording and media retention settings
- call flow / IVR / SIP / inbound voice options where relevant
- auth, API key, and callback signing settings
- billing / spend and verification requirements
Do not rely on stale memory.

Primary outcome:
A product-grade local telephony lab and number simulator that Vecells can use now, plus a later real account / number strategy that fails closed until procurement and spend gates are satisfied.

Deliverables to create:
1. `docs/external/32_local_telephony_lab_spec.md`
2. `docs/external/32_telephony_account_and_number_field_map.md`
3. `docs/external/32_telephony_live_gate_and_spend_controls.md`
4. `docs/external/32_telephony_webhook_and_recording_config_strategy.md`
5. `apps/mock-telephony-lab/README.md`
6. `apps/mock-telephony-lab/src/`
7. `services/mock-telephony-carrier/README.md`
8. `services/mock-telephony-carrier/src/`
9. `tests/playwright/mock-telephony-lab.spec.(ts|js)`
10. `tests/playwright/mock-telephony-call-flow.spec.(ts|js)`
11. `tools/browser-automation/telephony-account-and-number-dry-run.spec.(ts|js)`
12. `data/analysis/32_telephony_field_map.json`
13. `data/analysis/32_local_test_number_inventory.csv`
14. `data/analysis/32_telephony_webhook_matrix.csv`
15. `data/analysis/32_telephony_live_gate_checklist.json`
16. `tools/analysis/validate_telephony_lab_pack.(py|ts)`

Every number row must capture at minimum:
- `number_id`
- `e164_or_placeholder`
- `environment`
- `direction = inbound | outbound | both`
- `voice_enabled`
- `sms_enabled`
- `ivr_profile_ref`
- `recording_policy_ref`
- `webhook_profile_ref`
- `urgent_preemption_mode`
- `mock_now_use = yes | no`
- `actual_later_use = reserved | optional | rejected`
- `notes`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build two coordinated artifacts:
1. `services/mock-telephony-carrier` — the local carrier / provider twin
2. `apps/mock-telephony-lab` — the admin and flow-rehearsal surface

The local telephony twin must emulate enough behavior to let Vecells genuinely test:
- inbound and outbound call sessions
- IVR menus and DTMF capture
- urgent-live preemption states
- recording availability transitions
- transcript-job handoff hooks
- SMS continuation triggers
- webhook retries, disorder, and signature validation
- test-number assignment and release
- local-only vs provider-like environment profiles

The lab must let the team exercise at minimum:
- number inventory and routing
- call flow authoring / preview
- webhook endpoint mapping
- recording posture and retention preview
- continuation grant initiation
- failure and recovery states
- cost / spend gates for later real creation

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the later real telephony strategy. It must include:
- account / workspace field map
- number search and procurement decision record
- webhook endpoint pack
- recording and retention settings pack
- API key and secret class placeholders
- browser-automation dry-run harness for account and number setup
- live-execution gates that block any real account mutation or number purchase unless:
  - the telephony vendor was approved in task `031`
  - procurement and spend authority are explicit
  - named approver and target environment are present
  - callback endpoints and security posture are ready
  - `ALLOW_REAL_PROVIDER_MUTATION=true` and `ALLOW_SPEND=true` are set

Execution steps:
1. Start from task `031`’s telephony shortlist.
2. Build the local number inventory and webhook matrix first.
3. Implement the mock carrier second.
4. Build the lab third and wire it to the mock carrier.
5. Use `Playwright_or_other_appropriate_tooling` to drive the lab while developing.
6. Generate the real-later field map and dry-run harness from the same model.
7. Validate that call flows, recording state, and SMS continuation still preserve the blueprint’s telephony truth model.

Mandatory gap closures you must perform:
- Close the “test number provisioning is just a billing action” gap by tying numbers to IVR, recording, webhook, and continuity semantics.
- Close the “local telephony mocks can skip urgent-live preemption and transcript hooks” gap.
- Close the “real provider account setup can happen before webhook security and spend control are defined” gap.
- Close the “telephony and SMS number use can be conflated” gap by explicitly modeling voice and SMS capabilities per number.
- Close the “provider workspace success implies product readiness” gap by keeping readiness separate from account existence.

Non-negotiable rules:
- do not purchase or reserve real billable numbers unless live and spend gates pass
- do not expose real keys or webhook secrets in repo files or traces
- do not let mock numbers suggest routability outside the lab
- do not flatten recording, transcript, and urgent-live states into generic call success
- do not let telephony setup bypass callback, continuation, or identity fences

Mandatory UI and interaction specification for `apps/mock-telephony-lab`:
This must feel like a premium telephony operations lab, not a generic telecom dashboard.

Name the visual mode `Voice_Fabric_Lab`.

Brand and visual posture:
- include a subtle inline Vecells mark plus a clear `MOCK_TELEPHONY_LAB` ribbon
- tone: precise, real-time aware, minimal, quietly technical
- base palette:
  - canvas `#F5F7FA`
  - panel `#FFFFFF`
  - inset `#EEF2F6`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#0B57D0`
  - secondary accent `#0E9384`
  - voice accent `#7A5AF8`
  - caution accent `#B54708`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for phone numbers, call IDs, webhook signatures, and hashes

Layout:
- top status banner with active calls, number inventory, webhook health, and mock/actual mode switch
- left number rail
- center call-flow and event-stream workspace
- right inspector for recording, continuation, and security settings
- lower strip with one restrained line diagram of `inbound_call -> ivr -> verification -> recording -> transcript_hook -> continuation_or_triage`

Dimensional constraints:
- page max width `1440px`
- number rail `280px`
- inspector `360px`
- flow cards `min-height 160px`
- buttons `44px`
- chip height `28px`
- sticky header `72px`

Unique page set:
1. `Number_Inventory`
2. `IVR_Flow_Studio`
3. `Webhook_and_Signature_Map`
4. `Recording_and_Continuation`
5. `Live_Gates_and_Spend_Controls`

Motion:
- `120ms` hover and focus
- `180ms` timeline and flow transitions
- `240ms` inspector transitions
- reduced-motion support mandatory

Interaction requirements:
- number assignment and release in mock mode
- IVR branch simulation
- explicit failure / retry visualization
- keyboard-first interaction
- deterministic `data-testid` markers for number rail, flow editor, event stream, inspector, and mode toggle

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and testing from the start. At minimum cover:
- number switching
- IVR flow simulation
- webhook retry and signature states
- recording / continuation panel behavior
- mock/actual gating
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- one high-fidelity local telephony lab exists and is demonstrably useful now
- one later real account / number creation strategy exists with hard live and spend gates
- the output is usable as the authoritative handoff for telephony provisioning without sacrificing telephony truth semantics
```
