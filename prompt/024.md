# Prompt 024 For 024_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_submit_nhs_login_partner_access_requests

Use the shared operating contract first:
[shared_operating_contract_021_to_025.md](/Users/test/Code/V/prompt/shared_operating_contract_021_to_025.md)

```text
Execute roadmap task 024_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_submit_nhs_login_partner_access_requests.md.

Inherit the Shared_operating_contract_for_prompts_021_to_025 and consume the validated outputs from tasks `001-023`.

Mission:
Create the end-to-end NHS login partner-access-request execution pack in two explicit parts:

1. a high-fidelity local mock onboarding twin that Vecells can build and automate now
2. a gated real-provider strategy pack for later live submission when MVP, sponsor, assurance, and approval prerequisites actually exist

This task must not pretend that current-stage Vecells can automatically complete real NHS login onboarding if the programme is not yet eligible. The correct output is:
- executable mock onboarding now
- production-grade real-submission preparation later
- hard gates that prevent false live progression

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 2. Core product surfaces`
  - `### 10. Identity, consent, security, and policy`
  - the statement that NHS login is the patient authentication rail but does not replace local authorization, session, consent, or capability law
- `phase-0-the-foundation-protocol.md`, especially:
  - `1.4D AuthTransaction`
  - `1.4E AuthScopeBundle`
  - `1.4F PostAuthReturnIntent`
  - `1.4G SessionEstablishmentDecision`
  - `1.6 AccessGrant`
  - any rule preventing auth callback success from implying writable resume or request claim
- `phase-2-the-identity-and-echoes.md`, especially:
  - `2A. Trust contract and capability gates`
  - `2B. NHS login bridge and local session engine`
  - the fail-closed session and route-capability rules
- `phase-7-inside-the-nhs-app.md`, especially:
  - `7A. Journey inventory, integration manifest, and onboarding pack`
  - `7H. Sandpit, AOS, SCAL, and operational delivery pipeline`
  - use only as future-channel / future-onboarding input, not as a current-baseline blocker
- `platform-runtime-and-release-blueprint.md`, especially:
  - environment ring, release gate, and publication parity law
- `task 021` integration priority model
- `task 022` provider scorecards
- `task 023` sandbox-account and secret-ownership model
- `forensic-audit-findings.md`, especially findings where:
  - login success is misread as authorization
  - external onboarding or release truth is over-inferred from partial evidence
  - hidden drift between config, routes, and approved behavior would be dangerous

Supplemental external-process mechanics you must respect for the `Actual_provider_strategy_later` section:
Use the current official NHS login onboarding and developer materials for live-process structure, while keeping the blueprint corpus as the source of Vecells product logic. At minimum consult the current official guidance named:
- `Apply for NHS login`
- `Forms and documents`
- `Integrate`
- `How NHS login works`
- `How do I integrate to the sandpit?`
- `Compare NHS login environments`
- `Multiple redirect URIs`
Do not let those materials override the blueprint’s identity, session, route, or safety law.

Primary outcome:
A usable mock NHS login onboarding path plus a later live-submission pack that is fully prepared but safely gated.

Deliverables to create:
1. `docs/external/24_nhs_login_mock_onboarding_strategy.md`
2. `docs/external/24_nhs_login_actual_onboarding_strategy.md`
3. `docs/external/24_nhs_login_application_dossier.md`
4. `docs/external/24_nhs_login_manual_checkpoint_register.md`
5. `docs/external/24_nhs_login_onboarding_studio.html`
6. `apps/mock-nhs-login-onboarding/README.md`
7. `apps/mock-nhs-login-onboarding/src/` (implement the mock onboarding portal in the chosen frontend stack from task `014`)
8. `tests/playwright/mock-nhs-login-onboarding.spec.(ts|js)`
9. `tools/browser-automation/nhs-login-live-application-dry-run.spec.(ts|js)`
10. `data/analysis/nhs_login_application_field_map.json`
11. `data/analysis/nhs_login_live_gate_conditions.json`
12. `data/analysis/nhs_login_submission_artifact_checklist.csv`
13. `tools/analysis/validate_nhs_login_application_pack.(py|ts)`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build a local high-fidelity onboarding twin that simulates the product-facing side of NHS login partner onboarding. The goal is not to copy the NHS site visually. The goal is to model the operational workflow and artifact readiness so Vecells can rehearse and prove its package before real onboarding is possible.

The mock onboarding twin must cover these stages:
- `application_draft`
- `product_fit_review`
- `demo_prep`
- `sandpit_request_ready`
- `sandpit_requested`
- `product_demo_pending`
- `integration_request_blocked_until_demo`
- `integration_request_ready`
- `assurance_bundle_in_progress`
- `connection_agreement_pending`
- `service_desk_registration_pending`
- `ready_for_real_submission`

The mock must let the team exercise at minimum:
- application-form field completion from structured Vecells artifacts
- evidence attachment and validation
- sponsor / commissioner / service-eligibility placeholders
- architecture diagram and data-flow readiness
- user-journey inventory readiness
- product-demo preparation
- sandpit-request readiness
- integration-request dependency on successful demo
- blockers for missing safety / privacy / assurance evidence
- manual approval checkpoints and rejection reasons

The mock must also capture where the blueprint still leaves room for interpretation and convert that into explicit bounded fields or checklist items. Do not leave “we’ll fill this later” dead zones.

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the real-provider submission strategy for later use. This must include:
- the application dossier structure
- mapped fields for the live onboarding flow
- the list of prerequisite artifacts and approvals
- a browser-automation dry-run harness in `Playwright_or_other_appropriate_tooling`
- a live-execution gate that refuses to submit unless:
  - a credible MVP or demo environment exists
  - required sponsor / commissioner posture is explicitly known
  - architecture diagram and data-flow diagram are current
  - user journeys are current
  - current safety and privacy evidence bundle is present
  - named approver and environment target are present
  - live mutation flag is enabled
- a manual checkpoint register covering product-demonstration and legal / commercial steps that cannot be safely automated end-to-end

The real strategy must support:
- dry-run mode with synthetic values and no submission
- capture of screenshots / evidence only when secrets are absent or redacted
- selector-map based maintenance instead of brittle inline selectors
- pause-and-confirm points before final submission actions

Execution steps:
1. Start from tasks `021-023`.
2. Build the live field map for the NHS login application path, but do not assume the product is already eligible.
3. Build the mock onboarding twin first.
4. Use `Playwright_or_other_appropriate_tooling` to drive the mock twin end-to-end while developing it.
5. Generate the later live dry-run automation harness from the same field map and dossier model.
6. Build live-execution gates that fail closed.
7. Validate that the mock twin and the actual later strategy stay aligned to the same field and artifact model.

Mandatory gap closures you must perform:
- Close the “real onboarding is not currently realistic but the roadmap expects submission work” gap by splitting mock execution from actual strategy.
- Close the “application readiness lives in people’s heads” gap by creating a formal dossier and checkpoint register.
- Close the “product demo dependency on integration progression is implicit” gap.
- Close the “browser automation would either be fake or dangerously live” gap by creating an explicit dry-run and gated-live posture.
- Close the “NHS login auth rail could be treated as a product checkbox rather than a governed identity subsystem” gap by carrying blueprint identity law into the onboarding materials.
- Close the “later live submission could drift away from the mock rehearsal model” gap by sharing one field map and one validator.

Non-negotiable rules:
- do not submit a live application unless the live gate passes
- do not let real-provider process mechanics overwrite blueprint identity or authorization law
- do not copy NHS branding in a misleading way in the mock; it must be clearly marked as a Vecells internal mock / simulator
- do not store real secrets, signatory data, or legal details in repo fixtures
- do not allow the mock twin to suggest that a completed form implies technical approval, assurance approval, or live-readiness
- do not omit sponsor / commissioner / assurance blockers if they apply

Mandatory UI and interaction specification for `apps/mock-nhs-login-onboarding` and `24_nhs_login_onboarding_studio.html`:
This is a premium internal rehearsal surface. It should look unmistakably deliberate and high-end, not like a generic form builder.

Name the visual mode `Partner_Access_Atelier`.

Brand and visual posture:
- use an internal Vecells mock wordmark plus a clear `MOCK_ONBOARDING` ribbon
- never reproduce the real NHS login site styling exactly
- tone: formal, calm, clinical-operations grade, consequence-aware
- base palette:
  - canvas `#F5F7FA`
  - panel `#FFFFFF`
  - inset `#EEF2F6`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#2F5BFF`
  - secondary accent `#6E59D9`
  - progress accent `#0F9D58`
  - warning accent `#C98900`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for IDs, field keys, environment labels, and checklist hashes

Layout:
- top readiness banner:
  - current stage
  - blocker count
  - evidence freshness digest
  - mock / actual toggle
- left process rail:
  - each onboarding stage as a vertical step card
  - current stage highlighted
  - blocked stages show why
- center workspace:
  - stage-specific form canvas
  - two-column form on desktop, one-column on tablet/mobile
  - inline document checklist and evidence chips
- right evidence drawer:
  - selected artifact summary
  - source refs
  - last updated
  - blocker explanation
- lower strip:
  - process diagram from application -> demo -> sandpit -> integration -> assurance -> agreement -> service desk
  - tabular parity beneath it

Dimensional constraints:
- page max width `1440px`
- stage rail `280px`
- evidence drawer `380px`
- form section cards `min-height 160px`
- primary buttons `44px`
- step chips `28px`
- sticky header height `72px`

Motion:
- `120ms` hover/focus
- `180ms` step transitions
- `240ms` evidence drawer transitions
- reduced-motion support mandatory

Interaction requirements:
- autosave draft posture
- explicit blocker chips with precise copy
- no silent progression when required artifacts are missing
- keyboard-first form navigation
- deterministic `data-testid` markers for:
  - stage rail
  - form sections
  - blocker chips
  - evidence drawer
  - process diagram
  - mock/actual toggle

Unique design notes:
- include one restrained line-diagram showing the onboarding funnel and its gates
- include one minimal monoline “handoff” logo for the mock studio
- avoid giant hero illustrations, stock icons, or generic dashboard cards

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and testing from the start. At minimum cover:
- form completion and validation
- stage transitions
- blocker rendering
- autosave
- evidence attachment simulation
- mock/actual mode toggle
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- Vecells has a high-fidelity NHS login onboarding rehearsal surface it can use immediately.
- Vecells also has a gated live-submission strategy and dry-run harness for later real onboarding.
- Mock and actual paths are aligned but safely separated.
```