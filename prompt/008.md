# Prompt 008 For 008_seq_identify_external_dependency_inventory_and_assurance_obligations

Use the shared operating contract first:
[shared_operating_contract_006_to_010.md](/Users/test/Code/V/prompt/shared_operating_contract_006_to_010.md)

```text
Execute roadmap task 008_seq_identify_external_dependency_inventory_and_assurance_obligations.md.

Inherit the Shared_operating_contract_for_prompts_006_to_010 and consume the validated outputs from tasks 001-007.

Mission:
Produce the authoritative external-dependency inventory, dependency-classification model, assurance-obligation matrix, degraded-mode map, simulator strategy, and future provisioning/onboarding backlog for Vecells.

This task must not provision anything yet. It must determine exactly what Vecells depends on, why it depends on it, what authoritative proof each dependency must provide, what onboarding/assurance obligations attach to it, what fallback or simulator path is acceptable, and which future roadmap steps must use browser automation to acquire or configure it.

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 1. Product definition`
  - `### 2. Core product surfaces`
  - `### 6. Booking and access continuity`
  - `### 7. Pharmacy First pathway`
  - `### 10. Identity, consent, security, and policy`
  - `### 12. Practical engineering shape`
  - `### 14. Programme Baseline With NHS App Deferred`
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### AdapterContractProfile`
  - gateway/trust-zone rules
  - explicit “no browser may call GP-system, telephony, messaging, pharmacy, MESH, workflow, or audit services directly”
  - runtime/publication/trust implications for external dependencies
- `phase-2-identity-and-echoes.md`:
  - NHS login bridge
  - optional PDS enrichment
  - telephony capture, recording, transcript readiness, SMS continuation
  - contact-claim separation and IM1-out-of-critical-path guidance
- `phase-4-the-booking-engine.md`:
  - `4B Provider capability matrix and adapter seam`
  - `4C Slot search`
  - `4E Commit path`
  - `4F Manage flows`
  - `4G Smart Waitlist`
- `phase-5-the-network-horizon.md`:
  - capacity ingestion
  - cross-org messaging
  - hub-native booking commit
  - no-slot and callback fallback
- `phase-6-the-pharmacy-loop.md`:
  - directory abstraction and provider choice
  - dispatch adapters and transport contracts
  - urgent return channel config
  - outcome ingest / Update Record observation
- `phase-7-inside-the-nhs-app.md`:
  - web integration, site links, bridge capabilities
  - Sandpit / AOS / SCAL onboarding path
  - deferred baseline status
- `phase-8-the-assistive-layer.md`, especially `8H`
- `phase-9-the-assurance-ledger.md`, especially essential functions, dependency health, restore proof, and standards/evidence versioning
- `phase-cards.md` for baseline vs deferred scope
- `forensic-audit-findings.md`, especially findings around supplier truth, external confirmation, pharmacy transport, visibility boundaries, and support/ops/control-plane dependence

Deliverables to create:
1. `docs/analysis/08_external_dependency_inventory.md`
2. `docs/analysis/08_external_dependency_taxonomy.md`
3. `docs/analysis/08_assurance_obligations_matrix.md`
4. `docs/analysis/08_dependency_truth_and_fallback_matrix.md`
5. `docs/analysis/08_simulator_and_local_stub_strategy.md`
6. `docs/analysis/08_future_provisioning_and_browser_automation_backlog.md`
7. `docs/analysis/08_external_dependency_atlas.html`
8. `data/analysis/external_dependencies.json`
9. `data/analysis/external_dependency_inventory.csv`
10. `data/analysis/external_assurance_obligations.csv`
11. `data/analysis/dependency_truth_and_fallback_matrix.csv`
12. `data/analysis/dependency_simulator_strategy.json`
13. `data/analysis/future_browser_automation_backlog.csv`
14. `tools/analysis/build_external_dependency_inventory.(py|ts)`
15. `tools/analysis/validate_external_dependency_inventory.(py|ts)`

Every dependency row must capture at minimum:
- `dependency_id`
- `dependency_name`
- `dependency_class = identity_auth | patient_data_enrichment | telephony | sms | email | messaging_transport | gp_system | booking_supplier | pharmacy_directory | pharmacy_transport | pharmacy_outcome | embedded_channel | model_vendor | malware_scanning | transcription | analytics_observability | security_control | content_or_standards_source | other`
- `baseline_scope = baseline_required | optional_flagged | deferred_phase7 | future_optional`
- `source_file_refs[]`
- `business_purpose`
- `bound_bounded_contexts[]`
- `integration_mode`
- `authoritative_success_proof`
- `non_authoritative_signals[]`
- `ambiguity_modes[]`
- `fallback_or_recovery_modes[]`
- `simulator_allowed = yes | no | partial`
- `assurance_or_onboarding_obligations[]`
- `operator_or_runbook_dependencies[]`
- `future_provisioning_task_refs[]`
- `future_browser_automation_required = yes | no`
- `browser_automation_candidate_portal_or_console`
- `secrets_or_credentials_classes[]`
- `tenant_or_org_scope`
- `deferred_reason_if_any`
- `notes`

Execution steps:
1. Start from task 005’s external touchpoint matrix and expand it into a true dependency inventory.
2. Separate dependencies into the following layers:
   - clinical/platform rails
   - transport/message rails
   - supplier-specific adapters
   - channel/platform partner surfaces
   - security/assurance dependencies
   - optional feature-flagged enrichments
   - deferred channel-expansion dependencies
3. Normalize the dependency model so “dependency” does not mean only “HTTP API”. Include:
   - authentication/identity rails
   - partner onboarding environments
   - transport rails
   - batch or mailbox transports
   - managed service portals
   - required human-operated email or urgent-return routes
   - provider capability registries and directory feeds
   - standards/evidence dependencies that gate release or onboarding
4. Build the truth-and-fallback matrix. For each dependency, state:
   - what authoritative success means
   - what merely accepted/dispatched/observed means
   - what happens when truth is ambiguous
   - what degraded patient/staff posture must surface
   - whether the product may proceed under simulator or manual fallback
5. Build the assurance-obligation matrix. For each dependency, record:
   - what evidence or onboarding artifacts the source corpus expects
   - which later roadmap tasks must acquire/configure it
   - what operational evidence must exist before live use
6. Build the future browser automation backlog. For each dependency that later needs account creation, onboarding form submission, environment configuration, or console capture:
   - record the later task IDs
   - record candidate portal/console types
   - record likely manual checkpoints
   - record likely credential or secret classes
   - record whether the dependency is blocked by prior approval or contracting
   Do not execute those tasks here.
7. Build a simulator strategy that covers unavailable, contract-heavy, approval-heavy, or deferred dependencies.

You must classify at minimum these dependencies or dependency families:
- NHS login
- optional PDS enrichment
- telephony provider / IVR / recording availability
- transcription or transcript processing provider if used
- SMS provider
- email/notification provider
- IM1 Pairing and principal GP-system supplier paths
- local booking supplier adapter family
- network/hub capacity ingestion / external partner availability feeds where applicable
- cross-organisation messaging rail including MESH where the corpus relies on it
- pharmacy directory/search dependency
- pharmacy referral transport dependency
- pharmacy outcome / Update Record observation dependency
- monitored urgent-return or professional-contact routes for pharmacy safety net
- NHS App web integration / site-link / embedded bridge ecosystem (deferred baseline)
- model vendor or subprocessor family for assistive (bounded and optional)
- malware scanning / artifact scanning provider family where referenced
- observability/incident destinations only if the blueprint treats them as external dependencies rather than internal platform services

Mandatory gap closures you must perform:
- Close the “supplier/API list is scattered” gap by creating one inventory.
- Close the “authoritative proof vs transport acceptance” gap for every dependency.
- Close the “optional vs baseline vs deferred” gap, especially:
  - PDS as optional
  - NHS App embedded channel as deferred baseline scope
  - assistive vendor dependencies as bounded/optional to core functional completeness
- Close the “simulator vs real onboarding” gap by explicitly naming what can be simulated and what cannot.
- Close the “dependency assurances live only in prose” gap by turning them into machine-readable obligations.
- Close the “browser automation later” gap by explicitly preparing future automation touchpoints without executing them now.

Non-negotiable dependency rules you must encode:
- No browser may directly call external partner systems.
- Every external dependency boundary must map to one adapter/contract profile or equivalent declared boundary.
- No external dependency may be treated as authoritative solely because a request was accepted for processing or a webhook arrived.
- Deferred dependencies may still be inventoried, but they must not be mislabeled as current baseline blockers unless the corpus says so.
- A dependency that requires human or legal onboarding must expose that as an explicit operational blocker, not an afterthought.
- For every live dependency, the inventory must state how the product degrades when it is unavailable, ambiguous, stale, or suspended.

Specific dependency-sensitive source details you must preserve:
- `phase-2-the-identity-and-echoes.md` keeps IM1 out of the critical path for Phase 2 and separates NHS login contact claims from PDS/GP demographics.
- `phase-4-the-booking-engine.md` requires supplier capability resolution, provider adapter binding, and explicit confirmation truth.
- `phase-6-the-pharmacy-loop.md` separates provider choice, dispatch proof, outcome reconciliation, and urgent return; it explicitly says transport/mailbox delivery alone is not authoritative referral truth.
- `phase-7-inside-the-nhs-app.md` treats NHS App as a deferred channel expansion with Sandpit/AOS/SCAL onboarding semantics.
- `phase-8-the-assistive-layer.md` requires supplier assurance freshness and bounded change-control for model vendors/subprocessors.
- `phase-9-the-assurance-ledger.md` requires restore-order understanding and dependency-health visibility.

Validation rules:
- Every external touchpoint in task 005 must resolve to one dependency row or one explicit “internal only / not external” exclusion.
- Every dependency must declare authoritative proof and degraded fallback.
- Every dependency that later requires account setup or partner-console configuration must have a future provisioning backlog row.
- Every dependency with onboarding or assurance burden must declare that burden explicitly.
- Any dependency that can affect patient-visible truth without an explicit ambiguity/fallback posture must fail validation.
- Any dependency incorrectly labeled as baseline-required when task 003 marked it deferred or optional must fail validation.

Browser-viewable atlas requirements:
Build `08_external_dependency_atlas.html` and validate it with `Playwright_or_other_appropriate_tooling`.

Atlas composition:
- layered topology view:
  - product core
  - integration boundary layer
  - partner/dependency layer
  - assurance/onboarding layer
- dependency cards grouped by class with baseline/deferred chips
- truth ladder panel showing `accepted` vs `confirmed` vs `ambiguous` vs `failed`
- simulator panel showing allowed stub/simulator posture
- browser automation backlog panel showing later tasks that must provision/configure each dependency

Unique design requirements for this atlas:
- this page should feel like a “dependency constellation”
- use one restrained node-link map plus compact evidence/fallback tables
- include a small phase-baseline ribbon to separate current baseline from deferred Phase 7
- avoid generic cloud vendor icon soup; use neutral geometric marks and typed chips instead

Playwright validation must cover:
- topology render
- dependency filtering
- baseline/deferred toggles
- deep-linking into a dependency card
- search
- responsive layout
- keyboard navigation
- accessibility smoke checks
- table parity for the topology and truth ladder explanations

Definition of done:
- Vecells has one authoritative dependency inventory.
- Every dependency has explicit truth, ambiguity, fallback, simulator, and future provisioning semantics.
- Later provisioning tasks can proceed without rediscovering dependencies or onboarding obligations from prose.
```