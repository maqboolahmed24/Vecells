# Prompt 006 For 006_seq_map_canonical_domain_glossary_and_object_catalog

Use the shared operating contract first:
[shared_operating_contract_006_to_010.md](/Users/test/Code/V/prompt/shared_operating_contract_006_to_010.md)

```text
Execute roadmap task 006_seq_map_canonical_domain_glossary_and_object_catalog.md.

Inherit the Shared_operating_contract_for_prompts_006_to_010 and consume the validated outputs from tasks 001-005.

Mission:
Create the authoritative canonical domain glossary and object catalog for the full Vecells programme. This task must turn the corpus from “many rich documents” into one machine-readable semantic map that later architecture, code generation, runtime modeling, API design, UI work, and testing can trust.

This is not a loose glossary-writing exercise. It is a control-plane task. You must extract, normalize, classify, and relate the nouns that the product, platform, UI, runtime, assurance, and external-integration layers depend on.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `## 0B. Canonical domain kernel and state machine`
  - `## Canonical lifecycle, identity, safety, reservation, access, and reconciliation algorithm`
  - `### 1. Required platform objects`
  - object blocks `1.0A IntakeConvergenceContract` through `1.40G OperationalReadinessSnapshot`
- `blueprint-init.md`, especially:
  - `### 1. Product definition`
  - `### 2. Core product surfaces`
  - `### 3. The canonical request model`
  - `### 10. Identity, consent, security, and policy`
  - `### 11. Analytics and assurance`
  - `### 12. Practical engineering shape`
- `phase-cards.md`, especially:
  - `## Cross-Phase Conformance Scorecard`
  - `## Programme Summary-Layer Alignment`
  - `## Extended Summary-Layer Alignment`
- `forensic-audit-findings.md`, especially Findings:
  - 48-56 on state/object vocabulary drift
  - 57-79 on closure, events, booking/hub/pharmacy object semantics
  - 86-103 on shell continuity, visibility, patient/actionability, and evidence/control objects
- Cross-cutting object-rich blueprints:
  - `platform-frontend-blueprint.md`
  - `platform-runtime-and-release-blueprint.md`
  - `patient-account-and-communications-blueprint.md`
  - `staff-operations-and-support-blueprint.md`
  - `callback-and-clinician-messaging-loop.md`
  - `self-care-content-and-admin-resolution-blueprint.md`
  - `phase-8-the-assistive-layer.md`
  - `phase-9-the-assurance-ledger.md`

Primary outcome:
One canonical object catalog and glossary that distinguishes:
- durable domain aggregates
- append-only records
- gates and blockers
- cases and threads
- projections and digests
- contracts and policies
- tokens, grants, and leases
- manifests, bundles, and tuples
- settlements, checkpoints, and witnesses
- route/shell/visibility/runtime artifacts
- event namespaces and canonical descriptors

Deliverables to create:
1. `docs/architecture/06_canonical_domain_glossary.md`
2. `docs/architecture/06_object_catalog.md`
3. `docs/architecture/06_object_taxonomy_and_kind_model.md`
4. `docs/architecture/06_object_relationship_map.md`
5. `docs/architecture/06_aliases_deprecated_terms_and_forbidden_shorthand.md`
6. `docs/architecture/06_object_catalog_gap_report.md`
7. `docs/architecture/06_domain_glossary_atlas.html`
8. `docs/architecture/06_object_relationship_map.mmd`
9. `data/analysis/canonical_domain_glossary.csv`
10. `data/analysis/object_catalog.json`
11. `data/analysis/object_relationships.csv`
12. `data/analysis/object_alias_map.json`
13. `data/analysis/object_kind_taxonomy.json`
14. `data/analysis/object_source_traceability.jsonl`
15. `tools/analysis/build_object_catalog.(py|ts)`
16. `tools/analysis/validate_object_catalog.(py|ts)`

Each object row in the catalog must capture at minimum:
- `object_id`
- `canonical_name`
- `object_kind = aggregate | record | case | thread | projection | digest | contract | policy | bundle | manifest | tuple | token | grant | lease | gate | blocker | witness | settlement | checkpoint | event_contract | descriptor | artifact | namespace | other`
- `bounded_context`
- `phase_of_first_authoritative_introduction`
- `canonical_source_file`
- `canonical_source_heading_or_block`
- `supporting_source_refs[]`
- `canonical_definition`
- `why_it_exists`
- `authoritative_owner`
- `write_authority`
- `read_consumers`
- `durability_class = durable | append_only | derived_projection | ephemeral_runtime`
- `mutability_rule`
- `identity_or_subject_implications`
- `visibility_or_masking_implications`
- `external_dependency_implications`
- `authoritative_success_or_truth_role`
- `related_state_machine_refs[]`
- `related_event_refs[]`
- `related_object_ids[]`
- `aliases[]`
- `deprecated_or_forbidden_terms[]`
- `notes`

Execution steps:
1. Start from task 001’s requirement registry and task 002’s canonical alias decisions.
2. Build the object extraction pass in two layers:
   - Layer A: authoritative object extraction from explicit object blocks, schema lists, and formal contracts.
   - Layer B: implied-object extraction from flow documents, forensic patches, and phase prose where the corpus clearly relies on an object without naming it in a single canonical section.
3. Classify every extracted noun into one and only one primary `object_kind`, while allowing secondary tags for search.
4. Build a canonical object taxonomy that separates:
   - domain truths (e.g. `Request`, `Episode`, `IdentityBinding`)
   - continuity and control-plane primitives (e.g. `RequestLineage`, `LineageCaseLink`, `RouteIntentBinding`, `CommandSettlementRecord`)
   - policy/visibility/runtime publication primitives
   - patient/staff/support-facing projections and digests
   - assurance/audit/evidence graph primitives
   - external-adapter boundary primitives
5. Normalize object naming across all docs. Specific normalization rules you must implement:
   - never let `Request`, `RequestLineage`, and `LineageCaseLink` collapse into one family
   - never let `Session`, `AccessGrant`, `IdentityBinding`, and `CapabilityDecision` collapse into “auth/session”
   - never let `truth projection` objects stand in for authoritative durable records
   - never let `case-local reconciliation` labels become canonical request-state terms
   - never let UI shells, projections, and route artifacts be cataloged as if they were back-end aggregates
   - never let “support replay”, “return”, “restore”, “recovery”, and “continuation” be treated as synonyms without object-level proof
6. Build a canonical alias map. Every alias must land in one of:
   - exact alias to canonical object
   - deprecated shorthand
   - ambiguous phrase requiring context
   - not-an-object phrase that should stay glossary-only
7. Build a relationship graph for:
   - contains / references / supersedes / settles / guards / proves / projects / materializes / blocks / unblocks / publishes / compiles / binds / consumes / emits / governs
8. Build source traceability so a future agent can click from catalog row to exact conceptual block.
9. Validate the catalog rigorously.

Mandatory gap closures you must perform:
- Close the “objects scattered across many documents” gap by producing one authoritative catalog.
- Close the “projection vs authoritative object” gap by explicitly marking which objects are authoritative truth and which are read-side or UX-side reflections.
- Close the “token/grant/lease ambiguity” gap by separating grants, leases, tokens, sessions, and checkpoints.
- Close the “policy vs contract vs publication artifact” gap by separating policy sources, compiled contracts, runtime bindings, and release/publication tuples.
- Close the “event namespace vs domain object” gap by cataloging both without confusing them.
- Close the “object exists in flow but not in canonical object list” gap by surfacing `GAP_OBJECT_*` entries with bounded resolutions.

Specific object families you must not miss:
- Phase 0 foundational objects:
  - `SubmissionEnvelope`
  - `SubmissionPromotionRecord`
  - `RequestLineage`
  - `LineageCaseLink`
  - `Episode`
  - `Request`
  - `IdentityBinding`
  - `IdentityRepairCase`
  - `AccessGrant`
  - `DuplicateCluster`
  - `EvidenceAssimilationRecord`
  - `MaterialDeltaAssessment`
  - `SafetyPreemptionRecord`
  - `SafetyDecisionRecord`
  - `UrgentDiversionSettlement`
  - `ReachabilityDependency`
  - `RequestLifecycleLease`
  - `RequestClosureRecord`
  - `ProviderCapabilityMatrix`
  - `BookingProviderAdapterBinding`
  - `CapacityReservation`
  - `ExternalConfirmationGate`
  - `PharmacyCorrelationRecord`
  - `VisibilityProjectionPolicy`
  - `MinimumNecessaryContract`
  - `FallbackReviewCase`
  - `RouteIntentBinding`
  - `CommandActionRecord`
  - `CommandSettlementRecord`
  - `ReleaseApprovalFreeze`
  - `ChannelReleaseFreezeRecord`
  - `AssuranceSliceTrustRecord`
  - `PatientNavUrgencyDigest`
  - `PatientNavReturnContract`
  - `RecoveryContinuationToken`
  - `PatientActionRecoveryEnvelope`
  - `PatientReceiptEnvelope`
  - `SupportReplayRestoreSettlement`
  - `AudienceSurfaceRouteContract`
  - `DesignContractPublicationBundle`
  - `RuntimePublicationBundle`
- Frontend/runtime/control-plane families:
  - `PersistentShell`
  - `ShellContinuityFrame`
  - `ContinuityTransitionCheckpoint`
  - `AudienceSurfaceRuntimeBinding`
  - `UIEventEnvelope`
  - `UITransitionSettlementRecord`
  - `UITelemetryDisclosureFence`
  - `ArtifactPresentationContract`
  - `ArtifactSurfaceBinding`
  - `ArtifactParityDigest`
  - `OutboundNavigationGrant`
- Domain families introduced later:
  - triage, decision, approval, more-info, callback, clinician message, self-care, admin-resolution, booking, waitlist, hub coordination, pharmacy, assistive, assurance, incident, retention, audit, governance, and replay objects
- Evidence / assurance families:
  - `AssuranceLedgerEntry`
  - `EvidenceArtifact`
  - `AssuranceEvidenceGraphSnapshot`
  - `ControlStatusSnapshot`
  - `InvestigationScopeEnvelope`
  - `RetentionLifecycleBinding`
  - `DeletionCertificate`
  - `ArchiveManifest`

Additional cataloging rules:
- Catalog route families, shell types, audience tiers, and channel profiles as glossary concepts and contract artifacts, not as domain aggregates.
- Catalog events/namespaces as `event_contract` or `namespace`, never as aggregates.
- When a later phase introduces a case-local state object, link it to the owning case and explicitly forbid promotion into canonical request workflow unless the source allows it.
- When the corpus introduces UI/projection helper objects with strong control semantics, catalog them because they govern real behavior.

Validation rules:
- Every authoritative object block in Phase 0 must produce a catalog row.
- Every object used as a blocker, gate, settlement, grant, lease, or runtime/publication control in any later phase must appear in the catalog.
- Every alias from task 002’s summary reconciliation must resolve to one catalog entry or one explicit non-object glossary entry.
- Any object named in `vecells-complete-end-to-end-flow.md` or `.mmd` but missing from the catalog must fail validation.
- Any object that appears to allow direct child-domain writes to canonical `Request` lifecycle truth must raise `CONFLICT_OBJECT_OWNERSHIP_*`.

Browser-viewable atlas requirements:
Build `06_domain_glossary_atlas.html` as a premium internal explainer, validated with `Playwright_or_other_appropriate_tooling`.

Atlas composition:
- left rail: taxonomy filters and bounded-context chips
- hero band: counts for object kinds, authoritative objects, projections, gates/blockers, and unresolved gaps
- center pane: searchable object table with sticky columns and kind chips
- right detail panel: definition, source traceability, relationships, aliases, and “do not confuse with” section
- relationship canvas: one inline SVG or Mermaid-derived graph showing the currently selected object’s adjacency
- glossary spotlight strip: rotating callouts for high-risk confusions such as `Request` vs `RequestLineage`, `Session` vs `AccessGrant`, and `truth projection` vs authoritative record

Interaction rules:
- deep-linkable object IDs
- fuzzy search across canonical names and aliases
- filter by phase, bounded context, object kind, and owner
- keyboard access to filter changes and detail-panel expansion
- print-safe detail sheet for one object
- every graph view must have adjacent table/list parity
- no generic dashboard chrome; keep the page editorial and atlas-like

Playwright validation for the atlas must cover:
- page load and local asset integrity
- search results for at least 5 alias cases
- deep link to a selected object
- filter combinations
- keyboard navigation and focus order
- responsive behavior for desktop/tablet/mobile breakpoints
- basic accessibility smoke checks and diagram/table parity checks

Definition of done:
- One machine-readable catalog exists for the full programme.
- The catalog separates authoritative objects from projections, policies, UI artifacts, and runtime/publication controls.
- Terminology drift is controlled by an alias/deprecation map, not left to prose.
- Later autonomous agents can query the catalog to know what an object is, who owns it, what it guards, and what it must never be confused with.
```