# Prompt 012 For 012_seq_choose_monorepo_build_system_and_language_standards

Use the shared operating contract first:
[shared_operating_contract_011_to_015.md](/Users/test/Code/V/prompt/shared_operating_contract_011_to_015.md)

```text
Execute roadmap task 012_seq_choose_monorepo_build_system_and_language_standards.md.

Inherit the Shared_operating_contract_for_prompts_011_to_015 and consume the validated outputs from tasks 001-011.

Mission:
Choose the monorepo build system, workspace graph law, import-boundary enforcement model, primary language standards, code-generation pipeline, and developer workflow baseline for Vecells.

This task must turn the Phase 0 repository-shape guidance into an implementation-grade workspace contract. It is not enough to say “modular monorepo”. You must make the repo enforceable:
- package ownership must be machine-checkable
- cross-context imports must be blocked by policy
- generated contracts must be reproducible and versioned
- apps and services must share types without hidden coupling
- the chosen language posture must support deterministic contracts, design-system reuse, Playwright automation, runtime publication, and release evidence

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `## 0A. Delivery skeleton and repository architecture`
  - the repo starting shape under `apps/*`, `services/*`, `packages/*`, `infra/*`, `ops/*`
  - the repo boundary law stating apps may not own truth and cross-context reuse is limited to shared kernel packages
  - the requirement for build system, linting, type safety, code owners, import-boundary rules, preview environments, SBOM generation, signed-artifact verification, and one-command local startup
- `platform-runtime-and-release-blueprint.md`, especially:
  - `## Frontend and backend integration contract`
  - `## Verification ladder contract`
  - `## CI/CD and supply-chain pipeline contract`
  - the rules requiring generated schemas, typed clients, contract tests, provenance, and publication bundles
- `platform-frontend-blueprint.md`, especially:
  - `#### 4.1D Reference implementation shape`
  - `#### 7.8 Verification and Playwright contract`
  - `#### 2.15B DesignContractRegistry`
- `design-token-foundation.md` and `canonical-ui-contract-kernel.md`, especially where token exports, profile-selection, design-contract publication, automation anchors, and semantic coverage become machine-readable artifacts
- `accessibility-and-content-system-contract.md` for accessibility verification expectations that the workspace must support in build and test toolchains
- `forensic-audit-findings.md`, especially:
  - 91 on route / settlement / release / trust controls drifting into conventions
  - 118 on token export and design-contract conformance drifting outside the runtime tuple
- tasks 006, 007, 010, and 011 outputs, which now define the object catalog, invariants, data classes, and topology constraints this workspace must carry

Primary outcome:
One workspace baseline that answers:
- what the monorepo toolchain is
- what the primary implementation languages are
- which secondary languages are allowed for bounded use only
- how packages are named, tagged, and owned
- how API/event/design/runtime contracts are generated and published
- how import-boundary, code-owner, and code-generation drift are prevented
- how local development, CI, preview, contract testing, and Playwright testing fit together

Deliverables to create:
1. `docs/architecture/12_monorepo_build_system_decision.md`
2. `docs/architecture/12_workspace_layout_and_boundary_rules.md`
3. `docs/architecture/12_language_standards_and_allowed_toolchains.md`
4. `docs/architecture/12_contract_codegen_and_publication_strategy.md`
5. `docs/architecture/12_testing_toolchain_and_quality_gate_baseline.md`
6. `docs/architecture/12_developer_experience_and_local_bootstrap.md`
7. `docs/architecture/12_import_boundary_and_codeowners_policy.md`
8. `docs/architecture/12_workspace_graph_atlas.html`
9. `data/analysis/build_system_scorecard.csv`
10. `data/analysis/language_standard_matrix.csv`
11. `data/analysis/workspace_package_graph.json`
12. `data/analysis/import_boundary_rules.json`
13. `data/analysis/codeowners_matrix.csv`
14. `data/analysis/contract_codegen_matrix.csv`
15. `data/analysis/test_tooling_matrix.csv`
16. `tools/analysis/validate_workspace_baseline.(py|ts)`

You must evaluate at minimum these build-system candidates unless the repo already proves one is non-viable:
- `pnpm + Nx`
- `pnpm + Turborepo`
- `Bazel` or an equivalent graph-first build system

You must evaluate at minimum these language postures:
- TypeScript-first for browser, gateway, shared packages, and most services
- polyglot runtime with TypeScript + Go / Python / JVM split by service family
- TypeScript-first runtime plus narrowly bounded Python-only analysis / policy / assurance tooling

Execution steps:
1. Start from the canonical repository shape in Phase 0. Your job is to make it enforceable, not to replace it with a framework-centric workspace.
2. Build a scorecard for build-system selection. Required criteria:
   - explicit workspace graph
   - import-boundary enforcement
   - remote-cache independence for local determinism
   - incremental builds
   - typed code-generation orchestration
   - multiple app/service targets
   - Playwright integration
   - preview-environment friendliness
   - provenance / SBOM / policy checks integration
   - low accidental complexity for autonomous agents
3. Build a language-standards scorecard. Required criteria:
   - shared contract types across browser, gateway, services, and packages
   - deterministic schema/codegen support
   - runtime validation and strict typing
   - minimal cross-language impedance for canonical contracts
   - support for reducer-style deterministic front-end code
   - support for infrastructure, validators, and analysis tooling without turning the runtime into uncontrolled polyglot sprawl
4. Unless there is strong repo evidence otherwise, prefer a TypeScript-first baseline for apps, BFF/gateway-facing services, contract packages, design-system packages, and automated test tooling. Any secondary language must be explicitly bounded and must not become the hidden owner of canonical runtime truth.
5. Define the workspace graph using the canonical Phase 0 skeleton and any required extensions. At minimum classify:
   - apps
   - services
   - domain packages
   - contract packages
   - design-system / design-contract packages
   - observability / release-control packages
   - infra and ops packages
   - analysis / validation tooling
6. Define package tags and import-boundary rules such that:
   - bounded contexts own their own internals
   - sibling contexts cannot reach through each other’s aggregates / repos / worker-only helpers
   - apps and services consume published contracts, not sibling internals
   - generated code lives in explicit generated packages or directories and is never treated as hand-written source of truth
7. Define the language and style standards. At minimum cover:
   - strict compiler settings
   - runtime schema validation posture
   - date / time / timezone rules
   - ID and correlation-ID types
   - error family encoding
   - serialization and numeric precision rules
   - event / command envelope typing
   - testing conventions
   - formatting / linting / dead-code / dependency policies
8. Define the contract-codegen pipeline for:
   - route contracts
   - OpenAPI / typed client generation
   - live-channel contracts
   - event contracts
   - design-contract publication bundles
   - schema migrations and contract test fixtures
9. Define the testing toolchain baseline. It must explicitly include:
   - unit tests
   - contract tests
   - event schema compatibility tests
   - component and accessibility smoke tests
   - `Playwright_or_other_appropriate_tooling` for end-to-end shell verification
   - performance / security hooks needed later by task 015
10. Build a browser-viewable workspace graph atlas that helps future agents inspect package ownership, allowed edges, generated contracts, and code-owner domains.

Mandatory gap closures you must perform:
- Close the “modular monorepo is named but not enforced” gap.
- Close the “apps can accidentally own truth” gap by making package ownership and import-boundary rules machine-checkable.
- Close the “generated clients and contracts may drift outside runtime publication truth” gap by binding codegen outputs back to published contract artifacts.
- Close the “TypeScript-style front-end law exists, but workspace standards do not preserve it” gap.
- Close the “design-contract publication, semantic coverage, automation anchors, and telemetry vocabulary are sidecars” gap identified by Finding 118.
- Close the “polyglot creep” gap by making allowed vs prohibited language use explicit.
- Close the “test toolchain is present but not route-contract-aware” gap by tying tests to published contracts and runtime tuples.

Required workspace rules to encode:
- No app can own truth.
- Cross-context reuse is limited to shared kernel / published contract packages.
- Every sync route must have a generated schema, typed client, and contract test.
- Every async stream/webhook contract must have versioned types and compatibility tests.
- Generated artifacts must be reproducible from source contracts; snapshot-only codegen is forbidden.
- Design-system and design-contract packages are first-class workspace citizens, not docs sidecars.
- Playwright tests must be able to consume stable DOM markers and design-contract digests.
- No remote CDN assets, fonts, or browser scripts are allowed in workspace baseline outputs.
- Secrets, credentials, or provider-specific config may not appear in committed source or generated artifacts.

Validation rules:
- Every package / app / service must resolve to one owner domain and one package-tag class.
- Any import edge that crosses bounded-context internals must fail validation.
- Any generated route or event client that cannot trace back to a source contract must fail validation.
- Any proposed secondary language owning canonical runtime truth without explicit architectural justification must fail validation.
- Any workspace baseline that cannot support one-command local startup, CI builds, contract tests, and Playwright runs must fail validation.
- Any package graph that allows shell code to read raw data stores, adapter internals, or unpublished domain internals must fail validation.

Browser-viewable atlas requirements:
Build `12_workspace_graph_atlas.html` and validate it with `Playwright_or_other_appropriate_tooling`.

Atlas composition:
- left rail: package tags, owners, languages, and bounded contexts
- hero strip: counts for apps, services, packages, generated-contract sets, illegal-edge findings, and unresolved gaps
- center graph: workspace package graph with owner-colored nodes and allowed / forbidden edge styles
- inspector panel: selected package metadata, imports, exports, generated artifacts, and code-owner rules
- contract panel: route, event, design-contract, and migration codegen flows
- test panel: unit / contract / Playwright coverage by package family

Unique design requirements:
- the atlas should feel like an “architecture graph studio”, not a stock dependency graph tool
- use restrained graph motion and clear side-panels, not neon node clusters
- make forbidden edges visually obvious without turning the page into an alarm wall
- keep the visual hierarchy summary-first and evidence-rich

Playwright validation must cover:
- graph render
- node / edge filtering
- inspector interactions
- keyboard navigation
- responsive layout
- accessibility smoke checks
- stable markers for selected package, edge class, owner, language, and violation state

Definition of done:
- Vecells now has one enforceable workspace and language baseline.
- Future implementation agents can tell where code belongs, which language is allowed, which imports are legal, and how contracts are generated without reinterpreting the architecture.
- The repo can now resist framework-convenience drift and hidden cross-context coupling.
```
