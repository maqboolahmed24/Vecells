# Prompt 050 For 050_seq_phase0_define_frontend_contract_manifest_strategy

Use the shared operating contract first:
[shared_operating_contract_046_to_055.md](/Users/test/Code/V/prompt/shared_operating_contract_046_to_055.md)

```text
Execute roadmap task 050_seq_phase0_define_frontend_contract_manifest_strategy.md.

Inherit the Shared_operating_contract_for_prompts_046_to_055 and consume the validated outputs from tasks `001-049`.

Mission:
Define the canonical `FrontendContractManifest` strategy for Vecells so every audience surface and route-family set has one browser-consumable authority contract binding:
- route-family ownership
- gateway exposure
- query / mutation / live-channel contracts
- cache policy
- design-contract publication
- accessibility semantic coverage
- automation anchor coverage
- surface-state semantics
- release / recovery / route-freeze posture
- browser writability state

This task must stop shells, gateways, and tests from reconstructing browser authority out of codegen fragments, route-local selectors, or inferred query shape.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`
  - `### FrontendContractManifest`
  - `### ProjectionContractFamily`
  - `### ProjectionContractVersion`
  - `### ProjectionContractVersionSet`
  - `### ProjectionQueryContract`
  - `### MutationCommandContract`
  - `### LiveUpdateChannelContract`
  - `### ClientCachePolicy`
  - `AudienceSurfaceRuntimeBinding` rules, especially the fail-closed posture on parity / topology / accessibility / browser-boundary drift
- `platform-frontend-blueprint.md`
  - route inventory contract
  - typed patient transaction route contract
  - patient secure record route contract
  - typed support transaction route contract
  - shared visibility/hydration/empty-state rules
  - `AccessibilitySemanticCoverageProfile`
  - `AutomationAnchorProfile`
  - the rules that route-local ARIA or test selectors are invalid substitutes for published design/runtime truth
- `design-token-foundation.md`
  - `DesignTokenExportArtifact`
  - `TokenKernelLayeringPolicy`
  - `ProfileSelectionResolution`
- `canonical-ui-contract-kernel.md`
  - `SurfaceStateSemanticsProfile`
  - `SurfaceStateKernelBinding`
  - `AutomationAnchorMap`
  - `DesignContractVocabularyTuple`
  - `DesignContractPublicationBundle`
  - `DesignContractLintVerdict`
- `accessibility-and-content-system-contract.md`
- `ux-quiet-clarity-redesign.md`
- outputs from tasks `004`, `014`, `041-049`
- `forensic-audit-findings.md`, especially findings `86-88`, `91`, `97`, `101`, `116-120`

Primary outcome:
One manifest strategy and machine-readable manifest catalog proving exactly what each browser surface may do, show, cache, announce, automate, and degrade into.

Deliverables to create:
1. `docs/architecture/50_frontend_contract_manifest_strategy.md`
2. `docs/architecture/50_frontend_contract_manifest_catalog.md`
3. `docs/architecture/50_route_contract_to_manifest_matrix.md`
4. `docs/architecture/50_frontend_contract_studio.html`
5. `data/analysis/frontend_contract_manifests.json`
6. `data/analysis/frontend_route_to_query_command_channel_cache_matrix.csv`
7. `data/analysis/frontend_accessibility_and_automation_profiles.json`
8. `data/analysis/frontend_manifest_generation_rules.json`
9. `packages/api-contracts/schemas/frontend-contract-manifest.schema.json`
10. `tools/analysis/validate_frontend_contract_manifests.(py|ts)`
11. `tests/playwright/frontend-contract-studio.spec.(ts|js)`

For each manifest row capture at minimum:
- `frontendContractManifestId`
- `audienceSurface`
- `routeFamilyRefs[]`
- `gatewaySurfaceRef`
- `surfaceRouteContractRef`
- `surfacePublicationRef`
- `audienceSurfaceRuntimeBindingRef`
- `designContractPublicationBundleRef`
- `tokenKernelLayeringPolicyRef`
- `profileSelectionResolutionRefs[]`
- `surfaceStateKernelBindingRefs[]`
- `projectionContractVersionSetRef`
- `runtimePublicationBundleRef`
- `projectionQueryContractRefs[]`
- `mutationCommandContractRefs[]`
- `liveUpdateChannelContractRefs[]`
- `clientCachePolicyRef`
- `commandSettlementSchemaRef`
- `transitionEnvelopeSchemaRef`
- `releaseRecoveryDispositionRef`
- `routeFreezeDispositionRef`
- `browserPostureState`
- `frontendContractDigestRef`
- `designContractDigestRef`
- `designContractLintVerdictRef`
- `profileLayeringDigestRef`
- `kernelPropagationDigestRef`
- `accessibilitySemanticCoverageProfileRefs[]`
- `automationAnchorProfileRefs[]`
- `surfaceStateSemanticsProfileRefs[]`
- `accessibilityCoverageDigestRef`
- `accessibilityCoverageState`
- `projectionCompatibilityDigestRef`
- `surfaceAuthorityTupleHash`
- `generatedAt`

Execution steps:
1. Start from:
   - audience/shell/route inventory from task `004`
   - frontend stack and BFF pattern from task `014`
   - repo and app scaffold from tasks `041-044`
   - runtime topology and gateway map from tasks `046-047`
   - event and FHIR publication outputs from tasks `048-049`
2. Define the manifest-generation law:
   - what the generator consumes
   - how route families are grouped into manifest rows
   - which digest inputs form the manifest tuple
   - what changes force regeneration
3. Define projection-read families and version sets per audience surface / route family.
4. Define command, live-channel, and cache-policy linkage per route family.
5. Define the required design-contract, accessibility, automation, and state-semantics linkage per route family.
6. Produce manifest rows at minimum for:
   - patient public entry routes
   - authenticated patient portal routes
   - grant-scoped patient transaction routes
   - clinical workspace routes
   - support routes
   - hub desk routes
   - pharmacy console routes
   - operations console routes
   - governance/admin routes
7. For every manifest row, explicitly record the browser posture decision path:
   - when it may remain `publishable_live`
   - when it must fall to `read_only`
   - when it must fall to `recovery_only`
   - when it must be `blocked`
8. Build the studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
9. Validate that:
   - every browser-visible route family belongs to exactly one active manifest row
   - every manifest references one gateway surface and one runtime publication bundle
   - every active manifest references the same current design/accessibility/automation/state tuple required by the route family
   - no route family relies on undeclared query, mutation, channel, or cache semantics
   - any drift in parity, design lint, topology, accessibility coverage, projection compatibility, or provenance would degrade browser posture rather than remaining silent

Mandatory gap closures you must perform:
- Close the “browser authority is inferred from route code generation and gateway implementation” gap from the runtime blueprint.
- Close the “route-local selectors and ARIA can quietly replace published accessibility/automation truth” gap from the frontend blueprint.
- Close the “query version compatibility is inferred from deploy order” gap by binding each route family to one explicit projection contract version set.
- Close the “cache behavior is a frontend convenience” gap by publishing `ClientCachePolicy` as part of manifest authority.
- Close the “design tokens and design publication can drift outside runtime/browser authority” gap by making the manifest reference the active design bundle and lint verdict.
- Close the “surface recovery or freeze posture is route-local copy only” gap by binding recovery and freeze dispositions into the manifest.

Industry-best-practice requirements:
- machine-generated manifests, not hand-curated browser truth
- strict schema validation for manifest artifacts
- stable digests and tuple hashes
- explicit read / mutate / subscribe / cache authority
- explicit accessibility and automation coverage
- deterministic generation from source contracts
- CI failure on manifest incompleteness or drift

Non-negotiable rules:
- One `FrontendContractManifest` is the only browser-consumable authority contract for one audience surface and one declared route-family set.
- Browsers, gateways, support tools, and shells may not reassemble browser authority from route contract, design bundle, runtime binding, and parity fragments independently.
- Every browser-visible read, mutation, stream, settlement envelope, cache policy, recovery posture, and design-contract bundle reachable from the route family must be enumerated in the manifest.
- If runtime publication, topology, design-contract publication, lint posture, profile layering, kernel propagation, accessibility coverage, parity, provenance, read-path readiness, channel freeze, or assurance posture drifts, the manifest must degrade and writable posture must not remain silently live.
- If a browser call is not present in the active manifest, the gateway must reject it.

Mandatory UI and interaction specification for `50_frontend_contract_studio.html`:
Name the surface mode `Manifest_Studio`.

This is a frontend-governance surface. It must look premium, intentional, and unmistakably product-designed rather than “internal admin”. It should feel like a polished control room for browser authority.

Brand and visual posture:
- include a restrained Vecells wordmark plus a compact inline SVG `MANIFEST` mark
- aesthetic: minimalist premium, Quiet Clarity aligned, crisp geometry, generous whitespace, no generic dashboard chrome
- use a subtle “authority braid” motif showing route contract + design contract + runtime bundle converging into one manifest tuple

Palette:
- canvas `#F6F8FB`
- rail `#EEF2F7`
- panel `#FFFFFF`
- inset `#F3F5FA`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- design accent `#7C3AED`
- accessibility accent `#0EA5A4`
- cache/channel accent `#0F9D58`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono only for digests, contract IDs, route codes, and schema refs

Layout:
- sticky masthead with:
  - active manifest count
  - route-family coverage
  - live/posture state counts
  - drifted manifest count
- left rail (`300px`):
  - audience-surface filters
  - route-family groups
- center canvas:
  - one premium contract-braid diagram
  - manifest cards or table showing each surface tuple
- right inspector (`388px`):
  - selected manifest details
  - linked gateway / runtime bundle / design bundle / cache policy
  - browser posture explanation
- lower region:
  - query-command-channel-cache matrix
  - accessibility + automation profile matrix
  - drift / defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `580px`
- primary controls `44px`
- manifest cards `min-height 148px`
- inspector sections use `24px` spacing rhythm
- charts/diagrams must always have table parity immediately below or beside them

Motion:
- `120ms` hover/focus
- `180ms` manifest selection
- `220ms` inspector and braid emphasis transition
- reduced-motion support mandatory

Interaction requirements:
- filter by audience surface, shell type, route family, browser posture state, and drift state
- manifest cards and matrix rows must stay synchronized
- keyboard traversal across filters, manifest cards, and matrix rows
- deterministic `data-testid` markers for surface rail, braid canvas, manifest list, inspector, and defect strip
- every manifest card must expose digest snippets, posture state, and linked-contract counts without overwhelming the layout
- the studio must feel unique: a polished authority-view, not a generic table page

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- audience filtering
- manifest selection
- card/matrix parity
- inspector rendering
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks
- stable contract-digest markers in the DOM

Definition of done:
- Vecells has one canonical frontend-manifest strategy and machine-readable manifest catalog.
- Every browser-visible route family is bound to one active manifest row.
- Frontend, gateway, design, accessibility, automation, and runtime publication authority now converge through one explicit contract.
```