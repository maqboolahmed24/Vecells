# Prompt 083 For 083_par_phase0_track_backend_build_simulator_backplanes_for_nhs_login_im1_mesh_telephony_and_notifications

Use the shared operating contract first:
[shared_operating_contract_076_to_085.md](/Users/test/Code/V/prompt/shared_operating_contract_076_to_085.md)

```text
Execute roadmap task 083_par_phase0_track_backend_build_simulator_backplanes_for_nhs_login_im1_mesh_telephony_and_notifications.md.

Inherit the Shared_operating_contract_for_prompts_076_to_085 and consume the validated outputs from tasks `001-082`.

Mission:
Build the high-fidelity local simulator backplanes that let Vecells ship and verify the MVP before live onboarding is possible:
- NHS-login-like identity and redirect simulator
- IM1 / GP-system integration simulator
- MESH-style mailbox and message transport simulator
- telephony / IVR / recording / transcript simulator
- SMS and email notification simulator

This task is not a throwaway mock layer. It must preserve the same adapter contracts, replay semantics, callback timing classes, ambiguity states, and evidence shapes the live system will later use, so the MVP can prove end-to-end behavior without re-architecting later.

Algorithm grounding you must honor:
- `blueprint-init.md`, especially the rule that web, NHS App jump-off, and phone converge into one governed request pipeline
- `phase-0-the-foundation-protocol.md`, especially:
  - the canonical ingest, promotion, identity, grant, adapter replay, reachability, reservation, and closure rules
  - `#### 1.23B AdapterDispatchAttempt`
  - `#### 1.23C AdapterReceiptCheckpoint`
  - `#### 2.3 AccessGrantService`
  - `#### 2.3A AuthBridge`
  - `#### 2.4A ReachabilityGovernor`
- `phase-1-the-red-flag-gate.md`, which explicitly expects simulator-backed flows and placeholder content in early phases
- `phase-2-identity-and-echoes.md`, especially NHS-login-like redirect semantics, telephony continuation, recording / transcript readiness, and continuation grants
- `phase-4-the-booking-engine.md`, `phase-5-the-network-horizon.md`, and `phase-6-the-pharmacy-loop.md` where GP / supplier / transport / mailbox behaviors need realistic weak-confirmation and ambiguity paths
- `platform-runtime-and-release-blueprint.md`, especially dependency degradation profiles and simulator-backed integration verification
- `forensic-audit-findings.md`, especially Findings `66`, `72`, `89`, `90`, `99`, `100`, `101`, and `120`
- outputs from tasks `021-040`, `057`, `059`, `062-082`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must not depend on live provider credentials or live onboarding approvals. If an adapter contract is still incomplete, publish the smallest bounded contract draft and record `PARALLEL_INTERFACE_GAP_*`, but still deliver the simulator seam and fixtures.

Mock_now_execution:
Build the simulator stack now as a first-class product-development dependency.

For the NHS-login-like simulator:
- support browser redirect, state, nonce, PKCE, and callback replay classes
- expose configurable subject-claim payloads, age bands, NHS-number-like identifiers, contact claims, mismatch scenarios, and step-up / denial outcomes
- support post-auth return intents, callback expiry, and exact replay settlement

For the IM1 / GP simulator:
- simulate booking capability, slot search, hold, commit, reschedule, cancel, and ambiguous confirmation paths
- simulate patient or practice record lookup / update seams only where the existing adapter contracts require them
- emit realistic latency, conflict, timeout, retry, stale-read, and weak-confirmation cases

For the MESH-style simulator:
- simulate mailbox creation, outbox enqueue, message poll, download, receipt, rejection, duplicate correlation, and delayed acknowledgement
- preserve correlation keys, message hashes, retry states, and dead-letter cases

For the telephony simulator:
- simulate IVR pathing, menu choice, call sessions, callback intent, recordings, transcript readiness, transcript improvement, urgent-live flags, and continuation eligibility
- emit provider callbacks and webhooks with replay, duplication, and timing skew cases

For the notification simulator:
- simulate SMS and email accepted, delivered, bounced, delayed, blocked, opt-out, and disputed-delivery states
- drive `ReachabilityObservation` and related repair flows using the same evidence shapes production will consume later

Actual_production_strategy_later:
When live NHS login, principal-system / GP integrations, MESH mailboxes, telephony vendors, and notification vendors are onboarded, they must replace only the provider-facing adapter implementations and secrets. They must not change:
- canonical request, identity, grant, or route models
- adapter dispatch and receipt checkpoint semantics
- replay and collision handling
- weak-confirmation and ambiguity posture
- patient-facing degraded or recovery contracts
The simulator stack must therefore publish contract fixtures, sequence diagrams, and cutover checklists that make provider substitution an adapter exercise rather than a redesign.

Primary outcome:
The repository gains a realistic local integration substrate that allows the MVP to prove end-to-end flows, degraded states, and recovery paths without waiting for live provider approvals.

Implementation deliverables to create:
1. simulator service packages for NHS login, IM1 / GP integration, MESH transport, telephony, and notifications
2. local development orchestration for all simulator backplanes, preferably containerized and deterministic
3. seed fixture packs for test personas, GP sites, bookings, mailbox messages, call sessions, recordings, transcripts, notification outcomes, and delivery disputes
4. typed adapter-contract fixtures and stub SDK clients aligned to the canonical adapter profiles
5. webhook / callback generators with replay, duplication, delay, and drift controls
6. `docs/architecture/83_simulator_backplane_design.md`
7. `docs/architecture/83_mock_now_vs_actual_provider_cutover_strategy.md`
8. `docs/architecture/83_simulator_backplane_control_deck.html`
9. `data/analysis/simulator_contract_manifest.json`
10. `data/analysis/mock_to_live_cutover_checklist.csv`
11. `data/analysis/provider_behavior_casebook.json`
12. `tools/analysis/validate_simulator_backplanes.(py|ts)`
13. end-to-end test fixtures and contract tests spanning browser, API, callbacks, and webhooks
14. `tests/playwright/simulator-backplane-control-deck.spec.(ts|js)`
15. `tests/playwright/simulator-end-to-end-flows.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- configurable OIDC-like redirect and callback simulation with exact replay classes
- realistic GP / supplier capability, slot, hold, commit, and ambiguity simulation aligned to reservation and confirmation truth rules
- MESH-like mailbox and message transport with correlation, ack, delayed-ack, and duplicate-message semantics
- telephony call-session, IVR, recording, transcript, urgent-live, and continuation simulation with callback/webhook timing variation
- notification send and delivery simulation that can drive reachability and repair paths realistically
- unified simulator control plane for seeding scenarios, toggling delay / duplication / drift, and resetting state deterministically

Execution steps:
1. Read the external dependency inventory and adapter profile work from earlier tasks and map each simulator to a named adapter contract, replay class, and degraded-mode profile.
2. Implement each simulator as a bounded service with realistic request / response / callback semantics, not as static JSON fixtures only.
3. Publish typed fixture packs for nominal, degraded, ambiguous, replayed, and blocked scenarios.
4. Implement one local orchestrator / compose layer so the full simulator stack can boot, reset, seed, and run in CI or local development deterministically.
5. Expose a simulator admin and inspection surface through the control deck and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
6. Create cross-simulator end-to-end flows covering web intake, auth uplift, phone continuation, booking ambiguity, mailbox transport, and reachability repair.
7. Write cutover documentation that maps each simulator contract to the future live-provider adapter seam.

Mandatory gap closures you must perform:
- Close the “no MVP is possible until live provider access exists” gap by making simulators first-class, high-fidelity backplanes.
- Close the “mocks flatten ambiguity and replay semantics” gap by preserving real callback, duplicate, drift, timeout, and weak-confirmation classes.
- Close the “mock identity or transport flows bypass canonical grants and receipt checkpoints” gap by routing simulated behavior through the same control objects production will use.
- Close the “provider cutover requires redesign” gap by freezing adapter contracts now and binding simulators to those contracts.
- Close the “telephony, mailbox, and notification states stay outside canonical reachability and safety truth” gap by emitting evidence that the core services consume directly.

Industry-best-practice requirements:
- deterministic, containerized local integration environments
- contract-first simulator APIs and webhooks
- realistic latency, duplication, timeout, retry, and drift controls
- resettable seeded scenarios for CI repeatability
- no simulator-only control shortcuts that bypass canonical services
- observability hooks and logs that mirror provider-facing adapter events without leaking secrets or PHI

Non-negotiable rules:
- Simulators are required MVP infrastructure, not disposable mocks.
- Mock services must preserve canonical contracts, replay classes, ambiguity states, and degraded behavior.
- Live-provider cutover must be an adapter swap, not a model rewrite.
- Browser, API, callback, and webhook paths must all be testable end-to-end against the simulator stack.
- Any simulator UI or admin action must still keep canonical backend control services in the loop.

Mandatory UI and interaction specification for `83_simulator_backplane_control_deck.html`:
Name the surface mode `Simulator_Backplane_Control_Deck`.

Tone:
- premium developer-operations control deck
- energetic but disciplined
- should feel like a studio-grade integration console, not a toy mock server page

Branding:
- restrained Vecells wordmark plus inline SVG `SB` monogram
- one service-topology diagram linking all simulators to core platform surfaces
- one scenario-seed strip
- one delivery / callback / replay event timeline

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- identity accent `#2563EB`
- GP accent `#0EA5A4`
- mailbox accent `#7C3AED`
- telephony accent `#D97706`
- notification accent `#059669`
- error accent `#C24141`

Typography:
- system sans stack only
- monospaced digest styling for scenario IDs, callback correlation keys, mailbox message IDs, and token digests

Layout:
- sticky masthead with simulator health, seeded scenarios, replay injections, and blocked flows
- left rail (`332px`) with simulator-family filters, scenario filters, and failure-mode toggles
- central canvas with:
  - simulator topology diagram
  - scenario-seed strip
  - event timeline
- right inspector (`420px`) for selected simulator, scenario, callback, or transport message
- lower grid with one simulator-state table and one scenario / event table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- start / stop / reset / reseed actions for each simulator or the whole stack
- filter by simulator family, scenario family, replay mode, and failure mode
- selecting a simulator or event row must synchronize topology diagram, seed strip, timeline, inspector, and tables
- keyboard navigation across all controls and tables
- deterministic `data-testid` markers for topology diagram, seed strip, event timeline, simulator table, scenario table, and inspector
- diagrams must have textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- simulator startup, reset, reseed, and failure-mode toggles
- OIDC-like redirect flows and callback replay
- mailbox, telephony, and notification webhook flows
- synchronized selection behavior in the control deck
- keyboard navigation, reduced motion, responsiveness, and accessibility smoke checks

Definition of done:
- The repository has high-fidelity simulator backplanes for the critical external services needed to prove the MVP.
- Simulators preserve the same contracts and degraded semantics the live system will use later.
- End-to-end browser, API, callback, and webhook journeys can run entirely against the local simulator stack.
- Provider onboarding later can replace adapters and credentials without rewriting core product models or flows.
```
