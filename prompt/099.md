# Prompt 099 For 099_par_phase0_track_runtime_implement_runtime_topology_publication_checks_in_pipeline

Use the shared operating contract first:
[shared_operating_contract_096_to_105.md](/Users/test/Code/V/prompt/shared_operating_contract_096_to_105.md)

```text
Execute roadmap task 099_par_phase0_track_runtime_implement_runtime_topology_publication_checks_in_pipeline.md.

Inherit the Shared_operating_contract_for_prompts_096_to_105 and consume the validated outputs from tasks `001-098`.

Mission:
Implement the fail-closed pipeline and validator layer that proves the published runtime topology, gateway surfaces, manifests, and route bindings still match the approved runtime tuple.

This task must turn the `RuntimeTopologyManifest` from documentation into enforceable pipeline law.

This is real runtime and release-control work. Produce validators, pipeline stages, machine-readable drift reports, artifact publication checks, and automated tests. Do not allow topology, gateway exposure, trust-boundary reachability, or audience-surface publication to drift silently between IaC, runtime config, generated manifests, and released artifacts.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### RuntimeTopologyManifest`
  - `### GatewayBffSurface`
  - `### FrontendContractManifest`
  - `RuntimePublicationBundle`
  - `ReleasePublicationParityRecord`
  - pipeline and verification-ladder sections that require topology-aware runtime publication and fail-closed posture on drift
- `phase-0-the-foundation-protocol.md`, especially trust-zone, tenant isolation, route-intent, and freeze law
- outputs from tasks `041-047`, `050`, `084-091`, `094`, `096`, and `098`
- `forensic-audit-findings.md`, especially Findings `91`, `95`, `96`, `112`, `114`, and `118`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must publish the shared validation layer now so later design-contract publication, provenance, canary, governance, and readiness tasks can trust the runtime tuple. If a later task owns a richer shell or governance surface, publish the machine-checkable seam here and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build the full publication-check pipeline now against local and non-production topology manifests, gateway definitions, generated manifests, and preview artifacts.
- support intentional drift injection in CI and local rehearsal
- validate that browser and gateway publication fails closed on topology or trust-boundary mismatch
- use simulator-backed downstreams where external providers are not yet present
The mock-now path must mirror the later production publication checks so cutover is a data change, not a validator rewrite.

Actual_production_strategy_later:
When production topology and publishing are live, preserve the same validation graph, drift categories, and fail-closed behavior. Production hardening may add stronger attestation, private endpoint checks, or deployment-controller integration, but it must not allow topology or gateway exposure to become implicit knowledge outside the published tuple.

Primary outcome:
The repository gains a pipeline-enforced proof that runtime topology, gateway exposure, and browser publication all match one exact published runtime tuple.

Implementation deliverables to create:
1. `docs/architecture/99_runtime_topology_publication_validation.md`
2. `docs/architecture/99_gateway_surface_and_trust_boundary_check_matrix.md`
3. `docs/architecture/99_runtime_topology_publication_atlas.html`
4. `data/analysis/runtime_topology_publication_matrix.csv`
5. `data/analysis/runtime_topology_drift_catalog.json`
6. `data/analysis/gateway_surface_publication_matrix.json`
7. validator code that checks topology manifest, gateway surfaces, audience-surface bindings, frontend manifests, and runtime publication bundles together
8. CI/CD pipeline integration for fail-closed validation and artifact publication blocking
9. `tools/analysis/validate_runtime_topology_publication.(py|ts)`
10. unit, integration, and pipeline rehearsal tests for allowed and blocked publication scenarios
11. `tests/playwright/runtime-topology-publication-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- validate that every published `GatewayBffSurface` points to the current `RuntimeTopologyManifest`
- validate that every published browser surface and `FrontendContractManifest` points to the current gateway surface, runtime publication bundle, and topology tuple
- validate that workload-family membership, trust-zone boundaries, tenant isolation, service identities, and required assurance slices still match the approved topology manifest
- fail publication when route contracts, manifests, or bindings reference stale or withdrawn topology objects
- generate explicit drift categories such as:
  - missing manifest binding
  - gateway to undeclared workload
  - undeclared trust-boundary crossing
  - tenant-isolation mismatch
  - stale audience-surface runtime binding
  - design/publication bundle pointing at wrong topology tuple
- publish one deterministic drift report usable by pipeline, operations, and governance consumers

Implementation instructions:
1. Create a single validator graph that starts from the current `RuntimePublicationBundle` and walks outward to topology, gateway, frontend, and audience-surface objects.
2. Treat missing refs, stale refs, hash mismatch, and undeclared cross-boundary usage as distinct validation failures.
3. Validate both configuration intent and generated artifact reality. Do not trust source config if the emitted manifests or packages disagree.
4. Integrate validation into the pipeline before publication and again after artifact generation.
5. Support non-production rehearsal where topology drift can be injected intentionally and must be surfaced clearly.
6. Never allow the following anti-patterns:
   - gateway surface exists in code but not in topology manifest
   - browser manifest is published although its gateway surface points across an undeclared trust boundary
   - design or frontend contract bundles remain live after topology drift
   - route writability remains live because application code compiled even though publication parity is stale
7. Publish one browser-viewable topology-publication atlas that shows current runtime tuple, gateway split, audience-surface bindings, and any drift findings from the same machine-readable dataset.

Gap handling you must perform during this task:
- If the current repo lacks a generated manifest or binding that the validator expects, emit `GAP_PUBLICATION_OBJECT_*` and fail closed.
- If one topology or gateway concept from earlier tasks is still represented only in docs, generate the machine-readable artifact now and record `GAP_RESOLUTION_TOPOLOGY_ARTIFACT_*`.
- If a later task owns a richer parity surface, expose the validator output here and record `FOLLOW_ON_DEPENDENCY_PARITY_SURFACE_*`.

Backend engineering standards:
- deterministic validation order and canonical error codes
- fast-fail in CI with machine-readable output for downstream consumers
- support local developer invocation and pipeline invocation with the same validator core
- expose metrics for drift category frequency, blocked-publication rate, and binding completeness

Verification requirements:
- positive-path publication tests
- negative-path drift tests for each drift category
- pipeline-fail tests
- artifact-reality versus source-config mismatch tests
- `Playwright_or_other_appropriate_tooling` tests for the atlas view, including stable selectors, keyboard access, table fallback, and readable drift-state presentation

Definition of done:
- runtime topology publication can fail closed automatically on drift
- topology, gateway, and browser publication are checked as one graph, not as unrelated files
- engineers and reviewers can inspect the current publication graph from one atlas surface
- later provenance, readiness, canary, and design-publication work can rely on exact topology publication proof
```
