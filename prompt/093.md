# Prompt 093 For 093_par_phase0_track_runtime_build_observability_sdk_and_edge_correlation_spine

Use the shared operating contract first:
[shared_operating_contract_086_to_095.md](/Users/test/Code/V/prompt/shared_operating_contract_086_to_095.md)

```text
Execute roadmap task 093_par_phase0_track_runtime_build_observability_sdk_and_edge_correlation_spine.md.

Inherit the Shared_operating_contract_for_prompts_086_to_095 and consume the validated outputs from tasks `001-092`.

Mission:
Build the shared observability SDK and authoritative edge-correlation spine for Vecells.

This task must create the real cross-runtime observability foundation for:
- immutable `edgeCorrelationId` issuance at the edge
- propagation through browser, gateway, command handler, event bus, worker, projection, UI visibility receipt, and audit
- PHI-safe structured logs, traces, metrics, and UI event envelopes
- continuity, restore, stale, replay, and settlement reconstruction
- deterministic correlation across simulator-backed and future live-provider flows

This is real runtime and shared-library work. Produce shared SDK code, middleware, instrumentation packages, schemas, validators, and tests. Do not leave correlation, telemetry naming, or PHI redaction as handler-local habit.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `## 0G. Observability, security plumbing, and operational controls`
  - the immutable `edgeCorrelationId` rule
  - UI event envelope, `UIEventCausalityFrame`, `UITransitionSettlementRecord`, `UIProjectionVisibilityReceipt`, `UIEventEmissionCheckpoint`, and disclosure-fence rules
  - the requirement that continuity, restore, recovery, stale, and selected-anchor replay remain reconstructable from PHI-safe telemetry
- `platform-runtime-and-release-blueprint.md`, especially the operational-readiness and assurance-trust language where health, degradation, and publication posture must be observable from current runtime tuples
- `platform-frontend-blueprint.md`, `patient-portal-experience-architecture-blueprint.md`, and `staff-operations-and-support-blueprint.md` where same-shell continuity, replay, restore, and staff or support trust envelopes require deterministic observability
- outputs from tasks `004`, `046`, `048`, `065`, `084`, `087`, `088`, and `089`
- `forensic-audit-findings.md`, especially Findings `86`, `87`, `91`, `92`, `96`, `100`, `102`, `105`, and `112`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must provide the shared observability substrate now, but it must not steal ownership of later operations consoles, assurance-ledger graph work, or final release-watch policy. Publish shared SDKs, schemas, and bounded seams only.

Mock_now_execution:
Build the full observability path now using mock and simulator-backed flows.
- local and non-production environments must mint and propagate real `edgeCorrelationId` values through browser, gateway, commands, events, projections, and audits
- observability data must be PHI-safe even when working against synthetic preview environments and local simulators
- replay, restore, and stale-posture drills must emit the same causality and settlement structures production will require
The mock-now path is not disposable: it is the authoritative rehearsal path for the observability spine.

Actual_production_strategy_later:
When production observability backends and export destinations are finalized, they must preserve the same event schemas, correlation law, disclosure fencing, and PHI-safe field boundaries. Hardening may add managed telemetry backends, SIEM export, or retention policies, but it must not change the authoritative meaning of correlation or allow raw PHI leakage into logs, traces, or metrics.

Primary outcome:
The repository gains one shared observability SDK and correlation spine that all later services, shells, workers, and runtime controls can use consistently.

Implementation deliverables to create:
1. shared SDK packages for browser, gateway, service, worker, and job instrumentation
2. edge middleware and service middleware that mint, propagate, and validate `edgeCorrelationId`, `causalToken`, and related trace context
3. PHI-safe structured logging, metrics, and trace helpers with redaction fences and safe field classes
4. UI event and settlement instrumentation helpers aligned to the canonical Phase 0 UI event envelope model
5. local-development and CI exporters or sinks for deterministic test assertions and replay inspection
6. `docs/architecture/93_observability_sdk_and_edge_correlation_design.md`
7. `docs/architecture/93_phi_safe_telemetry_and_causality_rules.md`
8. `docs/architecture/93_edge_correlation_spine_explorer.html`
9. `data/analysis/observability_event_schema_manifest.json`
10. `data/analysis/correlation_propagation_matrix.csv`
11. `data/analysis/telemetry_redaction_policy.json`
12. `tools/analysis/validate_observability_sdk_and_correlation.(py|ts)`
13. SDK tests, middleware tests, browser-to-worker correlation tests, redaction tests, and replay reconstruction smoke tests
14. `tests/playwright/edge-correlation-spine-explorer.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- mint one immutable `edgeCorrelationId` at the edge and propagate it through browser, gateway, command, event, worker, projection, UI receipt, and audit layers
- provide typed telemetry helpers that prevent raw PHI emission outside approved safe classes
- implement UI-event and causality instrumentation for continuity, restore, stale, selected-anchor, command-following, and projection-visibility flows
- publish machine-readable event schema manifests and redaction policy manifests from code
- provide deterministic local sinks so tests can assert end-to-end correlation and disclosure fencing
- support replay-safe and restore-safe observability for both patient and staff surfaces

Execution steps:
1. Re-read the Phase 0 observability, UI-event, and disclosure-fence sections, then derive shared SDK and middleware requirements.
2. Honor the runtime topology, broker, gateway, cache, and secret baselines from earlier tasks; if prerequisites are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
3. Implement shared instrumentation packages and middleware for edge, backend, worker, and browser contexts.
4. Implement typed telemetry event schemas, redaction helpers, and safe emission sinks.
5. Add local exporters and deterministic test sinks for CI and preview verification.
6. Build the explorer and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write smoke tests proving a single correlation chain can be reconstructed across browser, gateway, command, event, worker, projection, UI receipt, and audit artifacts without leaking PHI.

Mandatory gap closures you must perform:
- Close the “correlation is handler-local” gap by minting and propagating one authoritative edge correlation spine.
- Close the “browser telemetry and backend telemetry are separate stories” gap by using one shared event and causality model.
- Close the “PHI-safe logging is a policy note” gap by building typed redaction and emission helpers.
- Close the “restore and stale posture are not reconstructable” gap by emitting continuity and replay-safe causality frames.
- Close the “local and preview environments do not test real observability” gap by providing deterministic local sinks and assertions.

Industry-best-practice requirements:
- immutable correlation IDs and structured logs
- typed event schemas and redaction policy enforcement
- shared instrumentation packages rather than ad hoc logging calls
- deterministic test sinks and replay inspection
- no raw PHI in logs, traces, or metrics beyond approved safe descriptors
- fail-closed behavior when required correlation context is absent on protected paths

Non-negotiable rules:
- `edgeCorrelationId` is minted once at the edge.
- Correlation must survive browser, gateway, command, event, worker, projection, UI, and audit hops.
- Telemetry must remain PHI-safe.
- UI restore, replay, stale, and selected-anchor behavior must be reconstructable from the emitted data.
- Observability may not invent business truth; it must observe the authoritative chain.

Mandatory UI and interaction specification for `93_edge_correlation_spine_explorer.html`:
Name the surface mode `Edge_Correlation_Spine_Explorer`.

Tone:
- premium trace-intelligence atlas
- minimalist, elegant, and deeply technical
- should feel like a luxury forensic timeline, not a generic APM dashboard

Branding:
- restrained Vecells wordmark plus inline SVG `EC` monogram
- one end-to-end causality river
- one telemetry disclosure fence matrix
- one restore and settlement timeline strip

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- trace accent `#2563EB`
- browser accent `#0EA5A4`
- worker accent `#7C3AED`
- redaction accent `#D97706`
- blocked accent `#C24141`
- verified accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for correlation IDs, causal tokens, envelope hashes, and settlement refs

Layout:
- sticky masthead with active traces, redaction warnings, missing-correlation alerts, and replay-health indicators
- left rail (`324px`) with environment filters, hop filters, event-family filters, and disclosure-state filters
- central canvas with:
  - end-to-end causality river
  - disclosure-fence matrix
  - restore / settlement timeline strip
- right inspector (`420px`) for selected trace, hop, or disclosure fence
- lower grid with one event table and one redaction / policy table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by environment, hop type, event family, disclosure state, and replay state
- selecting a row must synchronize river, matrix, timeline, inspector, and tables
- keyboard navigation across filters and tables
- deterministic `data-testid` markers for causality river, disclosure matrix, settlement timeline, event table, policy table, and inspector
- every visual must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification
- verification that missing-correlation and blocked-disclosure states are visibly distinct from verified states

Definition of done:
- The repository has one shared observability SDK and edge-correlation spine.
- Correlation and PHI-safe telemetry are consistent across browser and backend layers.
- Restore, replay, stale, and settlement behavior are reconstructable from emitted data.
- Later ops, assurance, and release tracks can build on this observability substrate instead of improvising instrumentation.
```
