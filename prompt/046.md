# Prompt 046 For 046_seq_phase0_define_runtime_topology_manifest_and_workload_families

Use the shared operating contract first:
[shared_operating_contract_046_to_055.md](/Users/test/Code/V/prompt/shared_operating_contract_046_to_055.md)

```text
Execute roadmap task 046_seq_phase0_define_runtime_topology_manifest_and_workload_families.md.

Inherit the Shared_operating_contract_for_prompts_046_to_055 and consume the validated outputs from tasks `001-045`.

Mission:
Define the canonical Vecells runtime-topology contract for Phase 0 by publishing:
- the workload-family model
- the environment-scoped runtime topology manifest
- the explicit failure-domain and egress posture
- the linkage back to the repo topology, bounded-context ownership, and release/publication law

This task must convert runtime topology from implied infrastructure intent into typed, machine-readable product authority that later runtime, gateway, publication, and release tasks can consume without guesswork.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`
  - `## Runtime topology contract`
  - `### RuntimeWorkloadFamily`
  - `### TrustZoneBoundary`
  - `### RuntimeTopologyManifest`
  - the rules stating that only published gateway surfaces may bridge browser traffic beyond `public_edge` and `shell_delivery`
- `phase-0-the-foundation-protocol.md`
  - `0A. Delivery skeleton and repository architecture`
  - bounded-context rules `1-10`
  - the rule that apps and workload services may compose context packages but may not own sibling internals
  - the rule that no app owns truth
  - the requirement that analytics, assurance, audit, support, operations, governance, and release-control remain explicit contexts rather than sidecars
- `blueprint-init.md`
  - `### 12. Practical engineering shape`
  - the modular platform rule and canonical app/service shape
- `phase-cards.md`
  - the hardening note that release foundation must freeze artifacts, bundle hashes, schemas, migrations, and environment compatibility as one promotable unit
- `forensic-audit-findings.md`
  - findings `91`, `95`, `96`, `102-106`, and any topology/publication/trust drift note that shows why runtime and watch surfaces must consume one published topology truth
- outputs from tasks `011-015` and `041-045`

Primary outcome:
One approved runtime-topology model that later tasks can use to:
- place services into explicit workload families
- enforce trust-zone boundaries
- derive gateway-surface legality
- bind runtime publication and release tuples
- prove which data classes, tenant modes, assurance modes, and egress paths are legal per runtime family

Deliverables to create:
1. `docs/architecture/46_runtime_topology_manifest_strategy.md`
2. `docs/architecture/46_workload_family_catalog.md`
3. `docs/architecture/46_failure_domain_and_egress_policy.md`
4. `docs/architecture/46_runtime_topology_atlas.html`
5. `docs/architecture/46_runtime_topology_edges.mmd`
6. `data/analysis/runtime_workload_families.json`
7. `data/analysis/runtime_topology_manifest.json`
8. `data/analysis/runtime_topology_edges.csv`
9. `data/analysis/runtime_failure_domains.csv`
10. `tools/analysis/validate_runtime_topology_manifest.(py|ts)`
11. `tests/playwright/runtime-topology-atlas.spec.(ts|js)`

Your topology artifacts must model at minimum these workload families unless the corpus plus tasks `011-015` prove a safer split:
- `public_edge`
- `shell_delivery`
- `command`
- `projection`
- `integration`
- `data`
- `assurance_security`

For each `RuntimeWorkloadFamily`, capture at minimum:
- `runtimeWorkloadFamilyId`
- `familyCode`
- `trustZoneRef`
- `ownedBoundedContextRefs[]`
- `ownedServiceRefs[]`
- `ingressMode`
- `allowedDownstreamFamilyRefs[]`
- `allowedDataClassificationRefs[]`
- `tenantContextMode`
- `assuranceTrustMode`
- `egressAllowlistRef`
- `serviceIdentityRef`
- `failureDomainRef`
- `declaredAt`
- source refs and rationale

For the `RuntimeTopologyManifest`, capture at minimum:
- environment ring or environment identifier
- workload family refs
- trust-zone boundary refs
- gateway surface refs (stubs are acceptable here; task `047` will refine them)
- ingress refs
- service identity refs
- data store refs
- queue refs
- egress allowlist refs
- tenant isolation mode
- release approval freeze linkage placeholder
- channel-manifest set placeholder
- minimum bridge capability set placeholder
- required assurance slices
- topology tuple hash
- publication state
- approved timestamp or pending publication timestamp

Execution steps:
1. Start from tasks `011-015` and `041-045`:
   - cloud region / trust-zone baseline
   - backend runtime baseline
   - service scaffold map
   - repo topology manifest
   - domain/shared package ownership
2. Enumerate every canonical service and runtime concern currently frozen by the repo:
   - gateway ingress
   - command handling
   - projection consumption
   - notification dispatch
   - adapter simulators
   - data stores
   - queues
   - secrets / KMS boundaries
   - observability / assurance / security infrastructure
3. Map each service or runtime concern into exactly one workload family.
4. Publish one workload-family catalog and justify every split or combination decision using runtime-truth criteria from the blueprint:
   - ingress mode
   - tenant context mode
   - data classification
   - assurance trust handling
   - egress policy
   - failure domain
5. Publish one topology manifest per environment ring that later tasks can refine. At minimum, cover:
   - local development
   - preview / ephemeral
   - integration or staging
   - production
   If the earlier baseline chose different ring names, keep those names but preserve the same semantic separation.
6. Define failure domains explicitly. A deployment-unit coincidence is not enough; the manifest must explain which outages are allowed to remain local and which must not spill into other planes.
7. Define egress allowlist posture per family, even if the actual CIDR/hostname sets are placeholders at this stage.
8. Build the atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
9. Validate:
   - every service belongs to exactly one workload family
   - every workload family belongs to exactly one trust zone
   - no browser-facing flow reaches command, projection, integration, or data workloads except through a published gateway surface placeholder
   - no workload family mixes materially different tenant-context, assurance-trust, or egress behavior without an explicit split rationale

Mandatory gap closures you must perform:
- Close the “workload families are named but not typed contracts” gap from the runtime blueprint.
- Close the “trust-zone, service identity, and egress posture live in infra folklore” gap by publishing them in the manifest.
- Close the “gateway can bridge anywhere because the topology is not explicit” gap by pre-binding allowed cross-family flows now.
- Close the “degraded dependency fallout is unbounded” gap by recording which workloads each dependency failure is allowed to affect.
- Close the “repo topology and runtime topology are separate stories” gap by binding services and packages back to one runtime family model.
- Close the “support, governance, operations, and assurance are optional sidecars” gap by giving them runtime homes and trust posture now.

Industry-best-practice requirements:
- least-privilege service identity separation
- explicit internal vs browser ingress separation
- explicit tenant and environment scoping
- explicit failure-domain modeling
- explicit egress policy rather than “allow all”
- machine-readable topology tuple hashes
- deterministic manifests suitable for CI verification
- documentation plus validator, not documentation instead of validator

Non-negotiable rules:
- `public_edge` and `shell_delivery` may terminate browser traffic or serve shells, but they may not directly become undeclared command, projection, integration, or data runtimes.
- Browser reachability past the edge must later occur only through published gateway surfaces; this task must preserve that law.
- If ingress mode, tenant context, assurance trust handling, or egress policy differs materially, split the workload family rather than hiding the difference in config.
- No service may sit in two workload families.
- No topology fact required by shells, operations, governance, or release may live only in a slide, README, or infra code comment.

Mandatory UI and interaction specification for `46_runtime_topology_atlas.html`:
Name the surface mode `Runtime_Topology_Atlas`.

Tone:
- minimalist premium internal engineering console
- exact, calm, trust-heavy, not devtool-noisy
- this should feel like a canonical runtime instrument, not a generic network diagram page

Branding:
- include a restrained Vecells wordmark plus a compact inline SVG `RTM` monogram
- use a monoline zone legend instead of decorative cloud icons

Palette:
- canvas `#F6F8FB`
- rail `#EEF2F7`
- panel `#FFFFFF`
- inset `#F3F5FA`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- trust accent `#0EA5A4`
- warning accent `#C98900`
- blocked accent `#C24141`
- shared/system accent `#7C3AED`

Typography:
- system sans stack only
- mono only for family codes, tuple hashes, policy IDs, and manifest digests

Layout:
- sticky masthead with:
  - environment selector
  - workload family count
  - trust-zone count
  - blocked-crossing count
  - topology tuple hash snippet
- left rail (`300px`):
  - workload-family cards
  - filter chips for ingress mode, tenant mode, assurance mode
- center canvas:
  - one refined trust-zone diagram
  - one compact failure-domain overlay
- right inspector (`360px`):
  - selected family details
  - service membership
  - allowed downstream families
  - allowed data classes
  - egress posture
- lower section:
  - topology edge table
  - manifest tuple/details table
  - defect strip

Dimensional constraints:
- max width `1480px`
- masthead height `72px`
- graph canvas min height `580px`
- family cards `min-height 112px`
- primary controls `44px`
- inspector chips `28px`

Motion:
- `120ms` hover/focus
- `180ms` family selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by environment, trust zone, workload family, tenant mode, and defect state
- keyboard navigation across workload-family cards and edge-table rows
- graph/table parity must stay synchronized
- every rendered family card and every edge row must expose stable `data-testid` markers
- every diagram must have adjacent table/list parity; the diagram may not carry unique meaning unavailable elsewhere

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- environment switching
- family filtering
- graph-node selection
- graph/table parity
- inspector rendering
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks

Definition of done:
- Vecells has one canonical workload-family catalog and one runtime-topology manifest family for the chosen environment rings.
- Every current service and runtime concern resolves to exactly one workload family.
- The topology is machine-readable, validator-backed, and ready for trust-zone and gateway refinement in task `047`.
```