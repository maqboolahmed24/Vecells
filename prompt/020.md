# Prompt 020 For 020_seq_approve_phase_0_entry_criteria_and_foundation_gate

Use the shared operating contract first:
[shared_operating_contract_016_to_020.md](/Users/test/Code/V/prompt/shared_operating_contract_016_to_020.md)

```text
Execute roadmap task 020_seq_approve_phase_0_entry_criteria_and_foundation_gate.md.

Inherit the Shared_operating_contract_for_prompts_016_to_020 and consume the validated outputs from tasks 001-019.

Mission:
Define and evaluate the objective entry criteria for Phase 0 and produce the formal Foundation Gate pack.

This task is the bridge between planning / architecture work and executable Phase 0 implementation work. It must answer:
- what must be true before Phase 0 implementation tasks may begin
- what artifacts prove that readiness
- what blockers prevent entry
- how the internal Phase 0 sub-phases should gate one another
- what verdict should be issued now: `approved`, `conditional`, or `withheld`

Do not rubber-stamp approval because the task title says “approve”. Approval must be evidence-driven. If the evidence set does not satisfy the gate rules, emit `withheld` or `conditional` with exact blockers.

Algorithm grounding you must honor:
- `phase-cards.md`, especially:
  - `## Card 1: Phase 0 - The Foundation Protocol`
  - the foundation-card defect list
  - platform work
  - parallel assurance tracks
  - front-end scope
  - the Phase 0 algorithm and test list
- `phase-0-the-foundation-protocol.md`, especially:
  - `## 0A. Delivery skeleton and repository architecture`
  - `## 0B. Canonical domain kernel and state machine`
  - the canonical governors
  - adapter boundaries and degradation substrate
  - runtime / release / design-contract publication substrate
  - the required tests and exit states
  - `## 0G. Observability, security plumbing, and operational controls`
  - the statement that Phase 0 is seven internal sub-phases with hard gates
- `platform-runtime-and-release-blueprint.md`, especially:
  - verification ladder
  - runtime publication
  - release watch and recovery posture
  - operational readiness contract
- `platform-admin-and-config-blueprint.md`, especially:
  - config compilation pipeline
  - production promotion gate
  - dependency and standards hygiene
- `phase-9-the-assurance-ledger.md`, especially:
  - `PhaseConformanceRow`
  - `CrossPhaseConformanceScorecard`
  - BAU and formal exit-gate posture
- all prerequisite task outputs from 001-019, especially:
  - requirement registry
  - reconciliation / conformance seed
  - scope boundary and deferred baseline
  - persona / channel / shell inventory
  - request-lineage model
  - glossary / object / state / invariant packs
  - external dependency inventory
  - assurance / privacy / safety workstreams
  - topology / runtime / frontend / tooling baselines
  - target ADR set
  - milestone / merge-gate graph
  - risk register and dependency watchlist
  - requirement-to-task traceability map
- `forensic-audit-findings.md`, especially:
  - the findings that show why Phase 0 must include control-plane truth, continuity proof, design-contract publication, trust posture, recovery posture, and degraded-mode law from the start

Primary outcome:
One machine-readable and human-readable foundation-gate decision pack that later agents can use to determine whether Phase 0 may begin and what evidence must remain current while it proceeds.

Deliverables to create:
1. `docs/programme/20_phase0_entry_criteria_and_foundation_gate.md`
2. `docs/programme/20_phase0_readiness_matrix.md`
3. `docs/programme/20_phase0_gate_verdict_and_blockers.md`
4. `docs/programme/20_phase0_evidence_pack_index.md`
5. `docs/programme/20_phase0_foundation_gate_runbook.md`
6. `docs/programme/20_phase0_foundation_gate_cockpit.html`
7. `docs/programme/20_phase0_foundation_gate_sequence.mmd`
8. `data/analysis/phase0_entry_criteria_matrix.csv`
9. `data/analysis/phase0_gate_blockers.csv`
10. `data/analysis/phase0_evidence_index.json`
11. `data/analysis/phase0_gate_verdict.json`
12. `data/analysis/phase0_subphase_gate_map.csv`
13. `tools/analysis/build_phase0_gate_cockpit.(py|ts)`
14. `tools/analysis/validate_phase0_foundation_gate.(py|ts)`

Each entry-criteria row must capture at minimum:
- `criterion_id`
- `criterion_title`
- `criterion_class = source_truth | scope | architecture | dependency_readiness | assurance_readiness | privacy_security | runtime_baseline | frontend_baseline | tooling_baseline | risk_posture | traceability | conformance`
- `required_artifact_refs[]`
- `required_requirement_ids[]`
- `required_task_refs[]`
- `required_dependency_refs[]`
- `required_risk_refs[]`
- `required_conformance_refs[]`
- `auto_check_rule`
- `manual_review_rule`
- `status = pass | review_required | blocked`
- `blocker_if_failed`
- `notes`

Each gate-verdict payload must capture at minimum:
- `gate_id`
- `gate_title`
- `scope = phase0_entry | phase0_internal_subphase | phase0_parallel_open`
- `evaluated_at`
- `evaluated_artifact_refs[]`
- `blocking_criterion_refs[]`
- `warning_criterion_refs[]`
- `verdict = approved | conditional | withheld`
- `reason`
- `next_required_actions[]`
- `notes`

Execution steps:
1. Start from the milestone graph and Phase 0 card. Separate:
   - pre-Phase-0 planning readiness
   - Phase 0 entry gate
   - internal Phase 0 sub-phase gates
   - the gate that opens the parallel foundation tracks later in the roadmap
2. Build the entry criteria matrix from prior task outputs. At minimum include these criterion families:
   - canonical source and terminology readiness
   - product scope and deferred-scope clarity
   - persona/channel/shell inventory completeness
   - request-lineage / object / state / invariant completeness
   - external dependency and simulator strategy readiness
   - safety / privacy / regulatory workstream readiness
   - cloud / runtime / backend / frontend / tooling baseline readiness
   - target architecture ADR readiness
   - milestone / merge-gate readiness
   - risk / dependency watch readiness
   - requirement-to-task traceability readiness
3. Build the evidence pack index. Every criterion must point to concrete artifacts, not broad statements like “architecture done”.
4. Evaluate the current evidence set and produce a verdict:
   - `approved` only if all blocking criteria pass
   - `conditional` only if no hard blocker remains and the remaining issues are explicitly bounded, time-bound, and non-foundational
   - `withheld` if any foundational criterion is blocked
5. Define the Phase 0 internal gate map. Even though implementation has not happened yet, you must define the objective gates for:
   - entry into 0A
   - exit from 0A / entry into the next internal foundation segment
   - readiness to expose canonical governors and shell scaffolding
   - readiness to open parallel foundation tracks later in the roadmap
6. Make the gate evidence-driven. At minimum require Phase 0 entry to prove:
   - architecture decisions frozen
   - scope baseline frozen
   - object / state / invariant model frozen enough to support implementation
   - dependency inventory and simulator strategy defined
   - safety / privacy / regulatory workstreams active
   - runtime / frontend / tooling baselines chosen
   - risk posture understood and blocking risks explicit
   - traceability established
7. Add validator logic that fails when:
   - a blocking criterion has no linked artifact
   - an entry criterion depends on deferred-only Phase 7 artifacts without an explicit waiver
   - the gate verdict is `approved` while any blocking criterion is `blocked`
   - the evidence index references stale, missing, or contradictory upstream artifacts
   - the internal gate map omits the published control-plane obligations that Phase 0 must establish first

Mandatory gate criteria you must include:
- source algorithm ingested and reconciled
- canonical scope and deferred scope frozen
- persona/channel/audience/shell inventory frozen
- request-lineage, endpoint, object, state, and invariant packs available
- external dependency inventory and simulator plan available
- safety, privacy, and regulatory workstreams opened
- cloud / runtime / backend / frontend / tooling baselines chosen
- target architecture ADR set frozen
- milestone and merge-gate model available
- master risk register and dependency watchlist available
- requirement-to-task traceability available

Mandatory Phase 0 readiness checks you must encode from the source algorithm:
- `LifecycleCoordinator` ownership is explicit
- adapter boundaries include idempotency, capability, inbox/outbox, and degradation posture
- shell scaffolding is governed by persistent-shell and visibility law, not prototype freedom
- unhappy-path invariants such as duplicate replay, quarantine, fallback review, repair hold, and confirmation-gate blocking are planned from the start
- release candidate freeze and publication parity are part of the foundation, not late hardening
- design-contract publication is part of the foundation, not a sidecar
- route-intent binding and command settlement are foundation concerns, not late UI concerns
- trust slices, watch tuples, and recovery posture are foundation concerns, not Phase 9-only concerns

Mandatory gap closures you must perform:
- Close the “Phase 0 readiness is implied but not objectively testable” gap.
- Close the “entry approval can be given by prose confidence rather than evidence” gap.
- Close the “internal Phase 0 gates are mentioned but not operationalized” gap.
- Close the “foundational control-plane obligations can be deferred behind feature work” gap.
- Close the “deferred Phase 7 artifacts might accidentally block current Phase 0 entry” gap.
- Close the “risk, dependency, and traceability posture is not consumed by the gate” gap.
- Close the “same-shell continuity, design-contract publication, route settlement, and trust posture might be treated as later polish” gap.

Non-negotiable gate rules you must encode:
- no Phase 0 entry approval without explicit source-truth, scope, architecture, dependency, and assurance readiness
- no green verdict while a foundational blocker remains
- no use of deferred Phase 7 artifacts as proof of current-baseline readiness
- no entry approval based only on narrative confidence, deck screenshots, or checklist completion
- no internal Phase 0 sub-phase gate may omit release / publication / trust / continuity obligations once the source algorithm requires them
- no gate cockpit may display optimistic green posture while blocking criteria remain unresolved

Mandatory UI and interaction specification for `20_phase0_foundation_gate_cockpit.html`:
The cockpit should feel like a premium readiness-review board: calm, exact, and consequence-aware.

Visual direction:
- name the visual mode `Foundation_Gateboard`
- tone: premium, formal, trust-heavy, review-ready
- base palette:
  - canvas `#F5F7FA`
  - shell `#FFFFFF`
  - inset `#EEF2F6`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#475467`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - ready accent `#0F9D58`
  - conditional accent `#C98900`
  - blocked accent `#C24141`
  - gate accent `#335CFF`
  - evidence accent `#6E59D9`
- typography: system-sans stack with mono role for IDs / hashes / timestamps only
- layout:
  - top verdict banner with exact verdict, blocking count, warning count, and evidence freshness digest
  - left criteria rail grouped by criterion class
  - center readiness matrix and blocker table
  - lower evidence-index table and sub-phase gate map
  - right inspector for selected criterion, blocker, or evidence item
- dimensional constraints:
  - readiness matrix min height `420px`
  - blocker table min height `320px`
  - evidence table min height `320px`
  - inspector width `360-440px`
  - status chips `28px` height
- motion:
  - `120ms` hover/focus
  - `180ms` tab / filter transitions
  - `240ms` inspector transitions
  - reduced motion support mandatory
- charts:
  - include at most one readiness distribution chart only if table parity exists
- branding:
  - understated inline-SVG Vecells monogram / wordmark in the header
- DOM markers:
  - `data-testid="gate-verdict-banner"`
  - `data-testid="criteria-rail"`
  - `data-testid="readiness-matrix"`
  - `data-testid="blocker-table"`
  - `data-testid="evidence-index-table"`
  - `data-testid="subphase-gate-map"`
  - `data-testid="gate-inspector"`
  - stable `data-criterion-id`, `data-verdict`, `data-blocker-state`, and `data-evidence-ref` markers

Playwright_or_other_appropriate_tooling requirements:
Use `Playwright_or_other_appropriate_tooling` as a first-class development and testing driver for the cockpit. Minimum required coverage:
- offline load
- keyboard navigation across criteria rail, matrix, tables, and inspector
- filter by criterion class and status
- open inspector from criterion row, blocker row, and evidence row
- responsive behavior at `768px`, `1024px`, and `1440px`
- reduced-motion mode
- stable DOM markers for criteria, blockers, verdict banner, and evidence rows
- matrix/table parity checks
- accessibility smoke checks
- no remote assets

Definition of done:
- Vecells has one objective Phase 0 entry-criteria model and one machine-readable gate decision pack.
- The gate verdict is evidence-driven rather than narrative.
- Phase 0 internal sub-phase gates are explicit enough to guide later implementation blocks.
- Later agents can determine whether Phase 0 may begin and what exactly still blocks it.
```
