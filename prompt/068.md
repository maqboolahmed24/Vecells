# Prompt 068 For 068_par_phase0_track_backend_implement_identity_binding_access_grant_and_supersession_models

Use the shared operating contract first:
[shared_operating_contract_066_to_075.md](/Users/test/Code/V/prompt/shared_operating_contract_066_to_075.md)

```text
Execute roadmap task 068_par_phase0_track_backend_implement_identity_binding_access_grant_and_supersession_models.md.

Inherit the Shared_operating_contract_for_prompts_066_to_075 and consume the validated outputs from tasks `001-067`.

Mission:
Implement the authoritative append-only models and persistence layer for identity binding and grant-scoped access:
- `IdentityBinding`
- `PatientLink`
- `AccessGrant`
- `AccessGrantScopeEnvelope`
- `AccessGrantRedemptionRecord`
- `AccessGrantSupersessionRecord`
plus any minimal authority-service interfaces required so later tasks can orchestrate these models without redefining them.

This task is model and persistence implementation, not the full orchestration service from task `078`. However, the models must already encode the invariants that later services will be forced to honor.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.4 IdentityBinding`
  - `#### 1.4A PatientLink`
  - `#### 1.6 AccessGrant`
  - `#### 1.6A AccessGrantScopeEnvelope`
  - `#### 1.6B AccessGrantRedemptionRecord`
  - `#### 1.6C AccessGrantSupersessionRecord`
  - `#### 2.2A IdentityBindingAuthority`
  - `#### 2.3 AccessGrantService`
  - `### 6. Unified AccessGrant and secure-link rules`
- `phase-2-identity-and-echoes.md`, especially:
  - the sections declaring `IdentityBindingAuthority` the sole serializer of binding versions
  - the rules that auth callbacks, telephony verification, support correction, and imports may produce evidence but may not mutate `patientRef` directly
  - the sections stating capability is not permission and writable routes must also hold a live `RouteIntentBinding`
  - the claim / uplift / rotation rules for grants and redemption
- `patient-account-and-communications-blueprint.md` for patient action scope and same-shell recovery expectations
- `forensic-audit-findings.md`, especially Findings `06`, `07`, `50`, and related identity / grant corrections
- outputs from tasks `001-007`, `019`, `041-045`, `054-057`, `061-067`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Do not depend on task `078` already existing. Publish only the smallest bounded interfaces or ports that later orchestration will need.

Mock_now_execution:
Build the identity-and-grant model layer now using synthetic but realistic evidence sources:
- simulated NHS-login-like subject claims
- simulated telephony verification outcomes
- synthetic public secure-link entry tokens
- synthetic claim-step-up and contact-route-bound grants
The models must behave as if those providers were real: append-only binding versions, one-time or rotating grant replay policy, exact-once redemption records, and authoritative supersession.

Actual_production_strategy_later:
When live NHS login, telephony, embedded return, support reissue, or secure-link delivery are fully onboarded, they must all resolve through the same `IdentityBinding` and `AccessGrant` models. Live subject claims, phone verification evidence, or delivery providers may change the evidence inputs, but they may not widen grant scope, skip redemption records, or overwrite binding state directly.

Primary outcome:
The repository gains real append-only identity and access primitives that later auth bridges, telephony continuation, patient-claim flows, and patient-action routes can use without inventing local shortcuts.

Implementation deliverables to create:
1. domain and persistence code for `IdentityBinding`, `PatientLink`, `AccessGrant`, `AccessGrantScopeEnvelope`, `AccessGrantRedemptionRecord`, and `AccessGrantSupersessionRecord`
2. minimal authority interfaces for `IdentityBindingAuthority` and `AccessGrantService` without implementing the full orchestration reserved for later tasks
3. migration artifacts and repository adapters for append-only version chains and supersession joins
4. token-hash and validator-family support types for grant families, replay policy, and token key versioning
5. `docs/architecture/68_identity_binding_and_access_grant_design.md`
6. `docs/architecture/68_identity_access_append_only_rules.md`
7. `docs/architecture/68_identity_access_atlas.html`
8. `data/analysis/identity_binding_manifest.json`
9. `data/analysis/access_grant_scope_matrix.csv`
10. `data/analysis/grant_supersession_casebook.json`
11. `tools/analysis/validate_identity_access_models.(py|ts)`
12. unit tests, migration tests, repository tests, and integration tests
13. `tests/playwright/identity-access-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- append a new `IdentityBinding` version without mutating prior settled versions in place
- keep `patientRef` nullable until the latest settled binding version says otherwise
- derive `PatientLink` from the latest settled binding version rather than making it an independent authority
- issue `AccessGrant` only as an immutable scope contract over one `AccessGrantScopeEnvelope`
- settle grant presentation through one exact-once `AccessGrantRedemptionRecord`
- invalidate or rotate grants only through `AccessGrantSupersessionRecord`
- preserve route family, action scope, identity-binding version, release tuple, and audience-surface runtime refs inside the grant envelope

Execution steps:
1. Implement `IdentityBinding` as an append-only versioned object with explicit supersession links and immutable evidence references.
2. Implement `PatientLink` as a derived trust record over the latest settled binding version. It must not be able to establish or correct lineage binding by itself.
3. Implement `AccessGrant` and `AccessGrantScopeEnvelope` exactly from the canonical fields, including route-intent, runtime tuple, channel freeze, and assurance-slice refs.
4. Implement exact-once `AccessGrantRedemptionRecord` semantics so duplicate presentation returns the same redemption result or linked supersession result.
5. Implement `AccessGrantSupersessionRecord` for rotation, promotion, claim completion, identity repair, session drift, route drift, publication drift, logout, and manual revoke.
6. Implement validators that fail when:
   - `patientRef` is stored as authoritative outside `IdentityBinding`
   - a grant can widen route, object, PHI, or runtime scope beyond its immutable scope envelope
   - a one-time or rotating grant can be redeemed twice without returning the original authoritative result
   - an older grant remains implicitly live after supersession
   - auth callback success or capability decision alone is treated as writable authority
7. Build the atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.

Mandatory gap closures you must perform:
- Close the “auth, matching, and ownership are the same thing” gap by keeping binding, capability, and grant scope separate.
- Close the “`patientRef` is always present once auth succeeds” gap by making it derived from the latest settled `IdentityBinding` and nullable until then.
- Close the “grants can be widened or reinterpreted after issue” gap with immutable `AccessGrantScopeEnvelope` and exact supersession records.
- Close the “link presentation can mint multiple sessions or route returns” gap by requiring exact-once `AccessGrantRedemptionRecord`.
- Close the “stale links survive promotion, correction, or logout by convention” gap with authoritative supersession records.
- Close the “capability decision equals permission” gap by forcing all later writable routes to bind grant + route intent + runtime tuple.

Industry-best-practice requirements:
- append-only versioning for identity and grant rows
- opaque token references only; store hashes, never raw URL tokens at rest
- family-specific validator namespaces and replay policies
- explicit supersession chains and revocation reasons
- deterministic integration fixtures for redemption and supersession
- repository separation from orchestration services

Non-negotiable rules:
- `IdentityBinding` is the sole serializer of patient-binding truth.
- `PatientLink` is derived, not authoritative.
- `AccessGrant` is immutable issuance truth over one scope envelope.
- Every terminal redemption or replacement must settle through explicit redemption or supersession records.
- Capability may narrow or deny, but it may not stand in for writable authority on its own.

Mandatory UI and interaction specification for `68_identity_access_atlas.html`:
Name the surface mode `Identity_Access_Atlas`.

Tone:
- premium internal trust and access atlas
- quiet, sharp, and governance-grade
- should look like one carefully designed authority map, not a table dump

Branding:
- restrained Vecells wordmark plus inline SVG `IA` monogram
- one binding-version chain diagram
- one grant lattice strip
- one scope-envelope inspector

Palette:
- canvas `#F8F9FC`
- panel `#FFFFFF`
- rail `#EEF2F8`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- identity accent `#3559E6`
- grant accent `#0EA5A4`
- supersession accent `#7C3AED`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for binding versions, grant IDs, hashes, and scope hashes

Layout:
- masthead with active bindings, live grants, redeemed grants, and superseded grants
- left rail (`300px`) for persona, grant-family, binding-state, and action-scope filters
- center canvas with binding chain, grant lattice, and current-scope cards
- right inspector (`404px`) for selected binding/grant details and supersession links
- lower region with redemption log table and scope-envelope rule table

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- chain diagram min height `240px`
- card min height `170px`
- controls `44px`

Motion:
- `120ms` hover/focus
- `180ms` chain/lattice transitions
- `220ms` inspector reveal
- reduced-motion support mandatory

Interaction requirements:
- filter by grant family, action scope, and binding state
- selecting a binding or grant must synchronize chain, lattice, inspector, and redemption log
- keyboard navigation across filters, diagrams, cards, and tables
- deterministic `data-testid` markers for binding chain, grant lattice, inspector, redemption log, and scope-rule table
- every diagram needs adjacent text parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- binding and grant filtering
- selection synchronization
- diagram / table parity
- keyboard navigation
- reduced motion
- responsive layout
- accessibility smoke checks

Definition of done:
- The repository has real append-only identity and grant models with persistence and validators.
- Binding truth, grant scope, redemption, and supersession are explicit and replay-safe.
- Later auth, claim, continuation, and patient-action services can build on one authoritative identity/access substrate.
```
