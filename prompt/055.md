# Prompt 055 For 055_seq_phase0_define_lifecycle_coordinator_and_closure_blocker_rules

Use the shared operating contract first:
[shared_operating_contract_046_to_055.md](/Users/test/Code/V/prompt/shared_operating_contract_046_to_055.md)

```text
Execute roadmap task 055_seq_phase0_define_lifecycle_coordinator_and_closure_blocker_rules.md.

Inherit the Shared_operating_contract_for_prompts_046_to_055 and consume the validated outputs from tasks `001-054`.

Mission:
Define the canonical `LifecycleCoordinator` strategy and the closure-blocker model for Vecells so request closure and governed reopen are derived exactly once from:
- milestone signals
- blocker facts
- confirmation gates
- degraded promises
- active lineage branches
- trust and reachability obligations
- terminal outcome evidence

This task must make it impossible for downstream domains to close requests directly or to hide closure blockers inside workflow-state convenience values.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`
  - `LifecycleCoordinator`
  - `RequestClosureRecord`
  - the `Workflow state` contract and orthogonal blocker rule
  - `RouteIntentBinding`
  - `CommandActionRecord`
  - `CommandSettlementRecord`
  - `RequestLifecycleLease`
  - the closure evaluation algorithm `9.6 Closure evaluation algorithm`
  - the reopen trigger logic `9.7 Reopen triggers`
- `phase-3-the-human-checkpoint.md`
  - triage milestone and handoff behavior
- `phase-4-the-booking-engine.md`
  - booking confirmation ambiguity and confirmation gates
- `phase-5-the-network-horizon.md`
  - hub confirmation, practice visibility debt, and alternative-offer truth
- `phase-6-the-pharmacy-loop.md`
  - dispatch proof, outcome reconciliation, bounce-back, urgent return, and reopen behavior
- `vecells-complete-end-to-end-flow.md`
  - the audited top-level flow, especially `PL_CLOSE` and child-domain milestone signaling
- `forensic-audit-findings.md`
  - findings `32`, `45`, `57-60`, `67`, `68`, and `69-79` where child domains were prevented from directly mutating canonical request truth
- outputs from tasks `005`, `007`, `016`, `048`, `049`, `051`, and `054`

Primary outcome:
One explicit coordinator and closure-blocker strategy proving:
- who is allowed to derive canonical request milestone change
- which child domains emit signals vs own request truth
- how closure blockers are materialized and persisted
- how the persisted `RequestClosureRecord` is structured
- how reopen triggers are evaluated
- how closure and reopen remain deterministic and auditable

Deliverables to create:
1. `docs/architecture/55_lifecycle_coordinator_strategy.md`
2. `docs/architecture/55_closure_blocker_ledger.md`
3. `docs/architecture/55_milestone_signal_and_coordinator_rules.md`
4. `docs/architecture/55_lifecycle_coordinator_lab.html`
5. `data/analysis/lifecycle_coordinator_inputs.csv`
6. `data/analysis/request_closure_record_schema.json`
7. `data/analysis/closure_blocker_taxonomy.json`
8. `data/analysis/milestone_signal_matrix.csv`
9. `data/analysis/reopen_trigger_matrix.csv`
10. `tools/analysis/validate_lifecycle_coordinator_rules.(py|ts)`
11. `tests/playwright/lifecycle-coordinator-lab.spec.(ts|js)`

At minimum, your closure-record and blocker model must capture:
- `closureRecordId`
- `episodeId`
- `requestId`
- `requestLineageRef`
- `evaluatedAt`
- `requiredLineageEpoch`
- `blockingLeaseRefs[]`
- `blockingPreemptionRefs[]`
- `blockingApprovalRefs[]`
- `blockingReconciliationRefs[]`
- `blockingConfirmationRefs[]`
- `blockingLineageCaseLinkRefs[]`
- `blockingDuplicateClusterRefs[]`
- `blockingFallbackCaseRefs[]`
- `blockingIdentityRepairRefs[]`
- `blockingGrantRefs[]`
- `blockingReachabilityRefs[]`
- `blockingDegradedPromiseRefs[]`
- `decision`
- `closedByMode`
- `deferReasonCodes[]`

Execution steps:
1. Start from the Phase 0 coordinator responsibilities and make them explicit:
   - sole logical policy authority for episode closure and governed reopen
   - deterministic state machine partitioned by `episodeId`
   - issuer/validator of `LineageFence.currentEpoch`
   - materializer/clearer of confirmation gates and closure blockers
   - persister of `RequestClosureRecord`
2. Publish the coordinator input model. At minimum capture how it consumes:
   - `RequestLifecycleLease`
   - `SafetyPreemptionRecord`
   - approvals and confirmation gates
   - reconciliation gates
   - `IdentityRepairCase`
   - `DuplicateCluster`
   - `FallbackReviewCase`
   - reachability dependencies
   - PHI-bearing active grants
   - `LineageCaseLink` states
   - terminal outcome presence
   - episode-level sibling state when relevant
3. Publish the milestone-signal matrix showing which domains may emit signals and what those signals mean without letting them write canonical request closure or request milestone directly.
   At minimum cover:
   - triage
   - booking
   - hub coordination
   - pharmacy
   - communications / callback / clinician messaging
   - admin resolution or direct self-care outcome
4. Publish the blocker taxonomy and ensure blockers remain orthogonal facts, not `Request.workflowState` values.
5. Publish the explicit closure-evaluation algorithm using the Phase 0 checklist:
   - no active or broken leases
   - no pending safety preemption
   - no unresolved approvals or confirmation gates unless policy downgrades them
   - no disputed booking / dispatch / pharmacy outcome / external confirmation state
   - no unresolved pharmacy weak-match reconciliation gate
   - no active identity-repair, duplicate-review, or fallback-review work
   - no active reachability repair or contact-route dependency for any current promise
   - no active PHI-bearing public / continuation / transactional grant
   - no remaining current blocker or confirmation refs
   - no active lineage case link without governed return/completion/supersession/compensation
   - required command-following projections consumed
   - terminal outcome exists
   - episode-level closure policy satisfied where relevant
   - required acknowledgement or consent-pending dependencies handled according to policy
6. Publish the reopen-trigger matrix using the Phase 0 signal dimensions:
   - urgent
   - unable-to-complete
   - unable-to-contact
   - bounce-back
   - consent revocation
   - contradiction
   - material change across clinical/contact/provider/consent/timing/identity dimensions
7. Build the lab and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that:
   - no downstream domain is allowed to write `Request.workflowState = closed`
   - no blocker type has been collapsed into workflow state
   - every closure blocker field required by findings `57-60` exists in the schema
   - any closure decision depends on a current lineage epoch
   - any child-domain milestone maps to coordinator consumption rather than direct canonical request mutation
   - the same inputs produce the same closure verdict deterministically

Mandatory gap closures you must perform:
- Close the “appointment creation can directly close the request” gap from forensic finding `32`.
- Close the “closure is a passive terminal instead of a governed decision” gap from forensic finding `45`.
- Close the persisted blocker omission gaps from findings `57-60`.
- Close the “external confirmation-gate and blocker changes lack first-class event/support” gap by ensuring the coordinator depends on the event families hardened in task `048`.
- Close the “child domains can write request truth directly” gap from findings `69-79` by making milestone-signal ingestion explicit and one-way.
- Close the “workflow state can absorb blocker meaning” gap by preserving milestone-only workflow semantics.

Industry-best-practice requirements:
- deterministic state-machine semantics
- explicit epoch/fence validation
- append-only blocker evidence with materialized current view
- clear separation of signal producers vs canonical state owner
- replay-safe close/reopen behavior
- validator-backed schema and rule completeness
- no hidden local closure paths

Non-negotiable rules:
- `LifecycleCoordinator` is the sole cross-domain closure and governed-reopen authority for `Request`.
- Only `LifecycleCoordinator` may set `Request.workflowState = closed`.
- Downstream contexts may only propose milestones or blockers through evidence and signals.
- Confirmation gates, repair holds, duplicate review, fallback recovery, reachability repair, and degraded promises must remain orthogonal blocker facts and must never become workflow-state values.
- `RequestClosureRecord` must persist precise blocker references rather than vague defer reasons alone.
- If any closure evaluation check fails, persist `RequestClosureRecord(decision = defer)` and do not close.

Mandatory UI and interaction specification for `55_lifecycle_coordinator_lab.html`:
Name the surface mode `Lifecycle_Coordinator_Lab`.

Tone:
- minimalist premium internal reasoning lab
- should feel like a deterministic control-plane instrument, not a workflow whiteboard
- must communicate rigor, not speculative visualization

Branding:
- restrained Vecells wordmark plus an inline SVG `LC` monogram
- include one elegant milestone-to-blocker constellation diagram and one closure-verdict panel

Palette:
- canvas `#F7F8FC`
- rail `#EEF2F8`
- panel `#FFFFFF`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- milestone accent `#0EA5A4`
- blocker accent `#6E59D9`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for blocker IDs, epoch values, hashes, and signal codes

Layout:
- sticky masthead with:
  - coordinator input count
  - blocker category count
  - defer-vs-close scenario count
  - reopen-trigger count
- left rail (`300px`):
  - input filters
  - child-domain signal filters
- center canvas:
  - milestone/blocker constellation
  - closure-verdict explanation panel
- right inspector (`396px`):
  - selected blocker or signal details
  - closure-record fields
  - consumed epoch / fence requirements
- lower region:
  - milestone-signal matrix
  - blocker taxonomy table
  - reopen-trigger matrix
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `600px`
- primary controls `44px`
- matrix rows `40px` minimum
- all diagrams require adjacent table/list parity

Motion:
- `120ms` hover/focus
- `180ms` signal/blocker selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by domain, blocker class, closure eligibility, and reopen-trigger class
- select a blocker or milestone signal and synchronize matrices plus inspector
- keyboard navigation across filters, matrix rows, and taxonomy tables
- deterministic `data-testid` markers for filters, constellation canvas, verdict panel, inspector, and defect strip
- must preserve clarity: no overwhelming state explosion visuals

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- blocker filtering
- signal selection
- matrix/inspector parity
- verdict-panel rendering
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks

Definition of done:
- Vecells has one explicit lifecycle-coordinator and closure-blocker strategy.
- Closure and reopen now depend on a deterministic, auditable coordinator model rather than local domain shortcuts.
- Later aggregate and service implementation tasks can build coordinator logic without re-deciding closure law.
```