# Prompt 044 For 044_seq_phase0_scaffold_domain_context_modules_and_shared_contract_packages

Use the shared operating contract first:
[shared_operating_contract_036_to_045.md](/Users/test/Code/V/prompt/shared_operating_contract_036_to_045.md)

```text
Execute roadmap task 044_seq_phase0_scaffold_domain_context_modules_and_shared_contract_packages.md.

Inherit the Shared_operating_contract_for_prompts_036_to_045 and consume the validated outputs from tasks `001-043`.

Mission:
Scaffold the domain-context modules and shared contract packages that embody the Vecells bounded-context model, shared kernel, published contracts, and explicit cross-context seams.

This task must turn the Phase 0 object catalog and context map into buildable package boundaries, not just documentation.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `BoundedContextDescriptor`
  - `ContextBoundaryContract`
  - the context ownership map and boundary rules
  - `CanonicalEventContract`
  - the canonical object catalog across lifecycle, identity, access, reservation, pharmacy, communications, support, operations, governance, analytics, audit, and release control
- `blueprint-init.md`, especially the modular-platform rule and product-surface inventory
- `platform-runtime-and-release-blueprint.md` where contract families and runtime publication artifacts are named
- `platform-frontend-blueprint.md` where frontend contract, projection, live-update, cache-policy, and route-inventory families are named
- outputs from tasks `006`, `007`, `015`, `041`, `042`, and `043`
- `forensic-audit-findings.md`, especially any finding that hardens event completeness, closure blockers, continuity evidence, token/export drift, acting-context drift, or route/trust/release semantics

Primary outcome:
A package skeleton layout where each bounded context and each shared contract family has an explicit home, typed exports, README ownership statement, and boundary-safe placeholder code.

Deliverables to create:
1. `packages/domains/<context-code>/` scaffolds for every bounded context frozen in task `041`
2. `packages/domain-kernel/` scaffold
3. `packages/event-contracts/` scaffold
4. `packages/api-contracts/` scaffold
5. `packages/fhir-mapping/` scaffold
6. `packages/authz-policy/` scaffold
7. `packages/design-system/` scaffold
8. `packages/test-fixtures/` scaffold
9. `packages/observability/` scaffold
10. `packages/release-controls/` scaffold
11. `docs/architecture/44_domain_package_contracts.md`
12. `data/analysis/domain_package_manifest.json`
13. `data/analysis/shared_contract_package_matrix.csv`
14. `tools/analysis/validate_domain_packages.(py|ts)`

At minimum, scaffold context packages for these contexts unless task `041` recorded a canonical alternative:
- `intake_safety`
- `identity_access`
- `triage_workspace`
- `booking`
- `hub_coordination`
- `pharmacy`
- `communications`
- `support`
- `operations`
- `governance_admin`
- `analytics_assurance`
- `audit_compliance`
- `release_control`

For each domain package, include at minimum:
- package manifest
- README with owning context, source refs, allowed dependencies, forbidden dependencies
- placeholder module exports for aggregates, domain services, events, policies, and projections relevant to that context
- test placeholder proving import boundaries and package bootstrapping

For shared packages, include at minimum:
- typed placeholder exports for the shared contract family
- README with ownership, consumers, and versioning posture
- no convenience dumping ground exports

Execution steps:
1. Start from the context map and object catalog.
2. Create package directories and manifests.
3. Place canonical objects and contract families into the correct package homes.
4. Generate the package manifest and contract matrix.
5. Validate that every canonical object family and every contract family has exactly one package owner.
6. Validate that no package becomes an undocumented catch-all.

Mandatory gap closures you must perform:
- Close the “domain list exists but package homes are unclear” gap.
- Close the “support, operations, governance, and assurance are afterthought packages” gap by giving them first-class homes.
- Close the “shared package becomes dumping ground” gap by sharply defining package purpose.
- Close the “frontend and runtime contract families float outside the package model” gap by giving them explicit shared-package ownership.
- Close the “canonical object appears in docs but has no code home” gap by using the object catalog as the completeness check.

Industry-best-practice requirements:
- explicit package manifests and versioning posture
- typed exports only; no magic side-effect imports
- clear public API surface per package
- README-based architecture intent at the package root
- automated validation for orphaned or multiply-owned object families
- no transitive deep-import reliance across package internals

Non-negotiable rules:
- One canonical object family may not have two owners.
- Shared packages may expose only documented, versionable contracts.
- Domain packages may not reach through sibling package internals.
- Convenience imports that violate package public APIs are forbidden.

Definition of done:
- Vecells has buildable domain-context and shared-contract package scaffolds.
- Every canonical context and contract family has an explicit code home.
- Later implementation tasks can build real logic without re-deciding package ownership.
```
