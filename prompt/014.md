# Prompt 014 For 014_seq_choose_frontend_stack_and_bff_pattern

Use the shared operating contract first:
[shared_operating_contract_011_to_015.md](/Users/test/Code/V/prompt/shared_operating_contract_011_to_015.md)

```text
Execute roadmap task 014_seq_choose_frontend_stack_and_bff_pattern.md.

Inherit the Shared_operating_contract_for_prompts_011_to_015 and consume the validated outputs from tasks 001-013.

Mission:
Choose the frontend stack, route/runtime consumption model, client-state baseline, and BFF / gateway pattern for Vecells. Then prove the decision with a browser-viewable showcase that demonstrates the chosen shell architecture, design-contract posture, and contract-aware UI composition.

This task is not just a framework pick. It must answer:
- what the browser runtime is
- how shells preserve continuity and same-shell morphing
- how typed query / mutation / live-channel contracts are consumed
- how many BFF surfaces exist and why
- how design tokens, semantic state, accessibility coverage, automation anchors, and runtime publication stay in one contract bundle
- how development and testing will be driven by `Playwright_or_other_appropriate_tooling`

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `### 0.2 Continuity key and shell law`
  - `#### 1.1 PersistentShell`
  - `#### 1.2 CasePulse`
  - `#### 1.3 StateBraid`
  - `#### 1.5 DecisionDock`
  - `#### 1.6 AmbientStateRibbon`
  - `#### 1.7 FreshnessChip`
  - `#### 1.11 QueueChangeBatch`
  - `#### 1.17 SelectedAnchor`
  - `#### 7.8 Verification and Playwright contract`
- `platform-frontend-blueprint.md`, especially:
  - non-negotiable invariants for shell continuity, topology, and support-region budgeting
  - `#### 2.15B DesignContractRegistry`
  - `#### 4.1D Reference implementation shape`
  - `#### 7.8 Verification and Playwright contract`
  - the browser / BFF boundary rules near the route-contract and client-cache sections
- `design-token-foundation.md`, especially:
  - token architecture and inheritance
  - breakpoints and layout lattice
  - typography
  - spacing, density, radius, elevation, semantic color, motion, and shell profiles
- `canonical-ui-contract-kernel.md` for shared component and semantic-state obligations
- `ux-quiet-clarity-redesign.md`, especially:
  - `## Redesign concept`
  - `Mission Frame`
  - quiet-clarity, continuity, and attention-budget contracts
  - validation expectations
- `patient-portal-experience-architecture-blueprint.md`, especially:
  - `### Design promise`
  - `### Portal entry and shell topology`
  - `### Visual token profile`
  - `### Primary navigation model`
- `staff-workspace-interface-architecture.md`, especially:
  - `## Design language: Quiet Clinical Mission Control`
  - route family, layout topology, projections, trust envelope, and assistive companion profile
- `operations-console-frontend-blueprint.md`, especially:
  - `NorthStarBand`, `BottleneckRadar`, `InterventionWorkbench`, `ServiceHealthGrid`, and board prominence rules
- `governance-admin-console-frontend-blueprint.md`, especially:
  - governance shell, scope ribbon, diff / impact / approval surfaces, and watch posture
- `accessibility-and-content-system-contract.md` for keyboard, announcement, form, visualization-parity, and content constraints
- `platform-runtime-and-release-blueprint.md`, especially:
  - `GatewayBffSurface`
  - `FrontendContractManifest`
  - `ProjectionContractVersionSet`
  - `ProjectionQueryContract`
  - `MutationCommandContract`
  - `LiveUpdateChannelContract`
  - `ClientCachePolicy`
  - `AudienceSurfaceRouteContract`
  - `AudienceSurfaceRuntimeBinding`
- `forensic-audit-findings.md`, especially Findings:
  - 86-103 on same-shell continuity, visibility, recovery, patient actionability, and settlement posture
  - 118 on design-contract drift
  - 120 on degraded-mode fragmentation across patient entry, section, recovery, embedded, and artifact shells

Primary outcome:
One frontend baseline that makes it impossible to accidentally build Vecells as a set of generic dashboards or framework-local mini-apps. The chosen stack and BFF pattern must preserve:
- same-shell continuity
- published browser boundaries
- deterministic state semantics
- design-contract publication
- accessibility and automation parity
- Playwright-driven verification from day one

Deliverables to create:
1. `docs/architecture/14_frontend_stack_decision.md`
2. `docs/architecture/14_gateway_bff_pattern_and_surface_split.md`
3. `docs/architecture/14_shell_and_route_runtime_architecture.md`
4. `docs/architecture/14_design_system_and_contract_publication_strategy.md`
5. `docs/architecture/14_client_state_data_fetch_and_live_update_baseline.md`
6. `docs/architecture/14_frontend_testing_and_Playwright_or_other_appropriate_tooling_baseline.md`
7. `docs/architecture/14_visual_direction_and_showcase_spec.md`
8. `docs/architecture/14_frontend_architecture_showcase.html`
9. `docs/architecture/14_shell_topology_and_bff_map.mmd`
10. `data/analysis/frontend_stack_scorecard.csv`
11. `data/analysis/gateway_surface_split_matrix.csv`
12. `data/analysis/client_state_and_cache_policy_matrix.csv`
13. `data/analysis/ui_contract_publication_matrix.csv`
14. `data/analysis/playwright_coverage_matrix.csv`
15. `tools/analysis/validate_frontend_architecture.(ts|js)`

You must evaluate at minimum these frontend-stack candidates unless the repo already rules one out:
- `React + TypeScript + Vite` (or an equivalent client-first shell stack)
- `React + TypeScript + Next.js` with explicit rejection of hidden server actions / route handlers unless they can be bounded to published gateway contracts
- one alternative framework with strong routing and testability if the repo already leans that way

You must evaluate at minimum these BFF patterns:
- one generic monolithic BFF for all audiences
- one BFF per audience family
- split BFF surfaces by route-family contract, tenant isolation, or trust-zone boundary when required by the runtime blueprint

Execution steps:
1. Start by scoring candidate frontend stacks against the actual Vecells requirements, not generic popularity. Required criteria:
   - same-shell continuity with persistent shells and soft route morphing
   - strict client-side consumption of published query / mutation / live-channel contracts
   - no hidden backend surface that can bypass `GatewayBffSurface`
   - strong TypeScript support
   - strong Playwright support
   - deterministic DOM marker strategy
   - embedded / constrained-browser adaptability
   - support for generated client types and contract digests
   - support for accessibility, reduced motion, and 320px / 400% zoom behavior
2. Penalize any stack choice that encourages hidden server actions, route-local API handlers, or framework-owned BFF shortcuts unless you can prove they still honor `GatewayBffSurface`, `FrontendContractManifest`, and `AudienceSurfaceRuntimeBinding` as the only browser authority.
3. Choose the client-state baseline. You must separate:
   - deterministic shell / posture reducers
   - server-state query caching
   - optimistic local acknowledgement vs authoritative settlement
   - live-update channel handling
   - selected-anchor and return-contract persistence
   - reduced-trust / read-only / recovery posture
4. Choose the BFF split. The result must explicitly honor:
   - different audience families
   - different tenant-isolation modes
   - different trust-zone boundaries
   - different downstream workload sets
   - different session policies and cache scopes
   A single broad BFF is acceptable only if the matrix proves those requirements do not diverge.
5. Define the generated contract-consumption model for:
   - query clients
   - mutation clients
   - live-channel clients
   - client-cache policies
   - route-runtime bindings
   - design-contract bundles and digests
6. Build the showcase. It must not be a generic admin page. It must demonstrate four shell families with unique but related composition:
   - patient shell demo
   - workspace shell demo
   - operations shell demo
   - governance shell demo
   The showcase may use synthetic data only, but every visible state must trace to the canonical UI contracts.
7. Drive development and verification with `Playwright_or_other_appropriate_tooling`. Use it to validate composition, shell continuity, keyboard flow, breakpoint behavior, reduced motion, stable markers, and accessibility smoke conditions.

Mandatory UI and interaction specification for the showcase:
The visual direction must be “Signal Atlas Live / Quiet Clarity”: minimalist premium, high-trust, summary-first, no generic startup-dashboard styling.

Required global visual system:
- overall layout max width: `1440px`
- grid: `xs/sm = 4 columns / 16px gutters`, `md = 8 columns / 24px gutters`, `lg = 12 columns / 24px gutters`, `xl = 16 columns / 32px gutters`
- shell rails: `72px collapsed`, `280px expanded`
- support peek: `320px`; support open: `clamp(320px, 28vw, 400px)`
- inline drawer: `clamp(360px, 33vw, 480px)`
- dialog sizes: `480 / 640 / 800`
- typography stack: use the canonical system-sans stack from `design-token-foundation.md`; if a local vendored font exists and is already licensed in the repo, it may be layered on top, but remote fonts are forbidden
- type scale: implement the canonical `display / headline / title / section / body.lg / body / body.sm / label / mono.sm` roles from `design-token-foundation.md`
- control heights: patient default `48px` with `44px` minimum; staff/professional default `44px` with `40px` minimum depending on density mode
- row heights: `40px` balanced, `36px` compact, `32px dense_data` only for non-editable operations / governance grids
- radius: `8px` cards and task panels, `12px` drawers and dialogs, `16px` only for patient spotlight or high-comfort highlight surfaces
- elevation: restrained `z0/z1` default, `z2` only for drawers / sticky docks, `z3` only for blocking overlays
- motion: `120ms`, `180ms`, `240ms`; animate opacity + translation first; translation limited to `4/8/12px`; no bounce; no decorative full-page transitions; obey reduced motion

Required semantic color scheme:
Implement one shared semantic token export with explicit values and alias it through semantic roles. You may use OKLCH or an equivalent accessible color system, but the showcase must materialize concrete CSS variables. Minimum role mapping:
- neutral canvas / shell / panel / inset surfaces in low-chroma graphite tones
- strong text, default text, muted text, inverse text roles
- border subtle / default / strong roles
- semantic roles for info, warning, critical, success, assistive, freshness, and trust
A concrete acceptable baseline is:
- canvas `#F5F7FA`
- shell / panel `#FFFFFF`
- inset `#EEF2F6`
- text strong `#121826`
- text default `#1D2939`
- text muted `#475467`
- border subtle `#E4E7EC`
- border default `#D0D5DD`
- info / signal `#335CFF`
- trust / teal `#0F8B8D`
- warning `#C98900`
- critical `#C24141`
- success `#0F9D58`
- assistive `#6E59D9`
These values are a shared provisional export for the showcase only. Route-local color invention is forbidden.

Required branding / logo treatment:
- build one subtle Vecells monogram from three connected nodes forming a restrained “V”
- render it as inline SVG or CSS only
- no stock icon libraries or clip-art medical symbols
- place it in the shell header of the showcase home and keep it understated

Required shell-specific composition:
1. Patient shell demo (`focus_frame`, `profile.patient_portal`)
   - five visible regions in this order: identity+utility, primary nav, section header, primary content, decision dock
   - one spotlight / hero card, up to four compact secondary cards, one trust cue per card/header, one dominant CTA at a time
   - content column respects the long-form reading measure and never collapses into dashboard density
2. Workspace shell demo (`two_plane`, `profile.staff_workspace`)
   - left queue/list plane, dominant task plane, one optional promoted support region only
   - queue surfaces use separator-first list/table styling, not floating cards
   - active task plane uses `task` surface role with compact-but-readable density
3. Operations shell demo (`two_plane`, `profile.operations_console`)
   - `NorthStarBand`, `BottleneckRadar`, `ServiceHealthGrid`, `InterventionWorkbench` summary layout
   - only one visually dominant anomaly surface plus one dominant action region at once
   - include one compact chart or heat surface with adjacent table parity and explicit authoritative-state markers
4. Governance shell demo (`two_plane`, `profile.governance_admin`)
   - `ScopeRibbon`, `ChangeEnvelope`, `ImpactPreview`, `ApprovalStepper`, `ReleaseFreezeTupleCard` style composition
   - dense analytical tables allowed only for non-editable matrices
   - diff / evidence / approval surfaces return to balanced density for consequence review

Required state and DOM-contract behavior:
- active shells must expose stable `data-shell-type`, `data-channel-profile`, `data-route-family`, `data-layout-topology`, `data-breakpoint-class`, `data-density-profile`, `data-writable-state`, and `data-anchor-state`
- route roots must expose design-contract and accessibility-coverage markers per `platform-frontend-blueprint.md`
- no shell may infer writable posture from route availability alone
- no shell may hydrate from undeclared query / mutation / stream / cache surfaces
- continuity reuse, selected-anchor preservation, and same-shell morphing must be observable in the DOM without timing guesses

Mandatory gap closures you must perform:
- Close the “frontend stack is chosen by convenience rather than shell law” gap.
- Close the “framework may blur browser and BFF boundaries” gap.
- Close the “one generic BFF can silently become too broad” gap on the browser side.
- Close the “design contract publication is a design-tool sidecar” gap identified by Finding 118.
- Close the “same-shell continuity and degraded-mode posture can fragment across routes / channels / artifacts” gap.
- Close the “generic dashboard or CRUD visuals can bypass Quiet Clarity attention law” gap.
- Close the “Playwright is bolted on after UI assembly” gap by making it part of the design and development flow.

Non-negotiable frontend rules you must encode:
- patient, workspace, pharmacy, hub, support, operations, and governance shells talk only to published gateway / BFF surfaces
- no browser-origin call reaches adapters directly
- no framework-local API shortcut may replace a published gateway surface
- one shell continuity key must reuse one persistent shell
- one dominant action and one promoted support region at most, unless a canonical blocker / compare / explicit pin justifies more
- charts and heat surfaces require summary + table parity
- no remote fonts, remote scripts, or CDN-delivered design assets
- no generic detached success pages for same-shell command settlement

Validation rules:
- Every audience surface from task 004 must map to one shell type, one route-family ownership model, and one gateway surface.
- Any chosen stack that cannot prove soft same-shell route morphing, typed contract consumption, and deterministic Playwright markers must fail validation.
- Any BFF split that ignores tenant-isolation, trust-zone, downstream workload, or session-policy differences must fail validation.
- Any showcase page that uses route-local colors, spacing scales, or token systems outside the shared export must fail validation.
- Any chart without adjacent table parity and authoritative-state markers must fail validation.
- Any route lacking required DOM markers or reduced-motion behavior must fail validation.

Playwright_or_other_appropriate_tooling requirements:
Use Playwright as a first-class development and testing driver. Minimum required coverage:
- shell load and layout at `320px`, `768px`, `1024px`, and `1440px`
- keyboard-only navigation through each shell demo
- same-shell route morph / tab / child-state transitions without full page reload
- selected-anchor preservation
- reduced-motion mode
- accessibility smoke checks
- visualization parity checks for the operations demo
- stable DOM markers for shell, status strip, decision dock, selected anchor, and design-contract digest

Definition of done:
- Vecells now has one explicit frontend stack and BFF baseline grounded in the canonical shell law.
- The showcase proves the chosen stack can deliver unique, premium, non-generic shells while still staying contract-aware and automation-friendly.
- Future agents can build real features without re-litigating continuity, browser boundaries, or UI-state semantics.
```
