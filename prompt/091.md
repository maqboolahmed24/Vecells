# Prompt 091 For 091_par_phase0_track_runtime_provision_ci_cd_pipeline_and_signed_build_provenance

Use the shared operating contract first:
[shared_operating_contract_086_to_095.md](/Users/test/Code/V/prompt/shared_operating_contract_086_to_095.md)

```text
Execute roadmap task 091_par_phase0_track_runtime_provision_ci_cd_pipeline_and_signed_build_provenance.md.

Inherit the Shared_operating_contract_for_prompts_086_to_095 and consume the validated outputs from tasks `001-090`.

Mission:
Provision the Vecells CI/CD pipeline, signed build provenance, and fail-closed verification baseline.

This task must create the real delivery control spine for:
- deterministic builds and artifact packaging
- SBOM and dependency-policy checks
- signed provenance and attestation verification
- build quarantine, revocation, and supersession behavior
- gated promotion into non-production environments using machine-readable records, not dashboard folklore

This is real runtime and delivery-control work. Produce pipeline code, signing and verification logic, provenance records, and tests. Do not leave build trust, artifact signing, or publish eligibility as CI job comments.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially the Phase 0 rules that build system, linting, type safety, environment loading, preview deploys, CI workflows, SBOM generation, dependency policy checks, and signed-artifact verification must exist first
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### BuildProvenanceRecord`
  - `### RuntimePublicationBundle`
  - `### ReleasePublicationParityRecord`
  - verification-ladder objects and gate rules
  - `## Security baseline contract`
  - `## Operational readiness contract`
- outputs from tasks `012`, `015`, `051`, `052`, `058`, `065`, `084`, `089`, and `090`
- `forensic-audit-findings.md`, especially Findings `91`, `95`, `96`, `103`, `104`, `112`, and `113`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It may provision pipeline gates, provenance generation, and verification steps now, but it must not steal ownership of later live-wave watch-tuple actioning, preview environment control, or final canary / rollback harness depth. Publish bounded seams and record `FOLLOW_ON_DEPENDENCY_*` where appropriate.

Mock_now_execution:
Build a fully working local and non-production pipeline now.
- produce signed build artifacts and verified provenance for repository builds using development-safe signing material and secret backends from task `089`
- enforce deterministic build, lint, test, SBOM, and provenance-verification gates in CI
- quarantine artifacts automatically when provenance, dependency hygiene, or verification drift fails
The mock-now path must preserve the same build graph, provenance records, artifact states, and refusal logic the production pipeline will use.

Actual_production_strategy_later:
When production signing services, artifact registries, and deployment targets are finalized, they must preserve the same provenance record shape, quarantine rules, policy gates, and verification posture. Production hardening may add stronger signing hardware, remote attestations, regional registries, or controlled promotion approvals, but it must not make unsigned or unverified artifacts eligible for runtime consumption.

Primary outcome:
The repository gains a real CI/CD and provenance-control baseline that can produce, verify, quarantine, and publish build artifacts deterministically.

Implementation deliverables to create:
1. CI pipeline definitions for lint, types, tests, artifact packaging, SBOM generation, dependency checks, provenance generation, provenance verification, and publish gating
2. build-signing and verification tooling integrated with the secret and key-management baseline from task `089`
3. machine-readable `BuildProvenanceRecord` generation, verification, quarantine, revocation, and supersession support
4. artifact-publish descriptors and non-production promotion hooks aligned to runtime publication work
5. local-development scripts for deterministic build and provenance rehearsal
6. `docs/architecture/91_ci_cd_and_build_provenance_design.md`
7. `docs/architecture/91_artifact_signing_quarantine_and_publish_rules.md`
8. `docs/architecture/91_build_provenance_pipeline_atlas.html`
9. `data/analysis/build_provenance_manifest.json`
10. `data/analysis/pipeline_gate_matrix.csv`
11. `data/analysis/artifact_quarantine_policy.json`
12. `tools/analysis/validate_ci_cd_and_build_provenance.(py|ts)`
13. pipeline tests, provenance tests, quarantine tests, dependency-policy tests, and publish-gate smoke tests
14. `tests/playwright/build-provenance-pipeline-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- run deterministic builds and test gates for the monorepo outputs required by current runtime work
- generate machine-readable signed `BuildProvenanceRecord` artifacts for every publishable build
- verify provenance before allowing artifact publication or environment deployment
- quarantine or block artifacts when signature, dependency policy, schema set, or runtime-target scope drifts
- publish machine-readable gate evidence and artifact states from code
- prevent ad hoc manual deployment of artifacts that do not pass provenance verification

Execution steps:
1. Re-read the build provenance, verification ladder, and security-baseline sections, then derive the exact gates required for current Phase 0 runtime work.
2. Honor the repo, runtime, secret, and gateway baselines from earlier tasks; if prerequisites are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
3. Implement the CI pipeline definitions and local rehearsal scripts.
4. Implement build signing and provenance verification using the secret and key infrastructure already provisioned.
5. Publish machine-readable provenance, quarantine, and publish-state records from the pipeline.
6. Build the atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write smoke tests proving unsigned, stale, drifted, or revoked artifacts cannot be published or deployed.

Mandatory gap closures you must perform:
- Close the “green CI means publishable” gap by requiring verified provenance and gate evidence.
- Close the “signing is only a timestamp” gap by implementing signed provenance plus quarantine and supersession behavior.
- Close the “manual deploy can bypass artifact trust” gap by making runtime consumption fail closed on missing or bad provenance.
- Close the “dependency policy drift is advisory” gap by binding policy checks into publish gating.
- Close the “local dev never rehearses provenance” gap by providing deterministic local rehearsal scripts.

Industry-best-practice requirements:
- deterministic builds and reproducible artifacts
- signed provenance and fail-closed verification
- SBOM and dependency-policy enforcement
- no raw long-lived CI secrets
- artifact quarantine and revocation behavior under version control
- machine-readable gate evidence published from pipeline runs

Non-negotiable rules:
- Unverified artifacts may not be published or deployed.
- Provenance verification state must be machine-readable.
- Quarantined or revoked artifacts block runtime consumption.
- CI gate evidence may not live only in build logs or screenshots.
- Manual deployment paths may not bypass provenance policy.

Mandatory UI and interaction specification for `91_build_provenance_pipeline_atlas.html`:
Name the surface mode `Build_Provenance_Pipeline_Atlas`.

Tone:
- premium delivery-control atlas
- minimalist, crisp, and confidence-building
- should feel like a luxury release-control board, not a generic CI dashboard

Branding:
- restrained Vecells wordmark plus inline SVG `BP` monogram
- one pipeline-lane diagram
- one provenance-state card wall
- one quarantine and publish-decision timeline strip

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- build accent `#2563EB`
- provenance accent `#0EA5A4`
- quarantine accent `#C24141`
- warning accent `#D97706`
- published accent `#059669`
- revoked accent `#7C3AED`

Typography:
- system sans stack only
- monospaced digest styling for build IDs, artifact IDs, provenance record IDs, and bundle hashes

Layout:
- sticky masthead with active build, quarantined artifacts, revoked artifacts, and gate-failure alerts
- left rail (`324px`) with environment filters, gate filters, artifact-state filters, and provenance-state filters
- central canvas with:
  - pipeline-lane diagram
  - provenance-state card wall
  - quarantine / publish-decision timeline strip
- right inspector (`420px`) for selected build, artifact, provenance record, or gate failure
- lower grid with one pipeline-run table and one provenance / policy table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by environment, gate, artifact state, provenance state, and build family
- selecting a row must synchronize diagrams, card wall, inspector, and tables
- keyboard navigation across filters and tables
- deterministic `data-testid` markers for pipeline diagram, provenance cards, decision timeline, run table, policy table, and inspector
- every visual must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification
- verification that quarantined, revoked, and published states are visually and semantically distinct

Definition of done:
- The repository has a functioning CI/CD and signed-provenance baseline.
- Publishable artifacts carry verified provenance and gate evidence.
- Quarantine and revocation behavior are real and testable.
- Later runtime publication and release-wave work can rely on this delivery-control substrate instead of recreating build trust from scratch.
```
