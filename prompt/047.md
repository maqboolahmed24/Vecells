# Prompt 047 For 047_seq_phase0_define_trust_zone_boundaries_and_gateway_surface_map

Use the shared operating contract first:
[shared_operating_contract_046_to_055.md](/Users/test/Code/V/prompt/shared_operating_contract_046_to_055.md)

```text
Execute roadmap task 047_seq_phase0_define_trust_zone_boundaries_and_gateway_surface_map.md.

Inherit the Shared_operating_contract_for_prompts_046_to_055 and consume the validated outputs from tasks `001-046`.

Mission:
Define the canonical trust-zone boundary model and the exact browser-addressable gateway surface map for Vecells. This task must establish:
- which runtime families may talk to which others
- how tenant transfer and assurance-trust transfer behave across boundaries
- which audience surfaces get which gateway or BFF boundary
- how route families, session policy, cache policy, and mutating context scope are bound to each gateway surface

This task must eliminate the possibility of a generic BFF silently becoming the hidden runtime owner of multiple incompatible audience surfaces.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`
  - `### TrustZoneBoundary`
  - `### GatewayBffSurface`
  - `### FrontendContractManifest`
  - the rules that only published gateway surfaces may bridge browser-facing traffic beyond edge and shell delivery
  - the rules that differing tenant isolation, trust-zone, downstream workload, or recovery posture must split surfaces rather than branch internally
- `phase-0-the-foundation-protocol.md`
  - bounded-context rules
  - `RouteIntentBinding`
  - `AudienceSurfaceRuntimeBinding`
  - `ReleaseApprovalFreeze`, `ChannelReleaseFreezeRecord`, and `AssuranceSliceTrustRecord` as browser/mutation preconditions
  - the rule that no route, gateway, or shell may reconstruct writable truth from fragments
- `platform-frontend-blueprint.md`
  - shell model
  - route inventory contract
  - audience-surface route contract
  - route-family ownership and same-shell continuity law
- `patient-portal-experience-architecture-blueprint.md`
- `patient-account-and-communications-blueprint.md`
- `staff-operations-and-support-blueprint.md`
- `operations-console-frontend-blueprint.md`
- `pharmacy-console-frontend-architecture.md`
- `governance-admin-console-frontend-blueprint.md`
- outputs from tasks `004`, `014`, `041-046`
- `forensic-audit-findings.md`, especially findings `91`, `92`, `95`, `96`, `97`, and `114`

Primary outcome:
One machine-readable boundary model proving:
- the legal trust-zone crossings
- the legal browser-compute boundaries
- the route-family-to-gateway mapping
- the mutating bounded contexts each gateway is allowed to target
- the tenant-isolation and recovery posture attached to each surface

Deliverables to create:
1. `docs/architecture/47_trust_zone_boundary_strategy.md`
2. `docs/architecture/47_gateway_surface_map.md`
3. `docs/architecture/47_gateway_surface_split_decisions.md`
4. `docs/architecture/47_trust_zone_and_gateway_studio.html`
5. `docs/architecture/47_trust_zone_gateway_graph.mmd`
6. `data/analysis/trust_zone_boundaries.json`
7. `data/analysis/gateway_bff_surfaces.json`
8. `data/analysis/gateway_route_family_matrix.csv`
9. `data/analysis/gateway_surface_contract_matrix.csv`
10. `tools/analysis/validate_gateway_surface_map.(py|ts)`
11. `tests/playwright/gateway-surface-studio.spec.(ts|js)`

At minimum, define `TrustZoneBoundary` rows with:
- `trustZoneBoundaryId`
- `sourceTrustZoneRef`
- `targetTrustZoneRef`
- `sourceWorkloadFamilyRefs[]`
- `targetWorkloadFamilyRefs[]`
- `allowedProtocolRefs[]`
- `allowedIdentityRefs[]`
- `allowedDataClassificationRefs[]`
- `tenantTransferMode`
- `assuranceTrustTransferMode`
- `egressAllowlistRef`
- `boundaryFailureMode`
- `boundaryState`
- `validatedAt`
- source refs and rationale

At minimum, define `GatewayBffSurface` rows with:
- `surfaceId`
- `audience`
- `routeFamilies`
- `servedBoundedContextRefs[]`
- `mutatingBoundedContextRefs[]`
- `requiredContextBoundaryRefs[]`
- `entryWorkloadFamilyRef`
- `downstreamWorkloadFamilyRefs[]`
- `trustZoneBoundaryRefs[]`
- `tenantIsolationMode`
- `openApiRef` or equivalent contract-ref placeholder
- `asyncChannelRef`
- `projectionSchemaRefs`
- `commandSettlementSchemaRefs`
- `sessionPolicyRef`
- `cachePolicyRef`
- `releaseApprovalFreezeRef` placeholder
- `channelGuardrailProfileRef`
- `recoveryDispositionProfileRef`
- `requiredAssuranceSliceRefs[]`
- manifest/query/command/channel/cache refs placeholders for later tasks
- `surfaceAuthorityTupleHash`
- `runtimePublicationBundleRef` placeholder
- source refs and rationale

Execution steps:
1. Start from task `046`’s runtime-topology manifest and task `004`’s audience/shell/route inventory.
2. Build the trust-zone matrix first:
   - which families may call across zones
   - over which protocols
   - under which identities
   - with which data-class ceilings
   - with what tenant-transfer and assurance-trust-transfer rules
3. Build the gateway-surface inventory second. At minimum, evaluate whether Vecells needs distinct browser-compute surfaces for:
   - public patient entry
   - authenticated patient portal
   - grant-scoped patient transaction routes
   - clinical workspace
   - support workspace
   - hub desk
   - pharmacy console
   - operations console
   - governance/admin shell
   If the corpus proves a different split, document the proof and keep it explicit.
4. For each gateway candidate, decide whether it may remain combined or must split based on:
   - audience
   - route family set
   - session policy
   - cache scope
   - tenant isolation
   - trust-zone boundary set
   - downstream workload set
   - mutating bounded contexts
   - recovery or freeze posture
5. Publish one gateway-route matrix proving every route family has exactly one primary gateway surface owner.
6. Publish one surface-contract matrix proving each gateway names the contexts it may read from and mutate.
7. Build the studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that:
   - no route family belongs to two primary gateway surfaces without an explicit, source-backed exception
   - no gateway surface crosses undeclared trust zones
   - no gateway surface mutates undeclared bounded contexts
   - no browser-visible route is left without a gateway owner
   - no patient/staff/governance surface remains combined when its tenant isolation or session policy differs materially

Mandatory gap closures you must perform:
- Close the “generic BFF can silently become too broad” gap from the runtime blueprint.
- Close the “gateway surfaces do not declare downstream workload families, data classes, or trust boundaries” gap.
- Close the “route families know their shell, but not their gateway owner” gap.
- Close the “patient and staff surfaces can share infrastructure without published separation” gap by publishing the actual separation criteria.
- Close the “browser authority is inferred from implementation and route code” gap by making the gateway map authoritative before frontend manifests are defined in task `050`.
- Close the “support, hub, pharmacy, ops, and governance may get folded into staff by convenience” gap by treating them as distinct audience-surface candidates and proving any merge explicitly.

Industry-best-practice requirements:
- explicit zero-trust style boundary modeling
- least-privilege identity between workload families
- explicit tenant-transfer and assurance-trust-transfer modeling
- no hidden browser-reachable services
- deterministic route-to-gateway mapping
- typed contract rows suitable for validator consumption
- secure defaults for blocked or constrained boundaries

Non-negotiable rules:
- One `GatewayBffSurface` is the only browser-addressable compute boundary for its published audience family or explicitly declared combined audience surface.
- If two route families require different tenant isolation, trust-zone boundaries, downstream workload sets, or recovery-disposition profiles, they must split.
- A gateway may read projections, dispatch commands, and subscribe to live channels only through its declared workload families and trust boundaries.
- Direct adapter egress, raw data-plane access, or undeclared cross-zone fanout from a gateway surface is forbidden.
- Browser-facing mutation may target only the contexts named in `mutatingBoundedContextRefs[]`.
- Undeclared passthrough endpoints, route-local aliases, and fallback BFFs are forbidden.

Mandatory UI and interaction specification for `47_trust_zone_and_gateway_studio.html`:
Name the surface mode `Boundary_Studio`.

Tone:
- minimalist premium internal control surface
- architectural, precise, visibly safer than a generic API map
- must feel like a boundary instrument, not a developer playground

Branding:
- restrained Vecells wordmark plus a compact `GATE` monogram in inline SVG
- trust-zone legend should use thin linework and subtle fills rather than illustrative clouds

Palette:
- canvas `#F7F8FC`
- rail `#EEF2F8`
- panel `#FFFFFF`
- inset `#F3F5FA`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- gateway accent `#315BEA`
- trust accent `#0F9D8A`
- patient accent `#2F5BFF`
- staff accent `#6E59D9`
- blocked accent `#C24141`
- warning accent `#C98900`

Typography:
- system sans stack only
- mono only for surface IDs, trust-zone IDs, hashes, and policy codes

Layout:
- sticky masthead with:
  - gateway count
  - trust-zone boundary count
  - blocked crossing count
  - route-family coverage percentage
- left rail (`296px`):
  - trust-zone filters
  - audience filters
  - gateway cards
- center canvas:
  - one trust-zone map with gateway nodes sitting on zone edges
  - one compact browser-to-downstream lane diagram
- right inspector (`372px`):
  - selected boundary or gateway details
  - route-family list
  - mutating context list
  - session/cache posture
  - recovery posture
- bottom region:
  - route-family matrix
  - trust-zone boundary matrix
  - split-decision log strip

Dimensional constraints:
- max width `1480px`
- masthead `72px`
- map canvas min height `600px`
- gateway cards `min-height 108px`
- primary controls `44px`
- chips `28px`

Motion:
- `120ms` hover/focus
- `180ms` gateway selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by audience, shell type, route family, trust zone, and defect state
- select either a gateway node or boundary edge and keep table parity in sync
- keyboard traversal across gateway cards, map nodes, and matrix rows
- deterministic `data-testid` markers for filters, map canvas, gateway list, boundary list, inspector, and matrices
- every diagram must have adjacent table/list parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- audience filtering
- gateway-node selection
- boundary-edge selection
- route-matrix parity
- inspector rendering
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks

Definition of done:
- Vecells has one explicit trust-zone boundary model and one explicit gateway surface map.
- Every browser-visible route family has one gateway owner.
- Every gateway surface has explicit downstream-workload, mutating-context, trust-zone, tenant-isolation, session, and recovery posture.
- The runtime topology is now ready for event, FHIR, and frontend-manifest publication work in tasks `048-050`.
```