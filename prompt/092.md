# Prompt 092 For 092_par_phase0_track_runtime_provision_preview_environments_and_ephemeral_test_data_reset

Use the shared operating contract first:
[shared_operating_contract_086_to_095.md](/Users/test/Code/V/prompt/shared_operating_contract_086_to_095.md)

```text
Execute roadmap task 092_par_phase0_track_runtime_provision_preview_environments_and_ephemeral_test_data_reset.md.

Inherit the Shared_operating_contract_for_prompts_086_to_095 and consume the validated outputs from tasks `001-091`.

Mission:
Provision governed preview environments and deterministic ephemeral test-data reset for Vecells.

This task must create real preview-runtime capability for:
- branch or release-candidate preview environments
- synthetic seeded tenant, lineage, artifact, and projection data
- deterministic reset and teardown flows
- preview publication bindings, environment lifetimes, and drift detection
- environment-safe diagnostics and browser-verifiable surface stability

This is real runtime and delivery-foundation work. Produce infrastructure, orchestration code, seed tooling, reset tooling, validation, and smoke tests. Do not leave preview setup, seeded data, or reset hygiene as manual operator steps.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially the Phase 0 mandate that preview deploys and local bootstrap exist early, plus the seed-data and simulator expectations from later foundational tasks
- `platform-runtime-and-release-blueprint.md`, especially:
  - `## Operational readiness contract`
  - verification-ladder expectations that synthetic journeys and constrained recovery proof exist
  - runtime publication and artifact-binding rules that preview surfaces must still respect
- `platform-frontend-blueprint.md` and audience-shell blueprints where preview surfaces must preserve shell continuity, anchor stability, placeholder honesty, and deterministic automation anchors
- outputs from tasks `004`, `046`, `050`, `058`, `059`, `084`, `085`, `088`, `089`, and `091`
- `forensic-audit-findings.md`, especially Findings `86`, `87`, `88`, `90`, `91`, `92`, `96`, `100`, `102`, and `112`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must provision preview and reset foundations now, but it must not steal ownership of later backup, restore, canary, release-watch, or final recovery-proof work. Publish bounded seams and record `FOLLOW_ON_DEPENDENCY_*` where appropriate.

Mock_now_execution:
Build the full preview path now using synthetic seeded data and simulator-backed external seams.
- every preview environment must be synthetic-only and safe for screenshots, demos, contract validation, and Playwright automation
- preview resets must restore canonical seed tuples, runtime publication refs, and deterministic data identities rather than merely wiping a subset of rows
- seeded data must exercise intake, identity, triage, booking, hub, pharmacy, communications, support, and degraded-mode examples where relevant to the current runtime baseline
The mock-now path is the actual working path for MVP and should be rich enough to support future partner onboarding, UI development, and release rehearsal.

Actual_production_strategy_later:
When production-like preview infra becomes more sophisticated, it must preserve the same synthetic-only safety posture, deterministic seed identities, TTL enforcement, runtime publication bindings, and reset semantics. Hardening may add stronger isolation, more environment tiers, or managed preview controllers, but it must not let preview environments quietly accumulate uncontrolled PHI or stale runtime tuples.

Primary outcome:
The repository gains deterministic preview environments that are safe, reproducible, and resettable, giving later frontend, runtime, and testing tracks a stable non-production target.

Implementation deliverables to create:
1. infrastructure and orchestration code for preview environment creation, TTL enforcement, teardown, and drift detection
2. deterministic synthetic seed-data packs and scenario manifests for current baseline domains and degraded states
3. preview reset tooling that restores storage, queues, caches, manifests, and seeded projections to exact known tuples
4. machine-readable preview environment manifests and seed-pack manifests
5. browser-safe preview access rules and environment banners so preview truth cannot be confused with production or local ad hoc data
6. `docs/architecture/92_preview_environment_and_seed_reset_design.md`
7. `docs/architecture/92_preview_tuple_and_ephemeral_data_rules.md`
8. `docs/architecture/92_preview_environment_control_room.html`
9. `data/analysis/preview_environment_manifest.json`
10. `data/analysis/preview_seed_pack_manifest.json`
11. `data/analysis/preview_reset_matrix.csv`
12. `tools/analysis/validate_preview_env_and_reset.(py|ts)`
13. preview creation tests, reset tests, TTL tests, drift tests, and seed-integrity smoke tests
14. `tests/playwright/preview-environment-control-room.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- provision ephemeral preview environments bound to one exact runtime tuple and one synthetic seed pack
- publish machine-readable manifests for environment identity, seed pack, reset state, and TTL
- implement deterministic reset across stores, broker state, cache state, object-store seed objects, and projection state needed by current runtime work
- prevent preview environments from becoming production-like mutable truth outside governed synthetic flows
- expose environment banners and markers so preview posture is always obvious in browser-visible surfaces
- support seeded degraded and recovery scenarios, not only healthy-path data

Execution steps:
1. Re-read the preview, seed, runtime publication, and shell-stability expectations from the blueprints and earlier task outputs.
2. Honor the runtime topology, storage, broker, cache, secret, and pipeline baselines from earlier tasks; if prerequisites are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
3. Provision preview environment orchestration, manifests, and TTL controls.
4. Build deterministic seed packs and reset flows that restore exact tuples across relevant runtime substrates.
5. Implement browser-visible environment markers and safe access policy.
6. Build the control-room atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write smoke tests proving that preview reset returns the environment to exact seed posture, wipes drift, and preserves deterministic automation anchors.

Mandatory gap closures you must perform:
- Close the “preview setup is manual” gap by provisioning environments and reset from code.
- Close the “seed data is only happy-path demo content” gap by including degraded and recovery examples.
- Close the “preview reset means truncate a few tables” gap by resetting every relevant runtime substrate and tuple.
- Close the “preview can silently look like production” gap by publishing explicit environment posture and banners.
- Close the “preview drift goes unnoticed” gap by publishing machine-readable manifests and drift detection.

Industry-best-practice requirements:
- synthetic-only non-production data
- deterministic seed identities and idempotent reset
- TTL-based cleanup and environment ownership metadata
- clear separation between preview and production trust
- automation-ready stable anchors and browser markers
- fail-closed behavior on stale or drifted preview tuples

Non-negotiable rules:
- Preview data is synthetic and governed.
- Reset must restore exact tuples, not just partial emptiness.
- Preview environments may not silently persist uncontrolled PHI or stale manifests.
- Browser-visible preview surfaces must clearly signal preview posture.
- Seeded degraded states are required, not optional.

Mandatory UI and interaction specification for `92_preview_environment_control_room.html`:
Name the surface mode `Preview_Environment_Control_Room`.

Tone:
- premium internal platform console
- minimalist, polished, and confidently operational
- should feel like a high-end environment studio, not a generic admin CRUD tool

Branding:
- restrained Vecells wordmark plus inline SVG `PE` monogram
- one environment-card grid
- one seed-pack scenario matrix
- one reset and TTL timeline ribbon

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- preview accent `#2563EB`
- seed accent `#0EA5A4`
- reset accent `#7C3AED`
- TTL accent `#D97706`
- expired accent `#C24141`
- ready accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for environment IDs, seed-pack IDs, tuple hashes, and reset IDs

Layout:
- sticky masthead with active previews, expiring environments, reset failures, and drift alerts
- left rail (`324px`) with owner filters, env-state filters, seed-pack filters, and drift filters
- central canvas with:
  - environment-card grid
  - seed-pack scenario matrix
  - reset / TTL timeline ribbon
- right inspector (`420px`) for selected environment, seed pack, or reset event
- lower grid with one manifest table and one reset / drift table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by owner, environment state, seed pack, drift state, and expiry window
- selecting a card or row must synchronize the grid, matrix, timeline, inspector, and tables
- keyboard navigation across filters and tables
- deterministic `data-testid` markers for environment grid, seed matrix, TTL timeline, manifest table, reset table, and inspector
- every visual must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification
- verification that expired or drifted environments are visibly and semantically distinct from ready ones

Definition of done:
- The repository has deterministic preview environments and reset tooling.
- Preview seeds and resets are machine-readable and reproducible.
- Browser-visible preview posture is explicit and safe.
- Later frontend, release, and testing tracks can rely on previews without manually rebuilding environment truth.
```
