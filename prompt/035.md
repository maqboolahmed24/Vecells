# Prompt 035 For 035_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_provision_transcription_and_malware_scanning_projects

Use the shared operating contract first:
[shared_operating_contract_026_to_035.md](/Users/test/Code/V/prompt/shared_operating_contract_026_to_035.md)

```text
Execute roadmap task 035_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_provision_transcription_and_malware_scanning_projects.md.

Inherit the Shared_operating_contract_for_prompts_026_to_035 and consume the validated outputs from tasks `001-034`, especially tasks `021`, `022`, `023`, `034`, and the evidence-processing architecture from the blueprint corpus.

Mission:
Create the transcription and malware-scanning provisioning pack in a way that respects the blueprint’s evidence and quarantine law:
- transcript jobs and scan jobs are governed evidence-processing contracts with safety implications
- real provider projects and keys are later, gated, and may be billable or PHI-sensitive
- the programme still needs a high-fidelity local evidence-processing lab now

This task therefore has two equally important jobs:
1. build a high-fidelity local transcript / scan lab and project model now
2. build a gated real project / workspace / key / webhook creation strategy later

Algorithm grounding you must honor:
- `phase-2-the-identity-and-echoes.md`, especially transcript readiness and evidence usability states
- `phase-8-the-assistive-layer.md`, especially the audio / transcript / artifact normalization pipeline
- `phase-0-the-foundation-protocol.md`, especially artifact quarantine, evidence assimilation, and fallback-review law
- `platform-runtime-and-release-blueprint.md`, especially adapter and degradation profiles
- `forensic-audit-findings.md`, especially the quarantine, artifact-event, and unreadable-evidence findings

Current commercial-provider mechanics you must respect in the `Actual_provider_strategy_later` section:
Use current official vendor docs from the shortlisted providers in task `034` as the external-process truth for later execution. At minimum support:
- project / workspace creation
- API key and service-account models
- webhook subscription and signing options
- bucket or storage-integration settings where relevant
- retention / deletion / region controls
- malware-scan engine or ruleset configuration where relevant
- billing / spend controls
Do not rely on stale memory.

Primary outcome:
A product-grade local evidence-processing lab and mock project model that Vecells can use now, plus a later real project creation strategy that preserves transcript-readiness, quarantine, and fallback-review semantics.

Deliverables to create:
1. `docs/external/35_local_evidence_processing_lab_spec.md`
2. `docs/external/35_transcription_and_scanning_project_field_map.md`
3. `docs/external/35_webhook_retention_and_region_strategy.md`
4. `docs/external/35_live_gate_and_spend_controls.md`
5. `apps/mock-evidence-gate-lab/README.md`
6. `apps/mock-evidence-gate-lab/src/`
7. `services/mock-transcription-engine/README.md`
8. `services/mock-transcription-engine/src/`
9. `services/mock-artifact-scan-gateway/README.md`
10. `services/mock-artifact-scan-gateway/src/`
11. `tests/playwright/mock-evidence-gate-lab.spec.(ts|js)`
12. `tests/playwright/mock-transcript-and-scan-states.spec.(ts|js)`
13. `tools/browser-automation/evidence-processing-project-dry-run.spec.(ts|js)`
14. `data/analysis/35_evidence_processing_field_map.json`
15. `data/analysis/35_transcript_job_profiles.csv`
16. `data/analysis/35_scan_and_quarantine_policy_matrix.csv`
17. `data/analysis/35_evidence_processing_live_gate_checklist.json`
18. `tools/analysis/validate_evidence_processing_lab_pack.(py|ts)`

Every job-profile row must capture at minimum:
- `job_profile_id`
- `provider_family = transcription | artifact_scanning`
- `environment`
- `input_class`
- `output_state_family`
- `webhook_profile_ref`
- `retention_policy_ref`
- `region_policy_ref`
- `quarantine_policy_ref`
- `mock_now_use`
- `actual_later_use`
- `notes`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build three coordinated artifacts:
1. `services/mock-transcription-engine` — the local transcript provider twin
2. `services/mock-artifact-scan-gateway` — the local scan / malware provider twin
3. `apps/mock-evidence-gate-lab` — the admin and evidence-processing workbench

The local evidence-processing twins must emulate enough behavior to let Vecells genuinely test:
- async job creation and completion
- partial transcript availability
- quality bands and coverage classes
- scan clean vs suspicious vs quarantined vs unreadable vs failed outcomes
- webhook retries and signature validation
- quarantine writeback and fallback-review triggers
- environment separation for local, preview, and provider-like modes

The lab must let the team exercise at minimum:
- project and environment setup
- transcript and scan job profiles
- retention and region policies
- quarantine policy configuration
- evidence event inspection
- failure, retry, and supersession handling
- later live and spend gate visibility for real project creation

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the later real evidence-processing strategy. It must include:
- project / workspace field map
- API key and secret-class placeholders
- webhook subscription and endpoint pack
- region / retention / deletion configuration pack
- scan-policy and bucket / storage integration pack where relevant
- browser-automation dry-run harness for project creation and configuration prep
- live-execution gates that block any real project mutation unless:
  - the provider was approved in task `034`
  - region and retention posture are explicit
  - webhook security and replay controls are ready
  - named approver and target environment are present
  - `ALLOW_REAL_PROVIDER_MUTATION=true` and `ALLOW_SPEND=true` are set where required

Execution steps:
1. Start from task `034`’s shortlist.
2. Build the job-profile and quarantine-policy models first.
3. Implement the mock transcription engine second.
4. Implement the mock scan gateway third.
5. Build the lab fourth and wire it to both services.
6. Use `Playwright_or_other_appropriate_tooling` to drive the lab while developing.
7. Generate the real-later field map and dry-run harness from the same model.
8. Validate that transcript and scan state machines remain consistent with the blueprint’s readiness and quarantine law.

Mandatory gap closures you must perform:
- Close the “project provisioning is just provider admin work” gap by tying projects to evidence-readiness and quarantine semantics.
- Close the “mock evidence-processing services can skip partial and failed states” gap.
- Close the “real projects can be created before webhook security, retention, and region posture are defined” gap.
- Close the “transcription completion means clinical usability” gap.
- Close the “scan completion means evidence may skip quarantine review” gap.

Non-negotiable rules:
- do not upload real PHI or real malware samples to external providers in this task
- do not create real billable projects or keys unless live and spend gates pass
- do not expose real API keys or secrets in repo files or traces
- do not flatten queued, partial, ready, failed, quarantined, and superseded states into one success badge
- do not let project existence substitute for safe evidence processing or fallback review

Mandatory UI and interaction specification for `apps/mock-evidence-gate-lab`:
This must feel like a premium evidence-processing lab, not a generic async-job dashboard.

Name the visual mode `Evidence_Gate_Lab`.

Brand and visual posture:
- include a subtle inline Vecells mark plus a clear `MOCK_EVIDENCE_GATE_LAB` ribbon
- tone: safety-aware, technically exact, minimal, high-trust
- base palette:
  - canvas `#F6F8FB`
  - panel `#FFFFFF`
  - inset `#EEF3F8`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#155EEF`
  - secondary accent `#7A5AF8`
  - transcript accent `#0E9384`
  - scan accent `#B54708`
  - quarantine accent `#C24141`
  - success accent `#12B76A`
- typography:
  - system sans stack only
  - mono only for job IDs, hashes, policy IDs, and webhook IDs

Layout:
- top posture banner with active jobs, quarantine count, webhook health, and mock/actual mode switch
- left job-profile rail
- center transcript / scan workspace
- right inspector for retention, region, and quarantine policies
- lower strip with one restrained pipeline diagram of `ingest -> scan -> quarantine_or_pass -> transcript -> readiness -> fallback_or_continue`

Dimensional constraints:
- page max width `1440px`
- job-profile rail `280px`
- inspector `360px`
- workspace cards `min-height 160px`
- buttons `44px`
- chip height `28px`
- sticky header `72px`

Unique page set:
1. `Project_and_Environment_Setup`
2. `Transcript_Job_Profiles`
3. `Scan_and_Quarantine_Policies`
4. `Evidence_Event_Inspector`
5. `Live_Gates_and_Retention_Posture`

Motion:
- `120ms` hover and focus
- `180ms` panel transitions
- `240ms` inspector transitions
- reduced-motion support mandatory

Interaction requirements:
- job-profile switching
- transcript and scan state previews
- quarantine-path visualization
- keyboard-first interaction
- deterministic `data-testid` markers for job rail, workspace, event inspector, policy drawer, and mode toggle

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and testing from the start. At minimum cover:
- job-profile switching
- transcript partial / ready / failed rendering
- scan clean / suspicious / quarantined / unreadable rendering
- webhook retry and signature states
- mock/actual gating
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- one high-fidelity local evidence-processing lab exists and is demonstrably useful now
- one later real project creation strategy exists with hard live and spend gates
- the output is usable as the authoritative handoff for transcription and scan project provisioning without sacrificing readiness or quarantine semantics
```
