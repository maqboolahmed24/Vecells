# Prompt 098 For 098_par_phase0_track_runtime_implement_dependency_degradation_profile_execution_engine

Use the shared operating contract first:
[shared_operating_contract_096_to_105.md](/Users/test/Code/V/prompt/shared_operating_contract_096_to_105.md)

```text
Execute roadmap task 098_par_phase0_track_runtime_implement_dependency_degradation_profile_execution_engine.md.

Inherit the Shared_operating_contract_for_prompts_096_to_105 and consume the validated outputs from tasks `001-097`.

Mission:
Implement the authoritative dependency-degradation execution engine for Vecells.

This task must turn `DependencyDegradationProfile` from a documented object into real runtime behavior across gateways, projections, commands, integration workers, and audience-facing recovery posture.

This is real backend and runtime-control work. Produce schemas, profile compilers, runtime decision engines, bindings into gateway and worker flows, observability, rehearsal tooling, and tests. Do not leave degraded mode to generic error handlers, framework retries, or shell-local banners.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### DependencyDegradationProfile`
  - `### AdapterContractProfile`
  - `RuntimeTopologyManifest`
  - `GatewayBffSurface`
  - `ReleaseRecoveryDisposition`
  - verification-ladder and operational-readiness requirements for dependency degradation and fallback behavior
- `phase-0-the-foundation-protocol.md`, especially workload-family boundaries, trust boundaries, freeze and trust rules, route-intent law, command-settlement law, and closure-blocker law
- `platform-frontend-blueprint.md` for calm degraded state contracts and route-family recovery posture
- `patient-account-and-communications-blueprint.md`, `staff-workspace-interface-architecture.md`, `operations-console-frontend-blueprint.md`, and `pharmacy-console-frontend-architecture.md` for audience-specific fallback semantics
- outputs from tasks `008`, `021-040`, `047`, `057`, `083`, `090`, `094`, `096`, and `097`
- `forensic-audit-findings.md`, especially Findings `88`, `89`, `91`, `96`, `112`, and `120`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must publish the shared degradation-execution engine now so later shell, canary, ops, pharmacy, and assurance work can consume the same bounded fallback law. If a later task owns a richer audience projection or evidence surface, publish the runtime seam here and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build the degradation engine now using simulator-backed dependency states, seeded failure modes, and preview route families.
- support isolated projection failure, integration-only failure, queue-only fallback, command halt, gateway read-only fallback, and patient-safe placeholder downgrade
- prove that bounded degradation propagates only to the allowed workload families and audiences
- allow rapid rehearsal of failure-mode transitions in local and non-production environments
The mock-now path must use the same profile schema and runtime engine that later live dependency health feeds will use.

Actual_production_strategy_later:
When live dependency health feeds and real outages are involved, preserve the same profile schema, escalation boundaries, assurance-trust effects, topology fallback modes, and audience-specific recovery semantics. Production hardening may add richer health probes, incident integration, or auto-remediation hooks, but it must not collapse slice-bounded degradation into generic platform-wide outage behavior.

Primary outcome:
The repository gains one data-driven degradation engine that can map a dependency failure into the exact allowed runtime, audience, and assurance consequences.

Implementation deliverables to create:
1. `docs/architecture/98_dependency_degradation_engine_design.md`
2. `docs/architecture/98_workload_family_fallback_and_escalation_rules.md`
3. `docs/architecture/98_dependency_degradation_atlas.html`
4. `data/analysis/dependency_degradation_profiles.json`
5. `data/analysis/dependency_failure_mode_matrix.csv`
6. `data/analysis/audience_fallback_matrix.csv`
7. `packages/api-contracts/schemas/dependency-degradation-profile.schema.json`
8. runtime-control code for profile resolution, failure-mode ingestion, escalation bounding, audience fallback computation, and assurance-trust effect publication
9. gateway and worker integration hooks that consume the engine instead of route-local error logic
10. `tools/analysis/validate_dependency_degradation_profiles.(py|ts)`
11. unit, integration, and rehearsal tests for bounded degradation, escalation ceilings, recovery, and freeze interaction
12. `tests/playwright/dependency-degradation-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- schema-validated `DependencyDegradationProfile` rows with dependency code, failure modes, patient fallback state, staff fallback state, impacted workload families, maximum escalation families, assurance-trust effect, topology fallback mode, retry policy, and alert threshold
- a runtime execution engine that maps observed dependency failure into allowed consequences without requiring route-local interpretation
- enforcement that degradation cannot escalate beyond `maximumEscalationFamilyRefs[]`
- audience-aware fallback decisions that distinguish patient-safe placeholders, gateway read-only, command halt, projection stale, and integration queue-only behavior
- explicit publication of assurance trust implications so slice-bounded degradation remains slice-bounded unless the profile says otherwise
- integration with route recovery posture and browser contracts so degraded state is visible, deterministic, and testable
- recovery path handling that transitions back from degraded posture only after the dependency condition and all governing publication/trust conditions permit it

Implementation instructions:
1. Create one profile compiler that normalizes profile data into executable runtime decisions. Do not parse profile JSON ad hoc in every service.
2. Bind profile execution into at least these runtime paths:
   - gateway read resolution
   - browser mutation refusal / downgrade
   - projection-worker stale or degraded publication
   - integration-worker queue-only or command-halt fallback
   - patient and staff recovery posture binding
3. Distinguish at least these failure-mode classes:
   - dependency timeout or transport loss
   - semantic contract mismatch
   - callback ambiguity or replay gap
   - upstream accepted-pending-confirmation stall
   - hard denial or trust revocation
4. Make degradation visible but calm:
   - patient surfaces must preserve summary and next safe action without leaking hidden data or pretending success
   - staff surfaces must preserve current anchor, explanation, and next safe recovery action
   - operations surfaces must retain diagnostic clarity and blast-radius visibility
5. Never allow these anti-patterns:
   - broad kill-switch because one isolated integration is degraded
   - patient CTA remains live because gateway cached previous capability
   - transport recovery instantly clears degraded posture without fresh publication or trust proof
   - generic banner replaces route-specific recovery posture when the blueprint requires a bounded fallback
6. Publish one degradation atlas that engineers can inspect. It must show dependency -> failure mode -> impacted families -> audience fallback -> assurance effect -> recovery path from one dataset.

Gap handling you must perform during this task:
- If a dependency from tasks `021-040` lacks a published degradation profile, emit `GAP_DEPENDENCY_PROFILE_*` and fail closed for live authority.
- If audience fallback copy or placeholder details are under-specified, bind placeholder-safe states now and record `FOLLOW_ON_DEPENDENCY_CONTENT_*` rather than inventing rich prose.
- If a dependency can affect more than one workload family but the corpus does not yet declare the escalation ceiling, publish the narrowest safe ceiling and record `GAP_RESOLUTION_ESCALATION_CEILING_*`.

Backend engineering standards:
- use deterministic profile resolution and canonical serialization
- keep decision logic side-effect-free where possible and separate from effect execution
- make escalation and recovery decisions observable and auditable
- support chaos-style rehearsal and simulator-driven state injection
- add metrics for degraded-entry count, recovery lag, bounded-versus-blocked outcomes, and fallback mode frequency

Verification requirements:
- schema and compiler tests
- bounded-escalation tests
- patient/staff/ops fallback behavior tests
- recovery and re-entry tests
- assurance-trust effect tests
- `Playwright_or_other_appropriate_tooling` tests for the atlas surface and any browser-visible fallback specimen shown there

Definition of done:
- every tracked dependency can be mapped through one executable degradation profile
- bounded fallback is enforced by runtime code, not by convention
- audience-specific degraded posture is explicit and testable
- assurance-trust effects are surfaced correctly
- degradation and recovery behavior can be rehearsed without provider-specific code rewrites
```
