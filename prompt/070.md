# Prompt 070 For 070_par_phase0_track_backend_implement_duplicate_cluster_and_duplicate_pair_evidence_models

Use the shared operating contract first:
[shared_operating_contract_066_to_075.md](/Users/test/Code/V/prompt/shared_operating_contract_066_to_075.md)

```text
Execute roadmap task 070_par_phase0_track_backend_implement_duplicate_cluster_and_duplicate_pair_evidence_models.md.

Inherit the Shared_operating_contract_for_prompts_066_to_075 and consume the validated outputs from tasks `001-069`.

Mission:
Implement the canonical duplicate-review evidence substrate centered on:
- `DuplicateCluster`
- `DuplicatePairEvidence`
- the mandatory supporting `DuplicateResolutionDecision` required by the blueprint to settle cluster meaning
plus the bounded calculators, repositories, validators, and test fixtures needed to make retry collapse, same-request continuation, same-episode review, related-episode linkage, and separation auditable.

This is model-plus-core-logic implementation work. It is not the final duplicate-orchestration service, but it must encode the cluster, evidence, and decision invariants that later workflows will use.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.7 DuplicateCluster`
  - `#### 1.7A DuplicatePairEvidence`
  - `#### 1.7B DuplicateResolutionDecision`
  - the duplicate / replay separation rules in the canonical lifecycle algorithm
- `phase-1-the-red-flag-gate.md`, especially the duplicate-submit and same-lineage continuation rules
- `vecells-complete-end-to-end-flow.md`, especially the `I_DUP` branch semantics
- `forensic-audit-findings.md`, especially Findings `05`, `19`, `35`, and the later corrections that duplicate review must remain explicit and closure-blocking where unresolved
- outputs from tasks `001-007`, `019`, `041-045`, `056-057`, `061-069`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Do not assume later duplicate-orchestration or triage services exist. If necessary, publish a bounded duplicate-decision interface stub and record `PARALLEL_INTERFACE_GAP_*`.

Mock_now_execution:
Build a synthetic duplicate-evidence simulator now. It must generate realistic pairwise cases for:
- exact retry collapse
- same-request continuation requiring continuity witness
- same-episode candidate with high but non-decisive similarity
- related-episode linkage
- clear separate request
- conflicting candidates with low margin and high uncertainty
The simulator must feed the same pair-evidence and cluster objects production will use later.

Actual_production_strategy_later:
When live calibration, channel-specific duplicate features, or production telemetry arrive, they must update model versions, threshold policies, and calibration refs only through the same `DuplicatePairEvidence` and `DuplicateCluster` contracts. Real-world evidence may improve pair scoring, but it may not erase the need for explicit `DuplicateResolutionDecision` or continuity witnesses.

Primary outcome:
The repository gains a durable duplicate-evidence and review substrate that later intake, telephony, portal, and staff workflows can consume without inventing ad hoc merge heuristics.

Implementation deliverables to create:
1. domain and persistence code for `DuplicateCluster`, `DuplicatePairEvidence`, and `DuplicateResolutionDecision`
2. bounded calculation utilities for cluster materialization, confidence / instability updates, and candidate-margin validation
3. validators enforcing non-transitivity, explicit continuity witness, and reversible supersession rules
4. `docs/architecture/70_duplicate_cluster_and_pair_evidence_design.md`
5. `docs/architecture/70_duplicate_resolution_rules.md`
6. `docs/architecture/70_duplicate_resolution_workbench.html`
7. `data/analysis/duplicate_cluster_manifest.json`
8. `data/analysis/duplicate_pair_evidence_matrix.csv`
9. `data/analysis/duplicate_resolution_casebook.json`
10. `tools/analysis/validate_duplicate_models.(py|ts)`
11. unit tests, simulation tests, repository tests, and integration tests
12. `tests/playwright/duplicate-resolution-workbench.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- persist immutable pairwise evidence with class probabilities, margins, uncertainty, blockers, model version, calibration ref, and threshold policy
- materialize `DuplicateCluster` as the review container rather than the final settlement
- persist `DuplicateResolutionDecision` as the only legal settlement object for attach, retry collapse, same-episode link, related-episode link, separation, or review requirement
- support supersession or reversion of prior duplicate decisions without mutating prior lineage history in place
- require explicit continuity witness for `same_request_attach`
- keep duplicate review closure-blocking while uncertainty, conflict, or review-required posture remains open

Execution steps:
1. Implement immutable `DuplicatePairEvidence` with all canonical fields, including class probabilities, candidate margins, uncertainty, and hard blockers.
2. Implement `DuplicateCluster` with member refs, candidate refs, pairwise evidence refs, confidence, instability state, and review status.
3. Implement `DuplicateResolutionDecision` even though it is not named in the task title; the source algorithm requires it as the settlement authority. Treat this as a mandatory gap closure.
4. Implement validators that enforce:
   - `same_episode_candidate` is clustering signal only, never settlement
   - pairwise candidate edges are not transitive proof
   - `same_request_attach` requires an explicit continuity witness
   - retry collapse, same-request attach, same-episode link, related-episode link, and separation remain separately auditable
   - unresolved or review-required clusters remain closure-blocking where policy says so
5. Implement simulation fixtures for high-confidence, low-confidence, conflicting-candidate, and reversible-resolution scenarios.
6. Build the workbench and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.

Mandatory gap closures you must perform:
- Close the “duplicate cluster alone is treated as settlement” gap by implementing `DuplicateResolutionDecision` explicitly.
- Close the “pairwise similarity can auto-attach same-request continuation” gap by requiring continuity witness for `same_request_attach`.
- Close the “pairwise edges are transitive proof” gap by validating canonical-centered and conflict-free rules explicitly.
- Close the “duplicate handling and idempotent replay are the same thing” gap by keeping these objects distinct from `IdempotencyRecord`.
- Close the “reversing a duplicate decision rewrites history” gap by using supersession or revert semantics rather than mutation in place.

Industry-best-practice requirements:
- immutable evidence rows with model-version provenance
- explicit decision objects separate from evidence containers
- deterministic simulation fixtures and threshold-policy snapshots
- repository-level support for supersession and reversibility
- no hidden auto-merge heuristics in controllers or projections

Non-negotiable rules:
- `DuplicateCluster` is the review container, not the settlement.
- `DuplicateResolutionDecision` is the only legal settlement for duplicate review outcomes.
- Pairwise evidence is not transitive proof.
- Same-request attach requires explicit continuity witness.
- Unresolved duplicate review must stay visible and block calm false closure where policy requires it.

Mandatory UI and interaction specification for `70_duplicate_resolution_workbench.html`:
Name the surface mode `Duplicate_Resolution_Workbench`.

Tone:
- premium internal investigation workbench
- should feel like a careful evidence desk, not an alert wall
- precise, quiet, and review-friendly

Branding:
- restrained Vecells wordmark plus inline SVG `DR` monogram
- one cluster constellation diagram
- one pair-evidence comparison lane
- one resolution-decision timeline

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F8`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- evidence accent `#3559E6`
- cluster accent `#0EA5A4`
- review accent `#7C3AED`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for model refs, evidence hashes, cluster IDs, and decision IDs

Layout:
- masthead with open clusters, resolved clusters, review-required cases, and unstable clusters
- left rail (`304px`) for relation-type, review-status, and uncertainty-band filters
- center canvas with cluster constellation, pair-comparison lane, and decision cards
- right inspector (`408px`) for selected cluster / pair / decision details
- lower region with evidence table and supersession history table

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- constellation min height `260px`
- card min height `172px`
- controls `44px`

Motion:
- `120ms` hover/focus
- `180ms` filter and selection changes
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by relation type, review state, and uncertainty band
- selecting a cluster must synchronize constellation, comparison lane, inspector, and tables
- keyboard navigation across filters, cards, and rows
- deterministic `data-testid` markers for constellation, comparison lane, inspector, evidence table, and supersession history
- every diagram needs adjacent textual parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- relation and status filtering
- cluster selection synchronization
- diagram / table parity
- keyboard navigation
- reduced motion
- responsive layout
- accessibility smoke checks

Definition of done:
- The repository has real duplicate-evidence and duplicate-decision models with validators.
- Retry collapse, same-request attach, same-episode review, related-episode linkage, and separation are durably auditable.
- Later intake and staff workflows can consume explicit duplicate truth instead of ad hoc merge heuristics.
```
