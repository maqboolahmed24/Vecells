# Prompt 043 For 043_seq_phase0_scaffold_api_gateway_command_api_projection_worker_and_notification_worker_services

Use the shared operating contract first:
[shared_operating_contract_036_to_045.md](/Users/test/Code/V/prompt/shared_operating_contract_036_to_045.md)

```text
Execute roadmap task 043_seq_phase0_scaffold_api_gateway_command_api_projection_worker_and_notification_worker_services.md.

Inherit the Shared_operating_contract_for_prompts_036_to_045 and consume the validated outputs from tasks `001-042`.

Mission:
Scaffold the canonical runtime service skeletons for Vecells:
- API Gateway
- Command API
- Projection Worker
- Notification Worker

The goal is to create contract-correct, secure, observable, buildable service foundations that embody the blueprint’s architecture before feature logic is layered in.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `0A. Delivery skeleton and repository architecture`
  - `CanonicalEventContract`
  - lifecycle / identity / safety / reservation / access objects that imply command and projection flows
  - the rule that truth lives in kernel, event contracts, and persistence, not in shells
- `platform-runtime-and-release-blueprint.md`, especially:
  - `RuntimeWorkloadFamily`
  - `RuntimeTopologyManifest`
  - `GatewayBffSurface`
  - `ProjectionContractFamily`
  - `ProjectionQueryContract`
  - `MutationCommandContract`
  - `LiveUpdateChannelContract`
  - `ClientCachePolicy`
  - `DependencyDegradationProfile`
  - `ReleaseGateEvidence`
  - `SchemaMigrationPlan`
  - `ProjectionBackfillPlan`
  - verification ladder and operational readiness contract
- `platform-frontend-blueprint.md`, especially the frontend/backend integration boundary contract and command-following-read rules
- `forensic-audit-findings.md`, especially findings on idempotency, degraded receipt, event-catalog completeness, projection freshness, route/settlement hardening, support replay restore, continuity evidence, and topology publication drift
- outputs from tasks `005`, `007`, `011-016`, `040`, `041`, and `042`

Primary outcome:
Buildable service skeletons with typed configs, health/readiness endpoints, secure internal boundaries, observability, event and API contract stubs, and test harnesses ready for later feature work.

Deliverables to create:
1. `services/api-gateway/` scaffold
2. `services/command-api/` scaffold
3. `services/projection-worker/` scaffold
4. `services/notification-worker/` scaffold
5. `docs/architecture/43_service_scaffold_map.md`
6. `docs/architecture/43_runtime_service_sequence.mmd`
7. `data/analysis/service_interface_manifest.json`
8. `tools/analysis/validate_service_scaffold.(py|ts)`
9. integration and unit test harnesses for each service according to the chosen stack

For each service, scaffold at minimum:
- typed config loader with environment validation
- structured logging bootstrap
- correlation / trace ID propagation
- health and readiness endpoints or equivalents
- dependency registration container or explicit composition root
- test entrypoints
- README with service purpose, ownership, and allowed dependencies

Service-specific requirements:

`api-gateway`
- own ingress HTTP boundary, auth/session edge adapters, request correlation, rate limiting, and route-to-BFF wiring
- do not let it own domain truth
- include stub wiring for release / publication / route-freeze awareness

`command-api`
- own mutation command ingress, request validation, idempotency envelope hook, command action / settlement skeletons, and outbox publication seam
- include placeholders for scoped mutation gates and route-intent validation hooks
- do not implement feature logic yet; scaffold the seams only

`projection-worker`
- own event-consumption scaffold, projection rebuild hooks, backfill hooks, projection freshness markers, and dead-letter / poison-event handling seams
- include placeholders for continuity, trust, and stale-read posture projections

`notification-worker`
- own dispatch-envelope scaffold, provider adapter boundary, delivery settlement seam, controlled resend hook, and safe secret-use boundaries
- include placeholder channel adapters only; no live provider credentials

Execution steps:
1. Start from the monorepo scaffold and topology manifest.
2. Create each service with the canonical runtime concerns above.
3. Produce one service-interface manifest capturing exposed ports, queues/topics, contract packages, and ownership.
4. Produce one runtime service sequence diagram showing request ingress, command path, event path, projection path, and notification path.
5. Implement service-level validation scripts and tests.
6. Validate that no service reaches into another context’s private package internals.

Mandatory gap closures you must perform:
- Close the “service names exist but runtime responsibilities are fuzzy” gap by writing service-purpose manifests.
- Close the “gateway or worker might become hidden truth owner” gap by constraining service responsibilities explicitly.
- Close the “projection freshness and stale posture come later” gap by scaffolding those seams now.
- Close the “notification worker is just a vendor wrapper” gap by scaffolding settlement and resend seams, not just send functions.
- Close the “idempotency and outbox are feature-phase concerns” gap by wiring the seams now.

Industry-best-practice requirements:
- secure by default configuration validation
- explicit internal/external port separation
- structured logs and OpenTelemetry hooks
- least-privilege secret boundaries
- graceful shutdown
- retry policies that distinguish transient vs permanent failure
- dead-letter and poison-message handling seams
- deterministic builds and tests
- no hidden network calls in unit tests

Non-negotiable rules:
- Do not let any service bypass contract packages and reach directly into another context’s private models.
- Do not let any service infer success from partial downstream evidence where the blueprint requires settlement or proof.
- Do not embed real provider credentials or URLs in source fixtures.
- Do not turn the gateway into a generic pass-through without policy hooks.

Definition of done:
- The four canonical runtime services exist as buildable skeletons.
- Their boundaries, configs, observability, and contract seams are explicit.
- Later feature work can extend them without re-litigating core runtime responsibilities.
```
