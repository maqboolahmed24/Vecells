# Prompt 033 For 033_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_create_sms_and_email_notification_projects

Use the shared operating contract first:
[shared_operating_contract_026_to_035.md](/Users/test/Code/V/prompt/shared_operating_contract_026_to_035.md)

```text
Execute roadmap task 033_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_create_sms_and_email_notification_projects.md.

Inherit the Shared_operating_contract_for_prompts_026_to_035 and consume the validated outputs from tasks `001-032`, especially tasks `021`, `022`, `023`, `031`, and the communication semantics from the patient and callback blueprints.

Mission:
Create the SMS and email project-provisioning pack in a way that respects the blueprint’s delivery-truth law:
- templates, senders, routing plans, delivery callbacks, bounce semantics, disputes, and resend repair are governed product concerns
- real provider projects and sender/domain verification are later, gated, and potentially billable or reputation-impacting
- the programme still needs a high-fidelity local notification studio now

This task therefore has two equally important jobs:
1. build a high-fidelity local notification studio and mock project model now
2. build a gated real project / sender / domain / webhook creation strategy later

Algorithm grounding you must honor:
- `patient-account-and-communications-blueprint.md`, especially patient-facing delivery-health and request-summary truth
- `callback-and-clinician-messaging-loop.md`, especially:
  - communication envelopes
  - conversation settlement
  - delivery evidence bundles
  - repair / resend logic
- `phase-0-the-foundation-protocol.md`, especially command-settlement, reachability, and audit law
- `platform-runtime-and-release-blueprint.md`, especially adapter and degradation profiles
- `forensic-audit-findings.md`, especially findings where delivery ambiguity, contact-route repair, or continuity fragmentation mattered

Current commercial-provider mechanics you must respect in the `Actual_provider_strategy_later` section:
Use current official vendor docs from the shortlisted SMS and email providers in task `031` as the external-process truth for later execution. At minimum support:
- project / workspace creation
- sender / phone / email / domain verification requirements
- webhook and event-subscription configuration
- template and environment separation
- test / sandbox posture
- billing / spend controls
- message retention and log-export controls
Do not rely on stale memory.

Primary outcome:
A product-grade local notification studio and mock project model that Vecells can use now, plus a later real project creation strategy that preserves delivery truth and repair semantics.

Deliverables to create:
1. `docs/external/33_local_notification_studio_spec.md`
2. `docs/external/33_notification_project_field_map.md`
3. `docs/external/33_sender_domain_and_webhook_strategy.md`
4. `docs/external/33_notification_live_gate_and_spend_controls.md`
5. `apps/mock-notification-studio/README.md`
6. `apps/mock-notification-studio/src/`
7. `services/mock-notification-rail/README.md`
8. `services/mock-notification-rail/src/`
9. `tests/playwright/mock-notification-studio.spec.(ts|js)`
10. `tests/playwright/mock-notification-delivery-truth.spec.(ts|js)`
11. `tools/browser-automation/notification-project-dry-run.spec.(ts|js)`
12. `data/analysis/33_notification_project_field_map.json`
13. `data/analysis/33_sender_and_domain_matrix.csv`
14. `data/analysis/33_template_and_routing_plan_registry.csv`
15. `data/analysis/33_notification_live_gate_checklist.json`
16. `tools/analysis/validate_notification_studio_pack.(py|ts)`

Every template row must capture at minimum:
- `template_id`
- `channel = sms | email | dual_if_supported`
- `message_intent`
- `personalisation_fields[]`
- `routing_plan_ref`
- `sender_identity_ref`
- `delivery_callback_required`
- `supports_markdown_or_rich_copy_rules`
- `mock_now_use`
- `actual_later_use`
- `notes`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build two coordinated artifacts:
1. `services/mock-notification-rail` — the local SMS / email rail twin
2. `apps/mock-notification-studio` — the admin and template / routing workbench

The local notification twin must emulate enough behavior to let Vecells genuinely test:
- template rendering and versioning
- sender identity and domain placeholders
- delivery accepted vs delivered vs delayed vs bounced vs disputed
- webhook retries and signature validation
- unsubscribe / non-delivery / invalid-recipient branches where appropriate
- controlled resend and repair flow triggers
- environment separation for local, preview, and provider-like modes

The studio must let the team exercise at minimum:
- project and environment setup
- template gallery and validation
- routing-plan authoring
- sender / domain placeholder configuration
- delivery event inspection
- bounce / dispute / resend repair handling
- later spend-gate visibility for real project creation

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the later real notification strategy. It must include:
- project / workspace field map
- sender and domain verification pack
- webhook subscription and endpoint pack
- template migration and environment-separation plan
- browser-automation dry-run harness for project creation and configuration prep
- live-execution gates that block any real project mutation unless:
  - the provider was approved in task `031`
  - sender or domain ownership posture is explicit
  - webhook security and replay controls are ready
  - named approver and target environment are present
  - `ALLOW_REAL_PROVIDER_MUTATION=true` and `ALLOW_SPEND=true` are set where required

Execution steps:
1. Start from task `031`’s SMS and email shortlist.
2. Build the template and routing registry first.
3. Implement the mock notification rail second.
4. Build the studio third and wire it to the rail.
5. Use `Playwright_or_other_appropriate_tooling` to drive the studio while developing.
6. Generate the real-later field map and dry-run harness from the same model.
7. Validate that delivery callbacks, repair, and resend semantics remain consistent with the blueprint.

Mandatory gap closures you must perform:
- Close the “project creation is just a provider admin task” gap by tying projects to delivery truth, resend repair, and template governance.
- Close the “SMS and email can share one undifferentiated sender model” gap by separating sender/domain concerns.
- Close the “mock notification rail can ignore bounce, dispute, and repair” gap.
- Close the “real provider projects can be created before webhook security is designed” gap.
- Close the “template success means patient-visible success” gap by preserving transport vs authoritative outcome separation.

Non-negotiable rules:
- do not send real messages or verify real domains unless live and spend gates pass
- do not expose real API keys or sender secrets in repo files or traces
- do not flatten accepted, delivered, bounced, disputed, and repaired states into one success badge
- do not let the mock skip controlled resend or delivery-repair semantics
- do not let project existence substitute for patient-safe delivery truth

Mandatory UI and interaction specification for `apps/mock-notification-studio`:
This must feel like a premium communication design studio, not a generic template admin.

Name the visual mode `Quiet_Send_Studio`.

Brand and visual posture:
- include a subtle inline Vecells mark plus a clear `MOCK_NOTIFICATION_STUDIO` ribbon
- tone: calm, editorial, exact, support-aware
- base palette:
  - canvas `#F7F8FA`
  - panel `#FFFFFF`
  - inset `#EFF2F6`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#2457F5`
  - secondary accent `#C11574`
  - sms accent `#B54708`
  - email accent `#7A5AF8`
  - success accent `#12B76A`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for template IDs, sender IDs, webhook IDs, and hashes

Layout:
- top status banner with active templates, sender readiness, webhook health, and mock/actual mode switch
- left template / project rail
- center composition and routing workspace
- right inspector for sender, domain, and delivery-truth settings
- lower strip with one restrained flow diagram of `compose -> route -> accepted -> delivered_or_bounced -> repair_or_settle`

Dimensional constraints:
- page max width `1440px`
- template rail `280px`
- inspector `360px`
- workspace cards `min-height 160px`
- buttons `44px`
- chip height `28px`
- sticky header `72px`

Unique page set:
1. `Project_and_Environment_Setup`
2. `Template_Gallery`
3. `Routing_Plan_Studio`
4. `Delivery_Truth_Inspector`
5. `Live_Gates_and_Sender_Readiness`

Motion:
- `120ms` hover and focus
- `180ms` panel transitions
- `240ms` inspector transitions
- reduced-motion support mandatory

Interaction requirements:
- template version switching
- routing-plan previews
- delivery-state timeline inspection
- keyboard-first interaction
- deterministic `data-testid` markers for template rail, workspace, delivery timeline, inspector, and mode toggle

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and testing from the start. At minimum cover:
- template switching
- routing-plan validation
- delivery event visualization
- bounce / dispute / repair rendering
- mock/actual gating
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- one high-fidelity local notification studio exists and is demonstrably useful now
- one later real project creation strategy exists with hard live and spend gates
- the output is usable as the authoritative handoff for SMS and email project provisioning without sacrificing delivery-truth semantics
```
