# Prompt 054 For 054_seq_phase0_define_tenant_isolation_and_acting_scope_tuple_model

Use the shared operating contract first:
[shared_operating_contract_046_to_055.md](/Users/test/Code/V/prompt/shared_operating_contract_046_to_055.md)

```text
Execute roadmap task 054_seq_phase0_define_tenant_isolation_and_acting_scope_tuple_model.md.

Inherit the Shared_operating_contract_for_prompts_046_to_055 and consume the validated outputs from tasks `001-053`.

Mission:
Define the tenant-isolation strategy and the shared `ActingScopeTuple` model so runtime isolation, audience visibility, purpose-of-use, organisation context, elevation, break-glass posture, and blast radius are bound into one machine-checkable authority model.

This task must prevent cross-organisation or multi-tenant work from remaining writable under stale scope, ambient role, remembered org selectors, or mixed tuple assembly.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`
  - `StaffIdentityContext`
  - `ActingContext`
  - `ActingScopeTuple`
  - `ActingContextDriftRecord`
  - `AudienceVisibilityCoverage`
  - `MinimumNecessaryContract`
  - the invariants `23A`, `23B`, `44D`, and `44E` around acting-scope and multi-tenant blast radius
  - the runtime / route-intent / binding algorithms around staff and governance scope checks
- `platform-runtime-and-release-blueprint.md`
  - runtime topology and gateway tenant-isolation rules
  - `tenantIsolationMode`
  - `tenantTransferMode`
  - wave/watch tuple blast-radius fields
- `platform-frontend-blueprint.md`
  - shell and route-family visibility requirements
  - support, hub, governance, and operations surface distinctions
- `staff-operations-and-support-blueprint.md`
- `governance-admin-console-frontend-blueprint.md`
- `platform-admin-and-config-blueprint.md`
- `phase-5-the-network-horizon.md` for cross-organisation coordination posture
- outputs from tasks `003`, `004`, `010`, `011`, `015`, `046`, `047`, and `053`
- `forensic-audit-findings.md`, especially finding `114`

Primary outcome:
One explicit model proving:
- how tenant isolation works in runtime and browser surfaces
- when staff work is single-tenant vs organisation-group vs multi-tenant vs platform-scoped
- how acting scope is issued and superseded
- how drift is detected
- how writable posture freezes on scope drift
- how blast radius is surfaced for governance/release work

Deliverables to create:
1. `docs/architecture/54_tenant_isolation_strategy.md`
2. `docs/architecture/54_acting_scope_tuple_model.md`
3. `docs/architecture/54_scope_and_isolation_atlas.html`
4. `data/analysis/tenant_isolation_modes.json`
5. `data/analysis/acting_scope_tuple_schema.json`
6. `data/analysis/route_to_scope_requirements.csv`
7. `data/analysis/acting_context_drift_triggers.json`
8. `data/analysis/surface_to_blast_radius_matrix.csv`
9. `tools/analysis/validate_tenant_scope_model.(py|ts)`
10. `tests/playwright/scope-isolation-atlas.spec.(ts|js)`

At minimum, your acting-scope model must capture:
- `StaffIdentityContext`
- `ActingContext`
- `ActingScopeTuple`
- `ActingContextDriftRecord`
- route-family scope requirements
- audience visibility coverage linkage
- minimum-necessary contract linkage
- required runtime binding refs
- required trust refs
- affected tenant / organisation counts
- tuple hash semantics
- issuance, expiry, supersession, and freeze behavior

Execution steps:
1. Start from:
   - persona / audience / shell / route inventory from task `004`
   - data classification and disclosure posture from task `010`
   - cloud/runtime topology and gateway model from tasks `011`, `046`, and `047`
   - audit/WORM strategy from task `053`
2. Publish the tenant-isolation strategy:
   - which surfaces are single-tenant
   - which are organisation-group
   - which are multi-tenant
   - which are platform-scoped
   - how runtime isolation and browser isolation correspond
3. Publish the acting-scope tuple schema using the Phase 0 fields:
   - staff identity
   - acting context
   - scope mode
   - tenant refs
   - organisation refs
   - environment ref
   - policy plane ref
   - purpose-of-use ref
   - elevation state
   - break-glass state
   - required visibility coverage refs
   - required runtime binding refs
   - required trust refs
   - affected tenant and organisation counts
   - tuple hash
4. Produce the route-to-scope matrix. At minimum cover:
   - clinical workspace routes
   - support workspace routes
   - hub routes
   - pharmacy console routes
   - operations routes
   - governance/admin routes
   - any cross-organisation or servicing-site routes named in the corpus
5. Define acting-context drift triggers and same-shell freeze behavior for:
   - organisation switch
   - tenant-scope change
   - environment change
   - policy-plane change
   - purpose-of-use change
   - elevation expiry
   - break-glass revocation
   - visibility-contract drift
6. Publish blast-radius rules for multi-tenant and platform-scoped work:
   - explicit affected tenant count
   - explicit affected organisation count
   - where those counts must appear before approval or mutation may settle
7. Build the atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that:
   - every relevant route family has a scope requirement row
   - every writable governance/support/hub/cross-org route requires one current acting scope tuple
   - no mixed tuple assembly is possible across visibility coverage, minimum-necessary contract, runtime binding, route intent, and governing object version
   - any scope drift would freeze mutation in place
   - multi-tenant and platform-scoped work surfaces explicit blast radius

Mandatory gap closures you must perform:
- Close the “governance scope token and hub/support acting context are separate local concepts” gap from forensic finding `114`.
- Close the “organisation switching or purpose-of-use drift can leave live work writable” gap.
- Close the “runtime tenant isolation and browser surface authority are separate stories” gap by binding them explicitly.
- Close the “blast radius is implied by route names or cohort labels” gap by making affected tenant/organisation counts first-class.
- Close the “ambient session role or browser-local selector can substitute for acting scope” gap.
- Close the “support, hub, governance, and servicing-site routes can widen visibility by convenience” gap through the route-to-scope matrix and visibility-coverage linkage.

Industry-best-practice requirements:
- least privilege and minimum necessary access
- explicit purpose-of-use
- explicit break-glass/elevation handling
- fail-closed drift detection
- machine-checkable scope tuples
- explicit blast radius for broad-scope actions
- no implicit org or tenant switching

Non-negotiable rules:
- Governance, support, hub, servicing-site, and cross-organisation staff work must bind one current `ActingScopeTuple` and, where applicable, one current governance scope token.
- Ambient session role, raw tenant selector, or remembered organisation state is not sufficient authority.
- Organisation switch, tenant-scope change, purpose-of-use change, elevation expiry, break-glass revocation, or visibility/runtime drift must supersede the tuple and freeze mutation in place until revalidated.
- Any writable governance, support, hub, servicing-site, or cross-organisation route must bind the same current acting scope tuple across visibility coverage, minimum-necessary contract, runtime binding, route intent, and governing object version.
- Multi-tenant or platform-scoped work must surface explicit affected-tenant and affected-organisation counts before approval or mutation may settle.

Mandatory UI and interaction specification for `54_scope_and_isolation_atlas.html`:
Name the surface mode `Scope_Atlas`.

Tone:
- minimalist premium governance-and-access surface
- crisp, calm, exact, visibly safer than a generic admin permissions page
- should feel like an authority inspector for live scope, not an IAM checkbox screen

Branding:
- restrained Vecells wordmark plus an inline SVG `SCOPE` monogram
- include one elegant tuple braid showing identity -> acting context -> scope tuple -> route authority

Palette:
- canvas `#F7F8FC`
- rail `#EEF2F8`
- panel `#FFFFFF`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- scope accent `#0EA5A4`
- governance accent `#6E59D9`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for tuple hashes, scope modes, policy-plane codes, and count fields

Layout:
- sticky masthead with:
  - scope tuple count
  - drift-trigger count
  - routes with explicit scope coverage
  - broad-scope blast-radius total
- left rail (`296px`):
  - audience filters
  - scope-mode filters
  - route groups
- center canvas:
  - tuple braid diagram
  - route-to-scope matrix card group
- right inspector (`392px`):
  - selected route or tuple details
  - visibility coverage
  - runtime binding
  - blast radius
  - drift triggers
- lower region:
  - drift-trigger table
  - surface-to-blast-radius matrix
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `580px`
- primary controls `44px`
- matrix cards `min-height 148px`
- any diagram must have adjacent table parity

Motion:
- `120ms` hover/focus
- `180ms` route or tuple selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by audience, shell type, scope mode, drift state, and blast-radius class
- keep route matrix, tuple braid, and inspector synchronized
- keyboard navigation across filters, route cards, and lower tables
- deterministic `data-testid` markers for route rail, tuple braid, inspector, drift table, and defect strip
- the surface must prioritize clarity over density; it should feel curated, not overloaded

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- scope filtering
- route selection
- matrix/inspector parity
- drift-trigger visibility
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks

Definition of done:
- Vecells has one explicit tenant-isolation and acting-scope tuple model.
- Staff, support, governance, hub, and cross-organisation routes now have authoritative scope rules.
- Later governance, watch, and cross-organisation workflow tasks can freeze or revalidate mutation using one tuple rather than local role logic.
```