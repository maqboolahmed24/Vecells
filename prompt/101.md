# Prompt 101 For 101_par_phase0_track_runtime_implement_backup_restore_jobs_and_operational_readiness_snapshot_baseline

Use the shared operating contract first:
[shared_operating_contract_096_to_105.md](/Users/test/Code/V/prompt/shared_operating_contract_096_to_105.md)

```text
Execute roadmap task 101_par_phase0_track_runtime_implement_backup_restore_jobs_and_operational_readiness_snapshot_baseline.md.

Inherit the Shared_operating_contract_for_prompts_096_to_105 and consume the validated outputs from tasks `001-100`.

Mission:
Implement the Phase 0 resilience baseline for Vecells by creating real backup / restore jobs, backup manifests, restore rehearsal evidence, runbook bindings, and `OperationalReadinessSnapshot` generation.

This task must build the runtime-control foundation that later resilience and assurance work can extend, without waiting for Phase 9 to invent the baseline from scratch.

This is real backend, runtime, and operational-readiness work. Produce jobs, schemas, manifests, rehearsal scripts, readiness compilers, inspection artifacts, and tests. Do not leave backup scope, restore freshness, runbook binding, or readiness state in wiki pages or manual checklists.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `OperationalReadinessSnapshot`
  - pipeline handoff objects that bind readiness to runtime publication and release watch tuples
  - the `## Operational readiness contract`
  - recovery-disposition and readiness freshness rules
- `phase-9-the-assurance-ledger.md`, especially `## 9F. Resilience architecture, restore orchestration, and chaos programme`, including:
  - `EssentialFunctionMap`
  - `RecoveryTier`
  - `BackupSetManifest`
  - `RestoreRun`
  - `RecoveryEvidencePack`
  - `ResilienceSurfaceRuntimeBinding`
  - `RecoveryControlPosture`
- `phase-0-the-foundation-protocol.md` for trust, freeze, audit, acting-scope, and authoritative-settlement rules
- outputs from tasks `060`, `085`, `086`, `091`, `094`, `095`, `097`, `098`, and `100`
- `forensic-audit-findings.md`, especially Findings `95`, `96`, `103`, `112`, `113`, and `119`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must publish the resilience baseline now so later Phase 9 assurance work can consume current manifests and evidence instead of retrofitting them. If a later task owns richer resilience surfaces or evidence graphs, create the baseline seam here and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build the full baseline now in local and non-production environments.
- create realistic backup jobs or snapshot workflows for transactional stores, FHIR stores, object storage manifests, and queue or event persistence where applicable
- create restore rehearsal flows against preview or non-production targets
- compile `OperationalReadinessSnapshot` from current artifacts, dashboards, runbooks, and rehearsal evidence
- support simulator-backed essential functions and synthetic journey proof where live providers are not yet present
The mock-now path must preserve the same object schemas and readiness semantics later used in production.

Actual_production_strategy_later:
When production readiness is activated, preserve the same `BackupSetManifest`, `RestoreRun`, `RecoveryEvidencePack`, and `OperationalReadinessSnapshot` semantics. Production hardening may add stronger storage tiers, immutable archival controls, separate disaster-recovery targets, failover orchestration, or audited operator approvals, but it must not replace machine-readable readiness proof with dashboard links alone.

Primary outcome:
The repository gains a real resilience-control baseline that can prove what is backed up, whether it has been restored recently enough, which runbooks and essential functions are in scope, and whether the current runtime tuple is ready for guarded promotion.

Implementation deliverables to create:
1. `docs/architecture/101_backup_restore_and_operational_readiness_baseline.md`
2. `docs/architecture/101_essential_function_map_and_recovery_tiers.md`
3. `docs/architecture/101_resilience_baseline_cockpit.html`
4. `data/analysis/backup_set_manifest_schema.json`
5. `data/analysis/operational_readiness_snapshot_schema.json`
6. `data/analysis/essential_function_map.json`
7. `data/analysis/recovery_tier_catalog.json`
8. `data/analysis/readiness_coverage_matrix.csv`
9. runtime jobs or scripts for backup manifest generation, restore rehearsal initiation, and readiness snapshot compilation
10. `tools/analysis/validate_resilience_baseline.(py|ts)`
11. tests for backup-manifest generation, restore rehearsal flow, readiness freshness, blocked readiness, and tuple drift handling
12. `tests/playwright/resilience-baseline-cockpit.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- schema-validated `BackupSetManifest` generation bound to the current runtime publication tuple
- explicit mapping from essential functions to supporting systems, supporting data, degraded modes, runbooks, and recovery tiers
- restore rehearsal jobs that can prove at least data-restored and journey-validation-pending stages, not just snapshot existence
- `OperationalReadinessSnapshot` compilation that binds runtime publication, parity, watch tuple, required assurance slices, runbooks, backup manifests, synthetic coverage, latest restore evidence, and current recovery posture into one machine-readable record
- readiness freshness and verdict rules that fail closed when required evidence is stale, blocked, or missing
- browser-viewable resilience cockpit that renders readiness state from the same machine-readable records without becoming the source of truth

Implementation instructions:
1. Start by mapping essential functions, not infrastructure components. Use the blueprint’s likely essential functions as the seed set and extend only where required by the current runtime tuple.
2. For each essential function, declare recovery tier, required backup scopes, required journey proof, and runbook bindings.
3. Generate backup manifests for each dataset scope under current policy. Make backup scope, immutability state, restore freshness, and tuple binding explicit.
4. Implement restore rehearsal on non-production targets with deterministic evidence capture. A “backup exists” check is insufficient.
5. Compile `OperationalReadinessSnapshot` only from current, machine-readable inputs. Stale dashboard links or old runbook pages must not keep readiness green.
6. Distinguish at least these readiness outcomes:
   - exact and ready
   - stale rehearsal evidence
   - missing backup manifest
   - blocked restore proof
   - tuple drift between readiness inputs and current runtime publication
   - required assurance or freeze posture preventing readiness
7. Never allow these anti-patterns:
   - declaring readiness because dashboards exist without fresh restore proof
   - using one generic restore run as proof for all essential functions
   - treating runbooks as current if their binding or rehearsal status is stale
   - leaving recovery disposition detached from the readiness tuple
8. Publish one resilience baseline cockpit with sections for essential functions, backup manifests, latest restore evidence, readiness tuple health, and blocker explanations.

Gap handling you must perform during this task:
- If exact production backup infrastructure is not yet available, use a faithful non-production or emulated baseline and record `GAP_RESOLUTION_BACKUP_RUNTIME_*` while preserving the canonical schemas.
- If some essential functions depend on later-phase surfaces that are not yet implemented, create bounded placeholder refs and record `FOLLOW_ON_DEPENDENCY_ESSENTIAL_FUNCTION_*` instead of omitting them.
- If current runbook inventory is incomplete, publish machine-readable placeholder bindings with freshness state `rehearsal_due` and record `GAP_RUNBOOK_BINDING_*`.

Backend and resilience engineering standards:
- immutable or tamper-evident manifest history where feasible
- deterministic tuple hashing and binding across readiness records
- audit all restore and readiness actions
- rehearse recovery repeatedly in CI or non-production, not once-off
- export metrics for readiness freshness, restore success rate, restore lag, manifest staleness, and blocker category frequency

Verification requirements:
- manifest schema and generator tests
- restore rehearsal tests
- readiness compilation tests
- tuple-drift and blocked-readiness tests
- essential-function coverage tests
- `Playwright_or_other_appropriate_tooling` tests for the cockpit surface, including landmark structure, table fallback, stable selectors, and readable blocker presentation

Definition of done:
- backup scope and restore freshness are machine-readable and current
- readiness state is compiled from exact tuple-bound evidence
- essential functions and recovery tiers are explicit
- promotion or canary work can consume real readiness proof instead of informal operations memory
```
