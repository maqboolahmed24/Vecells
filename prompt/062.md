# Prompt 062 For 062_par_phase0_track_backend_implement_submission_envelope_request_and_request_lineage_aggregates

Use the shared operating contract first:
[shared_operating_contract_056_to_065.md](/Users/test/Code/V/prompt/shared_operating_contract_056_to_065.md)

```text
Execute roadmap task 062_par_phase0_track_backend_implement_submission_envelope_request_and_request_lineage_aggregates.md.

Inherit the Shared_operating_contract_for_prompts_056_to_065 and consume the validated outputs from tasks `001-061`.

Mission:
Implement the canonical backend aggregates and persistence model for `SubmissionEnvelope`, `SubmissionPromotionRecord`, `RequestLineage`, `LineageCaseLink`, `Episode`, and `Request`.

This is real implementation work. Produce code, schemas, migrations, tests, and minimal application seams that make these aggregates authoritative and replay-safe. Do not defer core invariants into TODO comments.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.0A IntakeConvergenceContract`
  - `#### 1.1 SubmissionEnvelope`
  - `#### 1.1A SubmissionPromotionRecord`
  - `#### 1.1B RequestLineage`
  - `#### 1.1C LineageCaseLink`
  - `#### 1.2 Episode`
  - `#### 1.3 Request`
  - the non-negotiable invariants around drafts staying in `SubmissionEnvelope`, `patientRef` derivation, lineage continuity, and child-case ownership
- `vecells-complete-end-to-end-flow.md`, especially the entry-to-promotion-to-lineage shape
- `forensic-audit-findings.md`, especially:
  - Finding `01` missing durable lineage shell
  - Finding `02` missing submitted barrier
  - any finding where lineages, child links, or patient refs were under-specified
- outputs from tasks `001-007`, `019`, `041-045`, `056`, and `061`

Parallel-execution note:
This task runs in the Phase 0 parallel block. Do not depend on sibling implementation tasks `063-065` existing yet. If you need shared schemas or interfaces from those tracks, publish the smallest bounded stub in the correct shared package, mark `PARALLEL_INTERFACE_GAP_*`, and continue.

Primary outcome:
Working domain and persistence code for the request-intake backbone so later services, evidence handling, identity, triage, booking, and portal work can build on one canonical aggregate set.

Implementation deliverables to create:
1. domain code for `SubmissionEnvelope`, `SubmissionPromotionRecord`, `RequestLineage`, `LineageCaseLink`, `Episode`, and `Request` in their correct package homes
2. repository interfaces and persistence adapters for those aggregates
3. database migrations or persistence-model artifacts for those aggregates
4. domain events / event-contract entries for create, append, promote, branch, link, and state-change operations in the shared event-contract package
5. typed factory / command-service seams for creation, append, promotion, branch, and child-link operations
6. `docs/architecture/62_submission_and_lineage_aggregate_design.md`
7. `docs/architecture/62_submission_and_lineage_state_rules.md`
8. `data/analysis/submission_and_lineage_aggregate_manifest.json`
9. `data/analysis/submission_and_lineage_invariant_matrix.csv`
10. unit tests, repository tests, and integration tests covering the required invariants
11. validator or smoke scripts proving aggregate completeness and invariant coverage

At minimum, implement these aggregate capabilities:
- create `SubmissionEnvelope` from canonical intake convergence rules
- append immutable ingress / normalized / evidence refs to the envelope without turning it into a request prematurely
- governed promotion from one envelope into exactly one `Request` plus one `SubmissionPromotionRecord`
- exact replay of prior promotion returning the same promotion record and request
- create and update `RequestLineage` without letting it own workflow truth
- create and update `LineageCaseLink` without allowing child domains to write canonical request workflow directly
- allow `Request.patientRef` and `Episode.patientRef` to remain null until verified identity binding exists
- preserve orthogonal workflow / safety / identity axes on `Request`

Execution steps:
1. Re-read the canonical object definitions and convert each field set into explicit code-level aggregate and persistence models.
2. Implement aggregate invariants first. At minimum encode:
   - drafts and partial capture remain in `SubmissionEnvelope`
   - `Request` is not a draft store
   - one governed promotion per envelope lineage
   - `RequestLineage` owns continuity and joins, not lifecycle milestones
   - `LineageCaseLink` may carry blocker and confirmation refs but may not mutate request workflow directly
   - `patientRef` is nullable and derived only from identity binding
3. Implement constructor / state-transition methods for:
   - envelope creation
   - envelope append / supersession
   - governed promotion
   - lineage branch / reuse decisions
   - case-link acknowledgement / activation / return / supersession / compensation
4. Implement optimistic concurrency or compare-and-set support on aggregate versions where required by the chosen backend baseline.
5. Implement repository interfaces and concrete persistence adapters following the runtime and storage choices frozen earlier.
6. Emit domain events or canonical event stubs for the aggregate transitions above.
7. Write tests covering:
   - promotion exactly-once behavior
   - `Request` never appearing in draft state
   - same-request continuation reusing the same lineage
   - same-episode or related-episode branching producing a new lineage with explicit branch decision
   - patient refs remaining null until binding exists
   - child-case links not rewriting request workflow directly
8. Add any shared stub interfaces needed by sibling tracks only in shared packages and record `PARALLEL_INTERFACE_GAP_*` where appropriate.

Mandatory gap closures you must perform:
- Close the “no durable pre-submit shell exists” gap by implementing `SubmissionEnvelope` as a real aggregate, not a transient DTO.
- Close the “submitted barrier is implied but not explicit” gap by implementing `SubmissionPromotionRecord` and promotion exactly-once behavior.
- Close the “request lineage is only narrative” gap by implementing `RequestLineage` and `LineageCaseLink` as explicit aggregates or sub-aggregates with persistence.
- Close the “patientRef is assumed too early” gap by allowing null patient refs and binding-derived identity only.
- Close the “child domains can quietly mutate request truth” gap by enforcing link-only ownership boundaries.

Industry-best-practice requirements:
- aggregate-root invariants enforced in code
- immutable event / transition records
- optimistic concurrency or CAS semantics where required
- repository interfaces separate from storage adapters
- deterministic test fixtures
- no hidden writes from projections or controllers
- secure handling of tenant scope and audit correlations

Non-negotiable rules:
- `SubmissionEnvelope` and `Request` are distinct aggregates with a real promotion boundary.
- `RequestLineage` is the continuity anchor; it is not optional join prose.
- `Request.workflowState`, `Request.safetyState`, and `Request.identityState` remain orthogonal.
- `patientRef` may be null until governed identity binding settles.
- Child domains may contribute links, blockers, or milestones through `LineageCaseLink`; they may not directly write canonical request workflow or closure.

Definition of done:
- The canonical request-intake backbone exists as real backend code with persistence and tests.
- Promotion, lineage, and child-link behavior are explicit and replay-safe.
- Later evidence, identity, triage, booking, and frontend tracks can build on stable aggregate truth.
```
