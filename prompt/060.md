# Prompt 060 For 060_seq_phase0_define_backup_restore_and_recovery_tuple_baseline

Use the shared operating contract first:
[shared_operating_contract_056_to_065.md](/Users/test/Code/V/prompt/shared_operating_contract_056_to_065.md)

```text
Execute roadmap task 060_seq_phase0_define_backup_restore_and_recovery_tuple_baseline.md.

Inherit the Shared_operating_contract_for_prompts_056_to_065 and consume the validated outputs from tasks `001-059`.

Mission:
Define the Phase 0 resilience baseline for backup, restore, operational readiness, and governed recovery control. This task must publish one tuple-bound recovery authority model so restore, failover, chaos, and recovery-pack actions cannot be inferred from dashboards, old runbooks, or backup existence alone.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.40A EssentialFunctionMap`
  - `#### 1.40B RecoveryTier`
  - `#### 1.40C BackupSetManifest`
  - `#### 1.40D RestoreRun`
  - `#### 1.40J RecoveryControlPosture`
  - `#### 1.40K ResilienceActionRecord`
  - `#### 1.40L ResilienceActionSettlement`
  - `#### 1.40M RecoveryEvidenceArtifact`
  - `#### 2.10A ResilienceOrchestrator`
  - the invariants around `62A-62G`
- `platform-runtime-and-release-blueprint.md`, especially:
  - `OperationalReadinessSnapshot`
  - `RunbookBindingRecord`
  - `SyntheticRecoveryCoverageRecord`
  - the pipeline order requiring restore proof before canary
- `phase-cards.md`, especially the requirements for backup / restore automation, authoritative settlement for restore/failover/chaos controls, and operational readiness snapshots
- outputs from tasks `046-059`
- `forensic-audit-findings.md`, especially any findings that hardened resilience posture, continuity evidence, and recovery tuple completeness

Primary outcome:
One machine-readable and human-readable resilience pack proving:
- what essential functions exist
- what recovery tiers apply
- what backup scope and compatibility are required
- how restore runs and recovery evidence are recorded
- when recovery control is live, diagnostic-only, governed-recovery, or blocked

Deliverables to create:
1. `docs/architecture/60_backup_restore_and_recovery_tuple_baseline.md`
2. `docs/architecture/60_recovery_tier_and_essential_function_policy.md`
3. `docs/architecture/60_restore_and_recovery_control_matrix.md`
4. `docs/architecture/60_resilience_control_lab.html`
5. `data/analysis/essential_function_map.json`
6. `data/analysis/recovery_tiers.json`
7. `data/analysis/backup_scope_matrix.csv`
8. `data/analysis/restore_run_schema.json`
9. `data/analysis/recovery_control_posture_rules.json`
10. `data/analysis/recovery_evidence_artifact_catalog.csv`
11. `packages/api-contracts/schemas/recovery-control-posture.schema.json`
12. `tools/analysis/validate_recovery_tuple_baseline.(py|ts)`
13. `tests/playwright/resilience-control-lab.spec.(ts|js)`

Each `RecoveryTier` row must capture at minimum:
- `recoveryTierId`
- `functionCode`
- `rto`
- `rpo`
- `maxDiagnosticOnlyWindow`
- `degradedModeDefinitionRef`
- `restorePriority`
- `requiredJourneyProofRefs[]`
- `requiredBackupScopeRefs[]`
- `tierState`

Each `BackupSetManifest` row must capture at minimum:
- `backupSetManifestId`
- `datasetScopeRef`
- `essentialFunctionRefs[]`
- `snapshotTime`
- `immutabilityState`
- `checksumBundleRef`
- `restoreCompatibilityDigestRef`
- `runtimePublicationBundleRef`
- `manifestTupleHash`
- `manifestState`
- `verifiedAt`

Each `RecoveryControlPosture` rule row must capture at minimum:
- `scopeRef`
- `operationalReadinessSnapshotRef`
- `releaseTrustFreezeVerdictRef`
- `requiredRunbookBindingRefs[]`
- `restoreValidationFreshnessState`
- `dependencyCoverageState`
- `journeyRecoveryCoverageState`
- `backupManifestState`
- `postureState`
- `allowedActionRefs[]`
- `blockerRefs[]`
- `releaseRecoveryDispositionRef`

Execution steps:
1. Start from task `047`’s trust zones, task `051`’s release freeze / parity law, task `058`’s verification ladder, and task `059`’s reference-case / simulator corpus.
2. Publish the essential-function map. It must express recovery units in business and shell terms, not just infrastructure terms.
3. Publish the `RecoveryTier` policy and map every essential function to RTO/RPO, degraded-mode ceiling, required backup scope, and required journey proof.
4. Publish the backup scope matrix and `BackupSetManifest` rules so immutability, checksum coverage, and restore compatibility are mandatory.
5. Publish the `RestoreRun` schema and the authoritative recovery-action chain:
   - `ResilienceActionRecord`
   - `ResilienceActionSettlement`
   - `RecoveryEvidenceArtifact`
6. Publish the `RecoveryControlPosture` rule set and make explicit when posture may be:
   - `live_control`
   - `diagnostic_only`
   - `governed_recovery`
   - `blocked`
7. Publish how `OperationalReadinessSnapshot`, runbook bindings, synthetic recovery coverage, backup manifests, and recovery evidence all join the same resilience tuple.
8. Build the lab and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
9. Validate that:
   - backup existence alone never counts as live recovery authority
   - stale or tuple-mismatched runbooks, manifests, or recovery evidence cannot authorize recovery actions
   - essential functions are the governing recovery units for boards and control surfaces
   - recovery actions always settle through the same governed mutation and evidence chain
   - read-only or recovery-only surfaces remain truthful about degraded or blocked posture

Mandatory gap closures you must perform:
- Close the “backup exists therefore recovery is ready” gap by requiring manifest immutability, compatibility, and tuple alignment.
- Close the “restore authority can be inferred from dashboards and runbooks” gap by making `RecoveryControlPosture` the single runtime verdict.
- Close the “recovery evidence is detached operational folklore” gap by requiring `RecoveryEvidenceArtifact` and tuple binding.
- Close the “essential functions are infra assets rather than recovery units” gap by publishing the essential-function map.
- Close the “restore/failover/chaos actions are shell buttons or scripts” gap by requiring authoritative resilience action records and settlements.
- Close the “continuity and recovery proof drift apart” gap by binding route/journey recovery proof to the same readiness tuple.

Industry-best-practice requirements:
- immutable backup manifests and checksums
- recovery tiers tied to business functions
- explicit restore validation evidence
- action settlement for all recovery controls
- least-privilege artifact presentation and export posture
- deterministic recovery posture computation

Non-negotiable rules:
- `RecoveryControlPosture` is the single runtime verdict for restore, failover, and chaos authority.
- `OperationalReadinessSnapshot`, `RunbookBindingRecord`, `BackupSetManifest`, and `RecoveryEvidenceArtifact` must agree on the same current resilience tuple before live recovery control is legal.
- Stale, superseded, or tuple-drifted restore runs remain diagnostic history only.
- PHI-bearing recovery artifacts must still resolve through governed presentation and outbound-navigation law.
- No shell or operator workflow may imply successful recovery without authoritative settlement and recorded evidence.

Mandatory UI and interaction specification for `60_resilience_control_lab.html`:
Name the surface mode `Resilience_Control_Lab`.

Tone:
- minimalist premium resilience control surface
- should feel like a composed operational instrument, not a generic status dashboard
- exact, serious, and low-noise

Branding:
- restrained Vecells wordmark plus an inline SVG `RC` monogram
- include one elegant essential-function map and one recovery-posture panel
- include a compact restore-tuple inspector and recovery-evidence rail

Palette:
- canvas `#F7F8FC`
- rail `#EEF2F8`
- panel `#FFFFFF`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- resilience accent `#0EA5A4`
- recovery accent `#0F9D58`
- diagnostic accent `#7C3AED`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for manifest IDs, tier codes, hashes, and run refs

Layout:
- sticky masthead with:
  - essential-function count
  - current manifest count
  - live-control scope count
  - blocked scope count
- left rail (`300px`):
  - function filters
  - posture filters
  - run / evidence filters
- center canvas:
  - essential-function map
  - recovery-posture panel
  - curated posture cards
- right inspector (`396px`):
  - selected function / tier
  - backup manifest state
  - readiness tuple details
  - latest recovery evidence
- lower region:
  - backup scope matrix
  - recovery posture rule table
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `620px`
- primary controls `44px`
- posture cards `min-height 170px`
- every diagram requires adjacent table/list parity

Motion:
- `120ms` hover/focus
- `180ms` filter / card selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by function, recovery tier, posture state, and evidence freshness
- selecting a posture card must synchronize map, matrices, and inspector
- keyboard navigation across filters, cards, and table rows
- deterministic `data-testid` markers for function map, posture panel, inspector, evidence rail, and defect strip
- postures must remain understandable without color alone

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- function filtering
- posture filtering
- card selection
- map / matrix / inspector synchronization
- keyboard navigation
- responsive layout
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has one explicit backup / restore / recovery tuple baseline.
- Recovery authority is now governed by machine-readable posture rather than dashboards or assumptions.
- Later resilience, assurance, and ops tasks can build on a stable tuple model.
```
