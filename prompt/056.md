# Prompt 056 For 056_seq_phase0_define_scoped_mutation_gate_and_route_intent_binding_rules

Use the shared operating contract first:
[shared_operating_contract_056_to_065.md](/Users/test/Code/V/prompt/shared_operating_contract_056_to_065.md)

```text
Execute roadmap task 056_seq_phase0_define_scoped_mutation_gate_and_route_intent_binding_rules.md.

Inherit the Shared_operating_contract_for_prompts_056_to_065 and consume the validated outputs from tasks `001-055`.

Mission:
Define the canonical post-submit mutation law for Vecells. This task must publish one precise control model showing how writable routes are bound, how action scope is resolved, how stale or ambiguous state fails closed, and how same-shell recovery remains governed by authoritative settlement rather than controller success or UI optimism.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.21 RouteIntentBinding`
  - `#### 1.22 CommandActionRecord`
  - `#### 1.23 CommandSettlementRecord`
  - `#### 2.8 ScopedMutationGate`
  - invariants around exact target tuples, safety epochs, reachability dependencies, acting scope, and stale recovery in the non-negotiable invariant set and mutation algorithms around lines `5493-5563`
- `phase-cards.md`, especially the Phase 0 notes that elevate `RouteIntentBinding`, `CommandActionRecord`, `CommandSettlementRecord`, and `ScopedMutationGate` into shared foundation contracts
- `phase-2-identity-and-echoes.md`, especially the rule that capability or login success alone is not writable authority and that claim or continuation surfaces must resolve one live `RouteIntentBinding`
- `platform-runtime-and-release-blueprint.md`, especially the rule that mutable gateway surfaces require current `AudienceSurfaceRuntimeBinding`, current `ReleaseApprovalFreeze`, current channel posture, current parity, and current recovery disposition
- `platform-frontend-blueprint.md`, especially the route-family ownership rules, selected-anchor preservation rules, and the prohibition on reconstructing mutation authority from local route params, detached projections, or browser cache
- `patient-account-and-communications-blueprint.md`, `callback-and-clinician-messaging-loop.md`, and `staff-operations-and-support-blueprint.md` where same-shell command settlement, recovery, and reply/callback actionability depend on the bound route-intent tuple
- `forensic-audit-findings.md`, especially the patch response that hardened `RouteIntentBinding`, `CommandActionRecord`, `CommandSettlementRecord`, `ReleaseApprovalFreeze`, `ChannelReleaseFreezeRecord`, and `AssuranceSliceTrustRecord` as shared baseline controls
- outputs from tasks `004`, `005`, `007`, `014`, `046-055`

Primary outcome:
One machine-readable and human-readable mutation-control pack proving that every post-submit patient, staff, support, hub, pharmacy, or operations mutation:
- resolves exactly one governing object and target tuple
- proves current subject / acting scope / release / trust posture
- fences against stale or superseded state
- emits authoritative command settlement
- degrades into same-shell recovery instead of generic controller failure

Deliverables to create:
1. `docs/architecture/56_scoped_mutation_gate_strategy.md`
2. `docs/architecture/56_route_intent_binding_contract.md`
3. `docs/architecture/56_command_settlement_and_same_shell_recovery_rules.md`
4. `docs/architecture/56_action_scope_and_governing_object_matrix.md`
5. `docs/architecture/56_route_authority_and_runtime_tuple_matrix.md`
6. `docs/architecture/56_scoped_mutation_gate_lab.html`
7. `data/analysis/route_intent_binding_schema.json`
8. `data/analysis/scoped_mutation_gate_decision_table.csv`
9. `data/analysis/action_scope_to_governing_object_matrix.csv`
10. `data/analysis/command_settlement_result_matrix.csv`
11. `data/analysis/mutation_recovery_and_freeze_matrix.csv`
12. `packages/api-contracts/schemas/route-intent-binding.schema.json`
13. `packages/api-contracts/schemas/command-settlement-record.schema.json`
14. `tools/analysis/validate_scoped_mutation_gate.(py|ts)`
15. `tests/playwright/scoped-mutation-gate-lab.spec.(ts|js)`

Each route-intent row must capture at minimum:
- `routeIntentId`
- `audienceSurface`
- `shellType`
- `routeFamily`
- `actionScope`
- `governingObjectType`
- `governingObjectRef`
- `canonicalObjectDescriptorRef`
- `governingBoundedContextRef`
- `governingObjectVersionRef`
- `lineageScope`
- `requiredContextBoundaryRefs[]`
- `parentAnchorRef`
- `subjectRef`
- `grantFamily`
- `sessionEpochRef`
- `subjectBindingVersionRef`
- `actingScopeTupleRequirementRef`
- `audienceSurfaceRuntimeBindingRef`
- `releaseApprovalFreezeRef`
- `channelReleaseFreezeState`
- `requiredAssuranceSliceTrustRefs[]`
- `routeIntentTupleHash`
- `bindingState`
- `staleDisposition`
- `recoveryEnvelopeFamily`
- source refs and rationale

Each decision-table row must capture at minimum:
- `audienceSurface`
- `routeFamily`
- `actionScope`
- `governingObjectType`
- `requiredGrantFamily`
- `requiredActingContext`
- `requiredActingScopeTuple`
- `requiredLineageFenceEpoch`
- `requiredSafetyEpochValidation`
- `requiredReachabilityValidation`
- `requiredRuntimePublicationBinding`
- `requiredReleaseFreezeValidation`
- `requiredChannelFreezeValidation`
- `requiredParityValidation`
- `allowedCommandSettlementResults[]`
- `declinedScopeDisposition`
- `staleRecoverableDisposition`
- `sameShellRecoveryEnvelopeRef`
- `mustWriteCommandActionRecord`
- `mustWriteCommandSettlementRecord`

Execution steps:
1. Start from task `005`’s patient-action matrix, task `004`’s route-family / shell inventory, task `047`’s gateway surface map, task `050`’s frontend manifests, task `051`’s release / parity law, task `054`’s acting-scope tuple model, and task `055`’s coordinator / closure law.
2. Publish the canonical `RouteIntentBinding` tuple law:
   - why each field exists
   - what must be included in the tuple hash
   - what makes a binding `live`, `stale`, `superseded`, or `recovery_only`
   - which drift classes require immediate downgrade
3. Publish the `ScopedMutationGate` evaluation algorithm in exact order:
   - resolve audience surface and route family
   - resolve current acting context and `ActingScopeTuple`
   - resolve current `RouteIntentBinding`
   - resolve current `AudienceSurfaceRuntimeBinding`
   - validate release freeze, channel freeze, trust, parity, provenance, and recovery posture
   - validate session epoch, subject-binding version, grant family, fence epoch, and expiry
   - resolve exactly one governing object and version
   - validate safety epoch and any active preemption / urgent issuance state
   - validate any active reachability dependency and current reachability authority
   - create `CommandActionRecord`
   - return or advance one authoritative `CommandSettlementRecord`
4. Publish the action-scope matrix for at minimum:
   - `claim`
   - `respond_more_info`
   - `reply_message`
   - `respond_callback`
   - `manage_booking`
   - `accept_waitlist_offer`
   - `accept_network_alternative`
   - `pharmacy_choice`
   - `pharmacy_consent`
   - `support_repair_action`
   - `ops_resilience_action` where route law already requires the same governed mutation chain
5. Publish the settlement-result matrix and make the distinction explicit between:
   - processing acceptance
   - external observation
   - authoritative outcome
   - coarse result class
   - same-shell calm return eligibility
6. Publish the recovery matrix showing how `stale_recoverable`, `blocked_policy`, `denied_scope`, `expired`, `review_required`, and `reconciliation_required` each map to one governed same-shell recovery path.
7. Build the lab and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that:
   - no writable action can be reconstructed from URL params, cached projection fragments, or detached CTA state
   - ambiguous governing-object selection always fails closed into reissue / disambiguation
   - legacy or partial route intents automatically degrade to `recovery_only`
   - no controller or gateway can return calm success without `authoritativeOutcomeState = settled`
   - every post-submit mutation path writes both `CommandActionRecord` and `CommandSettlementRecord`

Mandatory gap closures you must perform:
- Close the “route authority is a local UI concern” gap by making the tuple machine-readable and validator-backed.
- Close the “capability or login success implies writability” gap by requiring route-intent, runtime-binding, and settlement law together.
- Close the “transport or HTTP success can stand in for business settlement” gap by separating processing, observation, and authoritative outcome.
- Close the “parent anchor and governing object version are optional” gap by making them mandatory tuple members.
- Close the “cross-context action can infer authority from launch source” gap by requiring explicit `requiredContextBoundaryRefs[]`.
- Close the “safety, reachability, or identity drift can leave stale CTAs live” gap by pushing those checks into the gate before dispatch.
- Close the “recovery is a detached error page” gap by binding every recoverable denial or staleness result to one same-shell recovery envelope.

Industry-best-practice requirements:
- fail-closed authorization and freshness validation
- exact-target resolution with no heuristic mutation fallback
- immutable action and settlement ledgers
- optimistic concurrency / fence validation on governing objects
- deterministic decision-table generation
- zero-trust browser posture: cache and URL never define write authority
- structured audit correlation from route intent through settlement

Non-negotiable rules:
- `ScopedMutationGate` is the sole gateway for post-submit mutation.
- Every writable route must bind exactly one current `RouteIntentBinding`.
- Every post-submit mutation must write one immutable `CommandActionRecord` and one authoritative `CommandSettlementRecord`.
- `CommandSettlementRecord.authoritativeOutcomeState` is the only settlement dimension that may drive calm success or terminal reassurance.
- If acting scope, grant scope, session epoch, subject binding, fence epoch, safety epoch, reachability authority, runtime binding, release posture, parity, or trust drifts, the route must downgrade to recovery or read-only posture in the same shell.
- Any route lacking `canonicalObjectDescriptorRef`, `governingObjectVersionRef`, `routeContractDigestRef`, `parentAnchorRef`, or `routeIntentTupleHash` is `recovery_only`.

Mandatory UI and interaction specification for `56_scoped_mutation_gate_lab.html`:
Name the surface mode `Mutation_Intent_Lab`.

Tone:
- minimalist premium control-plane instrument
- visually precise, calm, and consequence-aware
- should feel like a governed mutation observatory, not a generic docs page

Branding:
- restrained Vecells wordmark plus an inline SVG `MI` monogram
- include one elegant “authority braid” diagram showing route intent + acting scope + runtime binding converging into settlement
- include one compact recovery envelope rail and one settlement ladder panel

Palette:
- canvas `#F7F8FC`
- rail `#EEF2F8`
- panel `#FFFFFF`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- route accent `#0EA5A4`
- settlement accent `#6E59D9`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono only for tuple hashes, IDs, action scopes, and fence epochs

Layout:
- sticky masthead with:
  - live route-family count
  - action-scope count
  - recoverable-result count
  - blocked-result count
- left rail (`296px`):
  - audience / route-family / action-scope filters
  - binding-state filters
- center canvas:
  - authority braid diagram
  - settlement ladder panel
  - curated decision cards
- right inspector (`396px`):
  - selected route-intent tuple
  - governing-object details
  - acting-scope and runtime-binding requirements
  - resulting settlement and recovery envelope
- lower region:
  - action-scope matrix
  - settlement-result matrix
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `620px`
- primary controls `44px`
- matrix rows `40px` minimum
- every diagram requires adjacent table/list parity

Motion:
- `120ms` hover/focus
- `180ms` filter and selection changes
- `220ms` inspector and ladder transitions
- reduced-motion support mandatory

Interaction requirements:
- filter by audience, route family, action scope, binding state, and settlement class
- selecting a decision card must synchronize the braid diagram, ladder, matrices, and inspector
- keyboard navigation across filters, cards, and matrix rows
- deterministic `data-testid` markers for the braid diagram, settlement ladder, matrices, inspector, and defect strip
- all recoverable outcomes must remain understandable without relying on color alone

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- route-family filtering
- action-scope filtering
- decision-card selection
- braid/ladder/inspector synchronization
- keyboard navigation
- reduced motion
- responsive layout
- accessibility smoke checks
- matrix parity for the diagrams

Definition of done:
- Vecells has one explicit, validator-backed mutation gateway and route-intent law.
- Writable posture, stale drift, and same-shell recovery are now defined from authoritative tuples rather than inferred by route code.
- Later backend and frontend tracks can implement mutations without inventing local authority semantics.
```
