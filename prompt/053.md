# Prompt 053 For 053_seq_phase0_define_authoritative_audit_and_worm_ledger_strategy

Use the shared operating contract first:
[shared_operating_contract_046_to_055.md](/Users/test/Code/V/prompt/shared_operating_contract_046_to_055.md)

```text
Execute roadmap task 053_seq_phase0_define_authoritative_audit_and_worm_ledger_strategy.md.

Inherit the Shared_operating_contract_for_prompts_046_to_055 and consume the validated outputs from tasks `001-052`.

Mission:
Define the authoritative audit and WORM-ledger strategy for Vecells so immutable audit truth, causality, disclosure posture, replay admissibility, and retention protection are explicit from Phase 0 onward.

This task must establish one append-only audit spine that later assurance, investigation, replay, break-glass review, export, and retention work can build on without inventing alternate audit truth.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`
  - `AuditRecord`
  - the rule that `AuditRecord` is the immutable join row between ingress correlation, route intent, command settlement, visible shell decision, and disclosure posture
  - the rules that calm success and continuity-preserving surfaces require matching `UIProjectionVisibilityReceipt` plus `AuditRecord`
  - the linkage between `AuditRecord`, `AuditEvent`, and `Provenance`
  - the later invariants that `AssuranceEvidenceGraphSnapshot` and `AssuranceGraphCompletenessVerdict` are the authorities for pack, audit, replay, export, retention, and recovery completeness
- `phase-9-the-assurance-ledger.md`
  - the WORM audit-ledger posture
  - `AssuranceEvidenceGraphSnapshot`
  - `AssuranceGraphCompletenessVerdict`
  - audit/replay/export and retention rules
  - WORM / hash-chained / replay-critical retention exclusions
- `platform-runtime-and-release-blueprint.md`
  - immutable audit in the WORM ledger
  - release/publication actions that must append audit records
- `forensic-audit-findings.md`
  - findings `102-106`, `113`, `115`, and any item showing continuity proof, export admissibility, or audit evidence can drift without one authoritative graph
- outputs from tasks `009`, `010`, `015`, `048`, `049`, `051`, and `052`

Primary outcome:
One machine-readable audit/WORM strategy proving:
- what an authoritative audit record contains
- how the hash chain works
- which actions require WORM append
- how audit truth links to UI visibility, command settlement, route intent, disclosure posture, and FHIR companion artifacts
- how retention and export later consume the same evidence graph rather than local evidence lists

Deliverables to create:
1. `docs/architecture/53_audit_and_worm_strategy.md`
2. `docs/architecture/53_audit_chain_and_disclosure_model.md`
3. `docs/architecture/53_worm_storage_and_retention_boundary.md`
4. `docs/architecture/53_audit_ledger_explorer.html`
5. `data/analysis/audit_record_schema.json`
6. `data/analysis/audit_action_taxonomy.csv`
7. `data/analysis/worm_retention_classes.json`
8. `data/analysis/audit_to_fhir_companion_matrix.csv`
9. `data/analysis/audit_admissibility_dependencies.json`
10. `tools/analysis/validate_audit_and_worm_strategy.(py|ts)`
11. `tests/playwright/audit-ledger-explorer.spec.(ts|js)`

At minimum, the authoritative audit strategy must capture:
- append-only `AuditRecord` fields
- `previousHash` / `hash` chain semantics
- actor + acting-context binding
- action taxonomy
- target type + target id
- reason code
- `edgeCorrelationId`
- `routeIntentRef`
- `commandActionRef`
- `commandSettlementRef`
- `uiEventRef`
- `uiEventCausalityFrameRef`
- `uiTransitionSettlementRef`
- `projectionVisibilityRef`
- `selectedAnchorRef`
- `shellDecisionClass`
- `disclosureFenceRef`
- source IP / user agent / timestamp handling
- FHIR `AuditEvent` / `Provenance` companion policy
- retention / immutability class
- source refs and rationale

Execution steps:
1. Start from the Phase 0 `AuditRecord` definition and make the audit join explicit:
   - ingress correlation
   - route intent
   - command action
   - command settlement
   - UI event
   - UI visibility
   - shell decision
   - disclosure fence
2. Publish the append-only chain strategy:
   - how hashes are computed
   - how prior-hash linkage is maintained
   - how supersession is represented without mutation
   - how integrity verification works
3. Publish the action taxonomy and required audit cases. At minimum cover:
   - auth/session events
   - grant redemption and rotation
   - route-intent establishment
   - command ingestion and settlement
   - publication / release / governance actions
   - support replay / restore
   - break-glass use
   - export generation
   - recovery and rollback actions
4. Publish the WORM and retention-class strategy:
   - what is WORM
   - what is hash-chained
   - what is replay-critical
   - what may be archived only
   - what may never enter delete-ready posture
5. Define the relationship between internal audit truth and FHIR companions:
   - `AuditRecord` remains canonical
   - `AuditEvent` and `Provenance` are companion output only
6. Define the admissibility dependency model that later assurance/replay/export/retention work must consume:
   - which artifacts depend on the audit chain
   - which later graph objects will prove completeness
   - what happens when the graph is incomplete
7. Build the explorer and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that:
   - no authoritative action path lacks an audit record requirement
   - no audit record can be mutated in place
   - no WORM or hash-chained class may enter ordinary deletion posture
   - no export, replay, or investigation flow could claim authoritative completeness without the governing graph/admissibility dependencies
   - no FHIR companion artifact is treated as canonical audit truth

Mandatory gap closures you must perform:
- Close the “server acceptance or local acknowledgement can stand in for the audit join” gap.
- Close the “audit truth and replay/export truth can diverge” gap by defining admissibility dependencies explicitly.
- Close the “WORM posture is a storage detail rather than a contract” gap.
- Close the “break-glass and continuity-preserving UI states are weakly linked to audit” gap by binding them to the same audit join.
- Close the “retention could later delete graph-critical evidence” gap by publishing immutable retention classes now.
- Close the “FHIR AuditEvent / Provenance could replace the internal audit chain” gap.

Industry-best-practice requirements:
- append-only hash-chained audit
- explicit retention classes
- deterministic integrity verification
- least-necessary disclosure metadata
- no raw payload dependence where hashes and governed artifact refs suffice
- reproducible audit exports
- clear separation of canonical audit vs companion representations

Non-negotiable rules:
- `AuditRecord` is the immutable join row between ingress correlation, route intent, command settlement, visible shell decision, and disclosure posture.
- Server acceptance, local acknowledgement, or detached analytics events may not stand in for this audit join.
- WORM and hash-chained artifacts must never be scheduled for deletion.
- Replay, export, investigation, and retention must later consume the same admissibility graph / dependency model, not private evidence lists.
- `AuditEvent` and `Provenance` are companion representations; they may never replace immutable audit or replay joins.

Mandatory UI and interaction specification for `53_audit_ledger_explorer.html`:
Name the surface mode `Audit_Ledger_Explorer`.

Tone:
- minimalist premium investigation surface
- must feel trustworthy, forensic, and exact, not noisy or dark-pattern “security dashboard”
- precise enough for governance and support investigators

Branding:
- restrained Vecells wordmark plus an inline SVG `LEDGER` monogram
- include one subtle hash-chain lane and one causality braid, both backed by table parity

Palette:
- canvas `#F6F8FB`
- rail `#EEF2F7`
- panel `#FFFFFF`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- audit accent `#334155`
- disclosure accent `#0EA5A4`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for hashes, IDs, timestamps, and correlation keys

Layout:
- sticky masthead with:
  - audit-row count
  - chain-break count
  - WORM-class count
  - inadmissible dependency count
- left rail (`292px`):
  - action taxonomy filters
  - actor / scope filters
- center region:
  - hash-chain lane
  - audit-record table
- right inspector (`396px`):
  - selected record details
  - causality chain
  - disclosure fence
  - FHIR companion linkage
- lower region:
  - retention-class matrix
  - admissibility dependency table
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- main ledger region min height `580px`
- primary controls `44px`
- table rows `40px` minimum
- any diagrams must have immediate table/list parity

Motion:
- `120ms` hover/focus
- `180ms` record selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by action taxonomy, actor class, WORM class, admissibility state, and defect state
- select a record and keep chain lane, table, and inspector synchronized
- keyboard navigation across filters, table rows, dependency rows, and retention rows
- deterministic `data-testid` markers for filter rail, chain lane, ledger table, inspector, and defect strip
- the explorer must support safe redacted view defaults; do not require full-detail mode for basic inspection

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- taxonomy filtering
- record selection
- chain/table parity
- inspector rendering
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks

Definition of done:
- Vecells has one explicit authoritative audit and WORM-ledger strategy.
- Later assurance, replay, export, and retention tasks can rely on one canonical audit spine instead of inventing alternate audit truth.
- The repo now has a validator-backed audit model that is ready for Phase 9 assurance-ledger implementation.
```