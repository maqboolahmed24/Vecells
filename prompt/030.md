# Prompt 030 For 030_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_register_nhs_app_site_link_metadata_placeholders

Use the shared operating contract first:
[shared_operating_contract_026_to_035.md](/Users/test/Code/V/prompt/shared_operating_contract_026_to_035.md)

```text
Execute roadmap task 030_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_register_nhs_app_site_link_metadata_placeholders.md.

Inherit the Shared_operating_contract_for_prompts_026_to_035 and consume the validated outputs from tasks `001-029`, especially tasks `004`, `005`, `007`, `014`, `021`, `023`, `029`, and the route and channel outputs from Phase 7 planning.

Mission:
Create the NHS App site-link metadata execution pack in a way that respects the blueprint’s embedded-navigation and outbound-navigation law:
- deep links and site links are route-contract artifacts, not ad hoc URLs
- `.well-known` metadata must be generated from approved route families and environment posture
- placeholder metadata is useful now for design, path allowlists, and rehearsal
- real metadata registration is later, gated, and depends on official environment-specific values from the NHS App team

This task therefore has two equally important jobs:
1. build a high-fidelity site-link metadata studio and placeholder generator now
2. build a gated real environment-specific asset registration strategy later

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - embedded access and secure-link law
  - `OutboundNavigationGrant`
  - artifact presentation and return-safe continuity rules
- `phase-2-the-identity-and-echoes.md`, especially:
  - secure-link continuation and route-intent law
  - session / subject fence requirements on resumed routes
- `phase-7-inside-the-nhs-app.md`, especially:
  - `7D. Deep-link, site-link, and return-to-journey continuity`
  - `7E. NHS App bridge API, navigation model, and embedded behaviours`
  - `7F. Webview limitations, file handling, and resilient error UX`
- `platform-frontend-blueprint.md`
- `patient-account-and-communications-blueprint.md`
- `accessibility-and-content-system-contract.md`
- `forensic-audit-findings.md`, especially findings on embedded-channel control and patient degraded-mode authority

Current official NHS App site-link mechanics you must respect in the `Actual_provider_strategy_later` section:
Use the current NHS App developer guidance for web integrations and site links as the external-process truth for later execution. At minimum support the current rules around:
- hosting `/.well-known/assetlinks.json` for Android App Links
- hosting `/.well-known/apple-app-site-association` for iOS Associated Domains
- package name and certificate fingerprint varying by NHS App environment and being provided by the NHS App team during integration
- iOS `appID` varying by environment and being provided by the NHS App team during integration
- paths being explicitly allowlisted
- the need to coordinate changes with the NHS App onboarding team
These mechanics must not override Vecells architecture; they only shape the external registration path.

Primary outcome:
A product-grade Site Link Metadata Studio and placeholder generator that Vecells can use now, plus a later real environment-specific registration strategy that stays bound to route families, safe paths, and embedded continuity law.

Deliverables to create:
1. `docs/external/30_site_link_placeholder_strategy.md`
2. `docs/external/30_assetlinks_and_aasa_generation_spec.md`
3. `docs/external/30_route_path_allowlist_and_return_rules.md`
4. `docs/external/30_real_registration_and_hosting_strategy.md`
5. `apps/mock-nhs-app-site-link-studio/README.md`
6. `apps/mock-nhs-app-site-link-studio/src/`
7. `infra/site-links/assetlinks.template.json`
8. `infra/site-links/apple-app-site-association.template.json`
9. `tests/playwright/mock-site-link-studio.spec.(ts|js)`
10. `tests/playwright/site-link-local-hosting-validation.spec.(ts|js)`
11. `tools/browser-automation/site-link-registration-dry-run.spec.(ts|js)`
12. `data/analysis/site_link_route_allowlist.csv`
13. `data/analysis/site_link_environment_matrix.json`
14. `data/analysis/site_link_placeholder_values.json`
15. `tools/analysis/validate_site_link_pack.(py|ts)`

Every path row must capture at minimum:
- `route_family_ref`
- `path_pattern`
- `patient_visible_purpose`
- `embedded_safe = yes | no | conditional`
- `requires_outbound_navigation_grant`
- `requires_authenticated_session`
- `allows_secure_link_entry`
- `allows_from_nhs_app_query_marker`
- `placeholder_in_mock = yes | no`
- `real_registration_gate_refs[]`
- `notes`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build `apps/mock-nhs-app-site-link-studio` as a premium internal metadata generator and preview surface.

The studio must let the team exercise at minimum:
- route-family and path-pattern selection
- Android / iOS metadata generation with placeholders
- environment-specific variant preview (`local_mock`, `sandpit_like`, `aos_like`, `live_placeholder`)
- path allowlist validation
- secure-link and return-path safety review
- domain / host placeholder management
- local `.well-known` hosting and validation

The placeholder generator must make it impossible to claim:
- that environment-specific values are already known if they have not been issued by the NHS App team
- that any arbitrary route may be opened through site links
- that PHI-bearing or raw action URLs may be registered directly
- that real-domain hosting has been completed just because placeholder files exist

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the later real site-link registration strategy. It must include:
- environment-specific metadata field map
- host / domain ownership checklist
- package-name, certificate-fingerprint, and appID placeholder registry
- route-path approval workflow
- hosting and cache-control strategy for `.well-known` assets
- validation harness for production-like hosting
- browser-automation dry-run harness for registration prep and evidence capture
- live-execution gates that block any real registration unless:
  - the relevant NHS App environment has supplied the required Android / iOS values
  - the path allowlist is approved and source-traceable
  - the associated route families already satisfy embedded session, artifact, and return-safe continuity rules
  - named approver and environment target are present
  - `ALLOW_REAL_PROVIDER_MUTATION=true` is set

Execution steps:
1. Start from the route-family inventory and Phase 7 embedded-channel rules.
2. Build the path allowlist model first.
3. Generate placeholder asset files second.
4. Build the studio third and wire it to the generator.
5. Use `Playwright_or_other_appropriate_tooling` to validate local hosting of `.well-known` assets and preview flows.
6. Generate the real-later field map and hosting runbook from the same data model.
7. Validate that every path preserves safe return, no raw-PHI URL exposure, and embedded-session compatibility.

Mandatory gap closures you must perform:
- Close the “site links are just deep-link strings” gap by making them route-family and return-contract artifacts.
- Close the “placeholder metadata could drift from real registration needs” gap by sharing one environment matrix and field map.
- Close the “route allowlists can overexpose live actions” gap by tying allowlists to embedded-safe route families and `OutboundNavigationGrant` law.
- Close the “real environment values are unknown so nothing useful can be built” gap by creating a full placeholder generator and validation harness now.
- Close the “local `.well-known` files prove production readiness” gap by separating rehearsal hosting from gated real registration.

Non-negotiable rules:
- do not register arbitrary or wildcard routes without explicit allowlist reasoning
- do not expose PHI-bearing query parameters in registered site-link paths
- do not store real package IDs, fingerprints, or appIDs until officially supplied
- do not attempt real registration or hosting mutation unless the live gate passes
- do not let embedded preview bypass session, continuity, or return-safe rules

Mandatory UI and interaction specification for `apps/mock-nhs-app-site-link-studio`:
This must feel like a premium route-and-metadata atelier, not a generic JSON editor.

Name the visual mode `Linkloom_Metadata_Studio`.

Brand and visual posture:
- include a subtle inline Vecells mark plus a clear `MOCK_SITE_LINKS` ribbon
- do not imitate any official NHS portal directly
- tone: precise, lightly technical, quietly elegant
- base palette:
  - canvas `#F6F8FB`
  - panel `#FFFFFF`
  - inset `#EFF3F8`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#1D4ED8`
  - secondary accent `#0F766E`
  - path accent `#7A5AF8`
  - caution accent `#B54708`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for paths, hostnames, app IDs, fingerprints, and JSON keys

Layout:
- top environment banner with platform tabs, host status, and gate summary
- left route tree
- center metadata workbench with generated JSON previews
- right path-policy inspector
- lower strip with one restrained path-flow diagram showing `jump_off -> site_link -> embedded_route -> safe_return`

Dimensional constraints:
- page max width `1440px`
- route tree `280px`
- inspector `360px`
- preview cards `min-height 150px`
- buttons `44px`
- chip height `28px`
- sticky header `72px`

Unique page set:
1. `Route_Path_Allowlist`
2. `Android_Assetlinks_Generator`
3. `iOS_AASA_Generator`
4. `Local_Hosting_Validator`
5. `Real_Registration_Gates`

Motion:
- `120ms` hover and focus
- `180ms` tab and tree transitions
- `240ms` preview and inspector transitions
- reduced-motion support mandatory

Interaction requirements:
- route filtering
- environment tabs
- copy-safe JSON preview
- validation warnings for unsafe paths
- keyboard-first interaction
- deterministic `data-testid` markers for route tree, preview panes, inspectors, and environment switcher

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and testing from the start. At minimum cover:
- route selection and filtering
- JSON generation and validation
- local `.well-known` hosting checks
- unsafe-path warning rendering
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- one site-link metadata studio and placeholder generator exist and are demonstrably useful now
- one real-later registration strategy exists with hard live gates
- every path is tied to route family, safe return, and embedded-channel rules
- later registration can proceed from these artifacts without re-deriving metadata structure or allowlist discipline from scratch
```
