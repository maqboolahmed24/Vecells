# Prompt 042 For 042_seq_phase0_scaffold_monorepo_apps_services_and_packages

Use the shared operating contract first:
[shared_operating_contract_036_to_045.md](/Users/test/Code/V/prompt/shared_operating_contract_036_to_045.md)

```text
Execute roadmap task 042_seq_phase0_scaffold_monorepo_apps_services_and_packages.md.

Inherit the Shared_operating_contract_for_prompts_036_to_045 and consume the validated outputs from tasks `001-041`.

Mission:
Scaffold the Phase 0 Vecells monorepo according to the frozen topology and boundary rules. Create executable workspaces for apps, services, packages, shared tooling, and validation, while keeping every placeholder honest to the blueprint corpus.

This task must create a buildable foundation, not a decorative folder tree.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially `0A. Delivery skeleton and repository architecture`
- `blueprint-init.md`, especially `### 2. Core product surfaces` and `### 12. Practical engineering shape`
- `platform-frontend-blueprint.md`, especially shell model, route inventory contract, and shell-managed navigation / continuity law
- `design-token-foundation.md`, `canonical-ui-contract-kernel.md`, `accessibility-and-content-system-contract.md`, and `ux-quiet-clarity-redesign.md` for all front-end shell placeholders
- `platform-runtime-and-release-blueprint.md`, especially `RuntimeTopologyManifest`, `GatewayBffSurface`, `FrontendContractManifest`, and verification-ladder rules
- `forensic-audit-findings.md`, especially findings on shell continuity, route settlement, publication parity, continuity evidence, operations/gov surfaces, accessibility, and token/publication drift
- outputs from tasks `011-016`, `041`, and `040`

Primary outcome:
A buildable monorepo with the canonical app, service, and package skeletons in place, shared workspace configuration wired, placeholder shells rendered, and validation/testing entrypoints ready.

Deliverables to create:
1. root workspace configuration files from the toolchain chosen in task `012`
2. scaffolded directories and starter code for every canonical app, service, and package from task `041`
3. `docs/architecture/42_monorepo_scaffold_plan.md`
4. `docs/architecture/42_foundation_shell_gallery.html`
5. `data/analysis/monorepo_scaffold_manifest.json`
6. `tools/analysis/validate_monorepo_scaffold.(py|ts)`
7. `tests/playwright/foundation-shell-gallery.spec.(ts|js)`
8. package-level build / lint / test / typecheck scripts for each workspace

At minimum, scaffold these app families unless task `041` recorded an explicit canonical alternative with rationale:
- `apps/patient-web`
- `apps/clinical-workspace`
- `apps/ops-console`
- `apps/hub-desk`
- `apps/pharmacy-console`
- `apps/support-workspace`
- `apps/governance-console`

At minimum, scaffold these service families:
- `services/api-gateway`
- `services/command-api`
- `services/projection-worker`
- `services/notification-worker`
- `services/adapter-simulators`

At minimum, scaffold these shared or domain packages:
- `packages/domain-kernel`
- `packages/event-contracts`
- `packages/fhir-mapping`
- `packages/design-system`
- `packages/authz-policy`
- `packages/test-fixtures`
- `packages/observability`
- `packages/api-contracts`
- `packages/release-controls`
- any explicit domain packages frozen by task `041`

Execution steps:
1. Start from task `041`’s topology manifest and boundary rules.
2. Create the monorepo workspace root, package manager / build tool configuration, shared ts/js config, lint config, test config, and env-example structure according to task `012`.
3. Scaffold every app, service, and package with buildable placeholder entrypoints and README files that state ownership, allowed dependencies, and source refs.
4. Create a monorepo scaffold manifest listing every workspace, its scripts, and its owning bounded context.
5. Create the shell gallery page and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
6. Ensure all placeholder apps boot, render a distinct shell identity, and expose stable automation markers.
7. Validate that workspace scripts run and the scaffold manifest matches the real folder tree.

Mandatory gap closures you must perform:
- Close the “folder tree without build system” gap by wiring actual workspace tooling.
- Close the “placeholder shells all look the same” gap by giving each shell a distinct layout identity while still inheriting shared design tokens.
- Close the “support, pharmacy, and governance shells appear later only in docs” gap by scaffolding them now if task `041` kept them in baseline topology.
- Close the “monorepo exists but route / shell / package ownership is invisible” gap by embedding ownership metadata in scaffold files and manifests.
- Close the “apps boot but are not automation-ready” gap by adding stable landmarks and `data-testid` markers from day one.

Non-negotiable rules:
- No placeholder app may claim to implement live business truth it does not yet own.
- All placeholder shells must inherit the shared design system and token graph.
- All generated workspace configs must be deterministic and checked in.
- No scaffold may violate the import boundaries frozen in task `041`.

Mandatory UI and interaction specification for the shell placeholders and `42_foundation_shell_gallery.html`:
This is a premium internal foundation showcase. Name the gallery mode `Foundations_Gallery`.

Shared posture:
- minimalist premium, quiet, exact, obviously part of one product family, but with distinct identities per shell
- max width `1440px`
- 12-column desktop grid with `24px` gutters; 8-column tablet; 4-column mobile
- base canvas `#F5F7FA`
- panel `#FFFFFF`
- inset `#EEF2F6`
- text strong `#101828`
- text default `#1D2939`
- text muted `#667085`
- border subtle `#E4E7EC`
- border default `#D0D5DD`
- motion timings `120ms`, `180ms`, `240ms`
- system sans stack only; mono only for IDs and contract keys
- visible `2px` focus ring with `2px` offset

Each shell must feel unique:
- `patient-web`:
  - premium calm-entry shell
  - hero line: quiet lineage ribbon + next-step card + one minimal continuity diagram
  - accent set: blue `#2F5BFF`, teal `#0EA5A4`
  - animation: gentle content fade + 8px translate
- `clinical-workspace`:
  - queue-left / decision-dock-right composition
  - accent set: indigo `#4F46E5`, slate `#0F172A`
  - include a restrained lane diagram for review -> decision -> settlement
- `ops-console`:
  - telemetry horizon with KPI strip and one compact heat-map placeholder with table parity
  - accent set: emerald `#0F9D58`, graphite `#111827`
- `hub-desk`:
  - coordination-lane layout with comparison strip
  - accent set: cobalt `#3559E6`, plum `#7C3AED`
- `pharmacy-console`:
  - validation board with consent/proof chips and provider-choice rail
  - accent set: green `#147D64`, amber `#C98900`
- `support-workspace`:
  - ticket timeline + masked subject card + replay return strip
  - accent set: steel `#334155`, silver `#94A3B8`
- `governance-console`:
  - policy-ledger cards + release-freeze / trust tuple strip
  - accent set: violet `#6E59D9`, slate `#1F2937`

Dimensional constraints:
- shell masthead height `72px`
- primary cards `min-height 160px`
- side rails `280px` target
- primary controls `44px`
- chips `28px`

Interaction requirements:
- every shell must expose a unique root landmark and unique `data-testid` namespace
- keyboard-first navigation
- reduced-motion support mandatory
- responsive behavior down to mobile widths
- every chart/diagram must have adjacent table/list parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- gallery load
- each shell-card launch or preview
- unique shell landmarks
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks
- table parity for any charts or diagrams
- offline asset completeness for the gallery and placeholder shells

Definition of done:
- Vecells has a buildable monorepo foundation with canonical apps, services, and packages scaffolded.
- Every shell placeholder is visually distinct, automation-ready, and rooted in the shared design system.
- Workspace scripts, configs, and manifests are in place for later tasks to extend directly.
```
