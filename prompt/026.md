# Prompt 026 For 026_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_submit_im1_pairing_prerequisite_forms_and_track_scal_artifacts

Use the shared operating contract first:
[shared_operating_contract_026_to_035.md](/Users/test/Code/V/prompt/shared_operating_contract_026_to_035.md)

```text
Execute roadmap task 026_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_submit_im1_pairing_prerequisite_forms_and_track_scal_artifacts.md.

Inherit the Shared_operating_contract_for_prompts_026_to_035 and consume the validated outputs from tasks `001-025`, especially tasks `005`, `008`, `021`, `022`, `023`, `024`, and `025`.

Mission:
Create the IM1 Pairing execution pack in a way that respects the blueprint’s hard law:
- Vecells must model the GP / booking / provider boundary now
- Vecells must not make IM1 a hidden critical-path dependency for MVP identity or basic patient continuity
- real IM1 application work is likely later and gated
- the product still needs a rehearsal-grade internal twin now so the programme can shape its provider capability model, SCAL evidence pack, and eventual provider-supplier path without waiting for external approval

This task therefore has two equally important jobs:
1. build a high-fidelity internal IM1 pairing rehearsal studio now
2. build a gated, dry-run-first real IM1 prerequisites / SCAL / licence strategy for later

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 1. Product definition`
  - `### 3. The canonical request model`
  - `### 6. Booking and access continuity`
  - `### 10. Identity, consent, security, and policy`
  - `### 12. Practical engineering shape`
  - the principle that supplier capability sits behind adapters, not inside the core model
- `phase-0-the-foundation-protocol.md`, especially:
  - `1.13A ProviderCapabilityMatrix`
  - `1.13B BookingProviderAdapterBinding`
  - `1.17 ExternalConfirmationGate`
  - route-intent, command-settlement, adapter-profile, and closure-blocker law
  - the rule that browser clients do not talk directly to provider systems
- `phase-2-identity-and-echoes.md`, especially:
  - `Rule 5: keep IM1 out of the critical path for this phase`
  - `2C. Patient linkage, demographic confidence, and optional PDS enrichment`
  - any sections that forbid treating NHS login or IM1 linkage keys as a shortcut to writable patient authority
- `phase-4-the-booking-engine.md`, especially:
  - `4B. Provider capability matrix and adapter seam`
  - `4C. Slot search, normalisation, and availability snapshots`
  - `4E. Commit path, revalidation, booking record, and compensation`
- `phase-5-the-network-horizon.md` where provider capability and cross-org booking truth cannot be flattened into generic success
- `phase-8-the-assistive-layer.md`, especially the IM1 / RFC / AI change-control boundary
- `platform-runtime-and-release-blueprint.md`, especially:
  - `AdapterContractProfile`
  - `DependencyDegradationProfile`
- `forensic-audit-findings.md`, especially findings that punish:
  - direct supplier truth being mistaken for canonical truth
  - confirmation ambiguity being hidden
  - supplier-domain code writing canonical request state directly
  - AI or later functional expansion happening without explicit change control

Current official IM1 process mechanics you must respect in the `Actual_provider_strategy_later` section:
Use the current official IM1 Pairing process and treat it as the external-process truth for later execution. At minimum support the current process stages and rules around:
- IM1 Clinical and Information Governance prerequisites form
- stage one SCAL issuance when prerequisites are confirmed
- product compatibility assessment against provider suppliers
- Model Interface Licence execution with provider suppliers
- access to provider supplier mock API during initiation
- Pairing and Integration Pack usage during unsupported test
- Supported Test Environment access via fully completed SCAL
- assurance, Recommended to Connect, Plan to Connect, and live rollout steps
- RFC requirement for significant product evolution, including AI-related changes
These mechanics must not override Vecells architecture; they only shape the external onboarding path.

Primary outcome:
A product-grade IM1 Pairing Control Tower that lets Vecells rehearse the pairing journey now, plus a real later execution strategy that fails closed until the product, evidence, and sponsorship posture are genuinely ready.

Deliverables to create:
1. `docs/external/26_im1_pairing_rehearsal_strategy.md`
2. `docs/external/26_im1_pairing_prerequisites_field_map.md`
3. `docs/external/26_im1_scal_artifact_matrix.md`
4. `docs/external/26_im1_provider_supplier_and_licence_register.md`
5. `docs/external/26_im1_change_control_and_rfc_strategy.md`
6. `apps/mock-im1-pairing-studio/README.md`
7. `apps/mock-im1-pairing-studio/src/` (implement the rehearsal studio in the chosen stack)
8. `tests/playwright/mock-im1-pairing-studio.spec.(ts|js)`
9. `tests/playwright/mock-im1-pairing-form-validation.spec.(ts|js)`
10. `tools/browser-automation/im1-prerequisites-dry-run.spec.(ts|js)`
11. `data/analysis/im1_pairing_stage_matrix.csv`
12. `data/analysis/im1_prerequisites_field_map.json`
13. `data/analysis/im1_scal_artifact_matrix.csv`
14. `data/analysis/im1_provider_supplier_register.json`
15. `data/analysis/im1_live_gate_checklist.json`
16. `tools/analysis/validate_im1_pairing_pack.(py|ts)`

Every IM1 stage row must capture at minimum:
- `stage_id`
- `stage_name`
- `stage_class = internal_rehearsal | official_process | blocked_until_mvp | provider_supplier_specific`
- `entry_conditions[]`
- `required_artifacts[]`
- `manual_checkpoints[]`
- `browser_automation_possible = yes | partial | no`
- `mock_now_action`
- `actual_later_action`
- `outputs[]`
- `safety_and_privacy_dependencies[]`
- `risk_refs[]`
- `notes`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build `apps/mock-im1-pairing-studio` as a rehearsal-grade IM1 onboarding twin.

The studio must model the real-world flow without pretending Vecells is already approved. It must clearly separate:
- internal readiness
- formal IM1 application readiness
- provider-supplier compatibility readiness
- assurance readiness
- future RFC / change-control posture

The mock IM1 twin must include these stages:
- `product_profile_defined`
- `prerequisites_drafted`
- `stage_one_scal_stub_ready`
- `provider_supplier_targeting_ready`
- `compatibility_claim_ready`
- `model_interface_licence_placeholder_ready`
- `provider_mock_api_rehearsal_ready`
- `supported_test_readiness_blocked`
- `assurance_pack_in_progress`
- `rfc_watch_registered`
- `ready_for_real_im1_submission`

The rehearsal twin must let the team exercise at minimum:
- product-use-case articulation for IM1 pairing
- mapping Vecells modules to provider-supplier capability expectations
- evidence-linking into the right SCAL sections
- provider-supplier compatibility notes per route family
- draft licence / legal placeholder tracking
- unsupported-test preparation and PIP placeholder registry
- supported-test readiness gating
- assurance evidence freshness and blockers
- future AI / major-change RFC watchpoints

Build the mock around provider capability truth, not wishful product scope. The studio must make it impossible to claim:
- that IM1 is required for the current identity baseline
- that stage-one application implies technical approval
- that access to a provider mock API implies live use approval
- that a future assistive / AI feature can ride through existing IM1 paperwork without explicit RFC review

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the real later IM1 strategy. It must include:
- prerequisites-form field map
- stage-one SCAL input pack
- provider-supplier targeting matrix
- licence placeholder and signatory register
- provider mock-API entry plan
- supported-test request checklist
- assurance evidence upload sequence
- RFC trigger register for future product change classes
- a browser-automation dry-run harness for form preparation and screenshot evidence capture
- a live-execution gate that blocks any real submission unless:
  - the product has a credible MVP / demo and a bounded IM1 use case
  - the provider capability model is frozen enough for submission
  - safety, privacy, DPIA, architecture, and data-flow artifacts are current
  - named sponsor / commercial owner posture is known
  - the task confirms IM1 is not being used to sneak around Phase 2 identity law
  - named approver and environment target are present
  - `ALLOW_REAL_PROVIDER_MUTATION=true` is set

Execution steps:
1. Start from tasks `021-025`.
2. Build the full IM1 field map and stage model first.
3. Build the mock rehearsal studio before any real dry-run automation.
4. Use `Playwright_or_other_appropriate_tooling` to drive the rehearsal studio during development.
5. Generate the actual-later dry-run harness from the same field map and evidence schema.
6. Build provider-supplier compatibility rows from the blueprint capability model, not from generic booking assumptions.
7. Add AI / significant-change RFC watch rules so future Phase 8 or later changes do not silently invalidate pairing posture.

Mandatory gap closures you must perform:
- Close the “roadmap asks for IM1 submission work but the blueprint says keep IM1 out of the critical path” gap by splitting rehearsal from real submission and by explicitly preserving feature-flagged optionality.
- Close the “provider capability and IM1 paperwork can drift apart” gap by tying every submission claim to `ProviderCapabilityMatrix` and adapter profiles.
- Close the “stage one SCAL and provider compatibility are vague narrative steps” gap by making them machine-readable stages with specific required evidence.
- Close the “later AI or major feature expansion may invalidate prior pairing assurance” gap with an RFC watch register.
- Close the “mock portal could mistakenly imply live approval” gap with explicit visual disclaimers and blocked-live posture.
- Close the “provider supplier list can change” gap by forcing the actual-later workflow to fetch the current provider-supplier roster from official sources at runtime rather than hard-coding a stale list.

Non-negotiable rules:
- do not let IM1 become required for baseline patient sign-in, patient ownership, or Phase 2 continuation
- do not store licence drafts, legal names, or signatory secrets in repo fixtures
- do not encode provider-supplier-specific business logic into the core domain model
- do not treat provider mock API access as authoritative booking truth
- do not allow a real prerequisites form submission unless the live gate passes
- do not allow assistive / AI expansion to piggyback on a stale IM1 pairing assumption without explicit RFC handling

Mandatory UI and interaction specification for `apps/mock-im1-pairing-studio`:
This must feel like a premium internal control tower, not a generic compliance form.

Name the visual mode `Interface_Proof_Atelier`.

Brand and visual posture:
- include a subtle inline Vecells monogram plus a clear `MOCK_IM1_PAIRING` ribbon
- do not imitate any NHS or provider-supplier portal closely enough to be confused with the real thing
- tone: formal, technically exact, low-drama, consequence-aware
- base palette:
  - canvas `#F5F7FA`
  - panel `#FFFFFF`
  - inset `#EEF2F6`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#2457F5`
  - secondary accent `#0E9384`
  - progress accent `#12B76A`
  - watch accent `#B54708`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for stage IDs, field keys, provider-supplier codes, and document hashes

Layout:
- sticky top readiness header with stage, blocker count, assurance freshness, and mock/actual mode toggle
- left stage rail for `initiation -> unsupported_test -> supported_test -> assurance -> live -> rfc_watch`
- center dossier canvas with field sections and structured evidence attachments
- right evidence inspector for selected artifact, source refs, freshness, and blocker reasons
- lower split region:
  - provider-supplier compatibility matrix
  - one restrained line diagram of `prerequisites -> stage_one_scal -> licence -> provider_mock_api -> supported_test -> assurance -> live`

Dimensional constraints:
- page max width `1440px`
- stage rail `288px`
- evidence inspector `360px`
- dossier cards `min-height 160px`
- buttons `44px`
- chip height `28px`
- sticky header `72px`

Unique page set:
1. `IM1_Readiness_Overview`
2. `Prerequisites_Dossier`
3. `SCAL_Artifact_Map`
4. `Provider_Compatibility_Matrix`
5. `Licence_and_RFC_Watch`

Motion:
- `120ms` hover and focus
- `180ms` stage transitions
- `240ms` inspector drawer transitions
- reduced-motion support mandatory

Interaction requirements:
- autosave draft posture
- explicit blocker chips with machine-readable codes
- no silent stage progression
- keyboard-first navigation
- deterministic `data-testid` markers for stage rail, dossier cards, provider matrix rows, evidence drawer, and mode toggle

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and testing from the start. At minimum cover:
- stage navigation and blocking rules
- form completion and validation
- evidence attachment simulation
- provider compatibility matrix filtering and expansion
- RFC watch creation
- mock/actual toggle behavior
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- one rehearsal-grade IM1 pairing twin exists and is demonstrably usable now
- one actual-later IM1 prerequisites and SCAL strategy exists with hard live gates
- the outputs preserve the blueprint rule that IM1 is optional and adapter-bound until genuinely onboarded
- future provider-supplier engagement, SCAL completion, and RFC handling can proceed from these artifacts without re-deriving the process from scratch
```
