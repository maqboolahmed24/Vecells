# Prompt 096 For 096_par_phase0_track_runtime_implement_client_cache_policy_live_update_channels_and_recovery_postures

Use the shared operating contract first:
[shared_operating_contract_096_to_105.md](/Users/test/Code/V/prompt/shared_operating_contract_096_to_105.md)

```text
Execute roadmap task 096_par_phase0_track_runtime_implement_client_cache_policy_live_update_channels_and_recovery_postures.md.

Inherit the Shared_operating_contract_for_prompts_096_to_105 and consume the validated outputs from tasks `001-095`.

Mission:
Implement the authoritative browser-runtime truth layer for cache semantics, live-update channels, staleness disclosure, and audience-safe recovery posture.

This task must create real runtime-control code and contract publication for:
- `LiveUpdateChannelContract`
- `ClientCachePolicy`
- reconnect, replay-gap, and staleness disclosure behavior
- bounded offline or stale reuse posture
- browser downgrade from live to read-only or recovery-only mode
- authoritative recovery posture when freshness, parity, freeze, or trust drift prevents ordinary writable behavior

This is real backend and runtime-control work. Produce schemas, generators, validators, gateway integration, browser-runtime support libraries, deterministic tests, and a browser-viewable inspection surface. Do not leave cache law, socket/SSE behavior, stale disclosure, or recovery posture in framework defaults or route-local hooks.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### LiveUpdateChannelContract`
  - `### ClientCachePolicy`
  - `### FrontendContractManifest`
  - `RuntimePublicationBundle`
  - `ReleasePublicationParityRecord`
  - `ReleaseRecoveryDisposition`
  - the verification-ladder rules that bind query, mutation, channel, cache, and recovery contracts to one published runtime tuple
- `phase-0-the-foundation-protocol.md`, especially the visibility law, continuity law, route-intent law, command-settlement law, projection freshness, and authority-freeze rules
- `platform-frontend-blueprint.md`, especially:
  - projection freshness and shared status-strip law
  - `CalmDegradedStateContract`
  - `ProjectionSubscription`
  - `ProjectionFreshnessEnvelope`
  - motion and recovery rules that distinguish pending, stale, invalidated, recovered, and read-only posture
- `patient-account-and-communications-blueprint.md` for patient-safe continuity, reply / booking / recovery actionability, and same-shell recovery
- `staff-workspace-interface-architecture.md` and `staff-operations-and-support-blueprint.md` for workspace trust envelopes and stale review posture
- `operations-console-frontend-blueprint.md` for health and diagnostic read-only behavior on degraded trust
- `accessibility-and-content-system-contract.md`, especially timeout, freshness, recovery, live-region, and visualization-parity obligations
- outputs from tasks `014`, `047`, `050`, `058`, `088`, `090`, `094`, and `095`
- `forensic-audit-findings.md`, especially Findings `87`, `88`, `91`, `97`, `101`, `116`, and `120`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must publish the cache and live-channel contracts now so later frontend tasks, runtime publication checks, canary harnesses, and shell implementations can consume them. If a later task owns a specific audience manifest or shell specialization, publish the shared contract seam here and record `FOLLOW_ON_DEPENDENCY_*` rather than hard-coding shell-local policy.

Mock_now_execution:
Build the full cache and live-update law now against seeded data, simulator-backed channels, and non-production runtime tuples.
- support simulator-backed SSE or websocket feeds, cache invalidation rehearsal, stale-read disclosure, reconnect windows, and degraded read-only fallback
- prove that cached summaries and live deltas can preserve continuity without re-enabling stale writable posture
- use realistic patient, workspace, operations, and support route families even if later live providers are not yet wired in
The mock-now path must be the real contract path. Later cutover to live runtime traffic must not require rewriting cache scopes, channel semantics, or recovery posture.

Actual_production_strategy_later:
When production traffic is enabled, preserve the same `ClientCachePolicy`, `LiveUpdateChannelContract`, staleness disclosure, downgrade, and recovery semantics. Production hardening may change transport, fanout scale, edge cache strategy, reconnect tuning, or push infrastructure, but it must not allow local browser state to imply fresh or writable truth after parity, trust, or freeze drift.

Primary outcome:
The repository gains one authoritative cache and live-update execution layer that browsers, gateways, and verification tooling can all consume without inventing route-local truth.

Implementation deliverables to create:
1. `docs/architecture/96_client_cache_and_live_update_design.md`
2. `docs/architecture/96_recovery_posture_and_staleness_disclosure.md`
3. `docs/architecture/96_browser_freshness_and_recovery_matrix.md`
4. `docs/architecture/96_cache_channel_contract_studio.html`
5. `data/analysis/client_cache_policy_catalog.json`
6. `data/analysis/live_update_channel_contract_catalog.json`
7. `data/analysis/browser_recovery_posture_matrix.csv`
8. `packages/api-contracts/schemas/client-cache-policy.schema.json`
9. `packages/api-contracts/schemas/live-update-channel-contract.schema.json`
10. `packages/api-contracts/schemas/browser-recovery-posture.schema.json`
11. runtime-control code under the repo’s chosen package/service layout for cache-policy resolution, channel-policy resolution, reconnect handling, stale-disclosure, and downgrade execution
12. generated typed bindings for gateway and browser runtimes
13. `tools/analysis/validate_cache_and_live_update_contracts.(py|ts)`
14. integration tests for cache invalidation, reconnect, parity drift, freeze drift, and recovery downgrade
15. `tests/playwright/cache-channel-contract-studio.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- schema-validated `LiveUpdateChannelContract` rows with `channelCode`, message schema, reconnect policy, staleness disclosure, downgrade disposition, required projection readiness refs, and assurance-slice trust refs
- schema-validated `ClientCachePolicy` rows with cache scope, TTL/refetch rules, mutation invalidation rules, stale-while-revalidate mode, offline reuse disposition, and sensitive-field handling
- a deterministic resolver that joins route family, audience surface, runtime publication bundle, release recovery disposition, parity state, and freeze posture into one browser runtime decision
- support for `cacheScope = shell | route_family | entity | query_result` with explicit invalidation and TTL rules
- reconnect handling that distinguishes transport reconnect from truth freshness; successful reconnect must not silently imply fresh business truth without the required projection or publication readiness
- bounded stale reuse behavior that never reconstructs calm success, green freshness, or writable CTA enablement from old cached payloads
- browser downgrade to `read_only`, `recovery_only`, or `blocked` when runtime publication, parity, trust, freeze, or channel contract drift requires it
- patient-safe, staff-safe, and ops-safe staleness disclosure models that bind to one authoritative recovery or read-only posture instead of generic error banners
- PHI-safe cache handling rules for sensitive or partially visible fields, including placeholder or summary-only reuse where the corpus requires it
- observability and audit events for reconnect, stale disclosure, downgrade, recovery completion, cache invalidation, and channel suspension

Implementation instructions:
1. Generate machine-readable contract catalogs from the current route/audience/runtime inventory instead of hand-maintained static lists.
2. Bind every published query and live channel to exactly one `ClientCachePolicy`; validation must fail if a query or channel reaches the browser without a current policy.
3. Add a runtime resolver library that can be consumed by gateway, shell, and test harness code; route-local hooks may call the resolver, but they may not fork the underlying policy logic.
4. Implement a `ProjectionFreshnessEnvelope` or equivalent shared runtime object that carries freshness, parity, trust, and recovery implications together.
5. Make offline or stale reuse audience-aware:
   - patient surfaces default to summary-preserving and action-suppressing posture
   - workspace surfaces may preserve current anchor and explanation while freezing mutation
   - operations surfaces may preserve diagnostics and last-safe summaries while downgrading to explicit read-only or table-only modes
6. Distinguish at least these failure / degraded classes in code and tests:
   - transient transport disconnect
   - replay gap or message-order ambiguity
   - stale projection freshness
   - runtime publication drift
   - release or channel freeze
   - assurance or trust degradation
   - browser manifest drift versus current runtime tuple
7. Never allow the following anti-patterns:
   - websocket connected => fresh truth
   - cached entity present => writable CTA may remain enabled
   - successful poll => authoritative settlement implied
   - generic “syncing” badge that hides blocked or recovery-only posture
8. Publish one browser-viewable contract studio that lets engineers inspect route family -> query/channel/cache/recovery bindings from one authoritative dataset.

Gap handling you must perform during this task:
- If the corpus leaves reconnect timing bands or replay-gap thresholds under-specified, publish bounded defaults as `GAP_RESOLUTION_RECONNECT_POLICY_*` records and keep them tied to contract data, not transport code constants.
- If the corpus does not fully enumerate audience-specific stale-copy text, publish placeholder-safe contract entries now and record the copy debt as `FOLLOW_ON_DEPENDENCY_CONTENT_*`.
- If a route family currently lacks an explicit live-channel or cache-policy binding, fail closed and emit `GAP_ROUTE_CACHE_OR_CHANNEL_BINDING_*` rather than guessing.
- If runtime publication or parity state is unavailable in the current environment, degrade deterministically to the safest allowed browser posture and record `FOLLOW_ON_DEPENDENCY_RUNTIME_TUPLE_*`.

Backend engineering standards:
- use idempotent invalidation and event-handling logic
- design for backpressure and reconnect storms
- bound fanout and memory use per audience surface
- keep cache contents and channel state partitioned by tenant, audience, and session lineage where required by the upstream architecture
- expose metrics for reconnect churn, stale-read rate, downgrade rate, invalidation lag, and live-channel gap detection
- add property or fuzz tests for out-of-order deltas, duplicated deltas, and rapid freeze/unfreeze state churn

Verification requirements:
- contract-generation tests
- schema-validation tests
- gateway integration tests proving unpublished or stale channel contracts fail closed
- browser-runtime tests for stale reuse, mutation invalidation, reconnect, and downgrade behavior
- reduced-motion and accessibility checks in the studio surface
- `Playwright_or_other_appropriate_tooling` tests that assert stable automation anchors, keyboard navigation, landmark structure, DOM-state exposure, and correct visible downgrade behavior under simulated drift

Definition of done:
- every published query and live channel resolves one current cache and recovery contract
- browser runtime decisions are derived from published data, not scattered route code
- stale or offline continuity preserves context without re-enabling unsafe actions
- parity, trust, and freeze drift produce deterministic and testable browser downgrade behavior
- the contract studio and automated tests make the cache/live-update law inspectable and enforceable
```
