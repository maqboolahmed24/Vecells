# Prompt 028 For 028_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_request_mesh_test_mailbox_and_message_workflow_access

Use the shared operating contract first:
[shared_operating_contract_026_to_035.md](/Users/test/Code/V/prompt/shared_operating_contract_026_to_035.md)

```text
Execute roadmap task 028_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_request_mesh_test_mailbox_and_message_workflow_access.md.

Inherit the Shared_operating_contract_for_prompts_026_to_035 and consume the validated outputs from tasks `001-027`, especially tasks `005`, `008`, `021`, `022`, `023`, `026`, and `027`.

Mission:
Create the MESH execution pack in a way that respects the blueprint’s messaging and proof law:
- cross-org messaging and transport are external evidence sources, not canonical truth
- workflow IDs, mailbox ownership, and message acceptance are governed operational facts, not generic queue details
- the product needs a high-fidelity local MESH twin now
- real mailbox application and live workflow enablement are later, gated, and may depend on onboarding or partner context

This task therefore has two equally important jobs:
1. build a high-fidelity local MESH twin, mailroom console, and workflow-ID registry now
2. build a gated real mailbox / API-onboarding and workflow-request strategy later

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 6. Booking and access continuity`
  - `### 7. Pharmacy First pathway`
  - `### 12. Practical engineering shape`
- `phase-0-the-foundation-protocol.md`, especially:
  - adapter law
  - external proof vs canonical settlement law
  - `ExternalConfirmationGate`-style ambiguity handling
  - artifact and message audit rules
- `phase-5-the-network-horizon.md`, especially:
  - cross-org messaging and practice visibility expectations
  - no-slot, fallback, and reopen semantics that must not depend on silent mailbox success
- `phase-6-the-pharmacy-loop.md`, especially:
  - dispatch adapters
  - transport contract
  - dispatch proof and outcome reconciliation
  - bounce-back and urgent return rules
- `platform-runtime-and-release-blueprint.md`, especially:
  - `AdapterContractProfile`
  - `DependencyDegradationProfile`
- `forensic-audit-findings.md`, especially findings that harden:
  - pharmacy dispatch acknowledgement / retry / expiry behavior
  - attachment and quarantine events
  - external-state reconciliation not being treated as purely synchronous

Current official MESH mechanics you must respect in the `Actual_provider_strategy_later` section:
Use the current official MESH guidance as the external-process truth for later execution. At minimum support the current rules around:
- applying for a MESH mailbox
- testing in Path to Live integration and local MESH-sandbox options
- form fields for environment, organisation type, ODS code, nominated representative, third-party management, version choice, and workflow IDs
- needing API onboarding before applying for a live MESH API mailbox
- requesting new workflow IDs before mailbox submission if needed
- documenting the message business flow per workflow usage
These mechanics must not override Vecells architecture; they only shape the external onboarding path.

Primary outcome:
A product-grade mock MESH mailroom and workflow control pack that Vecells can use now, plus a later mailbox / workflow onboarding strategy that treats mailbox access as operational evidence rather than hidden business truth.

Deliverables to create:
1. `docs/external/28_mesh_mock_mailroom_spec.md`
2. `docs/external/28_mesh_mailbox_application_field_map.md`
3. `docs/external/28_mesh_workflow_group_and_id_registry.md`
4. `docs/external/28_mesh_message_route_and_proof_matrix.md`
5. `docs/external/28_mesh_live_gate_and_approval_strategy.md`
6. `services/mock-mesh/README.md`
7. `services/mock-mesh/src/` (implement the local mock MESH service)
8. `apps/mock-mesh-mailroom/README.md`
9. `apps/mock-mesh-mailroom/src/`
10. `tests/playwright/mock-mesh-mailroom.spec.(ts|js)`
11. `tests/playwright/mock-mesh-workflow-proof.spec.(ts|js)`
12. `tools/browser-automation/mesh-mailbox-dry-run.spec.(ts|js)`
13. `data/analysis/mesh_mailbox_field_map.json`
14. `data/analysis/mesh_workflow_registry.csv`
15. `data/analysis/mesh_message_route_matrix.csv`
16. `data/analysis/mesh_live_gate_checklist.json`
17. `tools/analysis/validate_mesh_pack.(py|ts)`

Every workflow row must capture at minimum:
- `workflow_group`
- `workflow_id`
- `message_family`
- `bounded_context_ref`
- `business_flow_summary`
- `proof_required_after_send`
- `acceptance_vs_authoritative_truth_note`
- `mailbox_direction = inbound | outbound | duplex`
- `path_to_live_need`
- `live_mailbox_need`
- `mock_now_support_level`
- `fallback_if_missing`
- `notes`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build two coordinated mock artifacts:
1. `services/mock-mesh` — a local MESH twin
2. `apps/mock-mesh-mailroom` — a premium internal mailroom console

The mock MESH twin must emulate enough behavior to let Vecells genuinely test:
- mailbox registration and ownership metadata
- workflow group and workflow ID restrictions
- message submission and acceptance
- delayed acknowledgement, retry, timeout, and expiry
- message pickup, duplicate delivery, and replay resistance
- attachment carriage and quarantine handoff
- proof-of-dispatch vs authoritative downstream outcome separation
- local sandbox mode vs path-to-live-like mode

The mailroom console must let the team exercise at minimum:
- mailbox identity and ODS ownership modeling
- workflow-ID assignment and validation
- outbound dispatch path visualization
- inbound proof / ack / failure / expiry visualization
- message timeline and replay evidence
- degraded mode and fallback-review handling
- third-party mailbox-manager posture

The mock must make it impossible to claim:
- that accepted by MESH equals authoritative downstream success
- that a missing workflow ID can be papered over by generic message send
- that a later real mailbox application is just an account-creation chore rather than a governed message-route decision

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the later live MESH strategy. It must include:
- mailbox application field map
- workflow-group and workflow-ID request pack
- API-onboarding prerequisite checklist for live API mailbox cases
- ODS and mailbox-ownership register
- third-party-management decision record
- business-flow statement templates
- browser-automation dry-run harness for mailbox application preparation
- a live-execution gate that blocks any real mailbox request unless:
  - the workflow set is source-traceable to real bounded-context needs
  - current ODS ownership / managing-party posture is known
  - it is clear whether Path to Live testing can proceed without a mailbox
  - named approver and environment target are present
  - `ALLOW_REAL_PROVIDER_MUTATION=true` is set
  - `ALLOW_SPEND=true` is set if the path could trigger commercial or managed-service cost

Execution steps:
1. Start from tasks `021-027`.
2. Build the workflow-ID registry and message-route matrix first.
3. Implement the mock MESH twin second.
4. Build the mailroom console third.
5. Drive both with `Playwright_or_other_appropriate_tooling` while developing.
6. Generate the live-later mailbox and workflow request pack from the same data model.
7. Validate that every route distinguishes workflow acceptance from authoritative downstream truth.

Mandatory gap closures you must perform:
- Close the “message sent means workflow complete” gap by explicitly separating transport acceptance, downstream proof, and canonical settlement.
- Close the “workflow IDs are just strings on the side” gap by making them first-class registry objects.
- Close the “test mailbox access is always required” gap by modeling Path to Live and local sandbox options separately.
- Close the “third-party mailbox management is an operational footnote” gap by making mailbox manager posture explicit.
- Close the “mock mailroom can ignore quarantine, expiry, or replay” gap by requiring those states up front.

Non-negotiable rules:
- do not let MESH acceptance imply patient-facing calmness or canonical closure
- do not store real mailbox credentials in repo fixtures
- do not submit a real mailbox application unless the live gate passes
- do not create or request workflow IDs without a traced business-flow need
- do not let the mock flatten duplicate, expired, failed, and quarantined states into one generic error

Mandatory UI and interaction specification for `apps/mock-mesh-mailroom`:
This must feel like a premium operational mailroom, not a generic table admin.

Name the visual mode `Signal_Post_Room`.

Brand and visual posture:
- include a subtle inline Vecells mark plus a clear `MOCK_MESH_MAILROOM` ribbon
- do not imitate any real NHS UI directly
- tone: industrial-precise, calm, traceable, low-noise
- base palette:
  - canvas `#F6F7F9`
  - panel `#FFFFFF`
  - inset `#EEF1F4`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#155EEF`
  - secondary accent `#0E9384`
  - delivery accent `#7A5AF8`
  - caution accent `#B54708`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for mailbox IDs, workflow IDs, message IDs, and hashes

Layout:
- top operational header with environment, mailbox count, workflow health, and degraded alerts
- left mailbox rail
- center message timeline and envelope workbench
- right workflow / proof inspector
- lower strip with a restrained line diagram of `compose -> submit -> accepted -> picked_up -> proof_pending -> settled_or_recovered`

Dimensional constraints:
- page max width `1440px`
- mailbox rail `280px`
- proof inspector `360px`
- message cards `min-height 140px`
- buttons `44px`
- chip height `28px`
- sticky header `72px`

Unique page set:
1. `Mailroom_Overview`
2. `Workflow_Registry`
3. `Message_Envelope_Timeline`
4. `Proof_and_Replay_Inspector`
5. `Mailbox_Application_Pack`

Motion:
- `120ms` hover and focus
- `180ms` timeline transitions
- `240ms` inspector drawer transitions
- reduced-motion support mandatory

Interaction requirements:
- workflow filters and state chips
- detailed replay and expiry explanations
- no silent transitions from accepted to settled
- keyboard-first navigation
- deterministic `data-testid` markers for mailbox rail, workflow table, message timeline, proof inspector, and environment switcher

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and testing from the start. At minimum cover:
- mailbox switching
- workflow validation
- message submission and delayed acknowledgement
- replay, duplicate, expiry, and quarantine states
- application-pack validation
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- one high-fidelity local MESH twin exists and is demonstrably useful now
- one later mailbox / workflow strategy exists with hard live gates
- the outputs preserve external-message proof law and do not confuse transport acceptance with canonical truth
- later mailbox requests can proceed from the field map and registry without re-deriving workflow semantics from scratch
```
