# Prompt 087 For 087_par_phase0_track_runtime_provision_event_bus_queueing_and_outbox_inbox_infrastructure

Use the shared operating contract first:
[shared_operating_contract_086_to_095.md](/Users/test/Code/V/prompt/shared_operating_contract_086_to_095.md)

```text
Execute roadmap task 087_par_phase0_track_runtime_provision_event_bus_queueing_and_outbox_inbox_infrastructure.md.

Inherit the Shared_operating_contract_for_prompts_086_to_095 and consume the validated outputs from tasks `001-086`.

Mission:
Provision the canonical event spine, queueing substrate, and durable outbox/inbox infrastructure for Vecells.

This task must turn the Phase 0 event model into concrete runtime infrastructure:
- broker topics or streams aligned to the canonical event namespace
- queue groups, retry paths, DLQ or quarantine paths, and subscription manifests
- durable outbox checkpoints for command-side event emission and integration effects
- durable inbox checkpoints and dedupe semantics for consumers and callbacks
- machine-readable topology, ordering, and retry policy contracts

This is real runtime and backend foundation work. Produce infrastructure, libraries, schemas, validation, and smoke tests. Do not leave event routing, queue naming, outbox behavior, or callback correlation as conventions hidden inside handlers.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - the canonical event namespace and event catalogue
  - the event-envelope requirements carrying `tenantId`, `eventName`, `schemaVersionRef`, `sourceBoundedContextRef`, `governingBoundedContextRef`, `edgeCorrelationId`, and `causalToken`
  - the rules that canonical events and continuity evidence are not optional side effects
- `platform-runtime-and-release-blueprint.md`, especially:
  - `## Runtime topology contract`
  - `### DependencyDegradationProfile`
  - adapter rules around durable outbox checkpoints, correlation policy, and exact settlement chains
  - any sections where browser-facing runtime may not directly become integration or event infrastructure
- outputs from tasks `048`, `057`, `062`, `063`, `064`, `065`, `066`, `067`, `082`, `084`, and `086`
- `forensic-audit-findings.md`, especially Findings `61`, `62`, `63`, `66`, `67`, `68`, `81`, `82`, `83`, and `84`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must publish extension points for later notification, projection, release, and observability tracks without stealing their ownership. If later tasks own policy depth or domain-specific subscribers, provide the event-spine seam now and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build a realistic local and non-production event spine now.
- provide a local or sandbox-safe broker baseline with topics, subscriptions, retries, and DLQ behavior
- keep the same canonical event names, envelope fields, idempotency strategy, ordering semantics, and callback-correlation posture the live system will require
- allow simulator-backed phone, notification, identity, and GP or pharmacy adapter flows to publish and consume through the same spine without live providers
The mock-now path must be realistic enough that production broker cutover is a managed-service swap, not a message-contract rewrite.

Actual_production_strategy_later:
When production broker services are finalized, they must preserve the same namespace law, outbox and inbox semantics, retry posture, dead-letter policy, checkpoint records, and correlation chain. Production hardening may add partitioning, higher throughput, managed tracing, or disaster-tolerant replication, but it must not rename canonical events, flatten queue classes, or bypass the outbox and inbox contracts.

Primary outcome:
The repository gains a concrete, deterministic, and auditable event spine that later runtime, adapter, projection, notification, and release tracks can build on without improvising transport semantics.

Implementation deliverables to create:
1. infrastructure-as-code for the message broker, queue groups, subscriptions, DLQ or quarantine paths, and runtime access controls
2. machine-readable broker topology aligned to the canonical event namespace and bounded-context ownership
3. a reusable outbox library or service component with durable checkpoints, effect keys, ordering support, and replay-safe dispatch
4. a reusable inbox library or service component with dedupe, checkpointing, ordering windows where required, and callback-correlation support
5. queue retry, DLQ, and replay policies bound to declared consumer classes and degradation profiles
6. local-development and CI broker bootstrap scripts plus deterministic reset flows
7. `docs/architecture/87_event_spine_and_queueing_design.md`
8. `docs/architecture/87_outbox_inbox_ordering_and_correlation_rules.md`
9. `docs/architecture/87_event_spine_topology_atlas.html`
10. `data/analysis/event_broker_topology_manifest.json`
11. `data/analysis/outbox_inbox_policy_matrix.csv`
12. `data/analysis/canonical_event_to_transport_mapping.json`
13. `tools/analysis/validate_event_spine_and_queueing.(py|ts)`
14. infrastructure tests, queueing tests, ordering tests, replay tests, and failure-injection smoke tests
15. `tests/playwright/event-spine-topology-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- provision broker topics or streams that map from the canonical event namespace without inventing shadow names
- publish deterministic queue and subscriber manifests for command, projection, integration, notification, assurance, and replay consumers
- implement durable outbox emission bound to command settlement and effect keys
- implement durable inbox processing with dedupe and explicit receipt checkpoints
- support retry, delay, DLQ or quarantine, and replay behavior without losing canonical event identity
- bind callback or webhook consumers to explicit correlation and receipt-ordering rules rather than handler-local guesswork
- keep browsers and generic BFF routes away from direct broker publication or subscription

Execution steps:
1. Re-read the canonical event namespace, event catalogue, and callback-correlation rules, then convert them into broker topology and message-envelope requirements.
2. Honor the network, data-plane, and event-contract baselines from earlier tasks; if required outputs are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
3. Provision the broker topology, access controls, queue classes, and retry or DLQ rules from code.
4. Implement the outbox component so command-side services persist, checkpoint, and dispatch canonical events and partner effects deterministically.
5. Implement the inbox component so consumers record receipt, dedupe, ordering state where required, and settlement linkage.
6. Publish the transport mapping manifest from canonical event contract to broker topic, queue class, and consumer group.
7. Build the event-spine atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Write smoke tests proving canonical quarantine, degraded-receipt, fallback-review, reachability, confirmation-gate, and closure-blocker events can flow through the spine without losing ordering or traceability.

Mandatory gap closures you must perform:
- Close the “canonical events exist only as a document list” gap by binding them to real broker and queue topology.
- Close the “outbox and inbox are implicit handler code” gap by implementing them as reusable, durable primitives.
- Close the “callback correlation is ad hoc” gap by publishing correlation and receipt-ordering policy contracts.
- Close the “DLQ or quarantine hides safety-relevant failures” gap by preserving canonical event identity and replay paths.
- Close the “degraded receipt and fallback review are invisible to event consumers” gap by ensuring those canonical events have first-class transport routes.
- Close the “local and cloud event semantics drift” gap by using the same manifests and checkpoints in local and non-production environments.

Industry-best-practice requirements:
- durable queueing and idempotent consumers
- separation of broker topology from business handlers
- explicit retry and DLQ policy under version control
- checkpointed outbox and inbox with deterministic replay
- least-privilege broker access per workload family
- fail-closed behavior when canonical event or schema contracts are absent
- no browser or loose cron job as a substitute event publisher

Non-negotiable rules:
- Canonical event names remain canonical; transport-specific aliases are forbidden.
- Outbox and inbox checkpoints are required for authoritative event or effect processing.
- DLQ or quarantine may not erase correlation to `edgeCorrelationId`, `causalToken`, effect key, or command settlement.
- Generic gateway surfaces may not publish directly to the broker.
- Replay must preserve canonical identity and ordering semantics where declared.

Mandatory UI and interaction specification for `87_event_spine_topology_atlas.html`:
Name the surface mode `Event_Spine_Topology_Atlas`.

Tone:
- premium distributed-systems atlas
- minimalist, crisp, and operationally confident
- should feel like a high-end message-flow observatory, not a generic broker admin panel

Branding:
- restrained Vecells wordmark plus inline SVG `EV` monogram
- one namespace-to-topic river diagram
- one queue and retry topology chart
- one outbox/inbox correlation trace strip

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- event accent `#2563EB`
- queue accent `#0EA5A4`
- replay accent `#7C3AED`
- warning accent `#D97706`
- DLQ accent `#C24141`
- verified accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for event names, queue IDs, checkpoint IDs, and effect keys

Layout:
- sticky masthead with active broker env, queued retries, DLQ count, and checkpoint lag alerts
- left rail (`324px`) with namespace filters, consumer-class filters, queue-state filters, and correlation filters
- central canvas with:
  - namespace-to-topic river diagram
  - queue / retry topology chart
  - outbox / inbox correlation trace strip
- right inspector (`420px`) for selected event contract, queue group, or checkpoint policy
- lower grid with one topology table and one checkpoint / policy table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by namespace, queue class, retry posture, consumer group, and event state
- selecting an event or queue row must synchronize river diagram, topology chart, trace strip, inspector, and tables
- keyboard navigation across filters and tables
- deterministic `data-testid` markers for river diagram, queue chart, trace strip, topology table, checkpoint table, and inspector
- every chart must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification
- verification that DLQ or quarantine routes remain visibly distinct from ordinary delivery

Definition of done:
- The repository has a real, deterministic event spine and queueing substrate.
- Canonical events map to explicit broker topology.
- Outbox and inbox behavior are reusable, durable, and testable.
- Later projection, notification, partner-adapter, and release tracks can consume the same spine without redefining transport semantics.
```
