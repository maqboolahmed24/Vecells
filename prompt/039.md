# Prompt 039 For 039_seq_external_document_manual_approval_checkpoints_and_browser_automation_retries

Use the shared operating contract first:
[shared_operating_contract_036_to_045.md](/Users/test/Code/V/prompt/shared_operating_contract_036_to_045.md)

```text
Execute roadmap task 039_seq_external_document_manual_approval_checkpoints_and_browser_automation_retries.md.

Inherit the Shared_operating_contract_for_prompts_036_to_045 and consume the validated outputs from tasks `001-038`.

Mission:
Create the authoritative manual-checkpoint and browser-automation retry policy for external-provider and onboarding work in two explicit parts:

1. a now-executable internal control model that rehearses manual approvals, resume points, and safe retry behavior for mock and dry-run work
2. a later live-provider control strategy that prevents dangerous blind retries, duplicate submissions, or secret leakage when real portals and accounts are involved

This task must convert “someone will approve it manually” and “just rerun the automation” into one explicit, governed operating model.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - submit / replay idempotency expectations
  - immutable evidence and authoritative settlement patterns
  - `RequestLifecycleLease` and other ownership / fence concepts as design analogies for checkpoint ownership
  - any rule that forbids over-inferring success from partial evidence
- `platform-runtime-and-release-blueprint.md`, especially:
  - `ReleaseGateEvidence`
  - `BuildProvenanceRecord`
  - `WaveActionRecord`
  - verification-ladder, operational-readiness, and recovery-control posture
- `phase-2-the-identity-and-echoes.md`, `phase-4-the-booking-engine.md`, `phase-6-the-pharmacy-loop.md`, and `phase-7-inside-the-nhs-app.md` where external dependencies, release gates, or deferred-channel mechanics create manual checkpoints or ambiguity
- `forensic-audit-findings.md`, especially findings on degraded receipt, fallback review, confirmation ambiguity, missing proof objects, route/settlement/release/trust control hardening, and continuity-safe recovery
- outputs from tasks `021-038`, especially mock/live split tasks, provider-path packs, simulator backlog, and sandbox-account strategy

Primary outcome:
A shared checkpoint and retry framework that every external-provider and onboarding automation path can reuse, with explicit classifications for safe retries, resume-only steps, human-only approvals, and never-automate actions.

Deliverables to create:
1. `docs/external/39_manual_approval_checkpoint_register.md`
2. `docs/external/39_browser_automation_retry_policy.md`
3. `docs/external/39_live_mutation_gate_policy.md`
4. `docs/external/39_provider_portal_control_tower.html`
5. `data/analysis/manual_approval_checkpoints.csv`
6. `data/analysis/browser_automation_retry_matrix.json`
7. `data/analysis/provider_portal_action_idempotency_rules.csv`
8. `data/analysis/live_mutation_gate_rules.json`
9. `tools/analysis/validate_manual_checkpoint_and_retry_policy.(py|ts)`
10. `tests/playwright/provider-portal-control-tower.spec.(ts|js)`
11. `tools/browser-automation/shared/provider_action_guard.(ts|js)`
12. `tools/browser-automation/shared/provider_checkpoint_model.(ts|js)`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build the internal checkpoint and retry model for mock portals, mock onboarding twins, and dry-run browser automation. At minimum, classify actions into:
- `safe_read_retry`
- `resume_from_checkpoint_only`
- `human_review_before_continue`
- `capture_evidence_then_stop`
- `never_auto_repeat`
- `secrets_redacted_only`

For each class, define:
- what evidence proves step completion
- what evidence is insufficient
- whether the step is idempotent
- maximum automatic retry count
- backoff posture
- screenshot / trace policy
- whether a human must confirm before next mutation
- whether a later live run may use the same rule

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the later live-provider checkpoint and retry strategy. It must include:
- one checkpoint register across all current provider/onboarding tasks
- a classification of human-only approvals such as legal / commercial, sponsor, commissioner, environment approval, signatory, or vendor-service-desk steps
- a browser-automation retry matrix that forbids blind re-submission for non-idempotent mutations
- a live-mutation gate that blocks real execution unless named approver, environment target, evidence freshness, and explicit live flag are all present
- a shared action-guard library that later live scripts must call before sensitive actions

Execution steps:
1. Start from tasks `021-038`.
2. Inventory every external or onboarding step that can pause, block, or partially succeed.
3. Define the canonical checkpoint classes and retry classes.
4. Build the machine-readable register and matrix first.
5. Create the shared action-guard and checkpoint-model helper.
6. Build the HTML control tower and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Validate that every non-idempotent action is either human-gated or explicitly classified as never-auto-repeat.
8. Validate that no live-mutation path can run without the live gate.

Mandatory gap closures you must perform:
- Close the “retry equals rerun” gap by distinguishing read-only retry, safe resume, and never-repeat mutation classes.
- Close the “manual approval exists somewhere outside the system” gap by building one explicit checkpoint register.
- Close the “evidence captured after the fact” gap by requiring completion evidence per step class.
- Close the “screenshots and traces may leak secrets” gap by defining secret-safe capture policy per action class.
- Close the “mock control model and live control model drift apart” gap by sharing one checkpoint vocabulary.
- Close the “partial success” gap by forcing ambiguous portal states into explicit stop-and-review posture.

Non-negotiable rules:
- Do not allow browser automation to blind-submit a form twice if the action is not provably idempotent.
- Do not classify destructive, legal, signatory, or credential-revealing actions as safe auto-retry.
- Do not let screenshots, traces, HAR files, or logs capture secrets unless redaction is proven and approved.
- Do not let completion be inferred from button-click success alone; completion must map to evidence.

Mandatory UI and interaction specification for `39_provider_portal_control_tower.html`:
This is a premium internal control-and-reliability surface. Name the visual mode `Provider_Control_Tower`.

Brand and posture:
- use a restrained Vecells wordmark plus a compact `CTRL_TOWER` monogram in inline SVG
- tone: operational, reliability-focused, exact, visibly risk-aware
- base palette:
  - canvas `#F7F8FC`
  - rail `#EEF2F7`
  - panel `#FFFFFF`
  - inset `#F3F5FA`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#2563EB`
  - review accent `#7C3AED`
  - safe accent `#0F9D58`
  - warning accent `#C98900`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for checkpoint IDs, action keys, and retry classes

Layout:
- sticky masthead:
  - checkpoint count
  - human-review count
  - never-repeat count
  - unresolved live blockers count
- left rail (`296px`): provider families and action classes
- center workspace:
  - checkpoint register table
  - retry-matrix grid
  - one restrained ladder diagram showing `Read -> Fill -> Review -> Commit -> Verify`
- right inspector (`360px`): selected action detail, evidence rules, redaction policy, retry policy
- lower section:
  - table parity for diagrams
  - live-gate summary strip

Dimensional constraints:
- max width `1440px`
- register row height `52px`
- action cards `min-height 96px`
- primary controls `44px`
- sticky header height `72px`

Motion:
- `120ms` hover/focus
- `180ms` table/filter transition
- `240ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by provider family, action class, and live-gate status
- visible chips for `safe_read_retry`, `resume_only`, `human_review`, `never_auto_repeat`
- sortable idempotency and evidence columns
- keyboard-first navigation
- deterministic `data-testid` markers for rails, register, retry matrix, inspector, and live-gate strip

Unique design notes:
- one compact risk ladder is preferred over multiple decorative charts
- never use generic alert-wall styling; this should feel like a calm control tower
- evidence class and retry class must be visually distinct

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filtering
- sorting
- action selection
- inspector rendering
- chip rendering
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has one explicit checkpoint and retry framework for provider/onboarding automation.
- Every action class has evidence, retry, redaction, and human-approval rules.
- Mock-now and actual-later provider work share the same control vocabulary and fail-closed live gate.
```
