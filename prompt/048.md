# Prompt 048 For 048_seq_phase0_define_canonical_event_namespace_and_schema_registry_process

Use the shared operating contract first:
[shared_operating_contract_046_to_055.md](/Users/test/Code/V/prompt/shared_operating_contract_046_to_055.md)

```text
Execute roadmap task 048_seq_phase0_define_canonical_event_namespace_and_schema_registry_process.md.

Inherit the Shared_operating_contract_for_prompts_046_to_055 and consume the validated outputs from tasks `001-047`.

Mission:
Define the canonical event namespace, schema registry, normalization pipeline, and evolution process for Vecells so every domain event, control-plane event, recovery event, continuity event, observability event, and audit event is published through one governed event authority.

This task must replace ad hoc event naming, code-local schema drift, and downstream alias handling with a deterministic event-contract system.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`
  - the canonical event taxonomy block beginning `At this stage, also define the canonical event taxonomy as a published cross-phase contract`
  - `CanonicalEventNamespace`
  - `CanonicalEventContract`
  - `CanonicalEventEnvelope`
  - `CanonicalEventNormalizationRule`
  - the published minimum required canonical event families
  - the mandatory rules on privacy posture, artifact refs, alias normalization, replay semantics, and version evolution
- `forensic-audit-findings.md`
  - finding `40` on external outcomes needing adapter events rather than UI actions
  - finding `43` on object-store artifacts entering the event spine
  - findings `61-68` on missing quarantine, degraded receipt, fallback-review, identity-repair, duplicate-review, reachability, confirmation-gate, and closure-blocker events
- `platform-runtime-and-release-blueprint.md`
  - publication and release-verification rules that require active canonical event contracts to be published as runtime truth
- outputs from tasks `006`, `007`, `040`, `044`, `046`, and `047`

Primary outcome:
One machine-readable event registry containing:
- canonical namespaces
- event contracts
- schema versions
- normalization rules
- compatibility rules
- publication/evolution process
- validator coverage proving downstream consumers cannot rely on local event aliases or hidden schema drift

Deliverables to create:
1. `docs/architecture/48_event_namespace_strategy.md`
2. `docs/architecture/48_event_schema_registry_process.md`
3. `docs/architecture/48_event_contract_catalog.md`
4. `docs/architecture/48_event_registry_studio.html`
5. `data/analysis/canonical_event_namespaces.json`
6. `data/analysis/canonical_event_contracts.json`
7. `data/analysis/canonical_event_normalization_rules.json`
8. `data/analysis/canonical_event_schema_versions.json`
9. `data/analysis/canonical_event_family_matrix.csv`
10. `packages/event-contracts/schemas/` populated with machine-readable schema artifacts appropriate to the chosen stack
11. `tools/analysis/validate_event_registry.(py|ts)`
12. `tests/playwright/event-registry-studio.spec.(ts|js)`

Your registry must capture at minimum for each namespace:
- `canonicalEventNamespaceId`
- `namespaceCode`
- `owningBoundedContextRef`
- `eventPurposeClass`
- `allowedProducerContextRefs[]`
- `defaultDisclosureClass`
- `namespaceState`
- `publishedAt`
- source refs and rationale

For each event contract capture at minimum:
- `canonicalEventContractId`
- `eventName`
- `namespaceRef`
- `owningBoundedContextRef`
- `governingObjectType`
- `eventPurpose`
- `requiredIdentifierRefs[]`
- `requiredCausalityRefs[]`
- `requiredPrivacyRefs[]`
- `requiredPayloadRefs[]`
- `legacyAliasRefs[]`
- `schemaVersionRef`
- `compatibilityMode`
- `replaySemantics`
- `contractState`
- `publishedAt`
- source refs and rationale

For each normalization rule capture at minimum:
- source producer / namespace pattern / event pattern
- target canonical contract ref
- normalization version
- payload rewrite policy
- privacy rewrite policy
- rule state
- published timestamp

Execution steps:
1. Start from the Phase 0 canonical event taxonomy and emit namespace rows for every published family:
   - `request`
   - `intake`
   - `identity`
   - `access`
   - `telephony`
   - `safety`
   - `triage`
   - `booking`
   - `hub`
   - `pharmacy`
   - `patient`
   - `communication`
   - `reachability`
   - `exception`
   - `confirmation`
   - `capacity`
   - `support`
   - `assistive`
   - `policy`
   - `release`
   - `analytics`
   - `audit`
2. Emit contract rows for the minimum required event families named in Phase 0. Do not stop at headings; create explicit contracts for the named events.
3. Add the missing families hardened by the forensic audit:
   - `intake.attachment.quarantined`
   - `patient.receipt.degraded`
   - `exception.review_case.opened`
   - `exception.review_case.recovered`
   - `identity.repair_case.opened`
   - `identity.repair_case.corrected`
   - `identity.repair_case.closed`
   - `request.duplicate.review_required`
   - `request.duplicate.resolved`
   - `reachability.dependency.created`
   - `reachability.dependency.failed`
   - `reachability.dependency.repaired`
   - `confirmation.gate.created`
   - `confirmation.gate.confirmed`
   - `confirmation.gate.disputed`
   - `request.closure_blockers.changed`
4. Define the canonical event envelope schema and the schema-version process:
   - which changes are additive
   - when new versions are required
   - when a namespace break is required
   - how replay safety is proved
5. Define the normalization process for all known non-canonical producers, including at minimum:
   - `ingest.* -> intake.*`
   - `tasks.* -> triage.task.*`
   - `fallback.review_case.* -> exception.review_case.*`
   - `external.confirmation.gate.* -> confirmation.gate.*`
6. Publish machine-readable schema artifacts for the active contracts. The exact schema technology may follow the chosen stack, but it must be deterministic, diffable, and validator-friendly.
7. Build the studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that:
   - every active event contract belongs to exactly one namespace
   - every active namespace has an owning bounded context
   - every event named in the Phase 0 minimum family list exists as a registry row
   - every forensic event gap is now represented or explicitly bounded as not applicable
   - every schema artifact is linked to one contract version
   - no local producer alias is treated as downstream authority

Mandatory gap closures you must perform:
- Close the “event catalogue is a loose bullet list” gap by producing a real registry.
- Close the “producer-local names can leak to projections, analytics, assurance, or audit” gap by publishing normalization rules.
- Close the “degraded, recovery, and blocker transitions are under-modeled” gap by elevating them into first-class event contracts.
- Close the “PHI can leak through raw payload convenience” gap by codifying artifact-reference and privacy-posture rules in the event envelope.
- Close the “schema evolution is governed by deploy order rather than contract semantics” gap.
- Close the “external adapter callbacks look synchronous or UI-local” gap by forcing them onto the canonical event spine.

Industry-best-practice requirements:
- deterministic schema generation
- explicit versioning and compatibility policy
- one authoritative event envelope
- alias normalization before downstream consumption
- CI-verifiable registry completeness
- replay-safe contract evolution
- PHI-safe envelope and payload discipline
- clear ownership and producer allowlists

Non-negotiable rules:
- Every state transition, blocker mutation, continuity-evidence change, degraded or recovery transition, and control-plane decision that affects projections, analytics, assurance, replay, or audit must emit a canonical event envelope.
- Producers may not deliver local or vendor event names directly to downstream consumers.
- Raw PHI, phone numbers, message bodies, transcripts, and artifact contents must travel by governed artifact reference or masked descriptor only.
- If the same business fact is emitted for replay, analytics, and assurance, they must share one canonical event contract.
- `compatibilityMode = namespace_break` is the only legal way to retire a family in place; otherwise evolve by additive schema or a new contract version with replay proof.

Mandatory UI and interaction specification for `48_event_registry_studio.html`:
Name the surface mode `Event_Registry_Studio`.

Tone:
- minimalist premium internal registry
- exact, inspectable, contract-first
- must feel like a product-grade schema authority surface, not a generic admin CRUD table

Branding:
- restrained Vecells wordmark plus a compact inline SVG `EVENT` monogram
- show one subtle event-spine line motif rather than decorative icons

Palette:
- canvas `#F7F9FC`
- rail `#EEF2F8`
- panel `#FFFFFF`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- namespace accent `#3559E6`
- normalization accent `#0EA5A4`
- compatibility accent `#6E59D9`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for event names, schema versions, compatibility codes, and hashes

Layout:
- sticky masthead with:
  - namespace count
  - active contract count
  - missing-contract count
  - blocked-schema count
- left rail (`280px`):
  - namespace list
  - purpose-class filters
- center region:
  - event contract table with expandable rows
  - one compact canonicalization diagram showing producer alias -> canonical event
- right inspector (`388px`):
  - selected event metadata
  - required identifiers / causality / privacy fields
  - legacy aliases
  - schema version history
- lower region:
  - schema diff ledger
  - normalization-rule table
  - registry defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- main table min height `520px`
- primary controls `44px`
- filter chips `28px`

Motion:
- `120ms` hover/focus
- `160ms` row expand
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by namespace, owning context, compatibility mode, replay semantics, and defect state
- keyboard navigation across namespace rail, contract rows, schema-diff rows, and normalization rules
- stable `data-testid` markers for namespace rail, contract table, diff ledger, inspector, and defect strip
- diagram/table parity mandatory; no unique meaning may live only in the diagram

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- namespace filtering
- event-row expansion
- inspector rendering
- normalization-rule visibility
- schema-version diff visibility
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks

Definition of done:
- Vecells has one canonical event namespace and schema-registry strategy.
- Every minimum required Phase 0 event family exists as a machine-readable contract.
- Forensic event gaps are closed.
- Later implementation and publication tasks can generate event types, validators, and projections from this registry rather than re-inventing event authority in code.
```