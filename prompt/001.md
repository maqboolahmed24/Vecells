# Prompt 001 For 001_seq_ingest_blueprint_corpus_and_establish_traceable_requirement_registry

Use the shared operating contract first:
[shared_operating_contract_001_to_005.md](/Users/test/Code/V/prompt/shared_operating_contract_001_to_005.md)

```text
Execute roadmap task 001_seq_ingest_blueprint_corpus_and_establish_traceable_requirement_registry.md.

Inherit the Shared_operating_contract_for_prompts_001_to_005.

Mission:
Ingest the full Vecells blueprint corpus and create the first authoritative, traceable requirement registry for the entire programme. This registry must become the machine-readable backbone for every later roadmap task.

Algorithm grounding you must honor:
- blueprint-init.md says Vecells is a FHIR-native primary care access and operations layer, not a thin appointment-booking front end.
- blueprint-init.md states that web, NHS App jump-off, and phone all converge into one governed request pipeline.
- blueprint-init.md makes SubmissionEnvelope -> governed promotion -> Request/RequestLineage the core control seam.
- phase-cards.md says programme summaries must reconcile through one CrossPhaseConformanceScorecard and must not drift from canonical blueprints.
- vecells-complete-end-to-end-flow.md names the audited baseline invariants: durable SubmissionEnvelope shells, immutable EvidenceSnapshot, RequestLineage + LineageCaseLink, duplicate clustering, fallback-review lanes, route-intent binding, command settlement, release freezes, assurance slice trust, and LifecycleCoordinator-gated closure.
- forensic-audit-findings.md Findings 01-04 explicitly patch missing durable lineage shell, missing submitted barrier, missing immutable snapshot before normalization, and missing idempotency envelope.
- phase-0-the-foundation-protocol.md contains the authoritative object and state model under 0A, 0B, and “Canonical lifecycle, identity, safety, reservation, access, and reconciliation algorithm”.

Deliverables to create:
1. docs/analysis/01_blueprint_source_manifest.md
2. docs/analysis/01_requirement_registry_overview.md
3. docs/analysis/01_requirement_taxonomy.md
4. docs/analysis/01_derived_gap_register.md
5. data/analysis/requirement_registry.jsonl
6. data/analysis/requirement_registry.csv
7. data/analysis/source_manifest.json
8. data/analysis/source_precedence_policy.json
9. tools/analysis/build_requirement_registry.(py|ts)
10. tools/analysis/validate_requirement_registry.(py|ts)

What the registry must capture per requirement row:
- stable requirement_id
- requirement_title
- requirement_type: functional | domain_object | invariant | state_machine | workflow | frontend | accessibility | content | backend | integration | security | privacy | analytics | runtime_release | test | assurance
- canonicality: canonical | supporting | derived_from_canonical_gap_closure
- source_file
- source_heading_or_logical_block
- direct_quote_or_precise_paraphrase
- rationale
- affected_phases
- affected_domains
- actors/personas
- audience_tiers
- channels
- primary_objects
- preconditions
- trigger
- expected_behavior
- failure_or_degraded_behavior
- closure_blockers_or_guards
- external_dependencies
- FHIR_or_representation_implications
- security/privacy implications
- observability implications
- acceptance_test_hint
- related_requirement_ids
- status: extracted | normalized | conflict_flagged | gap_resolved
- notes

Execution steps:
1. Recursively inventory every file in blueprint/. Separate the corpus into:
   - programme summary docs,
   - phase docs,
   - cross-cutting frontend/runtime/admin/support docs,
   - audit/diagnostic docs,
   - flow/diagram sources.
2. Build a source manifest that records for each file:
   - file role,
   - authority level,
   - primary concerns,
   - dependent tasks likely to consume it,
   - whether it defines canonical objects, UI rules, runtime rules, or defect corrections.
3. Establish and serialize the source precedence policy from the blueprint’s own wording. Make conflict rules explicit; do not leave precedence implicit.
4. Parse requirements from all docs, not only headings. Mine:
   - prose statements,
   - enumerated control priorities,
   - object definitions,
   - state definitions,
   - transition rules,
   - prohibition rules,
   - “must pass” test lists,
   - audit findings and patch responses,
   - mermaid node semantics from vecells-complete-end-to-end-flow.mmd or the audited flow markdown.
5. Normalize synonymous language into canonical terms without losing traceability. Examples:
   - request lineage continuity vs same request lineage shell,
   - degraded receipt vs fallback review initiation,
   - patient action recovery vs same-shell recovery posture,
   - booking ambiguity vs external confirmation gate,
   - duplicate merge language vs DuplicateCluster review.
6. Build one requirement taxonomy that separates:
   - business capability requirements,
   - control-plane requirements,
   - runtime/release requirements,
   - UI/continuity requirements,
   - safety/identity/privacy requirements,
   - audit/assurance requirements.
7. Explicitly derive missing-but-required requirements from the forensic audit where the blueprint has already identified a defect and a patch direction. Derived requirements must be tagged as derived_from_canonical_gap_closure, never mistaken for original prose.
8. Create relationship links between requirements so downstream tasks can answer:
   - which requirement depends on which invariant,
   - which requirement is phase-local vs cross-phase,
   - which requirement is tied to an external dependency,
   - which requirement needs runtime support,
   - which requirement needs frontend continuity evidence.
9. Add validation logic that fails when:
   - a requirement row has no source reference,
   - a canonical object appears in later docs but not in the registry,
   - a phase doc contains mandatory tests but the registry has no test requirements,
   - a forensic finding with implementation impact has not been represented as a requirement or an explicit exclusion.

Gap handling you must perform during this task:
- Close the missing “single registry” gap: the corpus currently spreads requirements across many documents.
- Close the “implied invariant is not machine-readable” gap: elevate implied invariants into traceable rows.
- Close the “summary prose is too broad” gap by converting vague prose into testable requirement language.
- Detect any object or state named in vecells-complete-end-to-end-flow.md that is absent from the registry and emit GAP_* rows.
- Detect any canonical object in phase-0-the-foundation-protocol.md that lacks at least one registry row and fail validation.

Mandatory edge cases to capture as requirements even if scattered across files:
- accepted retry returning prior accepted result
- duplicate cluster review instead of silent merge
- wrong-patient correction via IdentityRepairCase
- material-delta re-safety
- urgent_diversion_required vs urgent_diverted separation
- fallback review when accepted progress later degrades
- route-intent binding + command settlement for post-submit mutation
- release freeze / channel freeze / assurance slice trust before writable posture
- visibility projection policy before projections materialize
- child-case ambiguity that must not prematurely close the request

Definition of done:
- Every markdown and mermaid source file is represented in the source manifest.
- The requirement registry is machine-readable, deterministic, and traceable.
- The registry contains no orphaned canonical objects from Phase 0.
- High-priority forensic findings are all represented as requirements, gap closures, or explicit non-applicability decisions.
- A later autonomous agent could use this registry without rereading the whole corpus to find the requirement set for any phase, persona, channel, or object.
```
