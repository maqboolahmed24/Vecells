# Prompt 034 For 034_seq_external_select_transcription_and_artifact_scanning_providers

Use the shared operating contract first:
[shared_operating_contract_026_to_035.md](/Users/test/Code/V/prompt/shared_operating_contract_026_to_035.md)

```text
Execute roadmap task 034_seq_external_select_transcription_and_artifact_scanning_providers.md.

Inherit the Shared_operating_contract_for_prompts_026_to_035 and consume the validated outputs from tasks `001-033`, especially tasks `008`, `021`, `022`, `023`, `027`, `031`, and the audio / artifact contracts in the blueprint corpus.

Mission:
Create the provider-selection dossier for transcription and artifact scanning in a way that respects the blueprint’s evidence, quarantine, and safety law:
- transcript readiness and artifact-scan results are evidence-processing contracts, not generic utility jobs
- quality bands, coverage classes, partial results, failures, and quarantine states matter to clinical safety and workflow gating
- mock-first execution remains mandatory because real providers should not block MVP buildout
- real-provider market research must still happen now with current official docs so later acquisition is deliberate

This task therefore has two equally important jobs:
1. define the mock-now evidence-processing lane and local simulator baseline for transcription and artifact scanning
2. create the real-provider-later shortlist and due-diligence pack using current official vendor documentation

Algorithm grounding you must honor:
- `phase-2-the-identity-and-echoes.md`, especially:
  - `2F. Caller verification, voice capture, transcript stub, and SMS continuation`
  - transcript readiness and evidence usability states
- `phase-8-the-assistive-layer.md`, especially:
  - `8C. Audio, transcript, and artifact normalization pipeline`
- `phase-0-the-foundation-protocol.md`, especially:
  - artifact quarantine
  - evidence assimilation
  - safety preemption and fallback-review law
- `platform-runtime-and-release-blueprint.md`, especially adapter and degradation profiles
- `forensic-audit-findings.md`, especially:
  - `Finding 09 - Upload and audio quarantine rules were absent`
  - `Finding 61 - The event catalogue lacked attachment-quarantine events`
  - `Finding 81 - The standalone Mermaid source lacked an explicit quarantine path for unsafe or unreadable evidence`

Current commercial-provider mechanics you must respect in the `Actual_provider_strategy_later` section:
Provider facts are temporally unstable. During execution you must browse current official vendor docs and trust / pricing / security pages. Do not rely on stale memory. At minimum evaluate each shortlisted candidate for:
- async and streaming transcription modes where relevant
- partial transcript semantics and confidence scores
- data retention / deletion controls
- webhook authenticity and replay protection
- region / residency controls
- malware scanning depth, file-type coverage, update cadence, and quarantine hooks
- sandbox or test posture
- API key and project model
- audit and export capabilities
- cost governance and portability

Primary outcome:
One source-traceable transcription and artifact-scanning vendor dossier that clearly separates mock-now execution from real later vendor choice and preserves evidence-readiness, quarantine, and safety semantics.

Deliverables to create:
1. `docs/external/34_vendor_universe_transcription_and_scanning.md`
2. `docs/external/34_mock_provider_lane_for_evidence_processing.md`
3. `docs/external/34_actual_provider_shortlist_and_due_diligence.md`
4. `docs/external/34_vendor_selection_decision_log.md`
5. `docs/external/34_vendor_research_evidence_register.md`
6. `docs/external/34_evidence_signal_atlas.html`
7. `data/analysis/34_vendor_universe.csv`
8. `data/analysis/34_vendor_shortlist.json`
9. `data/analysis/34_vendor_research_evidence.jsonl`
10. `data/analysis/34_mock_vs_actual_vendor_lane_matrix.csv`
11. `data/analysis/34_vendor_kill_switches.csv`
12. `tools/analysis/build_evidence_processing_vendor_shortlist.(py|ts)`
13. `tools/analysis/validate_evidence_processing_vendor_shortlist.(py|ts)`
14. `tests/playwright/34_evidence_signal_atlas.spec.(ts|js)`

Every vendor row must capture at minimum:
- `vendor_id`
- `vendor_name`
- `provider_family = transcription | artifact_scanning | combined`
- `vendor_lane = candidate | shortlisted | mock_only | rejected`
- `supports_async_jobs`
- `supports_partial_results`
- `supports_confidence_or_quality_bands`
- `supports_webhooks`
- `supports_replay_protection`
- `supports_region_controls`
- `retention_and_deletion_notes`
- `quarantine_fit_score`
- `mock_now_fit_score`
- `actual_later_fit_score`
- `kill_switch_reason_if_any`
- `source_refs[]`
- `notes`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Select the mock provider lane for evidence processing. This means explicitly defining the internal defaults Vecells will build against now.

The mock-now lane must decide and document:
- the canonical local transcription twin
- the canonical local artifact-scan / malware-scan twin
- the shared webhook-signing and replay model
- the quality-band, coverage-class, and quarantine semantics that the mocks must preserve
- the evidence bar that proves the mocks are development-grade rather than toy-grade

The mock lane must explicitly preserve:
- transcript states `not_started | queued | running | partial | ready | failed | superseded`
- coverage classes and quality bands
- scan outcomes such as clean, suspicious, quarantined, unreadable, and failed
- artifact-hash and evidence provenance hooks
- fallback review when scan or transcript meaning is insufficient
- no direct elevation from processing success to clinical truth

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the real later vendor-selection strategy. It must include:
- vendor universe definition per family
- research plan using current official vendor docs only
- weighted scorecards per provider family using task `022`
- shortlist and rejection log
- kill-switch criteria for unacceptable vendors
- a no-purchase rule for this task
- a later handoff pack for task `035`
- explicit note on when local/open-source scanning remains preferable to a managed service

Execution steps:
1. Start from tasks `021-023` and the blueprint’s evidence-processing semantics.
2. Define the mock-now lane first and freeze the required simulator fidelity.
3. Browse current official vendor docs and build the actual-later universe and shortlist second.
4. Record retrieval timestamps, evidence links, and reasons for rejection.
5. Generate the atlas UI and validate it with `Playwright_or_other_appropriate_tooling`.
6. Produce a handoff pack for provisioning task `035`.

Mandatory gap closures you must perform:
- Close the “transcription and scanning are generic utilities” gap by tying them to readiness, quarantine, and fallback-review semantics.
- Close the “mock-first means we can defer real market research entirely” gap by producing a current, source-backed shortlist now without purchasing anything.
- Close the “transcript returned means evidence is clinically usable” gap.
- Close the “artifact scanned means artifact is safe and meaningful” gap.
- Close the “one combined evidence vendor automatically wins” gap by explicitly scoring lock-in, portability, and failure-domain coupling.

Non-negotiable rules:
- do not recommend a vendor solely because it has a flashy AI story or attractive sample demos
- do not allow any vendor to pass if it cannot express partial, failed, or low-confidence outputs clearly
- do not allow any vendor to pass if quarantine and audit export are weak
- do not initiate real sign-up, project creation, or spend in this task
- do not rely on stale vendor memory; browse current official docs during execution

Mandatory UI and interaction specification for `34_evidence_signal_atlas.html`:
This must feel like a premium evidence-processing comparison instrument, not a generic analyst spreadsheet.

Name the visual mode `Evidence_Signal_Atlas`.

Brand and visual posture:
- include a subtle inline Vecells mark plus a clear `EVIDENCE_PROCESSING_ATLAS` label
- tone: analytical, safety-aware, minimal, high-trust
- base palette:
  - canvas `#F6F8FB`
  - panel `#FFFFFF`
  - inset `#EEF3F8`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#155EEF`
  - secondary accent `#7A5AF8`
  - transcript accent `#0E9384`
  - scan accent `#B54708`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for vendor IDs, evidence hashes, and score keys

Layout:
- sticky header with shortlist count, rejected count, mock-lane status, and freshness timestamp
- tab rail for `Transcription`, `Artifact_Scanning`, `Mock_Lane`, `Actual_Lane`
- primary comparison grid in the center
- right-side due-diligence drawer
- lower strip with one restrained family-coverage diagram and one score-by-dimension chart, each with adjacent table parity

Dimensional constraints:
- page max width `1440px`
- due-diligence drawer `360px`
- provider cards `min-height 180px`
- buttons `44px`
- chips `28px`
- sticky header `72px`

Unique page set:
1. `Transcription_Shortlist`
2. `Artifact_Scanning_Shortlist`
3. `Mock_Lane_Fidelity`
4. `Actual_Lane_Due_Diligence`
5. `Decision_Log_and_Kill_Switches`

Motion:
- `120ms` hover and focus
- `180ms` tab transitions
- `240ms` drawer transitions
- reduced-motion support mandatory

Interaction requirements:
- dimension filters
- sortable scorecards
- evidence drawer with source timestamps
- explicit mock-vs-actual lane switch
- keyboard-first interaction
- deterministic `data-testid` markers for tabs, provider rows, score cells, evidence drawer, and lane toggle

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and validation from the start. At minimum cover:
- tab switching
- sorting and filtering
- evidence drawer rendering
- chart/table parity
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- one mock-now evidence-processing provider lane is explicitly selected
- one current, source-backed real-provider shortlist exists for transcription and artifact scanning
- the output is usable as the authoritative handoff into task `035`
- vendor selection preserves the blueprint’s readiness, quarantine, and safety requirements
```
