# Prompt 104 For 104_par_phase0_track_Playwright_or_other_appropriate_tooling_frontend_publish_canonical_ui_contract_kernel

Use the shared operating contract first:
[shared_operating_contract_096_to_105.md](/Users/test/Code/V/prompt/shared_operating_contract_096_to_105.md)

```text
Execute roadmap task 104_par_phase0_track_Playwright_or_other_appropriate_tooling_frontend_publish_canonical_ui_contract_kernel.md.

Inherit the Shared_operating_contract_for_prompts_096_to_105 and consume the validated outputs from tasks `001-103`.

Mission:
Publish the executable canonical UI contract kernel for Vecells.

This task must transform the kernel blueprint into machine-readable contracts, digestable publication bundles, lint verdicts, and a premium studio that demonstrates how visible state propagates into accessibility, automation, telemetry, and artifact posture.

This is frontend, design-system, and contract-publication work. You must implement schemas, generators, linting, runtime-facing bundle artifacts, a unique studio experience, and Playwright-driven verification. Do not stop at prose tables or component screenshots.

Algorithm grounding you must honor:
- `canonical-ui-contract-kernel.md`, especially:
  - `VisualTokenProfile`
  - `SurfaceStateSemanticsProfile`
  - `SurfaceStateKernelBinding`
  - `AutomationAnchorMap`
  - `TelemetryBindingProfile`
  - `ArtifactModePresentationProfile`
  - semantic color roles and state-precedence equation
- `platform-frontend-blueprint.md`, especially:
  - design-contract responsibilities
  - `DesignContractPublicationBundle`
  - `DesignContractLintVerdict`
  - motion and verification contracts
  - shell and state invariants
- `design-token-foundation.md` and outputs from task `103`
- `accessibility-and-content-system-contract.md`, especially landmark, focus, announcement, form, timeout, chart/table parity, and content-clarity rules
- `uiux-skill.md` and `ux-quiet-clarity-redesign.md` for calm hierarchy, density control, and “one dominant action” constraints
- outputs from tasks `050`, `094`, `096`, and `099`
- `forensic-audit-findings.md`, especially Findings `86`, `97`, `103`, `116`, `117`, `118`, and `120`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must publish the kernel-level contracts now so later components and route families can consume them without inventing local semantic-state vocabularies. If later tasks own route-specific specialization, publish the shared kernel and lint law here and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build the full kernel now with local and CI-friendly generation.
- generate kernel contracts and bundle digests from the design-token foundation and current route-family inventory
- expose a studio where engineers can inspect state precedence, automation anchors, accessibility coverage, and artifact-mode behavior without any live backend
- use mocked surface states and sample route families to prove the kernel works before real shells consume it
The mock-now path is the real kernel. Later shells must import the same published bundle artifacts and lint verdicts.

Actual_production_strategy_later:
When production shells consume the kernel, preserve the same schemas, publication bundle semantics, lint verdict rules, and kernel-propagation digests. Production hardening may change packaging or distribution, but it must not fork the state vocabulary or create shell-local semantic meaning.

Primary outcome:
The repository gains one executable UI kernel that makes state meaning, accessibility meaning, DOM markers, telemetry vocabulary, and artifact behavior consistent across all later shells.

Implementation deliverables to create:
1. `docs/architecture/104_canonical_ui_contract_kernel_publication.md`
2. `docs/architecture/104_surface_state_kernel_binding_strategy.md`
3. `docs/architecture/104_design_contract_publication_bundle_strategy.md`
4. `docs/architecture/104_ui_kernel_studio.html`
5. `data/analysis/design_contract_publication_bundle.json`
6. `data/analysis/design_contract_lint_verdicts.json`
7. `data/analysis/surface_state_kernel_bindings.csv`
8. `data/analysis/automation_anchor_maps.json`
9. `data/analysis/accessibility_semantic_coverage_profiles.json`
10. schemas and generators for the kernel contracts under the chosen `packages/design-system` or contract package layout
11. `tools/analysis/validate_ui_contract_kernel.(py|ts)`
12. unit, lint, and visual tests plus `tests/playwright/ui-kernel-studio.spec.(ts|js)`

You must publish at minimum these executable contract families:
- `VisualTokenProfile`
- `SurfaceStateSemanticsProfile`
- `SurfaceStateKernelBinding`
- `AutomationAnchorMap`
- `TelemetryBindingProfile`
- `ArtifactModePresentationProfile`
- `DesignContractVocabularyTuple`
- `DesignContractPublicationBundle`
- `DesignContractLintVerdict`

Visual and interactive direction you must implement:
1. Overall studio composition
Create a premium “Kernel Atlas” studio, not a documentation wiki.
- left rail at `280px` on large screens, `72px` collapsed option
- top masthead with bundle digest, lint verdict, active mode tuple, and route-family selector
- central two-plane layout: a live surface preview on the left and an explainer or contract inspector on the right
- lower fold for automation anchor map, accessibility coverage, artifact-mode policy, and telemetry vocabulary
- use the token foundation from task `103`; do not invent a second visual system

2. Exact visual mood
- light canvas should stay on the quiet neutral range from task `103`
- only one accent family may dominate a region at a time
- danger or review tones appear as contained chips, keylines, or dock emphasis, never full-screen washes
- hierarchy should come from spacing, keylines, topology, and one promoted panel, not shadow piles

3. Exact dimensional constraints
- main content max width `1600px`
- preview plane min width `min(640px, 100%)`
- inspector plane width `clamp(320px, 28vw, 420px)`
- section padding `24px` on `lg`, `32px` on `xl`
- inline gap scale: `8/12/16px`; region gap `24/32/48px`
- sticky kernel summary bar height `56px`
- table rows `40px` default, `36px` compact, `32px` dense-data only for non-editable parity tables

4. State-precedence visualizer
Create an interactive precedence visualizer that lets the engineer choose posture, state class, freshness, trust, settlement, writability, and artifact mode, then see:
- the winning effective tone
- the allowed motion intent
- the aria-live mode
- the dominant automation anchor
- the expected telemetry event vocabulary
- whether the binding remains `exact`, `stale`, or `blocked`
This must be rendered as a calm precedence ladder or equation strip, with accessible table fallback.

5. Kernel propagation diagram
Render one elegant propagation diagram showing:
`surface truth -> SurfaceStateSemanticsProfile -> SurfaceStateKernelBinding -> accessibility + automation + telemetry + artifact mode`
Use restrained line work, one active accent, and explicit labels. Provide summary text and a tabular fallback.

6. Artifact and automation views
- automation map should display stable DOM markers, dominant action marker, selected anchor marker, artifact marker, and continuity marker in one inspector
- artifact-mode panel should show summary / preview / print / download / export / handoff policy states without decorative chrome
- telemetry panel should show redacted event vocabulary in monospace with calm grouping

7. Motion
- use 120ms highlight transitions and 180-240ms state morphs only where they improve causal understanding
- every motion cue must have a visible static equivalent in the studio
- reduced motion must preserve the exact same ordering and inspector output

Implementation instructions:
1. Generate the kernel contracts from the token foundation and current route-family model. Do not hand-maintain unrelated files.
2. Build a lint pipeline that fails when visible state, automation anchors, accessibility coverage, or telemetry vocabulary drift apart.
3. Implement the state-precedence equation as executable code, not a screenshot or markdown table.
4. Never allow these anti-patterns:
   - route-local data-testid vocabulary that bypasses `AutomationAnchorMap`
   - ARIA states that are not backed by the current visible state
   - telemetry names that diverge from the same state or anchor vocabulary
   - artifact behavior implied by button labels instead of `ArtifactModePresentationProfile`
5. Make the `DesignContractPublicationBundle` and `DesignContractLintVerdict` production-grade artifacts that later runtime publication can consume.
6. Use `Playwright_or_other_appropriate_tooling` to drive the studio development and verify:
   - bundle switching
   - mode switching
   - precedence visualizer behavior
   - automation-anchor stability
   - accessibility and reduced-motion parity

Gap handling you must perform during this task:
- If the corpus does not fully specify one executable format for the precedence equation or lint verdict, define it now as `GAP_RESOLUTION_KERNEL_EXECUTION_FORMAT_*` while preserving the blueprint’s semantics.
- If any route family lacks required automation or accessibility coverage, emit `GAP_KERNEL_COVERAGE_*` and keep the binding blocked.
- If artifact-mode rules are incomplete for some surfaces, bind the safest summary-only posture and record `FOLLOW_ON_DEPENDENCY_ARTIFACT_MODE_*`.

Verification requirements:
- schema-validation tests for all kernel contracts
- precedence-equation unit tests
- lint-verdict tests
- publication-bundle digest tests
- `Playwright_or_other_appropriate_tooling` tests across light/dark, contrast, density, and motion modes plus keyboard-only interaction and screen-reader-visible semantics

Definition of done:
- Vecells has one executable UI kernel, not parallel state vocabularies
- design publication bundles and lint verdicts are generated and testable
- engineers can inspect propagation from state truth to DOM and telemetry in one premium studio
- later components and shells can consume exact bindings without inventing local semantics
```
