# Prompt 100 For 100_par_phase0_track_runtime_implement_supply_chain_sbom_and_signature_verification

Use the shared operating contract first:
[shared_operating_contract_096_to_105.md](/Users/test/Code/V/prompt/shared_operating_contract_096_to_105.md)

```text
Execute roadmap task 100_par_phase0_track_runtime_implement_supply_chain_sbom_and_signature_verification.md.

Inherit the Shared_operating_contract_for_prompts_096_to_105 and consume the validated outputs from tasks `001-099`.

Mission:
Implement the authoritative Vecells supply-chain integrity layer.

This task must create real build-provenance, SBOM, attestation, signing, verification, quarantine, revocation, and runtime-consumption controls for the artifact set that reaches release publication.

This is real backend, pipeline, and security-control work. Produce schemas, build pipeline steps, verification services, artifact quarantine paths, audit records, and tests. Do not leave provenance, SBOM, or signature handling as best-effort CI attachments.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### BuildProvenanceRecord`
  - `RuntimePublicationBundle`
  - verification-ladder Gate 0 and the release pipeline rules that require SBOM, signing, provenance, target runtime bindings, and runtime-consumption blocking on drift or revocation
- `phase-0-the-foundation-protocol.md`, especially publication, provenance, and authority-freeze expectations
- outputs from tasks `012`, `015`, `046`, `051`, `091`, `094`, and `099`
- `forensic-audit-findings.md`, especially Findings `95`, `112`, `113`, and `118`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must publish the shared provenance and verification layer now so runtime publication, readiness, canary, and design-publication tasks can consume a trustworthy artifact tuple. If a later task owns a broader assurance export or governance surface, publish the machine-readable provenance seam here and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build the full provenance, SBOM, signing, and verification path now for local and non-production artifacts.
- generate provenance for local and CI builds
- attach SBOMs and signatures or attestations to rehearsal artifacts
- support quarantine, revocation, and supersession simulation
- bind provenance to the target runtime manifest, gateway surfaces, and workload families even before production artifacts exist
The mock-now path must preserve the same fields, trust transitions, and fail-closed behavior required later in production.

Actual_production_strategy_later:
When production artifacts are built and promoted, preserve the same `BuildProvenanceRecord` fields, SBOM semantics, attestation chain, verification rules, quarantine and revocation behavior, and runtime-consumption blocking. Production hardening may use stronger key management, trusted builders, or registry integration, but it must not allow partial artifact replacement or detached provenance.

Primary outcome:
The repository gains a real supply-chain integrity plane that can prove which artifact set was built, how it was built, what it depends on, what runtime tuple it targets, and whether it is still safe to consume.

Implementation deliverables to create:
1. `docs/architecture/100_supply_chain_provenance_sbom_signature_design.md`
2. `docs/architecture/100_provenance_quarantine_revocation_and_supersession_rules.md`
3. `docs/architecture/100_build_provenance_cockpit.html`
4. `data/analysis/build_provenance_record_schema.json`
5. `data/analysis/provenance_policy_matrix.csv`
6. `data/analysis/sbom_scope_catalog.json`
7. pipeline code that generates provenance records, SBOMs, signatures or attestations, and verification outcomes
8. verification code that can quarantine, revoke, supersede, or unblock runtime consumption based on canonical rules
9. `tools/analysis/validate_build_provenance_and_sbom.(py|ts)`
10. unit, integration, and pipeline rehearsal tests for generation, verification, quarantine, revocation, and supersession
11. `tests/playwright/build-provenance-cockpit.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- schema-validated `BuildProvenanceRecord` generation with all required fields from the runtime blueprint
- SBOM generation for the exact promoted artifact set and its material inputs
- signing or attestation issuance bound to the same artifact digests, build invocation, and target runtime scope
- verification that fails runtime consumption when provenance is missing, drifted, quarantined, revoked, or superseded
- target-binding proof tying artifacts to runtime topology, gateway surfaces, trust boundaries, and workload families so artifacts cannot be reused against a different runtime tuple without re-verification
- quarantine and supersession handling that preserves prior audit history without allowing stale or replaced artifacts to remain silently publishable

Implementation instructions:
1. Create a canonical provenance builder that captures source-tree state, build recipe, build environment, artifact digests, material inputs, dependency locks, target runtime bindings, and reproducibility posture.
2. Emit provenance, SBOM, and signature or attestation artifacts in deterministic machine-readable formats that can be diffed and validated.
3. Bind provenance verification into runtime publication and pipeline progression; publication must fail if verification state or runtime-consumption state is blocked.
4. Support these state transitions explicitly:
   - pending -> verified
   - pending -> quarantined
   - verified -> revoked
   - verified -> superseded
   - quarantined -> verified only through a fresh valid record, never by manual override in place
5. Never allow these anti-patterns:
   - re-packaging or swapping one artifact after provenance verification
   - publishing on a dirty source tree
   - generic “signed” status without target runtime binding
   - reusing provenance for a different gateway, workload family, or topology tuple
6. Publish one provenance cockpit that lets engineers inspect artifact digests, target runtime scope, verification state, revocation or supersession lineage, and publication eligibility from one authoritative dataset.

Gap handling you must perform during this task:
- If the current repo lacks a standards-compatible SBOM or attestation format decision, select one that is machine-readable, deterministic, and pipeline-verifiable, record the choice as `GAP_RESOLUTION_PROVENANCE_FORMAT_*`, and keep the format choice replaceable behind the same canonical record fields.
- If build environments or trusted-builder identity are under-specified, publish a bounded trust model now and record `GAP_RESOLUTION_BUILDER_IDENTITY_*`.
- If later tasks own richer governance or assurance surfacing, expose the machine-readable records here and record `FOLLOW_ON_DEPENDENCY_SUPPLY_CHAIN_SURFACE_*`.

Backend and security engineering standards:
- use least-privilege secret or key access
- separate build, signing, and verification responsibilities where practical
- preserve immutable audit history for every verification or revocation decision
- protect against partial artifact replacement, TOCTOU drift, and hidden material inputs
- add metrics for unsigned builds, quarantined artifacts, revoked artifacts, superseded artifacts, and blocked runtime-consumption events

Verification requirements:
- provenance schema tests
- SBOM completeness tests
- signature/attestation verification tests
- quarantine and revocation tests
- target-runtime-binding drift tests
- pipeline block tests
- `Playwright_or_other_appropriate_tooling` tests for the cockpit surface and any browser-visible provenance summaries shown there

Definition of done:
- every publishable artifact set has one authoritative provenance record, one SBOM, and one verification outcome
- runtime publication is blocked automatically when provenance is stale, quarantined, revoked, or mis-bound
- provenance and runtime scope remain tied together
- engineers and reviewers can inspect supply-chain state from one cockpit surface without relying on CI logs or registry dashboards alone
```
