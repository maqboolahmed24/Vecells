# Prompt 016 For 016_seq_freeze_target_architecture_adr_set

Use the shared operating contract first:
[shared_operating_contract_016_to_020.md](/Users/test/Code/V/prompt/shared_operating_contract_016_to_020.md)

```text
Execute roadmap task 016_seq_freeze_target_architecture_adr_set.md.

Inherit the Shared_operating_contract_for_prompts_016_to_020 and consume the validated outputs from tasks 001-015.

Mission:
Freeze the Vecells target architecture as one authoritative ADR set plus one coherent architecture-view pack.

This task must convert the planning and baseline choices from tasks 001-015 into durable architecture law. The result must tell every later autonomous agent exactly what the platform is, what it is not, where truth lives, how shells and gateways may behave, how data and events move, where trust boundaries sit, how release and assurance freeze writable posture, and which alternatives were consciously rejected.

This is not a fluffy “architecture overview” document. It is the canonical architecture decision freeze for the current baseline:
- current baseline = Phases 0-6 + 8 + 9
- deferred baseline expansion = Phase 7 embedded NHS App channel
- bounded optionality = assistive layer rollout, optional PDS enrichment, supplier capability variance behind adapter seams

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 1. Product definition`
  - `### 2. Core product surfaces`
  - `### 3. Core end-to-end product flow`
  - `### 12. Practical engineering shape`
  - the release / continuity / summary-layer warnings that route-local shortcuts, false closure, and uncontrolled channel behavior are forbidden
- `phase-0-the-foundation-protocol.md`, especially:
  - `## 0A. Delivery skeleton and repository architecture`
  - `## 0B. Canonical domain kernel and state machine`
  - the required platform objects
  - the route and settlement substrate
  - the canonical governors
  - the runtime / release / design-contract publication rules
  - the Phase 0 tests that prove duplicate replay, quarantine, fallback review, repair hold, confirmation-gate blocking, release freeze, trust-zone, and closure-blocker behavior
- `phase-cards.md`, especially:
  - `## Cross-Phase Conformance Scorecard`
  - `## Programme Summary-Layer Alignment`
  - `## Extended Summary-Layer Alignment`
  - `## Card 1: Phase 0 - The Foundation Protocol`
- `platform-runtime-and-release-blueprint.md`, especially:
  - `## Runtime topology contract`
  - `### RuntimeWorkloadFamily`
  - `### TrustZoneBoundary`
  - `### RuntimeTopologyManifest`
  - `### GatewayBffSurface`
  - `### FrontendContractManifest`
  - `### AdapterContractProfile`
  - `### ReleaseWatchTuple`
  - `### WaveObservationPolicy`
  - `### ReleaseRecoveryDisposition`
  - the security, verification-ladder, and operational-readiness contract sections
- `platform-frontend-blueprint.md`, especially:
  - `## Canonical real-time interaction, motion, and live-projection experience algorithm`
  - the shell law
  - `ShellContinuityFrame`, `ContinuityFrame`, `ContinuityTransitionCheckpoint`, `ShellBoundaryDecision`
  - `SelectedAnchor`, `CasePulse`, `StateBraid`, `DecisionDock`
  - `ArtifactSurfaceFrame` and the kernel-binding rules
- `platform-admin-and-config-blueprint.md`, especially:
  - the config compilation pipeline
  - change-control rules
  - production promotion gate
  - dependency and standards hygiene
- `phase-9-the-assurance-ledger.md`, especially:
  - evidence graph and admissibility
  - resilience / restore / failover / chaos controls
  - tenant governance and dependency hygiene
  - full-program exit gate and `CrossPhaseConformanceScorecard`
- `forensic-audit-findings.md`, especially Findings:
  - 91, 95, 104-120
  - plus any earlier finding that materially changes ownership, state truth, continuity proof, release posture, or resilience authority

Primary outcome:
One accepted architecture decision set that later tasks can cite without re-litigating the architecture.

Minimum ADR families you must freeze:
1. product-shape ADR: Vecells-first domain model with FHIR at the representation / interoperability boundary
2. repository-shape ADR: modular monorepo and bounded-context package law
3. tenant / acting-scope ADR: shared platform with explicit tenant isolation and governed `ActingScopeTuple`
4. runtime topology ADR: workload families, trust zones, browser boundaries, and egress rules
5. gateway / BFF ADR: audience-specific gateway surfaces and browser-callable contract publication
6. state and event ADR: append-only domain events, outbox/inbox reliability, projection-first browser reads
7. evidence and artifact ADR: immutable capture bundles, derivation packages, parity proof, redaction transforms
8. lifecycle-control ADR: `LifecycleCoordinator`, closure blockers, route-intent binding, command settlement, and scoped mutation gates
9. integration ADR: adapter seams, capability matrices, degradation profiles, and proof-based settlement
10. frontend-shell ADR: persistent shells, continuity law, design-contract publication, automation anchors, and artifact posture
11. release and trust ADR: `ReleaseApprovalFreeze`, `ChannelReleaseFreezeRecord`, `RuntimePublicationBundle`, `ReleaseWatchTuple`, `WaveObservationPolicy`
12. assurance and resilience ADR: evidence graph, ops/governance parity, `OperationalReadinessSnapshot`, `RecoveryControlPosture`
13. data / privacy / disclosure ADR: classification, PHI boundaries, audit disclosure, masking, redaction
14. bounded assistive ADR: optional assistive layer, human control, insertion limits, trust envelope, and kill-switch behavior

Deliverables to create:
1. `docs/architecture/16_adr_index.md`
2. `docs/architecture/16_target_architecture_adr_set.md`
3. `docs/architecture/16_system_context_and_container_model.md`
4. `docs/architecture/16_domain_runtime_and_control_plane_architecture.md`
5. `docs/architecture/16_frontend_gateway_and_design_contract_architecture.md`
6. `docs/architecture/16_data_event_storage_and_integration_architecture.md`
7. `docs/architecture/16_release_assurance_and_resilience_architecture.md`
8. `docs/architecture/16_architecture_decision_matrix.md`
9. `docs/architecture/16_architecture_decision_studio.html`
10. `docs/architecture/16_architecture_views.mmd`
11. `data/analysis/adr_index.json`
12. `data/analysis/adr_decision_matrix.csv`
13. `data/analysis/architecture_contract_binding_matrix.csv`
14. `data/analysis/architecture_gap_register.json`
15. `tools/analysis/build_architecture_decision_studio.(py|ts)`
16. `tools/analysis/validate_adr_set.(py|ts)`

Each ADR row must capture at minimum:
- `adr_id`
- `decision_family`
- `title`
- `status = proposed | accepted | deferred | superseded`
- `scope = platform | cross_phase | audience_surface | integration | release | assurance`
- `problem_statement`
- `decision`
- `why_now`
- `source_refs[]`
- `upstream_task_refs[]`
- `affected_phase_refs[]`
- `affected_bounded_context_refs[]`
- `affected_persona_refs[]`
- `affected_channel_refs[]`
- `accepted_alternative_refs[]`
- `rejected_alternative_refs[]`
- `consequences_positive[]`
- `consequences_negative[]`
- `required_follow_on_task_refs[]`
- `linked_requirement_ids[]`
- `linked_risk_ids[]`
- `validation_obligations[]`
- `notes`

Execution steps:
1. Start from tasks 011-015. The ADR set must freeze those baselines rather than re-running the same decision work under new headings.
2. Cross-check tasks 001-010 so every architecture decision still aligns with:
   - requirement registry
   - terminology reconciliation
   - scope boundary
   - persona/channel/shell inventory
   - lineage model
   - glossary/object catalog
   - state/invariant atlas
   - dependency inventory
   - assurance obligations
   - data-classification posture
3. Build a candidate architecture-decision matrix. For each major decision family:
   - state the decision to be frozen
   - cite the canonical source blocks that require it
   - list viable alternatives
   - reject alternatives explicitly with source-grounded reasons
4. Freeze the minimum ADR families listed above. Add additional ADRs if the corpus clearly requires them.
5. Produce architecture views that remain faithful to the corpus:
   - system context view
   - container / workload-family view
   - bounded-context and gateway view
   - control-plane and publication-tuple view
   - event / storage / adapter view
   - assurance / resilience / release view
6. Build one `architecture_contract_binding_matrix` that makes it impossible to forget which published contract or tuple governs each surface or runtime plane. At minimum include rows for:
   - `RuntimeTopologyManifest`
   - `GatewayBffSurface`
   - `FrontendContractManifest`
   - `AudienceSurfaceRuntimeBinding`
   - `DesignContractPublicationBundle`
   - `RouteIntentBinding`
   - `CommandSettlementRecord`
   - `ReleaseApprovalFreeze`
   - `ChannelReleaseFreezeRecord`
   - `AssuranceSliceTrustRecord`
   - `ReleaseWatchTuple`
   - `WaveObservationPolicy`
   - `OperationalReadinessSnapshot`
   - `RecoveryControlPosture`
7. Build the browser-viewable architecture decision studio. It must not be a generic docs theme dump. It must expose:
   - ADR index and status chips
   - decision-family filters
   - one architecture view canvas / diagram surface
   - one right-side inspector with source refs, consequences, and linked tasks
   - one contract-binding matrix view
   - one gap / conflict panel
8. Add validator logic that fails when:
   - an ADR lacks source refs
   - an accepted ADR contradicts the scope baseline or requirement registry
   - a canonical contract/tuple appears in tasks 011-015 outputs but is absent from the ADR set
   - a decision is marked accepted but no consequence or follow-on task is listed
   - the architecture views disagree with the accepted ADRs

Mandatory gap closures you must perform:
- Close the “baseline decisions exist only as scattered prior-task prose” gap by freezing them into explicit ADRs.
- Close the “Phase 0 control plane can still look phase-local” gap identified by Findings 91 and 95.
- Close the “design-contract publication can drift outside runtime publication” gap identified by Finding 118.
- Close the “ops/governance continuity and resilience authority are separate mental models” gap identified by Findings 95, 104-120.
- Close the “Phase 7 deferred channel can accidentally re-enter the current baseline” gap by keeping its ADR status `deferred` unless the corpus explicitly says otherwise.
- Close the “assistive layer can appear mandatory or architecturally central to core request progression” gap by bounding it through the assistive ADR.
- Close the “tenant / acting-scope / governance scope drift” gap identified by Finding 114.
- Close the “artifact preview / handoff mode truth is an implementation detail” gap identified by Finding 115.
- Close the “patient degraded mode, continuity proof, or shell continuity can be route-local decisions” gap identified by Findings 107-120.

Non-negotiable architecture rules you must encode:
- no app owns truth
- no browser or shell talks directly to adapters
- no child domain directly derives canonical closure
- no calm or writable posture may outrun current release / trust / continuity tuples
- no external-success transport event becomes business truth without authoritative settlement and proof objects
- no generic fallback may replace audience-specific `ReleaseRecoveryDisposition`
- no standards / config / publication / runtime / watch posture may be tracked only in adjacent dashboards or prose
- no route family may claim shell ownership without an explicit ownership contract
- no supplier-specific booking, hub, pharmacy, telephony, or notification logic may live in the core model

Mandatory UI and interaction specification for `16_architecture_decision_studio.html`:
This is an internal architecture studio, not a product shell. It should feel like a premium systems-design workstation.

Visual direction:
- name the visual mode `Architecture_Atelier`
- tone: minimalist premium, calm, precise, low-noise
- base palette:
  - canvas `#F4F7FA`
  - shell `#FFFFFF`
  - inset `#EEF2F6`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#475467`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - architecture accent `#335CFF`
  - topology accent `#0F8B8D`
  - caution `#C98900`
  - critical `#C24141`
- typography: system-sans from the design-token foundation; one `mono.sm` role for IDs/hashes only
- layout:
  - left rail `72px / 296px`
  - center workspace with diagram canvas and table parity
  - right inspector `360-440px`
  - top summary strip with accepted/deferred/superseded counts and current source digest
- dimensional constraints:
  - diagram canvas min height `560px`
  - table rows `40px` default, `32px` read-only dense mode
  - chips `28px` height
  - filters wrap cleanly above `1024px`, stack below
- motion:
  - `120ms` hover/focus
  - `180ms` panel reveal
  - `240ms` inspector swap
  - reduced motion support mandatory
- branding:
  - include one understated inline-SVG Vecells monogram in the header
- diagrams/charts:
  - include at most one dominant diagram per tab
  - every diagram must have a synchronized adjacent table/list
- DOM markers:
  - `data-testid="adr-rail"`
  - `data-testid="adr-summary-strip"`
  - `data-testid="adr-filter-bar"`
  - `data-testid="architecture-canvas"`
  - `data-testid="contract-binding-table"`
  - `data-testid="adr-inspector"`
  - stable `data-adr-id`, `data-view-id`, and `data-contract-ref` markers on rows/nodes

Playwright_or_other_appropriate_tooling requirements:
Use `Playwright_or_other_appropriate_tooling` as a first-class development and testing driver for the studio. Minimum required coverage:
- load without network access
- left-rail navigation and keyboard traversal
- filter by ADR status and decision family
- open inspector from matrix row and from diagram node
- responsive layout at `768px`, `1024px`, and `1440px`
- reduced-motion mode
- stable DOM markers for ADR rows, diagram nodes, filters, and inspector panels
- diagram/table parity checks
- offline asset completeness checks
- accessibility smoke checks for landmarks, headings, and focus order

Definition of done:
- Vecells has one explicit accepted target architecture ADR set.
- The accepted ADRs align with tasks 001-015 and the canonical blueprint corpus.
- Later implementation agents can cite one decision record instead of inferring architecture from scattered documents.
- The architecture studio makes those decisions explorable without sacrificing determinism, traceability, or offline validation.
```
