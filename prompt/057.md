# Prompt 057 For 057_seq_phase0_define_adapter_contract_profile_template_and_degradation_profiles

Use the shared operating contract first:
[shared_operating_contract_056_to_065.md](/Users/test/Code/V/prompt/shared_operating_contract_056_to_065.md)

```text
Execute roadmap task 057_seq_phase0_define_adapter_contract_profile_template_and_degradation_profiles.md.

Inherit the Shared_operating_contract_for_prompts_056_to_065 and consume the validated outputs from tasks `001-056`.

Mission:
Define the canonical external-boundary contract model for Vecells. This task must publish one rigorous template for `AdapterContractProfile` and one bounded `DependencyDegradationProfile` strategy so that transport, replay, callback ordering, proof of success, and failure blast radius are governed centrally rather than inferred ad hoc inside workers, vendor SDK wrappers, or shell CTAs.

This task must also formalize the simulator-first operating mode required for Phase 0. For every provider-shaped boundary, the outputs must include two explicit sections:
- `Mock_now_execution`
- `Actual_provider_strategy_later`

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### DependencyDegradationProfile`
  - `### AdapterContractProfile`
  - the integration-workload and trust-zone rules
  - the rule that adapters consume only published `FhirRepresentationContract` and `FhirExchangeBundle` rows
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.13B BookingProviderAdapterBinding`
  - `#### 1.23B AdapterDispatchAttempt`
  - `#### 1.23C AdapterReceiptCheckpoint`
  - `#### 2.9 ExceptionOrchestrator`
  - `#### 6.6A Adapter outbox, inbox, and callback replay rule`
  - the invariants that require authoritative proof and bounded degradation
- `phase-cards.md`, especially:
  - the rule that every adapter boundary ships one `AdapterContractProfile`
  - the requirement to create local simulators for NHS login, IM1, MESH, telephony, and notifications
- `phase-4-the-booking-engine.md`, especially provider capability, binding, reservation semantics, confirmation ambiguity, and fallback rules
- `phase-5-the-network-horizon.md`, especially imported confirmation handling and cross-org proof rules
- `phase-6-the-pharmacy-loop.md`, especially dispatch proof, outcome ingest, bounce-back, and explicit `AdapterDispatchAttempt` / `AdapterReceiptCheckpoint` reuse
- `callback-and-clinician-messaging-loop.md`, especially telephony and delivery replay / dedupe law
- `forensic-audit-findings.md`, especially any place where transport optimism, missing fallback, or direct supplier assumptions were corrected
- outputs from tasks `008`, `021-040`, `046-056`

Primary outcome:
One machine-readable contract template proving, for each external dependency boundary:
- which integration workload and trust boundary may touch it
- what external effect families it may perform
- which replay, ordering, duplicate, and collision rules apply
- what counts as authoritative proof versus transport evidence
- how degraded mode stays bounded
- how simulator-first execution maps cleanly onto later live-provider cutover

Deliverables to create:
1. `docs/architecture/57_adapter_contract_profile_template.md`
2. `docs/architecture/57_dependency_degradation_profile_strategy.md`
3. `docs/architecture/57_mock_now_vs_actual_provider_strategy.md`
4. `docs/architecture/57_provider_binding_and_effect_family_matrix.md`
5. `docs/architecture/57_adapter_contract_studio.html`
6. `data/analysis/adapter_contract_profile_template.json`
7. `data/analysis/dependency_degradation_profiles.json`
8. `data/analysis/adapter_effect_family_matrix.csv`
9. `data/analysis/simulator_vs_live_adapter_matrix.csv`
10. `packages/api-contracts/schemas/adapter-contract-profile.schema.json`
11. `packages/api-contracts/schemas/dependency-degradation-profile.schema.json`
12. `tools/analysis/validate_adapter_contract_profiles.(py|ts)`
13. `tests/playwright/adapter-contract-studio.spec.(ts|js)`

Each `AdapterContractProfile` row must capture at minimum:
- `adapterContractProfileId`
- `adapterCode`
- `dependencyCode`
- `effectFamilies[]`
- `supportedActionScopes[]`
- `capabilityMatrixRef`
- `outboxCheckpointPolicyRef`
- `receiptOrderingPolicyRef`
- `callbackCorrelationPolicyRef`
- `idempotencyWindowRef`
- `duplicateDispositionRef`
- `collisionDispositionRef`
- `retryPolicyRef`
- `dependencyDegradationProfileRef`
- `integrationWorkloadFamilyRef`
- `requiredTrustZoneBoundaryRef`
- `allowedFhirRepresentationContractRefs[]`
- `allowedFhirExchangeBundleTypes[]`
- `authoritativeProofRulesRef`
- `manualReviewFallbackRef`
- `simulatorContractRef`
- `liveCutoverChecklistRef`
- `updatedAt`

Each `DependencyDegradationProfile` row must capture at minimum:
- `profileId`
- `dependencyCode`
- `failureMode`
- `patientFallbackState`
- `staffFallbackState`
- `impactedWorkloadFamilyRefs[]`
- `maximumEscalationFamilyRefs[]`
- `assuranceTrustEffect`
- `topologyFallbackMode`
- `queueingMode`
- `manualReviewMode`
- `freezeMode`
- `retryPolicyRef`
- `alertThresholdRef`
- `recoveryTriggerRef`

Execution steps:
1. Start from task `040`’s frozen integration assumptions and degraded defaults, task `047`’s trust-zone map, task `048`’s event namespace, task `049`’s FHIR representation contract strategy, and task `056`’s mutation / settlement law.
2. Publish the dependency taxonomy and effect-family catalog. At minimum cover:
   - identity / auth handoff
   - GP / booking read-write boundaries
   - telephony / callback boundaries
   - notifications / delivery boundaries
   - MESH and partner-message boundaries
   - pharmacy dispatch / outcome boundaries
   - optional enrichment / lookup boundaries
3. Publish the `DependencyDegradationProfile` template first. It must make blast radius explicit and machine-checkable.
4. Publish the `AdapterContractProfile` template next. It must make outbox, inbox, replay, callback ordering, proof of success, and failure-handling explicit.
5. For each Phase 0 simulator-backed boundary, include two required sections:
   - `Mock_now_execution`:
     - simulator auth model
     - request / response schema
     - callback/webhook patterns
     - fault-injection cases
     - ordering and replay behaviors
     - seeded fixtures and observability hooks
   - `Actual_provider_strategy_later`:
     - live onboarding prerequisites
     - security / secret posture
     - operational evidence required before cutover
     - contract differences that must remain zero or tightly bounded
     - rollback back to simulator-safe mode
6. Publish explicit rows at minimum for:
   - NHS-login-like auth bridge simulator
   - GP booking / GP linkage simulator
   - MESH-like mailbox simulator
   - telephony / IVR simulator
   - notification delivery simulator
   - pharmacy dispatch / directory simulator or placeholder where later tracks require one
7. Publish the provider binding and effect-family matrix showing how `BookingProviderAdapterBinding`, later pharmacy dispatch bindings, and callback / message effect chains reference the shared profile template.
8. Build the studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
9. Validate that:
   - every external effect family has one owning adapter contract template
   - no adapter can be run outside its integration workload family and trust-zone boundary
   - no dependency failure mode has unbounded workload-family escalation
   - transport acceptance never counts as authoritative success unless the profile explicitly binds the proof class
   - simulator behavior mirrors the callback ordering, replay, and degradation semantics expected later in live cutover

Mandatory gap closures you must perform:
- Close the “workers or webhooks can invent replay and callback rules” gap by moving those rules into published adapter profiles.
- Close the “transport success implies business success” gap by requiring authoritative proof rules per effect family.
- Close the “provider degradation can spread across the platform” gap by publishing bounded degradation profiles.
- Close the “supplier-specific logic leaks into core booking or pharmacy code” gap by binding such variation only through published adapter profiles and provider bindings.
- Close the “Phase 0 waits for real providers” gap by making simulator-first execution the default and by separating it cleanly from later live cutover.
- Close the “simulator is a toy while live cutover is different” gap by requiring the mock and later-live sections to describe the same effect, callback, replay, and degradation semantics.

Industry-best-practice requirements:
- explicit trust-zone and workload-family ownership
- durable outbox / inbox semantics
- monotone settlement chains
- replay-safe callback handling
- deterministic fault-injection contracts for simulators
- blast-radius-aware degradation policy
- provider-cutover checklists that preserve the same domain truth semantics

Non-negotiable rules:
- Every external dependency boundary must publish one `AdapterContractProfile` and one `DependencyDegradationProfile`.
- Gateway, command, projection, or shell workloads may not become side-door integration runtimes.
- Booking-capable or other provider-shaped integrations must bind through explicit capability and adapter-binding tuples, not vendor-name switches or feature flags.
- Transport acceptance, queue dequeue, webhook arrival, or inbox receipt are never authoritative success by themselves.
- Simulator-first execution is the legal default in Phase 0; live-provider cutover is a later bounded strategy, not an implicit runtime toggle.

Mandatory UI and interaction specification for `57_adapter_contract_studio.html`:
Name the surface mode `Adapter_Contract_Studio`.

Tone:
- minimalist premium integration-governance surface
- should feel like a clean systems-engineering instrument, not a generic admin grid
- calm, exact, with visible blast-radius and fallback thinking

Branding:
- restrained Vecells wordmark plus an inline SVG `AC` monogram
- include one elegant topology-impact diagram and one simulator-vs-live comparison rail
- include a compact authoritative-proof ladder showing dispatch -> receipt -> observation -> settlement

Palette:
- canvas `#F7F8FC`
- rail `#EEF2F8`
- panel `#FFFFFF`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- integration accent `#0EA5A4`
- simulator accent `#7C3AED`
- live-cutover accent `#0F9D58`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for adapter codes, hashes, failure codes, and queue/effect IDs

Layout:
- sticky masthead with:
  - adapter profile count
  - degradation profile count
  - simulator-backed boundary count
  - live-cutover pending count
- left rail (`300px`):
  - dependency filters
  - effect-family filters
  - simulator/live toggle
- center canvas:
  - topology-impact diagram
  - authoritative-proof ladder
  - curated adapter profile cards
- right inspector (`396px`):
  - selected adapter profile
  - selected degradation profile
  - mock-now vs actual-later comparison
- lower region:
  - effect-family matrix
  - degradation matrix
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `620px`
- primary controls `44px`
- profile cards `min-height 170px`
- all diagrams require adjacent table/list parity

Motion:
- `120ms` hover/focus
- `180ms` filter / card selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by dependency, effect family, workload family, degradation severity, and simulator/live posture
- synchronized selection across cards, matrices, and inspector
- keyboard navigation across filters, cards, and matrix rows
- deterministic `data-testid` markers for topology diagram, proof ladder, inspector, simulator/live toggle, and defect strip
- mock-vs-live comparison must remain understandable without color alone

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- dependency filtering
- simulator/live comparison toggle
- card selection
- matrix / inspector synchronization
- keyboard navigation
- responsive layout
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has one explicit adapter-contract and degradation-profile template for all external boundaries.
- Simulator-first execution is now formalized without corrupting later live-provider cutover.
- Later backend and integration tracks can implement provider seams without inventing transport truth or unbounded degradation behavior.
```
