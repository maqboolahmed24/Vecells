# Prompt 079 For 079_par_phase0_track_backend_build_evidence_assimilation_coordinator_and_safety_orchestrator

Use the shared operating contract first:
[shared_operating_contract_076_to_085.md](/Users/test/Code/V/prompt/shared_operating_contract_076_to_085.md)

```text
Execute roadmap task 079_par_phase0_track_backend_build_evidence_assimilation_coordinator_and_safety_orchestrator.md.

Inherit the Shared_operating_contract_for_prompts_076_to_085 and consume the validated outputs from tasks `001-078`.

Mission:
Build the two canonical services that own post-submit evidence truth and safety truth:
- `EvidenceAssimilationCoordinator`
- `SafetyOrchestrator`

These services must turn later patient replies, telephony outcomes, booking narratives, pharmacy returns, support-side structured capture, async enrichment, and adapter observations into one governed evidence-assimilation and re-safety pipeline. They must not let local workflows or local UI states decide that new evidence is calm, non-material, or safe to bypass.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.7C EvidenceAssimilationRecord`
  - `#### 1.7D MaterialDeltaAssessment`
  - `#### 1.8 SafetyPreemptionRecord`
  - `#### 1.8A EvidenceClassificationDecision`
  - `#### 1.8B SafetyDecisionRecord`
  - `#### 1.8C UrgentDiversionSettlement`
  - `#### 2.3C EvidenceAssimilationCoordinator`
  - `#### 2.4 SafetyOrchestrator`
  - `### 7. Universal safety-preemption and reachability-risk algorithm`
- `phase-1-the-red-flag-gate.md`, especially the canonical post-submit evidence, material-delta, and re-safety rules
- `phase-2-identity-and-echoes.md`, especially the telephony convergence and duplicate-followup re-safety rules
- `callback-and-clinician-messaging-loop.md`, especially the rule that clinically material replies and callback outcomes must first settle canonical assimilation and re-safety
- `phase-6-the-pharmacy-loop.md`, where bounce-back, weak-match outcomes, consent withdrawal, or urgent return evidence can alter current safety meaning
- `forensic-audit-findings.md`, especially Findings `61`, `62`, `63`, `64`, `65`, `66`, `67`, `72`, `79`, `89`, and `99`
- outputs from tasks `001-007`, `019`, `041-045`, `048`, `056-057`, `061`, `063`, `067`, `069`, and `077`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Do not wait for later triage, callback, booking, or pharmacy domains to be fully implemented. Where a domain-specific ingress contract is missing, publish the smallest bounded typed adapter or intake event shape and record `PARALLEL_INTERFACE_GAP_*`.

Mock_now_execution:
Build a full local evidence-and-safety simulation harness now. It must cover:
- post-submit patient reply with no material change
- reply that is clinically material and forces re-safety
- contradictory low-assurance detail that cannot clear a prior urgent antecedent
- callback outcome or telephony transcript delta that triggers urgent diversion
- support-side structured capture that changes contact-safety meaning
- weak pharmacy outcome or consent withdrawal that forces manual review or re-safety
- exact replay and overlapping in-flight assimilations for the same lineage
The harness must produce the same assimilation records, material-delta assessments, safety decisions, and urgent-diversion settlements the live system will later require.

Actual_production_strategy_later:
When live patient messaging, telephony, booking, pharmacy, support, transcription, enrichment, and extraction pipelines are onboarded, they must all feed the same canonical assimilation and safety services. Live sources may improve evidence richness or latency, but they may not bypass immutable snapshot rules, re-safety triggers, contradiction handling, or urgent-diversion semantics.

Primary outcome:
The repository gains the canonical pipeline that decides whether new inbound evidence changes request truth, whether safety must rerun, and whether ordinary continuation must stop for urgent or unresolved risk.

Implementation deliverables to create:
1. `EvidenceAssimilationCoordinator` service implementation
2. `SafetyOrchestrator` service implementation
3. rule-pack loader and evaluator interfaces for canonical safety rules and calibration data
4. incremental impacted-rule recomputation engine with dependency graph support
5. bounded snapshot-supersession and candidate-snapshot freeze logic integrated with task `063` outputs
6. event schemas for evidence-assimilation, material-delta, classification, preemption, safety-decision, urgent-diversion, and blocker changes
7. `docs/architecture/79_evidence_assimilation_and_safety_design.md`
8. `docs/architecture/79_incremental_resafety_and_urgent_diversion_rules.md`
9. `docs/architecture/79_safety_assimilation_observatory.html`
10. `data/analysis/evidence_assimilation_casebook.json`
11. `data/analysis/safety_rule_dependency_matrix.csv`
12. `data/analysis/urgent_diversion_truth_manifest.json`
13. `tools/analysis/validate_assimilation_and_safety.(py|ts)`
14. unit tests, replay tests, overlapping-assimilation tests, rule-evaluator tests, and integration tests
15. `tests/playwright/safety-assimilation-observatory.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- settle exactly one `EvidenceAssimilationRecord` and one `MaterialDeltaAssessment` for each active-lineage post-submit ingress
- decide whether the ingress becomes `new_snapshot`, `derivative_only`, `replay_existing`, or `hold_pending_review`
- coalesce exact replay, semantic replay, and overlapping inflight assimilation for the same lineage
- invoke canonical safety only when trigger rules demand it
- classify evidence using the canonical feature-support lattice and contradiction model
- compute urgent and residual-risk probabilities while still honoring hard-stop rules above soft calibration
- append immutable `EvidenceClassificationDecision`, `SafetyPreemptionRecord`, `SafetyDecisionRecord`, and `UrgentDiversionSettlement` when required
- prevent routine continuation, calm projections, or closure while assimilation or safety settlement is pending or blocked

Execution steps:
1. Implement the coordinator to freeze candidate evidence input sets before making material-delta decisions.
2. Implement `MaterialDeltaAssessment` logic that differentiates material clinical change, contact-safety change, operational-only change, technical-only change, and replay/coalesced paths.
3. Integrate with snapshot supersession from task `063` so new snapshots are immutable and linked by supersession rather than overwritten.
4. Implement the safety rule-evaluator seam so authored rule packs, feature dependencies, hard stops, contradiction handling, and calibrated scoring can be tested deterministically.
5. Implement `SafetyOrchestrator` to append classification, preemption, safety decision, and urgent-diversion outputs under one canonical safety epoch.
6. Enforce that no ordinary workflow advance or calm surface refresh may occur while the owning assimilation or safety action is pending, blocked, or unresolved.
7. Build the observatory and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Write concurrency tests for duplicate ingress, overlapping asynchronous enrichment, replayed callback evidence, and urgent-diversion issuance.

Mandatory gap closures you must perform:
- Close the “post-submit evidence enters through local feature code” gap by routing all active-lineage evidence through `EvidenceAssimilationCoordinator`.
- Close the “new evidence can silently rewrite prior snapshots” gap by enforcing immutable snapshot supersession.
- Close the “low-assurance contradiction can clear urgent truth implicitly” gap by preserving hard-stop and contradiction rules.
- Close the “urgent required and urgent issued collapse into one blurry state” gap by persisting both `SafetyPreemptionRecord` and `UrgentDiversionSettlement` distinctly.
- Close the “material delta is decided ad hoc in each domain” gap by making `MaterialDeltaAssessment` the one canonical decision artifact.
- Close the “event catalogue lacks evidence and safety lifecycle events” gap with explicit event entries for assimilation, fallback, reachability, confirmation, and closure-blocker change.
- Close the “routine continuation continues while safety is unresolved” gap by freezing calm progression under pending or blocked canonical safety.

Industry-best-practice requirements:
- deterministic rule evaluation with versioned rule packs and calibration artifacts
- incremental recomputation rather than full rescoring when only bounded feature deltas changed
- append-only safety evidence chain and immutable audit joins
- strict replay handling and inflight coalescing
- no UI- or controller-local heuristics for materiality or urgentness
- test fixtures for contradictory, late, partial, and low-confidence evidence

Non-negotiable rules:
- `EvidenceAssimilationCoordinator` is the sole gateway for post-submit evidence on active lineages.
- `SafetyOrchestrator` is the sole owner of evidence classification, safety preemption, and canonical re-safety.
- Hard-stop rules beat soft calibration.
- `UrgentDiversionSettlement` is distinct from “urgent required” or “urgent suspected” posture.
- Local domains may not mark evidence as non-material or safe to bypass outside the canonical pipeline.

Mandatory UI and interaction specification for `79_safety_assimilation_observatory.html`:
Name the surface mode `Safety_Assimilation_Observatory`.

Tone:
- premium clinical-control instrument
- calm but unmistakably safety-critical
- should feel like an evidence lineage and risk observatory, not a triage dashboard

Branding:
- restrained Vecells wordmark plus inline SVG `SO` monogram
- one evidence-lineage ribbon
- one rule-impact heat surface
- one urgent-diversion decision ladder

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- evidence accent `#2563EB`
- safety accent `#7C3AED`
- urgent accent `#C24141`
- residual accent `#D97706`
- cleared accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for snapshot refs, rule-pack digests, safety epochs, and record IDs

Layout:
- sticky masthead with pending assimilations, urgent-preempted lineages, residual-risk count, and replay-coalesced count
- left rail (`324px`) with ingress-type filters, trigger-decision filters, rule-pack filters, and safety-state filters
- central canvas with:
  - evidence-lineage ribbon
  - rule-impact heat surface
  - urgent-diversion ladder
- right inspector (`416px`) for selected lineage, evidence set, material delta, current safety epoch, and diversion status
- lower grid with one assimilation table and one safety-decision table

Dimensional constraints:
- max width `1560px`
- masthead `76px`
- main visual zone min height `300px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by ingress source, trigger decision, safety state, and urgent-diversion state
- selecting an assimilation or decision row must synchronize ribbon, heat surface, ladder, inspector, and tables
- keyboard navigation across filters, tables, and inspector
- deterministic `data-testid` markers for evidence ribbon, rule heat surface, urgent ladder, assimilation table, safety table, and inspector
- charts and diagrams must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filtering and synchronization across assimilation and safety views
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification

Definition of done:
- The repository has canonical evidence-assimilation and safety orchestration services with deterministic replay-safe behavior.
- Post-submit evidence can no longer bypass immutable snapshot and re-safety law.
- Urgent-diversion, residual-risk, manual-review, and replay-coalesced outcomes are explicit and durable.
- Later patient, workspace, callback, booking, hub, pharmacy, and support tasks can rely on the same safety substrate instead of reinventing risk logic.
```
