# Prompt 097 For 097_par_phase0_track_runtime_implement_wave_observation_policy_and_release_watch_tuple_pipeline

Use the shared operating contract first:
[shared_operating_contract_096_to_105.md](/Users/test/Code/V/prompt/shared_operating_contract_096_to_105.md)

```text
Execute roadmap task 097_par_phase0_track_runtime_implement_wave_observation_policy_and_release_watch_tuple_pipeline.md.

Inherit the Shared_operating_contract_for_prompts_096_to_105 and consume the validated outputs from tasks `001-096`.

Mission:
Implement the authoritative rollout-watch control plane for Vecells.

This task must create the machine-executable pipeline for:
- `ReleaseWatchTuple`
- `WaveObservationPolicy`
- tuple publication and supersession
- watch-tuple drift detection
- observation-window management
- rollback-trigger arming and evaluation
- tuple-aware widening, pause, resume, rollback, and close eligibility checks

This is real runtime and release-control work. Produce schemas, services, validators, action pipelines, observation evaluators, inspection endpoints, and rehearsal tests. Do not leave watch posture, dwell obligations, or tuple drift handling as CI notes, spreadsheets, or runbook prose.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### WaveGuardrailSnapshot`
  - `### ReleaseWatchTuple`
  - `### WaveObservationPolicy`
  - `ReleaseWatchEvidenceCockpit` and related wave-action handoff objects where they clarify tuple lineage and observation semantics
  - the pipeline and verification-ladder rules that require exact tuple publication before canary, widening, or resume
- `phase-0-the-foundation-protocol.md`, especially the release-freeze, route-freeze, assurance-trust, command-settlement, and audit append rules
- `platform-admin-and-config-blueprint.md` for guarded promotion, rollback, kill-switch, and handoff expectations
- `operations-console-frontend-blueprint.md` and `governance-admin-console-frontend-blueprint.md` for watch-surface consumption and bounded recovery posture
- outputs from tasks `015`, `017`, `018`, `051`, `058`, `091`, `094`, `095`, and `096`
- `forensic-audit-findings.md`, especially Findings `91`, `95`, `96`, `103`, `104`, `112`, and `119`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must publish the reusable watch-tuple and observation-policy control plane now so later canary, operational-readiness, governance, and operations tasks can consume it. If a later task owns a richer cockpit or resilience binding, publish the authoritative tuple and observation engine seam here and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build the full watch and observation pipeline now for local and non-production release rehearsals.
- support synthetic promotion intents, simulated wave scopes, synthetic probes, continuity evidence feeds, stale-tuple injection, and rollback-trigger rehearsal
- prove that canary-like progression is blocked unless one exact tuple and one exact observation policy are published and current
- support tuple supersession, narrowing, close-out, and rollback rehearsal without touching production infrastructure
The mock-now path must preserve the exact tuple fields, drift rules, and observation semantics required later in production.

Actual_production_strategy_later:
When production rollout begins, preserve the same tuple schema, observation-policy schema, supersession rules, drift detection, dwell enforcement, and rollback-trigger semantics. Production hardening may add stronger scheduler integration, alerting, and blast-radius data feeds, but it must not allow dashboards or operator memory to replace the authoritative published tuple.

Primary outcome:
The repository gains one authoritative release-watch pipeline that can prove what wave is active, what observation obligations apply, whether the tuple is still exact, and whether widening or rollback is legally allowed.

Implementation deliverables to create:
1. `docs/architecture/97_wave_observation_and_watch_tuple_pipeline.md`
2. `docs/architecture/97_tuple_supersession_and_drift_rules.md`
3. `docs/architecture/97_wave_observation_probe_and_trigger_model.md`
4. `docs/architecture/97_release_watch_pipeline_cockpit.html`
5. `data/analysis/release_watch_tuple_schema.json`
6. `data/analysis/wave_observation_policy_schema.json`
7. `data/analysis/wave_observation_probe_catalog.json`
8. `data/analysis/rollback_trigger_matrix.csv`
9. runtime-control code for tuple generation, publishing, supersession, drift detection, observation scheduling, trigger evaluation, and close-out
10. machine-readable example tuples and policies for preview, integration, and preprod rings
11. `tools/analysis/validate_release_watch_pipeline.(py|ts)`
12. unit, integration, and rehearsal tests for tuple publication, drift, supersession, dwell windows, rollback triggers, and close-out rules
13. `tests/playwright/release-watch-pipeline-cockpit.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- schema-validated `ReleaseWatchTuple` generation with all fields required by the blueprint, including tuple hash composition and tuple state
- schema-validated `WaveObservationPolicy` generation with required probes, required continuity controls, required parity state, required route posture, required provenance state, stabilization criteria, and rollback triggers
- explicit tuple supersession whenever scope, observation duty, guardrail input, recovery disposition, or continuity evidence changes
- drift detection that marks tuples stale when any bound artifact or runtime proof drifts
- observation-window lifecycle control that moves policies through `armed`, `satisfied`, `blocked`, or `superseded`
- action gating so widen, resume, and close are blocked when tuple or policy state is stale, blocked, or superseded
- durable recording of why an observation policy satisfied, blocked, or triggered rollback review
- deterministic audit and timeline append for every tuple publication, supersession, state transition, and trigger evaluation

Implementation instructions:
1. Treat `ReleaseWatchTuple` publication as a first-class release action. Do not bury it inside deployment scripts or ad hoc JSON emitted by CI.
2. Create a tuple-hash composer that uses the canonical tuple members from the blueprint and fails if required inputs are missing.
3. Separate these concerns in code:
   - tuple assembly
   - policy assembly
   - tuple publication / supersession
   - observation scheduling
   - probe evaluation
   - trigger evaluation
   - wave-action eligibility checks
4. Support at least these probe classes in a generic plugin model:
   - publication parity probe
   - provenance verification probe
   - continuity-evidence probe
   - route posture / recovery posture probe
   - assurance slice trust probe
   - synthetic user-journey probe
5. Support at least these rollback-trigger classes:
   - hard parity drift
   - trust freeze or assurance block
   - continuity control regression
   - critical synthetic journey failure
   - route publication or recovery disposition drift
   - operator-approved manual rollback trigger
6. Never allow the following anti-patterns:
   - widening a wave on a stale tuple because telemetry “looks green”
   - extending dwell windows by convention without a superseding policy
   - closing a tuple before the bound observation policy is satisfied
   - treating action acceptance as live convergence without observation evidence
7. Publish browser-viewable inspection surfaces for engineers and reviewers, but keep authoritative state in machine-readable records and services.

Gap handling you must perform during this task:
- If exact probe thresholds or dwell windows are under-specified, publish bounded defaults as `GAP_RESOLUTION_WAVE_POLICY_*` tied to policy data, not hidden constants.
- If later tasks own richer readiness or resilience objects that are not yet implemented, bind to explicit placeholder refs and emit `FOLLOW_ON_DEPENDENCY_READINESS_*` rather than dropping those fields.
- If a wave action changes scope or observation duty, force tuple supersession and record the reason. Never mutate tuple meaning in place.

Backend engineering standards:
- event-sourced or append-only publication history for tuples and policies
- deterministic hashing and canonical serialization
- idempotent publish and supersede actions
- safe concurrency around one active tuple per release-wave step
- explicit scheduling and time-source control for repeatable tests
- metrics for tuple drift, probe lag, blocked policies, supersession rate, and rollback-trigger activation

Verification requirements:
- hash-composition tests
- tuple supersession tests
- blocked-widen and blocked-close tests
- observation-window expiry tests
- rollback-trigger tests
- audit/timeline append tests
- `Playwright_or_other_appropriate_tooling` tests for the cockpit surface, including stable selectors, landmark structure, reduced-motion behavior, and clear distinction between accepted, satisfied, blocked, stale, and rollback-required states

Definition of done:
- every rehearsal wave step can publish one exact watch tuple and one exact observation policy
- tuple drift is detectable and blocks unsafe progression
- observation obligations are explicit, schedulable, and testable
- rollback-trigger logic is data-driven and auditable
- operations and governance consumers can inspect the current tuple without reconstructing it from mixed artifacts
```
