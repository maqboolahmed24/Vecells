# Prompt 041 For 041_seq_phase0_freeze_repository_topology_and_package_boundary_rules

Use the shared operating contract first:
[shared_operating_contract_036_to_045.md](/Users/test/Code/V/prompt/shared_operating_contract_036_to_045.md)

```text
Execute roadmap task 041_seq_phase0_freeze_repository_topology_and_package_boundary_rules.md.

Inherit the Shared_operating_contract_for_prompts_036_to_045 and consume the validated outputs from tasks `001-040`.

Mission:
Freeze the authoritative Vecells repository topology, package boundary rules, bounded-context ownership map, and import-governance model before wider scaffold work begins.

This task is not a generic repo-layout exercise. It must translate the canonical Phase 0 topology law into a machine-enforced repository contract that later tasks `042-045` can implement directly.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `0A. Delivery skeleton and repository architecture`
  - the canonical starter shape (`apps/patient-web`, `apps/clinical-workspace`, `apps/ops-console`, `apps/hub-desk`, `services/api-gateway`, `services/command-api`, `services/projection-worker`, `services/notification-worker`, `services/adapter-simulators`, `packages/domain-kernel`, `packages/event-contracts`, `packages/fhir-mapping`, `packages/design-system`, `packages/authz-policy`, `packages/test-fixtures`, `packages/observability`, `packages/api-contracts`, `packages/release-controls`)
  - the rule that no app can own truth
  - the repo boundary law
  - `BoundedContextDescriptor`
  - `ContextBoundaryContract`
  - the eight boundary/ownership rules listed under the context map
- `blueprint-init.md`, especially `### 2. Core product surfaces` and `### 12. Practical engineering shape`
- `platform-frontend-blueprint.md`, especially shell model, route inventory contract, governance/admin shell contract, assistive companion contract, and frontend/backend integration boundary contract
- `staff-operations-and-support-blueprint.md`, `operations-console-frontend-blueprint.md`, `pharmacy-console-frontend-architecture.md`, `governance-admin-console-frontend-blueprint.md`, and `patient-portal-experience-architecture-blueprint.md` where additional shell families imply repository surfaces beyond the minimal starter shape
- `platform-runtime-and-release-blueprint.md`, especially `RuntimeTopologyManifest`, `GatewayBffSurface`, `FrontendContractManifest`, and `AudienceSurfaceRuntimeBinding`
- `forensic-audit-findings.md`, especially findings `47`, `91-96`, `100`, `107`, `112`, `114`, `118`, and anything that hardens route, topology, trust, continuity, or acting-context ownership
- outputs from tasks `004`, `006`, `007`, `011-016`, and `040`

Primary outcome:
One machine-readable repository topology manifest and one enforceable package-boundary rule set that later scaffolding can implement without ambiguity.

Deliverables to create:
1. `docs/architecture/41_repository_topology_rules.md`
2. `docs/architecture/41_package_boundary_rules.md`
3. `docs/architecture/41_repo_topology_atlas.html`
4. `docs/architecture/41_repo_topology_atlas.mmd`
5. `data/analysis/repo_topology_manifest.json`
6. `data/analysis/package_boundary_rules.csv`
7. `data/analysis/context_boundary_contracts.json`
8. `tools/analysis/validate_repo_topology.(py|ts)`
9. `tools/architecture/dependency_boundary_rules.(json|yaml|js|cjs)`
10. `tests/playwright/repo-topology-atlas.spec.(ts|js)`

Execution steps:
1. Start from the canonical Phase 0 starter shape.
2. Extend it only where later canonical blueprints require additional shell families or surfaces that the starter shape did not enumerate explicitly.
3. Produce one repository topology manifest with rows for every app, service, package, and special workspace.
4. For each row, record:
   - repo path
   - artifact type (`app | service | package | docs-only | tools-only`)
   - owning bounded context or shared-kernel class
   - allowed dependencies
   - forbidden dependencies
   - route families or shell types owned
   - source refs
5. Produce one package-boundary rules file that can later be consumed by lint and CI.
6. Produce explicit context-boundary contracts for every allowed cross-context seam.
7. Produce the HTML atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that every route family, shell family, and bounded context resolves to exactly one owner in the topology manifest.

Mandatory gap closures you must perform:
- Close the “Phase 0 starter shape omitted later shell families” gap by explicitly deciding how support, pharmacy, governance, and other later surfaces are represented in the topology.
- Close the “shared util folder” loophole by making shared code legal only in explicit shared-kernel or published-contract packages.
- Close the “app owns truth” gap by codifying that write-model ownership stays in packages / services, never in shell code.
- Close the “route-family ownership vs app ownership” ambiguity by recording both.
- Close the “context boundary exists only in prose” gap by generating machine-readable contracts.
- Close the “later tasks may scaffold illegal imports by accident” gap by producing rules that lint and CI can enforce.

Non-negotiable rules:
- Every aggregate, route family, gateway surface, and package namespace must resolve to exactly one owner.
- No app, tool, or script may reach into another context’s private internals.
- Shared code is limited to versioned shared-kernel or published-contract packages.
- If a route, service, or package requires more than one context’s private write model, treat that as a topology defect, not a convenience.

Mandatory UI and interaction specification for `41_repo_topology_atlas.html`:
This is a premium internal architecture surface. Name the visual mode `Topology_Atlas`.

Brand and posture:
- use a restrained Vecells wordmark plus a compact `TOPOLOGY` monogram in inline SVG
- tone: architectural, exact, calm, high-trust, not slide-deck-like
- base palette:
  - canvas `#F6F8FB`
  - rail `#EEF2F7`
  - panel `#FFFFFF`
  - inset `#F3F5FA`
  - text strong `#0F172A`
  - text default `#1E293B`
  - text muted `#667085`
  - border subtle `#E2E8F0`
  - border default `#CBD5E1`
  - primary accent `#3559E6`
  - context accent `#0EA5A4`
  - warning accent `#C98900`
  - blocked accent `#C24141`
  - shared accent `#7C3AED`
- typography:
  - system sans stack only
  - mono only for package paths, context codes, and contract IDs

Layout:
- sticky masthead:
  - app count
  - service count
  - package count
  - topology defect count
- left context rail (`296px`): bounded-context cards and shared-kernel cards
- center graph canvas:
  - repository topology graph
  - one restrained context-boundary diagram
- right inspector (`360px`): selected node details, allowed/forbidden imports, route ownership, source refs
- lower section:
  - table parity for graph nodes and edges
  - defect strip

Dimensional constraints:
- max width `1440px`
- graph canvas minimum height `560px`
- node cards `min-height 100px`
- primary controls `44px`
- sticky header height `72px`

Motion:
- `120ms` hover/focus
- `180ms` node selection transition
- `240ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by artifact type, bounded context, and defect state
- keyboard navigation between graph nodes and table rows
- deterministic `data-testid` markers for context rail, graph canvas, node table, inspector, and defect strip
- graph and table parity must stay synchronized

Unique design notes:
- prefer one refined topology graph and one compact boundary legend over many disconnected diagrams
- use subtle monoline node glyphs rather than generic cloud icons
- the atlas should feel like an engineering instrument panel, not a generic knowledge-base page

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filtering
- graph-node selection
- table parity
- inspector rendering
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks

Definition of done:
- Vecells has one frozen repo-topology manifest and one enforceable boundary rule set.
- Every route family, shell family, app, service, and package has an explicit owner.
- Later scaffolding tasks can implement the topology without re-deciding boundary law.
```
