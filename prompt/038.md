# Prompt 038 For 038_seq_external_create_local_adapter_simulator_backlog_for_unavailable_or_manual_integrations

Use the shared operating contract first:
[shared_operating_contract_036_to_045.md](/Users/test/Code/V/prompt/shared_operating_contract_036_to_045.md)

```text
Execute roadmap task 038_seq_external_create_local_adapter_simulator_backlog_for_unavailable_or_manual_integrations.md.

Inherit the Shared_operating_contract_for_prompts_036_to_045 and consume the validated outputs from tasks `001-037`.

Mission:
Create the authoritative local adapter-simulator backlog for Vecells in two explicit parts:

1. a now-executable simulator backlog and contract manifest for all unavailable, manual, delayed, or gated integrations
2. a later real-provider replacement / coexistence strategy that shows how each simulator graduates, stays hybrid, or remains a permanent fallback

This task must convert “we will mock it for now” from a vague statement into a governed implementation queue with fidelity targets, proof semantics, and exit criteria.

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 6. Booking and access continuity`
  - `### 7. Pharmacy First pathway`
  - `### 8. Data and platform architecture`
  - `### 12. Practical engineering shape`
  - the rule that Vecells is modular, contract-first, and must keep supplier-specific behavior behind adapters
- `phase-0-the-foundation-protocol.md`, especially:
  - `0A. Delivery skeleton and repository architecture`
  - `BoundedContextDescriptor`
  - `ContextBoundaryContract`
  - `CanonicalEventContract`
  - `1.10B ProviderCapabilitySnapshot`
  - `1.10A ExternalConfirmationGate`
  - `1.11 PharmacyDispatchEnvelope` and downstream proof objects
  - any rule that requires degraded, ambiguous, or manual fallback states to be first-class
- `phase-2-the-identity-and-echoes.md`, `phase-4-the-booking-engine.md`, `phase-5-the-network-horizon.md`, `phase-6-the-pharmacy-loop.md`, and `phase-7-inside-the-nhs-app.md` where adapter seams, external dependencies, or channel bridges are defined
- `platform-runtime-and-release-blueprint.md`, especially `AdapterContractProfile`, `DependencyDegradationProfile`, `RuntimeTopologyManifest`, `GatewayBffSurface`, `ClientCachePolicy`, and verification-ladder rules
- `forensic-audit-findings.md`, especially findings on duplicate control, fallback review, confirmation ambiguity, transport proof, closure blockers, and route/trust/release hardening
- outputs from tasks `008`, `021-037`, especially the integration inventory, provider scorecards, sandbox-account strategy, and the GP/pharmacy path packs

Primary outcome:
A prioritized simulator backlog that tells Vecells what to build now, what fidelity each simulator must reach, which proof semantics cannot be faked away, and how each simulator maps to later real-provider adoption.

Deliverables to create:
1. `docs/external/38_local_adapter_simulator_backlog.md`
2. `docs/external/38_local_adapter_simulator_execution_order.md`
3. `docs/external/38_real_provider_delta_register.md`
4. `docs/external/38_simulator_fidelity_policy.md`
5. `docs/external/38_simulator_backlog_studio.html`
6. `data/analysis/adapter_simulator_backlog.csv`
7. `data/analysis/adapter_simulator_priority_scores.json`
8. `data/analysis/adapter_simulator_contract_manifest.json`
9. `data/analysis/adapter_real_provider_gap_map.json`
10. `services/adapter-simulators/manifests/README.md`
11. `tools/analysis/validate_adapter_simulator_backlog.(py|ts)`
12. `tests/playwright/adapter-simulator-backlog-studio.spec.(ts|js)`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build the now-executable simulator backlog and contract manifest. At minimum, include rows for:
- NHS login auth/session twin
- IM1 / GP principal-system booking twin(s)
- optional PDS enrichment twin
- MESH message path twin
- NHS App sandpit/embedded bridge twin
- telephony / IVR twin
- SMS provider twin
- email notification twin
- transcription twin
- malware / artifact scanning twin
- pharmacy directory twin
- pharmacy update-record / visibility observation twin
- booking provider confirmation twin
- support replay / resend provider twin where relevant

For each simulator backlog item, capture:
- simulator ID
- canonical dependency name
- owning bounded context(s)
- priority score
- current blocker removed by the simulator
- minimum fidelity class (`shape_only | workflow_twin | proof_twin | fault_injection_twin | near-live_contract_twin`)
- authoritative proof semantics that must be preserved
- ambiguity / degraded behaviors that must be simulated
- route families or surfaces affected
- test types required
- graduation or replacement trigger
- permanent fallback status yes/no
- linked actual-provider task(s)

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the later real-provider delta register. For each simulator, explicitly state:
- whether it will be replaced, remain hybrid, or remain permanent fallback
- what real onboarding or contract evidence will be required
- what parts of the simulator contract must remain unchanged when the live provider arrives
- what parts may be provider-specific
- what migration and coexistence tests will be needed
- what live gate blocks real replacement today

Execution steps:
1. Start from tasks `008` and `021-037`.
2. Inventory every external dependency that the current roadmap touches but cannot yet use live.
3. Score each dependency by baseline criticality, blocker severity, implementation lead time, and proof sensitivity.
4. Build the simulator backlog and execution order.
5. For every backlog row, declare the minimum fidelity needed to preserve blueprint truth.
6. Build the HTML studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Create the real-provider delta register from the same manifest.
8. Validate that no simulator row lacks a later live strategy or explicit permanent-fallback rationale.

Mandatory gap closures you must perform:
- Close the “mock later” vagueness gap by creating a concrete backlog with priority and fidelity.
- Close the “simulator only needs happy path” gap by requiring ambiguity, degraded, expiry, and manual fallback behaviors.
- Close the “simulator contract will drift from live contract” gap by creating one shared contract manifest and one delta register.
- Close the “all simulators deserve the same effort” gap by explicitly ranking them.
- Close the “provider unavailable means product logic may defer proof semantics” gap by preserving proof and blocker semantics in the simulator.
- Close the “hybrid forever” ambiguity by explicitly marking permanent-fallback or coexistence cases.

Non-negotiable rules:
- Do not let simulator backlog rows collapse multiple dependencies into a single generic mock if they have materially different proof or ambiguity semantics.
- Do not allow a simulator to return optimistic success when the blueprint requires confirmation ambiguity, dispatch proof, or manual fallback.
- Do not keep simulator scope implicit; every simulator needs clear exit criteria or a permanent-fallback rationale.
- Do not bury simulator-specific assumptions in prose only; they must live in the manifest.

Mandatory UI and interaction specification for `38_simulator_backlog_studio.html`:
This is a premium internal planning-and-architecture surface. Name the visual mode `Simulator_Foundry_Board`.

Brand and posture:
- use a restrained Vecells wordmark plus a compact `SIM_FOUNDRY` monogram in inline SVG
- tone: precise, analytical, calm, operator-grade, visibly prioritization-oriented
- base palette:
  - canvas `#F6F7FB`
  - rail `#EEF1F7`
  - panel `#FFFFFF`
  - inset `#F3F5FA`
  - text strong `#111827`
  - text default `#1F2937`
  - text muted `#667085`
  - border subtle `#E5E7EB`
  - border default `#CBD5E1`
  - primary accent `#4F46E5`
  - fidelity accent `#0EA5A4`
  - priority accent `#2563EB`
  - blocked accent `#C24141`
  - fallback accent `#D97706`
- typography:
  - system sans stack only
  - mono only for simulator IDs, dependency codes, and manifest keys

Layout:
- sticky masthead:
  - simulator count
  - baseline-critical count
  - proof-twin count
  - unresolved live blockers count
- left filter rail (`296px`): dependency families, priority tiers, fidelity classes
- center workspace:
  - prioritized backlog table
  - fidelity legend cards
  - one restrained replacement-path diagram showing `Simulator -> Hybrid -> Live` states
- right inspector (`360px`): selected simulator detail, proof semantics, migration notes, blockers
- lower section:
  - table parity for diagrams
  - execution-order strip

Dimensional constraints:
- max width `1440px`
- backlog row height `52px`
- filter cards `min-height 96px`
- primary controls `44px`
- sticky header height `72px`

Motion:
- `120ms` hover/focus
- `180ms` filter transition
- `240ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by priority, fidelity, phase, and dependency family
- sortable backlog columns
- visible blocker chips
- keyboard-first table interaction
- deterministic `data-testid` markers for filter rail, backlog table, fidelity legend, inspector, and execution strip

Unique design notes:
- include one compact priority-versus-fidelity scatter or matrix only if table parity is immediate
- avoid kanban cliché styling; this should read as an engineering foundry, not a task app
- the inspector should feel evidence-heavy, not decorative

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filtering
- sorting
- row selection
- inspector rendering
- blocker and priority badges
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has one prioritized simulator backlog aligned to the blueprint and to later provider adoption.
- Every simulator row preserves the proof, ambiguity, degraded, and manual-fallback semantics that matter.
- Simulator-now and actual-later strategies stay aligned through one manifest and one delta register.
```
