# Prompt 021 For 021_seq_external_inventory_rank_required_vs_optional_integrations

Use the shared operating contract first:
[shared_operating_contract_021_to_025.md](/Users/test/Code/V/prompt/shared_operating_contract_021_to_025.md)

```text
Execute roadmap task 021_seq_external_inventory_rank_required_vs_optional_integrations.md.

Inherit the Shared_operating_contract_for_prompts_021_to_025 and consume the validated outputs from tasks `001-020`, especially `008`, `018`, `019`, and `020`.

Mission:
Take the already-inventoried external dependency set and turn it into one authoritative, evidence-driven integration priority model for Vecells.

This task is not just “rank APIs”. It must decide:
- which integrations are truly required to build the current MVP / baseline
- which are required only as high-fidelity mocks right now
- which can remain strategy-only until product, assurance, or sponsor prerequisites exist
- which are optional, feature-flagged, deferred, or prohibited
- which dependencies are so control-plane critical that even the mock version must be unusually faithful
- which roadmap tasks, gates, and risks each dependency now influences

You must explicitly honor the user’s operating constraint:
Vecells needs high-quality mock services first because several real NHS / partner onboarding routes are not realistically available before there is an MVP and a credible demonstration pack. This task must therefore separate “must simulate now” from “must acquire later” without weakening the architectural contract.

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 1. Product definition`
  - `### 2. Core product surfaces`
  - `### 3. The canonical request model`
  - `### 6. Booking and access continuity`
  - `### 7. Pharmacy First pathway`
  - `### 10. Identity, consent, security, and policy`
  - `### 12. Practical engineering shape`
  - `### 14. Programme Baseline With NHS App Deferred`
- `phase-0-the-foundation-protocol.md`, especially:
  - `1.4D AuthTransaction`
  - `1.4E AuthScopeBundle`
  - `1.13A ProviderCapabilityMatrix`
  - `1.13B BookingProviderAdapterBinding`
  - `1.17 ExternalConfirmationGate` and related reservation / confirmation objects
  - adapter-boundary, confirmation-truth, and degraded-mode rules
  - the hard law that browser clients do not directly call partner systems
- `phase-2-the-identity-and-echoes.md`, especially:
  - `2A. Trust contract and capability gates`
  - `2B. NHS login bridge and local session engine`
  - `2C. Patient linkage, demographic confidence, and optional PDS enrichment`
  - `2E. Telephony edge, IVR choreography, and call-session persistence`
  - `2F. Caller verification, voice capture, transcript stub, and SMS continuation`
  - the explicit rule to keep IM1 out of the critical path for this phase
- `phase-4-the-booking-engine.md`, especially:
  - `4B. Provider capability matrix and adapter seam`
  - `4C. Slot search, normalisation, and availability snapshots`
  - `4E. Commit path, revalidation, booking record, and compensation`
- `phase-5-the-network-horizon.md`, especially:
  - `5C. Enhanced Access policy engine and network capacity ingestion`
  - `5F. Native hub booking commit, practice continuity, and cross-org messaging`
  - `5G. No-slot handling, urgent bounce-back, callback fallback, and reopen mechanics`
- `phase-6-the-pharmacy-loop.md`, especially:
  - `6C. Pharmacy discovery, provider choice, and directory abstraction`
  - `6D. Referral pack composer, dispatch adapters, and transport contract`
  - `6F. Outcome ingest, Update Record observation, and reconciliation`
  - `6G. Bounce-back, urgent return, and reopen mechanics`
- `phase-7-inside-the-nhs-app.md` only as deferred-channel input
- `platform-runtime-and-release-blueprint.md`, especially:
  - `DependencyDegradationProfile`
  - `AdapterContractProfile`
  - environment ring and promotion contract
- `phase-cards.md` for current-baseline vs deferred scope
- `forensic-audit-findings.md`, especially findings that harden:
  - supplier truth vs optimistic acceptance
  - confirmation ambiguity
  - weak-mailbox or weak-transport evidence
  - closure blockers tied to unresolved external proof

Primary outcome:
One implementation-grade integration priority model that lets the programme move without confusing:
- current MVP blockers
- mock-first implementation needs
- live-provider onboarding needs
- optional enrichments
- deferred channel work

Deliverables to create:
1. `docs/external/21_integration_priority_and_execution_matrix.md`
2. `docs/external/21_mock_first_vs_actual_later_strategy.md`
3. `docs/external/21_integration_priority_rationale.md`
4. `docs/external/21_mock_live_divergence_register.md`
5. `docs/external/21_external_integration_priority_cockpit.html`
6. `data/analysis/integration_priority_matrix.csv`
7. `data/analysis/integration_priority_matrix.json`
8. `data/analysis/integration_priority_scores.csv`
9. `data/analysis/mock_live_lane_assignments.json`
10. `data/analysis/integration_divergence_register.csv`
11. `tools/analysis/build_integration_priority_model.(py|ts)`
12. `tools/analysis/validate_integration_priority_model.(py|ts)`

Every integration-priority row must capture at minimum:
- `integration_id`
- `integration_family`
- `integration_name`
- `source_dependency_ids[]`
- `baseline_role = baseline_required | baseline_mock_required | optional_flagged | deferred_channel | future_optional | prohibited`
- `authoritative_truth_class`
- `patient_safety_impact = none | low | medium | high | critical`
- `control_plane_impact = none | low | medium | high | critical`
- `lineage_or_closure_dependency = none | indirect | direct`
- `channel_dependency_classes[]`
- `bounded_context_refs[]`
- `current_mock_feasibility = high | medium | low | unacceptable`
- `live_onboarding_latency_band = unknown | short | medium | long | contract_heavy`
- `sponsor_or_assurance_gate = none | likely | required`
- `recommended_lane = mock_now | hybrid_mock_then_live | actual_later | deferred`
- `why_mock_now`
- `why_actual_later`
- `minimum_mock_fidelity`
- `minimum_live_readiness_conditions[]`
- `later_task_refs[]`
- `risk_refs[]`
- `notes`

Scoring model requirements:
Build and serialize a deterministic weighted model for ranking integration families. At minimum score:
- patient-safety consequence if absent
- effect on canonical request / lineage / closure truth
- effect on patient-visible continuity
- effect on operator truth and supportability
- mockability quality
- live acquisition latency
- external approval or sponsorship burden
- security/privacy burden
- risk of architectural coupling if delayed
- readiness value unlocked for subsequent tasks

Section A — `Mock_now_execution`
This section is mandatory.

You must produce a mock-first execution ranking that says, with no ambiguity:
- which integrations must be simulated immediately to let product and platform work proceed
- which simulations must be high fidelity because they touch trust, proof, or patient-visible truth
- which mocks may be thinner because they only unblock non-authoritative flows
- what the quality bar is for each mock family
- what evidence will prove that the mock is good enough for MVP-phase development

The mock-first ranking must explicitly evaluate at minimum:
- NHS login
- optional PDS enrichment
- telephony / IVR / recording / transcript backplane
- SMS continuation
- email / notification rails
- GP / IM1 booking / patient-facing capability seam
- MESH or comparable cross-org messaging seam
- pharmacy directory / provider discovery
- pharmacy dispatch / proof / urgent return channel
- pharmacy outcome / Update Record observation seam
- NHS App embedded channel dependencies as deferred but inventoried
- assistive-layer model vendor dependencies as bounded and optional to core completeness

For each of these, define:
- what the mock must simulate faithfully
- what may remain placeholder-only
- what failure or ambiguity states must be injected
- what cannot be treated as authoritative even in the mock

Section B — `Actual_provider_strategy_later`
This section is mandatory.

You must produce the later live-provider acquisition ranking that says:
- which integrations should be sought first once MVP evidence exists
- which require sponsor, commissioner, safety, legal, or assurance prerequisites before realistic onboarding
- which are live-production concerns only and should not block current build
- which can use a mock until late pilot or release waves
- what “ready to attempt live onboarding” means for each dependency family

Execution steps:
1. Start from task `008`’s dependency inventory and task `005`’s external touchpoint matrix.
2. Collapse duplicate naming and re-key everything to one stable integration-family model.
3. Build one weighted priority model and run it twice:
   - once for `mock_now_execution`
   - once for `actual_provider_strategy_later`
4. Explicitly compare the two ranked outputs. Do not assume they are the same.
5. Build a divergence register showing where:
   - the mock is mandatory now but live onboarding can wait
   - live onboarding is critical later but not a current implementation blocker
   - the mock must model proof/ambiguity more deeply than the real provider’s public docs describe
6. Add clear sequencing recommendations for tasks `024-040` and the later Phase 0 simulator work, especially:
   - `024-025` NHS login
   - `026-030` IM1 / PDS / MESH / NHS App-related onboarding
   - `031-035` commercial vendor provisioning families
   - `036-040` provider-path discovery, simulator backlog, manual checkpoints, and degraded defaults
7. Generate an operator-friendly cockpit page for review.

Mandatory gap closures you must perform:
- Close the “inventory exists but no ordering law exists” gap.
- Close the “real provider unavailable pre-MVP means work stalls” gap by creating a mock-first ranking.
- Close the “optional enrichment vs baseline rail” gap, especially:
  - NHS login is a core identity rail
  - PDS enrichment is optional / feature-flagged
  - NHS App embedded channel remains deferred for current baseline
  - assistive vendor dependencies remain bounded and optional to core functional completeness
- Close the “proof-bearing integrations and simple transport integrations are ranked as if equivalent” gap.
- Close the “mock can be low-fidelity even when it drives patient-visible truth” gap.
- Close the “supplier-specific business logic might leak into priority decisions” gap by keeping priorities family-based and adapter-bound.

Non-negotiable ranking rules:
- do not treat deferred Phase 7 NHS App dependencies as current-baseline blockers
- do not rank PDS higher than the core identity rail for current baseline
- do not treat IM1 linkage or provider APIs as a prerequisite for Phase 2 identity completion when the blueprint explicitly keeps IM1 out of the critical path there
- do not let notification acceptance outrank authoritative booking, hub, or pharmacy truth
- do not let any integration with weak proof semantics be classed as “done” without a matching fallback or confirmation gate
- do not let a mock be approved if it cannot preserve authoritative proof vs ambiguity vs degraded fallback
- do not let vendor glamour, market awareness, or contract reputation replace blueprint-grounded control-plane need

The cockpit UI requirements for `21_external_integration_priority_cockpit.html`:
Name the visual mode `Integration_Constellation_Board`.

This must feel like a premium internal strategy board, not a spreadsheet export.

Visual direction:
- canvas `#F4F7FB`
- shell `#FFFFFF`
- inset `#EEF2F7`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#475569`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- mock lane accent `#335CFF`
- actual lane accent `#7C3AED`
- deferred accent `#94A3B8`
- warning accent `#C98900`
- blocked accent `#C24141`
- success accent `#0F9D58`

Typography and rhythm:
- system sans stack only
- mono only for IDs, hashes, environment keys, and task refs
- page max width `1440px`
- header block with title, baseline-scope ribbon, and source-freshness digest
- left filter rail for integration families and lane chips
- center upper quadrant chart:
  - x-axis = live acquisition friction
  - y-axis = MVP/control-plane necessity
  - each node labeled and color-coded by lane
  - always provide a table fallback under the chart
- center lower lane swimlanes:
  - `mock_now`
  - `hybrid`
  - `actual_later`
  - `deferred`
- right inspector:
  - selected integration rationale
  - proof / ambiguity summary
  - mock quality bar
  - actual readiness gates
- lower panels:
  - divergence register
  - later-task linkage table
  - top risks list

Dimensional constraints:
- quadrant chart min height `420px`
- swimlane board min height `360px`
- inspector width `360px` target
- risk list min height `220px`
- chip height `28px`
- filter controls `44px`

Motion:
- `120ms` hover/focus
- `180ms` filter and tab transitions
- `240ms` inspector transitions
- reduced-motion support mandatory

Unique design notes:
- include one small inline SVG “constellation” motif tied to integration nodes
- avoid cloud logos, API-brand sticker walls, or decorative vendor icons
- use restrained connection lines and confidence halos only where they clarify priority

Playwright requirements:
Validate the cockpit with `Playwright_or_other_appropriate_tooling` for:
- filter behavior
- quadrant/table parity
- lane drag-free navigation (read-only, not editable)
- deep-linking into an integration card
- responsive layout
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- Vecells has one ranked integration model for mock-now work and one for later live-provider work.
- The ranking is deterministic, source-traceable, and constrained by the blueprint rather than intuition.
- Every major integration family has an explicit lane and rationale.
- Later tasks can proceed without re-arguing which integrations truly block MVP and which belong in the mock-first programme.
```