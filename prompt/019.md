# Prompt 019 For 019_seq_create_requirement_to_task_traceability_map

Use the shared operating contract first:
[shared_operating_contract_016_to_020.md](/Users/test/Code/V/prompt/shared_operating_contract_016_to_020.md)

```text
Execute roadmap task 019_seq_create_requirement_to_task_traceability_map.md.

Inherit the Shared_operating_contract_for_prompts_016_to_020 and consume the validated outputs from tasks 001-018.

Mission:
Create the canonical requirement-to-task traceability model for the full Vecells roadmap.

This task must connect:
- the requirement registry from task 001
- the source reconciliation from task 002
- the scope and audience inventory from tasks 003-004
- the lineage / endpoint / object / state / dependency models from tasks 005-010
- the architecture and tooling baselines from tasks 011-016
- the programme milestone graph and risk/watch posture from tasks 017-018
- the actual roadmap serialization in `prompt/checklist.md`
- the actual implementation prompt files in `prompt/*.md`

The resulting traceability map must allow later agents and reviewers to answer:
- which task defines a requirement
- which task implements it
- which task tests it
- which task gates it
- which task provides operational or assurance evidence for it
- which requirements are orphaned, under-covered, over-bundled, deferred, or only partially represented

Algorithm grounding you must honor:
- `prompt/AGENT.md` and `prompt/checklist.md` as the live roadmap serialization; do not invent a parallel task universe
- task 001 requirement registry as the canonical requirement source
- task 002 canonical alias map and conformance seed
- task 003 scope boundary and deferred-scope rules
- task 004 persona / audience / shell / route inventory
- task 005 endpoint matrix and patient-action route-binding matrix
- task 006 object catalog and task 007 state / invariant atlas
- task 008 dependency inventory and task 018 risk / watchlist outputs
- `phase-cards.md`, especially:
  - the current baseline and deferred Phase 7 statement
  - cross-phase conformance expectations
- `phase-9-the-assurance-ledger.md`, especially:
  - `PhaseConformanceRow`
  - `CrossPhaseConformanceScorecard`
  - BAU readiness expectations that require machine-auditable coverage
- `forensic-audit-findings.md`, especially any finding that revealed a missing control, missing invariant, or missing continuity / trust / publication coverage

Primary outcome:
One machine-readable and explorable map that ties the source algorithm’s requirements to the roadmap tasks that must fulfill them.

Deliverables to create:
1. `docs/analysis/19_requirement_to_task_traceability_map.md`
2. `docs/analysis/19_traceability_rules_and_coverage_model.md`
3. `docs/analysis/19_requirement_coverage_gap_report.md`
4. `docs/analysis/19_traceability_decision_log.md`
5. `docs/analysis/19_traceability_explorer.html`
6. `data/analysis/requirement_task_traceability.csv`
7. `data/analysis/task_requirement_traceability.csv`
8. `data/analysis/coverage_summary.json`
9. `data/analysis/orphan_requirements.csv`
10. `data/analysis/orphan_tasks.csv`
11. `data/analysis/coverage_by_phase.csv`
12. `data/analysis/coverage_by_domain.csv`
13. `tools/analysis/build_traceability_explorer.(py|ts)`
14. `tools/analysis/validate_traceability_map.(py|ts)`

Each requirement-to-task row must capture at minimum:
- `traceability_id`
- `requirement_id`
- `task_id`
- `task_title`
- `task_prompt_ref`
- `mapping_type = define | refine | architecture_decision | implement | integrate | frontend_surface | test | gate | release | assurance_evidence | deferred_placeholder`
- `coverage_strength = direct | supporting | partial | inferred_gap_closure`
- `baseline_scope = current | deferred | optional | out_of_scope`
- `phase_ref`
- `domain_ref`
- `persona_refs[]`
- `channel_refs[]`
- `object_refs[]`
- `state_or_invariant_refs[]`
- `dependency_refs[]`
- `risk_refs[]`
- `source_requirement_refs[]`
- `reason`
- `notes`

Each task-to-requirement summary row must capture at minimum:
- `task_id`
- `task_title`
- `total_requirement_count`
- `direct_requirement_count`
- `test_requirement_count`
- `gate_requirement_count`
- `assurance_requirement_count`
- `orphan_state = none | weakly_grounded | ungrounded`
- `notes`

Execution steps:
1. Parse the requirement registry from task 001 and the live task inventory from `prompt/checklist.md`.
2. Parse all existing numbered prompt files in `prompt/`. Use them as the task-spec layer, but do not let prompt prose override the checklist or canonical blueprint sources.
3. Build a canonical task catalog from the checklist with at least:
   - task id
   - sequence / parallel class
   - phase family
   - task title
   - prompt path
   - baseline scope
4. Build the traceability ontology. At minimum separate these mapping types:
   - `define` for registry / glossary / scope / model-defining tasks
   - `architecture_decision` for baseline and ADR-freezing tasks
   - `implement` for tasks that create code or canonical artifacts
   - `integrate` for tasks that join internal systems or external dependencies
   - `frontend_surface` for audience-facing or operator-facing UI tasks
   - `test` for tasks whose primary purpose is verification
   - `gate` for merge / readiness / approval tasks
   - `release` for rollout and publication tasks
   - `assurance_evidence` for tasks whose primary role is operational / governance proof
   - `deferred_placeholder` for deferred scope representation such as Phase 7 baseline handling
5. For every requirement, map the minimum required task coverage chain. At minimum determine:
   - where it is first defined
   - where architecture decisions affecting it are frozen
   - where it is implemented or represented
   - where it is integrated
   - where it is tested
   - where it is gated or evidenced
6. For every task, determine whether it is:
   - tightly grounded in canonical requirements
   - only indirectly grounded
   - currently orphaned
   - too broad and needs decomposition notes
7. Compute coverage summaries by:
   - phase
   - domain
   - persona
   - channel
   - object family
   - requirement type
   - baseline vs deferred scope
8. Emit explicit gap reports for:
   - requirements with no implementing task
   - requirements with no testing task
   - requirements with no gate/evidence task where the corpus clearly requires one
   - tasks with weak or absent requirement grounding
   - deferred requirements incorrectly mapped into current-baseline delivery
9. Add validator logic that fails when:
   - a canonical requirement has no task coverage at all
   - a canonical invariant has no testing or gate coverage
   - a current-baseline external dependency requirement has no inventory/provisioning or simulator-related task
   - a mutating patient/staff/operator route requirement has no route / settlement / release / trust coverage in the mapped task chain
   - a task is mapped only to vague summary prose when stronger canonical sources exist
   - an orphan task remains unexplained

Mandatory traceability analyses you must perform:
- requirement -> task coverage chain by lifecycle stage
- task -> requirement grounding strength
- coverage density by phase and domain
- current-baseline vs deferred-scope separation
- cross-phase control-family coverage, especially:
  - continuity proof
  - release / watch tuple
  - trust slices
  - resilience readiness
  - governance / audit / evidence graph
- external dependency coverage from inventory through provisioning / simulator / integration / test / gate

Mandatory gap closures you must perform:
- Close the “requirements exist but are not linked to execution tasks” gap.
- Close the “checklist tasks exist but are not tied back to canonical requirements” gap.
- Close the “deferred Phase 7 tasks can accidentally appear as current-baseline coverage” gap.
- Close the “tests and gates are present in the roadmap but not linked to the invariants they prove” gap.
- Close the “cross-phase controls can disappear into phase-local tasks” gap.
- Close the “orphan task titles can drift away from the source algorithm” gap.
- Close the “frontend route / shell requirements can appear covered without release, trust, or Playwright verification coverage” gap.

Non-negotiable traceability rules you must encode:
- every canonical requirement must trace to at least one task
- every canonical invariant must trace to at least one proving or gating task
- every current-baseline requirement must have a current-baseline task chain unless explicitly blocked or deferred with rationale
- no deferred-only task may be counted as current-baseline completion evidence
- no task may be considered well grounded when it only references summary prose and ignores stronger canonical docs
- no external dependency requirement may be counted as covered if it lacks either provisioning / simulator work or an explicit non-applicability decision
- no frontend requirement involving continuity, shells, artifacts, or writable posture may be counted as covered without verification coverage that includes `Playwright_or_other_appropriate_tooling` where relevant in the roadmap

Mandatory UI and interaction specification for `19_traceability_explorer.html`:
The explorer should feel like a premium coverage-analysis workstation, not a graph demo.

Visual direction:
- name the visual mode `Lineage_Loom`
- tone: precise, analytic, calm, source-traceable
- base palette:
  - canvas `#F5F7FA`
  - shell `#FFFFFF`
  - inset `#EEF2F6`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#475467`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - trace accent `#335CFF`
  - relation accent `#0F8B8D`
  - gate accent `#6E59D9`
  - warning `#C98900`
  - critical `#C24141`
- typography: system-sans with mono role for IDs only
- layout:
  - left filter rail for phase / domain / mapping type / baseline scope
  - center split with trace graph and matrix view
  - lower tab strip for orphan requirements, orphan tasks, and coverage summaries
  - right inspector for selected requirement or task
- dimensional constraints:
  - graph canvas min height `520px`
  - matrix table min height `420px`
  - inspector width `360-440px`
  - chips `28px` height
- motion:
  - `120ms` hover/focus
  - `180ms` filter transitions
  - `240ms` inspector transitions
  - reduced motion support mandatory
- diagrams/charts:
  - include one requirement-task graph and one coverage matrix
  - every graph / chart must have table/list parity
- branding:
  - understated inline-SVG Vecells monogram in the header
- DOM markers:
  - `data-testid="trace-filter-rail"`
  - `data-testid="trace-graph-canvas"`
  - `data-testid="trace-matrix-table"`
  - `data-testid="coverage-summary-panel"`
  - `data-testid="trace-inspector"`
  - stable `data-requirement-id`, `data-task-id`, `data-mapping-type`, `data-baseline-scope`, and `data-coverage-strength` markers

Playwright_or_other_appropriate_tooling requirements:
Use `Playwright_or_other_appropriate_tooling` as a first-class development and testing driver for the explorer. Minimum required coverage:
- offline load
- filter interaction across phase / domain / mapping type / baseline scope
- open inspector from requirement row, task row, and graph edge
- keyboard navigation across graph, matrix, tabs, and inspector
- responsive behavior at `768px`, `1024px`, and `1440px`
- reduced-motion mode
- stable DOM markers for requirement rows, task rows, graph nodes, and edge highlights
- graph/table parity checks
- accessibility smoke checks
- no remote assets

Definition of done:
- Every canonical requirement can be traced to the roadmap tasks that cover it.
- Coverage gaps and orphan tasks are explicit and machine-readable.
- Deferred scope is separated cleanly from the current baseline.
- Later agents can use the traceability map to justify work, detect missing coverage, and prove that implementation and testing tasks actually connect back to the source algorithm.
```
