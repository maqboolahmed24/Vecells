# Prompt 010 For 010_seq_define_data_classification_phi_masking_and_audit_posture

Use the shared operating contract first:
[shared_operating_contract_006_to_010.md](/Users/test/Code/V/prompt/shared_operating_contract_006_to_010.md)

```text
Execute roadmap task 010_seq_define_data_classification_phi_masking_and_audit_posture.md.

Inherit the Shared_operating_contract_for_prompts_006_to_010 and consume the validated outputs from tasks 001-009.

Mission:
Define the canonical data-classification model, PHI/PII masking strategy, audience-specific disclosure ceilings, audit/event disclosure posture, break-glass scope rules, and retention-sensitive artifact sensitivity matrix for Vecells.

This task must make it impossible for later engineering work to treat privacy, masking, telemetry redaction, or audit scope as local UI or API choices. These are platform contracts.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `1.17 VisibilityProjectionPolicy`
  - `1.17A SectionVisibilityContract`
  - `1.17B PreviewVisibilityContract`
  - `1.17C MinimumNecessaryContract`
  - `1.17D AudienceVisibilityCoverage`
  - safety/identity/access objects where raw evidence vs masked references are separated
- `patient-account-and-communications-blueprint.md`, especially:
  - patient_public vs patient_authenticated separation
  - patient communication visibility projections
  - wrong-patient hold behavior
  - artifact preview and placeholder discipline
- `staff-operations-and-support-blueprint.md`, especially:
  - audience tiers and support/break-glass posture
- `platform-frontend-blueprint.md`, especially:
  - shell continuity and visibility
  - artifact presentation and parity
  - `UIEventVisibilityProfile`
  - PHI-safe telemetry requirements
  - support replay / restore masking rules
- `phase-2-the-identity-and-echoes.md`, especially:
  - raw identity evidence stored outside hot operational records
  - events carrying references/hashes/masked forms only
  - local session and contact-claim separation
- `phase-7-inside-the-nhs-app.md`, especially:
  - embedded PHI restrictions and cache/download constraints
- `phase-9-the-assurance-ledger.md`, especially:
  - assurance ledger entries
  - investigation scope envelope
  - audit query / replay / break-glass review
  - retention lifecycle, legal holds, deletion certificates, archive manifests
- `accessibility-and-content-system-contract.md`
- `platform-runtime-and-release-blueprint.md` where trust zones, tenant isolation, and browser boundaries constrain disclosure
- `forensic-audit-findings.md`, especially Findings:
  - 41-47
  - 48-56
  - 86-103 where visibility, placeholders, continuity, and audit/replay scope are hardened

Deliverables to create:
1. `docs/architecture/10_data_classification_model.md`
2. `docs/architecture/10_phi_masking_and_redaction_policy.md`
3. `docs/architecture/10_audit_posture_and_event_disclosure.md`
4. `docs/architecture/10_break_glass_and_investigation_scope_rules.md`
5. `docs/architecture/10_retention_and_artifact_sensitivity_matrix.md`
6. `docs/architecture/10_data_classification_atlas.html`
7. `data/analysis/data_classification_matrix.csv`
8. `data/analysis/field_sensitivity_catalog.json`
9. `data/analysis/redaction_policy_matrix.csv`
10. `data/analysis/audit_event_disclosure_matrix.csv`
11. `data/analysis/break_glass_scope_rules.json`
12. `data/analysis/artifact_sensitivity_matrix.csv`
13. `tools/analysis/build_data_classification_model.(py|ts)`
14. `tools/analysis/validate_data_classification_model.(py|ts)`

You must define at minimum:
- data sensitivity classes
- audience tiers and purpose-of-use pairings
- disclosure ceilings for previews, sections, artifacts, and telemetry
- redaction policy families
- audit/export/replay masking rules
- break-glass widening rules
- retention-sensitive classes for durable artifacts
- event/log/telemetry disclosure policy

Suggested top-level sensitivity classes:
- `public_safe`
- `operational_internal_non_phi`
- `patient_identifying`
- `contact_sensitive`
- `clinical_sensitive`
- `identity_proof_sensitive`
- `security_or_secret_sensitive`
- `audit_investigation_restricted`
- `retention_governance_restricted`

You may refine or split these if the corpus requires it, but do not collapse them into a single “sensitive” bucket.

Each field/classification row must capture:
- `field_or_artifact_id`
- `canonical_object_id`
- `field_name_or_artifact_class`
- `sensitivity_class`
- `contains_phi = yes | no | contextual`
- `allowed_audience_tiers[]`
- `allowed_purposes_of_use[]`
- `preview_ceiling`
- `detail_ceiling`
- `artifact_ceiling`
- `telemetry_ceiling`
- `log_ceiling`
- `audit_replay_ceiling`
- `redaction_policy_ref`
- `break_glass_behavior`
- `retention_class_ref_if_artifact`
- `source_refs[]`
- `notes`

Execution steps:
1. Start from the object catalog and audience-surface inventory already created in earlier tasks.
2. Build a classification matrix that distinguishes between:
   - object schema classification
   - route/surface disclosure classification
   - telemetry/log/event classification
   - artifact/export/replay classification
3. Map data classes onto the canonical visibility model:
   - `VisibilityProjectionPolicy`
   - section contracts
   - preview contracts
   - minimum-necessary contracts
   - audience visibility coverage
4. Explicitly model how the same underlying object may have different lawful renderings across:
   - patient public
   - patient authenticated
   - grant-scoped patient recovery
   - practice/origin operational staff
   - hub
   - pharmacy
   - support
   - operations
   - governance
   - break-glass investigation
5. Build a redaction-policy matrix that covers:
   - projections
   - previews/snippets
   - artifacts/documents/downloads
   - telemetry
   - audit/replay export
6. Build an audit posture matrix that states which identifiers, hashes, references, timestamps, and payload fragments may appear in:
   - operational telemetry
   - UI telemetry
   - immutable audit
   - assurance ledger
   - replay views
   - export bundles
7. Build break-glass and investigation rules from the canonical audit/replay model.
8. Build an artifact sensitivity matrix covering:
   - uploads and attachments
   - telephony recordings
   - transcripts
   - evidence snapshots
   - derivation packages
   - summaries / parity witnesses
   - record artifacts / letters / results
   - deletion certificates
   - archive manifests
   - audit/replay evidence bundles

Mandatory gap closures you must perform:
- Close the “UI collapse is a privacy control” gap by explicitly forbidding it.
- Close the “payload trimmed after hydration” gap by requiring allowlisted materialization before the browser receives data.
- Close the “preview and detail can share one payload” gap by separating preview/detail/artifact contracts.
- Close the “patient_public and patient_authenticated share one broad payload” gap.
- Close the “break-glass is just a role flag” gap by defining time-bound, reason-coded, scope-bound widening rules.
- Close the “telemetry can include useful raw identifiers” gap by defining PHI-safe telemetry and log ceilings explicitly.
- Close the “wrong-patient hold may reuse cached PHI” gap by defining suppression and cache demotion rules.
- Close the “support replay can widen or drift scope” gap by binding replay to investigation scope, masking ceiling, and selected anchor.
- Close the “retention/deletion artifacts are ordinary docs” gap by classifying them and their disclosure/export rules explicitly.

Non-negotiable disclosure rules you must encode:
- Minimum-necessary access is enforced before projection materialization.
- Previews, alerts, rows, digests, and receipts are independent disclosure surfaces.
- Artifact visibility is a separate ceiling from preview/detail visibility.
- Acting-context elevation or break-glass switches to a separately governed purpose-of-use row; it does not widen an already materialized payload.
- No raw identity claims, phone numbers, raw JWTs, or raw secret material appear in event payloads, logs, or standard telemetry.
- Any route or artifact posture can degrade to placeholder, summary-only, recovery-only, or blocked; omission is not the only lawful privacy response.
- Investigation/export/replay routes must bind masking ceiling, disclosure ceiling, selected anchor, and diagnostic question.
- WORM/audit/assurance classes have special retention and export behavior and must not be treated as ordinary downloadable content.

Specific source-sensitive details you must preserve:
- `phase-0-the-foundation-protocol.md` says `materializationMode = allowlisted_only` and that acting-context elevation may not widen a payload already materialized for a lower-trust audience.
- `patient-account-and-communications-blueprint.md` requires governed placeholders, preview modes such as `public_safe_summary`, and suppression of PHI during wrong-patient hold.
- `phase-2-the-identity-and-echoes.md` explicitly keeps raw identity claims, telephony identifiers, consent artifacts, and evidence provenance in encrypted append-only storage while operational tables/events keep references, hashes, or masked fragments only.
- `phase-7-inside-the-nhs-app.md` prohibits PHI-bearing outbound URLs and stale embedded artifact exposure.
- `phase-9-the-assurance-ledger.md` requires one `InvestigationScopeEnvelope` with masking and disclosure ceilings for audit search, replay, break-glass review, and export.
- `forensic-audit-findings.md` adds hard requirements around visibility projection, placeholders, dominant blocked actions, and continuity-safe same-shell recovery.

Validation rules:
- Every audience/surface pair from task 004 must resolve to explicit classification and visibility rules or be explicitly out of scope.
- Every PHI-bearing route family or artifact family must have a redaction/masking policy row.
- Every event/telemetry family that can cross a browser or operator boundary must have a disclosure ceiling.
- Any field or artifact marked sensitive but allowed in previews, telemetry, or exports without a policy ref must fail validation.
- Any rule that widens data after browser delivery must fail validation.
- Any break-glass rule that does not narrow to a distinct purpose-of-use row must fail validation.
- Any wrong-patient hold path that allows cached PHI replay must fail validation.

Browser-viewable atlas requirements:
Build `10_data_classification_atlas.html` and validate it with `Playwright_or_other_appropriate_tooling`.

Atlas composition:
- left rail: audience tiers, purposes of use, and sensitivity classes
- hero strip: counts for classified fields/artifacts, PHI-bearing classes, redaction policies, and unresolved gaps
- center matrix: audience × surface × disclosure ceiling explorer
- field inspector: selected object fields with classification chips and allowed disclosures
- artifact panel: artifact classes, parity witnesses, transfer modes, and retention classes
- audit/replay panel: investigation scope, masking ceiling, export posture, and break-glass expansion summary
- one compact disclosure ladder diagram showing preview/detail/artifact/telemetry/audit ceilings

Unique design requirements:
- this page should feel like a “masking atlas”
- use layered cards, matrix views, and disclosure ladders rather than generic grids alone
- include restrained redaction preview examples using synthetic placeholders only
- do not use fake patient data or decorative medical imagery

Playwright validation must cover:
- classification matrix render
- audience and purpose-of-use filtering
- field inspector and artifact panel interactions
- keyboard navigation
- responsive layout
- accessibility smoke checks
- confirmation that synthetic preview examples never contain live sensitive values
- print/export-safe rendering for policy review if implemented

Definition of done:
- Vecells now has one canonical data-classification and masking model.
- Later API, frontend, runtime, telemetry, audit, replay, export, and retention work can follow explicit disclosure contracts instead of local guesswork.
```