# Prompt 007 For 007_seq_map_state_machines_and_cross_phase_invariants

Use the shared operating contract first:
[shared_operating_contract_006_to_010.md](/Users/test/Code/V/prompt/shared_operating_contract_006_to_010.md)

```text
Execute roadmap task 007_seq_map_state_machines_and_cross_phase_invariants.md.

Inherit the Shared_operating_contract_for_prompts_006_to_010 and consume the validated outputs from tasks 001-006.

Mission:
Build the authoritative state-machine atlas and cross-phase invariant library for Vecells. This task must convert the corpus into explicit state ownership, legal transitions, guard conditions, proof requirements, supersession rules, degraded modes, and closure constraints.

This is one of the most important control-plane tasks in the programme. Later implementation must not infer state machines from prose. You must formalize them now.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `## 0B. Canonical domain kernel and state machine`
  - `## Canonical lifecycle, identity, safety, reservation, access, and reconciliation algorithm`
  - `#### 1.1 SubmissionEnvelope`
  - `#### 1.1B RequestLineage`
  - `#### 1.2 Episode`
  - `#### 1.3 Request`
  - `#### 1.4 IdentityBinding`
  - `#### 1.4C Session`
  - `#### 1.6 AccessGrant`
  - `#### 1.7 DuplicateCluster`
  - `#### 1.8* safety family`
  - `#### 1.9* reachability family`
  - `#### 1.10 RequestLifecycleLease`
  - `#### 1.12 RequestClosureRecord`
  - `#### 1.14* reservation family`
  - `#### 1.15* confirmation family`
  - `#### 1.20 FallbackReviewCase`
  - `#### 1.21 RouteIntentBinding`
  - `#### 1.22 CommandActionRecord`
  - `#### 1.23 CommandSettlementRecord`
  - `#### 1.24* release trust/freeze family`
- Phase-specific behavioral sources:
  - `phase-1-the-red-flag-gate.md`
  - `phase-2-identity-and-echoes.md`
  - `phase-3-the-human-checkpoint.md` sections `3A` through `3G`
  - `phase-4-the-booking-engine.md` sections `4A` through `4H`
  - `phase-5-the-network-horizon.md` sections `5A` through `5H`
  - `phase-6-the-pharmacy-loop.md` sections `6A` through `6H`
  - `callback-and-clinician-messaging-loop.md`
  - `self-care-content-and-admin-resolution-blueprint.md`
  - `phase-8-the-assistive-layer.md`
  - `phase-9-the-assurance-ledger.md`
- `forensic-audit-findings.md`, especially Findings:
  - 01-18 for ingress, identity, evidence, safety, duplicate, and queue foundations
  - 18-45 for more-info, direct outcomes, callback/messaging, booking, hub, pharmacy, and closure hazards
  - 48-79 for state-axis drift and illegal cross-domain write hazards
  - 86-103 for continuity, visibility, shell, and control-plane enforcement

Deliverables to create:
1. `docs/architecture/07_state_machine_atlas.md`
2. `docs/architecture/07_cross_phase_invariants.md`
3. `docs/architecture/07_transition_tables.md`
4. `docs/architecture/07_guard_and_proof_matrix.md`
5. `docs/architecture/07_illegal_transition_and_conflict_report.md`
6. `docs/architecture/07_state_machine_atlas.html`
7. `docs/architecture/07_state_machine_relationships.mmd`
8. `data/analysis/state_machines.json`
9. `data/analysis/state_transition_table.csv`
10. `data/analysis/cross_phase_invariants.json`
11. `data/analysis/guard_and_proof_matrix.csv`
12. `data/analysis/illegal_transitions.json`
13. `tools/analysis/build_state_machine_atlas.(py|ts)`
14. `tools/analysis/validate_state_machines.(py|ts)`

Every state machine definition must capture at minimum:
- `machine_id`
- `canonical_name`
- `owning_object_id`
- `state_axis_type = lifecycle | workflow | identity | safety | settlement | gate | lease | publication | trust | continuity | case_local | other`
- `source_file`
- `source_heading_or_block`
- `states[]`
- `initial_state`
- `terminal_states[]`
- `supersession_states[]`
- `legal_transitions[]`
- `illegal_transitions[]`
- `transition_triggers[]`
- `required_guards[]`
- `required_authoritative_proofs[]`
- `required_related_objects[]`
- `degraded_or_recovery_states[]`
- `closure_blocker_interactions[]`
- `whether_transition_is_coordinator_owned`
- `notes`

Execution steps:
1. Build a machine inventory from the object catalog created in task 006.
2. Separate state machines into:
   - canonical shared-control machines
   - domain-local case machines
   - UI/runtime/control-plane machines that still govern real actionability
3. Formalize the canonical shared-control state axes first:
   - `SubmissionEnvelope.state`
   - `Request.workflowState`
   - `Request.safetyState`
   - `Request.identityState`
   - `Episode.state`
   - `IdentityBinding.bindingState`
   - `Session.sessionState`
   - `Session.routeAuthorityState`
   - `AccessGrant` lifecycle
   - `DuplicateCluster` lifecycle
   - `FallbackReviewCase` lifecycle
   - `RequestLifecycleLease` lifecycle
   - `RouteIntentBinding` lifecycle
   - `CommandActionRecord` / `CommandSettlementRecord` settlement chain
   - `CapacityReservation` / reservation truth
   - `ExternalConfirmationGate`
   - release/channel/assurance trust state families
4. Formalize later domain-local machines next:
   - triage task / decision epoch / more-info cycle
   - callback case
   - clinician message thread
   - self-care advice issue and admin-resolution case
   - booking case / booking transaction / waitlist family
   - hub coordination case / alternative offer / practice continuity
   - pharmacy case / consent / dispatch / outcome reconciliation / bounce-back
   - assistive rollout and change-control families where they gate live posture
   - assurance / incident / retention / investigation families where they gate exports or writable posture
5. For each machine, distinguish clearly:
   - canonical state
   - case-local state
   - projection-only posture
   - publication or trust posture
6. Build a cross-phase invariant library. Each invariant must have:
   - stable invariant ID
   - canonical wording
   - scope
   - affected objects
   - related guards/proofs
   - violating transitions
   - source refs
   - test hint
7. Build a guard-and-proof matrix that answers:
   - what must be true before a transition may happen
   - what authoritative record proves it happened
   - what degraded posture must appear when proof is missing
8. Build an illegal-transition report that explicitly calls out where the prose would be dangerous if implemented naïvely.

Non-negotiable invariants you must encode:
- `Request.workflowState` contains milestones only.
- Blockers remain orthogonal to workflow milestones.
- `patientRef` is nullable and derived from verified `IdentityBinding`.
- Only `LifecycleCoordinator` derives canonical request milestone and closure changes.
- Child domains may not directly write canonical `Request.workflowState`, `Episode.state`, or closure truth.
- No business success state may be inferred from transport acceptance alone.
- No post-submit mutation may remain writable after route-intent, session, binding, release, or trust fences drift.
- No closure may occur while coordinator-materialized blocker sets remain non-empty.
- Same-request continuation reuses the existing `RequestLineage`; same-episode or related-episode branching must be explicit.
- Wrong-patient correction is a repair/hold path, not a workflow milestone.
- Evidence assimilation and re-safety are explicit when material new evidence arrives.
- Confirmation ambiguity must remain explicit through gate objects and not collapse into generic success or generic error.
- Projection freshness, visibility, and continuity evidence are part of actionability, not decoration.

Mandatory state machines and state families you must formalize even if the corpus distributes them across multiple docs:
- `SubmissionEnvelope.state`
- `Request.workflowState`
- `Request.safetyState`
- `Request.identityState`
- `Episode.state`
- `IdentityBinding.bindingState`
- `Session.sessionState`
- `AccessGrant` issuance/redemption/supersession
- `DuplicateCluster` review and resolution
- `TelephonyEvidenceReadinessAssessment.usabilityState`
- `TelephonyContinuationEligibility.eligibilityState`
- triage review state and more-info lifecycle
- callback and clinician messaging lifecycles
- booking confirmation truth and waitlist continuation families
- hub coordination and alternative-offer expiry families
- pharmacy dispatch truth and outcome reconciliation families
- `RequestClosureRecord` blocker evaluation
- `AudienceSurfaceRuntimeBinding` / publication-trust writable posture family where it gates mutation

Mandatory gap closures you must perform:
- Separate `urgent_diversion_required` from `urgent_diverted`.
- Separate `confirmation_pending` / `supplier_reconciliation_pending` / `outcome_reconciliation_pending` from canonical workflow milestones.
- Make more-info TTL, supersession, late reply, and re-entry explicit.
- Make callback/message delivery failure and resend/repair explicit.
- Make waitlist deadlines, fallback obligations, and hub transfer explicit.
- Make pharmacy consent, dispatch proof, weak-match review, urgent return, and bounce-back explicit.
- Make assistive freeze/quarantine/degraded posture explicit where it affects visible capability.
- Make closure blockers explicit for duplicate review, fallback review, identity repair, grants, reachability, confirmation ambiguity, and any phase-local gate that the audit elevates into closure relevance.

Specific forensic findings you must convert into invariants or illegal-transition warnings:
- 48-56
- 57-68
- 69-79
- 85-103 where they affect state ownership, continuity, visibility, or actionability

Validation rules:
- Every canonical state axis from Phase 0 must have a formal machine definition.
- Every domain-local case with suggested states in later phases must have an explicit machine or an explicit reason why the source is insufficient.
- Every state value must belong to one owning machine only; duplicate string reuse across machines must be flagged when meaning differs.
- Every legal transition must declare guard(s) and proof(s), or validation fails.
- Every machine that can gate patient/staff actionability must link to runtime/publication/trust or visibility controls where relevant.
- Any machine definition that implies a child domain can directly write canonical request milestones must fail validation.
- Any machine that uses overloaded terms such as `reconciliation_required` without contextual scoping must raise a conflict.

Browser-viewable atlas requirements:
Build `07_state_machine_atlas.html` and validate it with `Playwright_or_other_appropriate_tooling`.

Atlas composition:
- left rail: machine families and invariant groups
- hero ribbon: counts for machines, states, guards, proofs, invariants, and conflicts
- center: interactive state diagram panel for the selected machine
- lower split: transition table and illegal-transition table
- right rail: invariant cards, owning objects, related proofs, and source traceability
- invariant lattice view: a dedicated diagram showing how Phase 0 invariants constrain phase-local machines

Interaction rules:
- switch between machine view and invariant view without losing selected context
- searchable machine and state names
- filter by phase, bounded context, axis type, and “coordinator-owned”
- clickable transitions opening guard/proof details
- deep links to machine IDs and invariant IDs
- mobile behavior should degrade to stacked cards, not broken diagrams

Unique design requirements for this atlas:
- this page should feel like a “state observatory”, not a Jira board
- use one restrained system diagram, one invariant lattice, and one compact timeline stripe per selected machine
- include a subtle animated transition only when switching machine views; no decorative motion
- always pair diagrams with exact tabular parity

Playwright validation must cover:
- load and diagram rendering
- machine switching
- invariant deep-linking
- search and filtering
- keyboard navigation
- responsive breakpoints
- accessibility smoke checks
- confirmation that every diagram has a corresponding table/list parity section

Definition of done:
- Vecells state ownership is explicit.
- Illegal transitions are bounded before implementation begins.
- Cross-phase invariants are machine-readable and testable.
- Later backend, frontend, workflow, and testing tasks can build from one authoritative state/control atlas instead of rediscovering semantics from prose.
```