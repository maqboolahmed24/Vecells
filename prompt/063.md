# Prompt 063 For 063_par_phase0_track_backend_implement_evidence_snapshot_capture_bundle_derivation_package_and_redaction_transforms

Use the shared operating contract first:
[shared_operating_contract_056_to_065.md](/Users/test/Code/V/prompt/shared_operating_contract_056_to_065.md)

```text
Execute roadmap task 063_par_phase0_track_backend_implement_evidence_snapshot_capture_bundle_derivation_package_and_redaction_transforms.md.

Inherit the Shared_operating_contract_for_prompts_056_to_065 and consume the validated outputs from tasks `001-061`.

Mission:
Implement the immutable evidence backbone for Vecells: `EvidenceCaptureBundle`, `EvidenceDerivationPackage`, `EvidenceRedactionTransform`, `EvidenceSummaryParityRecord`, and the authoritative `EvidenceSnapshot` join structure.

This is real implementation work. Produce code, schemas, migrations, storage abstractions, and tests that guarantee evidence immutability, supersession discipline, parity checking, and safe redaction / presentation derivation.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - the top-level canonical definitions for `EvidenceSnapshot`, `EvidenceCaptureBundle`, `EvidenceDerivationPackage`, `EvidenceRedactionTransform`, and `EvidenceSummaryParityRecord`
  - the invariants stating clinically material evidence must create immutable snapshots and may never be overwritten in place
  - the evidence pipeline rules around lines `5115-5120`
  - the duplicate / replay rules around `5035-5038`
- `forensic-audit-findings.md`, especially Finding `03` requiring immutable `EvidenceSnapshot` before normalization
- `phase-cards.md`, especially the Phase 0 object list and the requirement for immutable evidence snapshots, source bundles, derivation packages, and summary-parity proof
- outputs from tasks `007`, `010`, `041-045`, `049`, `056`, and `061`

Parallel-execution note:
Do not block on sibling task `062` implementation details. Use published aggregate IDs / shared package seams and create bounded stubs only where absolutely necessary.

Primary outcome:
Working backend evidence infrastructure so all later intake, telephony, support, booking, pharmacy, callback, and portal flows can point at one immutable evidence history with governed redaction and presentation outputs.

Implementation deliverables to create:
1. domain and persistence code for `EvidenceCaptureBundle`, `EvidenceDerivationPackage`, `EvidenceRedactionTransform`, `EvidenceSummaryParityRecord`, and `EvidenceSnapshot`
2. storage abstractions for source artifacts, derived artifacts, and redacted artifacts
3. immutability and supersession enforcement logic
4. schema and migration artifacts for the evidence objects above
5. typed services or coordinators for:
   - capture bundle freezing
   - derivation package creation
   - redaction transform creation
   - summary parity verification
   - snapshot creation and supersession
6. `docs/architecture/63_evidence_backbone_design.md`
7. `docs/architecture/63_evidence_immutability_and_parity_rules.md`
8. `data/analysis/evidence_object_manifest.json`
9. `data/analysis/evidence_pipeline_invariant_matrix.csv`
10. unit tests, repository tests, and integration tests for immutability, supersession, parity, and redaction behavior
11. validator or smoke scripts proving no in-place mutation of evidence artifacts is possible

At minimum, implement these capabilities:
- freeze one immutable capture bundle before canonical normalization
- create one or more immutable derivation packages from one frozen bundle
- create redaction transforms that preserve source hash and policy version
- create and verify summary parity records before summaries can present as authoritative
- create one immutable `EvidenceSnapshot` pointing at exactly one frozen bundle and the authoritative normalized / derived refs
- supersede evidence objects by appending new rows rather than mutating old rows

Execution steps:
1. Implement the capture-bundle freeze model first. It must preserve raw payload references, artifact manifests, checksums, and capture metadata immutably.
2. Implement derivation packages next. At minimum support derivation classes for:
   - canonical normalization
   - transcript
   - structured fact extraction
   - safety feature set
   - patient-safe summary
   - staff-review summary
3. Implement redaction transforms as immutable derived artifacts tied to one source hash and one redaction-policy version.
4. Implement summary parity verification and make `parityState = verified | stale | blocked | superseded` explicit.
5. Implement `EvidenceSnapshot` as the authoritative immutable join over the frozen capture bundle and chosen derived refs.
6. Implement supersession handling so late transcript improvement, enrichment, schema migration, or redaction change append new derivations or snapshots rather than rewriting old rows.
7. Write tests covering:
   - no normalization before bundle freeze
   - no in-place mutation of any evidence object
   - clinically material changes produce new snapshots
   - technical-only changes can remain unattached when policy allows
   - parity failure blocks authoritative summary posture
   - collision-review and duplicate cases still preserve frozen evidence
8. Ensure all code honors the data-classification and masking posture established earlier.

Mandatory gap closures you must perform:
- Close the “normalization runs before immutable evidence freeze” gap from forensic finding `03`.
- Close the “late transcript or enrichment can rewrite prior evidence truth” gap by enforcing append-only supersession.
- Close the “visible summaries are presented without parity proof” gap by implementing `EvidenceSummaryParityRecord` and parity enforcement.
- Close the “redaction is a destructive rewrite” gap by implementing immutable redaction transforms.
- Close the “duplicate or collision replay can skip freezing evidence” gap by preserving frozen capture bundles in all replay classes.

Industry-best-practice requirements:
- append-only immutable evidence records
- content-addressed or checksum-verified artifact references
- clear separation of source, derived, and redacted artifacts
- deterministic derivation versioning
- policy-versioned redaction and parity verification
- no raw blob storage inside primary relational objects

Non-negotiable rules:
- Every evidence flow freezes one immutable `EvidenceCaptureBundle` before canonical normalization.
- No existing `EvidenceSnapshot`, capture bundle, derivation package, redaction transform, or parity record may be rewritten in place.
- Any patient-visible or staff-visible summary claiming authority must resolve through `EvidenceSummaryParityRecord(parityState = verified)`.
- Late changes append new immutable derivatives or snapshots; they do not mutate history.
- Raw artifacts remain references only; never inline binary payloads into aggregate state.

Definition of done:
- Vecells has a real immutable evidence backbone with storage, parity, and redaction discipline.
- Later ingestion, telephony, support, and downstream flows can reference evidence safely without inventing local artifact truth.
- Evidence history is replayable, auditable, and safe to expose through governed transforms.
```
