# Prompt 085 For 085_par_phase0_track_runtime_provision_domain_transaction_store_and_fhir_storage

Use the shared operating contract first:
[shared_operating_contract_076_to_085.md](/Users/test/Code/V/prompt/shared_operating_contract_076_to_085.md)

```text
Execute roadmap task 085_par_phase0_track_runtime_provision_domain_transaction_store_and_fhir_storage.md.

Inherit the Shared_operating_contract_for_prompts_076_to_085 and consume the validated outputs from tasks `001-084`.

Mission:
Provision the Phase 0 data-plane baseline for authoritative domain truth and FHIR representation truth:
- transactional domain store
- FHIR representation store or FHIR-capable storage layer
- storage connectivity, isolation, encryption, and manifest publication

This task must make the separation between internal aggregate truth and FHIR representation truth concrete. It must not collapse the domain write model into a FHIR-only store, and it must not let projections or adapters improvise raw persistence outside the published contracts.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - the Phase 0 persistence split and FHIR mapping guidance near the delivery skeleton and domain-kernel sections
  - `**FhirRepresentationContract**`
  - `**FhirRepresentationSet**`
  - `**FhirResourceRecord**`
  - `**FhirExchangeBundle**`
  - the rule that `Request` and control-plane objects remain internal aggregates even when FHIR companions exist
- `platform-runtime-and-release-blueprint.md`, especially:
  - `## Data persistence and migration contract`
  - the explicit separation of transactional domain aggregates, FHIR representation rows, immutable events, audience projections, binary artifacts, and WORM audit
  - the runtime rule that data workloads are never browser-addressable
- outputs from tasks `011`, `013`, `046`, `049`, `053`, `060`, `064`, `084`
- `forensic-audit-findings.md`, especially Findings `43`, `49`, `50`, `51`, `52`, `112`, and `113`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must expose extension points for later object storage, event spine, audit ledger, migration runner, and backup tasks without stealing their ownership. If a later task owns a dependent capability, provision only the seams and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build a development- and sandbox-safe storage baseline now.
- provision a transactional store for authoritative aggregates and control-plane writes
- provision a separate FHIR representation store or isolated FHIR-capable schema / cluster according to the baseline decided in prior ADRs
- keep local development and CI realistic enough that mapping contracts, FHIR resource records, and exchange bundles can be materialized and validated without live partner infrastructure
The mock-now path must preserve the same data-shape separation, migration discipline, and connectivity isolation the production system will later use.

Actual_production_strategy_later:
When production-grade managed databases or FHIR-capable stores are finalized, they must preserve the same separation of truth layers. Production hardening may add HA, managed encryption, read replicas, point-in-time recovery, or vendor-native FHIR features, but it must not:
- make FHIR resources the hidden owner of aggregate lifecycle
- let adapters write raw FHIR rows outside published representation contracts
- let projections or partner responses backfill canonical domain truth directly

Primary outcome:
The repository gains concrete, isolated, and machine-readable storage infrastructure for authoritative domain state and FHIR representation state, ready for later event, object, audit, backup, and migration tracks.

Implementation deliverables to create:
1. infrastructure-as-code for the transactional domain store and the separate FHIR representation store or isolated FHIR-capable deployment
2. connectivity, private-network placement, and service-identity-ready access policies aligned to the network foundation from task `084`
3. base schema/bootstrap migrations for domain-store connectivity plus FHIR representation-store bootstrap structures
4. machine-readable store manifests and connectivity descriptors bound to the runtime topology manifest
5. development and CI bootstrap scripts for seeded local or sandbox-safe storage
6. `docs/architecture/85_domain_transaction_store_and_fhir_storage_design.md`
7. `docs/architecture/85_data_plane_truth_layer_and_fhir_separation_rules.md`
8. `docs/architecture/85_data_storage_topology_atlas.html`
9. `data/analysis/domain_store_manifest.json`
10. `data/analysis/fhir_store_manifest.json`
11. `data/analysis/data_plane_separation_matrix.csv`
12. `tools/analysis/validate_domain_and_fhir_storage.(py|ts)`
13. infrastructure tests, connectivity tests, bootstrap tests, and migration smoke tests
14. `tests/playwright/data-storage-topology-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- provision an authoritative relational store for transactional domain aggregates and control-plane rows
- provision a separate FHIR representation store or isolated FHIR-capable layer for `FhirRepresentationSet`, `FhirResourceRecord`, and `FhirExchangeBundle`
- enforce private connectivity and non-browser-addressable placement for both stores
- publish store manifests that declare purpose, runtime binding, encryption posture, network placement, and owning workload families
- prevent direct projection, adapter, or browser writes into the domain store or FHIR store outside the published service and mapping seams
- keep aggregate IDs, FHIR logical IDs, and projection row identifiers distinct

Execution steps:
1. Honor the storage and cloud-baseline decisions already frozen in tasks `011`, `013`, and `049`; if those outputs are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
2. Provision the domain transaction store with private connectivity, encryption-at-rest-ready posture, and least-privilege service access.
3. Provision the FHIR representation store or FHIR-capable layer as a separate truth plane; if the chosen baseline is still unresolved but no ADR conflicts exist, record `ASSUMPTION_FHIR_STORE_BASELINE` explicitly and keep the separation concrete.
4. Publish machine-readable manifests for both stores and bind them to the runtime topology manifest from task `084`.
5. Provide local and CI bootstrap flows that materialize realistic development stores without collapsing the truth layers.
6. Build the topology atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write smoke tests proving that domain-store and FHIR-store paths are isolated, privately reachable only through allowed workloads, and ready for later mapping and migration work.

Mandatory gap closures you must perform:
- Close the “FHIR-native” misunderstanding gap by making Vecells domain-first internally while still provisioning explicit FHIR representation storage.
- Close the “aggregate identifiers can double as FHIR identifiers” gap by preserving distinct identifier spaces and manifests.
- Close the “adapters or projections can persist raw FHIR or domain truth ad hoc” gap by isolating stores and documenting allowed write seams.
- Close the “data persistence split is only prose” gap by provisioning separate storage layers and publishing their purpose explicitly.
- Close the “local development quietly collapses truth layers” gap by giving local and CI environments the same separation model as cloud-backed environments.

Industry-best-practice requirements:
- private connectivity and least-privilege service access
- encryption at rest and readiness for managed key integration
- schema bootstrap under version control
- store manifests published from code, not wiki prose
- no browser reachability to data-plane workloads
- clear preparation for later backup, migration, audit, and object-storage tasks without overstepping their ownership

Non-negotiable rules:
- Transactional domain truth and FHIR representation truth are separate layers.
- FHIR resources do not own aggregate lifecycle, blocker, or continuity truth.
- Data-plane workloads are never browser-addressable.
- Adapters and projections may not write raw rows outside published mapping and service seams.
- Store manifests must remain bound to the runtime topology manifest.

Mandatory UI and interaction specification for `85_data_storage_topology_atlas.html`:
Name the surface mode `Data_Storage_Topology_Atlas`.

Tone:
- premium data-plane architecture atlas
- minimalist, precise, and technically elegant
- should feel like a high-end systems map, not a database admin console

Branding:
- restrained Vecells wordmark plus inline SVG `DS` monogram
- one truth-layer topology diagram
- one store-binding matrix
- one representation-flow strip from aggregate to FHIR set

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- domain accent `#2563EB`
- FHIR accent `#0EA5A4`
- manifest accent `#7C3AED`
- warning accent `#D97706`
- blocked accent `#C24141`
- ready accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for store IDs, contract refs, representation-set refs, and tuple hashes

Layout:
- sticky masthead with active store bindings, stale manifests, connectivity issues, and bootstrap health
- left rail (`324px`) with environment filters, store-family filters, binding-state filters, and encryption / access filters
- central canvas with:
  - truth-layer topology diagram
  - store-binding matrix
  - representation-flow strip
- right inspector (`420px`) for selected store, binding, or representation path
- lower grid with one store manifest table and one separation / binding table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by environment, store family, binding state, and access posture
- selecting a store or binding row must synchronize topology diagram, matrix, flow strip, inspector, and tables
- keyboard navigation across filters and tables
- deterministic `data-testid` markers for topology diagram, binding matrix, representation strip, store table, binding table, and inspector
- every diagram must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification

Definition of done:
- The repository has provisioned, isolated data-plane foundations for transactional domain truth and FHIR representation truth.
- The separation between internal aggregates and FHIR representations is concrete and machine-readable.
- Local, CI, and non-production environments can exercise the same truth-layer model used in cloud deployment.
- Later event, object, backup, migration, and audit tasks can build on this data-plane foundation without redefining storage semantics.
```
