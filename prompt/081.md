# Prompt 081 For 081_par_phase0_track_backend_build_reservation_authority_and_queue_ranking_coordinator

Use the shared operating contract first:
[shared_operating_contract_076_to_085.md](/Users/test/Code/V/prompt/shared_operating_contract_076_to_085.md)

```text
Execute roadmap task 081_par_phase0_track_backend_build_reservation_authority_and_queue_ranking_coordinator.md.

Inherit the Shared_operating_contract_for_prompts_076_to_085 and consume the validated outputs from tasks `001-080`.

Mission:
Build the two canonical control services that own resource contention and work ordering:
- `ReservationAuthority`
- `QueueRankingCoordinator`

These services must ensure patient-visible exclusivity only appears when real reservation truth exists, and workspace assignment guidance only appears after canonical queue order is fixed from one consistent fact cut.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.13 CapacityIdentity`
  - `#### 1.13A ProviderCapabilityMatrix`
  - `#### 1.13B BookingProviderAdapterBinding`
  - `#### 1.14 CapacityReservation`
  - `#### 1.14A ReservationTruthProjection`
  - `#### 1.15 ExternalConfirmationGate`
  - `#### 2.2 ReservationAuthority`
  - `#### 2.11 QueueRankingCoordinator`
  - `#### 10. Reservation, confirmation, and truth rules`
- `phase-3-the-human-checkpoint.md` where queue state, more-info return, and assignment advice depend on canonical ranking rather than local sorting
- `staff-workspace-interface-architecture.md`, especially the rules around `QueueRankSnapshot`, `TaskCompletionSettlementEnvelope`, `NextTaskPrefetchWindow`, and source rank snapshot consistency
- `phase-4-the-booking-engine.md`, especially truthful hold semantics, commit revalidation, waitlist continuation, and callback / hub fallback
- `phase-5-the-network-horizon.md`, especially candidate ranking, SLA pressure, alternative offers, and cross-org booking coordination
- `forensic-audit-findings.md`, especially Findings `72`, `73`, `74`, `76`, `97`, and `119`
- outputs from tasks `001-007`, `019`, `041-045`, `056-057`, `061`, `073`, `074`, `077`, and `080`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Do not wait for later booking, hub, or workspace UIs to exist. Publish only the bounded query or snapshot contracts those later tracks will need, and record `PARALLEL_INTERFACE_GAP_*` when a seam is missing.

Mock_now_execution:
Build a local capacity and queue simulator now. It must cover:
- soft-selected supply with no exclusive hold
- real held reservation with hold expiry and revalidation
- overlapping local and hub claims on the same canonical reservation key
- fair queue ranking under normal load, overload, and fairness merge
- assignment suggestions that never reorder the base queue
- next-task suggestions derived only from committed rank snapshots
The simulator must emit the same reservation fences, rank snapshots, assignment suggestions, and stale-owner protections production will later require.

Actual_production_strategy_later:
When live booking suppliers, hub feeds, triage workload signals, and workforce inputs arrive, they must all pass through the same reservation and queue services. Live sources may improve data freshness and scale, but they may not bypass canonical reservation fencing, truthful exclusivity rules, queue-rank snapshot commitment, or assignment-suggestion discipline.

Primary outcome:
The repository gains authoritative services for patient-facing reservation truth and staff-facing queue ordering, preventing fake exclusivity on one side and mixed-snapshot work ordering on the other.

Implementation deliverables to create:
1. `ReservationAuthority` service implementation
2. `QueueRankingCoordinator` service implementation
3. bounded locking / fencing abstraction over `canonicalReservationKey`
4. overload, fairness-merge, and assignment-advice evaluators
5. queue snapshot persistence, replay, and stale-owner protection hooks
6. canonical-event entries for reservation claimed, reservation released, hold expired, rank snapshot committed, assignment suggestion refreshed, fairness merge changed, and queue pressure escalated
7. `docs/architecture/81_reservation_authority_and_queue_ranking_design.md`
8. `docs/architecture/81_reservation_truth_and_queue_fairness_rules.md`
9. `docs/architecture/81_reservation_queue_control_studio.html`
10. `data/analysis/reservation_queue_casebook.json`
11. `data/analysis/queue_rank_snapshot_matrix.csv`
12. `data/analysis/reservation_fence_manifest.json`
13. `tools/analysis/validate_reservation_authority_and_queue_ranking.(py|ts)`
14. unit tests, concurrency tests, rank replay tests, overload tests, and integration tests
15. `tests/playwright/reservation-queue-control-studio.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- serialize every patient-facing or staff-facing claim on `canonicalReservationKey`
- mint and validate fencing tokens for reservation operations
- enforce truthful separation between `soft_selected`, `held`, `truthful_nonexclusive`, `pending_confirmation`, `confirmed`, `expired`, `released`, and `disputed`
- compute one committed `QueueRankSnapshot` from one consistent fact cut under one active plan
- persist overload and fairness-merge state so replay and rebuild reproduce the same queue order
- emit assignment suggestions only after base queue ordering is fixed
- supply one committed rank snapshot to workbench and next-task launch consumers so mixed-snapshot resorting is impossible

Execution steps:
1. Implement `ReservationAuthority` so local booking, waitlist offers, staff booking, and hub booking all serialize on the same `canonicalReservationKey` law.
2. Integrate reservation fences with the existing reservation and confirmation models from tasks `074` and `075` without overstepping into later phase-specific booking orchestration.
3. Implement `QueueRankingCoordinator` to derive eligibility and ordering from one consistent task, safety, trust, and policy fact cut.
4. Persist rank snapshots and fairness / overload state as first-class objects rather than transient in-memory sort outputs.
5. Implement assignment suggestions as downstream advice only; they may never rewrite the already-committed queue order.
6. Publish bounded snapshot and suggestion query contracts for later workspace and ops tracks.
7. Build the control studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Write concurrency tests for overlapping reservation claims, queue recomputation under concurrent facts, and assignment-advice regeneration without base-order drift.

Mandatory gap closures you must perform:
- Close the “countdown copy implies exclusivity without real hold truth” gap by routing all exclusivity through `ReservationAuthority` plus persisted reservation truth.
- Close the “supplier ambiguity collapses into calm success” gap by preserving `pending_confirmation` and confirmation-gate posture distinctly.
- Close the “workbench or operator local sort becomes queue truth” gap by making committed rank snapshots authoritative.
- Close the “assignment heuristics can quietly reorder the queue” gap by separating suggestion from canonical order.
- Close the “hub and local booking can contend without shared serialization” gap by enforcing a single `canonicalReservationKey` authority.
- Close the “next-task readiness is inferred from stale local completion state” gap by binding it to committed queue snapshots only.

Industry-best-practice requirements:
- short-lived fenced critical sections
- no locks held across avoidable retries, notifications, or projection work
- deterministic queue ranking with replayable snapshots
- explicit separation between authoritative order and advisory assignment
- full concurrency and fairness testing
- no UI-local or worker-local shortcut around reservation fencing or queue snapshot commitment

Non-negotiable rules:
- `ReservationAuthority` is the only serializer over `canonicalReservationKey`.
- `QueueRankingCoordinator` fixes the queue order before reviewer-specific assignment guidance applies.
- `soft_selected` is not exclusivity.
- `pending_confirmation` is not final success.
- Next-task or workbench consumers may not derive calmer order truth from mixed snapshots.

Mandatory UI and interaction specification for `81_reservation_queue_control_studio.html`:
Name the surface mode `Reservation_Queue_Control_Studio`.

Tone:
- premium operations and contention-control surface
- polished, quiet, and data-dense without feeling cluttered
- should feel like a pricing-trading hybrid for truth and fairness, not a backlog board

Branding:
- restrained Vecells wordmark plus inline SVG `RQ` monogram
- one reservation-fence timeline
- one queue-fairness heat surface
- one next-task advisory strip

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- reservation accent `#2563EB`
- fairness accent `#7C3AED`
- overload accent `#D97706`
- blocked accent `#C24141`
- ready accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for reservation keys, fencing tokens, snapshot IDs, and plan hashes

Layout:
- sticky masthead with active holds, pending confirmations, overloaded queues, and assignment-ready snapshots
- left rail (`324px`) with reservation-state filters, queue filters, fairness-mode filters, and actor filters
- central canvas with:
  - reservation-fence timeline
  - queue-fairness heat surface
  - next-task advisory strip
- right inspector (`416px`) for selected reservation or queue snapshot, active fences, fairness state, and suggestions
- lower grid with one reservation table and one queue snapshot / assignment table

Dimensional constraints:
- max width `1560px`
- masthead `76px`
- visual zone min height `300px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by reservation state, queue, fairness mode, and suggestion posture
- selecting a reservation or queue row must synchronize fence timeline, heat surface, advisory strip, inspector, and tables
- keyboard navigation across filters, tables, and inspector
- deterministic `data-testid` markers for fence timeline, fairness heat surface, advisory strip, reservation table, queue table, and inspector
- every chart or diagram must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filtering and synchronized selection behavior
- keyboard navigation and focus order
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification

Definition of done:
- The repository has authoritative reservation and queue-control services with concurrency-safe behavior.
- Patient-facing exclusivity and staff-facing queue order are explicit and replayable.
- Assignment suggestions can no longer mutate or obscure canonical queue truth.
- Later booking, hub, workspace, and ops tasks can consume the same reservation and ranking authorities directly.
```
