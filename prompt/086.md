# Prompt 086 For 086_par_phase0_track_runtime_provision_object_storage_quarantine_buckets_and_retention_controls

Use the shared operating contract first:
[shared_operating_contract_086_to_095.md](/Users/test/Code/V/prompt/shared_operating_contract_086_to_095.md)

```text
Execute roadmap task 086_par_phase0_track_runtime_provision_object_storage_quarantine_buckets_and_retention_controls.md.

Inherit the Shared_operating_contract_for_prompts_086_to_095 and consume the validated outputs from tasks `001-085`.

Mission:
Provision the governed binary-artifact storage plane for Vecells.

This task must create the concrete object-storage foundation for:
- quarantined uploads and recordings
- immutable raw evidence bundles
- derived packages such as transcripts and extracted fact packs
- redacted and presentation-safe outputs
- tightly governed outbound-export or handoff artifacts
- retention, legal-hold, and purge controls that stay bound to policy rather than ad hoc operator action

This is real runtime and infrastructure work. Produce code, IaC, manifests, lifecycle policies, validation, and smoke tests. Do not leave storage-class law, retention semantics, or quarantine behavior as comments or wiki prose.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - the immutable artifact chain around `CaptureBundle`, `DerivationPackage`, `RedactionTransform`, and `RecordArtifactParityWitness`
  - `EvidenceSnapshot` and governed artifact references
  - the canonical event catalogue including `intake.attachment.quarantined` and exception / evidence events
  - the rule that visible artifacts require governed presentation contracts rather than loose URLs
- `platform-runtime-and-release-blueprint.md`, especially:
  - `## Data persistence and migration contract`
  - `## Security baseline contract`
  - `## Operational readiness contract`
  - any runtime rules that treat object storage, exports, backups, and artifacts as non-browser-addressable governed data-plane concerns
- `platform-frontend-blueprint.md`, `patient-portal-experience-architecture-blueprint.md`, and `staff-operations-and-support-blueprint.md` where artifact preview, presentation, export, and return-safe continuity are constrained by publication and visibility posture
- outputs from tasks `010`, `013`, `046`, `047`, `053`, `057`, `060`, `063`, `084`, and `085`
- `forensic-audit-findings.md`, especially Findings `09`, `61`, `81`, `88`, and `112`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must prepare extension points for later audit-ledger, backup, retention-governance, recovery, and artifact-presentation tracks without stealing their ownership. If a later task owns a dependent capability, provision only the seam and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build a high-fidelity local and non-production artifact-storage plane now.
- provide a local or sandbox-safe object-store emulator aligned to the chosen cloud baseline
- preserve the same bucket or container classes, object-key strategy, retention labels, quarantine flows, immutability posture, and signed-access pattern the live system will require
- allow seeded uploads, telephony recordings, transcripts, redaction outputs, and presentation-safe artifacts to flow through the same governed storage contract without live provider dependencies
The mock-now path must be realistic enough that later cutover to managed cloud object storage is a configuration and policy switch, not a storage-model rewrite.

Actual_production_strategy_later:
When production object storage is finalized, it must preserve the same storage classes, object-manifest law, retention controls, legal-hold hooks, object immutability semantics, access policies, and auditability. Production hardening may add cross-zone durability, replication, archival tiers, malware-scanning integrations, or KMS-managed encryption, but it must not collapse quarantine, source evidence, derivatives, redactions, and outbound artifacts into one undifferentiated bucket or one public URL pattern.

Primary outcome:
The repository gains a governed object-storage substrate for evidence, derivatives, redactions, and presentation artifacts that is isolated, machine-readable, retention-aware, and ready for later policy, recovery, and publication tracks.

Implementation deliverables to create:
1. infrastructure-as-code modules for object-storage namespaces, buckets or containers, access policies, lifecycle policies, retention locks, and legal-hold-ready controls
2. machine-readable storage classes for at minimum:
   - `quarantine_raw`
   - `evidence_source_immutable`
   - `derived_internal`
   - `redacted_presentation`
   - `outbound_ephemeral`
   - `ops_recovery_staging`
3. a canonical object-key and object-manifest strategy bound to governing artifact refs, tenant scope, lineage scope, evidence class, and hash identity
4. a malware-scan and quarantine handoff seam for uploads and recordings that never exposes unverified artifacts to browser-visible paths
5. a local-development and CI bootstrap flow for seeded binary objects and deterministic reset
6. `docs/architecture/86_object_storage_and_retention_design.md`
7. `docs/architecture/86_artifact_storage_classes_and_visibility_rules.md`
8. `docs/architecture/86_object_storage_retention_atlas.html`
9. `data/analysis/object_storage_class_manifest.json`
10. `data/analysis/object_retention_policy_matrix.csv`
11. `data/analysis/object_key_manifest_rules.json`
12. `tools/analysis/validate_object_storage_and_retention.(py|ts)`
13. infrastructure tests, policy tests, retention smoke tests, and emulator bootstrap tests
14. `tests/playwright/object-storage-retention-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- provision separate storage classes for quarantined, trusted-source, derived, redacted, and ephemeral outbound artifacts
- publish one machine-readable manifest describing each class, its purpose, encryption posture, immutability mode, retention policy, legal-hold readiness, and allowed workload-family access
- enforce content-addressed or hash-bound storage metadata so `CaptureBundle`, `DerivationPackage`, `RedactionTransform`, and presentation-safe artifacts remain traceable and immutable
- prevent direct browser access to raw or quarantined artifacts
- ensure browser-deliverable artifact access can occur only through governed presentation or download seams, never through bucket paths or stable object URLs alone
- provide purge-ready and hold-ready retention controls that can be bound to later legal-hold, assurance, and archive tasks without changing object identity strategy
- keep object manifests, retention labels, and access policies bound to the runtime topology and data-plane manifests from earlier tasks

Execution steps:
1. Re-read the Phase 0 artifact model and convert its objects into explicit storage classes, naming rules, and access rules.
2. Honor the network and data-plane baselines already frozen in tasks `084-085`; if those outputs are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
3. Provision object-storage classes with private-network placement, encryption-at-rest-ready posture, explicit workload-family access, and retention controls.
4. Implement the object-key strategy so every stored object can trace back to the governing artifact ref and source hash without leaking PHI through path names.
5. Publish manifests for storage classes, lifecycle policies, access policies, and retention labels from code.
6. Add development and CI bootstrap scripts that seed representative uploads, recordings, transcript artifacts, and redacted outputs while preserving the same class separation.
7. Build the retention atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Write smoke tests proving that quarantined artifacts cannot be delivered to browser-facing paths, retention policies are attached deterministically, and legal-hold-ready controls can be asserted without mutating artifact identity.

Mandatory gap closures you must perform:
- Close the “artifact storage is an implied blob bucket” gap by provisioning explicit storage classes and manifests.
- Close the “quarantine is only a workflow comment” gap by giving unsafe and unreadable evidence a real storage class plus event-ready seams.
- Close the “object path can become authority” gap by binding visibility and delivery to governed contracts rather than bucket URLs.
- Close the “retention is a future ops note” gap by publishing retention and hold posture now.
- Close the “local development collapses artifact classes” gap by mirroring the same storage taxonomy in local and CI emulation.
- Close the “export copies linger without policy” gap by giving outbound artifacts an explicitly ephemeral class and purge posture.

Industry-best-practice requirements:
- least-privilege workload access to object storage
- encryption at rest and readiness for KMS-backed keys
- immutable source-object posture for evidence-bearing classes where policy requires it
- lifecycle policies and retention rules under version control
- no public buckets and no ad hoc long-lived signed URLs
- malware-scan and quarantine-ready seams
- object manifests generated from code rather than wiki prose

Non-negotiable rules:
- Quarantine, trusted evidence, derived outputs, redacted outputs, and outbound artifacts are different classes with different policy.
- Browser access to raw or quarantined artifacts is forbidden.
- Object storage is not the source of truth for visibility, writability, or release posture.
- Object keys may not leak PHI or become durable public identifiers.
- Retention or hold changes may not mutate artifact identity or hash lineage.

Mandatory UI and interaction specification for `86_object_storage_retention_atlas.html`:
Name the surface mode `Object_Storage_Retention_Atlas`.

Tone:
- premium evidence-governance atlas
- minimalist, calm, and technically exact
- should feel like a high-end digital evidence vault, not a cloud console clone

Branding:
- restrained Vecells wordmark plus inline SVG `OS` monogram
- one artifact-flow diagram from ingress to quarantine / trusted / derived / redacted classes
- one retention-lifecycle ribbon
- one access-policy matrix

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- quarantine accent `#C24141`
- trusted accent `#2563EB`
- derived accent `#0EA5A4`
- redacted accent `#7C3AED`
- ephemeral accent `#D97706`
- hold accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for bucket IDs, class IDs, retention policy refs, and artifact hashes

Layout:
- sticky masthead with active environments, quarantined-object count, hold-enabled classes, and stale-policy alerts
- left rail (`324px`) with environment filters, storage-class filters, retention-state filters, and access-scope filters
- central canvas with:
  - artifact-flow diagram
  - retention-lifecycle ribbon
  - access-policy matrix
- right inspector (`420px`) for selected storage class, retention policy, or object-manifest rule
- lower grid with one manifest table and one retention / access policy table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by environment, storage class, retention state, hold readiness, and workload-family access
- selecting a class or policy row must synchronize the flow diagram, lifecycle ribbon, matrix, inspector, and tables
- keyboard navigation across filters, tables, and inspector tabs
- deterministic `data-testid` markers for flow diagram, lifecycle ribbon, matrix, manifest table, retention table, and inspector
- every visual must have adjacent textual parity and table fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification
- verification that quarantine and trusted classes are visually and semantically distinct

Definition of done:
- The repository has a real, governed object-storage substrate for artifact-bearing flows.
- Quarantine, trusted evidence, derived artifacts, redacted outputs, and ephemeral outbound artifacts are concretely separated.
- Retention and hold posture are machine-readable and testable.
- Later artifact, recovery, audit, and governance tracks can build on this storage plane without redefining artifact-class semantics.
```
