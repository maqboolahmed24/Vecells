# Prompt 089 For 089_par_phase0_track_runtime_provision_secrets_kms_and_key_rotation_baseline

Use the shared operating contract first:
[shared_operating_contract_086_to_095.md](/Users/test/Code/V/prompt/shared_operating_contract_086_to_095.md)

```text
Execute roadmap task 089_par_phase0_track_runtime_provision_secrets_kms_and_key_rotation_baseline.md.

Inherit the Shared_operating_contract_for_prompts_086_to_095 and consume the validated outputs from tasks `001-088`.

Mission:
Provision the secrets, key-management, and rotation baseline for Vecells.

This task must turn security-baseline promises into concrete runtime control-plane infrastructure:
- managed-secret or emulated-secret backends for local, CI, and cloud
- KMS-backed or equivalent envelope-encryption hierarchy
- service and workload secret-class manifests
- deterministic rotation, revocation, and supersession posture
- access logging and break-glass-ready read controls

This is real runtime and security foundation work. Produce infrastructure, secret-loading libraries, manifests, validation, and smoke tests. Do not leave secret classes, key rotation, or environment loading as `.env folklore`.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - Phase 0 requirements around environment loading, secret injection, and local dev bootstrap
  - the rule that secrets never appear in source, logs, or build output
  - rotation-sensitive items such as session identifiers, CSRF secrets, secure-link handling, and grant or session supersession
  - the observability and security plumbing expectations that secrets can rotate without code changes
- `platform-runtime-and-release-blueprint.md`, especially:
  - `## Security baseline contract`
  - verification-ladder rules around secret leak checks and configuration tuple drift
  - any config or publication rules that require machine-readable secret-class awareness
- outputs from tasks `011`, `015`, `023`, `046`, `047`, `051`, `060`, `084`, `085`, and `086`
- `forensic-audit-findings.md`, especially Findings `91`, `95`, `112`, `113`, and `114`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must provide shared secret and key-management primitives for later provider onboarding, gateway surfaces, pipelines, preview envs, and release publication work without stealing their ownership. If a future task owns a concrete provider credential or live cutover, publish only the secret-class seam now and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build a realistic local and non-production secret-management baseline now.
- provide a local or CI-safe secret backend emulator or dev-managed store with the same retrieval and rotation APIs the live system will use
- emulate KMS-backed envelope encryption with deterministic test keys or environment-safe master-key material that never ships in source control
- keep secret class names, access rules, rotation flows, and audit logging identical to the future production posture
The mock-now path must be realistic enough that production hardening is a backend or policy swap, not a secret-loading rewrite.

Actual_production_strategy_later:
When production secret managers and KMS services are finalized, they must preserve the same secret classes, key hierarchies, access controls, rotation windows, audit trails, and retrieval APIs. Production hardening may add HSM-backed keys, stronger separation by environment, or incident-driven revocation flows, but it must not let services fall back to source-controlled values, long-lived CI variables, or manual copy-paste secrets.

Primary outcome:
The repository gains a governed secret-management and envelope-encryption baseline that later services, gateways, pipelines, integrations, and preview environments can consume consistently and rotate without code changes.

Implementation deliverables to create:
1. infrastructure-as-code for secret stores, key hierarchies, envelope-encryption-ready resources, and access policies
2. machine-readable secret-class manifests covering service, environment, purpose, rotation cadence, sensitivity class, and owning workload family
3. reusable runtime secret-loading libraries and configuration adapters for services, workers, gateways, and CI jobs
4. local-development and CI secret bootstrap plus deterministic test-key handling that never leaks into source or build output
5. rotation hooks and supersession workflows for application-secret classes, including safe refresh and fail-closed behavior
6. `docs/architecture/89_secrets_kms_and_rotation_design.md`
7. `docs/architecture/89_secret_class_and_access_boundary_rules.md`
8. `docs/architecture/89_secret_and_key_rotation_atlas.html`
9. `data/analysis/secret_class_manifest.json`
10. `data/analysis/key_hierarchy_manifest.json`
11. `data/analysis/rotation_policy_matrix.csv`
12. `tools/analysis/validate_secrets_kms_and_rotation.(py|ts)`
13. infrastructure tests, secret-load tests, rotation smoke tests, revocation tests, and leak-prevention checks
14. `tests/playwright/secret-and-key-rotation-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- provision secret classes for runtime services, gateways, workers, CI, preview environments, and integration adapters
- provision a key hierarchy that can support data-plane encryption, object-storage encryption, queue or broker secrets, CI signing material refs, and provider credential wrapping without collapsing those uses into one key
- publish machine-readable manifests for secret classes, access scopes, and rotation windows
- implement secret-loading libraries that refresh or reload from the secret backend without code changes
- implement fail-closed startup and reload behavior when a required secret is missing, stale, revoked, or inaccessible
- keep audit-safe access logging and break-glass-ready read posture available without logging secret values

Execution steps:
1. Re-read the security-baseline and Phase 0 secret-injection rules, then derive the secret classes required by the current runtime topology and workload families.
2. Honor the cloud, trust-zone, and runtime topology decisions from earlier tasks; if prerequisites are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
3. Provision the secret backend, key hierarchy, access policies, and machine-readable manifests from code.
4. Implement shared secret-loading libraries and config adapters for services, gateways, workers, and CI jobs.
5. Implement rotation hooks and fail-closed startup or reload behavior, including safe cache invalidation of fetched secrets.
6. Build the atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write smoke tests proving secrets never appear in source snapshots, build artifacts, logs, or browser bundles; stale or revoked secrets must halt or degrade safely.

Mandatory gap closures you must perform:
- Close the “environment loading is just `.env` wiring” gap by publishing secret classes and retrieval law.
- Close the “one master key is enough” gap by creating a layered key hierarchy and secret classes.
- Close the “rotation requires code change or restart folklore” gap by implementing rotation-ready retrieval seams.
- Close the “CI variables are an acceptable long-term secret store” gap by publishing explicit CI secret classes and secure retrieval.
- Close the “break-glass and audit are disconnected from secret reads” gap by preparing access logging and break-glass-ready posture.

Industry-best-practice requirements:
- managed secret store or equivalent emulated contract for non-production
- envelope encryption and key separation by purpose
- least-privilege access per workload family and service identity
- rotation and revocation under version control and machine-readable policy
- no raw secrets in source control, logs, build output, browser assets, or screenshots
- deterministic startup validation and safe runtime reload

Non-negotiable rules:
- Secrets never appear in source, logs, or build output.
- Long-lived CI variables are not authoritative secret storage.
- Key hierarchy must separate purposes and blast radii.
- Missing, stale, or revoked secrets must fail closed.
- Secret values may never reach browser bundles or public diagnostics.

Mandatory UI and interaction specification for `89_secret_and_key_rotation_atlas.html`:
Name the surface mode `Secret_And_Key_Rotation_Atlas`.

Tone:
- premium security-control atlas
- minimalist, precise, and quietly fortified
- should feel like an executive-grade trust map, not a password manager clone

Branding:
- restrained Vecells wordmark plus inline SVG `SK` monogram
- one key-hierarchy diagram
- one secret-class access matrix
- one rotation and revocation timeline strip

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- key accent `#2563EB`
- secret accent `#0EA5A4`
- rotation accent `#7C3AED`
- warning accent `#D97706`
- revoked accent `#C24141`
- verified accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for secret-class IDs, key IDs, access-policy refs, and rotation windows

Layout:
- sticky masthead with active environments, secret drift alerts, overdue rotations, and revoked-access warnings
- left rail (`324px`) with environment filters, secret-class filters, key-purpose filters, and access-state filters
- central canvas with:
  - key-hierarchy diagram
  - secret-class access matrix
  - rotation and revocation timeline strip
- right inspector (`420px`) for selected secret class, key, or access policy
- lower grid with one manifest table and one rotation / access policy table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by environment, workload family, secret class, key purpose, and access state
- selecting a row must synchronize diagrams, timeline, inspector, and tables
- keyboard navigation across filters and tables
- deterministic `data-testid` markers for hierarchy diagram, access matrix, rotation timeline, manifest table, policy table, and inspector
- every visual must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification
- verification that revoked and overdue states remain visually and semantically distinct from verified states

Definition of done:
- The repository has a governed secret and key-management baseline.
- Secret classes, key hierarchy, access scopes, and rotation windows are machine-readable.
- Services and pipelines can load and rotate secrets without code changes.
- Later provider, gateway, publication, and preview tasks can consume this baseline without redefining secret law.
```
