# Prompt 071 For 071_par_phase0_track_backend_implement_request_lifecycle_lease_lineage_fence_and_command_action_records

Use the shared operating contract first:
[shared_operating_contract_066_to_075.md](/Users/test/Code/V/prompt/shared_operating_contract_066_to_075.md)

```text
Execute roadmap task 071_par_phase0_track_backend_implement_request_lifecycle_lease_lineage_fence_and_command_action_records.md.

Inherit the Shared_operating_contract_for_prompts_066_to_075 and consume the validated outputs from tasks `001-070`.

Mission:
Implement the distributed-control primitives that later staff, patient, worker, booking, hub, pharmacy, and governance mutations will rely on:
- `RequestLifecycleLease`
- `StaleOwnershipRecoveryRecord`
- `LeaseTakeoverRecord`
- `LineageFence`
- `CommandActionRecord`
plus the minimal compare-and-set and validation machinery needed so these are enforceable primitives rather than documentation concepts.

This task is foundational backend control-plane implementation. Treat lease, fence, and action-record law as non-optional.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.10 RequestLifecycleLease`
  - `#### 1.10A StaleOwnershipRecoveryRecord and LeaseTakeoverRecord`
  - `#### 1.11 LineageFence`
  - `#### 1.22 CommandActionRecord`
  - the canonical rules that stale-owner recovery is mandatory and cross-domain invariant changes must present the current lineage epoch
- `phase-3-the-human-checkpoint.md` and `staff-workspace-interface-architecture.md`, where staff actions, queue launch, next-task continuity, and review focus require live lease posture and source queue snapshot continuity
- `platform-runtime-and-release-blueprint.md`, where every mutation contract must bind route intent, idempotency, acting context, and command settlement lineage
- `forensic-audit-findings.md`, especially Findings `15`, `75`, `93`, and the corrections around mutation leases and route / settlement grounding
- outputs from tasks `001-007`, `041-048`, `054-057`, `061-070`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Publish minimal shared lease/fence interfaces for sibling domains if necessary, but do not wait for lifecycle coordinator or ranking coordinator tasks.

Mock_now_execution:
Build a deterministic concurrency harness now. It must simulate:
- two reviewers attempting to act on the same task / case
- worker restarts with stale fencing tokens
- supervisor takeover while a stale owner is still open
- cross-domain close/reopen attempts with stale lineage epochs
- repeated UI actions that must reuse or supersede the same command action record
The harness must use the same compare-and-set primitives production will require later.

Actual_production_strategy_later:
When real multi-worker infrastructure, real staff sessions, and real external domains arrive, they must all call the same lease, fence, and action-record primitives. Production scale may change storage tuning, heartbeat cadence, or observability, but it may not change the monotone epoch, fencing, or action-record law.

Primary outcome:
The repository gains real lease, fence, and action-record infrastructure that later domains can use to prevent stale writes, reconstruct authoritative mutation intent, and keep same-shell recovery honest.

Implementation deliverables to create:
1. domain and persistence code for `RequestLifecycleLease`, `StaleOwnershipRecoveryRecord`, `LeaseTakeoverRecord`, `LineageFence`, and `CommandActionRecord`
2. compare-and-set lease acquire / heartbeat / release / break / takeover primitives
3. lineage-fence issuance and validation utilities
4. action-record factory and validators that bind route intent tuple, context boundaries, idempotency, acting context, and governing object target
5. `docs/architecture/71_lease_fence_and_command_action_design.md`
6. `docs/architecture/71_stale_owner_and_lineage_epoch_rules.md`
7. `docs/architecture/71_control_plane_lab.html`
8. `data/analysis/request_lifecycle_lease_manifest.json`
9. `data/analysis/lineage_fence_and_command_action_matrix.csv`
10. `data/analysis/stale_ownership_casebook.json`
11. `tools/analysis/validate_lease_fence_action_models.(py|ts)`
12. unit tests, concurrency tests, takeover tests, and integration tests
13. `tests/playwright/control-plane-lab.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- acquire, heartbeat, release, break, and takeover `RequestLifecycleLease` under compare-and-set semantics
- mint monotone `ownershipEpoch` and `fencingToken` pairs for every ownership change
- open or update `StaleOwnershipRecoveryRecord` on stale write rejection, heartbeat expiry, lineage drift, reacquire, or supervisor takeover
- mint `LineageFence` for cross-domain invariant changes and validate current epoch on writes
- persist immutable `CommandActionRecord` that is sufficient to reconstruct exact route-intent tuple, governing object target, context boundary crossing, idempotency link, and initiating UI causality

Execution steps:
1. Implement the lease model with explicit TTL, fencing token, ownership epoch, heartbeat, break eligibility, and supersession links.
2. Implement authoritative compare-and-set flows for lease acquisition, renewal, release, supervisor takeover, and stale-write rejection.
3. Implement `StaleOwnershipRecoveryRecord` and `LeaseTakeoverRecord` so stale-owner recovery is visible and actionable rather than log-only.
4. Implement `LineageFence` issuance and validation for cross-domain invariant changes such as close, reopen, identity repair, ownership change, urgent preemption, and cross-domain commit.
5. Implement `CommandActionRecord` exactly from the canonical fields, including route contract digest, required context boundaries, action scope, acting context, idempotency refs, source command ID, transport correlation ID, causal token, and route-intent tuple hash.
6. Implement validators that fail when:
   - stale ownership tokens are accepted after takeover or expiry
   - lineage-changing writes do not present the current epoch
   - cross-context mutations omit required context-boundary refs
   - action records cannot reconstruct the exact governing target and route-intent tuple proven at write time
7. Build the lab and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.

Mandatory gap closures you must perform:
- Close the “task ownership is a soft flag” gap by implementing real lease and fencing primitives.
- Close the “stale-owner recovery is log-only” gap by making recovery a first-class record with same-shell recovery refs.
- Close the “cross-domain writes can race on stale lineage knowledge” gap with explicit `LineageFence` validation.
- Close the “mutations can be inferred later from route params or cached UI state” gap by persisting immutable `CommandActionRecord` tuples.
- Close the “supervisor takeover is a blind overwrite” gap with explicit takeover records, new epochs, and new fencing tokens.

Industry-best-practice requirements:
- compare-and-set persistence and concurrency tests
- monotone versioning and epoch advancement
- explicit stale-writer rejection paths
- immutable action records with joinable causality refs
- no implicit lease resurrection or takeover-by-overwrite
- deterministic concurrent test harnesses

Non-negotiable rules:
- `RequestLifecycleLease` is a real distributed-systems primitive.
- `ownershipEpoch` and `fencingToken` must change monotonically with ownership changes.
- `LineageFence` must guard cross-domain invariant changes.
- `CommandActionRecord` must be sufficient to reconstruct the exact write tuple later.
- Stale writes fail closed to recovery or reacquire posture.

Mandatory UI and interaction specification for `71_control_plane_lab.html`:
Name the surface mode `Lease_Fence_Command_Control_Lab`.

Tone:
- premium internal control-plane instrument
- exact, composed, and low-noise
- should feel like a command-center for write authority, not a log dump

Branding:
- restrained Vecells wordmark plus inline SVG `LC` monogram
- one lease timeline rail
- one lineage-epoch strip
- one command-action inspector

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F8`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- lease accent `#3559E6`
- fence accent `#0EA5A4`
- action accent `#7C3AED`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for epoch numbers, fencing tokens, action IDs, and digest refs

Layout:
- masthead with active leases, stale-owner recoveries, fence mismatches, and action records
- left rail (`304px`) for domain, lease-state, and fence-scope filters
- center canvas with lease timeline, fence strip, and action cards
- right inspector (`408px`) for selected lease / fence / action details
- lower region with concurrency-case table and validator results table

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- timeline min height `250px`
- cards min height `168px`
- controls `44px`

Motion:
- `120ms` hover/focus
- `180ms` filter and timeline transitions
- `220ms` inspector reveal
- reduced-motion support mandatory

Interaction requirements:
- filter by domain, lease state, and fence issue type
- selecting a lease or action must synchronize timeline, epoch strip, inspector, and case table
- keyboard navigation across filters, cards, and rows
- deterministic `data-testid` markers for timeline, epoch strip, inspector, case table, and validator table
- every diagram needs adjacent text parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior
- selection synchronization
- diagram / table parity
- keyboard navigation
- reduced motion
- responsive layout
- accessibility smoke checks

Definition of done:
- The repository has real lease, fence, and command-action primitives with compare-and-set safety.
- Stale-owner recovery and cross-domain epoch drift are explicit and testable.
- Later mutation services can rely on authoritative write tuples instead of ad hoc per-route conventions.
```
