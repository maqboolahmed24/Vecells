# Prompt 003 For 003_seq_define_product_scope_boundary_and_non_goals

Use the shared operating contract first:
[shared_operating_contract_001_to_005.md](/Users/test/Code/V/prompt/shared_operating_contract_001_to_005.md)

```text
Execute roadmap task 003_seq_define_product_scope_boundary_and_non_goals.md.

Inherit the Shared_operating_contract_for_prompts_001_to_005 and consume the outputs from tasks 001-002.

Mission:
Define the explicit product scope boundary, delivery baseline, deferred scope, and hard non-goals for Vecells. Convert dispersed narrative into enforceable scope statements that later engineering, testing, and release tasks can follow without ambiguity.

Algorithm grounding you must honor:
- blueprint-init.md defines Vecells as a demand orchestration system for primary care, not an appointments-first product.
- blueprint-init.md says web, NHS App jump-off, and phone converge into one request pipeline.
- blueprint-init.md defines booking as only one subset of outcomes and splits booking into local booking orchestrator plus network coordination desk.
- blueprint-init.md defines Pharmacy First as a structured referral and closure loop, not direct GP record mutation by Vecells.
- blueprint-init.md defines analytics as an operational subsystem, not a BI afterthought.
- blueprint-init.md defines a modular platform with bounded contexts, not an unstructured monolith and not tightly coupled microservice sprawl.
- phase-cards.md says the current programme baseline is Phases 0-6 + 8 + 9, with Phase 7 (NHS App embedded channel expansion) deferred.
- vecells-complete-end-to-end-flow.md makes assistive behavior optional and bounded, not a mandatory linear gate.
- forensic-audit-findings.md identifies unsafe simplifications that must become explicit non-goals.

Deliverables to create:
1. docs/architecture/03_product_scope_boundary.md
2. docs/architecture/03_non_goals_and_explicit_exclusions.md
3. docs/architecture/03_current_delivery_baseline.md
4. docs/architecture/03_deferred_and_conditional_scope.md
5. docs/architecture/03_scope_decision_log.md
6. data/analysis/product_scope_matrix.csv
7. data/analysis/product_scope_matrix.json
8. tools/analysis/validate_scope_boundary.(py|ts)

Your scope artifact must classify every major capability as one of:
- core_now
- core_enabling_control_plane
- deferred_channel_expansion
- future_optional
- explicitly_out_of_scope
- prohibited_architecture_pattern

Required scope categories:
- intake and safety
- identity and access
- patient portal/account/communications
- clinical workspace
- support workspace
- operations console
- governance/admin shell
- local booking
- network/hub coordination
- pharmacy referral loop
- assistive layer
- assurance ledger
- runtime/release control plane
- external integrations/adapters
- analytics and continuity evidence

Execution steps:
1. Derive the product mission from canonical documents, then decompose it into bounded in-scope capabilities.
2. Produce a phase-aware scope matrix showing:
   - capability,
   - why it exists,
   - which phase(s) own it,
   - whether it is patient-visible, staff-visible, ops-visible, or governance-visible,
   - which dependencies gate it,
   - whether it is required for current baseline completion.
3. Write a non-goals document that is just as explicit as the in-scope document. Non-goals must be direct, testable exclusions.

Mandatory non-goals or exclusions you must encode unless the corpus explicitly contradicts them:
- Vecells is not a thin appointment-booking front end.
- Vecells is not allowed to fork phone requests into a separate operational workflow.
- Vecells is not a native NHS App product in the current baseline; embedded NHS App support is a deferred channel expansion.
- Vecells is not allowed to hard-code supplier-specific booking/business logic into the core model.
- Vecells is not allowed to infer clinical authorization, request claim, or writable resume directly from NHS login success.
- Vecells is not allowed to treat authentication as consent.
- Vecells is not allowed to auto-merge duplicates without governed review.
- Vecells is not allowed to let child domains directly write canonical Request.workflowState or closure.
- Vecells is not allowed to imply exclusivity from countdown UX without real reservation truth.
- Vecells is not allowed to imply booked-state reassurance when supplier confirmation is ambiguous.
- Vecells is not allowed to treat Pharmacy First as direct GP record mutation by Vecells.
- Vecells is not allowed to make AI assistance mandatory for request progression or clinical decisioning.
- Vecells is not allowed to treat analytics, assurance, recovery, support, or governance as post-hoc add-ons.

4. Build a deferred/conditional scope artifact for:
   - NHS App embedded channel
   - optional PDS enrichment
   - model-vendor-backed assistive rollout cohorts
   - supplier capabilities that vary behind adapter seams
5. For every scope item, record the external dependencies and whether the dependency is:
   - required at baseline,
   - optional,
   - feature-flagged,
   - deferred,
   - replaceable by simulator.
6. Add a scope decision log that records:
   - the source statement,
   - the normalized scope interpretation,
   - the reason,
   - any assumption,
   - any risk introduced by the interpretation.

Gap resolution requirements:
- Convert broad prose such as “the patient can” or “the product should” into enforceable scope statements with acceptance implications.
- Close the “scope is scattered across many docs” gap by producing one bounded baseline definition.
- Close the “deferred channel ambiguity” gap by making Phase 7 deferral explicit in the baseline scope.
- Close the “feature vs control-plane” ambiguity by explicitly keeping runtime/release, assurance, governance, and continuity proof in scope as foundational control-plane work.
- Close the “AI posture” ambiguity by separating assistive optionality from core functional completeness.
- Close the “pharmacy boundary” ambiguity by explicitly separating referral/dispatch/outcome reconciliation from third-party pharmacy system behavior.
- Close the “booking boundary” ambiguity by explicitly separating core booking orchestration from supplier implementations.

Security, performance, and architectural best-practice requirements to reflect in scope:
- bounded contexts are in scope
- append-only audit is in scope
- outbox/event-bus reliability is in scope
- object-store quarantine for binary artifacts is in scope
- RBAC + ABAC + break-glass governance is in scope
- release/publication parity is in scope
- backup/restore/recovery control is in scope
- direct ad hoc transport success as business truth is out of scope

Definition of done:
- The current delivery baseline is explicit and phase-aware.
- Deferred and conditional work is clearly separated from current baseline scope.
- Non-goals are concrete enough to block architectural drift.
- Every major capability has a classification and source-backed rationale.
- Later implementation agents could determine whether a requested feature belongs in the product, is deferred, or must be rejected as a non-goal.
```
