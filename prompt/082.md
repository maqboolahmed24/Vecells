# Prompt 082 For 082_par_phase0_track_backend_build_projection_rebuild_worker_and_event_applier

Use the shared operating contract first:
[shared_operating_contract_076_to_085.md](/Users/test/Code/V/prompt/shared_operating_contract_076_to_085.md)

```text
Execute roadmap task 082_par_phase0_track_backend_build_projection_rebuild_worker_and_event_applier.md.

Inherit the Shared_operating_contract_for_prompts_076_to_085 and consume the validated outputs from tasks `001-081`.

Mission:
Build the deterministic event-driven projection substrate that lets Vecells rebuild audience-safe read models from immutable events:
- `EventApplier`
- `ProjectionRebuildWorker`

This task must make replay, projection rebuild, catch-up, and version-aware read-model refresh explicit and restart-safe. It must not turn projections into a second source of authoritative domain truth, and it must not let audience surfaces imply calm completeness while rebuild or compatibility posture is degraded.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### ProjectionContractFamily`
  - `### ProjectionContractVersion`
  - `### ProjectionContractVersionSet`
  - `### ProjectionReadinessVerdict`
  - `### SchemaMigrationPlan`
  - `### ProjectionBackfillPlan`
  - `### ReadPathCompatibilityWindow`
  - `## Data persistence and migration contract`
  - `## Verification ladder contract`
- `phase-0-the-foundation-protocol.md`, especially the canonical event and projection principles, runtime publication law, and the rule that only immutable events plus published contracts drive read-model truth
- `phase-1-the-red-flag-gate.md`, especially the projection rebuild and receipt/status parity test requirements
- `staff-workspace-interface-architecture.md`, especially the backfill and committed-snapshot consistency rules for workbench state
- `forensic-audit-findings.md`, especially Findings `44`, `61`, `62`, `63`, `68`, `91`, `102`, `105`, and `119`
- outputs from tasks `001-007`, `041-052`, `056-060`, `061`, `064-065`, `071-081`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Do not wait for the full migration runner from later tasks. If `SchemaMigrationPlan`, `ProjectionBackfillPlan`, or `ProjectionReadinessVerdict` implementations are not present yet, publish the smallest bounded execution-ledger and verdict stubs required for deterministic rebuild behavior, and record `PARALLEL_INTERFACE_GAP_*`.

Mock_now_execution:
Build a local immutable-event replay lab now. It must cover:
- cold rebuild from an empty projection store
- partial rebuild from a checkpoint
- contract-version mismatch blocking apply
- additive projection evolution with dual-read compatibility
- replay after crash or worker restart
- stale read posture where shells must degrade instead of implying completeness
The mock path must use the same event shapes, contract digests, checkpoints, and readiness verdicts production will later require.

Actual_production_strategy_later:
When live event-bus, migration-runner, backfill-runner, and projection stores are fully onboarded, they must consume the same `EventApplier` and `ProjectionRebuildWorker` contracts. Production infrastructure may change transport, scale, and operational controls, but it may not change deterministic apply semantics, checkpoint rules, or the separation between authoritative domain truth and audience projections.

Primary outcome:
The repository gains a deterministic read-model rebuild engine that can replay immutable events into audience projections safely, while exposing explicit readiness and compatibility posture instead of letting shells guess.

Implementation deliverables to create:
1. `EventApplier` library with contract-version-aware, idempotent apply semantics
2. `ProjectionRebuildWorker` with checkpointed replay, resumability, and bounded parallelism
3. bounded execution-ledger and cursor models for rebuild progress and restart safety
4. compatibility and readiness evaluator stubs sufficient to block stale or incompatible projection publication
5. replay CLI or job runner for targeted projection rebuild and dry-run validation
6. canonical-event consumption registry and applier-dispatch table
7. `docs/architecture/82_projection_rebuild_and_event_applier_design.md`
8. `docs/architecture/82_projection_rebuild_readiness_and_compatibility_rules.md`
9. `docs/architecture/82_projection_rebuild_observatory.html`
10. `data/analysis/projection_rebuild_casebook.json`
11. `data/analysis/event_applier_dispatch_matrix.csv`
12. `data/analysis/projection_checkpoint_manifest.json`
13. `tools/analysis/validate_projection_rebuild_worker.(py|ts)`
14. unit tests, replay tests, restart tests, version-mismatch tests, and integration tests
15. `tests/playwright/projection-rebuild-observatory.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- replay immutable canonical events into projection stores deterministically
- maintain per-projection or per-family checkpoints and restart-safe cursors
- block apply or publication when contract-version compatibility is not exact or explicitly dual-read compatible
- support full rebuild, bounded replay, resume, and dry-run validation
- emit explicit readiness posture so shells and operators know when a projection is live, stale, blocked, or recovering
- prevent any write path from treating projections as canonical domain authority

Execution steps:
1. Implement the `EventApplier` with typed handlers bound to the canonical event registry from prior tasks; fail closed on unknown or incompatible event versions.
2. Implement the rebuild worker with deterministic ordering, checkpoint persistence, and crash-safe resume.
3. Publish bounded readiness and compatibility verdicts sufficient to keep route publication and writable posture from implying safe reads while rebuild state is degraded.
4. Implement dry-run comparison mode so rebuild output can be validated before cutover.
5. Expose replayable diagnostics and progress metrics through machine-readable ledgers and the observatory.
6. Build the observatory and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write tests for full rebuild, partial rebuild, restart, version drift, and stale-surface downgrade behavior.

Mandatory gap closures you must perform:
- Close the “projection rebuild is operational folklore instead of governed runtime behavior” gap by implementing explicit rebuild worker and event-applier authority.
- Close the “read models can be rebuilt from projections or dashboard memory” gap by replaying immutable canonical events only.
- Close the “mixed-version projections can still look healthy” gap with explicit compatibility and readiness posture.
- Close the “route shells imply completeness while rebuild lags or blocks” gap by surfacing stale / blocked posture explicitly.
- Close the “worker restart can duplicate or skip apply effects” gap with checkpointed deterministic replay.
- Close the “projection logic silently diverges from event contracts” gap with version-aware applier dispatch and validator coverage.

Industry-best-practice requirements:
- idempotent event application
- deterministic ordering and checkpointing
- bounded parallelism with explicit partition safety
- dry-run and compare-before-cutover support
- no ad hoc mutation of projection rows outside the rebuild or live-applier path
- clear separation between domain store, event spine, and projection stores

Non-negotiable rules:
- Projections are audience-safe read truth, not canonical domain truth.
- `EventApplier` must fail closed on unknown or incompatible event versions.
- Projection rebuild must be restart-safe and deterministic.
- Audience surfaces may not imply live completeness when readiness is stale or blocked.
- Contract drift must block publication or writable posture, not merely log a warning.

Mandatory UI and interaction specification for `82_projection_rebuild_observatory.html`:
Name the surface mode `Projection_Rebuild_Observatory`.

Tone:
- premium runtime and data-operations surface
- sleek, measured, and technically exact
- should feel like an event-spine observatory, not a batch-job admin page

Branding:
- restrained Vecells wordmark plus inline SVG `PR` monogram
- one event-to-projection flow diagram
- one checkpoint-lag heat surface
- one readiness and compatibility strip

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- event accent `#2563EB`
- checkpoint accent `#0EA5A4`
- compatibility accent `#7C3AED`
- lag accent `#D97706`
- blocked accent `#C24141`

Typography:
- system sans stack only
- monospaced digest styling for contract digests, projection versions, checkpoint tokens, and rebuild job IDs

Layout:
- sticky masthead with active rebuilds, blocked applies, stale projections, and restart-safe resumes
- left rail (`324px`) with projection-family filters, readiness filters, compatibility filters, and environment filters
- central canvas with:
  - event-to-projection flow diagram
  - checkpoint-lag heat surface
  - readiness / compatibility strip
- right inspector (`420px`) for selected projection family, current checkpoint, contract version, and rebuild state
- lower grid with one rebuild ledger table and one applier-dispatch / verdict table

Dimensional constraints:
- max width `1560px`
- masthead `76px`
- visual zone min height `300px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by projection family, readiness state, compatibility state, and environment
- selecting a rebuild or projection row must synchronize flow diagram, lag surface, readiness strip, inspector, and tables
- keyboard navigation across filters, tables, and inspector
- deterministic `data-testid` markers for flow diagram, lag surface, readiness strip, rebuild table, applier table, and inspector
- charts and diagrams must have adjacent textual parity and table fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification

Definition of done:
- The repository has a deterministic event applier and projection rebuild worker with restart-safe behavior.
- Projection rebuild and readiness posture are explicit and machine-readable.
- Read-model drift, version mismatch, and replay issues are detectable before they leak into calm audience surfaces.
- Later migration, backfill, live-update, and runtime-publication tasks can build on this substrate directly.
```
