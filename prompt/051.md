# Prompt 051 For 051_seq_phase0_define_release_candidate_freeze_and_publication_parity_strategy

Use the shared operating contract first:
[shared_operating_contract_046_to_055.md](/Users/test/Code/V/prompt/shared_operating_contract_046_to_055.md)

```text
Execute roadmap task 051_seq_phase0_define_release_candidate_freeze_and_publication_parity_strategy.md.

Inherit the Shared_operating_contract_for_prompts_046_to_055 and consume the validated outputs from tasks `001-050`.

Mission:
Define the canonical release-candidate freeze and publication-parity strategy for Vecells so promoted runtime truth is pinned to one coherent approval tuple and one exact publication parity verdict before any writable or calmly trustworthy surface remains live.

This task must make release approval, runtime publication, provenance, parity, and watch posture machine-readable and fail-closed.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`
  - `ReleaseApprovalFreeze`
  - `RuntimePublicationBundle`
  - `AudienceSurfaceRuntimeBinding`
  - the invariants requiring exact publication parity and compatible freeze/channel posture before writable routes stay live
- `platform-runtime-and-release-blueprint.md`
  - `ReleaseCandidate`
  - `BuildProvenanceRecord`
  - `RuntimePublicationBundle`
  - `ReleasePublicationParityRecord`
  - `WaveGuardrailSnapshot`
  - `ReleaseWatchTuple`
  - `WaveObservationPolicy`
  - `ReleaseRecoveryDisposition`
  - `ReleaseWatchEvidenceCockpit`
  - the rules that topology, manifests, design bundles, schemas, migrations, parity, and provenance must publish together
- `phase-cards.md`
  - the requirement to promote one signed release candidate with frozen artifacts, bundle hashes, schemas, migrations, and environment compatibility
- `forensic-audit-findings.md`
  - findings `91`, `95`, `103`, `104`, `118`
- outputs from tasks `015`, `016`, `046-050`

Primary outcome:
One release and publication-parity strategy proving:
- what a promotable release candidate contains
- how the approved freeze tuple is assembled
- how runtime publication is published and compared
- how exact parity is measured
- how watch tuples and observation policy depend on parity
- how drift fails closed before browser writability, calm trust, or wave widening remain active

Deliverables to create:
1. `docs/architecture/51_release_candidate_freeze_strategy.md`
2. `docs/architecture/51_publication_parity_strategy.md`
3. `docs/architecture/51_release_parity_cockpit.html`
4. `data/analysis/release_candidate_schema.json`
5. `data/analysis/release_approval_freeze_schema.json`
6. `data/analysis/release_publication_parity_rules.json`
7. `data/analysis/release_freeze_tuple_matrix.csv`
8. `data/analysis/release_watch_required_evidence.csv`
9. `tools/analysis/validate_release_freeze_and_parity.(py|ts)`
10. `tests/playwright/release-parity-cockpit.spec.(ts|js)`

At minimum, define release-candidate and freeze artifacts that cover:
- git ref and artifact digests
- bundle hash refs
- behavior contract set
- surface schema set
- release contract verification matrix
- environment compatibility
- schema migration plan
- projection backfill plan
- runtime topology manifest ref
- topology tuple hash
- release approval freeze ref
- channel manifest set ref
- minimum bridge capability set ref
- required assurance slice refs
- watch tuple hash
- publication parity ref
- runtime publication bundle ref
- SBOM ref
- provenance ref
- approval refs
- wave state

For `ReleaseApprovalFreeze`, define at minimum:
- `releaseApprovalFreezeId`
- `releaseCandidateRef`
- `governanceReviewPackageRef`
- `standardsDependencyWatchlistRef`
- `compiledPolicyBundleRef`
- `baselineTupleHash`
- `scopeTupleHash`
- `compilationTupleHash`
- `approvalTupleHash`
- `reviewPackageHash`
- `standardsWatchlistHash`
- `artifactDigestSetHash`
- `surfaceSchemaSetHash`
- `bridgeCapabilitySetHash`
- `migrationPlanHash`
- `compatibilityEvidenceRef`
- `approvedBy`
- `approvedAt`
- `freezeState`

Execution steps:
1. Start from the runtime/publication artifacts defined in tasks `046-050`.
2. Publish the release-candidate composition model. Explicitly state which artifact families are frozen together and why.
3. Publish the approval-freeze tuple model and explain why:
   - artifacts
   - policies
   - schemas
   - bridge capabilities
   - migration posture
   - baseline snapshot
   - review package
   - standards watchlist
   must promote and roll back as one unit.
4. Define the runtime-publication and publication-parity comparison process:
   - what the published bundle contains
   - which digests must match exactly
   - what counts as stale, conflict, withdrawn, provenance-blocked, or blocked
5. Define how `ReleasePublicationParityRecord` is computed and how it gates:
   - audience-surface runtime binding
   - browser writability
   - calm trust posture
   - route activation
   - wave widening
6. Define the minimum watch tuple and observation policy evidence set so operations and governance consume published tuple truth rather than dashboards or chat.
7. Build the cockpit and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that:
   - no promotable release candidate lacks a freeze tuple
   - no freeze tuple omits reviewed baseline, scope, standards watchlist, or migration posture
   - no runtime publication can be considered live without parity evaluation
   - no exact parity verdict can be emitted if topology, manifests, design bundles, schemas, provenance, or recovery dispositions drift
   - no writable surface could remain live under stale or conflicting parity

Mandatory gap closures you must perform:
- Close the “release hardening froze provenance and rollback separately instead of one tuple” gap from `phase-cards.md`.
- Close the “route, settlement, release, and trust controls are phase-local conventions” gap from forensic finding `91`.
- Close the “governance watch surfaces omit parity and recovery posture” gap from forensic findings `95` and `104`.
- Close the “token/design publication can drift outside the release tuple” gap from forensic finding `118`.
- Close the “green deployment can coexist with stale publication truth” gap by making exact parity mandatory for writable posture.
- Close the “wave widening can depend on dashboards rather than the published watch tuple” gap.

Industry-best-practice requirements:
- immutable release-candidate composition
- signed provenance and SBOM linkage
- deterministic tuple hashing
- explicit migration and backfill binding
- environment compatibility as a release artifact
- fail-closed parity verification
- machine-readable watch/observation prerequisites
- no manual spreadsheet parity

Non-negotiable rules:
- Production-facing mutating routes must pin one approved release tuple rather than relying on a floating bundle hash alone.
- Artifacts, policies, schemas, bridge capabilities, migration posture, immutable baseline, and approval package must promote and roll back as one coherent approval unit.
- `ReleasePublicationParityRecord(parityState = exact)` is mandatory for writable or calmly trustworthy posture.
- If topology, manifests, design bundles, schemas, parity, provenance, or recovery posture drift, writable posture must freeze before route-local cache or compiled code reopens it.
- Watch and widening truth must come from `ReleaseWatchTuple` and `WaveObservationPolicy`, not from operator memory or dashboard interpretation.

Mandatory UI and interaction specification for `51_release_parity_cockpit.html`:
Name the surface mode `Release_Parity_Cockpit`.

Tone:
- minimalist premium governance-and-runtime cockpit
- should feel more like a mission control board than a deployment dashboard
- exact, sober, high-trust, with strong emphasis on tuple integrity

Branding:
- restrained Vecells wordmark plus an inline SVG `FREEZE` monogram
- one subtle tuple-strip diagram showing candidate -> freeze -> publication bundle -> parity -> watch tuple

Palette:
- canvas `#F6F8FB`
- rail `#EEF2F7`
- panel `#FFFFFF`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- parity accent `#0EA5A4`
- governance accent `#6E59D9`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for hashes, digests, schema-set refs, and parity codes

Layout:
- sticky masthead with:
  - candidate count
  - exact-parity count
  - stale/conflict count
  - watch-tuple count
- left rail (`296px`):
  - release candidates
  - parity-state filters
  - environment filters
- center canvas:
  - tuple strip
  - parity matrix across artifacts / manifests / design bundles / schemas / migrations / provenance
- right inspector (`392px`):
  - selected release or parity verdict
  - drift reasons
  - freeze members
  - watch / observation dependencies
- lower region:
  - evidence table
  - wave watch prerequisites
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- tuple/parity canvas min height `560px`
- primary controls `44px`
- matrix rows `40px` minimum height

Motion:
- `120ms` hover/focus
- `180ms` candidate selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by environment, release candidate, parity state, and wave state
- select a candidate and keep matrix / evidence / inspector synchronized
- keyboard traversal across filters, candidate list, parity matrix, and evidence rows
- deterministic `data-testid` markers for candidate rail, tuple strip, parity matrix, inspector, and defect strip
- any diagram must have table parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- candidate filtering
- candidate selection
- matrix/inspector parity
- drift-state visibility
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks

Definition of done:
- Vecells has one explicit release-candidate freeze model and one explicit publication-parity strategy.
- Writable and calmly trustworthy posture can now be mechanically tied to exact parity rather than optimistic deploy state.
- Later runtime publication, watch, and wave-control tasks can build on a pinned release tuple instead of ad hoc release folklore.
```