# Prompt 011 For 011_seq_choose_cloud_region_tenant_model_and_trust_zone_strategy

Use the shared operating contract first:
[shared_operating_contract_011_to_015.md](/Users/test/Code/V/prompt/shared_operating_contract_011_to_015.md)

```text
Execute roadmap task 011_seq_choose_cloud_region_tenant_model_and_trust_zone_strategy.md.

Inherit the Shared_operating_contract_for_prompts_011_to_015 and consume the validated outputs from tasks 001-010.

Mission:
Choose the canonical cloud-region posture, tenant model, acting-scope model, workload-family split, trust-zone boundaries, and gateway surface topology for Vecells. This task must turn the runtime blueprint from “rich contract language” into one implementation-grade topology baseline that later infrastructure, backend, frontend, security, release, and operations work can inherit without reinterpretation.

This is not a cloud-vendor shopping exercise. It is a control-plane and blast-radius exercise. The output must explain:
- where runtime truth may live
- where browser traffic may terminate
- which workloads may cross which trust boundaries
- how tenant and organisation scope may legally move
- how publication / trust / recovery posture freezes writable behavior when topology drifts

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 1. Product definition`
  - `### 2. Core product surfaces`
  - `### 12. Practical engineering shape`
  - the requirement that Vecells runs on UK-hosted cloud infrastructure with object storage, FHIR-capable relational storage, event bus + outbox, workflow/timer capability, and append-only audit
- `phase-0-the-foundation-protocol.md`, especially:
  - `## 0A. Delivery skeleton and repository architecture`
  - `### 1. Required platform objects`
  - objects `1.36 AudienceSurfaceRouteContract` through `1.40N PatientDegradedModeProjection`
  - services `2.7 AssuranceSupervisor`, `2.8 ScopedMutationGate`, `2.10 RuntimeContractPublisher`, and `2.10A ResilienceOrchestrator`
  - the invariant set around tenant scoping, browser boundaries, release posture, and recovery control
- `platform-runtime-and-release-blueprint.md`, especially:
  - `## Runtime topology contract`
  - `### RuntimeWorkloadFamily`
  - `### TrustZoneBoundary`
  - `### RuntimeTopologyManifest`
  - `### GatewayBffSurface`
  - `## Runtime rules`
  - `## Frontend and backend integration contract`
  - `## Security baseline contract`
  - `## Operational readiness contract`
- `phase-9-the-assurance-ledger.md`, especially:
  - `## 9F. Resilience architecture, restore orchestration, and chaos programme`
  - `## 9H. Tenant governance, config immutability, and dependency hygiene`
- `platform-admin-and-config-blueprint.md`, especially:
  - `## Change control rules`
  - `## Release governance contract`
  - `## Dependency and standards hygiene`
- `forensic-audit-findings.md`, especially Findings:
  - 91 on route, settlement, release, and trust controls being too phase-local
  - 95 on governance watch-tuple parity and recovery posture drift
  - 112 on restore authority fragmenting across runbooks and dashboards
  - 114 on tenant and acting context drift
  - 119 on continuity incidents rebasing to fresher proof without bounded tuple truth
  - 120 on degraded-mode fragmentation across shells and channels

Primary outcome:
One authoritative topology baseline that answers, for every audience, workload, and environment ring:
- which UK-hosted region(s) are allowed
- which runtime workload family owns the work
- which trust zone it sits in
- how tenant context flows
- how acting-scope changes are governed
- which gateway surface the browser may use
- which cross-zone and cross-tenant transitions are legal
- what failover and degraded posture exists when topology, trust, or publication drifts

Deliverables to create:
1. `docs/architecture/11_cloud_region_and_residency_decision.md`
2. `docs/architecture/11_tenant_model_and_acting_scope_strategy.md`
3. `docs/architecture/11_trust_zone_and_workload_family_strategy.md`
4. `docs/architecture/11_gateway_surface_and_runtime_topology_baseline.md`
5. `docs/architecture/11_region_resilience_and_failover_posture.md`
6. `docs/architecture/11_trust_zone_decision_log.md`
7. `docs/architecture/11_runtime_topology_atlas.html`
8. `docs/architecture/11_runtime_topology_baseline.mmd`
9. `data/analysis/runtime_workload_families.json`
10. `data/analysis/trust_zone_boundaries.csv`
11. `data/analysis/gateway_surface_matrix.csv`
12. `data/analysis/tenant_isolation_matrix.csv`
13. `data/analysis/acting_scope_tuple_matrix.csv`
14. `data/analysis/region_resilience_matrix.json`
15. `tools/analysis/build_runtime_topology_baseline.(py|ts)`
16. `tools/analysis/validate_runtime_topology_baseline.(py|ts)`

Each workload-family / trust-zone row must capture at minimum:
- `workload_family_id`
- `family_code = public_edge | shell_delivery | command | projection | integration | data | assurance_security`
- `environment_ring`
- `uk_region_role = primary | secondary | recovery | nonprod_local`
- `trust_zone_ref`
- `owned_bounded_context_refs[]`
- `owned_service_refs[]`
- `ingress_mode`
- `allowed_downstream_family_refs[]`
- `allowed_data_classification_refs[]`
- `tenant_context_mode`
- `tenant_isolation_mode`
- `acting_scope_requirements[]`
- `assurance_trust_mode`
- `service_identity_ref`
- `egress_allowlist_ref`
- `browser_reachable = yes | no`
- `gateway_surface_refs[]`
- `session_boundary_ref`
- `failure_domain_ref`
- `backup_restore_scope_ref`
- `recovery_disposition_refs[]`
- `source_refs[]`
- `notes`

Execution steps:
1. Start from tasks 003, 004, 005, 006, 007, 008, and 010. You must not choose topology first and retrofit scope, data class, or audience surfaces afterwards.
2. Build a scorecard for region and residency posture. At minimum compare:
   - single UK-region baseline with same-region recovery only
   - dual-UK-region primary/secondary baseline
   - shared-control-plane / shared-runtime multi-tenant baseline with strong tenant isolation
   - fully isolated per-tenant deployment baseline
   - hybrid baseline with shared platform services plus tenant-scoped data/runtime slices
3. Unless the repository already pins a provider and exact regions, choose provider-neutral region roles rather than speculative vendor SKU names. Required result:
   - all production and disaster-recovery data paths remain UK-hosted
   - the topology names one `uk_primary_region` and one `uk_secondary_region` (or an equivalent provider-neutral pair)
   - blast radius and failover rules are explicit
4. Choose the tenant model. Your decision must explicitly handle:
   - ordinary tenant-scoped operations
   - platform-scoped governance and assurance actions
   - multi-tenant blast-radius reporting for releases
   - cross-organisation hub/support/governance visibility under explicit acting context
   - explicit `ActingScopeTuple` drift handling
5. Build the authoritative workload-family split using the exact `RuntimeWorkloadFamily.familyCode` set from `platform-runtime-and-release-blueprint.md`. Do not invent a second taxonomy unless you also preserve a lossless mapping back to the canonical family codes.
6. Map every current or planned runtime executable and logical runtime plane to those families, including at minimum:
   - public edge / shell delivery
   - gateway/BFF surfaces
   - command API and mutation handling
   - projection worker / projection read pipelines
   - integration workers / adapter simulators
   - data stores and storage services
   - assurance / security / audit runtime
7. Build the trust-zone map. For every cross-zone boundary, define:
   - source and target zones
   - allowed protocols
   - allowed service identities
   - allowed data classes
   - allowed tenant-transfer mode
   - assurance-trust transfer mode
   - boundary failure mode
8. Build the gateway-surface matrix for patient, workspace, support, hub, pharmacy, operations, and governance audiences. If two route families differ in tenant isolation, trust-zone boundary, downstream workload set, recovery profile, or session policy, they must not share a single broad BFF contract.
9. Define the region-resilience and failover posture. You must specify:
   - primary / secondary region roles
   - data replication posture by store class
   - queue and outbox recovery posture
   - what remains local to a region vs replicated
   - failover decision authority
   - recovery-only, read-only, or blocked shell posture when tuple drift or failover is in progress
10. Generate a browser-viewable topology atlas that helps later teams inspect workload families, trust boundaries, gateway surfaces, and tenant / acting-scope rules without replacing the machine-readable source of truth.

Mandatory gap closures you must perform:
- Close the “UK-hosted cloud” gap by translating the bootstrap statement into an enforceable region-residency rule, not a vague note.
- Close the “trust zones are loose manifest refs” gap identified in `platform-runtime-and-release-blueprint.md`.
- Close the “one generic BFF can silently become too broad” gap by explicitly splitting gateway surfaces where the blueprint requires it.
- Close the “tenant blast radius is visible only through route names or cohort labels” gap by making platform, multi-tenant, and tenant-specific blast radius machine-readable.
- Close the “acting context can drift between governance scope and live cross-organisation work” gap from Finding 114.
- Close the “browser / gateway / data boundary is folklore” gap by making browser termination and downstream families explicit.
- Close the “restore authority is fragmented across runbooks, dashboards, and release tuples” gap by tying resilience posture back to workload families, regions, and failure domains.
- Close the “degraded modes can fragment across shells or channels” gap by explicitly binding topology drift to `ReleaseRecoveryDisposition` / route recovery posture instead of generic maintenance fallback.

Non-negotiable topology rules you must encode:
- `data` workloads are never browser-addressable.
- Browsers terminate only at the public edge and published gateway / BFF layer.
- Every storage record, queue message, cache key, emitted event, and object prefix must carry `tenantId` or a stronger tenant-scoping tuple where the corpus requires it.
- Cross-tenant access is legal only through explicit acting context, immutable audit, and purpose-of-use-aware visibility control.
- Gateway surfaces may not reach adapters, raw stores, or undeclared downstream workloads directly.
- Patient and staff/admin audiences may share infrastructure only when session policy, cache scope, tenant isolation, and assurance-trust requirements remain separate and auditable.
- TrustZoneBoundary is the only legal declaration of cross-plane reachability; diagrams, hostnames, or mesh policy are not the source of truth.
- `RuntimeTopologyManifest` and `RuntimePublicationBundle` must include topology truth; infra code comments or cloud console state are not sufficient runtime authority.

Validation rules:
- Every current audience surface from task 004 must resolve to exactly one gateway surface and one browser-reachable workload family.
- Every workload family must resolve to one trust zone and one failure domain.
- Every cross-zone interaction must have an explicit boundary row.
- Any browser-reachable route or service that points directly to data or integration workloads must fail validation.
- Any tenant or platform operation that lacks explicit blast-radius, acting-scope, and audit posture must fail validation.
- Any failover posture that widens data scope or leaves writable calm posture live during tuple drift must fail validation.
- Any region posture that is not UK-hosted for production or disaster-recovery paths must fail validation.

Browser-viewable atlas requirements:
Build `11_runtime_topology_atlas.html` and validate it with `Playwright_or_other_appropriate_tooling`.

Atlas composition:
- left rail: environment ring, audience, workload family, tenant scope, and trust-zone filters
- hero band: counts for workload families, boundaries, gateway surfaces, tenant-isolation patterns, and unresolved gaps
- center canvas: region → trust-zone → workload-family topology diagram with selectable boundaries
- gateway inspector: selected audience surface, session policy, tenant-isolation mode, downstream families, and recovery disposition
- boundary inspector: protocol, identity, data class, tenant-transfer mode, assurance-trust transfer mode, and failure mode
- resilience panel: region roles, backup / restore scope, failover authority, and degraded user posture summary

Unique design requirements:
- the atlas should feel like a “topology cockpit”, not a cloud-provider diagram dump
- prefer layered topology bands, adjacency lines, and evidence drawers over generic cards
- use one subtle Vecells monogram in the header and keep the rest operationally quiet
- no stock cloud icons or decorative network imagery

Playwright validation must cover:
- topology canvas render
- filter interactions
- boundary / gateway inspector interactions
- keyboard navigation
- responsive layout
- accessibility smoke checks
- stable `data-*` markers for selected region, trust zone, workload family, gateway surface, and boundary state

Definition of done:
- Vecells now has one explicit UK-hosted runtime topology baseline.
- Tenant scope, acting scope, trust boundaries, and gateway splits are explicit enough for infra, backend, frontend, security, and release teams to build against.
- A future agent can answer “may this audience / route / service / boundary exist?” from one machine-readable topology package instead of re-reading the corpus.
```
