# Prompt 049 For 049_seq_phase0_define_fhir_representation_contract_strategy

Use the shared operating contract first:
[shared_operating_contract_046_to_055.md](/Users/test/Code/V/prompt/shared_operating_contract_046_to_055.md)

```text
Execute roadmap task 049_seq_phase0_define_fhir_representation_contract_strategy.md.

Inherit the Shared_operating_contract_for_prompts_046_to_055 and consume the validated outputs from tasks `001-048`.

Mission:
Define the canonical FHIR representation strategy for Vecells so domain aggregates remain authoritative while FHIR becomes:
- explicit, replayable representation
- governed external interchange
- callback-correlation support
- audit-companion output

This task must prevent raw FHIR resources from becoming the hidden lifecycle owner of request, booking, hub, pharmacy, communication, or audit truth.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`
  - `FhirRepresentationContract`
  - `FhirRepresentationSet`
  - `FhirResourceRecord`
  - `FhirExchangeBundle`
  - the phase-0 FHIR-side mapping law beginning `On the FHIR side, the mapping layer must now be explicit and replayable`
  - the rule that only `ClinicalRepresentationMapper` and declared adapter workloads may speak raw FHIR payloads
- `phase-3-the-human-checkpoint.md`
- `phase-4-the-booking-engine.md`
- `phase-5-the-network-horizon.md`
- `phase-6-the-pharmacy-loop.md`
  Use these to understand where external commitment, dispatch, callback-correlation, confirmation ambiguity, and outcome reconciliation create legitimate FHIR representation needs.
- `platform-runtime-and-release-blueprint.md`
  - `AdapterContractProfile`
  - the rule that adapters may consume only published `FhirRepresentationContract` rows and accepted `FhirExchangeBundle.bundleType` values
- `forensic-audit-findings.md`
  - any finding that hardens event-spine integrity, external confirmation truth, pharmacy dispatch proof, or closure/control-plane separation
- outputs from tasks `005-008`, `040`, `044`, `048`

Primary outcome:
One machine-readable FHIR representation strategy that answers:
- which domain aggregates may materialize which resource types
- for which representation purpose
- under which identifier/status/redaction/replay/supersession policies
- how outbound and inbound FHIR exchange is packaged
- what may never be treated as FHIR-owned lifecycle truth

Deliverables to create:
1. `docs/architecture/49_fhir_representation_strategy.md`
2. `docs/architecture/49_fhir_representation_contract_catalog.md`
3. `docs/architecture/49_domain_to_fhir_mapping_matrix.md`
4. `docs/architecture/49_fhir_representation_atlas.html`
5. `data/analysis/fhir_representation_contracts.json`
6. `data/analysis/fhir_mapping_matrix.csv`
7. `data/analysis/fhir_exchange_bundle_policies.json`
8. `data/analysis/fhir_identifier_and_status_policies.json`
9. `packages/fhir-mapping/contracts/` populated with machine-readable contract artifacts appropriate to the chosen stack
10. `tools/analysis/validate_fhir_representation_contracts.(py|ts)`
11. `tests/playwright/fhir-representation-atlas.spec.(ts|js)`

At minimum, for each `FhirRepresentationContract` capture:
- `fhirRepresentationContractId`
- `owningBoundedContextRef`
- `governingAggregateType`
- `representationPurpose`
- `triggerMilestoneTypes[]`
- `requiredEvidenceRefs[]`
- `allowedResourceTypes[]`
- `requiredProfileCanonicalUrls[]`
- `identifierPolicyRef`
- `statusMappingPolicyRef`
- `cardinalityPolicyRef`
- `redactionPolicyRef`
- `companionArtifactPolicyRef`
- `replayPolicyRef`
- `supersessionPolicyRef`
- `callbackCorrelationPolicyRef`
- `contractVersionRef`
- `contractState`
- `publishedAt`
- source refs and rationale

For each representation set or exchange policy define:
- what aggregate version or settlement chain triggers materialization
- which resource records belong to the set
- which bundle type is legal
- which adapter profiles may consume it
- what authoritative success looks like
- how supersession and invalidation behave
- which proof objects or audit joins are mandatory

Execution steps:
1. Start from the domain-first mapping law in Phase 0 and publish the core representation principles explicitly:
   - domain aggregates own lifecycle truth
   - FHIR is representation, not hidden aggregate schema
   - one aggregate version may emit multiple resources
   - replay must reproduce the same representation set for the same causal inputs
2. Create the domain-to-FHIR matrix. At minimum cover:
   - `Request` -> typically FHIR `Task`
   - `ServiceRequest` only when a real external or clinical service commitment exists
   - `EvidenceSnapshot`, attachments, transcripts, and presentation-safe derivatives -> `DocumentReference` plus governed binary artifact references
   - conversation and callback domain objects -> `Communication` where appropriate, while delivery and reassurance truth stays on settlement/evidence chains
   - clinically meaningful sharing or dispatch decisions -> `Consent` where appropriate
   - `AuditRecord` -> `AuditEvent` / `Provenance` as companion representation, never replacement
3. Explicitly mark internal control-plane objects that must not become FHIR-owned truth, including at minimum:
   - `AccessGrant`
   - `CapabilityDecision`
   - `Session`
   - `RequestLifecycleLease`
   - `CapacityReservation`
   - route / release / trust / parity control objects
4. Define representation-purpose profiles:
   - `clinical_persistence`
   - `external_interchange`
   - `partner_callback_correlation`
   - `audit_companion`
5. Define identifier, status-mapping, cardinality, redaction, companion-artifact, replay, supersession, and callback-correlation policies as explicit machine-readable artifacts.
6. Define `FhirExchangeBundle` strategy for outbound and inbound exchange:
   - legal bundle types
   - adapter ownership
   - correlation keys
   - receipt checkpoints
   - exchange states
   - proof upgrade path from dispatched -> accepted -> replayed / failed / superseded
7. Build the atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that:
   - every representation contract has one owning bounded context
   - no resource type is allowed without an explicit contract row
   - no internal control-plane object is accidentally modeled as FHIR authority
   - every adapter-facing exchange names a legal bundle type and contract
   - the same aggregate version rematerializes a stable representation set under replay

Mandatory gap closures you must perform:
- Close the “FHIR mapping is implicit in application code” gap by making contracts explicit.
- Close the “FHIR rows can quietly become lifecycle owners” gap by writing that prohibition into the strategy and validator.
- Close the “partner callback and external interchange use ad hoc payload shapes” gap by binding them to `FhirExchangeBundle`.
- Close the “identifier and status semantics differ per adapter implementation” gap by defining machine-readable policies.
- Close the “audit and provenance can replace internal audit truth” gap by codifying them as companion representations only.
- Close the “same aggregate version may fork resource membership on replay” gap by defining replay and supersession law.

Industry-best-practice requirements:
- contract-first mapping
- replay-safe materialization
- deterministic schema/policy artifacts
- clear separation between internal domain truth and interoperability format
- minimal PHI exposure and explicit redaction
- stable identifier policy
- explicit adapter-boundary bundle contracts
- no raw FHIR payload access outside mapper or adapter workloads

Non-negotiable rules:
- `FhirRepresentationContract` is the only legal place to define how an aggregate version, evidence snapshot, or settlement chain becomes FHIR resources.
- `FhirRepresentationSet` is the atomic mapping unit for replay, audit, and partner exchange.
- `FhirExchangeBundle` is the only FHIR payload family adapters may send or accept at the platform boundary.
- FHIR `Task`, `ServiceRequest`, `DocumentReference`, `Communication`, `Consent`, `AuditEvent`, and `Provenance` may not replace aggregate lifecycle, blocker, or continuity truth.
- Shells, projections, and support tools may not improvise raw resource shapes or infer business truth from partial resource subsets.

Mandatory UI and interaction specification for `49_fhir_representation_atlas.html`:
Name the surface mode `Clinical_Representation_Atlas`.

Tone:
- minimalist premium internal clinical-integration atlas
- domain-first, precise, calm
- should feel like a mapping authority instrument, not a generic interoperability dashboard

Branding:
- restrained Vecells wordmark plus a compact inline SVG `FHIR` monogram
- include one subtle domain-to-resource braid diagram, never a decorative hospital icon set

Palette:
- canvas `#F7F9FC`
- rail `#EEF3F8`
- panel `#FFFFFF`
- inset `#F3F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- domain accent `#3559E6`
- representation accent `#0EA5A4`
- interchange accent `#6E59D9`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono only for resource types, profile URLs, contract versions, IDs, and hashes

Layout:
- sticky masthead with:
  - contract count
  - mapped aggregate count
  - exchange-bundle policy count
  - blocked mapping count
- left rail (`284px`):
  - aggregate-type filters
  - representation-purpose filters
- center region:
  - domain-to-resource mapping table
  - compact braid diagram showing aggregate -> representation set -> resources -> exchange bundle
- right inspector (`392px`):
  - selected contract details
  - allowed resource types
  - identifier/status/redaction policies
  - replay/supersession rules
- lower region:
  - exchange-bundle matrix
  - policy ledger
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- primary mapping table min height `520px`
- primary controls `44px`
- policy chips `28px`

Motion:
- `120ms` hover/focus
- `180ms` row selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by aggregate type, owning context, representation purpose, resource type, and defect state
- select a mapping row and synchronize the inspector plus lower matrices
- keyboard navigation across filters, mapping rows, and policy tables
- stable `data-testid` markers for aggregate rail, mapping table, braid diagram, inspector, and defect strip
- every diagram must have adjacent table/list parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- aggregate filtering
- mapping-row selection
- inspector rendering
- bundle-policy visibility
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks

Definition of done:
- Vecells has one explicit FHIR representation strategy that keeps domain truth authoritative.
- Every allowed representation has a contract row, policy set, and bundle policy.
- Adapter and publication work can now consume FHIR authority without re-inventing mapping semantics in code.
```