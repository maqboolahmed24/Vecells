# Prompt 065 For 065_par_phase0_track_backend_build_api_contract_registry_for_queries_mutations_and_live_channels

Use the shared operating contract first:
[shared_operating_contract_056_to_065.md](/Users/test/Code/V/prompt/shared_operating_contract_056_to_065.md)

```text
Execute roadmap task 065_par_phase0_track_backend_build_api_contract_registry_for_queries_mutations_and_live_channels.md.

Inherit the Shared_operating_contract_for_prompts_056_to_065 and consume the validated outputs from tasks `001-061`.

Mission:
Build the backend registry and publication machinery for browser-facing API contracts:
- `ProjectionQueryContract`
- `MutationCommandContract`
- `LiveUpdateChannelContract`
- `ClientCachePolicy`
- their route-family grouping into registry entries consumable by gateways, shells, validators, and release tooling

This is real backend implementation work. Produce the registry code, schema validation, generation pipeline, and a browsable internal explorer validated with Playwright.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### ProjectionQueryContract`
  - `### MutationCommandContract`
  - `### LiveUpdateChannelContract`
  - `### ClientCachePolicy`
  - the rules that every browser-origin read, mutation, and stream must publish through those contracts
- `platform-frontend-blueprint.md`, especially the rule that browser authority may not be inferred from route code or route-local hooks
- `phase-0-the-foundation-protocol.md`, especially the route-intent, settlement, and runtime publication invariants that those contracts must align with
- outputs from tasks `047`, `050`, `051`, `056`, `058`, and `061`
- `forensic-audit-findings.md`, especially any findings where cache, channel, or route truth could drift from publication law

Parallel-execution note:
This task runs in parallel with other Phase 0 backend tracks. Use published shared package seams only. If a contract type or shared schema is missing, create the smallest bounded stub in the shared contract package and record `PARALLEL_INTERFACE_GAP_*`.

Primary outcome:
A working API-contract registry that later gateways and shells can consume as one authoritative source of query, mutation, live-channel, and cache-policy truth.

Implementation deliverables to create:
1. registry code and persistence for `ProjectionQueryContract`, `MutationCommandContract`, `LiveUpdateChannelContract`, and `ClientCachePolicy`
2. generation or ingestion pipeline that builds registry rows from published route / surface / runtime inputs
3. schema validation for each contract family
4. query APIs or internal service endpoints for registry lookup by audience surface, route family, gateway surface, and contract digest
5. `docs/architecture/65_api_contract_registry_design.md`
6. `docs/architecture/65_contract_registry_generation_and_lookup_rules.md`
7. `docs/architecture/65_contract_registry_explorer.html`
8. `data/analysis/api_contract_registry_manifest.json`
9. `data/analysis/route_family_to_query_mutation_channel_cache_matrix.csv`
10. `packages/api-contracts/schemas/projection-query-contract.schema.json`
11. `packages/api-contracts/schemas/mutation-command-contract.schema.json`
12. `packages/api-contracts/schemas/live-update-channel-contract.schema.json`
13. `packages/api-contracts/schemas/client-cache-policy.schema.json`
14. `tools/analysis/validate_api_contract_registry.(py|ts)`
15. `tests/playwright/api-contract-registry-explorer.spec.(ts|js)`
16. unit tests and integration tests for registry generation, validation, and lookup

At minimum, the registry must support:
- lookup by `audienceSurface`
- lookup by `routeFamilyRef`
- lookup by `gatewaySurfaceRef`
- lookup by `contractDigestRef`
- grouping into manifest-ready route-family sets
- validation against current runtime publication / parity inputs

Execution steps:
1. Implement the contract models and persistence layer for the four contract families.
2. Implement registry generation or ingestion rules so every contract row binds the route family, gateway surface, and digest inputs required by the blueprint.
3. Implement lookup APIs / services for contract retrieval and validation.
4. Implement cross-family validation so that:
   - query contracts reference valid projection families / versions
   - mutation contracts reference valid action scopes, settlement schemas, and recovery dispositions
   - live-channel contracts reference valid readiness and trust requirements
   - client-cache policies align to the published query/channel contract set
5. Implement route-family grouping so the registry can later feed `FrontendContractManifest` generation without duplicating truth.
6. Build the explorer and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write tests covering:
   - missing contract rows causing validation failure
   - route-family lookup returning the expected query / mutation / live-channel / cache bundle
   - invalid digests or mismatched route-family groupings being rejected
   - no browser-facing contract existing outside the registry
   - contract lookup by digest remaining deterministic

Mandatory gap closures you must perform:
- Close the “query, mutation, live-channel, and cache contracts live only in docs or codegen” gap by building a real registry.
- Close the “route families can infer cache or live-channel behavior from route-local hooks” gap by requiring registry publication and lookup.
- Close the “mutation contract can drift from route-intent or settlement law” gap by validating those references explicitly.
- Close the “cache policy is a frontend convenience only” gap by making it a backend-published contract family.
- Close the “manifest generation must scrape unrelated sources” gap by making the registry group contracts into manifest-ready route-family sets.

Industry-best-practice requirements:
- typed contract schemas with validation
- deterministic digest-based lookup
- explicit route-family grouping
- separation of generation and lookup concerns
- no browser authority outside published registry rows
- integration tests for contract completeness and lookup stability

Non-negotiable rules:
- Every browser-origin read, mutation, live channel, and cache policy must exist as a published contract row in the registry.
- The registry is authoritative for backend contract lookup; route code or shell code may not invent undeclared contract semantics.
- Mutation contracts must align with route-intent, settlement, and recovery law.
- Live channels may not imply writability or completeness beyond their published contract.
- Cache policies may preserve continuity but may not preserve writability beyond the active published contract tuple.

Mandatory UI and interaction specification for `65_contract_registry_explorer.html`:
Name the surface mode `Contract_Registry_Explorer`.

Tone:
- minimalist premium internal registry surface
- should feel like a polished source-of-truth atlas, not a generic CRUD table
- crisp, calm, and visibly trustworthy

Branding:
- restrained Vecells wordmark plus an inline SVG `CR` monogram
- include one elegant contract-family constellation and one route-family bundle strip
- include a compact digest inspector and validation rail

Palette:
- canvas `#F7F8FC`
- rail `#EEF2F8`
- panel `#FFFFFF`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- query accent `#0EA5A4`
- mutation accent `#7C3AED`
- channel accent `#0F9D58`
- cache accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for digests, contract IDs, schema refs, and route-family codes

Layout:
- sticky masthead with:
  - total contract count
  - query contract count
  - mutation contract count
  - live-channel count
- left rail (`300px`):
  - audience / route-family / contract-family filters
  - validation-state filters
- center canvas:
  - contract-family constellation
  - route-family bundle strip
  - curated contract cards
- right inspector (`396px`):
  - selected contract details
  - digest and schema refs
  - linked route-family bundle members
- lower region:
  - route-family matrix
  - validation-rule table
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `620px`
- primary controls `44px`
- contract cards `min-height 170px`
- every diagram requires adjacent table/list parity

Motion:
- `120ms` hover/focus
- `180ms` filter / card selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by audience, route family, contract family, and validation state
- selecting a contract card must synchronize constellation, bundle strip, matrix, and inspector
- keyboard navigation across filters, cards, and table rows
- deterministic `data-testid` markers for constellation, bundle strip, inspector, matrix, and defect strip
- validation state must be understandable without color alone

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- contract-family filtering
- audience and route-family filtering
- card selection
- constellation / matrix / inspector synchronization
- keyboard navigation
- responsive layout
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has a real backend API-contract registry for browser-facing queries, mutations, live channels, and cache policy.
- Route-family contract lookup is deterministic and validator-backed.
- Later gateway and shell work can consume one authoritative contract source rather than reconstructing browser behavior from scattered code.
```
