# Prompt 027 For 027_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_request_optional_pds_fhir_sandbox_access_and_feature_flag_plan

Use the shared operating contract first:
[shared_operating_contract_026_to_035.md](/Users/test/Code/V/prompt/shared_operating_contract_026_to_035.md)

```text
Execute roadmap task 027_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_request_optional_pds_fhir_sandbox_access_and_feature_flag_plan.md.

Inherit the Shared_operating_contract_for_prompts_026_to_035 and consume the validated outputs from tasks `001-026`, especially tasks `005`, `006`, `007`, `008`, `021`, `022`, `023`, `024`, `025`, and `026`.

Mission:
Create the PDS FHIR onboarding-and-feature-flag execution pack in a way that respects the blueprint’s identity law:
- local matching and governed identity binding are the default now
- PDS enrichment is valuable but optional
- PDS access must not be assumed, implied, or hard-coded into the baseline MVP
- any PDS use must be feature-flagged, use-case-justified, legally grounded, and bounded by the exact access mode and route family that need it

This task therefore has two equally important jobs:
1. build a rehearsal-grade internal PDS access studio and high-fidelity mock PDS FHIR sandbox now
2. build a gated real digital-onboarding and optional-live-access strategy later

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 3. The canonical request model`
  - `### 10. Identity, consent, security, and policy`
  - `### 12. Practical engineering shape`
- `phase-0-the-foundation-protocol.md`, especially:
  - identity-binding authority rules
  - the law that `Request.patientRef` is derived, not guessed
  - route-intent, grant, and release-fence law
- `phase-2-the-identity-and-echoes.md`, especially:
  - `2C. Patient linkage, demographic confidence, and optional PDS enrichment`
  - the rule that only `IdentityBindingAuthority` may advance durable patient binding
  - the requirement to keep NHS login claims, patient preferences, and PDS / GP demographics separate
  - the guidance that PDS is a separate system with its own legal basis and onboarding
- `phase-7-inside-the-nhs-app.md` only where embedded identity continuity should not silently depend on PDS access
- `platform-runtime-and-release-blueprint.md`, especially:
  - `AdapterContractProfile`
  - `DependencyDegradationProfile`
- `forensic-audit-findings.md`, especially findings that harden:
  - nullable patient binding
  - wrong-patient repair
  - identity hold / repair fences
  - the danger of assuming enrichment equals authority

Current official PDS mechanics you must respect in the `Actual_provider_strategy_later` section:
Use the current official PDS FHIR API and PDS-access guidance as the external-process truth for later execution. At minimum support the current rules around:
- PDS FHIR using digital onboarding
- describing purpose and use case to show legal basis
- access-method choice and secure network connection expectations
- hazard-log template submission
- connecting-systems risk-log submission for each access mode used
- risk-based assurance and evidence testing of mitigations
- access-mode-specific documents and uploaded evidence inside the digital onboarding portal
These mechanics must not override Vecells architecture; they only shape the external onboarding path.

Primary outcome:
A product-grade PDS Access Studio and mock PDS FHIR sandbox that Vecells can use now, plus a later digital-onboarding strategy that keeps PDS optional, bounded, and reversible.

Deliverables to create:
1. `docs/external/27_pds_feature_flag_strategy.md`
2. `docs/external/27_pds_digital_onboarding_field_map.md`
3. `docs/external/27_pds_access_mode_and_route_family_matrix.md`
4. `docs/external/27_pds_hazard_and_risk_log_strategy.md`
5. `docs/external/27_pds_live_gate_and_rollback_plan.md`
6. `services/mock-pds-fhir/README.md`
7. `services/mock-pds-fhir/src/` (implement a high-fidelity local mock PDS FHIR sandbox)
8. `apps/mock-pds-access-studio/README.md`
9. `apps/mock-pds-access-studio/src/`
10. `tests/playwright/mock-pds-access-studio.spec.(ts|js)`
11. `tests/playwright/mock-pds-fhir-sandbox.spec.(ts|js)`
12. `tools/browser-automation/pds-digital-onboarding-dry-run.spec.(ts|js)`
13. `data/analysis/pds_access_mode_matrix.csv`
14. `data/analysis/pds_feature_flag_registry.json`
15. `data/analysis/pds_onboarding_field_map.json`
16. `data/analysis/pds_hazard_risk_artifact_matrix.csv`
17. `tools/analysis/validate_pds_pack.(py|ts)`

Every PDS access row must capture at minimum:
- `pds_use_case_id`
- `route_family_ref`
- `action_scope`
- `access_mode = application_restricted | healthcare_worker | healthcare_worker_with_update | patient_access | other_if_officially_supported`
- `why_pds_is_needed`
- `why_local_matching_alone_is_not_sufficient`
- `legal_basis_summary`
- `feature_flag_name`
- `default_state = off | internal_only | cohort_limited | ready_for_live`
- `identity_binding_impact`
- `wrong_patient_risk_controls[]`
- `required_hazard_log_refs[]`
- `required_risk_log_refs[]`
- `mock_now_support_level`
- `actual_later_gate_refs[]`
- `fallback_if_unavailable`
- `notes`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build two coordinated mock artifacts:
1. `services/mock-pds-fhir` — a high-fidelity local PDS FHIR sandbox
2. `apps/mock-pds-access-studio` — the control tower for access modes, legal-basis reasoning, feature flags, and identity risk

The mock PDS sandbox must be good enough for Vecells to test:
- patient search and read flows at the contract level
- multiple access-mode profiles
- matched, ambiguous, low-confidence, and no-match responses
- stale demographic data and contradictory detail scenarios
- response throttling, degraded availability, and partial-field policies
- nominated pharmacy and registered GP fields where relevant to downstream route semantics
- audit-safe logging with masked identifiers

The access studio must let the team exercise at minimum:
- route-family-to-access-mode mapping
- why a PDS call is optional rather than required
- feature-flag lifecycle and rollout cohorts
- legal-basis narrative capture
- hazard / risk log attachment and freshness
- rollback conditions if PDS data quality, latency, or wrong-patient risk becomes unacceptable
- proof that PDS success does not directly write `Request.patientRef`

The studio must make it impossible to claim:
- that PDS is required for baseline sign-in or request ownership
- that enriched demographics equal durable identity binding
- that NHS login contact claims and PDS demographics are interchangeable
- that a successful trace eliminates wrong-patient repair obligations

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the later live PDS strategy. It must include:
- digital-onboarding field map
- use-case and legal-basis dossier structure
- access-mode selection rules by route family
- hazard-log and risk-log file inventory
- evidence-testing preparation checklist
- feature-flag rollout plan with kill switches and rollback
- browser-automation dry-run harness for onboarding preparation
- live-execution gates that block any real onboarding or credential capture unless:
  - the exact PDS use case is source-traceable and justified
  - legal basis is explicitly documented
  - the relevant risk log and hazard log are current
  - the route family is feature-flagged and default-off
  - a wrong-patient mitigation plan is present
  - named approver and environment target are present
  - `ALLOW_REAL_PROVIDER_MUTATION=true` is set

Execution steps:
1. Start from the identity-law outputs and task `026`’s regulated-onboarding patterns.
2. Build the access-mode matrix and feature-flag model first.
3. Implement the mock PDS sandbox second.
4. Build the mock access studio third and bind it to the sandbox.
5. Use `Playwright_or_other_appropriate_tooling` to drive both during development.
6. Generate the actual-later digital-onboarding dossier from the same machine-readable model.
7. Validate that every PDS-enabled route still preserves local matching, wrong-patient repair, and `IdentityBindingAuthority` ownership.

Mandatory gap closures you must perform:
- Close the “optional PDS enrichment will accidentally become baseline truth” gap by requiring default-off feature flags and explicit route-family binding.
- Close the “use case and legal basis are vague narrative paragraphs” gap by turning them into structured dossier fields.
- Close the “PDS access mode can be selected ad hoc” gap by creating a route-family-by-access-mode matrix.
- Close the “hazard/risk logs are later compliance paperwork” gap by making them first-class readiness artifacts now.
- Close the “successful PDS lookup can directly set durable identity” gap by explicitly routing all durable identity updates back through `IdentityBindingAuthority`.
- Close the “mock PDS service can be a simplistic patient-search demo” gap by requiring ambiguous, stale, contradictory, and degraded responses.

Non-negotiable rules:
- do not enable any PDS-backed route by default in the current baseline
- do not store real NHS numbers or real demographic data in repo fixtures
- do not let PDS results directly mutate durable patient binding or canonical request ownership
- do not let PDS success mask wrong-patient repair or ambiguity
- do not submit real digital-onboarding steps unless the live gate passes
- do not erase the ability to operate safely when PDS is unavailable

Mandatory UI and interaction specification for `apps/mock-pds-access-studio`:
This must feel like a premium identity-risk control surface, not a generic admin form.

Name the visual mode `Identity_Trace_Studio`.

Brand and visual posture:
- include a subtle inline Vecells monogram plus a clear `MOCK_PDS_SANDBOX` ribbon
- do not imitate any real NHS portal closely enough to be confused with the real thing
- tone: precise, clinical-safety aware, minimal, high-trust
- base palette:
  - canvas `#F4F7FB`
  - panel `#FFFFFF`
  - inset `#EEF3F8`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#0F5CC0`
  - secondary accent `#127A6A`
  - identity accent `#6941C6`
  - success accent `#12B76A`
  - caution accent `#B54708`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for access modes, field keys, patient IDs, and hash fragments

Layout:
- top posture banner with flag state, access mode, legal-basis completeness, and wrong-patient risk summary
- left route-family rail
- center access / feature-flag workbench
- right identity-risk inspector with hazard and risk-log drawers
- lower strip with one restrained lineage diagram of `local_match -> optional_pds_enrichment -> identity_binding_review -> durable_binding`

Dimensional constraints:
- page max width `1440px`
- route rail `280px`
- inspector `380px`
- workbench cards `min-height 160px`
- buttons `44px`
- chips `28px`
- sticky header `72px`

Unique page set:
1. `PDS_Flag_Overview`
2. `Access_Mode_Lattice`
3. `Use_Case_and_Legal_Basis`
4. `Risk_Log_and_Hazard_Map`
5. `Rollback_and_Kill_Switches`

Motion:
- `120ms` hover and focus
- `180ms` panel transitions
- `240ms` inspector expansion
- reduced-motion support mandatory

Interaction requirements:
- route-family filters
- explicit blockers and kill-switch chips
- side-by-side mock response preview and identity law notes
- keyboard-first controls
- deterministic `data-testid` markers for route rail, access cards, risk drawers, flag toggles, and lineage diagram

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and testing from the start. At minimum cover:
- route-family switching
- access-mode editing and validation
- feature-flag transitions
- mock PDS search/read flows
- ambiguous and degraded response rendering
- rollback trigger rendering
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- one high-fidelity mock PDS sandbox exists and is demonstrably useful now
- one feature-flagged, route-scoped live-later PDS strategy exists with hard gates
- every PDS-enabled concept remains optional, bounded, and reversible
- the outputs let later onboarding proceed without re-deriving the legal, risk, and route semantics from scratch
```
