# Prompt 025 For 025_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_capture_nhs_login_credentials_and_redirect_uris

Use the shared operating contract first:
[shared_operating_contract_021_to_025.md](/Users/test/Code/V/prompt/shared_operating_contract_021_to_025.md)

```text
Execute roadmap task 025_seq_external_use_Playwright_or_other_appropriate_tooling_browser_automation_to_capture_nhs_login_credentials_and_redirect_uris.md.

Inherit the Shared_operating_contract_for_prompts_021_to_025 and consume the validated outputs from tasks `001-024`.

Mission:
Create the NHS login credentials, redirect-URI, scopes, environment-profile, and client-registration execution pack in two explicit parts:

1. a high-fidelity local mock NHS login service and admin console that Vecells can use now
2. a gated real-provider credential-capture and redirect-registration strategy for later live onboarding

This task is where the mock-first approach becomes truly useful. The mock must not only look good; it must exercise the exact control seams that Vecells later depends on:
- client registration
- environment isolation
- redirect URI governance
- state / nonce / PKCE posture
- scopes and claims
- vector-of-trust requests
- callback error handling
- local session and route-intent binding downstream

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 3. The canonical request model`
  - `### 10. Identity, consent, security, and policy`
  - the rules that NHS login does not itself grant clinical authorization or writable resume
- `phase-0-the-foundation-protocol.md`, especially:
  - `1.4D AuthTransaction`
  - `1.4E AuthScopeBundle`
  - `1.4F PostAuthReturnIntent`
  - `1.4G SessionEstablishmentDecision`
  - `1.4H SessionTerminationSettlement`
  - `1.6 AccessGrant`
  - route-intent, session-epoch, and subject-binding fence law
- `phase-2-the-identity-and-echoes.md`, especially:
  - `2A. Trust contract and capability gates`
  - `2B. NHS login bridge and local session engine`
  - `2D. Signed-in portal uplift and authenticated request ownership`
  - the required tests around sessions, revocation, stale evidence, and forbidden-route access
- `phase-7-inside-the-nhs-app.md`, especially:
  - `7C. NHS App SSO bridge and local session continuity`
  - `7D. Deep links, site links, and return-to-journey continuity`
  - only as future-channel input, not a current-baseline blocker
- `platform-runtime-and-release-blueprint.md`, especially:
  - `GatewayBffSurface`
  - `FrontendContractManifest`
  - `AudienceSurfaceRuntimeBinding`
  - no browser-to-provider direct-call law
- `patient-portal-experience-architecture-blueprint.md`
- `platform-frontend-blueprint.md`
- `accessibility-and-content-system-contract.md`
- `ux-quiet-clarity-redesign.md`
- `task 023` secret ownership model
- `forensic-audit-findings.md`, especially any finding where:
  - auth success is over-interpreted
  - callback races or stale sessions could mis-settle
  - claims, route intent, or session binding drift

Supplemental external-process mechanics you must respect for the `Actual_provider_strategy_later` section:
Use the current official NHS login guidance for live environment and credential mechanics, while keeping Vecells logic governed by the blueprint corpus. At minimum respect the current official guidance named:
- `How do I integrate to the sandpit?`
- `Compare NHS login environments`
- `Test data`
- `Multiple redirect URIs`
- `Technical Conformance`
- `What is NHS login?`
- `Using NHS login to create or retrieve GP credentials`
Do not let those materials override Vecells product logic.

Primary outcome:
A usable mock NHS login identity service + admin console now, and a later live credential / redirect strategy that can safely capture real details when approvals arrive.

Deliverables to create:
1. `docs/external/25_nhs_login_mock_service_spec.md`
2. `docs/external/25_redirect_uri_and_scope_matrix.md`
3. `docs/external/25_credential_capture_and_vault_ingest_runbook.md`
4. `docs/external/25_nhs_login_environment_profile_pack.md`
5. `apps/mock-nhs-login/README.md`
6. `apps/mock-nhs-login/src/` (implement the mock NHS login service and admin console in the chosen stack)
7. `tests/playwright/mock-nhs-login-user-journeys.spec.(ts|js)`
8. `tests/playwright/mock-nhs-login-admin-console.spec.(ts|js)`
9. `tools/browser-automation/nhs-login-credential-intake-dry-run.spec.(ts|js)`
10. `data/analysis/nhs_login_redirect_uri_matrix.csv`
11. `data/analysis/nhs_login_scope_claim_matrix.csv`
12. `data/analysis/nhs_login_mock_client_registry.json`
13. `data/analysis/nhs_login_actual_credential_placeholders.json`
14. `tools/analysis/validate_nhs_login_capture_pack.(py|ts)`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build a local high-fidelity mock NHS login service with two primary surfaces:
- an admin console for client registration, redirect-URI management, scope selection, VoT presets, environment profiles, and test-user management
- a user-facing auth flow surface for sign-in, step-up, consent, error return, and settings-link simulation

The mock service must implement enough behavior to let Vecells genuinely integrate and test:
- authorize
- token
- userinfo
- logout
- client registration
- public-key / JWKS handling
- redirect-URI validation
- `state` and `nonce` round-tripping
- PKCE validation
- consent screen and `error=access_denied` / consent-not-given return
- test-user accounts and environment-specific fixtures
- low / medium / high verification-like presets
- session expiry and re-auth
- callback races and stale code rejection
- wrong redirect URI rejection
- environment separation for `local_mock`, `sandpit_like`, and `integration_like`

The mock must also support:
- redirect URI limit handling and state-based fan-out strategy
- scope / claims matrix management
- optional `gp_integration_credentials` simulation only behind an explicit IM1-enabled flag
- seeded test-data export packs
- settings-link simulation and return behavior
- event logging and audit entries with no raw PHI leakage in general logs

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the later live credential / redirect strategy for when NHS login onboarding progresses. This must include:
- environment-specific client registration checklist
- redirect-URI matrix per environment and route family
- public-key submission pack
- scope and claims request pack
- vector-of-trust matrix by route family
- vault-ingest procedure for received credentials
- placeholder registry for client IDs, secrets, and test data packs until real values arrive
- live-execution gates for any browser automation that could touch real portals or capture real credentials
- a dry-run automation harness that exercises the intake flow without persisting real secrets

Execution steps:
1. Start from the identity-law outputs and task `024`’s application-dossier model.
2. Define the redirect URI inventory from Vecells route families, not from ad hoc page lists.
3. Build the mock admin console first.
4. Build the mock user-facing auth flow second, using the admin console’s client registry.
5. Drive both with `Playwright_or_other_appropriate_tooling` during development.
6. Generate the later live credential / redirect intake pack from the same data model.
7. Validate that the redirect matrix, scope matrix, and environment profiles remain consistent with the blueprint’s route and session law.

Mandatory gap closures you must perform:
- Close the “credential capture depends on real onboarding so nothing useful can be built now” gap by creating the mock service now.
- Close the “redirect URIs are just config strings” gap by tying them to route families, return intents, and environment posture.
- Close the “client registration can drift away from session and capability law” gap.
- Close the “multiple environments and multiple redirect URIs are operational details rather than audited contract data” gap.
- Close the “GP credentials scope could be misused as a one-off linkage retrieval path” gap by enforcing the blueprint and official guidance boundary.
- Close the “browser automation for real credential intake could accidentally leak secrets” gap with strict dry-run / gated-live design.
- Close the “mock auth service might be visually generic or technically shallow” gap with explicit product-grade UI and protocol fidelity.

Non-negotiable rules:
- do not use the mock service to imply real NHS approval or real identity proof
- do not let auth success imply writable route access without the blueprint’s local session and capability pipeline
- do not expose real secrets in repo files, traces, screenshots, or logs
- do not allow more redirect URIs than the approved strategy can govern; model overflow handling explicitly
- do not enable any simulated `gp_integration_credentials` flow unless the IM1-enabled flag and route policy allow it
- do not let the mock flatten consent-denied, stale-session, and callback-race failures into one generic error page

Mandatory UI and interaction specification for `apps/mock-nhs-login`:
This must be a premium mock identity service, not a generic login page.

Name the visual mode `Bluewoven_Identity_Simulator`.

Brand and visual posture:
- include a subtle custom inline SVG mark, clearly labeled `MOCK_NHS_LOGIN`
- never imitate the real NHS login brand so closely that it could be confused as official
- tone: premium, calm, trustworthy, minimal, technically exact
- base palette:
  - canvas `#F4F7FB`
  - panel `#FFFFFF`
  - inset `#EFF3F8`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#0B57D0`
  - secondary accent `#335CFF`
  - consent accent `#6E59D9`
  - success accent `#0F9D58`
  - warning accent `#C98900`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for client IDs, codes, scopes, redirect URIs, hashes, and environment labels

You must design at least these unique pages:
1. `Admin_Client_Registry`
   - route family mapping
   - redirect URI cards
   - scope chips
   - VoT presets
   - environment toggles
   - test user table
2. `User_Sign_In`
   - email / password stage
   - passcode or step-up stage
   - subtle journey indicator
3. `Consent_and_Share`
   - requested claims
   - allow / deny
   - explicit mock disclaimer
4. `Return_or_Error`
   - success return details
   - consent denied
   - stale or invalid callback
   - expired session / re-auth required
5. `Settings_Link_Simulator`
   - account-setting deep link simulation
   - safe return behavior

Layout constraints:
- page max width `1440px`
- auth forms centered with `440px` max width
- admin console uses:
  - left nav `280px`
  - center registry canvas
  - right inspector `360px`
- cards min radius `16px`
- primary inputs `44px`
- chips `28px`
- code / URI blocks use mono and subtle inset panels
- include one thin route-map diagram showing authorize -> token -> userinfo -> local session handoff, with table parity

Motion:
- `120ms` hover/focus
- `180ms` page-to-page transitions
- `240ms` drawer / inspector transitions
- reduced-motion support mandatory

Interaction requirements:
- deterministic `data-testid` markers for:
  - client cards
  - redirect URI entries
  - scope chips
  - environment switcher
  - consent buttons
  - auth return states
- no generic “something went wrong” copy
- all failures must render precise and calm explanations
- keyboard-first navigation
- accessible error summary and inline validation
- visible reduced-motion handling

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and testing from the start. At minimum cover:
- client registration and edit flows
- redirect-URI validation
- state / nonce / PKCE correctness
- authorize -> token -> userinfo happy path
- consent denied path
- expired auth code / stale session / reused code
- environment switching
- test-user login
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks

Definition of done:
- Vecells has a high-fidelity local NHS login simulator and admin console it can integrate with immediately.
- Vecells has a governed redirect URI, scopes, and environment-profile pack for later real onboarding.
- Real credential capture remains safely gated and secrets-safe.
```