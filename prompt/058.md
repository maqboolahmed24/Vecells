# Prompt 058 For 058_seq_phase0_define_verification_ladder_and_environment_ring_policy

Use the shared operating contract first:
[shared_operating_contract_056_to_065.md](/Users/test/Code/V/prompt/shared_operating_contract_056_to_065.md)

```text
Execute roadmap task 058_seq_phase0_define_verification_ladder_and_environment_ring_policy.md.

Inherit the Shared_operating_contract_for_prompts_056_to_065 and consume the validated outputs from tasks `001-057`.

Mission:
Define the Phase 0 verification ladder, ring-promotion law, and candidate-bound environment policy for Vecells so that compile, simulation, runtime publication, preprod rehearsal, canary, widening, pause, rollback, and recovery proof all consume the same exact release tuple.

This task must stop release proof from fragmenting into separate local checks for route contracts, frontend manifests, cache policy, settlement schemas, continuity evidence, or recovery evidence.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `## Environment ring and promotion contract`
  - `EnvironmentBaselineFingerprint`
  - `PromotionIntentEnvelope`
  - `WaveEligibilitySnapshot`
  - `WaveControlFence`
  - `VerificationScenario`
  - `ReleaseContractVerificationMatrix`
  - `ContinuityContractCoverageRecord`
  - `Gate 1` through `Gate 5`
  - `OperationalReadinessSnapshot`
  - `SyntheticRecoveryCoverageRecord`
- `phase-0-the-foundation-protocol.md`, especially:
  - the delivery skeleton and release-control requirements in `0A`
  - the invariants that require publication parity, exact route/mutation contracts, and recovery tuple binding
  - the recovery and resilience contracts in the `1.40*` family
- `phase-cards.md`, especially the requirement that one exact `VerificationScenario` plus one exact `ReleaseContractVerificationMatrix` govern release proof and the requirement for preview environments, simulator-backed flows, preprod rehearsal, canary, and rollback harness
- `platform-admin-and-config-blueprint.md`, especially config compilation and simulation governance
- outputs from tasks `015`, `020`, `046-057`
- `forensic-audit-findings.md`, especially the findings that hardened continuity controls, design publication, runtime publication, and recovery proof as promotion-critical rather than optional

Primary outcome:
One machine-readable verification and promotion pack proving:
- what each ring means
- what evidence is required to move between rings
- how one release candidate freezes all cross-layer contracts into one matrix
- how waves are guarded, halted, resumed, or rolled back
- how simulator-backed flows, synthetic recovery coverage, and operational readiness all remain bound to the same candidate tuple

Deliverables to create:
1. `docs/architecture/58_verification_ladder_and_environment_ring_policy.md`
2. `docs/architecture/58_release_contract_verification_matrix_strategy.md`
3. `docs/architecture/58_promotion_intent_and_wave_fence_policy.md`
4. `docs/architecture/58_verification_cockpit.html`
5. `data/analysis/environment_ring_policy.json`
6. `data/analysis/verification_scenarios.json`
7. `data/analysis/release_contract_verification_matrix.json`
8. `data/analysis/ring_gate_matrix.csv`
9. `data/analysis/promotion_intent_schema.json`
10. `data/analysis/synthetic_recovery_coverage_matrix.csv`
11. `packages/api-contracts/schemas/release-contract-verification-matrix.schema.json`
12. `tools/analysis/validate_verification_ladder.(py|ts)`
13. `tests/playwright/verification-cockpit.spec.(ts|js)`

Each ring policy row must capture at minimum:
- `ringCode = local | ci-preview | integration | preprod | production`
- `purpose`
- `entryPrerequisites[]`
- `requiredGateRefs[]`
- `requiredSimulatorCoverageRefs[]`
- `requiredContinuityCoverageRefs[]`
- `requiredRecoveryCoverageRefs[]`
- `requiredOperationalEvidenceRefs[]`
- `promotionBlockers[]`
- `rollbackMode`
- `notes`

Each `VerificationScenario` row must capture at minimum:
- `verificationScenarioId`
- `releaseRef`
- `candidateBundleHash`
- `compiledPolicyBundleRef`
- `configCompilationRecordRef`
- `configSimulationEnvelopeRef`
- `standardsDependencyWatchlistRef`
- `releaseContractVerificationMatrixRef`
- `releaseContractMatrixHash`
- `releaseApprovalFreezeRef`
- `runtimeTopologyManifestRef`
- `runtimePublicationBundleRef`
- `releasePublicationParityRef`
- `designContractBundleRefs[]`
- `requiredContinuityControlRefs[]`
- `requiredSyntheticRecoveryCoverageRefs[]`
- `watchTupleTemplateRef`
- `scenarioState`

Execution steps:
1. Start from tasks `046-052` plus task `057` and gather the cross-layer contract families that must be frozen together:
   - runtime topology
   - route contracts
   - frontend manifests
   - projection query / mutation / live-channel contracts
   - client cache policy
   - settlement schemas and transition envelopes
   - recovery dispositions
   - design-contract bundles and lint verdicts
   - continuity evidence contracts
   - simulator-backed integration coverage
2. Publish the exact ring policy for `local`, `ci-preview`, `integration`, `preprod`, and `production`. Do not invent extra rings unless the repo already froze them earlier.
3. Publish the `VerificationScenario` template and make it the single authority for all gates.
4. Publish the `ReleaseContractVerificationMatrix` template and bind every route, manifest, query, mutation, cache, settlement, recovery, and continuity artifact into the same matrix.
5. Publish the gate rules for:
   - Gate 1: contract and component
   - Gate 2: integration and end-to-end
   - Gate 3: performance and security
   - Gate 4: resilience and recovery
   - Gate 5: live wave proof
6. Publish `PromotionIntentEnvelope`, `EnvironmentBaselineFingerprint`, `WaveEligibilitySnapshot`, and `WaveControlFence` rules so ring progression, canary, pause, rollback, and resume are explicit.
7. Publish how simulator-backed flows and `SyntheticRecoveryCoverageRecord` evidence participate at integration, preprod, and production-wave levels.
8. Build the cockpit and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
9. Validate that:
   - a release cannot move rings without one aligned baseline fingerprint and one live promotion intent
   - a candidate cannot pass if the verification matrix is incomplete or tuple-mismatched
   - continuity-sensitive workflows cannot pass on generic simulation alone
   - synthetic recovery coverage remains bound to the same verification scenario and watch tuple as live-wave proof
   - drift in standards watchlist, config simulation, release freeze, parity, or runtime publication forces restart or halt rather than silent continuation

Mandatory gap closures you must perform:
- Close the “rings are assumed equivalent” gap by publishing `EnvironmentBaselineFingerprint` as mandatory evidence.
- Close the “release proof is assembled from local subsets” gap by making `ReleaseContractVerificationMatrix` the single cross-layer tuple.
- Close the “wave pause / resume / rollback is operational folklore” gap by publishing `WaveControlFence` and `PromotionIntentEnvelope` law.
- Close the “continuity evidence and synthetic recovery are afterthoughts” gap by making them gate-critical artifacts.
- Close the “preprod or canary may proceed on stale rehearsal or stale standards watch” gap by tying those artifacts to the same verification scenario.
- Close the “simulator-backed flows are separate from real release proof” gap by making them required ring evidence in Phase 0.

Industry-best-practice requirements:
- immutable release candidates and promotion intents
- strict environment drift detection
- exact gate evidence with restart-on-drift semantics
- candidate-bound recovery and continuity coverage
- machine-readable wave control and rollback posture
- no rebuild-from-source between rings

Non-negotiable rules:
- Every ring advancement must be backed by one live `PromotionIntentEnvelope` and one aligned `EnvironmentBaselineFingerprint`.
- Every release must bind all gates to one `VerificationScenario` and one exact `ReleaseContractVerificationMatrix`.
- Production waves may widen, pause, roll back, or resume only through the declared watch tuple and wave-control fence.
- Continuity-sensitive workflows and recovery-sensitive workflows must carry exact coverage records; generic smoke or uptime probes are insufficient.
- If any matrix member, watchlist hash, config simulation tuple, freeze tuple, parity tuple, or recovery tuple drifts, promotion must halt or restart; no silent carry-forward is allowed.

Mandatory UI and interaction specification for `58_verification_cockpit.html`:
Name the surface mode `Verification_Ladder_Cockpit`.

Tone:
- minimalist premium release-governance surface
- should feel like a precise launch console, not a CI log viewer
- calm, formal, and obviously consequence-aware

Branding:
- restrained Vecells wordmark plus an inline SVG `VL` monogram
- include one elegant ring-progression diagram and one gate-ladder strip
- include one compact watch-fence panel showing canary / widen / halt / rollback posture

Palette:
- canvas `#F6F8FB`
- rail `#EEF2F7`
- panel `#FFFFFF`
- inset `#F3F5FA`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- gate accent `#0EA5A4`
- verification accent `#7C3AED`
- live-wave accent `#0F9D58`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for hashes, gate IDs, ring codes, and tuple refs

Layout:
- sticky masthead with:
  - active verification-scenario count
  - ring count
  - gate pass/block counts
  - synthetic recovery coverage count
- left rail (`300px`):
  - ring filters
  - gate filters
  - scenario list
- center canvas:
  - ring-progression diagram
  - gate-ladder strip
  - curated scenario cards
- right inspector (`396px`):
  - selected verification scenario
  - selected matrix members
  - wave fence and rollback posture
- lower region:
  - ring gate matrix
  - continuity and recovery coverage table
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `620px`
- primary controls `44px`
- scenario cards `min-height 160px`
- every diagram requires adjacent table/list parity

Motion:
- `120ms` hover/focus
- `180ms` filter / card selection
- `220ms` inspector transitions
- reduced-motion support mandatory

Interaction requirements:
- filter by ring, gate, scenario state, and drift state
- select a scenario and synchronize diagrams, matrices, and inspector
- keyboard navigation across filters, cards, and matrix rows
- deterministic `data-testid` markers for ring diagram, gate strip, inspector, coverage table, and defect strip
- wave-fence and rollback state must be understandable without color alone

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- ring filtering
- gate filtering
- scenario selection
- diagram / matrix / inspector synchronization
- keyboard navigation
- responsive layout
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has one exact verification ladder and ring-promotion policy bound to the same cross-layer candidate tuple.
- Simulator-backed flows, continuity coverage, and synthetic recovery coverage are now promotion-critical evidence rather than optional extras.
- Later release and recovery tasks can operate from one machine-readable gate model.
```
