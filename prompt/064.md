# Prompt 064 For 064_par_phase0_track_backend_build_fhir_representation_set_and_mapping_compiler

Use the shared operating contract first:
[shared_operating_contract_056_to_065.md](/Users/test/Code/V/prompt/shared_operating_contract_056_to_065.md)

```text
Execute roadmap task 064_par_phase0_track_backend_build_fhir_representation_set_and_mapping_compiler.md.

Inherit the Shared_operating_contract_for_prompts_056_to_065 and consume the validated outputs from tasks `001-061`.

Mission:
Build the explicit FHIR mapping compiler and persistence layer for `FhirRepresentationContract`, `FhirRepresentationSet`, `FhirResourceRecord`, and `FhirExchangeBundle`.

This is real backend implementation work. Produce code, schemas, mapping compiler infrastructure, deterministic resource generation, and replay tests. Do not let FHIR resources quietly become the internal domain model.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - the canonical definitions for `FhirRepresentationContract`, `FhirRepresentationSet`, `FhirResourceRecord`, and `FhirExchangeBundle`
  - the statements that Vecells is domain-first and FHIR is representation / interchange / callback-correlation, not hidden lifecycle ownership
  - the mapping notes around lines `410-413`
  - the replay and adapter-consumption expectations around lines `440-441`
- `platform-runtime-and-release-blueprint.md`, especially the rule that adapters may consume only published `FhirRepresentationContract` rows and accepted `FhirExchangeBundle` bundle types
- outputs from tasks `006`, `007`, `041-045`, `049`, `057`, and `061`
- `forensic-audit-findings.md` where resource-level truth or hidden schema drift were corrected

Parallel-execution note:
Do not depend on sibling tasks `062` or `063` having full implementations yet. Consume only published aggregate IDs / schema seams and create bounded shared-package stubs if necessary.

Primary outcome:
A deterministic mapping compiler and persistence layer that can materialize FHIR representations from domain aggregates without letting FHIR own canonical business truth.

Implementation deliverables to create:
1. code for `FhirRepresentationContract`, `FhirRepresentationSet`, `FhirResourceRecord`, and `FhirExchangeBundle`
2. a mapping compiler that reads published representation contracts and materializes deterministic representation sets
3. schema and migration artifacts for FHIR representation persistence
4. bundle-generation logic for supported bundle types
5. contract validation logic for profile URLs, identifier policy, and allowed resource types
6. `docs/architecture/64_fhir_mapping_compiler_design.md`
7. `docs/architecture/64_fhir_representation_set_rules.md`
8. `data/analysis/fhir_representation_contract_manifest.json`
9. `data/analysis/fhir_resource_type_matrix.csv`
10. unit tests, contract tests, replay tests, and integration tests for deterministic rematerialization and identifier stability
11. validator or smoke scripts proving FHIR output is contract-driven rather than ad hoc

At minimum, implement support for the canonical resource families named in Phase 0:
- `Task`
- `ServiceRequest`
- `DocumentReference`
- `Communication`
- `Consent`
- `AuditEvent`
- `Provenance`
- `Bundle`

Execution steps:
1. Implement the contract models and persistence layer first.
2. Implement a mapping compiler that takes:
   - one published `FhirRepresentationContract`
   - one governing aggregate type / ref / version
   - optional evidence snapshot or settlement refs as required by the contract
   and emits one deterministic `FhirRepresentationSet` plus `FhirResourceRecord` rows.
3. Implement identifier and profile handling so logical IDs, identifier sets, profile canonical URLs, and bundle membership remain deterministic and replay-safe.
4. Implement bundle generation for the allowed exchange types named by the contract set.
5. Implement supersession and invalidation behavior for representation sets when aggregate versions advance.
6. Write tests covering:
   - the same aggregate version rematerializes the same representation set
   - identifiers do not silently fork across replay
   - disallowed resource types or bundle types fail validation
   - adapters can consume only published contract rows and bundle types
   - FHIR output cannot become the hidden owner of request lifecycle or closure truth
7. Ensure provenance and audit joins are present where the contract requires them.

Mandatory gap closures you must perform:
- Close the “FHIR resources are treated like the internal model” gap by keeping domain aggregates authoritative and FHIR explicitly derived.
- Close the “resource shapes are improvised in application code” gap by building a contract-driven compiler.
- Close the “bundle membership and identifiers can drift on replay” gap by enforcing deterministic rematerialization.
- Close the “adapters can read raw aggregate tables” gap by requiring them to consume published FHIR exchange bundles or resource records only where the contract allows.
- Close the “snapshot and artifact mapping can rewrite history” gap by using supersession / invalidation rather than in-place mutation.

Industry-best-practice requirements:
- contract-driven mapping compiler
- deterministic identifier generation and replay
- schema validation for profiles and bundle types
- separation of domain state from representation state
- provenance-aware resource materialization
- integration-safe persistence and bundle caching

Non-negotiable rules:
- Domain aggregates remain authoritative; FHIR is representation or interchange only.
- All FHIR materialization flows must pass through published `FhirRepresentationContract` rows.
- A `FhirRepresentationSet` is the atomic mapping unit; shells and adapters may not reconstruct business truth from a partial subset of resources.
- The same aggregate version must rematerialize the same identifiers and bundle membership.
- No application code or adapter may improvise resource shapes outside the published contracts.

Definition of done:
- Vecells has a real FHIR representation compiler and persistence model.
- FHIR materialization is deterministic, replay-safe, and explicitly contract-driven.
- Later adapter and integration work can consume published FHIR output without corrupting domain ownership.
```
