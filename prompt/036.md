# Prompt 036 For 036_seq_external_identify_gp_system_provider_sandbox_paths_and_booking_capability_evidence

Use the shared operating contract first:
[shared_operating_contract_036_to_045.md](/Users/test/Code/V/prompt/shared_operating_contract_036_to_045.md)

```text
Execute roadmap task 036_seq_external_identify_gp_system_provider_sandbox_paths_and_booking_capability_evidence.md.

Inherit the Shared_operating_contract_for_prompts_036_to_045 and consume the validated outputs from tasks `001-035`.

Mission:
Create the authoritative GP principal-system pathway pack for Vecells in two explicit parts:

1. a high-fidelity local provider-path twin and capability-evidence rehearsal pack that Vecells can use now
2. a gated live-provider route strategy for later real access once MVP, sponsor, assurance, and provider onboarding prerequisites actually exist

This task must turn the blueprint’s booking and access abstractions into one concrete provider-path matrix without pretending that current-stage Vecells already has real supplier access.

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 1. Product definition`
  - `### 6. Booking and access continuity`
  - `### 12. Practical engineering shape`
  - the rules that booking stays behind one published `BookingProviderAdapterBinding`, supplier-specific behavior remains behind capability matrices, and patient actions stay subordinate to reservation truth, confirmation gates, and recovery
- `phase-0-the-foundation-protocol.md`, especially:
  - `0A. Delivery skeleton and repository architecture`
  - `BoundedContextDescriptor`
  - `ContextBoundaryContract`
  - `1.10 ReservationAuthorityRecord`
  - `1.10A ExternalConfirmationGate`
  - `1.10B ProviderCapabilitySnapshot`
  - `1.10C ReservationCompensationRecord`
  - the rule that no adapter or provider integration may directly own canonical request closure or milestone truth
- `phase-4-the-booking-engine.md`, especially:
  - `4A. Booking contract, case model, and state machine`
  - `4B. Provider capability matrix and adapter seam`
  - `4C. Slot search, normalisation, and availability snapshots`
  - `4D. Slot scoring, offer orchestration, and selection experience`
  - `4E. Commit path, revalidation, booking record, and compensation`
  - `4F. Appointment management: cancel, reschedule, reminders, and detail updates`
  - `4G. Smart Waitlist and local auto-fill`
  - `4H. Staff booking handoff panel, assisted booking, and exception queue`
- `phase-5-the-network-horizon.md`, especially where network coordination consumes local booking capability and no-slot fallbacks
- `platform-runtime-and-release-blueprint.md`, especially `AdapterContractProfile`, `DependencyDegradationProfile`, `AudienceSurfaceRuntimeBinding`, `RuntimeTopologyManifest`, and environment-ring rules
- `forensic-audit-findings.md`, especially findings `27-34`, `45`, `72-76`, and any finding that prohibits optimistic booked truth, generic reconciliation wording, or provider logic writing canonical request state directly
- outputs from tasks `021-035`, especially:
  - integration priority model
  - provider scorecards
  - sandbox-account strategy
  - IM1 pairing preparation pack
  - NHS App and MESH strategy where they intersect future patient or notification continuity

Supplemental external-process mechanics you must respect for the `Actual_provider_strategy_later` section:
Use the current official materials named at minimum:
- `IM1 Pairing integration`
- `GP Connect`
- `Booking and Referral Standard`
Use them only for current provider-path mechanics, route availability, and onboarding framing. They must not override Vecells booking truth, confirmation ambiguity, or adapter-law semantics.

Primary outcome:
A provider-path matrix that tells Vecells exactly which GP-system route is baseline-now, which routes are watch-only, what evidence is required before claiming support for a capability, and how the simulator path mirrors the future live path.

Deliverables to create:
1. `docs/external/36_gp_system_pathways_mock_strategy.md`
2. `docs/external/36_gp_system_pathways_actual_strategy.md`
3. `docs/external/36_booking_capability_evidence_dossier.md`
4. `docs/external/36_gp_provider_gap_and_watch_register.md`
5. `docs/external/36_gp_provider_pathfinder.html`
6. `data/analysis/gp_principal_system_path_matrix.csv`
7. `data/analysis/gp_booking_capability_evidence.json`
8. `data/analysis/gp_provider_decision_register.json`
9. `data/analysis/gp_provider_sandbox_gap_register.csv`
10. `tools/analysis/validate_gp_provider_strategy.(py|ts)`
11. `tests/playwright/gp-provider-pathfinder.spec.(ts|js)`
12. `tools/browser-automation/gp-provider-path-discovery-dry-run.spec.(ts|js)`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build a local high-fidelity GP provider-path twin that rehearses how Vecells will reason about principal GP-system routes before real supplier access exists.

The mock must represent at minimum these provider-path categories:
- `im1_pairing_optum_emisweb`
- `im1_pairing_tpp_systmone`
- `gp_connect_appointment_management_watch_only`
- `bars_watch_only`
- `local_adapter_simulator_required`
- `manual_practice_handoff_only`

For each path, the mock pack must classify these capability dimensions separately:
- patient booking search
- staff-assisted search
- slot freshness proof
- hold / soft-hold support
- authoritative commit proof
- cancellation support
- reschedule support
- reminder / detail update support
- waitlist continuation compatibility
- practice-visibility evidence
- ambiguity mode
- degraded fallback mode
- exact proof object required before the UI may reassure the patient

The mock must make it impossible to collapse these into a single generic “booking supported” boolean.

You must create one local capability-evidence dossier format that proves or disproves each claim. The dossier must require:
- source references
- actor mode (`patient_self_service | staff_assisted | hub_assisted | manual_only`)
- environment class
- evidence freshness
- proof object type
- ambiguity / caveat text
- linked degraded default
- linked simulator or manual fallback

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the real-provider route strategy for later use. It must include:
- current route classification for IM1, GP Connect Appointment Management, and watchlist alternatives
- provider family mapping to principal systems and actor modes
- current evidence requirements before Vecells may claim a booking capability in product or onboarding materials
- future live discovery / onboarding dry-run harness using `Playwright_or_other_appropriate_tooling`
- a live-execution gate that refuses any real-provider mutation or onboarding progression unless:
  - a credible booking MVP exists
  - provider-selection scorecards are approved
  - direct-care / commissioning / sponsor posture is explicit
  - current architecture and data-flow diagrams exist
  - named approver and target environment are present
  - live mutation flag is enabled
  - no unresolved blocker exists in the watch / gap register

Execution steps:
1. Start from tasks `021-035`.
2. Extract all booking capability claims from the blueprint corpus and normalize them into an evidence-backed capability vocabulary.
3. Build the mock provider-path twin first.
4. Separate baseline-now provider paths from watch-only or future routes.
5. Create one evidence-dossier schema that later live-provider work must also use.
6. Build the HTML pathfinder and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Create the dry-run browser-automation harness for later provider-path discovery, but gate all real mutation.
8. Validate that every booking capability claim in the mock or actual strategy maps back to a canonical blueprint requirement.

Mandatory gap closures you must perform:
- Close the “booking route” gap between the blueprint’s canonical adapter seam and the real NHS provider landscape.
- Close the “booking supported” oversimplification gap by decomposing support into search, hold, commit, manage, reminder, visibility, and ambiguity-proof dimensions.
- Close the “watch routes look baseline-ready” gap by clearly marking paused, future, or non-baseline routes.
- Close the “provider differences leak into core logic” gap by forcing every provider-path fact behind capability evidence rather than business-logic branching.
- Close the “mock path cannot prove live truth semantics” gap by making confirmation ambiguity, degraded fallback, and authoritative proof first-class in the mock twin.
- Close the “staff-assisted vs patient self-service” gap by making actor mode explicit in every capability row.

Non-negotiable rules:
- Do not claim that Vecells can safely reassure a patient of a booked appointment without the exact authoritative proof object required by the blueprint.
- Do not treat a supplier-reported acceptance as equivalent to confirmed booking truth when the blueprint requires an `ExternalConfirmationGate` or equivalent ambiguity posture.
- Do not present GP Connect Appointment Management watch-only routes as current baseline implementation routes if official process posture says otherwise.
- Do not let the pathfinder UI imply equal maturity for baseline, watch, and blocked routes.
- Do not store real supplier credentials, practice identifiers, or contract documents in repo fixtures.

Mandatory UI and interaction specification for `36_gp_provider_pathfinder.html`:
This is a premium internal architecture-and-onboarding surface. Name the visual mode `Principal_System_Pathfinder`.

Brand and posture:
- use a restrained Vecells wordmark plus a compact `GP_PATHS` monogram in inline SVG
- tone: clinical-operations grade, sober, technical, evidence-first, not sales-like
- base palette:
  - canvas `#F6F8FB`
  - rail `#EEF2F7`
  - panel `#FFFFFF`
  - inset `#F2F5FA`
  - text strong `#0F172A`
  - text default `#1E293B`
  - text muted `#667085`
  - border subtle `#E2E8F0`
  - border default `#CBD5E1`
  - primary accent `#3559E6`
  - evidence accent `#0EA5A4`
  - watch accent `#D97706`
  - blocked accent `#C24141`
  - future accent `#7C3AED`
- typography:
  - system sans stack only
  - mono only for provider codes, environment names, capability keys, and evidence hashes

Layout:
- sticky top masthead with:
  - baseline route count
  - watch-route count
  - unresolved gap count
  - last evidence refresh time
- left provider rail (`300px`): provider-path cards with maturity badges
- center canvas:
  - capability-evidence matrix
  - proof ladder cards
  - one restrained line diagram showing `Vecells -> AdapterBinding -> ProviderPath -> ProofObject`
- right inspector (`360px`): selected path details, assumptions, risks, official-process notes
- lower section:
  - table parity for the matrix
  - gap register strip

Dimensional constraints:
- max width `1440px`
- row height `52px` in the capability matrix
- provider cards `min-height 120px`
- primary controls `44px`
- sticky header height `72px`

Motion:
- `120ms` hover/focus
- `180ms` path selection transition
- `240ms` inspector drawer transition
- reduced-motion support mandatory

Interaction requirements:
- filter by actor mode, maturity, and proof class
- exact blocker chips with terse but specific copy
- sortable evidence-freshness column
- keyboard-first navigation
- deterministic `data-testid` markers for provider rail, matrix, proof ladder, inspector, and gap strip

Unique design notes:
- use one minimal comparison diagram and one compact capability radar only if table parity is adjacent
- avoid generic KPI tiles or crowded admin-dashboard styling
- every maturity badge must be visually distinct but low-chroma and professional

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- provider filtering
- path selection
- matrix sorting
- inspector rendering
- blocker and watch badges
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has one credible GP-system provider-path matrix that is aligned to the blueprint and usable now.
- Baseline, watch-only, blocked, and simulator-required routes are clearly separated.
- Every capability claim is evidence-backed and tied to a proof object.
- Mock and actual-later strategies stay aligned but safely separated.
```
