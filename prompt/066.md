# Prompt 066 For 066_par_phase0_track_backend_implement_submission_promotion_record_and_exactly_once_envelope_to_request_mapping

Use the shared operating contract first:
[shared_operating_contract_066_to_075.md](/Users/test/Code/V/prompt/shared_operating_contract_066_to_075.md)

```text
Execute roadmap task 066_par_phase0_track_backend_implement_submission_promotion_record_and_exactly_once_envelope_to_request_mapping.md.

Inherit the Shared_operating_contract_for_prompts_066_to_075 and consume the validated outputs from tasks `001-065`.

Mission:
Implement the authoritative backend promotion boundary from `SubmissionEnvelope` to `Request` through one immutable `SubmissionPromotionRecord`, with exact-once envelope-to-request mapping under replay, tab race, auth-return race, support-assisted continuation, and delayed client retries.

This is real backend implementation work. Produce code, migrations, repositories, transactional promotion logic, validators, and tests. Do not leave promotion uniqueness or same-lineage replay as route-controller convention.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.1 SubmissionEnvelope`
  - `#### 1.1A SubmissionPromotionRecord`
  - `#### 1.1B RequestLineage`
  - `### 4. Canonical ingest, pre-submission capture, promotion, and episode formation`
  - `#### 4.1 Command ingest and envelope creation`
- `phase-1-the-red-flag-gate.md`, especially the sections stating that governed submit promotion must emit one immutable `SubmissionPromotionRecord`, that exactly-once promotion must survive duplicate submit / tab race / auth-return race, and that draft resume grants plus draft leases must be superseded in the same transaction as promotion
- `vecells-complete-end-to-end-flow.md`, especially `I_ENVELOPE`, `I_SUBMIT`, and `I_NORM`
- `forensic-audit-findings.md`, especially Findings `01`, `02`, `03`, `04`, `07`, and any finding that reinforces durable submit barriers, immutable evidence snapshot, or grant-scoped continuation
- outputs from tasks `001-007`, `019`, `041-045`, `055-056`, `061-065`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Do not depend on unpublished sibling internals beyond the shared seams from tasks `062-065`. If you need a shared promotion or receipt-contract stub that is absent, create the smallest bounded stub and record `PARALLEL_INTERFACE_GAP_*`.

Mock_now_execution:
Build a high-fidelity promotion simulator and replay harness now. It must simulate:
- double submit from the same browser tab
- cross-tab submit races against the same `SubmissionEnvelope`
- auth-return replay after a promotion already completed
- secure-link or support-assisted resume hitting an already-promoted lineage
- delayed network retry that replays the same submit after the first response was lost
The simulator must emit the same transaction boundaries, replay outcomes, and supersession side effects that production will require later.

Actual_production_strategy_later:
When live NHS login, embedded return, telephony continuation, and support-console resume are fully wired, they must all call the same promotion service and consume the same `SubmissionPromotionRecord`. Do not create provider-specific promotion paths. Live integrations may change ingress evidence, authentication proofs, or channel profile, but they may not change the promotion object model, receipt consistency semantics, or exact-once guarantees.

Primary outcome:
A working promotion subsystem that proves one governed request creation happened for one envelope lineage, and that every legitimate replay returns the same authoritative promotion result instead of minting a second `Request`.

Implementation deliverables to create:
1. domain and persistence code for `SubmissionPromotionRecord` in the correct foundation package(s)
2. compare-and-set / transactional promotion service that maps one envelope lineage to exactly one request lineage and exactly one request
3. unique-constraint and migration artifacts that enforce envelope-lineage uniqueness and replay-safe request mapping
4. repository interfaces and storage adapters for promotion lookup by envelope, source lineage, request lineage, receipt consistency key, and status consistency key
5. domain-event / canonical-event entries for promotion started, promotion committed, promotion replay-returned, and promotion superseded-grants-applied
6. grant- and draft-lease supersession hook seams that close draft mutability in the same authoritative transaction
7. `docs/architecture/66_submission_promotion_boundary_design.md`
8. `docs/architecture/66_exactly_once_promotion_algorithm.md`
9. `docs/architecture/66_promotion_mapping_atlas.html`
10. `data/analysis/submission_promotion_record_manifest.json`
11. `data/analysis/envelope_to_request_mapping_matrix.csv`
12. `data/analysis/promotion_replay_casebook.json`
13. `tools/analysis/validate_submission_promotion_mapping.(py|ts)`
14. unit tests, repository tests, transactional race tests, and integration tests
15. `tests/playwright/submission-promotion-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- create exactly one immutable `SubmissionPromotionRecord` for a promotable envelope lineage
- bind that promotion record to the exact `SubmissionEnvelope`, `RequestLineage`, `Request`, frozen `EvidenceSnapshot`, frozen normalized submission, draft version, intake bundle, and continuity keys that crossed the boundary
- return the prior promotion result for exact or semantic submit replay rather than creating another request
- supersede active draft resume grants and active draft leases in the same transaction as promotion
- route same-lineage recovery and later resume flows to the authoritative request shell rather than reopening a mutable draft lane
- preserve `Request.patientRef` nullability and identity derivation; promotion must not smuggle in patient identity from session state alone

Execution steps:
1. Implement the `SubmissionPromotionRecord` model exactly from the canonical field contract, including `receiptConsistencyKey`, `statusConsistencyKey`, `patientJourneyLineageRef`, and the superseded grant / lease refs.
2. Implement one compare-and-set promotion transaction over the envelope lineage. At minimum the transaction must:
   - verify the envelope is in a promotable state under current invariant checks
   - freeze the exact `EvidenceSnapshot` and normalized submission refs crossing the boundary
   - create or resolve the canonical `RequestLineage`
   - create the `Request` only if no prior promotion exists for that lineage
   - persist the immutable `SubmissionPromotionRecord`
   - supersede draft grants and draft leases
   - mark the envelope as `promoted` with the exact promoted request ref
3. Implement deterministic replay resolution so that exact replays, late retries, and auth-return resubmissions locate and return the prior promotion record and prior request.
4. Implement request-shell handoff metadata so receipts, status, auth return, embedded resume, secure-link recovery, and support-assisted resume all resolve from the immutable promotion record rather than client cache or mutable draft projections.
5. Implement validators that fail when:
   - more than one promotion record points at the same envelope lineage
   - an envelope is marked `promoted` without a persisted immutable promotion record
   - a promotion record references mutable snapshot or draft state
   - draft resume grants or draft leases remain live after promotion
6. Build the atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write race tests for duplicate submit, cross-tab submit, auth-return replay, and delayed response loss.

Mandatory gap closures you must perform:
- Close the “submitted barrier is implied but not durably modeled” gap by making promotion an explicit immutable record and transaction boundary.
- Close the “one envelope lineage can mint multiple requests under retries” gap with compare-and-set uniqueness and replay return paths.
- Close the “receipt and status continuity can be guessed from mutable client state” gap by persisting the canonical continuity keys in the promotion record.
- Close the “promotion can leave draft mutation surfaces live” gap by superseding all relevant draft grants and leases transactionally.
- Close the “promotion can infer identity from auth success” gap by keeping patient identity derived from `IdentityBinding`, not from session callback success.
- Close the “already-promoted lineage can reopen as a second draft lane” gap by redirecting resume to authoritative request truth or bounded recovery.

Industry-best-practice requirements:
- exact-once behavior enforced by storage and code, not UI timing
- immutable promotion records and append-only audit joins
- explicit transaction boundaries and compare-and-set semantics
- replay-safe lookup indices by lineage and continuity keys
- deterministic race fixtures and concurrent integration tests
- no hidden controller-side fallbacks that bypass the promotion service

Non-negotiable rules:
- `SubmissionPromotionRecord` is the only durable bridge from envelope to request.
- Exactly one request may exist for one promoted envelope lineage.
- Replay returns the existing authoritative result; it never mints a second request.
- Promotion closes draft mutability and hands same-lineage continuity to the authoritative request shell.
- `Request` creation and promotion are authoritative only after the immutable promotion record exists.

Mandatory UI and interaction specification for `66_promotion_mapping_atlas.html`:
Name the surface mode `Promotion_Mapping_Atlas`.

Tone:
- premium internal lineage instrument
- crisp, quiet, and unmistakably authoritative
- should feel like a flight recorder for the submit boundary, not a CRUD admin page

Branding:
- restrained Vecells wordmark plus inline SVG `PM` monogram
- one envelope-to-request bridge diagram
- one continuity-key ribbon
- one replay-case strip showing exact replay vs duplicate-risk vs promoted truth

Palette:
- canvas `#F6F8FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F3F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- promotion accent `#3559E6`
- envelope accent `#0EA5A4`
- replay accent `#7C3AED`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- monospaced digest styling for keys, lineage refs, request refs, and hashes

Layout:
- sticky masthead with promotion totals, replay returns, and blocked anomalies
- left rail (`312px`) with lineage filters, state filters, and replay-case filters
- central canvas with:
  - envelope-to-request bridge diagram
  - promotion record cards
  - continuity-key ribbon
- right inspector (`400px`) with selected promotion record details, superseded grants, and request refs
- lower matrix region with one envelope/request mapping table and one replay-case table

Dimensional constraints:
- max width `1520px`
- masthead `72px`
- bridge diagram min height `260px`
- card min height `176px`
- controls `44px`

Motion:
- `120ms` hover/focus
- `180ms` card/filter transitions
- `220ms` inspector reveal
- full reduced-motion support mandatory

Interaction requirements:
- filter by envelope state, replay class, and channel profile
- selecting a promotion card must synchronize bridge diagram, continuity ribbon, inspector, and mapping table
- keyboard navigation across cards, filters, and table rows
- deterministic `data-testid` markers for bridge diagram, continuity ribbon, inspector, mapping table, and replay-case table
- every diagram must have adjacent textual parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- lineage and replay filtering
- promotion card selection and synchronized inspector updates
- bridge diagram/table parity
- keyboard navigation
- reduced motion
- responsive layout
- accessibility smoke checks

Definition of done:
- The repository has real exact-once promotion code, persistence, and validators.
- One envelope lineage maps to one canonical request lineage and one request.
- Replay, resume, and auth-return flows reuse the existing promotion truth instead of creating forks.
- Later intake, identity, portal, and downstream workflow tasks can rely on this boundary as authoritative.
```
