# Prompt 067 For 067_par_phase0_track_backend_implement_idempotency_record_and_replay_collision_review_services

Use the shared operating contract first:
[shared_operating_contract_066_to_075.md](/Users/test/Code/V/prompt/shared_operating_contract_066_to_075.md)

```text
Execute roadmap task 067_par_phase0_track_backend_implement_idempotency_record_and_replay_collision_review_services.md.

Inherit the Shared_operating_contract_for_prompts_066_to_075 and consume the validated outputs from tasks `001-066`.

Mission:
Implement the canonical replay and collision-control layer centered on `IdempotencyRecord` and `ReplayCollisionReview`, together with the backend services and calculators that classify commands as `exact_replay`, `semantic_replay`, `collision_review`, or `distinct`.

This is real backend implementation work. Produce the hashing/canonicalization utilities, storage model, compare-and-set service, collision review opening logic, callback/checkpoint seams, validators, and tests. Do not defer replay recognition to load balancer retries, frontend debouncing, or provider-local conventions.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - the object contract for `IdempotencyRecord` in the canonical object inventory
  - `### 4. Canonical ingest, pre-submission capture, promotion, and episode formation`
  - `#### 4.1 Command ingest and envelope creation`
  - `#### 1.23A ReplayCollisionReview`
  - `#### 1.23B AdapterDispatchAttempt`
  - `#### 1.23C AdapterReceiptCheckpoint`
  - the adapter replay law in the canonical external dispatch section
- `forensic-audit-findings.md`, especially Findings `04`, `05`, `18`, `35`, and any correction where replays, duplicate submits, or callback reuse required stronger controls
- `phase-4-the-booking-engine.md`, `phase-5-the-network-horizon.md`, and `phase-6-the-pharmacy-loop.md` where external attempts, duplicate callbacks, and disputed receipts must flow through the same replay authority
- outputs from tasks `001-007`, `041-048`, `056-057`, `061-066`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Do not assume later reservation, booking, or dispatch services already exist. Publish bounded shared interfaces if needed and record `PARALLEL_INTERFACE_GAP_*`.

Mock_now_execution:
Build a replay harness that simulates:
- repeated browser taps with identical raw payloads
- semantically identical payloads with different transport framing or field ordering
- reused source command IDs with changed semantic meaning
- duplicate callbacks and out-of-order provider receipts
- delayed duplicate jobs from the outbox
The harness must prove that exact and semantic replay return the same authoritative settlement while divergent reuse opens `ReplayCollisionReview` and blocks automatic mutation.

Actual_production_strategy_later:
When live providers, live webhooks, or production queues arrive, they must plug into the same canonical replay service and the same `AdapterReceiptCheckpoint` semantics. Provider-specific correlation IDs, webhook signatures, or transport identifiers may enrich the replay basis, but they may not replace the canonical `IdempotencyRecord` and collision-review chain.

Primary outcome:
A working replay-control subsystem that classifies commands deterministically, prevents duplicate side effects, and routes semantically divergent identifier reuse into governed collision review.

Implementation deliverables to create:
1. domain and persistence code for `IdempotencyRecord`, `ReplayCollisionReview`, and the bounded callback/checkpoint support objects required to make replay safe
2. canonicalization utilities for `h_raw`, `h_sem`, `replayKey`, and `scopeFingerprint`
3. compare-and-set idempotency resolution service for inbound commands
4. replay-safe callback / receipt checkpoint service for inbound acknowledgements and confirmations
5. validators that enforce monotone replay classification and exactly one accepted action chain per effect scope
6. `docs/architecture/67_idempotency_and_collision_review_design.md`
7. `docs/architecture/67_replay_classification_algorithm.md`
8. `docs/architecture/67_replay_collision_studio.html`
9. `data/analysis/idempotency_record_manifest.json`
10. `data/analysis/replay_classification_matrix.csv`
11. `data/analysis/replay_collision_casebook.json`
12. `tools/analysis/validate_replay_classification.(py|ts)`
13. unit tests, property tests, callback-ordering tests, and integration tests
14. `tests/playwright/replay-collision-studio.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- persist `IdempotencyRecord` with source identifiers, raw and semantic hashes, replay key, scope fingerprint, expected effect set, and decision class
- classify inbound commands as exactly one of `exact_replay`, `semantic_replay`, `collision_review`, or `distinct`
- return the prior authoritative settlement for `exact_replay` and `semantic_replay`
- open `ReplayCollisionReview` and quarantine automatic mutation for divergent identifier reuse or callback scope drift
- ensure callback or webhook dedupe advances only the existing authoritative settlement chain and never creates a second effect
- publish replay decisions that later domains can reuse without reclassifying the same command independently

Execution steps:
1. Implement the canonicalization and hashing layer exactly as the blueprint requires. Preserve actor-, scope-, lineage-, and intent-bearing content in `h_sem`; strip transport-only noise only.
2. Implement compare-and-set resolution on `(actionScope, governingLineageRef, replayKey, scopeFingerprint)` with deterministic decision output.
3. Implement replay classification rules for exact, semantic, collision, and distinct cases.
4. Implement the return path for `exact_replay` and `semantic_replay` so the current authoritative settlement chain is returned without creating a second mutation path.
5. Implement collision-review opening so any divergent identifier reuse freezes a bounded evidence bundle, points to the existing accepted action and settlement, and blocks automatic mutation.
6. Implement callback / receipt checkpoint handling that dedupes inbound acknowledgements or confirmations against the canonical effect scope and ordering rules.
7. Implement validators that fail when:
   - identical replay input resolves to different decision classes on re-run
   - a collision review exists but automatic mutation still proceeded
   - multiple accepted action chains exist for the same effect scope
   - callback dedupe created a second side effect or second settlement chain
8. Build the studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.

Mandatory gap closures you must perform:
- Close the “frontend debouncing is treated as idempotency” gap by making replay resolution a durable backend authority.
- Close the “transport-only differences can accidentally create duplicate effects” gap by classifying semantic replay separately from exact replay.
- Close the “reused identifiers with changed semantics are silently accepted” gap by opening explicit `ReplayCollisionReview` and quarantining automatic processing.
- Close the “callback dedupe is provider-local and not canonical” gap by routing inbound acknowledgements through canonical receipt checkpoints.
- Close the “duplicate review and idempotency are conflated” gap by keeping `IdempotencyRecord` and `DuplicateCluster` as separate authorities.

Industry-best-practice requirements:
- deterministic hashing and canonicalization
- compare-and-set uniqueness at the storage layer
- append-only collision review records
- replay-safe callback handling with ordering policy
- property-based tests for hash-stability and classification determinism
- exact-once effect ownership enforced per effect scope

Non-negotiable rules:
- `IdempotencyRecord` owns replay recognition.
- `ReplayCollisionReview` is the only legal place to hold semantically divergent reuse of identifiers or effect scopes.
- Exact or semantic replay returns the prior authoritative settlement chain.
- Callback dedupe may advance the same chain; it may not create a second side effect or second business result.
- Duplicate clustering is not a substitute for idempotency.

Mandatory UI and interaction specification for `67_replay_collision_studio.html`:
Name the surface mode `Replay_Collision_Studio`.

Tone:
- premium forensic control panel
- should feel analytical, calm, and precise
- no noisy developer-console aesthetics

Branding:
- restrained Vecells wordmark plus inline SVG `RC` monogram
- one raw-vs-semantic comparison pane
- one replay-decision timeline rail
- one collision-case spotlight card

Palette:
- canvas `#F7F8FC`
- panel `#FFFFFF`
- rail `#EEF2F8`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- exact replay accent `#0EA5A4`
- semantic replay accent `#3559E6`
- collision accent `#7C3AED`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for hashes, replay keys, correlation IDs, and scope fingerprints

Layout:
- masthead with counts by replay decision class
- left rail (`300px`) for action-scope, decision-class, and effect-scope filters
- center canvas with:
  - raw vs semantic diff pane
  - replay decision timeline
  - collision-case cards
- right inspector (`408px`) for selected idempotency record, prior settlement, and collision links
- lower area for callback checkpoint table and validator results

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- diff pane min height `260px`
- card min height `168px`
- controls `44px`

Motion:
- `120ms` hover/focus
- `180ms` filter and tab changes
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by action scope, decision class, and effect scope
- selecting a record must synchronize diff pane, timeline, inspector, and callback table
- keyboard navigation across filters, records, and timeline steps
- deterministic `data-testid` markers for diff pane, timeline, inspector, checkpoint table, and validator rail
- color is not the only indicator of replay class

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- decision-class filtering
- record selection and synchronization
- diff pane / table parity
- keyboard navigation
- reduced motion
- responsive layout
- accessibility smoke checks

Definition of done:
- The repository has durable replay-recognition and collision-review services.
- Exact and semantic replay reuse the existing authoritative settlement chain.
- Divergent identifier reuse opens governed collision review and blocks automatic mutation.
- Later booking, hub, pharmacy, notifications, and callback integrations can rely on one canonical replay authority.
```
