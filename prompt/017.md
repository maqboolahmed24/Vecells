# Prompt 017 For 017_seq_define_programme_milestones_parallel_tracks_and_merge_gates

Use the shared operating contract first:
[shared_operating_contract_016_to_020.md](/Users/test/Code/V/prompt/shared_operating_contract_016_to_020.md)

```text
Execute roadmap task 017_seq_define_programme_milestones_parallel_tracks_and_merge_gates.md.

Inherit the Shared_operating_contract_for_prompts_016_to_020 and consume the validated outputs from tasks 001-016.

Mission:
Turn the Vecells roadmap into an executable programme graph with:
- explicit milestones
- explicit strict-sequence dependencies
- explicit parallel-eligible tracks
- explicit merge gates
- explicit critical path
- explicit long-lead dependency milestones
- explicit evidence obligations for advancing from one block to the next

The goal is not to rewrite the roadmap. The goal is to convert the existing roadmap serialization in `prompt/checklist.md` plus the canonical blueprint corpus into an execution model that autonomous agents and human reviewers can follow without ambiguity.

Algorithm grounding you must honor:
- `prompt/AGENT.md`, especially the sequential and parallel claim protocol
- `prompt/checklist.md`, as the only live synchronized task board and the only task-order source inside the repo
- `phase-cards.md`, especially:
  - `## Programme Baseline Update (NHS App Deferred)`
  - `## Cross-Phase Conformance Scorecard`
  - `## Programme Summary-Layer Alignment`
  - `## Card 1: Phase 0 - The Foundation Protocol`
  - every later card and any explicit phase-level completion statements
- `phase-0-the-foundation-protocol.md`, especially:
  - the statement that Phase 0 runs as seven internal sub-phases with hard gates
  - `## 0A. Delivery skeleton and repository architecture`
  - `## 0B. Canonical domain kernel and state machine`
  - the route / settlement / runtime / release / design-contract substrate
  - the Phase 0 tests and exit states
  - `## 0G. Observability, security plumbing, and operational controls`
- `platform-runtime-and-release-blueprint.md`, especially:
  - the verification ladder contract
  - `ReleaseWatchTuple`
  - `WaveObservationPolicy`
  - `ReleaseRecoveryDisposition`
  - operational readiness and resilience rules
- `platform-admin-and-config-blueprint.md`, especially:
  - config compilation pipeline
  - change-control rules
  - production promotion gate
  - dependency and standards hygiene
- `phase-9-the-assurance-ledger.md`, especially:
  - `CrossPhaseConformanceScorecard`
  - `PhaseConformanceRow`
  - BAU and final exit-gate expectations
- `forensic-audit-findings.md`, especially:
  - 91, 95, 104-120 where the audit proves that continuity, trust, publication, and resilience controls must be milestone / gate-visible rather than phase-local footnotes

Primary outcome:
One machine-readable and human-readable programme execution model that can answer:
- which work is strictly sequential
- which work can run in parallel
- what blocks each merge gate
- which external approvals and assurance workstreams sit on the critical path
- how Phase 0 should be decomposed internally
- how deferred Phase 7 is represented without corrupting the current baseline
- which proof and evidence artifacts are required before claiming a milestone or phase is complete

Deliverables to create:
1. `docs/programme/17_programme_milestones_and_gate_model.md`
2. `docs/programme/17_parallel_track_plan.md`
3. `docs/programme/17_merge_gate_policy.md`
4. `docs/programme/17_phase0_subphase_execution_plan.md`
5. `docs/programme/17_critical_path_and_long_lead_plan.md`
6. `docs/programme/17_programme_decision_log.md`
7. `docs/programme/17_programme_control_tower.html`
8. `docs/programme/17_programme_dependency_graph.mmd`
9. `docs/programme/17_programme_timeline.mmd`
10. `data/analysis/programme_milestones.json`
11. `data/analysis/task_to_milestone_map.csv`
12. `data/analysis/milestone_dependency_edges.csv`
13. `data/analysis/parallel_track_matrix.csv`
14. `data/analysis/merge_gate_matrix.csv`
15. `data/analysis/critical_path_summary.json`
16. `tools/analysis/build_programme_control_tower.(py|ts)`
17. `tools/analysis/validate_programme_milestones.(py|ts)`

Each milestone row must capture at minimum:
- `milestone_id`
- `milestone_title`
- `milestone_class = planning | dependency_readiness | foundation | phase_delivery | cross_phase_control | release_gate | assurance_gate | deferred_channel`
- `baseline_scope = current | deferred | optional`
- `source_task_refs[]`
- `source_phase_refs[]`
- `entry_condition_refs[]`
- `exit_condition_refs[]`
- `required_artifact_refs[]`
- `required_requirement_ids[]`
- `required_risk_ids[]`
- `required_dependency_refs[]`
- `allowed_parallel_track_refs[]`
- `merge_gate_ref`
- `gate_owner_role`
- `conformance_row_refs[]`
- `critical_path_state = on_path | near_path | off_path`
- `status_model = not_started | enabled | blocked | merge_ready | complete`
- `notes`

Each merge-gate row must capture at minimum:
- `merge_gate_id`
- `gate_title`
- `gate_type = seq_release | par_block_merge | external_readiness | phase_entry | phase_exit | programme_conformance`
- `incoming_task_refs[]`
- `required_completed_task_refs[]`
- `required_artifact_refs[]`
- `required_dependency_refs[]`
- `required_risk_posture_refs[]`
- `required_conformance_refs[]`
- `blocking_rule_refs[]`
- `failure_mode_if_skipped`
- `notes`

Execution steps:
1. Parse `prompt/checklist.md` and classify every task by:
   - sequential (`seq_`)
   - parallel (`par_`)
   - merge / gate task
   - external dependency task
   - phase-local delivery task
   - cross-phase control task
2. Preserve the canonical order from the checklist. Do not reorder, renumber, or collapse tasks. Your job is to derive milestone groupings and dependency edges around the existing ordering.
3. Build the milestone model over the entire roadmap, not just tasks 016-020. At minimum represent:
   - planning and discovery block (001-020)
   - external readiness block (021-040)
   - Phase 0 foundation definition block (041-061)
   - active parallel foundation tracks and their merge (062-138)
   - later phase blocks and their merge gates
   - programme finalization and release waves
4. Use the AGENT protocol to derive legal parallel windows. Make it impossible for a later tool or agent to misread eligibility.
5. Create a dedicated Phase 0 sub-phase plan. The corpus says Phase 0 has seven internal sub-phases with hard gates. You must make those sub-phases explicit, even if the source headings are unevenly distributed across the document.
6. Build merge gates that are evidence-driven rather than title-driven. A gate is not satisfied because adjacent tasks are complete; it is satisfied only when the bound artifacts and proofs exist.
7. Explicitly represent long-lead tracks that must run in parallel with engineering:
   - DCB0129 safety case and hazard log
   - DSPT readiness
   - IM1 prerequisites and SCAL
   - NHS login onboarding
   - external provider sandbox readiness where the roadmap schedules it
8. Explicitly mark Phase 7 as `deferred_channel` in the current baseline. It remains inventoried and planned, but it must not become a hidden blocker for current baseline completion.
9. Build a critical-path analysis that considers:
   - strict sequential roadmap blocks
   - long-lead external approvals
   - required assurance and privacy workstreams
   - prerequisite architecture freeze / traceability / risk readiness
   - phase-merge gates
   - final programme conformance and release gates
10. Build a merge-gate policy that states what evidence is needed to move from:
   - planning -> external readiness
   - external readiness -> phase0 definition
   - phase0 definition -> open parallel foundation tracks
   - each later parallel block -> its merge gate
   - final programme readiness -> go-live waves
11. Add validator logic that fails when:
   - a parallel block violates the AGENT protocol
   - a merge gate has no required artifacts
   - a milestone in the current baseline depends on a deferred-only Phase 7 artifact without an explicit waiver or simulator path
   - a phase is marked complete while its conformance row would necessarily be `stale | blocked`
   - a critical-path dependency is missing from the machine-readable graph

Mandatory gap closures you must perform:
- Close the “checklist order exists but milestone structure does not” gap.
- Close the “parallel work is implied but not machine-readable” gap.
- Close the “Phase 0 internal sub-phases are mentioned but not operationalized” gap.
- Close the “merge gates are named by task titles rather than evidence tuples” gap.
- Close the “external approvals and assurance workstreams sit outside the programme graph” gap.
- Close the “Phase 7 deferred channel can still look like a current-baseline blocker” gap.
- Close the “cross-phase controls, continuity proof, watch tuples, and resilience obligations are not milestone-visible” gap.
- Close the “programme planning can say complete while conformance proof is stale” gap required by `CrossPhaseConformanceScorecard`.

Non-negotiable programme rules you must encode:
- no later `seq_` task becomes eligible while an earlier `seq_` task is incomplete
- no later `seq_` gate becomes eligible before the active contiguous `par_` block is complete
- no phase summary may be considered complete while its conformance row is stale or blocked
- no merge gate may pass while its required dependency, risk, or evidence posture remains blocked
- no long-lead external dependency may be hidden inside “implementation” if it can stall later gates
- no milestone may be marked current-baseline complete using deferred Phase 7 work as proxy evidence
- no final programme milestone may bypass runtime publication, verification, governance proof, ops proof, and BAU readiness

Mandatory UI and interaction specification for `17_programme_control_tower.html`:
The control tower should feel like a premium programme operations surface, not a Gantt screenshot.

Visual direction:
- name the visual mode `Programme_Control_Tower`
- tone: calm, executive, systems-level, high trust
- base palette:
  - canvas `#F5F7FA`
  - shell `#FFFFFF`
  - inset `#EEF2F6`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#475467`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - milestone accent `#335CFF`
  - parallel accent `#0F8B8D`
  - gate accent `#6E59D9`
  - caution `#C98900`
  - critical `#C24141`
  - complete `#0F9D58`
- typography: same system-sans stack as the shared contract
- layout:
  - top programme summary band with baseline/deferred counts and current critical-path digest
  - left filter rail for phase, milestone class, path state, and gate status
  - center timeline / dependency canvas
  - lower split table with milestone rows and gate rows
  - right inspector for selected milestone or gate
- dimensional constraints:
  - timeline canvas min height `520px`
  - dependency graph canvas min height `480px`
  - milestone cards min width `280px`
  - gate chips `28px` height
- motion:
  - `120ms` hover/focus
  - `180ms` filter transitions
  - `240ms` inspector transitions
  - reduced motion support mandatory
- diagrams/charts:
  - include one timeline view and one dependency-graph view
  - every visual view must have adjacent table/list parity
- branding:
  - include a small inline-SVG Vecells monogram / wordmark in the header
- DOM markers:
  - `data-testid="programme-summary-band"`
  - `data-testid="milestone-filter-rail"`
  - `data-testid="timeline-canvas"`
  - `data-testid="dependency-graph-canvas"`
  - `data-testid="milestone-table"`
  - `data-testid="merge-gate-table"`
  - `data-testid="programme-inspector"`
  - stable `data-milestone-id`, `data-gate-id`, `data-critical-path`, and `data-baseline-scope` markers

Playwright_or_other_appropriate_tooling requirements:
Use `Playwright_or_other_appropriate_tooling` as a first-class development and testing driver for the control tower. Minimum required coverage:
- offline load
- filter interaction across phase / milestone class / baseline scope
- keyboard navigation across rail, graph, tables, and inspector
- responsive behavior at `768px`, `1024px`, and `1440px`
- reduced-motion mode
- stable DOM markers for milestones, gates, path states, and baseline-scope badges
- diagram/table parity checks for timeline and dependency graph
- accessibility smoke checks
- no hidden CDN or remote asset dependency

Definition of done:
- The roadmap is represented as one executable milestone and dependency graph.
- Parallel-eligible work and strict sequence are explicit.
- Merge gates are evidence-driven and machine-readable.
- The current baseline and deferred channel scope are clearly separated.
- Later agents can determine exactly what is eligible, blocked, merge-ready, or critical-path without reinterpreting the checklist or phase cards from scratch.
```
