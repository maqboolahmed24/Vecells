# Prompt 074 For 074_par_phase0_track_backend_implement_capacity_reservation_and_external_confirmation_gate_models

Use the shared operating contract first:
[shared_operating_contract_066_to_075.md](/Users/test/Code/V/prompt/shared_operating_contract_066_to_075.md)

```text
Execute roadmap task 074_par_phase0_track_backend_implement_capacity_reservation_and_external_confirmation_gate_models.md.

Inherit the Shared_operating_contract_for_prompts_066_to_075 and consume the validated outputs from tasks `001-073`.

Mission:
Implement the authoritative reservation-truth and weak-confirmation substrate required by booking, hub, and pharmacy flows:
- `CapacityReservation`
- `ReservationTruthProjection`
- `ExternalConfirmationGate`
plus the bounded truth calculators, versioning, and validators needed so later user-facing surfaces cannot overstate exclusivity or final confirmation.

This task is model-plus-core-logic implementation, not the full reservation authority or confirmation coordinator from later tasks. However, the models must already encode the truth rules that later services will be forced to respect.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.14 CapacityReservation`
  - `#### 1.14A ReservationTruthProjection`
  - `#### 1.15 ExternalConfirmationGate`
- `phase-4-the-booking-engine.md`, especially the sections on offer selection, revalidation, truthful reservation cues, commit ambiguity, and pending confirmation
- `phase-5-the-network-horizon.md`, especially the hub commit and alternative-offer confirmation rules
- `phase-6-the-pharmacy-loop.md`, especially dispatch proof and confirmation-gate rules for weak or manual transport
- `forensic-audit-findings.md`, especially Findings `72`, `73`, and `74`, plus the booking/pharmacy corrections that ambiguous external truth must remain explicit and closure-blocking
- outputs from tasks `001-007`, `041-045`, `056-057`, `061`, `067`, `071-073`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Publish minimal reservation / confirmation interfaces for sibling tracks if necessary, but do not wait for the later reservation authority service.

Mock_now_execution:
Build a high-fidelity supplier / confirmation simulator now. It must cover:
- soft selection with no exclusive hold
- real exclusive hold with real hold expiry
- truthful nonexclusive availability
- commit that returns immediate authoritative confirmation
- commit that remains pending external confirmation
- contradictory or competing confirmation evidence
- weak/manual confirmation that requires at least two independent source families
The simulator must feed the same reservation and gate models production will use later.

Actual_production_strategy_later:
When live booking suppliers, hub systems, or pharmacy transport channels arrive, they must publish reservation and confirmation evidence into the same canonical models. Provider-specific booking semantics may alter adapter contracts and evidence atoms, but they may not change the meaning of `soft_selected`, `held`, `pending_confirmation`, `confirmed`, `disputed`, or the gate thresholds and hard-match requirements.

Primary outcome:
The repository gains real reservation-truth and external-confirmation models that later patient and staff flows can use to speak honestly about availability, holds, pending confirmation, and final reassurance.

Implementation deliverables to create:
1. domain and persistence code for `CapacityReservation`, `ReservationTruthProjection`, and `ExternalConfirmationGate`
2. bounded truth calculators for reservation projection and confirmation-confidence updates
3. validators enforcing hold-truth rules, countdown legality, hard-match requirements, and weak/manual corroboration rules
4. `docs/architecture/74_capacity_reservation_and_confirmation_gate_design.md`
5. `docs/architecture/74_reservation_truth_and_confirmation_rules.md`
6. `docs/architecture/74_reservation_confirmation_truth_lab.html`
7. `data/analysis/capacity_reservation_manifest.json`
8. `data/analysis/reservation_truth_matrix.csv`
9. `data/analysis/external_confirmation_gate_casebook.json`
10. `tools/analysis/validate_reservation_confirmation_models.(py|ts)`
11. unit tests, simulation tests, repository tests, and integration tests
12. `tests/playwright/reservation-confirmation-truth-lab.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- persist `CapacityReservation` with explicit state, commit mode, reservation version, truth-basis hash, and active fencing token
- project user-visible reservation truth through `ReservationTruthProjection`, not route-local copy or client timers
- compute and persist `ExternalConfirmationGate.posteriorLogOdds`, `confirmationConfidence`, `competingGateMargin`, and state under the canonical Bayesian rule
- require required hard matches plus threshold tests before `confirmed`
- make `soft_selected`, `truthful_nonexclusive`, `pending_confirmation`, `confirmed`, `released`, `expired`, and `disputed` distinct and durable
- expose countdown only when a real exclusive hold exists with real hold expiry

Execution steps:
1. Implement `CapacityReservation` exactly from the canonical fields, including commit mode, state, reservation version, and terminal reason handling.
2. Implement `ReservationTruthProjection` and the rules that determine display exclusivity, countdown legality, and truth-state degradation.
3. Implement `ExternalConfirmationGate` and the canonical Bayesian update calculation for confirmation confidence and competing-gate margin.
4. Implement validators that fail when:
   - `soft_selected` is rendered or treated as exclusivity
   - countdown is shown without a real hold expiry from an actual held reservation
   - pending or weak confirmation is rendered as final booked or referred truth
   - weak/manual paths confirm without independent corroboration from at least two source families
   - contradictory evidence does not move the gate to `disputed`
5. Implement simulator fixtures for strong, moderate, weak, and manual confirmation paths, plus contradictory or competing evidence.
6. Build the lab and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.

Mandatory gap closures you must perform:
- Close the “slot selection or offer selection implies exclusivity” gap by modeling `soft_selected` as non-exclusive focus posture only.
- Close the “client timer or offer expiry can drive hold countdown” gap by requiring real held reservations and real hold expiry.
- Close the “pending external truth can be collapsed into final success” gap by modeling `pending_confirmation` and gate states explicitly.
- Close the “weak/manual transport can close the loop without independent corroboration” gap by enforcing the gate’s hard-match and multi-source rules.
- Close the “ambiguous confirmation can disappear into generic reconciliation text” gap by keeping `disputed` and confirmation gate refs durable and visible.

Industry-best-practice requirements:
- immutable evidence-driven gate evolution and monotone reservation versioning
- deterministic calculators and threshold configs
- truthful user-facing projection separate from raw reservation state
- simulation fixtures for happy, pending, contradictory, and expired paths
- no local countdown or exclusivity invention outside `ReservationTruthProjection`

Non-negotiable rules:
- `CapacityReservation` holds canonical reservation state.
- `ReservationTruthProjection` is the sole user-visible authority for reservation language and countdowns.
- `ExternalConfirmationGate` is mandatory for weak, async, or manual confirmation paths.
- Pending, disputed, released, and expired states may not be flattened into success.
- Final patient reassurance or closure cannot rely on weak/manual external truth without a satisfied gate.

Mandatory UI and interaction specification for `74_reservation_confirmation_truth_lab.html`:
Name the surface mode `Reservation_Confirmation_Truth_Lab`.

Tone:
- premium internal truth-in-advertising lab
- composed, trustworthy, and intentionally restrained
- should feel like a polished truth board, not a flashy booking dashboard

Branding:
- restrained Vecells wordmark plus inline SVG `RC` monogram
- one reservation-state rail
- one truth card strip
- one confirmation-confidence panel

Palette:
- canvas `#F8F9FC`
- panel `#FFFFFF`
- rail `#EEF2F8`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- reservation accent `#3559E6`
- truth accent `#0EA5A4`
- confirmation accent `#7C3AED`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for reservation versions, truth hashes, and gate IDs

Layout:
- masthead with held, pending-confirmation, confirmed, disputed, and expired counts
- left rail (`304px`) for state, commit-mode, and assurance-level filters
- center canvas with state rail, truth cards, and confidence panel
- right inspector (`408px`) for selected reservation / truth / gate details
- lower region with evidence table and validator table

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- state rail min height `240px`
- cards min height `170px`
- controls `44px`

Motion:
- `120ms` hover/focus
- `180ms` filter and selection changes
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by reservation state, commit mode, and assurance level
- selecting a truth card must synchronize state rail, confidence panel, inspector, and evidence table
- keyboard navigation across filters, cards, and tables
- deterministic `data-testid` markers for state rail, truth cards, confidence panel, inspector, and evidence table
- every chart/diagram needs adjacent text parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- state and assurance filtering
- selection synchronization
- chart / table parity
- keyboard navigation
- reduced motion
- responsive layout
- accessibility smoke checks

Definition of done:
- The repository has real reservation and confirmation-gate models with validators.
- Later booking, hub, and pharmacy flows can speak honestly about selection, holds, pending confirmation, and final truth.
- Final reassurance or closure cannot outrun authoritative reservation and confirmation state.
```
