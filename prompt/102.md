# Prompt 102 For 102_par_phase0_track_runtime_implement_non_production_canary_and_rollback_harness

Use the shared operating contract first:
[shared_operating_contract_096_to_105.md](/Users/test/Code/V/prompt/shared_operating_contract_096_to_105.md)

```text
Execute roadmap task 102_par_phase0_track_runtime_implement_non_production_canary_and_rollback_harness.md.

Inherit the Shared_operating_contract_for_prompts_096_to_105 and consume the validated outputs from tasks `001-101`.

Mission:
Implement the full non-production rehearsal harness for canary, widen, pause, rollback, rollforward, and kill-switch behavior in Vecells.

This task must make release-wave behavior executable and testable before any production rollout exists.

This is real runtime and release-control work. Produce orchestration code, rehearsal environments, scenario catalogs, action and settlement recording, rollback drills, impact previews, browser-viewable inspection surfaces, and tests. Do not leave canary strategy or rollback confidence as runbook prose.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `ReleaseWatchTuple`
  - `WaveObservationPolicy`
  - `WaveGuardrailSnapshot`
  - `ReleaseWatchEvidenceCockpit`
  - `WaveActionImpactPreview`
  - `WaveActionExecutionReceipt`
  - `WaveActionRecord`
  - `WaveActionSettlement`
  - `ReleaseRecoveryDisposition`
  - pipeline and operational-readiness rules around canary, widening, rollback, and recovery activation
- `phase-0-the-foundation-protocol.md` for authoritative settlement, route-intent, audit, and freeze rules
- `platform-admin-and-config-blueprint.md` for promotion, rollback, kill-switch, and handoff expectations
- `operations-console-frontend-blueprint.md` for ops-facing watch posture and intervention semantics
- outputs from tasks `091`, `094`, `095`, `097`, `099`, `100`, and `101`
- `forensic-audit-findings.md`, especially Findings `95`, `96`, `103`, `104`, `112`, and `119`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must publish the rehearsal harness now so later wave-control and governance work build on demonstrated behavior instead of imagined rollback posture. If a later task owns richer live-wave surfaces, create the executable seam here and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build the entire harness now for preview, integration, and preprod-like environments.
- support synthetic tenant scopes, simulated probe outcomes, guardrail state changes, stale tuple injection, rollback triggers, and recovery-disposition activation
- execute canary start, widen, pause, rollback, rollforward, and kill-switch in rehearsal only
- preserve the same action, preview, receipt, and settlement semantics required later in production
The mock-now path is the official rehearsal path. Do not build a toy simulator disconnected from the canonical wave objects.

Actual_production_strategy_later:
When live production rollouts occur, preserve the same action record, impact preview, execution receipt, settlement, and watch-cockpit semantics. Production hardening may add stronger blast-radius controls, live incident integrations, and operator approvals, but it must not change the meaning of settled wave actions or rollback evidence.

Primary outcome:
The repository gains a non-production canary and rollback harness that can prove whether the current release tuple, watch tuple, readiness tuple, and recovery posture can survive guarded rollout actions.

Implementation deliverables to create:
1. `docs/architecture/102_non_production_canary_and_rollback_harness.md`
2. `docs/architecture/102_wave_action_rehearsal_and_rollback_rules.md`
3. `docs/architecture/102_canary_and_rollback_cockpit.html`
4. `data/analysis/canary_scenario_catalog.json`
5. `data/analysis/wave_action_preview_schema.json`
6. `data/analysis/wave_action_settlement_schema.json`
7. `data/analysis/canary_guardrail_matrix.csv`
8. orchestration code for rehearsal canary start, widen, pause, rollback, rollforward, and kill-switch flows
9. scenario drivers for synthetic probes, degraded trust, tuple drift, blocked readiness, and recovery-disposition activation
10. `tools/analysis/validate_canary_and_rollback_harness.(py|ts)`
11. integration and rehearsal tests for each action path, including tuple supersession and rollback evidence
12. `tests/playwright/canary-and-rollback-cockpit.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- wave-action impact preview generation from the current watch tuple, guardrail snapshot, publication parity, and continuity evidence
- action execution receipt and authoritative settlement capture
- blocked-action handling when readiness, tuple, parity, trust, or recovery posture is stale or invalid
- rollback and kill-switch rehearsal that proves the target tuple and recovery disposition are known before action execution
- supersession handling when a new watch tuple is required before widen or resume
- cockpit views that distinguish preview, accepted, observed, satisfied, constrained, rollback-required, and superseded states

Implementation instructions:
1. Model canary actions as first-class commanded operations with durable records and settlements, not as CI steps plus logs.
2. Support at least these rehearsal scenarios:
   - happy-path canary start and dwell
   - widen after satisfied observation
   - pause due to constrained guardrail
   - rollback due to trigger breach
   - kill-switch due to severe trust or parity failure
   - rollforward after superseding tuple and fresh readiness proof
3. Require a fresh readiness snapshot and current watch tuple before widening or resume.
4. Never allow these anti-patterns:
   - widening after observation-policy drift without superseding tuple
   - rollback without explicit target tuple and declared recovery disposition
   - action settlement based only on CI transport success
   - mutating the meaning of an existing action or tuple in place
5. Publish one canary cockpit that exposes preview, execution, observation, settlement, rollback readiness, and current blockers from one canonical dataset.

Gap handling you must perform during this task:
- If live ring structure or cohort logic is still provisional, publish bounded non-production ring and cohort definitions as `GAP_RESOLUTION_CANARY_RING_*` while preserving the canonical action and tuple law.
- If later tasks own richer governance approvals or resilience surfaces, bind to placeholder refs and record `FOLLOW_ON_DEPENDENCY_WAVE_GOVERNANCE_*`.
- If some recovery dispositions are not yet fully authored, use the narrowest safe read-only or recovery-only posture and record `GAP_RECOVERY_DISPOSITION_*`.

Backend and release engineering standards:
- deterministic scenario seeding and repeatable time control
- idempotent action submission and settlement
- explicit blast-radius accounting per scenario
- append-only audit for every rehearsal action
- metrics for blocked actions, supersession frequency, rollback trigger frequency, and rollback rehearsal success

Verification requirements:
- scenario catalog tests
- preview and settlement schema tests
- blocked-action tests
- rollback and kill-switch tests
- tuple supersession tests
- readiness freshness tests
- `Playwright_or_other_appropriate_tooling` tests for the cockpit surface, including stable selectors, readable state transitions, keyboard navigation, and reduced-motion equivalence

Definition of done:
- non-production wave actions can be rehearsed end to end
- rollback and kill-switch posture are proven, not assumed
- watch tuple, readiness tuple, and recovery posture stay joined through execution and settlement
- later production rollout work has a trustworthy rehearsal harness to build on
```
