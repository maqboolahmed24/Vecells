# Prompt 059 For 059_seq_phase0_define_seed_data_and_simulator_strategy

Use the shared operating contract first:
[shared_operating_contract_056_to_065.md](/Users/test/Code/V/prompt/shared_operating_contract_056_to_065.md)

```text
Execute roadmap task 059_seq_phase0_define_seed_data_and_simulator_strategy.md.

Inherit the Shared_operating_contract_for_prompts_056_to_065 and consume the validated outputs from tasks `001-058`.

Mission:
Define the canonical Phase 0 seed-data, reference-case, and simulator strategy that powers preview environments, integration flows, early shells, backend tests, and release verification without waiting for live NHS or provider onboarding.

This task must formalize one high-fidelity, simulator-first execution posture. Every simulator-backed dependency must have both:
- `Mock_now_execution`
- `Actual_provider_strategy_later`

Algorithm grounding you must honor:
- `phase-cards.md`, especially:
  - the requirement to create local simulators for NHS login, IM1, MESH, telephony, and notifications
  - the requirement to mount shell UIs against seed data
  - the requirement to prove a synthetic request can be created, duplicate-replayed, quarantined, routed to fallback review, held behind identity or confirmation blockers, and audited end to end while closure remains coordinator-owned
- `phase-0-the-foundation-protocol.md`, especially:
  - the delivery skeleton note for `services/adapter-simulators`
  - `SubmissionEnvelope`, `Request`, `RequestLineage`, `IdentityBinding`, `DuplicateCluster`, `FallbackReviewCase`, `RouteIntentBinding`, `CommandSettlementRecord`, and related invariants
  - telephony readiness and continuation eligibility rules
  - the reference-case simulation requirement for config and release proof
- `platform-runtime-and-release-blueprint.md`, especially preview environments, simulator-backed integration flows, reference-case simulation, `ContinuityContractCoverageRecord`, and `SyntheticRecoveryCoverageRecord`
- `platform-frontend-blueprint.md` and `ux-quiet-clarity-redesign.md` where same-shell continuity and degraded / recovery posture must remain truthful under seed data
- outputs from tasks `004`, `005`, `040`, `042-058`
- `forensic-audit-findings.md`, especially the findings that corrected continuity, degraded-mode, duplicate, blocker, and release-proof gaps

Primary outcome:
One machine-readable seed and simulator pack proving:
- which canonical scenarios exist
- how they map to shells, routes, aggregates, and continuity families
- how Phase 0 shells and services can run end to end on local simulated dependencies
- how those simulators later cut over to real providers without changing domain truth semantics

Deliverables to create:
1. `docs/architecture/59_seed_data_and_simulator_strategy.md`
2. `docs/architecture/59_reference_case_corpus.md`
3. `docs/architecture/59_mock_now_vs_actual_provider_cutover.md`
4. `docs/architecture/59_seed_and_simulator_studio.html`
5. `data/analysis/reference_case_catalog.json`
6. `data/analysis/seed_dataset_matrix.csv`
7. `data/analysis/simulator_contract_catalog.json`
8. `data/analysis/simulator_fault_injection_matrix.csv`
9. `data/analysis/reference_case_to_continuity_control_matrix.csv`
10. `packages/test-fixtures/reference-cases/README.md`
11. `packages/test-fixtures/reference-cases/reference_case_index.json`
12. `tools/analysis/validate_seed_and_simulator_strategy.(py|ts)`
13. `tests/playwright/seed-and-simulator-studio.spec.(ts|js)`

Each reference-case row must capture at minimum:
- `referenceCaseId`
- `caseCode`
- `persona`
- `channelProfile`
- `routeFamily`
- `startingSurface`
- `requiredSeedObjects[]`
- `requiredSimulatorRefs[]`
- `requiredContinuityControlRefs[]`
- `expectedStateAxisTransitions[]`
- `expectedSettlementRefs[]`
- `expectedClosureBlockerRefs[]`
- `expectedRecoveryEnvelopeRefs[]`
- `releaseVerificationUse = preview | integration | preprod | wave_probe`
- `notes`

Each simulator row must capture at minimum:
- `simulatorId`
- `dependencyCode`
- `simulatorType`
- `mock_now_execution` object describing request/response, callbacks, replay, ordering, faults, and seeded identities/messages
- `actual_provider_strategy_later` object describing onboarding prerequisites, secret classes, proof obligations, cutover checklist, and rollback-back-to-simulator strategy
- `supportedReferenceCaseIds[]`
- `supportedFaultModes[]`
- `contractDigestRef`

Execution steps:
1. Start from task `005`’s lineage matrix, task `056`’s mutation law, task `057`’s adapter profile strategy, and task `058`’s verification-ring policy.
2. Publish the reference-case corpus. At minimum include cases for:
   - clean self-service submit
   - duplicate retry returning prior accepted result
   - duplicate collision opening review
   - fallback review after accepted progress degrades
   - wrong-patient freeze / identity repair hold
   - urgent diversion required then urgent diversion issued
   - telephony urgent-live-only capture
   - telephony seeded continuation versus challenge continuation
   - booking confirmation-pending / ambiguity
   - pharmacy dispatch proof pending and weak-match outcome
   - support replay restore and same-shell recovery
3. Publish the seed dataset matrix for:
   - tenants / organisations / practices
   - patients and patient identity states
   - envelopes / requests / lineages / child-case links
   - bookings / hub / pharmacy / communications child cases
   - grants, route intents, acting scopes, leases, blockers, confirmation gates
   - release/runtime binding rows needed for shell truth
4. Publish the simulator contract catalog. At minimum define simulator-first boundaries for:
   - NHS-login-like auth bridge
   - GP / IM1-like booking or linkage boundary
   - MESH-like mailbox
   - telephony / IVR and callback transport
   - notifications / SMS / email
5. For each simulator, include the two required sections:
   - `Mock_now_execution`
   - `Actual_provider_strategy_later`
   Make the cutover delta explicit and bounded.
6. Publish the fault-injection matrix covering timeouts, replays, duplicates, stale callbacks, disputed receipts, ordering inversion, and partial outage.
7. Publish the reference-case-to-continuity-control matrix so later verification work can prove `patient_nav`, `more_info_reply`, `conversation_settlement`, `intake_resume`, `booking_manage`, `hub_booking_manage`, `support_replay_restore`, `workspace_task_completion`, and `pharmacy_console_settlement` using the same case corpus.
8. Build the studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
9. Validate that:
   - every required Phase 0 reference case is backed by seed data and simulators
   - no seed case relies on live provider credentials or live external side effects
   - simulators cover duplicate, replay, disputed, degraded, and recovery paths, not only happy paths
   - simulator and future live-provider strategies preserve the same canonical aggregate, event, settlement, and blocker semantics
   - shell preview environments can run from the seed corpus without inventing fake success states

Mandatory gap closures you must perform:
- Close the “Phase 0 waits on real external onboarding” gap by making simulators the legal default.
- Close the “seed data covers only happy paths” gap by including duplicate, blocker, confirmation, identity-repair, and degraded cases.
- Close the “preview shells can look polished while ignoring continuity truth” gap by binding reference cases to continuity controls and runtime bindings.
- Close the “simulator and future live provider behave differently” gap by forcing each simulator to describe its later cutover strategy and rollback path.
- Close the “telephony seeded continuation can be guessed from transcript state or caller heuristics” gap by using the exact readiness and continuation-eligibility law in the seed corpus.
- Close the “release proof and seed/reference-case simulation are separate worlds” gap by making cases reusable across preview, integration, preprod, and wave probes.

Industry-best-practice requirements:
- deterministic fixtures and stable IDs
- simulator APIs that model replay and callback ordering explicitly
- fixture composability by case code and tenant scope
- PHI-safe synthetic data with realistic structure but no real personal data
- fault injection as first-class contract behavior
- cutover strategies that preserve aggregate and settlement semantics

Non-negotiable rules:
- Phase 0 external dependencies are simulator-first.
- Seed data must include both happy and unhappy path truth, not visual placeholders only.
- Reference cases must reuse the same canonical objects and state axes defined by the blueprint corpus.
- No simulator may introduce domain semantics that the later live provider strategy cannot honor.
- No shell may display calm or writable posture in preview or seed mode unless the governing seed tuples and settlements justify it.

Mandatory UI and interaction specification for `59_seed_and_simulator_studio.html`:
Name the surface mode `Seed_And_Simulator_Studio`.

Tone:
- minimalist premium validation atlas
- should feel like a curated scenario gallery plus systems harness, not a test-run dump
- calm, crisp, and visually distinctive

Branding:
- restrained Vecells wordmark plus an inline SVG `SS` monogram
- include one elegant case-flow map and one simulator-boundary diagram
- include compact badges for happy, degraded, blocked, and recovery posture

Palette:
- canvas `#F7F8FC`
- rail `#EEF2F8`
- panel `#FFFFFF`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- scenario accent `#0EA5A4`
- simulator accent `#7C3AED`
- recovery accent `#0F9D58`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for case codes, IDs, simulator codes, and hash refs

Layout:
- sticky masthead with:
  - reference-case count
  - simulator count
  - degraded-case count
  - continuity-control count
- left rail (`300px`):
  - persona / channel / case filters
  - simulator filters
- center canvas:
  - case-flow map
  - simulator-boundary diagram
  - curated case cards
- right inspector (`396px`):
  - selected reference case
  - seed objects
  - simulator bindings
  - mock-now vs actual-later comparison
- lower region:
  - seed dataset matrix
  - fault-injection matrix
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `620px`
- primary controls `44px`
- case cards `min-height 170px`
- all diagrams require adjacent table/list parity

Motion:
- `120ms` hover/focus
- `180ms` filter / case selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by persona, channel, continuity control, simulator, and posture class
- selecting a case card must synchronize diagrams, matrices, and inspector
- keyboard navigation across filters, cards, and table rows
- deterministic `data-testid` markers for case-flow map, simulator diagram, case cards, inspector, and defect strip
- mock-vs-live comparison must remain understandable without color alone

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- persona and channel filtering
- case-card selection
- simulator filtering
- diagram / matrix / inspector synchronization
- keyboard navigation
- responsive layout
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has one high-fidelity seed-data and simulator strategy that supports preview, integration, and release proof without waiting for live providers.
- Reference cases cover both happy and unhappy path truth.
- Later provider cutover can proceed without rewriting domain semantics.
```
