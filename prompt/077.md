# Prompt 077 For 077_par_phase0_track_backend_build_lifecycle_coordinator_service

Use the shared operating contract first:
[shared_operating_contract_076_to_085.md](/Users/test/Code/V/prompt/shared_operating_contract_076_to_085.md)

```text
Execute roadmap task 077_par_phase0_track_backend_build_lifecycle_coordinator_service.md.

Inherit the Shared_operating_contract_for_prompts_076_to_085 and consume the validated outputs from tasks `001-076`.

Mission:
Build the canonical `LifecycleCoordinator` service as the sole cross-domain authority for request-level milestone derivation, closure evaluation, governed reopen, lineage-fence issuance, and blocker / confirmation materialization.

This task is full service implementation. It must consume domain evidence from triage, booking, hub, pharmacy, callback, messaging, recovery, and support flows without allowing any of those domains to write canonical request closure or cross-domain milestone truth directly.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.11 LineageFence`
  - `#### 1.12 RequestClosureRecord`
  - `#### 2.1 LifecycleCoordinator`
  - `### 3. Non-negotiable invariants`
  - `#### 9.1 Lease acquisition`
  - `#### 9.4 Workflow state ownership`
  - `#### 9.5 Required workflow semantics`
  - `#### 9.6 Closure evaluation algorithm`
  - `#### 9.7 Reopen triggers`
- `phase-3-the-human-checkpoint.md`, where triage now emits milestone signals and decision evidence rather than writing canonical request state
- `phase-4-the-booking-engine.md`, where booking success, waitlist, fallback, and ambiguity remain case-local first and must surface through signals plus confirmation gates
- `phase-5-the-network-horizon.md`, where hub-native truth and practice-visibility debt remain case-local and feed the coordinator via hub signals
- `phase-6-the-pharmacy-loop.md`, where pharmacy outcomes, bounce-back, reopen, and reconciliation gates remain case-local and feed the coordinator via pharmacy signals
- `callback-and-clinician-messaging-loop.md`, where callback or thread completion may close the child object but not the request directly
- `forensic-audit-findings.md`, especially Findings `45`, `68`, `69`, `70`, `71`, `72`, `74`, `75`, `76`, `77`, `78`, and `79`
- outputs from tasks `001-007`, `019`, `041-045`, `055-057`, `061`, `067`, `071-076`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. You may create minimal signal contracts or event adapters for sibling tracks if required, but do not depend on later phase-specific services already existing. Where a signal contract is missing, publish the smallest bounded typed stub and record `PARALLEL_INTERFACE_GAP_*`.

Mock_now_execution:
Build a lifecycle simulation harness now. It must cover:
- normal intake to triage to outcome to legal closure
- triage more-info cycles with late reply and reopen pressure
- booking success with confirmation pending and later confirmation clearance
- hub return and practice-visibility debt
- pharmacy weak-match review and later settled outcome
- wrong-patient repair hold and release
- duplicate-review hold, fallback-review hold, and reachability-repair hold
The harness must drive the same signals, blocker materialization, reopen triggers, and closure artifacts the live system will later use.

Actual_production_strategy_later:
When live triage, booking, hub, pharmacy, callback, messaging, and support services are onboarded, they must all emit their evidence into the same signal and case-link contracts consumed by `LifecycleCoordinator`. Live services may enrich the incoming signal stream, but they may not bypass the coordinator and write request closure, cross-domain milestone truth, or blocker-materialization state directly.

Primary outcome:
The repository gains the deterministic coordinator that turns distributed domain evidence into one canonical request milestone stream, one authoritative blocker set, and one governed close / reopen decision path.

Implementation deliverables to create:
1. `LifecycleCoordinator` service implementation partitioned by `episodeId`
2. deterministic command / event intake for lifecycle-relevant signals and case-link deltas
3. `LineageFence` issuance, compare-and-set validation, and epoch-advance logic
4. blocker and confirmation materializers that publish `Request.currentClosureBlockerRefs[]` and `Request.currentConfirmationGateRefs[]`
5. milestone-derivation engine that maps domain signals into request-level milestone changes without allowing direct child-domain writes
6. close / defer evaluator using `RequestClosureRecord`
7. governed reopen evaluator implementing the canonical reopen trigger family
8. `docs/architecture/77_lifecycle_coordinator_service_design.md`
9. `docs/architecture/77_closure_and_reopen_state_machine.md`
10. `docs/architecture/77_lifecycle_control_center.html`
11. `data/analysis/lifecycle_signal_contract_matrix.csv`
12. `data/analysis/lifecycle_coordinator_casebook.json`
13. `data/analysis/lineage_fence_epoch_manifest.json`
14. `tools/analysis/validate_lifecycle_coordinator.(py|ts)`
15. unit tests, deterministic replay tests, partition-order tests, stale-epoch tests, and cross-domain race tests
16. `tests/playwright/lifecycle-control-center.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- consume lifecycle-relevant signals, case-link transitions, lease changes, repair cases, confirmation gates, grants, reachability dependencies, and fallback-review cases under one deterministic coordinator stream
- issue and validate `LineageFence.currentEpoch`
- derive `Request.workflowState` milestones under coordinator control only
- materialize request-level and episode-level blocker sets explicitly, not as inferred prose
- persist `RequestClosureRecord(decision = defer | close)` for every close evaluation
- reopen a closed or nearly-closed request only through the canonical reopen algorithm when new evidence, unable-to-complete, bounce-back, contact failure, or revocation signals require it
- reject stale signal writes and stale close attempts against the current epoch

Execution steps:
1. Implement the coordinator as a deterministic replicated state machine partitioned by `episodeId`, with per-partition replayable input ordering.
2. Publish a typed lifecycle-signal contract set covering at least:
   - triage milestone / decision / lease signals
   - booking milestone / confirmation / waitlist / fallback signals
   - hub milestone / return / continuation / practice-visibility signals
   - pharmacy outcome / reopen / reconciliation / continuation signals
   - callback and thread completion / failure signals
   - duplicate, fallback, identity-repair, reachability, grant, and confirmation delta signals
3. Implement blocker materialization from active domain rows plus case-link state. The materialized blocker refs must be explicit, replayable, and directly validated before closure.
4. Implement milestone derivation rules that preserve the canonical `triage_ready`, `triage_active`, `handoff_active`, `outcome_recorded`, and `closed` semantics without smuggling blocker classes into workflow state.
5. Implement the close / defer evaluator using the full closure algorithm and persist `RequestClosureRecord` every time closure is evaluated.
6. Implement governed reopen so new authoritative evidence can reopen safely without mutating historical closure artifacts in place.
7. Build the lifecycle control center and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Write tests that prove child domains cannot write request closure directly, stale epochs are rejected, replayed signals are stable, and mixed ordering does not produce contradictory closure truth.

Mandatory gap closures you must perform:
- Close the “close is a passive terminal state” gap by making closure a coordinator-owned, persisted decision.
- Close the “child domains write canonical request state directly” gap by routing all cross-domain milestone truth through lifecycle signals and coordinator derivation.
- Close the “blockers exist in prose but not in materialized current refs” gap by publishing and validating the blocker refs directly.
- Close the “repair and reconciliation look like workflow states” gap by keeping them in blocker or gate refs only.
- Close the “booking ambiguity blocks closure only implicitly” gap by requiring current confirmation gates to participate in closure evaluation.
- Close the “closed requests can silently reopen by local domain rewrite” gap by implementing explicit governed reopen logic and preserved closure history.
- Close the “stale lineage views can still mutate milestone truth” gap through fence-epoch validation.

Industry-best-practice requirements:
- deterministic input ordering and replay-safe partitioning
- append-only authority events and audit joins
- explicit state-transition documentation and validator coverage
- fail-closed behavior on stale fence, missing blocker basis, or ambiguous signal scope
- no controller or projection shortcuts around coordinator authority
- concurrency tests for close-vs-reopen, close-vs-blocker-open, and multi-domain signal races

Non-negotiable rules:
- Only `LifecycleCoordinator` may derive cross-domain request milestone changes and write `Request.workflowState = closed`.
- Child domains emit local truth and lifecycle evidence; they do not write canonical closure or reconciliation meaning directly.
- `LineageFence.currentEpoch` must guard invariant-changing lifecycle mutation.
- Closure requires empty coordinator-materialized blocker refs and persisted `RequestClosureRecord(decision = close)`.
- Reopen is governed and explicit; it is never a silent side effect of a local case update.

Mandatory UI and interaction specification for `77_lifecycle_control_center.html`:
Name the surface mode `Lifecycle_Control_Center`.

Tone:
- premium operational control room
- precise, calm, and highly legible under pressure
- should feel like a mission-control timeline for request truth, not a dashboard wall

Branding:
- restrained Vecells wordmark plus inline SVG `LC` monogram
- one request-milestone ribbon
- one blocker and confirmation-gate lattice diagram
- one lineage-fence epoch waterfall

Palette:
- canvas `#F6F8FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F3F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- milestone accent `#2563EB`
- blocker accent `#C24141`
- confirmation accent `#7C3AED`
- reopen accent `#D97706`
- settled accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for epochs, causal tokens, request IDs, and case-link refs

Layout:
- sticky masthead with active partitions, close-ready requests, deferred requests, and stale-epoch incidents
- left filter rail (`320px`) for phase signal families, blocker families, partition filters, and reopen triggers
- central canvas with:
  - request-milestone ribbon
  - blocker and gate lattice diagram
  - lineage-fence epoch waterfall
- right inspector (`420px`) for selected request lineage, current blockers, current gates, active epoch, and last close evaluation
- lower grid with one signal stream table and one closure-history table

Dimensional constraints:
- max width `1560px`
- masthead `76px`
- main visual zone min height `300px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filtering by request milestone, blocker family, signal family, reopen trigger, and partition
- selecting a request or signal must synchronize milestone ribbon, blocker lattice, epoch waterfall, inspector, and tables
- keyboard navigation across all filters and tables
- deterministic `data-testid` markers for milestone ribbon, blocker lattice, epoch waterfall, signal table, closure table, and inspector
- every diagram must have a textual parity panel and table fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior across signals, blockers, and milestones
- synchronized selection state across diagrams and tables
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification

Definition of done:
- The repository has a real lifecycle coordinator service with deterministic replay-safe behavior.
- Request milestone derivation, closure, and reopen are coordinator-owned and explicit.
- Child domains can no longer write canonical request closure or cross-domain milestone truth directly.
- Blocker and confirmation sets are materialized, validated, and visible.
- Later workspace, booking, hub, pharmacy, portal, and ops tasks can consume this coordinator as the single authority for request lifecycle truth.
```
