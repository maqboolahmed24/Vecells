# Prompt 095 For 095_par_phase0_track_runtime_implement_schema_migration_runner_and_projection_backfill_runner

Use the shared operating contract first:
[shared_operating_contract_086_to_095.md](/Users/test/Code/V/prompt/shared_operating_contract_086_to_095.md)

```text
Execute roadmap task 095_par_phase0_track_runtime_implement_schema_migration_runner_and_projection_backfill_runner.md.

Inherit the Shared_operating_contract_for_prompts_086_to_095 and consume the validated outputs from tasks `001-094`.

Mission:
Implement the authoritative schema-migration runner and projection-backfill runner for Vecells.

This task must create real runtime-control code for:
- `SchemaMigrationPlan`
- `MigrationExecutionBinding`
- `MigrationImpactPreview`
- `MigrationActionRecord`
- `MigrationActionSettlement`
- `ProjectionBackfillPlan`
- `ProjectionReadinessVerdict`
- expand-migrate-contract execution, dry-run preview, dual-read or partial-read handling, and observation windows

This is real backend and runtime-control work. Produce runner code, plans, validators, APIs or CLIs, and tests. Do not leave migration sequencing, backfill behavior, or readiness verdicts as release notes.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### ProjectionBackfillPlan`
  - `### ProjectionReadinessVerdict`
  - the verification-ladder sections that require migration and backfill proof, compatibility windows, rollback posture, and audience-surface readiness evidence
  - `## Data persistence and migration contract`
- `phase-0-the-foundation-protocol.md`, especially the rule that migration runners enforce expand-migrate-contract discipline and emit route-scoped readiness evidence before writable routes remain live
- outputs from tasks `049`, `058`, `060`, `064`, `065`, `082`, `085`, `091`, and `094`
- `forensic-audit-findings.md`, especially Findings `91`, `95`, `96`, `102`, `112`, and `113`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It may implement generic migration and backfill runners plus example plans now, but domain-specific migration content for later phases must remain attachable through bounded schemas and plan manifests. If future tasks own a specific migration, publish the runner seam and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build the full migration and backfill control path now in local and non-production environments.
- support dry runs, synthetic data rehearsal, dual-read simulation, projection rebuild drills, observation windows, and fail-closed readiness verdicts against preview and seeded data
- prove that the current runtime tuple can survive additive schema change and projection backfill without pretending that a green rebuild alone means audience safety
- use realistic seeded route families and projection sets even where live providers are not yet present
The mock-now path is the real rehearsal path for future production migrations and must preserve the same plan, action, settlement, and readiness semantics.

Actual_production_strategy_later:
When production migrations occur, they must preserve the same plan schema, dry-run evidence, rollout bindings, backfill convergence checks, readiness verdict rules, and observation-window semantics. Production hardening may add higher throughput, stronger scheduling, or ops approval controls, but it must not allow direct one-off SQL or projection rebuild jobs to become authoritative migration truth.

Primary outcome:
The repository gains a real migration and projection-backfill control system that later release and runtime work can use safely and repeatedly.

Implementation deliverables to create:
1. runner code for schema migrations with plan parsing, dry-run preview, execution, settlement recording, and observation windows
2. runner code for projection backfill with plan parsing, checkpointing, resumability, convergence tracking, and readiness verdict generation
3. machine-readable plan schemas for schema migration and projection backfill
4. APIs, CLIs, or service endpoints for rehearsal, execution, inspection, and verdict retrieval
5. local-development and CI rehearsal flows using preview or seeded tuples
6. `docs/architecture/95_schema_migration_and_projection_backfill_design.md`
7. `docs/architecture/95_expand_migrate_contract_and_readiness_rules.md`
8. `docs/architecture/95_migration_and_backfill_control_room.html`
9. `data/analysis/schema_migration_plan_schema.json`
10. `data/analysis/projection_backfill_plan_schema.json`
11. `data/analysis/migration_readiness_matrix.csv`
12. `tools/analysis/validate_migration_and_backfill_runner.(py|ts)`
13. migration tests, dry-run tests, resumability tests, convergence tests, readiness-verdict tests, and fail-closed smoke tests
14. `tests/playwright/migration-and-backfill-control-room.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- parse and validate schema migration plans and projection backfill plans from machine-readable manifests
- execute migrations using expand-migrate-contract discipline with dry-run preview and settlement records
- execute projection backfills with checkpoints, resumability, idempotence, and convergence measurement
- generate `ProjectionReadinessVerdict` artifacts that bind readiness to runtime tuple, projection set, route family, and observation evidence rather than rebuild completion alone
- expose machine-readable execution, settlement, and readiness state
- fail closed when required evidence, compatibility windows, or observation checks are missing or stale

Execution steps:
1. Re-read the migration, backfill, and verification-ladder sections plus earlier runtime outputs that shape projection and data-plane truth.
2. Honor the data-plane, FHIR, API-contract, rebuild, provenance, and publication baselines from earlier tasks; if prerequisites are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
3. Implement plan schemas and runner code for schema migration and projection backfill.
4. Implement dry-run preview, migration action records, settlements, checkpointing, resumability, and observation-window handling.
5. Implement `ProjectionReadinessVerdict` generation and refusal behavior when required evidence is incomplete.
6. Build the control room and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write smoke tests proving partial-read, dual-read, backfill lag, replay restart, observation-window failure, and rollback-mode mismatches all fail into explicit blocked or constrained verdicts.

Mandatory gap closures you must perform:
- Close the “schema migration is a one-off DBA action” gap by implementing governed plan and settlement objects.
- Close the “projection rebuild success implies audience safety” gap by generating explicit readiness verdicts.
- Close the “backfill jobs cannot resume safely” gap by implementing checkpointed, resumable backfill control.
- Close the “preview cannot rehearse migration risk” gap by supporting dry-run and seeded rehearsal paths.
- Close the “readiness evidence lives in separate logs and dashboards” gap by publishing machine-readable settlement and verdict artifacts.

Industry-best-practice requirements:
- expand-migrate-contract discipline
- dry-run preview and impact inspection before execution
- resumable, idempotent backfill runners
- observation windows and readiness verdicts under version control
- no direct ad hoc migration or rebuild actions treated as authoritative
- fail-closed gating when evidence or compatibility windows are incomplete

Non-negotiable rules:
- Projection backfill may not become business-truth authority.
- Migration and backfill plans must be machine-readable and validated.
- Readiness requires explicit verdict artifacts, not green job status alone.
- Runners must be restart-safe and idempotent.
- Preview and non-production rehearsals must use the same runner semantics as later production use.

Mandatory UI and interaction specification for `95_migration_and_backfill_control_room.html`:
Name the surface mode `Migration_And_Backfill_Control_Room`.

Tone:
- premium runtime-change control room
- minimalist, exact, and operationally serious
- should feel like a luxury mission-control board, not a generic job-status table

Branding:
- restrained Vecells wordmark plus inline SVG `MB` monogram
- one migration-phase lane diagram
- one projection-readiness matrix
- one execution and observation timeline strip

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- migration accent `#2563EB`
- backfill accent `#0EA5A4`
- readiness accent `#7C3AED`
- warning accent `#D97706`
- blocked accent `#C24141`
- ready accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for plan IDs, execution IDs, settlement IDs, checkpoint IDs, and verdict hashes

Layout:
- sticky masthead with active migrations, lagging backfills, blocked verdicts, and observation-window alerts
- left rail (`324px`) with environment filters, plan-state filters, route-family filters, and verdict filters
- central canvas with:
  - migration-phase lane diagram
  - projection-readiness matrix
  - execution / observation timeline strip
- right inspector (`420px`) for selected plan, execution, checkpoint, or verdict
- lower grid with one execution table and one readiness / evidence table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by environment, plan state, route family, verdict state, and rollback mode
- selecting a row must synchronize diagrams, timeline, inspector, and tables
- keyboard navigation across filters and tables
- deterministic `data-testid` markers for phase diagram, readiness matrix, execution timeline, execution table, evidence table, and inspector
- every visual must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification
- verification that blocked, constrained, and ready verdicts remain visually and semantically distinct

Definition of done:
- The repository has a real schema-migration and projection-backfill control system.
- Migration and backfill plans, executions, settlements, and readiness verdicts are machine-readable.
- Dry-run rehearsal and fail-closed readiness behavior work in non-production now.
- Later release, verification, and scale-out work can reuse this runner system instead of inventing migration control from scratch.
```
