# Prompt 084 For 084_par_phase0_track_runtime_provision_core_cloud_network_and_private_egress_controls

Use the shared operating contract first:
[shared_operating_contract_076_to_085.md](/Users/test/Code/V/prompt/shared_operating_contract_076_to_085.md)

```text
Execute roadmap task 084_par_phase0_track_runtime_provision_core_cloud_network_and_private_egress_controls.md.

Inherit the Shared_operating_contract_for_prompts_076_to_085 and consume the validated outputs from tasks `001-083`.

Mission:
Provision the Phase 0 runtime network foundation for Vecells:
- core cloud network / VPC-or-equivalent topology
- workload-family segmentation
- trust-zone boundaries
- private egress controls and allowlists
- service-identity-ready internal connectivity

This task must turn the runtime topology and trust-boundary strategy into concrete, machine-readable infrastructure. It must not leave topology, cross-plane reachability, or egress policy implied by diagrams or deployment folklore.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `## Runtime topology contract`
  - `### RuntimeWorkloadFamily`
  - `### TrustZoneBoundary`
  - `### RuntimeTopologyManifest`
  - `## Runtime rules`
  - `## Security baseline contract`
  - `## Operational readiness contract`
- `phase-0-the-foundation-protocol.md`, especially the trust-zone, runtime publication, acting-scope, and freeze / trust constraints that later gateway surfaces consume
- outputs from tasks `011`, `015`, `046`, `047`, `051`, `054`, `058`, `060`, `075`, and `083`
- `forensic-audit-findings.md`, especially Findings `91`, `95`, `96`, `102`, and `114`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must prepare extension points for later object storage, event bus, cache, secrets, and gateway tasks without stealing their ownership. Where a later runtime task owns a dependency, publish the network seam and record `FOLLOW_ON_DEPENDENCY_*` rather than silently provisioning unrelated infrastructure.

Mock_now_execution:
Build a sandbox-safe and local-development-ready network baseline now.
- provide local emulation of workload families and trust boundaries for developer and CI use
- provide non-production cloud IaC for the chosen platform baseline from task `011`
- implement deny-by-default outbound policy and explicit allowlist stubs even when the destination services are still simulators
- publish the same `RuntimeTopologyManifest` and `TrustZoneBoundary` contracts in local and cloud-backed environments
The mock-now path must be realistic enough that later live provider adapters can move from local or sandbox targets to production targets without changing topology law.

Actual_production_strategy_later:
When production-grade cloud networking is finalized, it must preserve the same workload-family segmentation, trust-boundary contracts, service identities, and egress allowlist semantics. Production hardening may add HA, multi-zone routing, managed firewalling, private endpoints, inspection, or egress brokers, but it must not collapse `public_edge`, `shell_delivery`, `command`, `projection`, `integration`, `data`, and `assurance_security` into one implicit network trust zone.

Primary outcome:
The repository gains infrastructure-as-code and runtime manifests that make Vecells network segmentation, cross-plane reachability, and private egress posture explicit, testable, and ready for later runtime tracks.

Implementation deliverables to create:
1. infrastructure-as-code modules for the core network, workload-family segments, subnets or equivalent isolation units, routing, and network policy
2. machine-readable provisioning of `RuntimeWorkloadFamily`, `TrustZoneBoundary`, and `RuntimeTopologyManifest` aligned to the chosen cloud baseline
3. private egress controls with deny-by-default behavior and explicit allowlist objects / rules per workload family
4. service-identity-ready internal connectivity and trust-boundary documentation for service-to-service calls
5. local-development / CI network emulation aligned to the same workload-family model
6. `docs/architecture/84_core_network_and_private_egress_design.md`
7. `docs/architecture/84_runtime_topology_and_trust_boundary_realization.md`
8. `docs/architecture/84_runtime_network_trust_atlas.html`
9. `data/analysis/runtime_topology_manifest.json`
10. `data/analysis/trust_zone_boundary_matrix.csv`
11. `data/analysis/private_egress_allowlist_manifest.json`
12. `tools/analysis/validate_runtime_network_and_egress.(py|ts)`
13. infrastructure tests, policy tests, and environment smoke tests
14. `tests/playwright/runtime-network-trust-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- define and provision distinct workload-family network placement for `public_edge`, `shell_delivery`, `command`, `projection`, `integration`, `data`, and `assurance_security`
- publish `TrustZoneBoundary` as the only legal declaration of cross-plane reachability
- enforce deny-by-default outbound policy with explicit per-family allowlists
- prohibit direct browser access to integration, data, or assurance workloads
- preserve tenant-transfer and assurance-trust-transfer semantics across boundaries
- keep the topology manifest current and mark it stale or withdrawn when infra drift occurs

Execution steps:
1. Honor the cloud-region, tenancy, and trust-zone choices already frozen in tasks `011` and `046-047`; if those outputs are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
2. Provision the base network and isolation model using the chosen IaC tool from prior ADRs; if the tool choice is absent but no ADR conflicts exist, use `ASSUMPTION_IAC_TOOL = terraform_or_opentofu` and record it explicitly.
3. Materialize workload-family placements, route tables or equivalent, trust-boundary controls, and egress allowlists as code plus published manifests.
4. Publish the local-development or CI topology emulator so simulator-backed flows and non-production smoke tests exercise the same network law.
5. Integrate static validation that fails when a workload crosses an undeclared trust boundary, receives undeclared browser ingress, or has undeclared outbound reachability.
6. Build the trust atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write smoke tests that prove traffic can flow only across allowed boundaries and that all other flows fail closed.

Mandatory gap closures you must perform:
- Close the “workload families exist only in prose” gap by provisioning them as typed runtime artifacts and concrete network isolation.
- Close the “trust zones and allowlists live in folklore” gap by publishing `TrustZoneBoundary` and egress manifests from code.
- Close the “one generic gateway can silently overreach” gap by enforcing boundary and downstream-family declarations.
- Close the “browser can drift closer to adapters or data stores” gap by making only published gateway surfaces browser-facing.
- Close the “degraded dependencies can spill across planes” gap by containing egress and trust impact per declared workload family.
- Close the “local development topology diverges from cloud topology” gap by providing local emulation bound to the same manifest concepts.

Industry-best-practice requirements:
- least-privilege networking and service identity
- deny-by-default outbound policy
- reproducible infrastructure-as-code with environment parameterization
- immutable or versioned manifest publication after apply
- no manual security-group or firewall drift accepted as truth
- smoke tests and policy-as-code validation in CI

Non-negotiable rules:
- `public_edge` and `shell_delivery` may serve browser assets, but only a published gateway may bridge browser traffic into internal compute planes.
- No browser may call GP-system, telephony, messaging, pharmacy, MESH, workflow, or audit services directly.
- All service-to-service traffic must use workload identity and mutual-auth-ready posture.
- Egress must be allowlisted per workload family.
- Data workloads are never browser-addressable.

Mandatory UI and interaction specification for `84_runtime_network_trust_atlas.html`:
Name the surface mode `Runtime_Network_Trust_Atlas`.

Tone:
- premium runtime architecture atlas
- minimalist, architectural, and quietly authoritative
- should feel like a high-end cloud control diagram brought to life, not a generic infra dashboard

Branding:
- restrained Vecells wordmark plus inline SVG `NT` monogram
- one multi-plane topology diagram
- one trust-boundary matrix
- one egress allowlist flow map

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- edge accent `#2563EB`
- command accent `#0EA5A4`
- integration accent `#7C3AED`
- data accent `#D97706`
- blocked accent `#C24141`
- trusted accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for family IDs, trust-zone refs, allowlist IDs, and service identities

Layout:
- sticky masthead with active environments, stale manifests, blocked boundaries, and allowlist drift alerts
- left rail (`324px`) with environment filters, workload-family filters, boundary-state filters, and egress-scope filters
- central canvas with:
  - multi-plane topology diagram
  - trust-boundary matrix
  - egress flow map
- right inspector (`420px`) for selected workload family, boundary, or allowlist entry
- lower grid with one manifest table and one boundary / egress table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by environment, workload family, trust-boundary state, and egress scope
- selecting a family or boundary row must synchronize topology diagram, boundary matrix, flow map, inspector, and tables
- keyboard navigation across all filters and tables
- deterministic `data-testid` markers for topology diagram, boundary matrix, egress flow map, manifest table, boundary table, and inspector
- every diagram must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification

Definition of done:
- The repository has infrastructure-as-code and manifests for the core runtime network and private egress controls.
- Workload families, trust boundaries, and allowlists are explicit and testable.
- Local development and non-production environments can exercise the same topology law used for cloud deployment.
- Later runtime tasks can layer object storage, event bus, caches, secrets, and gateways onto this foundation without redefining network trust semantics.
```
