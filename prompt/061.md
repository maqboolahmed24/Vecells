# Prompt 061 For 061_seq_phase0_open_parallel_foundation_tracks_gate

Use the shared operating contract first:
[shared_operating_contract_056_to_065.md](/Users/test/Code/V/prompt/shared_operating_contract_056_to_065.md)

```text
Execute roadmap task 061_seq_phase0_open_parallel_foundation_tracks_gate.md.

Inherit the Shared_operating_contract_for_prompts_056_to_065 and consume the validated outputs from tasks `001-060`.

Mission:
Evaluate whether the Phase 0 parallel foundation block may open, publish the exact shard map for that parallel block, and freeze the cross-track seam rules that let tasks `062+` proceed without forking truth.

This task is a gate, not an automatic green light. It must only mark tracks eligible if the sequential foundation work is complete enough that parallel implementation can proceed safely on published shared contracts.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `0A. Delivery skeleton and repository architecture`
  - `0B. Canonical domain kernel and state machine`
  - the statement that Phase 0 runs through hard gates
  - the canonical governors and invariants that later tracks depend on
- `phase-cards.md`, especially:
  - the Phase 0 card
  - the list of Phase 0 parallel tracks across backend, runtime, frontend, and assurance
  - the rule that simulators and seed data must exist before shell and integration tracks rely on them
- `platform-runtime-and-release-blueprint.md`, especially the requirement that runtime publication, release verification, and recovery proof remain exact before live or mutable posture appears
- outputs from tasks `020`, `041-060`
- `forensic-audit-findings.md`, especially the findings that corrected foundational control-plane omissions that must not be deferred into parallel feature work

Primary outcome:
One evidence-driven gate pack proving:
- whether the parallel Phase 0 block may open now
- which specific tracks are eligible
- which shared package seams and stub contracts are required for safe parallelism
- which tracks remain blocked and why

Deliverables to create:
1. `docs/programme/61_parallel_foundation_tracks_gate.md`
2. `docs/programme/61_parallel_track_eligibility_matrix.md`
3. `docs/programme/61_parallel_dependency_shard_map.md`
4. `docs/programme/61_parallel_foundation_gate_cockpit.html`
5. `data/analysis/parallel_foundation_track_groups.json`
6. `data/analysis/parallel_track_eligibility.csv`
7. `data/analysis/parallel_track_prerequisite_edges.csv`
8. `data/analysis/parallel_track_shared_seams.json`
9. `data/analysis/parallel_foundation_gate_verdict.json`
10. `tools/analysis/validate_parallel_foundation_gate.(py|ts)`
11. `tests/playwright/parallel-foundation-gate-cockpit.spec.(ts|js)`

Each track row must capture at minimum:
- `trackTaskId`
- `trackTitle`
- `trackGroup = backend_kernel | runtime_control_plane | frontend_shells | assurance | other`
- `claimableMode = eligible | conditional | blocked`
- `requiredSeqTaskRefs[]`
- `requiredSharedPackageRefs[]`
- `requiredSimulatorRefs[]`
- `requiredSchemaRefs[]`
- `forbiddenSiblingCouplings[]`
- `parallelInterfaceStubRefs[]`
- `blockingReasonRefs[]`
- `notes`

Execution steps:
1. Start from the Phase 0 sequential deliverables and identify the first contiguous parallel block that this gate is opening. For this task, treat at minimum tasks `062-126` as the candidate Phase 0 parallel block unless the already-published repo prompts narrowed it with source-backed rationale.
2. Partition the parallel block into implementation groups, at minimum:
   - backend kernel and authoritative domain services
   - runtime / infrastructure publication and observability
   - frontend shell seeding and route scaffolding
   - assurance / safety / privacy parallel work
3. Publish the eligibility rules. A track may be `eligible` only if its required sequential artifacts are already frozen and machine-readable.
4. Publish the shared seam inventory that parallel tracks must use instead of reaching into sibling internals. At minimum include seams for:
   - canonical aggregate IDs and event envelope types
   - schema package locations
   - shared contract package homes
   - simulator fixture registries
   - runtime publication tuple references
   - design / accessibility / automation contract refs
5. Publish the blocking rules. A track must be `blocked` if it would need to:
   - infer unresolved seq-task law
   - invent a sibling-owned schema
   - bypass a required simulator or runtime binding
   - depend on unpublished route / settlement / recovery semantics
6. Produce the verdict. `open` is legal only when the control-plane prerequisites are complete enough for safe parallelism; otherwise produce `conditional` or `withheld` with exact blockers.
7. Build the cockpit and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that:
   - every eligible track has explicit shared seams and prerequisites
   - no blocked track is marked claimable
   - no track depends on live provider onboarding instead of the simulator-first strategy
   - no frontend track can open without the required runtime/design/accessibility tuple work frozen
   - no backend track can open without the required aggregate, mutation, adapter, or recovery law frozen

Mandatory gap closures you must perform:
- Close the “parallel block starts because the roadmap says so” gap by making the verdict evidence-driven.
- Close the “parallel tracks can infer sibling contracts later” gap by publishing the shared seam inventory now.
- Close the “frontend or backend tracks can proceed without simulator and recovery law” gap by making those prerequisites explicit.
- Close the “parallel work can silently fork schema ownership” gap by requiring package-home and schema refs per track.
- Close the “gate can hide blocked tracks under a general green verdict” gap by publishing per-track eligibility.

Industry-best-practice requirements:
- explicit shard ownership and coupling boundaries
- prerequisite graphs rather than prose-only gating
- fail-closed eligibility for tracks with unresolved foundational dependencies
- stable shared contract seams for concurrent implementation
- simulator-first and schema-first parallel execution

Non-negotiable rules:
- Parallel work may open only through an evidence-backed verdict.
- No track is eligible if it would need to infer or redefine sequentially frozen law.
- No track may rely on sibling private internals instead of published shared seams.
- Simulator-backed dependencies remain mandatory for Phase 0 parallel execution.
- The gate cockpit may not show optimistic green posture while per-track blockers remain unresolved.

Mandatory UI and interaction specification for `61_parallel_foundation_gate_cockpit.html`:
Name the surface mode `Parallel_Foundation_Gate`.

Tone:
- minimalist premium programme-control surface
- should feel like a formal readiness gate with crisp dependency visibility
- calm, exact, and low-drama

Branding:
- restrained Vecells wordmark plus an inline SVG `PF` monogram
- include one elegant track-shard map and one seam-dependency diagram
- include a compact verdict banner and blocker rail

Palette:
- canvas `#F6F8FB`
- rail `#EEF2F7`
- panel `#FFFFFF`
- inset `#F3F5FA`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- eligible accent `#0F9D58`
- conditional accent `#C98900`
- blocked accent `#C24141`
- seam accent `#7C3AED`

Typography:
- system sans stack only
- mono for task IDs, seam IDs, and hashes

Layout:
- sticky masthead with:
  - eligible-track count
  - conditional-track count
  - blocked-track count
  - seam count
- left rail (`300px`):
  - track-group filters
  - eligibility filters
- center canvas:
  - track-shard map
  - seam-dependency diagram
  - curated track cards
- right inspector (`396px`):
  - selected track
  - prerequisite list
  - shared seam list
  - blockers / conditions
- lower region:
  - eligibility matrix
  - prerequisite-edge table
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `620px`
- primary controls `44px`
- track cards `min-height 160px`
- every diagram requires adjacent table/list parity

Motion:
- `120ms` hover/focus
- `180ms` filter / card selection
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by track group and eligibility state
- selecting a track card must synchronize diagram, matrix, and inspector
- keyboard navigation across filters, cards, and table rows
- deterministic `data-testid` markers for shard map, seam diagram, inspector, verdict banner, and defect strip
- track eligibility must remain understandable without color alone

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- group and eligibility filtering
- track-card selection
- diagram / matrix / inspector synchronization
- keyboard navigation
- responsive layout
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has one evidence-backed gate for opening the Phase 0 parallel foundation block.
- Each parallel track now has explicit prerequisites, seams, and blockers.
- Later parallel implementation can proceed without silently forking foundational truth.
```
