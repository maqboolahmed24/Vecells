# Prompt 094 For 094_par_phase0_track_runtime_implement_runtime_publication_bundle_and_release_publication_parity_records

Use the shared operating contract first:
[shared_operating_contract_086_to_095.md](/Users/test/Code/V/prompt/shared_operating_contract_086_to_095.md)

```text
Execute roadmap task 094_par_phase0_track_runtime_implement_runtime_publication_bundle_and_release_publication_parity_records.md.

Inherit the Shared_operating_contract_for_prompts_086_to_095 and consume the validated outputs from tasks `001-093`.

Mission:
Implement the authoritative runtime publication system for Vecells:
- `RuntimePublicationBundle`
- `ReleasePublicationParityRecord`
- runtime bundle generation, validation, and publication-state refusal logic
- parity checks between route contracts, gateway surfaces, frontend manifests, design bundles, projection contracts, cache policy refs, live channels, topology, and build provenance

This is real runtime-control code. Produce generators, validators, persistence, APIs or CLIs, and tests. Do not leave publication authority as a deploy checklist or human reasoning exercise.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### RuntimePublicationBundle`
  - `### ReleasePublicationParityRecord`
  - `### BuildProvenanceRecord`
  - `### GatewayBffSurface`
  - `### FrontendContractManifest`
  - verification-ladder sections that bind route, frontend, design, projection, live-channel, cache, provenance, recovery, and continuity artifacts to one tuple
- `phase-0-the-foundation-protocol.md`, especially the rules that route intent, command settlement, release freeze, channel freeze, and assurance trust are shared foundational controls
- `platform-frontend-blueprint.md` and design/publication blueprints where surface publication, authority tuples, and same-shell continuity depend on exact manifest and runtime parity
- outputs from tasks `046` through `053`, `065`, `084`, `090`, `091`, `092`, and `093`
- `forensic-audit-findings.md`, especially Findings `91`, `95`, `96`, `103`, `104`, `105`, `112`, and `113`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It may implement bundle generation, parity recording, and fail-closed publication checks now, but it must not steal ownership of later live-wave observation and widening policy. Publish bounded seams and record `FOLLOW_ON_DEPENDENCY_*` where necessary.

Mock_now_execution:
Build a fully working publication system now for local and non-production environments.
- generate real `RuntimePublicationBundle` and `ReleasePublicationParityRecord` artifacts from current manifests, runtime topology, gateway surfaces, provenance records, and design bundles
- use synthetic or preview-safe route families and runtime tuples where live providers are not yet present
- fail closed in local and preview environments when bundle members drift, are missing, or are not provenance-verified
The mock-now path is the real publication path for MVP and must be good enough to drive preview, shell work, and non-production release rehearsals.

Actual_production_strategy_later:
When production release promotion becomes active, it must preserve the same bundle shape, parity rules, provenance checks, tuple binding, and refusal posture. Production hardening may add stronger approval workflows, artifact registries, or environment promotion guards, but it must not allow routes, manifests, or gateway surfaces to be treated as live outside the authoritative runtime bundle.

Primary outcome:
The repository gains a working publication-authority layer that can prove what runtime tuple is actually live or publishable for each audience and route family.

Implementation deliverables to create:
1. code to generate, persist, validate, and publish `RuntimePublicationBundle` artifacts from current manifests, topology, gateway surfaces, provenance, and contract sets
2. code to generate and validate `ReleasePublicationParityRecord` artifacts
3. bundle and parity validators that fail closed on missing, drifted, stale, unverified, or contradictory tuple members
4. machine-readable publication APIs, CLIs, or service endpoints for inspection and validation
5. local and CI publication rehearsal flows integrated with preview and pipeline work
6. `docs/architecture/94_runtime_publication_bundle_design.md`
7. `docs/architecture/94_release_publication_parity_rules.md`
8. `docs/architecture/94_runtime_publication_bundle_console.html`
9. `data/analysis/runtime_publication_bundle_schema.json`
10. `data/analysis/release_publication_parity_schema.json`
11. `data/analysis/publication_tuple_dependency_matrix.csv`
12. `tools/analysis/validate_runtime_publication_bundle.(py|ts)`
13. publication tests, drift tests, parity tests, provenance-gate tests, and fail-closed smoke tests
14. `tests/playwright/runtime-publication-bundle-console.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- generate one `RuntimePublicationBundle` from current topology, gateway, manifest, design, projection, live-channel, cache, provenance, recovery, and publication members
- generate one `ReleasePublicationParityRecord` proving route, surface, and artifact tuple parity for the current candidate
- validate that bundle members and digests are current, coherent, and provenance-verified before a route family or audience surface is considered publishable
- expose machine-readable publication state and refusal reasons
- prevent route families, manifests, or gateways from becoming live or writable when the bundle or parity record is absent, stale, drifted, quarantined, or blocked
- support preview and non-production rehearsal without weakening publication law

Execution steps:
1. Re-read the runtime publication and parity sections plus the design and frontend publication strategy outputs from earlier tasks.
2. Honor the topology, design publication, route manifest, gateway, provenance, and observability baselines from earlier tasks; if prerequisites are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
3. Implement bundle generation and persistence from the current authoritative manifests and tuples.
4. Implement parity-record generation and validation from the same tuple members.
5. Implement fail-closed validation and refusal APIs or commands.
6. Build the console and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write smoke tests proving a drift in topology, gateway surface, frontend contract manifest, design bundle, provenance state, or recovery posture blocks publication.

Mandatory gap closures you must perform:
- Close the “compiled route code means published” gap by requiring inclusion in an authoritative runtime bundle.
- Close the “parity is a human checklist” gap by generating machine-readable parity records.
- Close the “topology, design, and gateway are validated separately” gap by bundling them into one publishability tuple.
- Close the “preview and non-production can skip publication law” gap by using the same fail-closed publication system there.
- Close the “publication drift is visible only in dashboards” gap by publishing machine-readable refusal reasons and states.

Industry-best-practice requirements:
- deterministic bundle generation from authoritative sources
- fail-closed validation on missing or drifted tuple members
- machine-readable schemas and validators under version control
- explicit publication and refusal states per audience surface and route family
- provenance-verified publication only
- no runtime authority inferred from deployment success alone

Non-negotiable rules:
- `RuntimePublicationBundle` and `ReleasePublicationParityRecord` are the only publication authority.
- Missing, stale, drifted, or quarantined bundle members block publication.
- Routes, manifests, and gateway surfaces may not be considered live outside the bundle.
- Publication state must be machine-readable.
- Preview and non-production environments obey the same bundle law.

Mandatory UI and interaction specification for `94_runtime_publication_bundle_console.html`:
Name the surface mode `Runtime_Publication_Bundle_Console`.

Tone:
- premium publication-authority console
- minimalist, exact, and executive-grade
- should feel like a luxury release-governance surface, not a generic JSON inspector

Branding:
- restrained Vecells wordmark plus inline SVG `RB` monogram
- one publication-tuple diagram
- one parity-status matrix
- one drift and refusal timeline strip

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- bundle accent `#2563EB`
- parity accent `#0EA5A4`
- drift accent `#D97706`
- blocked accent `#C24141`
- verified accent `#059669`
- quarantine accent `#7C3AED`

Typography:
- system sans stack only
- monospaced digest styling for bundle IDs, parity IDs, tuple hashes, and manifest digests

Layout:
- sticky masthead with active bundle, blocked surfaces, drift alerts, and provenance-state warnings
- left rail (`324px`) with audience filters, route-family filters, publication-state filters, and drift filters
- central canvas with:
  - publication-tuple diagram
  - parity-status matrix
  - drift / refusal timeline strip
- right inspector (`420px`) for selected bundle member, route family, or refusal state
- lower grid with one bundle-member table and one parity / refusal table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by audience, route family, publication state, drift state, and provenance state
- selecting a row must synchronize diagrams, timeline, inspector, and tables
- keyboard navigation across filters and tables
- deterministic `data-testid` markers for tuple diagram, parity matrix, refusal timeline, member table, refusal table, and inspector
- every visual must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification
- verification that blocked, drifted, and verified states remain visually and semantically distinct

Definition of done:
- The repository has a working runtime publication bundle and parity-record system.
- Publication and refusal states are machine-readable and fail closed.
- Preview and non-production publication rehearsals use the same authoritative tuple law.
- Later release-watch and canary work can consume this publication layer instead of reconstructing runtime truth ad hoc.
```
