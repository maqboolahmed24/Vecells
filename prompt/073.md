# Prompt 073 For 073_par_phase0_track_backend_implement_queue_rank_plan_snapshot_and_assignment_suggestion_models

Use the shared operating contract first:
[shared_operating_contract_066_to_075.md](/Users/test/Code/V/prompt/shared_operating_contract_066_to_075.md)

```text
Execute roadmap task 073_par_phase0_track_backend_implement_queue_rank_plan_snapshot_and_assignment_suggestion_models.md.

Inherit the Shared_operating_contract_for_prompts_066_to_075 and consume the validated outputs from tasks `001-072`.

Mission:
Implement the deterministic queue-ordering model substrate required by later staff workspace and ranking-coordinator work:
- `QueueRankPlan`
- `QueueRankSnapshot`
- `QueueRankEntry`
- `QueueAssignmentSuggestionSnapshot`
plus the bounded explanation, hashing, and validation utilities needed so later queue and next-task consumers cannot recompute truth ad hoc.

This task is model-plus-core-ranking-contract implementation. It is not yet the full coordinator from task `081`, but it must encode the ranking law and replay invariants now.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.11A QueueRankPlan`
  - `#### 1.11B QueueRankSnapshot`
  - `#### 1.11C QueueRankEntry`
  - `#### 1.11D QueueAssignmentSuggestionSnapshot`
  - the queue and queue-change invariants in the canonical object section
- `phase-3-the-human-checkpoint.md`, especially `## 3B. Deterministic queue engine, assignment, and fairness controls`
- `staff-workspace-interface-architecture.md`, especially the rules that source queue snapshot refs must match across launch, prefetch, completion, and continuity evidence
- `operations-console-frontend-blueprint.md`, where boards may not resort stale rows client-side
- `forensic-audit-findings.md`, especially Findings `14`, `16`, `92`, and `93`
- outputs from tasks `001-007`, `041-045`, `056-058`, `061`, `071-072`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Publish bounded rank-plan and rank-snapshot interfaces for sibling tracks, but do not wait for the full ranking coordinator.

Mock_now_execution:
Build a seeded synthetic queue simulator now. It must generate deterministic ranked queues for:
- normal load with mixed urgency and fairness needs
- overload-critical posture
- duplicate-review blockers
- active reachability risk and safety holds
- reviewer-suggestion windows over the top `M` tasks
- mixed task archetypes with expected service-time priors
The simulator must produce the same snapshots, row-order hashes, and suggestion snapshots that production will later require.

Actual_production_strategy_later:
When live workloads, staffing levels, and real task facts arrive, they must flow into the same plan and snapshot model. Real telemetry may change priors, fairness parameters, or reviewer-fit weights through versioned plan updates, but it may not move canonical queue order into browser-local sort or reviewer-local heuristics.

Primary outcome:
The repository gains replayable, explainable queue-ranking and suggestion models that later workspaces, operations boards, and coordinators can consume as authoritative.

Implementation deliverables to create:
1. domain and persistence code for `QueueRankPlan`, `QueueRankSnapshot`, `QueueRankEntry`, and `QueueAssignmentSuggestionSnapshot`
2. hashing and explanation utilities for `rowOrderHash`, canonical tie-break keys, and explanation payload normalization
3. bounded ranking-contract helpers that validate the plan structure, factor sets, and suggestion isolation rules
4. `docs/architecture/73_queue_rank_model_design.md`
5. `docs/architecture/73_deterministic_queue_ranking_rules.md`
6. `docs/architecture/73_queue_rank_explanation_studio.html`
7. `data/analysis/queue_rank_plan_manifest.json`
8. `data/analysis/queue_rank_entry_factor_matrix.csv`
9. `data/analysis/assignment_suggestion_casebook.json`
10. `tools/analysis/validate_queue_rank_models.(py|ts)`
11. unit tests, simulation tests, repository tests, and integration tests
12. `tests/playwright/queue-rank-explanation-studio.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- persist one versioned `QueueRankPlan` with eligibility, lexicographic precedence, within-tier factors, fairness merge, overload guard, and explanation schema refs
- persist one replayable `QueueRankSnapshot` for one fact cut and one plan
- persist one `QueueRankEntry` per ranked task with exact normalized factors, tiers, fairness credits, and tie-break data
- persist `QueueAssignmentSuggestionSnapshot` as a derived reviewer-suggestion view over an already-committed rank snapshot
- guarantee that suggestion logic cannot rewrite canonical task ordinals, row order, or explanation payloads from the source snapshot

Execution steps:
1. Implement `QueueRankPlan` exactly from the canonical fields and the Phase 3 ranking law, including plan hash and explanation schema references.
2. Implement `QueueRankSnapshot` and `QueueRankEntry` so the same fact cut plus the same plan yields the same row order hash and explanation payloads.
3. Implement `QueueAssignmentSuggestionSnapshot` as a separate derived object tied to an existing rank snapshot.
4. Implement validators that fail when:
   - queue rows or next-task candidates are published without a committed rank snapshot
   - a snapshot uses mixed-freshness facts or missing trust inputs
   - suggestion rows mutate canonical ordinals or explanation payloads
   - overload-critical posture fails to suppress inappropriate fairness promises
5. Implement simulation fixtures for normal load, overload, duplicate review, safety holds, and reviewer-fit scenarios.
6. Build the studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.

Mandatory gap closures you must perform:
- Close the “queue order is one unconstrained weighted sum” gap by encoding plan fields that support lexicographic precedence, normalized within-tier urgency, and deterministic fair merge.
- Close the “queue rows and suggestions recompute order ad hoc” gap by making snapshots and entries authoritative.
- Close the “reviewer-specific skill changes canonical queue order” gap by isolating `QueueAssignmentSuggestionSnapshot` downstream of `QueueRankSnapshot`.
- Close the “operations or workspace boards can silently mix snapshot generations” gap by binding all consumers to committed snapshot refs and row-order hashes.
- Close the “fairness claims survive overload dishonestly” gap by modeling overload guard posture explicitly.

Industry-best-practice requirements:
- deterministic ranking hashes and explanation payloads
- replayable simulations and fixed random seeds
- explicit separation between canonical order and reviewer suggestion
- no browser-local sort or reviewer-local ranking authority
- stable tie-break policy and monotone plan versioning

Non-negotiable rules:
- `QueueRankPlan` is the only versioned source of canonical queue ordering.
- `QueueRankSnapshot` is the replayable canonical ordering cut.
- `QueueRankEntry` holds the authoritative explanation for one ordinal.
- `QueueAssignmentSuggestionSnapshot` is derived and may not rewrite canonical order.
- Mixed-snapshot queue truth is forbidden.

Mandatory UI and interaction specification for `73_queue_rank_explanation_studio.html`:
Name the surface mode `Queue_Rank_Explanation_Studio`.

Tone:
- premium internal triage and fairness studio
- clear, confident, and mathematically honest
- should feel more like a carefully designed navigation instrument than a list dashboard

Branding:
- restrained Vecells wordmark plus inline SVG `QR` monogram
- one rank ladder diagram
- one fairness-merge ribbon
- one suggestion-window panel

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F8`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- rank accent `#3559E6`
- fairness accent `#0EA5A4`
- suggestion accent `#7C3AED`
- overload accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for plan hashes, snapshot refs, and tie-break keys

Layout:
- masthead with active plans, snapshots, overloaded queues, and suggestion windows
- left rail (`304px`) for queue family, overload state, and tier filters
- center canvas with rank ladder, fairness ribbon, and explanation cards
- right inspector (`408px`) for selected plan / snapshot / entry details
- lower region with factor table and suggestion table

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- rank ladder min height `260px`
- cards min height `172px`
- controls `44px`

Motion:
- `120ms` hover/focus
- `180ms` filter and selection changes
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by queue family, overload state, and lexicographic tier
- selecting a row or card must synchronize ladder, fairness ribbon, inspector, and factor table
- keyboard navigation across filters, cards, and tables
- deterministic `data-testid` markers for ladder, fairness ribbon, inspector, factor table, and suggestion table
- every diagram needs adjacent textual parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- queue-family and tier filtering
- row / card selection synchronization
- diagram / table parity
- keyboard navigation
- reduced motion
- responsive layout
- accessibility smoke checks

Definition of done:
- The repository has real queue-rank and suggestion models with deterministic hashes and validators.
- Later staff workspace and operations surfaces can consume authoritative ranking truth.
- Reviewer suggestions remain downstream of canonical order instead of corrupting it.
```
