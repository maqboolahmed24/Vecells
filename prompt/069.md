# Prompt 069 For 069_par_phase0_track_backend_implement_contact_route_snapshot_and_reachability_assessment_models

Use the shared operating contract first:
[shared_operating_contract_066_to_075.md](/Users/test/Code/V/prompt/shared_operating_contract_066_to_075.md)

```text
Execute roadmap task 069_par_phase0_track_backend_implement_contact_route_snapshot_and_reachability_assessment_models.md.

Inherit the Shared_operating_contract_for_prompts_066_to_075 and consume the validated outputs from tasks `001-068`.

Mission:
Implement the canonical contact-route and reachability authority layer:
- `ContactRouteSnapshot`
- `ReachabilityObservation`
- `ReachabilityAssessmentRecord`
- `ReachabilityDependency`
- `ContactRouteRepairJourney`
- `ContactRouteVerificationCheckpoint`
plus the bounded calculators and validators required so later patient, staff, callback, booking, hub, and pharmacy flows all consume the same dependency truth.

This task is model plus calculation implementation. It must make route trustworthiness, reachability risk, and same-shell repair first-class. It must not leave delivery truth buried in channel-specific logs or mutable preference rows.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.8D ContactRouteSnapshot`
  - `#### 1.8E ReachabilityObservation`
  - `#### 1.8F ReachabilityAssessmentRecord`
  - `#### 1.9 ReachabilityDependency`
  - `#### 1.9A ContactRouteRepairJourney`
  - `#### 1.9B ContactRouteVerificationCheckpoint`
  - the universal safety-preemption and reachability-risk algorithm
- `patient-account-and-communications-blueprint.md` where patient actionability must degrade to contact-route repair or support recovery when dependencies are blocked
- `callback-and-clinician-messaging-loop.md`, `phase-5-the-network-horizon.md`, and `phase-6-the-pharmacy-loop.md` where callback, reminder, alternative-offer, pharmacy-contact, and urgent-return truth depends on current reachability assessment rather than send success
- `forensic-audit-findings.md`, especially the findings that patient or staff surfaces must not imply success while contact-route dependencies remain unresolved
- outputs from tasks `001-007`, `019`, `041-045`, `056-057`, `061-068`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Publish minimal shared calculator or projection stubs only where necessary. Do not wait for later callback, hub, or pharmacy services.

Mock_now_execution:
Build a local delivery and verification simulator now. It must emit realistic reachability observations for:
- SMS accepted, delivered, bounced, expired, or opt-out
- voice no-answer, invalid-route, manual confirmed reachable/unreachable
- email accepted, bounced, disputed, and preference changes
- OTP verification success/failure/expiry for route repair
The simulator must feed the same observation and assessment models production will use later.

Actual_production_strategy_later:
When live SMS, email, telephony, callback, reminder, or pharmacy-notification providers arrive, they must publish observations into the same canonical reachability objects. Provider-specific delivery receipts or webhook quirks may change observation sources and authority weights, but they may not redefine reachability truth, repair requirements, or same-shell recovery semantics.

Primary outcome:
The repository gains one authoritative, append-only reachability model that every later domain can consume for delivery risk, blocked actions, repair journeys, and bounded patient or operator recovery.

Implementation deliverables to create:
1. domain and persistence code for the six canonical contact-route and reachability objects listed above
2. calculation utilities that derive `ReachabilityAssessmentRecord` from current snapshot plus observations
3. validators enforcing snapshot supersession, route-authority rules, dependency blocking, and repair-journey reopening conditions
4. `docs/architecture/69_contact_route_and_reachability_design.md`
5. `docs/architecture/69_reachability_assessment_rules.md`
6. `docs/architecture/69_reachability_truth_studio.html`
7. `data/analysis/contact_route_snapshot_manifest.json`
8. `data/analysis/reachability_dependency_matrix.csv`
9. `data/analysis/reachability_assessment_casebook.json`
10. `tools/analysis/validate_reachability_models.(py|ts)`
11. unit tests, repository tests, simulation tests, and integration tests
12. `tests/playwright/reachability-truth-studio.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- freeze each contact-route version as a new `ContactRouteSnapshot` rather than mutating older route truth in place
- append `ReachabilityObservation` rows without collapsing them into calm health prematurely
- compute current `ReachabilityAssessmentRecord` as the only current authority for route freshness, deliverability, and repair state
- bind patient- or staff-facing promises to explicit `ReachabilityDependency` rows with blocked action scopes and deadlines
- open and advance `ContactRouteRepairJourney` only in the same shell / continuity context, not through detached settings pages
- require `ContactRouteVerificationCheckpoint` and a resulting clear assessment before blocked actions can reopen

Execution steps:
1. Implement immutable `ContactRouteSnapshot` versioning, including verification, demographic freshness, preference freshness, and source-authority class.
2. Implement append-only `ReachabilityObservation` with positive, negative, and ambiguous authority weights.
3. Implement the `ReachabilityAssessmentRecord` calculator so it derives route authority state, deliverability, delivery risk, false-negative guard state, and resulting repair state from the current snapshot plus considered observations.
4. Implement `ReachabilityDependency` as the governing object that upgrades a contact issue into a domain blocker with blocked action scopes, same-shell return references, and failure effect.
5. Implement `ContactRouteRepairJourney` and `ContactRouteVerificationCheckpoint` so route edits do not reopen blocked actions until verification, rebind, and resulting assessment all succeed.
6. Implement validators that fail when:
   - mutable profile state is used as current reachability authority without a snapshot
   - send success or transport ack is treated as reachability proof
   - a blocked dependency leaves patient or staff action scopes live
   - contact edits reopen actions without a new snapshot, verification checkpoint, and clear resulting assessment
7. Build the studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.

Mandatory gap closures you must perform:
- Close the “current contact route truth is inferred from mutable profile rows” gap by making snapshots and assessments authoritative.
- Close the “delivery acceptance proves reachability” gap by distinguishing observations from current assessment.
- Close the “callback, reminder, and pharmacy flows each invent their own delivery truth” gap by centralizing dependency and assessment objects.
- Close the “repair happens in detached settings without continuity” gap by implementing same-shell `ContactRouteRepairJourney`.
- Close the “successful verification can toggle an old route healthy again” gap by minting a new route snapshot and resulting assessment after verification.

Industry-best-practice requirements:
- append-only observation and assessment history
- immutable route snapshots with explicit supersession
- deterministic assessment calculators and reason codes
- replay-safe delivery simulation fixtures
- clear separation between route existence, route trust, and domain blocking
- reusable dependency model across domains

Non-negotiable rules:
- `ContactRouteSnapshot` is the frozen route authority for one version.
- `ReachabilityAssessmentRecord` alone converts route + observations into current dependency posture.
- `ReachabilityDependency` is the domain blocker when promises depend on current reachability.
- Repair must preserve same-shell continuity and return context.
- Transport success, outbound enqueue, or mutable demographics may not stand in for authoritative reachability truth.

Mandatory UI and interaction specification for `69_reachability_truth_studio.html`:
Name the surface mode `Reachability_Truth_Studio`.

Tone:
- premium internal trust-and-repair instrument
- calm, legible, and lightly clinical without looking alarmist
- should feel like a precise promise-protection board

Branding:
- restrained Vecells wordmark plus inline SVG `RT` monogram
- one dependency constellation diagram
- one route-snapshot stack
- one repair-journey spotlight ribbon

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF3F8`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- clear accent `#0F9D58`
- at-risk accent `#C98900`
- repair accent `#3559E6`
- disputed accent `#7C3AED`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for snapshot refs, checkpoint refs, and dependency epochs

Layout:
- masthead with active dependencies, blocked promises, repair journeys, and clear routes
- left rail (`308px`) for domain, route-kind, assessment-state, and repair-state filters
- center canvas with dependency constellation, snapshot stack, and assessment cards
- right inspector (`404px`) for selected dependency / snapshot / repair journey details
- lower region with observation table and verification checkpoint table

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- constellation min height `260px`
- cards min height `170px`
- controls `44px`

Motion:
- `120ms` hover/focus
- `180ms` filter and card changes
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by domain, route kind, and assessment or repair state
- selecting a dependency must synchronize constellation, snapshot stack, inspector, and observation table
- keyboard navigation across cards, tables, and filters
- deterministic `data-testid` markers for constellation, snapshot stack, inspector, observation table, and checkpoint table
- every chart/diagram needs adjacent text parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- domain and state filtering
- dependency selection synchronization
- diagram / table parity
- keyboard navigation
- reduced motion
- responsive layout
- accessibility smoke checks

Definition of done:
- The repository has real contact-route and reachability models with validators.
- Every later callback, reminder, pharmacy-contact, and repair path can consume the same dependency truth.
- Blocked promises degrade through same-shell repair rather than detached or optimistic flows.
```
