# Prompt 088 For 088_par_phase0_track_runtime_provision_cache_and_live_update_transport_baseline

Use the shared operating contract first:
[shared_operating_contract_086_to_095.md](/Users/test/Code/V/prompt/shared_operating_contract_086_to_095.md)

```text
Execute roadmap task 088_par_phase0_track_runtime_provision_cache_and_live_update_transport_baseline.md.

Inherit the Shared_operating_contract_for_prompts_086_to_095 and consume the validated outputs from tasks `001-087`.

Mission:
Provision the baseline runtime substrate for cache-backed read continuity and live-update transport across Vecells audience surfaces.

This task must create the shared technical foundation for:
- server-side cache infrastructure and policy-ready cache namespaces
- live-update transport infrastructure such as websocket, SSE, or equivalent gateway-safe channels
- connection registries, replay buffers, heartbeat and reconnect hooks, and route-family-ready subscription scaffolds
- machine-readable transport and cache topology aligned to audience surfaces and workload boundaries

This task provisions the substrate only. It must not silently finalize route-family-specific `ClientCachePolicy`, `LiveUpdateChannelContract`, downgrade posture, or recovery semantics that the roadmap reserves for task `096`, except where a minimal bounded shared seam is strictly required.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### LiveUpdateChannelContract`
  - `### ClientCachePolicy`
  - runtime rules that stale cache or transport health may not imply writable posture
  - any recovery, downgrade, and projection-readiness language that constrains live-update honesty
- `platform-frontend-blueprint.md`, especially the same-shell continuity rules, freshness honesty, selected-anchor preservation, and recovery posture expectations
- `patient-portal-experience-architecture-blueprint.md`, `patient-account-and-communications-blueprint.md`, and `staff-operations-and-support-blueprint.md` where patient or staff shells depend on freshness, visibility, and replay-safe live updates
- outputs from tasks `004`, `046`, `047`, `050`, `065`, `084`, and `087`
- `forensic-audit-findings.md`, especially Findings `86`, `87`, `92`, `96`, `97`, `99`, `100`, `101`, and `102`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must publish extension points for later route-level cache policy, live channel semantics, recovery posture, and frontend state work without stealing their ownership. If a later task owns policy depth, provision the substrate and emit `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build a high-fidelity local and non-production cache and live-update substrate now.
- provide local and CI-safe cache backends and live transport emulators or dev-grade services
- keep the same channel identifiers, reconnect semantics, heartbeat cadence, subscription handshake shape, cache namespace model, and invalidation hooks the live runtime will require
- allow simulator-backed and seeded projections to exercise reconnect, staleness, pause, and replay behavior before real partner traffic exists
The mock-now path must be realistic enough that production cache and transport services are a backend swap, not a semantic rewrite.

Actual_production_strategy_later:
When production-grade cache and live transport services are finalized, they must preserve the same namespace model, connection identity posture, replay hooks, heartbeat rules, downgrade pathways, and route-family binding strategy. Production hardening may add scale-out connection brokers, regional fan-out, or managed cache services, but it must not let caches or live connections become the hidden authority for freshness, mutation readiness, or user reassurance.

Primary outcome:
The repository gains a shared cache and live-transport runtime substrate that future route-family policy work can bind to without improvising connection, replay, or invalidation behavior.

Implementation deliverables to create:
1. infrastructure-as-code for cache services, live-transport services, connection registries, and any required broker or fan-out components
2. machine-readable cache namespace and live-transport topology manifests aligned to audience surfaces and gateway boundaries
3. reusable connection and subscription infrastructure for gateway-safe live channels, including heartbeat, reconnect, replay-window, and downgrade-ready hooks
4. reusable cache substrate for manifest, projection, route-family, and query-result cache classes without finalizing route-level policy semantics
5. local-development and CI bootstrap flows for cache reset, transport restart, replay-window simulation, and stale-mode drills
6. `docs/architecture/88_cache_and_live_transport_design.md`
7. `docs/architecture/88_cache_transport_boundary_and_honesty_rules.md`
8. `docs/architecture/88_live_update_and_cache_atlas.html`
9. `data/analysis/cache_namespace_manifest.json`
10. `data/analysis/live_transport_topology_manifest.json`
11. `data/analysis/cache_transport_boundary_matrix.csv`
12. `tools/analysis/validate_cache_and_live_transport.(py|ts)`
13. infrastructure tests, connection tests, replay-window tests, invalidation tests, and failure-injection smoke tests
14. `tests/playwright/live-update-and-cache-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- provision cache infrastructure with separate namespaces for runtime-manifest, projection-read, route-family, entity-scoped, and transient replay-support classes
- provision live-transport infrastructure that can serve later gateway-bound audience channels without direct browser-to-internal-service reachability
- publish cache and channel topology manifests from code
- provide connection registration, reconnect tokens or equivalents, heartbeat, stale-mode hooks, and bounded replay buffers
- ensure transport or cache degradation can be surfaced honestly and fail closed into later recovery posture instead of silently pretending freshness
- keep cache invalidation and live-update emissions behind declared runtime seams rather than ad hoc handler calls

Execution steps:
1. Re-read the live-update and client-cache sections plus audience continuity blueprints, then derive what substrate can be safely provisioned now without overstepping task `096`.
2. Honor the network, gateway, and broker baselines from earlier tasks; if prerequisites are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
3. Provision cache and transport infrastructure plus machine-readable topology manifests.
4. Implement reusable connection-registry and replay-window primitives that later route-family contracts can bind to.
5. Implement reusable cache namespace helpers and invalidation hooks without finalizing audience-specific policy decisions prematurely.
6. Build the atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write smoke tests proving reconnect, heartbeat loss, stale transport, cache reset, and replay-window exhaustion all fail into explicit degraded infrastructure posture rather than implied healthy state.

Mandatory gap closures you must perform:
- Close the “live update is just websocket plumbing” gap by publishing governed channel topology and replay hooks.
- Close the “cache policy can be inferred from route code” gap by provisioning named cache classes and manifests now.
- Close the “presence or heartbeat implies freshness” gap by preventing transport health from being treated as truth.
- Close the “same-shell continuity can drift across stale projections” gap by preparing explicit stale-mode and reconnect hooks.
- Close the “local development never exercises degraded transport or stale cache” gap by providing drills and reset flows.

Industry-best-practice requirements:
- private transport behind gateway-safe boundaries
- explicit cache namespaces and bounded TTL or invalidation hooks
- deterministic replay-window and reconnect behavior
- no cache or live channel as hidden business-truth authority
- observable connection lifecycle and safe shutdown behavior
- fail-closed posture when manifests or trust inputs are absent

Non-negotiable rules:
- Cache warmth does not imply writable posture.
- Live connection state does not imply fresh or settled truth.
- Browsers may not subscribe directly to internal buses or internal workload families.
- This task may provision substrate, but route-family policy finalization belongs to later tasks unless a minimal shared seam is required.
- Degraded transport and stale cache must remain explicit, not visually flattened into healthy chrome.

Mandatory UI and interaction specification for `88_live_update_and_cache_atlas.html`:
Name the surface mode `Live_Update_And_Cache_Atlas`.

Tone:
- premium real-time continuity atlas
- minimalist, precise, and quietly dynamic
- should feel like a luxury control-room explainer, not a chat-widget admin tool

Branding:
- restrained Vecells wordmark plus inline SVG `LC` monogram
- one live-channel topology diagram
- one cache namespace grid
- one reconnect and replay-state timeline strip

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- live accent `#2563EB`
- cache accent `#0EA5A4`
- replay accent `#7C3AED`
- stale accent `#D97706`
- blocked accent `#C24141`
- restored accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for channel IDs, cache IDs, replay-window IDs, and connection epochs

Layout:
- sticky masthead with active environments, channel health, cache-reset alerts, and replay-window drift warnings
- left rail (`324px`) with audience filters, channel-state filters, cache-class filters, and degradation filters
- central canvas with:
  - live-channel topology diagram
  - cache namespace grid
  - reconnect / replay-state timeline strip
- right inspector (`420px`) for selected channel, cache class, or reconnect policy
- lower grid with one topology table and one cache / replay policy table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by audience, channel state, cache class, environment, and degraded mode
- selecting a row must synchronize diagrams, timeline, inspector, and tables
- keyboard navigation across filters and tables
- deterministic `data-testid` markers for topology diagram, cache grid, replay timeline, topology table, policy table, and inspector
- every visual must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification
- verification that stale and blocked modes remain visibly distinct from restored or healthy modes

Definition of done:
- The repository has a real cache and live-transport substrate aligned to audience and gateway boundaries.
- Connection, replay, and invalidation primitives are reusable and testable.
- Transport or cache degradation fails into explicit degraded infrastructure posture.
- Later route-family cache and live-channel contracts can bind to this substrate without redefining the runtime foundations.
```
