# Prompt 090 For 090_par_phase0_track_runtime_provision_audience_specific_gateway_bff_surfaces

Use the shared operating contract first:
[shared_operating_contract_086_to_095.md](/Users/test/Code/V/prompt/shared_operating_contract_086_to_095.md)

```text
Execute roadmap task 090_par_phase0_track_runtime_provision_audience_specific_gateway_bff_surfaces.md.

Inherit the Shared_operating_contract_for_prompts_086_to_095 and consume the validated outputs from tasks `001-089`.

Mission:
Provision the audience-specific gateway and BFF surface layer for Vecells.

This task must turn `GatewayBffSurface` from architecture text into concrete runtime contracts and implementation seams. It must provision:
- gateway surface manifests per audience family or explicitly justified shared audience group
- service entrypoints, route families, session policy bindings, downstream workload boundaries, and trust-zone refs
- openapi or browser-callable contract publication bound to the gateway surface
- deployment-ready gateway scaffolds or services that refuse undeclared reads, mutations, streams, or cache semantics

This is real runtime and backend foundation work. Produce gateway code, manifests, validators, and smoke tests. Do not leave BFF splitting, audience isolation, or downstream access scope as route-folder convention.

Algorithm grounding you must honor:
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### GatewayBffSurface`
  - `### FrontendContractManifest`
  - runtime rules that only published gateway surfaces may bridge browser traffic into internal workload planes
- `platform-frontend-blueprint.md`, especially shell ownership, route-family ownership, audience surface runtime binding, and same-shell continuity expectations
- `patient-portal-experience-architecture-blueprint.md`, `patient-account-and-communications-blueprint.md`, `staff-operations-and-support-blueprint.md`, `operations-console-frontend-blueprint.md`, `pharmacy-console-frontend-architecture.md`, and `governance-admin-console-frontend-blueprint.md`
- outputs from tasks `004`, `014`, `046`, `047`, `050`, `065`, `084`, `088`, and `089`
- `forensic-audit-findings.md`, especially Findings `86`, `87`, `90`, `91`, `92`, `95`, `96`, `97`, `100`, and `101`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must create audience-specific gateway surfaces and authority manifests now, but it must not steal later ownership of route-specific cache policy, live-channel semantics, or frontend-shell implementation. If a sibling track owns a downstream contract not yet materialized, create the smallest bounded stub and emit `PARALLEL_INTERFACE_GAP_*`.

Mock_now_execution:
Build working gateway surfaces now against mock projections, simulator-backed commands, and sandbox-safe dependencies.
- create real gateway services or modules for the current baseline audiences using mock or seeded downstream services
- preserve the same route-family mapping, session policy, tenant isolation, trust boundaries, manifest publication, and refusal behavior the live system will require
- allow premium internal atlas surfaces and later shell work to exercise the real gateway boundaries in local and non-production environments
The mock-now path must be realistic enough that live downstream cutover is a configuration and dependency swap, not a gateway-surface redesign.

Actual_production_strategy_later:
When production ingress, identity, and downstream dependencies are finalized, they must preserve the same gateway surface split, trust boundaries, route-family ownership, manifest bindings, and refusal posture. Production hardening may add WAF, autoscaling, stricter auth enforcement, or region-aware routing, but it must not collapse patient, workspace, support, ops, hub, pharmacy, and governance surfaces into one generic BFF.

Primary outcome:
The repository gains concrete gateway surfaces that make browser-to-runtime authority explicit, auditable, and audience-specific.

Implementation deliverables to create:
1. gateway service code or modules for each current baseline audience surface, with explicit routing, session policy, downstream-family allowlists, and manifest-driven refusal behavior
2. machine-readable `GatewayBffSurface` manifests and deployment descriptors
3. openapi or browser-contract publication for declared reads, mutations, and live transport entrypoints bound to each surface
4. runtime validation that rejects undeclared endpoints, undeclared cross-context writes, undeclared downstream calls, and undeclared cache or stream exposure
5. local-development and CI bootstrap for gateway surfaces against seeded projections and simulator-backed command or event seams
6. `docs/architecture/90_gateway_bff_surface_design.md`
7. `docs/architecture/90_audience_gateway_split_and_boundary_rules.md`
8. `docs/architecture/90_gateway_surface_authority_atlas.html`
9. `data/analysis/gateway_surface_manifest.json`
10. `data/analysis/audience_route_family_to_gateway_matrix.csv`
11. `data/analysis/gateway_downstream_boundary_matrix.csv`
12. `tools/analysis/validate_gateway_surfaces.(py|ts)`
13. backend tests, contract tests, refusal-behavior tests, session-policy tests, and dependency-boundary smoke tests
14. `tests/playwright/gateway-surface-authority-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- provision distinct gateway surfaces for the current baseline audience families unless an explicit combined-surface justification proves shared posture is safe
- bind each surface to exact route families, session policy, tenant isolation mode, and allowed downstream workload families
- reject undeclared reads, writes, streams, and cache exposure
- prohibit raw data-plane access, direct adapter egress, and undeclared cross-context fanout from gateway surfaces
- keep manifest publication and route-family ownership machine-readable and testable
- support embedded or constrained channel boundary hooks where required without collapsing them into ordinary browser rules

Execution steps:
1. Re-read the gateway, frontend manifest, shell ownership, and audience-surface blueprints, then derive the gateway-surface split required for the current baseline.
2. Honor the runtime topology, trust-boundary, route-family, and API-contract outputs from earlier tasks; if prerequisites are absent or contradictory, fail fast with `PREREQUISITE_GAP_*`.
3. Materialize gateway services or modules with manifest-driven routing and explicit downstream-family binding.
4. Publish `GatewayBffSurface` manifests and deployment descriptors from code.
5. Implement refusal behavior for undeclared routes, undeclared mutations, stale or absent manifests, and unsupported audience or channel combinations.
6. Build the authority atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write smoke tests proving that patient, workspace, support, ops, hub, pharmacy, and governance paths cannot silently bleed into each other or into raw internal systems.

Mandatory gap closures you must perform:
- Close the “one generic BFF can do everything” gap by splitting surfaces by actual audience and authority needs.
- Close the “route-family ownership lives only in the frontend” gap by publishing gateway-surface bindings.
- Close the “browser can reach internal data or adapters indirectly” gap by enforcing downstream-family allowlists and refusal behavior.
- Close the “same-shell continuity can survive manifest drift by guesswork” gap by blocking or downgrading when required gateway surface authority is absent.
- Close the “embedded and constrained channels are just browser aliases” gap by preserving channel-aware boundary hooks.

Industry-best-practice requirements:
- audience-specific boundary enforcement
- least-privilege downstream access per gateway surface
- manifest-driven route publication and refusal behavior
- shared infrastructure only where authority tuples remain distinct and auditable
- strict session, cache-scope, and tenant-isolation separation
- no raw internal endpoint passthrough from browser to internal services

Non-negotiable rules:
- One `GatewayBffSurface` is the only browser-addressable compute boundary for one audience family or explicitly justified shared surface.
- Gateway surfaces may access only their declared downstream workload families and trust-zone boundaries.
- Gateway surfaces may not call adapters directly or touch raw data planes.
- Undeclared reads, writes, streams, and cache semantics must be rejected.
- Audience and channel isolation may not be reconstructed from route names alone.

Mandatory UI and interaction specification for `90_gateway_surface_authority_atlas.html`:
Name the surface mode `Gateway_Surface_Authority_Atlas`.

Tone:
- premium control-plane atlas
- minimalist, architectural, and quietly luxurious
- should feel like a top-tier interface architecture map, not a Swagger wrapper

Branding:
- restrained Vecells wordmark plus inline SVG `GW` monogram
- one audience-to-gateway topology diagram
- one route-family authority map
- one downstream-boundary matrix

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- patient accent `#2563EB`
- staff accent `#0EA5A4`
- support accent `#7C3AED`
- ops accent `#D97706`
- governance accent `#C24141`
- verified accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for gateway IDs, route-family refs, boundary IDs, and manifest digests

Layout:
- sticky masthead with active surfaces, publication drift alerts, blocked audience states, and undeclared-route warnings
- left rail (`324px`) with audience filters, channel filters, surface-state filters, and downstream-boundary filters
- central canvas with:
  - audience-to-gateway topology diagram
  - route-family authority map
  - downstream-boundary matrix
- right inspector (`420px`) for selected surface, route family, or downstream boundary
- lower grid with one manifest table and one boundary / refusal policy table

Dimensional constraints:
- max width `1580px`
- masthead `76px`
- visual zone min height `320px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by audience, channel, boundary state, route family, and publication state
- selecting a surface or route row must synchronize the topology diagram, authority map, matrix, inspector, and tables
- keyboard navigation across filters and tables
- deterministic `data-testid` markers for topology diagram, authority map, matrix, manifest table, policy table, and inspector
- every visual must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification
- verification that undeclared route or boundary states visibly downgrade or block the surface instead of remaining calm

Definition of done:
- The repository has real, audience-specific gateway surfaces with explicit authority and downstream boundaries.
- Browser-to-runtime boundaries are machine-readable and testable.
- Undeclared endpoints and cross-context overreach fail closed.
- Later shell and route work can bind to these surfaces without redefining gateway authority from scratch.
```
