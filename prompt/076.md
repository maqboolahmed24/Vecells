# Prompt 076 For 076_par_phase0_track_backend_implement_request_closure_record_and_exception_case_models

Use the shared operating contract first:
[shared_operating_contract_076_to_085.md](/Users/test/Code/V/prompt/shared_operating_contract_076_to_085.md)

```text
Execute roadmap task 076_par_phase0_track_backend_implement_request_closure_record_and_exception_case_models.md.

Inherit the Shared_operating_contract_for_prompts_076_to_085 and consume the validated outputs from tasks `001-075`.

Mission:
Implement the authoritative closure artifact and the canonical exception-case substrate that lets the platform defer closure safely without losing lineage continuity:
- `RequestClosureRecord`
- `FallbackReviewCase`
- the bounded blocker-taxonomy, defer-reason, and evaluation-input value objects the coordinator will need later

This task is model, persistence, and evaluator implementation. It is not the full `LifecycleCoordinator` service from task `077`, but the models and validators created here must already force later orchestration to obey the canonical closure law.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.12 RequestClosureRecord`
  - `#### 1.20 FallbackReviewCase`
  - `#### 2.1 LifecycleCoordinator`
  - `#### 2.9 ExceptionOrchestrator`
  - `#### 9.4 Workflow state ownership`
  - `#### 9.5 Required workflow semantics`
  - `#### 9.6 Closure evaluation algorithm`
  - `#### 9.7 Reopen triggers`
- `phase-1-the-red-flag-gate.md`, especially the degraded-submit, artifact-quarantine, and accepted-progress fallback-review rules
- `phase-3-the-human-checkpoint.md`, `phase-4-the-booking-engine.md`, `phase-5-the-network-horizon.md`, and `phase-6-the-pharmacy-loop.md` wherever unresolved approval, confirmation, review, or recovery must remain explicit and closure-blocking
- `callback-and-clinician-messaging-loop.md` where callback or thread completion still requires coordinator-owned request closure
- `forensic-audit-findings.md`, especially Findings `45`, `57`, `58`, `59`, `60`, `68`, and `71`
- outputs from tasks `001-007`, `019`, `041-045`, `055`, `061`, `067`, `069`, `070`, `074`, and `075`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Do not depend on task `077` already existing. Publish only the smallest bounded evaluator or repository interfaces that the later coordinator service will need.

Mock_now_execution:
Build a local closure-and-exception simulator now. It must cover:
- a request with no blockers and legal closure
- a request blocked by duplicate review
- a request blocked by fallback review after accepted degraded progress
- a request blocked by identity repair
- a request blocked by unresolved external confirmation
- a request blocked by PHI-bearing grants or contact-route repair
- a request whose blocker refs are stale even though prose checks appear green
The simulator must emit the same blocker classes, defer reasons, and closure artifacts production will require later.

Actual_production_strategy_later:
When live triage, booking, hub, pharmacy, callback, messaging, recovery, and assurance services are fully wired, they must all project closure blockers into the same canonical `RequestClosureRecord` and `FallbackReviewCase` substrate. Live services may enrich blocker evidence, but they may not invent route-local closure semantics, generic “resolved” labels, or hidden side tickets outside the request lineage.

Primary outcome:
The repository gains real append-only closure artifacts that make “why the request cannot close yet” explicit, machine-readable, and replayable, while preserving same-lineage recovery for degraded or manual exception flows.

Implementation deliverables to create:
1. domain and persistence code for `RequestClosureRecord`
2. domain and persistence code for `FallbackReviewCase`
3. bounded internal value objects for closure-blocker classes, defer-reason codes, and evaluation-input snapshots
4. repository interfaces and storage adapters for querying active blockers by request, episode, lineage, blocker class, and required epoch
5. validators enforcing closure-record completeness, blocker-array integrity, epoch monotonicity, and decision legality
6. canonical-event entries for fallback case opened, fallback case advanced, fallback case recovered, fallback case closed, closure evaluated, closure deferred, closure closed, and closure blockers changed
7. `docs/architecture/76_request_closure_and_exception_case_design.md`
8. `docs/architecture/76_closure_blocker_taxonomy_and_rules.md`
9. `docs/architecture/76_closure_governance_atlas.html`
10. `data/analysis/request_closure_record_manifest.json`
11. `data/analysis/fallback_review_case_matrix.csv`
12. `data/analysis/closure_blocker_casebook.json`
13. `tools/analysis/validate_request_closure_models.(py|ts)`
14. unit tests, repository tests, replay tests, and concurrent close-vs-blocker race tests
15. `tests/playwright/closure-governance-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- persist `RequestClosureRecord` with every canonical blocker ref family named in the source algorithm, including duplicate, fallback, identity-repair, grant, reachability, confirmation, lease, approval, reconciliation, and degraded-promise blockers
- persist `FallbackReviewCase` as a same-lineage recovery artifact rather than a detached ticket or generic note
- make `decision = close | defer` explicit and immutable
- require `requiredLineageEpoch` and fail validation if closure is attempted against stale epoch input
- preserve `closedByMode` and `deferReasonCodes[]` as explicit output, not reconstructible folklore
- support lineage scopes `envelope | request | episode` for fallback review without breaking audit continuity
- keep `Request.workflowState` milestone-only; blockers and gates must stay orthogonal to workflow state

Execution steps:
1. Implement `RequestClosureRecord` exactly from the canonical field contract. Treat missing blocker arrays as a schema bug, not an optional omission.
2. Implement `FallbackReviewCase` exactly from the canonical field contract and semantics. Its `patientVisibleState` must preserve accepted progress and same-lineage recovery.
3. Add bounded supporting vocabularies for:
   - blocker class
   - defer reason code
   - fallback trigger class
   - patient-visible degraded posture
   These helper types may support the models, but they must not replace the canonical object identities.
4. Implement validators that fail when:
   - `decision = close` is persisted while any blocker-ref array is non-empty
   - `decision = close` is persisted without a required epoch
   - `decision = defer` is persisted with no blocker basis and no explicit defer reasons
   - a `FallbackReviewCase` closes without recovery, governed supersession, or policy-bound manual settlement
   - fallback review is opened for wrong-patient repair instead of `IdentityRepairCase`
5. Implement closure-event and fallback-event schemas so later projections do not have to reverse-engineer active closure posture from unrelated domain rows.
6. Build the governance atlas and drive it with `Playwright_or_other_appropriate_tooling` during implementation.
7. Write race tests where blockers appear between close evaluation and persistence, and confirm the persisted close record cannot outrun blocker truth.

Mandatory gap closures you must perform:
- Close the “passive terminal close” gap by making closure evaluation a persisted authoritative artifact.
- Close the “duplicate review not represented in closure artifact” gap with `blockingDuplicateClusterRefs[]`.
- Close the “fallback review not represented in closure artifact” gap with `blockingFallbackCaseRefs[]`.
- Close the “wrong-patient repair not represented in closure artifact” gap with `blockingIdentityRepairRefs[]`.
- Close the “PHI-bearing grants and reachability not represented in closure artifact” gap with `blockingGrantRefs[]` and `blockingReachabilityRefs[]`.
- Close the “closure may rely on prose checks while materialized blocker refs stay stale” gap by validating blocker-set emptiness directly.
- Close the “degraded progress spawns detached operational tickets” gap by modeling same-lineage `FallbackReviewCase` with explicit patient-visible degraded posture.
- Close the “exception case means generic admin note” gap by giving fallback review its own explicit trigger class, SLA anchor, queue, receipt time, and recovery state.

Industry-best-practice requirements:
- append-only closure artifacts and exception-case history
- compare-and-set-safe epoch validation
- no hidden controller-side closure logic
- no overloading of human-readable status text as machine truth
- full replay safety for repeated close attempts and repeated degraded-case observations
- explicit database constraints where decision and blocker shape must agree

Non-negotiable rules:
- `Request.workflowState = closed` is not legal without a persisted `RequestClosureRecord(decision = close)`.
- Blockers stay orthogonal to workflow milestones.
- `FallbackReviewCase` keeps the same request lineage, public tracking continuity, and audit chain.
- Wrong-patient repair uses `IdentityRepairCase`, not `FallbackReviewCase`.
- Transport success, local acknowledgement, or visual calmness do not count as closure authority.

Mandatory UI and interaction specification for `76_closure_governance_atlas.html`:
Name the surface mode `Closure_Governance_Atlas`.

Tone:
- premium internal control surface
- quiet, forensic, and unambiguously authoritative
- should feel like a lineage-governance instrument, not a ticket dashboard

Branding:
- restrained Vecells wordmark plus inline SVG `CG` monogram
- one closure-decision ladder diagram
- one blocker-family constellation diagram
- one fallback-review continuity ribbon showing accepted progress preserved through degraded handling

Palette:
- canvas `#F7F8FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1F2937`
- text muted `#64748B`
- border subtle `#E2E8F0`
- close accent `#2563EB`
- defer accent `#7C3AED`
- blocker accent `#C24141`
- fallback accent `#D97706`
- recovered accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for lineage refs, epoch refs, record IDs, and blocker hashes

Layout:
- sticky masthead with close-ready count, deferred count, and stale-epoch anomalies
- left rail (`320px`) for blocker-family filters, decision filters, and lineage-scope filters
- central canvas with:
  - closure-decision ladder diagram
  - blocker-family constellation / matrix
  - fallback-review continuity ribbon
- right inspector (`404px`) showing selected request closure record, blocker refs, defer reasons, and linked fallback cases
- lower analytical grid with one closure-history table and one exception-case table

Dimensional constraints:
- max width `1540px`
- masthead `72px`
- central diagram zone min height `280px`
- inspector sticky top offset `88px`
- control heights `44px`

Motion:
- `120ms` hover and focus feedback
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory with no position-based animation

Interaction requirements:
- filter by decision state, blocker family, fallback trigger class, and lineage scope
- selecting a closure row must synchronize the decision ladder, blocker diagram, inspector, and fallback ribbon
- keyboard navigation across filters, tables, and inspector sections
- deterministic `data-testid` markers for decision ladder, blocker matrix, fallback ribbon, closure table, exception table, and inspector
- any visual diagram must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filter behavior across decision and blocker classes
- selection synchronization between table, inspector, and diagrams
- keyboard navigation
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification

Definition of done:
- The repository has real closure and exception-case models, persistence, validators, and tests.
- Closure cannot be represented without a persisted `RequestClosureRecord`.
- The blocker families required by the forensic audit are all first-class in the closure artifact.
- `FallbackReviewCase` preserves same-lineage recovery instead of leaking into ad hoc manual work.
- Task `077` can consume these artifacts directly without redefining closure or exception semantics.
```
