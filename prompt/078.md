# Prompt 078 For 078_par_phase0_track_backend_build_access_grant_service

Use the shared operating contract first:
[shared_operating_contract_076_to_085.md](/Users/test/Code/V/prompt/shared_operating_contract_076_to_085.md)

```text
Execute roadmap task 078_par_phase0_track_backend_build_access_grant_service.md.

Inherit the Shared_operating_contract_for_prompts_076_to_085 and consume the validated outputs from tasks `001-077`.

Mission:
Build the full canonical `AccessGrantService` that issues, redeems, rotates, revokes, and supersedes all patient-facing grants across draft resume, request claim, secure-link continuation, callback and message response, booking manage, network alternative choice, pharmacy choice, support recovery, and auth uplift flows.

This task is the full service implementation. It must use the append-only models from task `068`, respect route-intent and settlement law from tasks `056`, `071`, and `072`, and integrate with bounded `SessionGovernor` and `AuthBridge` ports without re-inventing grant semantics in each channel.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.4C Session`
  - `#### 1.4D AuthTransaction`
  - `#### 1.4E AuthScopeBundle`
  - `#### 1.4F PostAuthReturnIntent`
  - `#### 1.6 AccessGrant`
  - `#### 1.6A AccessGrantScopeEnvelope`
  - `#### 1.6B AccessGrantRedemptionRecord`
  - `#### 1.6C AccessGrantSupersessionRecord`
  - `#### 2.3 AccessGrantService`
  - `#### 2.3A AuthBridge`
  - `#### 2.3B SessionGovernor`
  - `### 6. Unified AccessGrant and secure-link rules`
- `phase-1-the-red-flag-gate.md`, especially draft-resume and promotion-driven grant supersession
- `phase-2-identity-and-echoes.md`, especially claim, continuation, rotation, redemption, family-drift, and subject-binding rules
- `patient-account-and-communications-blueprint.md` and `callback-and-clinician-messaging-loop.md`, where patient actions must remain same-shell, bounded, and route-intent-governed
- `forensic-audit-findings.md`, especially Findings `06`, `07`, `50`, `89`, `97`, `98`, `99`, `100`, and `101`
- outputs from tasks `001-007`, `019`, `041-045`, `056-057`, `061`, `068`, `071-072`, and `077`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Do not wait for a full live NHS login bridge, telephony service, or notification platform. Implement the grant authority now against minimal subject-proof, auth-transaction, and session ports. If a port is missing, publish the smallest bounded interface and record `PARALLEL_INTERFACE_GAP_*`.

Mock_now_execution:
Build a full local grant authority with synthetic but realistic issuance and redemption sources:
- simulated draft-resume grants
- simulated request-claim and continuation grants
- simulated NHS-login-like post-auth uplift
- simulated callback / message reply grants
- simulated booking-manage and pharmacy-choice transaction grants
- simulated support reissue and repair-driven revocation
The mock path must produce the same `AccessGrant`, `AccessGrantScopeEnvelope`, `AccessGrantRedemptionRecord`, and `AccessGrantSupersessionRecord` semantics the live system will later require.

Actual_production_strategy_later:
When live NHS login, telephony continuation, secure-link distribution, support reissue, and embedded return are onboarded, they must all call the same `AccessGrantService`. Live providers may change identity evidence, transport channels, or token delivery mechanisms, but they may not widen scope, skip exact-once redemption settlement, or upgrade anonymous / mismatched sessions in place.

Primary outcome:
The repository gains one authoritative grant service that every patient-facing continuation or action path can use safely, without each feature inventing its own link, token, replay, or route-scope rules.

Implementation deliverables to create:
1. `AccessGrantService` implementation for issue, redeem, rotate, revoke, replace, and supersede flows
2. grant-family policy registry covering at least draft resume, request claim, secure continuation, callback reply, message reply, booking manage, waitlist action, network alternative choice, pharmacy choice, support replay/reissue, and recover-only grants
3. token materialization and verifier layer using hashed / signed opaque tokens with key-version support and explicit expiry handling
4. bounded `SessionGovernor` and `AuthBridge` ports plus local development adapters sufficient to test redemption and post-auth re-entry safely
5. route-intent and runtime-tuple validation inside redemption and issuance
6. promotion, logout, wrong-patient repair, and support-reissue supersession hooks
7. `docs/architecture/78_access_grant_service_design.md`
8. `docs/architecture/78_access_grant_family_and_redemption_rules.md`
9. `docs/architecture/78_access_grant_journey_lab.html`
10. `data/analysis/access_grant_family_matrix.csv`
11. `data/analysis/access_grant_casebook.json`
12. `data/analysis/access_grant_runtime_tuple_manifest.json`
13. `tools/analysis/validate_access_grant_service.(py|ts)`
14. unit tests, crypto / token tests, redemption replay tests, scope-drift tests, and integration tests
15. `tests/playwright/access-grant-journey-lab.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- issue one immutable `AccessGrantScopeEnvelope` for every redeemable grant
- bind grant scope to the current governing object, route family, grant family, allowed action scope, and runtime tuple
- settle one exact-once `AccessGrantRedemptionRecord` before session creation, route re-entry, or replacement grant issuance proceeds
- return the current redemption or supersession settlement on exact replay
- enforce family-specific subject binding, route ceilings, replay control, and no-family-drift
- reject anonymous, stale, or mismatched-session upgrade in place
- supersede older grants on promotion, rotation, logout, repair-driven revocation, reissue, and route-drift invalidation
- support `manual_only` and `recover_only` outcomes explicitly rather than forcing a link where no grant is safe

Execution steps:
1. Implement the service around the append-only models from task `068`; do not re-model grants locally.
2. Publish a grant-family registry that defines for each family:
   - redeemability
   - subject-binding rule
   - session requirement
   - route-family ceiling
   - action-scope ceiling
   - expiry policy
   - replay policy
   - supersession triggers
3. Implement issue and redeem flows that validate current route-intent, grant family, scope envelope, subject binding, runtime tuple, and session posture before allowing access.
4. Integrate bounded session-establishment and post-auth-return ports without letting those ports mint grant truth outside the service.
5. Implement exact replay return, legacy recover-only handling, and family-drift rejection.
6. Implement promotion-driven, repair-driven, logout-driven, and support-driven supersession paths.
7. Build the access-grant journey lab and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Write tests for replay, subject mismatch, stale route tuple, old-link supersession, post-auth uplift, and wrong-patient repair revocation.

Mandatory gap closures you must perform:
- Close the “authentication equals permission” gap by requiring grant-family, route-intent, and scope-envelope validation beyond auth success.
- Close the “each feature invents its own token semantics” gap by forcing all patient-facing grants through one service.
- Close the “anonymous or mismatched session upgrades in place” gap by requiring new session posture through `SessionGovernor` instead of in-place upgrade.
- Close the “old links remain live after rotation, promotion, or repair” gap with authoritative supersession chains.
- Close the “replay produces a second side effect” gap by returning the current redemption or supersession settlement on exact replay.
- Close the “grant family drifts from action scope” gap by binding one immutable `AccessGrantScopeEnvelope` to the current governing tuple.
- Close the “recover-only or manual-only situations still render a live action link” gap by supporting those outcomes explicitly.

Industry-best-practice requirements:
- opaque token handling with hashed storage and key-version support
- exact-once redemption and replay-safe settlement
- explicit cryptographic abstraction with deterministic test harnesses
- no raw token storage after first issuance materialization
- strict expiry, scope, and subject-binding checks
- full audit joins for issue, redeem, revoke, rotate, and supersede paths

Non-negotiable rules:
- `AccessGrantService` is the only authority allowed to issue, redeem, or supersede patient-access grants.
- `AccessGrantScopeEnvelope` is mandatory for every redeemable grant.
- Grant redemption alone may not widen route or embedded capability scope.
- Exact replay returns existing settlement; it never produces a second effect.
- Anonymous or mismatched sessions may not be upgraded in place.

Mandatory UI and interaction specification for `78_access_grant_journey_lab.html`:
Name the surface mode `Access_Grant_Journey_Lab`.

Tone:
- premium internal verification studio
- elegant, calm, and cryptographically serious
- should feel like a secure token observatory, not a generic auth admin panel

Branding:
- restrained Vecells wordmark plus inline SVG `AG` monogram
- one grant-family ladder diagram
- one redemption-to-session transition map
- one supersession timeline ribbon

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- issue accent `#2563EB`
- redeem accent `#0EA5A4`
- supersede accent `#7C3AED`
- warning accent `#D97706`
- blocked accent `#C24141`

Typography:
- system sans stack only
- monospaced digest styling for token hashes, key versions, grant IDs, and tuple hashes

Layout:
- sticky masthead with active grants, redeemed grants, superseded grants, and replay-block incidents
- left rail (`316px`) with family filters, redemption-state filters, subject-binding filters, and route-family filters
- central canvas with:
  - grant-family ladder diagram
  - redemption-to-session transition map
  - supersession ribbon
- right inspector (`408px`) with selected grant, scope envelope, last redemption, and supersession chain
- lower table region with one issuance table and one redemption / supersession table

Dimensional constraints:
- max width `1540px`
- masthead `72px`
- diagram zone min height `280px`
- inspector sticky top offset `88px`
- control heights `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion support mandatory

Interaction requirements:
- filter by grant family, redemption state, subject-binding state, and route family
- selecting a grant row must synchronize ladder diagram, transition map, supersession ribbon, inspector, and redemption table
- keyboard navigation across filters, tables, and inspector sections
- deterministic `data-testid` markers for family ladder, transition map, supersession ribbon, issuance table, redemption table, and inspector
- every visual representation must have adjacent textual parity and table fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- issuance / redemption / supersession filters
- synchronized selection across diagrams and tables
- keyboard navigation and focus order
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification

Definition of done:
- The repository has a real access-grant service with issue, redeem, revoke, rotate, and supersession logic.
- All patient-facing continuation and action links run through one authoritative grant model.
- Replay, family drift, scope drift, stale sessions, and repair-driven revocation are handled explicitly.
- Later identity, portal, booking, callback, and support tasks can consume this service directly without inventing new token semantics.
```
