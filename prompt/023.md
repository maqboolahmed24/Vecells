# Prompt 023 For 023_seq_external_create_sandbox_account_strategy_and_secret_ownership_model

Use the shared operating contract first:
[shared_operating_contract_021_to_025.md](/Users/test/Code/V/prompt/shared_operating_contract_021_to_025.md)

```text
Execute roadmap task 023_seq_external_create_sandbox_account_strategy_and_secret_ownership_model.md.

Inherit the Shared_operating_contract_for_prompts_021_to_025 and consume the validated outputs from tasks `001-022`, especially tasks `008`, `011`, `013`, `015`, `018`, `021`, and `022`.

Mission:
Create the definitive sandbox-account, test-account, environment-credential, and secret-ownership strategy for external integrations across both:
- mock services and internal simulators that Vecells can run now
- real provider and partner accounts that Vecells may acquire later

This task must freeze:
- what kinds of accounts and credentials exist
- who owns them
- where they live
- who may rotate or revoke them
- what may exist now as mock credentials
- what must remain placeholder / pending until real onboarding happens
- how credentials enter the system without ever becoming repo content or loose operational state

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `1.4D AuthTransaction`
  - `1.4E AuthScopeBundle`
  - `1.4G SessionEstablishmentDecision`
  - `1.6 AccessGrant` family
  - contact / reachability objects where delivery routes matter
  - the requirement that no browser directly calls partner systems
  - the broader append-only audit and exact-once settlement law
- `phase-2-the-identity-and-echoes.md`, especially:
  - `2B. NHS login bridge and local session engine`
  - `2E. Telephony edge, IVR choreography, and call-session persistence`
  - `2F. Caller verification, voice capture, transcript stub, and SMS continuation`
- `phase-4-the-booking-engine.md` and `phase-6-the-pharmacy-loop.md` where provider credentials, environment bindings, and proof chains must remain adapter-bound
- `phase-7-inside-the-nhs-app.md`, especially:
  - `7H. Sandpit, AOS, SCAL, and operational delivery pipeline`
  - environment-profile and evidence-bundle expectations
- `platform-runtime-and-release-blueprint.md`, especially:
  - trust-zone rules
  - environment ring and promotion contract
  - dependency-degradation and runtime-publication expectations
- task `011` cloud/trust-zone strategy
- task `015` observability/security/release tooling baseline
- task `018` risk register and dependency watchlist
- `forensic-audit-findings.md`, especially any finding where loose secret handling, hidden environment coupling, or unsupported mutability would create unsafe drift

Primary outcome:
One authoritative ownership and handling model for all external accounts and secrets, across mock-now and actual-later execution.

Deliverables to create:
1. `docs/external/23_sandbox_account_strategy.md`
2. `docs/external/23_secret_ownership_and_rotation_model.md`
3. `docs/external/23_mock_account_bootstrap_plan.md`
4. `docs/external/23_actual_partner_account_governance.md`
5. `docs/external/23_credential_ingest_and_redaction_runbook.md`
6. `docs/external/23_external_access_governance_cockpit.html`
7. `data/analysis/external_account_inventory.csv`
8. `data/analysis/secret_classification_matrix.csv`
9. `data/analysis/secret_ownership_map.json`
10. `data/analysis/mock_account_seed_plan.json`
11. `data/analysis/credential_capture_checklist.csv`
12. `tools/analysis/build_external_access_governance_pack.(py|ts)`
13. `tools/analysis/validate_external_access_governance_pack.(py|ts)`

Every account / secret row must capture at minimum:
- `account_or_secret_id`
- `dependency_family`
- `environment = local_mock | ci_mock | shared_dev | sandpit | integration | preprod | production`
- `record_class = account | test_user | service_principal | client_registration | client_secret | private_key | public_key | webhook_secret | mailbox_credential | phone_number | sender_identity | signing_key | sandbox_dataset | other`
- `current_lane = mock_now | actual_later | hybrid`
- `owner_role`
- `backup_owner_role`
- `creator_role`
- `approver_role`
- `storage_backend`
- `distribution_method`
- `rotation_policy`
- `revocation_policy`
- `audit_sink`
- `exposure_constraints`
- `allowed_usage_surfaces[]`
- `manual_checkpoint_required = yes | no`
- `live_gate_refs[]`
- `mock_equivalent_ref`
- `notes`

Section A — `Mock_now_execution`
This section is mandatory.

Define the mock account and secret posture for high-fidelity local development. At minimum cover:
- mock NHS login client registrations and signing keys
- mock test users and synthetic identity records
- mock telephony webhook secrets, callback URLs, test numbers, and recording fixtures
- mock SMS/email sender identities and delivery webhooks
- mock GP / booking provider accounts and capability matrices
- mock pharmacy directory / dispatch / outcome provider identities
- mock MESH or cross-org route identities if simulated
- fixture packs for synthetic patients, requests, appointments, pharmacy cases, and transcripts

The mock strategy must:
- forbid real credentials
- enforce deterministic seeding
- separate team-local mock identities from shared-environment mock identities
- define how mock secrets are rotated and reset
- define how Playwright and CI obtain mock credentials without storing them in repo snapshots or traces
- define which mock credentials are static fixtures and which are generated per environment spin-up

Section B — `Actual_provider_strategy_later`
This section is mandatory.

Define the later real-account / real-secret governance posture. At minimum cover:
- who requests real provider accounts
- who approves them
- where credentials land initially
- how they move into the chosen secret manager / vault
- how redirect-URI changes, key rotation, and environment additions are governed
- what must be dual-controlled
- how test accounts and production accounts are segregated
- how browser automation may use the secrets without exposing them
- how expiry, revocation, and incident response are handled
- how screenshots, traces, and logs are scrubbed

Execution steps:
1. Start from task `021` lane assignments and task `022` provider scorecards.
2. Build a complete account / secret class inventory for:
   - mock accounts now
   - live provider accounts later
3. Freeze an ownership model for each class:
   - engineering owner
   - platform/security owner
   - business / product / ops approver where required
4. Define storage and flow:
   - where the secret originates
   - temporary landing zone if any
   - permanent storage
   - runtime injection path
   - audit sink
5. Define separate handling for:
   - client IDs vs client secrets
   - public keys vs private keys
   - test user credentials vs integration service credentials
   - webhook secrets
   - mailbox / transport credentials
   - service phone numbers / sender identities
6. Build a runbook for credential capture, redaction, and vault ingest.
7. Generate a browser-viewable governance cockpit for review.

Mandatory gap closures you must perform:
- Close the “mock credentials and live credentials might blur together” gap.
- Close the “future partner accounts have no ownership law yet” gap.
- Close the “browser automation could leak secrets through traces or screenshots” gap.
- Close the “who can rotate or revoke what” gap.
- Close the “redirect-URI, key, and environment changes are operational but not governed” gap.
- Close the “shared test accounts and production identities could drift into each other” gap.
- Close the “credential capture is a one-time setup task rather than an audited lifecycle” gap.

Non-negotiable secret-handling rules:
- no real credential may be committed to the repo
- no live credential may be present in generated markdown examples
- no browser automation trace, video, or screenshot may expose live secrets
- no shared production credential may be single-owner only
- no test account may silently acquire production privileges
- no environment may inherit secrets from another environment without explicit governance
- no redirect-URI or signing-key change may bypass audit and approval where the dependency family requires it

The cockpit UI requirements for `23_external_access_governance_cockpit.html`:
Name the visual mode `Credential_Observatory`.

This must feel like a premium governance console rather than a security spreadsheet.

Visual direction:
- canvas `#F6F8FB`
- shell `#FFFFFF`
- inset `#EEF2F7`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- ownership accent `#335CFF`
- vault accent `#7C3AED`
- rotation accent `#0F9D58`
- warning accent `#C98900`
- blocked accent `#C24141`

Layout:
- top posture banner with counts for mock, live-pending, dual-control, and missing-owner items
- left inventory rail filtered by dependency family and environment
- center governance tables:
  - account inventory
  - secret class matrix
  - rotation schedule strip
- right inspector:
  - selected account or secret lifecycle
  - owner chain
  - allowed surfaces
  - capture and revocation notes
- lower diagram:
  - secret origin -> landing -> vault -> runtime injection -> audit flow
  - always pair with tabular parity

Dimensional constraints:
- inventory table min height `420px`
- inspector width `360px`
- lifecycle diagram min height `280px`
- banner cards `160px` min width

Motion:
- `120ms` hover/focus
- `180ms` filter transitions
- `240ms` inspector transitions
- reduced-motion support mandatory

Unique design notes:
- use line-based flow diagrams, not padlock clip art
- include one subtle inline SVG “key orbit” motif
- keep the tone calm and exact rather than alarmist

Playwright requirements:
Validate the cockpit with `Playwright_or_other_appropriate_tooling` for:
- environment filtering
- inspector synchronization
- diagram/table parity
- keyboard navigation
- reduced motion
- responsive behavior
- accessibility smoke checks
- offline asset completeness

Definition of done:
- Mock and live external-account posture are now explicitly separated.
- Every account and secret class has an owner model, storage rule, and audit path.
- Later provider onboarding and credential capture work can happen without improvising security posture.
```