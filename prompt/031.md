# Prompt 031 For 031_seq_external_select_telephony_ivr_sms_and_email_vendors

Use the shared operating contract first:
[shared_operating_contract_026_to_035.md](/Users/test/Code/V/prompt/shared_operating_contract_026_to_035.md)

```text
Execute roadmap task 031_seq_external_select_telephony_ivr_sms_and_email_vendors.md.

Inherit the Shared_operating_contract_for_prompts_026_to_035 and consume the validated outputs from tasks `001-030`, especially tasks `008`, `021`, `022`, `023`, `026`, `027`, `028`, and `030`.

Mission:
Create the vendor-selection dossier for telephony / IVR, SMS, and email in a way that respects the blueprint’s communication and continuity law:
- telephony, SMS, and email are not generic commodity pipes; each carries different truth, reachability, failure, and evidence semantics
- no commercial vendor may become hidden lifecycle authority
- mock-first execution remains mandatory because real accounts and spend should not block MVP buildout
- real-provider choice must still be prepared with current market evidence so later acquisition is deliberate rather than ad hoc

This task therefore has two equally important jobs:
1. define the mock-now provider lane and internal simulator baseline for telephony / IVR, SMS, and email
2. create the real-provider-later shortlist and due-diligence pack using current official vendor documentation

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 2. Core product surfaces`
  - `### 3. The canonical request model`
  - `### 10. Identity, consent, security, and policy`
  - `### 12. Practical engineering shape`
- `phase-0-the-foundation-protocol.md`, especially:
  - route-intent and command-settlement law
  - contact-route separation
  - artifact and audit rules
  - dependency degradation and fallback review posture
- `phase-2-the-identity-and-echoes.md`, especially:
  - `2E. Telephony edge, IVR choreography, and call-session persistence`
  - `2F. Caller verification, voice capture, transcript stub, and SMS continuation`
  - the requirement that telephony parity does not mean telephony sameness
- `patient-account-and-communications-blueprint.md`, especially:
  - patient-visible callback / message / reminder truth
  - delivery-health failures becoming first-class blockers
- `callback-and-clinician-messaging-loop.md`, especially:
  - `ConversationCommandSettlement`
  - reachability repair
  - callback and thread settlement law
- `platform-runtime-and-release-blueprint.md`, especially:
  - `AdapterContractProfile`
  - `DependencyDegradationProfile`
- `forensic-audit-findings.md`, especially findings that punish:
  - transport success being mistaken for authoritative truth
  - delivery disputes and reachability blockers being hidden
  - degraded channels fragmenting patient experience

Current commercial-provider mechanics you must respect in the `Actual_provider_strategy_later` section:
Provider facts are temporally unstable. During execution you must browse current official vendor docs and trust / pricing / support pages. Do not rely on stale memory. At minimum, evaluate each shortlisted candidate for:
- account and project model
- test / sandbox posture
- webhook authenticity and replay protection
- UK region / residency / telephony geography where relevant
- call recording and IVR capability where relevant
- delivery callback richness for SMS and email
- sender / number / domain verification mechanics
- audit / log export and support model
- cost governance and exit friction

Primary outcome:
One source-traceable telephony / IVR / SMS / email vendor dossier that clearly separates mock-now execution from real later vendor choice and preserves the blueprint’s truth, continuity, and fallback posture.

Deliverables to create:
1. `docs/external/31_vendor_universe_telephony_sms_email.md`
2. `docs/external/31_mock_provider_lane_for_communications.md`
3. `docs/external/31_actual_provider_shortlist_and_due_diligence.md`
4. `docs/external/31_vendor_selection_decision_log.md`
5. `docs/external/31_vendor_research_evidence_register.md`
6. `docs/external/31_vendor_signal_fabric_atlas.html`
7. `data/analysis/31_vendor_universe.csv`
8. `data/analysis/31_vendor_shortlist.json`
9. `data/analysis/31_vendor_research_evidence.jsonl`
10. `data/analysis/31_mock_vs_actual_vendor_lane_matrix.csv`
11. `data/analysis/31_vendor_kill_switches.csv`
12. `tools/analysis/build_communications_vendor_shortlist.(py|ts)`
13. `tools/analysis/validate_communications_vendor_shortlist.(py|ts)`
14. `tests/playwright/31_vendor_signal_fabric_atlas.spec.(ts|js)`

Every vendor row must capture at minimum:
- `vendor_id`
- `vendor_name`
- `provider_family = telephony_ivr | sms | email | combined`
- `vendor_lane = candidate | shortlisted | mock_only | rejected`
- `supports_test_mode`
- `supports_webhooks`
- `supports_replay_protection`
- `supports_delivery_callbacks`
- `supports_recording_or_attachment_references`
- `uk_or_required_region_support_summary`
- `trust_and_compliance_evidence_refs[]`
- `cost_governance_notes`
- `lock_in_risk`
- `mock_now_fit_score`
- `actual_later_fit_score`
- `kill_switch_reason_if_any`
- `source_refs[]`
- `notes`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Select the mock provider lane for communications. This does not mean “pick any stub”. It means explicitly define the internal defaults Vecells will build against now.

The mock-now lane must decide and document:
- the canonical local telephony / IVR twin
- the canonical local SMS rail twin
- the canonical local email rail twin
- the shared webhook-signing and replay model across all three
- the delivery-truth semantics that the mocks must preserve
- the evidence bar that proves the mocks are development-grade rather than toy-grade

The mock lane must explicitly preserve:
- telephony call session lifecycle
- IVR menu / DTMF and urgent-live preemption states
- SMS continuation handoff and wrong-recipient risk
- email bounce, dispute, and delayed delivery behavior
- local acknowledgement vs transport acceptance vs authoritative outcome separation
- route repair, callback fallback, and patient-facing degraded guidance

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the real later vendor-selection strategy. It must include:
- vendor universe definition per family
- research plan using current official vendor docs only
- weighted scorecards per provider family using task `022`
- shortlist and rejection log
- kill-switch criteria for unacceptable vendors
- a no-purchase rule for this task
- a later handoff pack for tasks `032` and `033`
- explicit note on whether one vendor may cover multiple families or whether split-vendor posture is safer

Execution steps:
1. Start from tasks `021-023`.
2. Build the provider-family decision dimensions for telephony / IVR, SMS, and email from the blueprint semantics.
3. Define the mock-now lane first and freeze the required simulator fidelity.
4. Browse current official vendor docs and build the actual-later universe and shortlist second.
5. Record retrieval timestamps, evidence links, and reasons for rejection.
6. Generate the atlas UI and validate it with `Playwright_or_other_appropriate_tooling`.
7. Produce a handoff pack for account-creation tasks `032` and `033`.

Mandatory gap closures you must perform:
- Close the “voice, SMS, and email can be scored as one generic communications vendor” gap by separating family-specific truth semantics.
- Close the “mock-first means we can defer real market research entirely” gap by producing a current, source-backed shortlist now without purchasing anything.
- Close the “transport success equals communication truth” gap across all families.
- Close the “combined suite vendors automatically win” gap by explicitly scoring lock-in, portability, and failure-domain coupling.
- Close the “callback and message repair can be added after vendor choice” gap by scoring repair and webhook evidence quality directly.

Non-negotiable rules:
- do not recommend a vendor solely because it is cheap or easy to sign up for
- do not allow any vendor to pass if it cannot represent delivery ambiguity or replay-safe callbacks
- do not allow a combined vendor suite to hide weak evidence semantics in one channel
- do not initiate real sign-up, number purchase, sender verification, or spend in this task
- do not rely on stale vendor memory; browse current official docs during execution

Mandatory UI and interaction specification for `31_vendor_signal_fabric_atlas.html`:
This must feel like a premium vendor-comparison instrument, not a spreadsheet with colors.

Name the visual mode `Signal_Fabric_Atlas`.

Brand and visual posture:
- include a subtle inline Vecells mark plus a clear `COMMUNICATIONS_VENDOR_ATLAS` label
- tone: analytical, minimal, executive-review ready
- base palette:
  - canvas `#F6F8FB`
  - panel `#FFFFFF`
  - inset `#EEF3F8`
  - text strong `#101828`
  - text default `#1D2939`
  - text muted `#667085`
  - border subtle `#E4E7EC`
  - border default `#D0D5DD`
  - primary accent `#1D4ED8`
  - secondary accent `#7A5AF8`
  - telephony accent `#0E9384`
  - sms accent `#B54708`
  - email accent `#C11574`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for vendor IDs, evidence hashes, and score keys

Layout:
- sticky header with shortlist count, rejected count, mock-lane status, and freshness timestamp
- tab rail for `Telephony_IVR`, `SMS`, `Email`, `Mock_Lane`, `Actual_Lane`
- primary comparison grid in the center
- right-side due-diligence drawer
- lower strip with one restrained family-coverage diagram and one score-by-dimension chart, each with adjacent table parity

Dimensional constraints:
- page max width `1440px`
- tab rail `full width`
- due-diligence drawer `360px`
- provider cards `min-height 180px`
- buttons `44px`
- chips `28px`
- sticky header `72px`

Unique page set:
1. `Voice_and_IVR_Shortlist`
2. `SMS_Shortlist`
3. `Email_Shortlist`
4. `Mock_Lane_Fidelity`
5. `Decision_Log_and_Kill_Switches`

Motion:
- `120ms` hover and focus
- `180ms` tab transitions
- `240ms` drawer transitions
- reduced-motion support mandatory

Interaction requirements:
- dimension filters
- sortable scorecards
- evidence drawer with source timestamps
- explicit mock-vs-actual lane switch
- keyboard-first interaction
- deterministic `data-testid` markers for tabs, provider rows, score cells, evidence drawer, and lane toggle

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` to drive development and validation from the start. At minimum cover:
- tab switching
- sorting and filtering
- evidence drawer rendering
- chart/table parity
- responsive behavior
- keyboard navigation
- reduced motion
- accessibility smoke checks
- offline asset completeness

Definition of done:
- one mock-now communications provider lane is explicitly selected
- one current, source-backed real-provider shortlist exists for telephony / IVR, SMS, and email
- the output is usable as the authoritative handoff into tasks `032` and `033`
- vendor selection preserves the blueprint’s truth, replay, fallback, and continuity requirements
```
