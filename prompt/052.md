# Prompt 052 For 052_seq_phase0_define_design_contract_publication_bundle_strategy

Use the shared operating contract first:
[shared_operating_contract_046_to_055.md](/Users/test/Code/V/prompt/shared_operating_contract_046_to_055.md)

```text
Execute roadmap task 052_seq_phase0_define_design_contract_publication_bundle_strategy.md.

Inherit the Shared_operating_contract_for_prompts_046_to_055 and consume the validated outputs from tasks `001-051`.

Mission:
Define the design-contract publication-bundle strategy so every audience surface and route-family set publishes one machine-readable design authority contract joining:
- token export
- profile selection
- surface-state semantics
- state-kernel propagation
- accessibility semantic coverage
- automation anchors
- telemetry vocabulary
- artifact-mode presentation posture
- structural evidence
- fail-closed lint verdict

This task must stop route-local styling, test selectors, telemetry names, and accessibility semantics from drifting outside the published runtime tuple.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`
  - `DesignContractPublicationBundle`
  - `DesignContractLintVerdict`
  - the invariants that writable or calmly trustworthy surfaces require a current published bundle plus passing lint verdict
- `design-token-foundation.md`
  - `DesignTokenExportArtifact`
  - `TokenKernelLayeringPolicy`
  - `ProfileSelectionResolution`
  - implementation law and shell-profile model
- `canonical-ui-contract-kernel.md`
  - `VisualTokenProfile`
  - `SurfaceStateSemanticsProfile`
  - `SurfaceStateKernelBinding`
  - `AutomationAnchorMap`
  - `DesignContractVocabularyTuple`
  - `DesignContractPublicationBundle`
  - `DesignContractLintVerdict`
- `platform-frontend-blueprint.md`
  - `AutomationAnchorProfile`
  - `AccessibilitySemanticCoverageProfile`
  - the rules that manifest/runtime/design/accessibility/automation tuples must agree
  - Playwright and verification contract
- `accessibility-and-content-system-contract.md`
- `ux-quiet-clarity-redesign.md`
- outputs from tasks `014`, `042`, `044`, `050`, and `051`
- `forensic-audit-findings.md`, especially findings `116`, `117`, `118`, and `120`

Primary outcome:
One design-contract publication strategy proving that visible state, motion, semantics, automation, telemetry, and artifact posture are part of the same governed bundle rather than route-local UI convenience.

Deliverables to create:
1. `docs/architecture/52_design_contract_publication_strategy.md`
2. `docs/architecture/52_design_contract_bundle_catalog.md`
3. `docs/architecture/52_design_contract_studio.html`
4. `data/analysis/design_contract_publication_bundles.json`
5. `data/analysis/design_token_export_artifacts.json`
6. `data/analysis/design_contract_vocabulary_tuples.csv`
7. `data/analysis/design_contract_lint_rules.json`
8. `data/analysis/design_contract_structural_evidence_matrix.csv`
9. `packages/design-system/contracts/design-contract-publication.schema.json`
10. `tools/analysis/validate_design_contract_publication.(py|ts)`
11. `tests/playwright/design-contract-studio.spec.(ts|js)`

At minimum, each `DesignContractPublicationBundle` row must capture:
- `designContractPublicationBundleId`
- `audienceSurface`
- `routeFamilyRefs[]`
- `shellType`
- `breakpointCoverageRefs[]`
- `modeTupleCoverageRef`
- `designTokenExportArtifactRef`
- `visualTokenProfileRefs[]`
- `surfaceStateSemanticsProfileRefs[]`
- `automationAnchorMapRefs[]`
- `telemetryBindingProfileRefs[]`
- `artifactModePresentationProfileRefs[]`
- `designContractVocabularyTupleRefs[]`
- `designContractDigestRef`
- `structuralSnapshotRefs[]`
- `lintVerdictRef`
- `publicationState`
- `publishedAt`
- source refs and rationale

At minimum, each lint verdict must capture:
- token lattice state
- mode resolution state
- surface semantics state
- automation / telemetry parity state
- artifact-mode parity state
- surface-role usage state
- structural snapshot state
- final result
- recorded timestamp

Execution steps:
1. Start from the design-token foundation, UI kernel, frontend blueprint, and manifest strategy from task `050`.
2. Publish the design-token export strategy:
   - how token artifacts are generated
   - which layers are canonical
   - what invalidates downstream bundles
3. Publish the bundle-generation strategy:
   - how a bundle is assembled from token export, profile selection, state semantics, accessibility coverage, automation anchors, telemetry bindings, and artifact profiles
   - which route families group together
   - how digests are computed
4. Publish the lint-verdict strategy:
   - what checks are mandatory
   - what constitutes drift
   - what constitutes a hard block
   - how stale structural evidence is handled
5. Define bundle rows at minimum for:
   - patient public entry surfaces
   - patient authenticated shell surfaces
   - patient transaction / recovery surfaces
   - clinical workspace surfaces
   - support surfaces
   - hub surfaces
   - pharmacy console surfaces
   - operations console surfaces
   - governance/admin surfaces
6. Publish the `DesignContractVocabularyTuple` matrix so DOM-state names, assistive narration classes, Playwright assertions, telemetry event keys, artifact modes, and selected-anchor vocabulary remain aligned.
7. Build the studio and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
8. Validate that:
   - every active audience surface has exactly one current published design bundle
   - every bundle resolves to one current token export artifact
   - every bundle resolves to one passing lint verdict before live publication
   - route-local color, px, motion, selector, or telemetry drift would be flagged as blocked or drifted
   - structural evidence is part of the publication verdict, not a detached screenshot folder

Mandatory gap closures you must perform:
- Close the “token export and design-contract conformance drift outside the published runtime tuple” gap from forensic finding `118`.
- Close the “route-local CSS, marker aliases, and telemetry names can remain authoritative” gap by making the bundle and lint verdict fail closed.
- Close the “accessibility coverage and automation coverage can drift away from the same bundle” gap by binding them into one publication unit.
- Close the “artifact-mode posture is separate from design-state semantics” gap by including artifact presentation profiles in the same bundle.
- Close the “structural screenshots exist but are not a formal publication input” gap by turning structural evidence into a linted requirement.
- Close the “degraded patient mode can fragment across entry, section, recovery, embedded, and artifact shells” gap by forcing all such route families to resolve through the same vocabulary and semantics contracts where the corpus requires it.

Industry-best-practice requirements:
- deterministic token export
- machine-readable bundle artifacts
- digest- and lint-backed publication
- no route-local meaning-bearing style or DOM vocabulary
- shared automation and accessibility vocabulary
- CI-verifiable structural evidence
- explicit bundle invalidation when token or semantics inputs drift

Non-negotiable rules:
- One `DesignContractPublicationBundle` is the machine-readable surface contract for one audience surface and route-family set.
- Route-local constants, CSS overrides, profile packs, marker aliases, analytics names, or route-local accessibility vocabularies are not authoritative once a bundle exists.
- `DesignContractLintVerdict.result = pass` is required before a design contract may be published or reused as live runtime truth.
- Calm or writable posture is illegal once the governing design bundle is `stale`, `blocked`, or `withdrawn`.
- Any route family whose rendered DOM markers, telemetry event names, token aliases, or artifact-mode labels are not reachable from the current published bundle must degrade.

Mandatory UI and interaction specification for `52_design_contract_studio.html`:
Name the surface mode `Design_Contract_Studio`.

This must be one of the most polished internal pages in the repo. It should feel like a premium design-governance surface, not a component library doc.

Brand and visual posture:
- include a restrained Vecells wordmark plus a compact inline SVG `DCPB` monogram
- aesthetic: minimalist premium, calm, luminous but restrained, high-end product instrumentation
- include one elegant token-to-surface constellation diagram and one compact lint-verdict rail
- avoid generic card grids; use curated composition and strong visual hierarchy

Palette:
- canvas `#F7F8FC`
- rail `#EFF2F8`
- panel `#FFFFFF`
- inset `#F3F5FA`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- border default `#CBD5E1`
- primary accent `#3559E6`
- design accent `#7C3AED`
- semantic accent `#0EA5A4`
- lint accent `#0F9D58`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for digests, token IDs, lint codes, and vocabulary keys

Layout:
- sticky masthead with:
  - active bundle count
  - lint-pass count
  - blocked bundle count
  - vocabulary tuple count
- left rail (`296px`):
  - audience-surface filters
  - bundle list
- center canvas:
  - token constellation / bundle-composition diagram
  - curated bundle cards with route-family chips and lint state
- right inspector (`396px`):
  - selected bundle details
  - linked token export / vocabulary tuple / automation / telemetry / artifact profiles
  - structural evidence summary
- lower region:
  - vocabulary matrix
  - lint-rule table
  - defect strip

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- center canvas min height `600px`
- primary controls `44px`
- bundle cards `min-height 160px`
- chart or diagram parity tables mandatory

Motion:
- `120ms` hover/focus
- `180ms` bundle selection
- `220ms` inspector and constellation emphasis transition
- reduced-motion support mandatory

Interaction requirements:
- filter by audience surface, shell type, lint result, publication state, and defect state
- bundle cards and lower matrices must stay synchronized
- keyboard navigation across filters, bundle cards, and lint/vocabulary rows
- deterministic `data-testid` markers for bundle rail, constellation canvas, inspector, lint rail, and vocabulary matrix
- the page must feel unique and product-grade, not like a generic docs portal

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- audience filtering
- bundle selection
- lint-state visibility
- matrix parity
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks
- stable contract-digest markers in the DOM

Definition of done:
- Vecells has one explicit design-contract publication-bundle strategy and machine-readable bundle catalog.
- Token export, semantics, automation, telemetry, accessibility, and artifact posture now publish as one governable contract bundle.
- Runtime publication and frontend manifest work can consume design authority without relying on route-local styling or selector folklore.
```