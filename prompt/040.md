# Prompt 040 For 040_seq_external_freeze_integration_contract_assumptions_and_degraded_mode_defaults

Use the shared operating contract first:
[shared_operating_contract_036_to_045.md](/Users/test/Code/V/prompt/shared_operating_contract_036_to_045.md)

```text
Execute roadmap task 040_seq_external_freeze_integration_contract_assumptions_and_degraded_mode_defaults.md.

Inherit the Shared_operating_contract_for_prompts_036_to_045 and consume the validated outputs from tasks `001-039`.

Mission:
Freeze the authoritative external-integration assumption ledger and degraded-mode default pack for Vecells in two explicit parts:

1. a now-executable mock/simulator contract ledger that every current twin and fallback path must obey
2. a later real-provider assumption pack that every live integration must either satisfy or explicitly override through governed evidence

This task must eliminate silent defaults. Every external dependency must have one written contract for success proof, ambiguity, degraded behavior, manual fallback, and patient/staff-visible posture.

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 4. The end-to-end patient journey`
  - `### 6. Booking and access continuity`
  - `### 7. Pharmacy First pathway`
  - `### 8. Data and platform architecture`
  - the rules that degraded, fallback, repair, and ambiguity states are first-class outcomes
- `phase-0-the-foundation-protocol.md`, especially:
  - `CanonicalEventContract`
  - `EvidenceSnapshot`, `EvidenceDerivationPackage`, and parity rules
  - `SafetyDecisionRecord`
  - `UrgentDiversionSettlement`
  - `ReachabilityAssessmentRecord`
  - `ExternalConfirmationGate`
  - pharmacy dispatch / ack / expiry / outcome match objects
  - closure-blocker rules
- `platform-runtime-and-release-blueprint.md`, especially:
  - `AdapterContractProfile`
  - `DependencyDegradationProfile`
  - `ReleaseRecoveryDisposition`
  - `AudienceSurfaceRuntimeBinding`
  - verification-ladder and operational-readiness rules
- `platform-frontend-blueprint.md` and `ux-quiet-clarity-redesign.md`, especially where degraded, frozen, partial, or calm states must be rendered with truthful posture
- `forensic-audit-findings.md`, especially findings `12`, `18-25`, `31-40`, `57-68`, `72-79`, `86-120` where ambiguity, degraded truth, blocker semantics, or continuity-safe recovery were strengthened
- outputs from tasks `008`, `021-039`, especially the dependency inventory, provider scorecards, simulator backlog, checkpoint register, and path matrices

Primary outcome:
One shared assumption ledger and degraded-default pack that later scaffolding, services, projections, UIs, support flows, and assurance work can rely on without re-inventing external truth semantics.

Deliverables to create:
1. `docs/external/40_integration_contract_assumptions.md`
2. `docs/external/40_degraded_mode_defaults.md`
3. `docs/external/40_mock_vs_actual_contract_delta.md`
4. `docs/external/40_integration_contract_conflict_register.md`
5. `docs/external/40_integration_contract_cockpit.html`
6. `data/analysis/integration_assumption_ledger.csv`
7. `data/analysis/degraded_mode_defaults.json`
8. `data/analysis/provider_contract_defaults.yaml`
9. `data/analysis/integration_contract_conflict_register.json`
10. `tools/analysis/validate_integration_contract_defaults.(py|ts)`
11. `tests/playwright/integration-contract-cockpit.spec.(ts|js)`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build the mock/simulator contract ledger. For every external dependency in current scope or watch scope, declare at minimum:
- dependency ID
- dependency family
- canonical purpose in Vecells
- authoritative success proof object
- insufficient evidence patterns
- ambiguity class
- degraded mode default
- manual fallback default
- patient-visible posture default
- staff-visible posture default
- support-visible posture default
- closure-blocker implications
- simulator counterpart
- source references

At minimum cover:
- NHS login
- optional PDS
- IM1 / GP principal-system booking
- GP Connect watch routes where relevant
- pharmacy directory routes
- pharmacy update-record / visibility routes
- MESH and message-transport routes
- telephony / IVR
- SMS / email notifications
- transcription
- malware / artifact scanning
- NHS App embedded-channel watch routes

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the later real-provider assumption pack. It must state for every dependency:
- what live evidence would upgrade the mock assumption to live evidence
- which assumptions are non-negotiable because they reflect blueprint truth rather than provider preference
- which assumptions may vary by provider but must remain behind one adapter contract
- what live gate or approval is required before the assumption can move from simulated to live
- what conflicts or overrides must be recorded explicitly rather than silently applied

Execution steps:
1. Start from tasks `008` and `021-039`.
2. Inventory every external dependency and every current or planned simulator.
3. Build the assumption-ledger schema first.
4. Populate authoritative proof, ambiguity, degraded, and manual-fallback defaults per dependency.
5. Create the mock-vs-actual delta register.
6. Build the HTML cockpit and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Validate that no dependency row lacks success proof, ambiguity class, degraded default, or fallback policy.
8. Validate that any live override path requires explicit conflict-registration rather than silent mutation of defaults.

Mandatory gap closures you must perform:
- Close the “integration behavior is obvious” gap by writing every default down.
- Close the “generic success/error” gap by requiring success proof, insufficient-evidence patterns, ambiguity class, and degraded default separately.
- Close the “patient and staff see the same degraded meaning” gap by declaring audience-specific posture defaults where needed.
- Close the “simulator and live assumptions drift apart” gap by using one ledger and one delta register.
- Close the “provider-specific differences leak into business truth” gap by confining variability to adapter-contract layers.
- Close the “closure can proceed while dependency truth is ambiguous” gap by making blocker implications explicit.

Non-negotiable rules:
- Do not let any dependency row omit authoritative proof or degraded default.
- Do not collapse confirmation ambiguity into generic failure or generic success.
- Do not let provider-specific overrides silently mutate the default pack; every override needs explicit conflict registration.
- Do not let a calm UI posture imply finality when the dependency’s contract row says the state is ambiguous, degraded, or blocked.

Mandatory UI and interaction specification for `40_integration_contract_cockpit.html`:
This is a premium internal contract-and-operations surface. Name the visual mode `Contract_Cockpit`.

Brand and posture:
- use a restrained Vecells wordmark plus a compact `CONTRACTS` monogram in inline SVG
- tone: exact, high-trust, calm, clearly differentiating proof from assumption
- base palette:
  - canvas `#F6F8FA`
  - rail `#EEF2F6`
  - panel `#FFFFFF`
  - inset `#F3F6F9`
  - text strong `#0F172A`
  - text default `#1E293B`
  - text muted `#667085`
  - border subtle `#E2E8F0`
  - border default `#CBD5E1`
  - primary accent `#2563EB`
  - proof accent `#0EA5A4`
  - ambiguity accent `#7C3AED`
  - degraded accent `#C98900`
  - blocked accent `#C24141`
- typography:
  - system sans stack only
  - mono only for dependency IDs, contract keys, and evidence hashes

Layout:
- sticky masthead:
  - dependency count
  - ambiguous-default count
  - degraded-default count
  - unresolved conflicts count
- left rail (`296px`): dependency families and status filters
- center workspace:
  - assumption-ledger table
  - proof-versus-ambiguity cards
  - one restrained fallback ladder diagram showing `Proof -> Ambiguity -> Degraded -> Manual Fallback`
- right inspector (`360px`): selected dependency details, override notes, blocker implications
- lower section:
  - table parity for diagrams
  - conflict-register strip

Dimensional constraints:
- max width `1440px`
- ledger row height `52px`
- card blocks `min-height 108px`
- primary controls `44px`
- sticky header height `72px`

Motion:
- `120ms` hover/focus
- `180ms` filter transition
- `240ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by dependency family, ambiguity class, and blocker impact
- visible chips for proof, ambiguity, degraded, manual fallback, and override
- sortable freshness and blocker columns
- keyboard-first navigation
- deterministic `data-testid` markers for filters, ledger table, proof cards, inspector, and conflict strip

Unique design notes:
- prefer one precise fallback ladder and one compact proof/ambiguity comparison over multiple decorative charts
- avoid loud warning-wall styling; this should be calm and precise
- proof and assumption text should feel structurally distinct

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filtering
- sorting
- row selection
- inspector rendering
- chip rendering
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has one explicit external-assumption ledger and degraded-default pack.
- Every dependency has proof, ambiguity, degraded, fallback, and blocker semantics written down.
- Mock-now and actual-later assumptions stay aligned through one shared model and one conflict register.
```
