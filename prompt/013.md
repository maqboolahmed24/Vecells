# Prompt 013 For 013_seq_choose_backend_runtime_eventing_and_storage_baselines

Use the shared operating contract first:
[shared_operating_contract_011_to_015.md](/Users/test/Code/V/prompt/shared_operating_contract_011_to_015.md)

```text
Execute roadmap task 013_seq_choose_backend_runtime_eventing_and_storage_baselines.md.

Inherit the Shared_operating_contract_for_prompts_011_to_015 and consume the validated outputs from tasks 001-012.

Mission:
Choose the backend runtime baseline for Vecells: service decomposition posture, event spine, outbox/inbox discipline, storage topology, projection strategy, timer/workflow orchestration, and external-effect proof model.

This task must convert the corpus’s event-driven, proof-heavy architecture into a concrete backend baseline that later service implementation can follow without improvising:
- what writes authoritative truth
- how events are published and versioned
- how projections rebuild and stay compatible
- how timers and long-running waits are governed
- how external effects prove success
- which data lives in which store class
- how FHIR representations stay subordinate to the Vecells domain model

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 1. Product definition`
  - `### 3. The canonical request model`
  - `### 7. Core system and data architecture`
  - `### 12. Practical engineering shape`
  - the CQRS-style posture with domain transaction store, FHIR representation storage, event bus, object store, feature store, and WORM audit ledger
- `phase-0-the-foundation-protocol.md`, especially:
  - `## 0B. Canonical domain kernel and state machine`
  - `### 1. Required platform objects`
  - `### 2. Required platform services`
  - the canonical event taxonomy under `CanonicalEventNamespace`, `CanonicalEventContract`, `CanonicalEventEnvelope`, and `CanonicalEventNormalizationRule`
  - `#### 6.6A Adapter outbox, inbox, and callback replay rule`
  - lifecycle, safety, duplicate, booking, pharmacy, and release invariants that depend on durable proof and replay-safe eventing
- `platform-runtime-and-release-blueprint.md`, especially:
  - `### ProjectionContractFamily`
  - `### ProjectionContractVersion`
  - `### ProjectionContractVersionSet`
  - `### ProjectionQueryContract`
  - `### MutationCommandContract`
  - `### LiveUpdateChannelContract`
  - `### ClientCachePolicy`
  - `### DependencyDegradationProfile`
  - `### AdapterContractProfile`
  - `## Frontend and backend integration contract`
  - `## Data persistence and migration contract`
  - `## Security baseline contract`
- `phase-3-the-human-checkpoint.md` for more-info TTL, reminder cadence, expiry checkpoints, and human review control flow
- `phase-4-the-booking-engine.md` for slot search, reservation truth, external confirmation, waitlist continuation, and reminder scheduling
- `phase-5-the-network-horizon.md` for candidate refresh, patient-choice expiry, SLA timers, MESH continuity, and practice-visibility ack flows
- `phase-6-the-pharmacy-loop.md` for dispatch proof, outcome ingest, replay-safe reconciliation, and weak-source restrictions
- `callback-and-clinician-messaging-loop.md` for callback expectations, repair semantics, and delivery truth
- `forensic-audit-findings.md`, especially Findings:
  - 01-04 on durable shell, submission barrier, immutable snapshot, and idempotency envelope
  - 18-25 on triage, decision provenance, endpoint semantics, and closure-blocking truth
  - 27-40 on callback, message, booking, hub, and pharmacy ambiguity handling
  - 57-79 on downstream domains writing canonical state directly, closure blockers, and reconciliation proof

Primary outcome:
One backend baseline that answers:
- what the runtime services are
- how commands, events, projections, timers, and external effects flow
- what the authoritative stores are
- how proofs, checkpoints, and settlements are persisted
- how read models remain projection-first and audience-safe
- how rebuild, replay, and migration stay deterministic

Deliverables to create:
1. `docs/architecture/13_backend_runtime_service_baseline.md`
2. `docs/architecture/13_event_spine_and_namespace_baseline.md`
3. `docs/architecture/13_storage_and_persistence_baseline.md`
4. `docs/architecture/13_async_workflow_timer_and_effect_processing.md`
5. `docs/architecture/13_outbox_inbox_callback_replay_and_idempotency.md`
6. `docs/architecture/13_fhir_representation_and_projection_boundary.md`
7. `docs/architecture/13_backend_runtime_topology_atlas.html`
8. `docs/architecture/13_backend_dataflow.mmd`
9. `data/analysis/service_runtime_matrix.csv`
10. `data/analysis/canonical_event_namespace_matrix.csv`
11. `data/analysis/store_and_retention_matrix.csv`
12. `data/analysis/async_effect_proof_matrix.csv`
13. `data/analysis/timer_orchestration_matrix.csv`
14. `data/analysis/idempotency_and_replay_rules.json`
15. `tools/analysis/validate_backend_runtime_baseline.(py|ts)`

Each service / store / effect row must capture at minimum:
- `runtime_component_id`
- `component_kind = service | worker | queue | stream | store | object_prefix | cache | timer_engine | audit_sink`
- `owning_bounded_context_refs[]`
- `runtime_workload_family_ref`
- `governing_object_refs[]`
- `accepted_command_contract_refs[]`
- `emitted_event_contract_refs[]`
- `consumed_event_contract_refs[]`
- `projection_contract_refs[]`
- `store_class = transactional_domain | fhir_representation | projection_read | object_artifact | cache | event_log | worm_audit | feature_store | timer_state`
- `authoritative_truth_role`
- `tenant_scope_mode`
- `data_classification_refs[]`
- `idempotency_strategy_ref`
- `replay_strategy_ref`
- `external_proof_class = none | projection_visible | external_confirmation | review_disposition | recovery_disposition`
- `timer_dependency_refs[]`
- `source_refs[]`
- `notes`

Execution steps:
1. Start from the endpoint matrix, object catalog, invariants ledger, dependency inventory, and data-classification model. The backend baseline must satisfy all of them simultaneously.
2. Choose the service-decomposition posture. You must explicitly compare:
   - deploy-many microservices by bounded context
   - modular services with a small number of executables and strong bounded-context modules
   - modular monolith for some domains plus workers for integration/projection
   Then choose the simplest shape that still preserves workload-family boundaries, replay discipline, and later scaling needs.
3. Define the minimum runtime executables and logical runtime planes. At minimum evaluate and baseline:
   - API gateway / BFF ingress
   - command API / mutation handling
   - projection worker(s)
   - notification / communication worker
   - integration / adapter workers
   - audit / assurance ingestion worker(s)
   - timer / workflow orchestrator
   - simulator or local-adapter backplane
4. Define the authoritative store topology. You must keep separate the following classes:
   - transactional domain aggregates and control-plane writes
   - FHIR representation storage
   - immutable canonical events / event spine
   - audience projection read stores
   - binary artifact object storage
   - WORM audit storage
   - optional feature-store capability for later assistive work (mark baseline vs deferred explicitly)
5. Define the canonical event baseline. Map namespaces, contract ownership, compatibility modes, replay semantics, disclosure posture, and required identifiers. Make clear which families are authoritative vs observational.
6. Define the command / settlement / outbox / inbox baseline. For every browser or adapter effect, specify:
   - command contract
   - outbox boundary
   - adapter dispatch attempt
   - receipt checkpoint
   - authoritative proof upgrade
   - final settlement classes
7. Define the timer / workflow baseline for at least:
   - more-info reply windows
   - callback expectation windows and missed-window repair
   - booking hold / confirmation / waitlist deadlines
   - hub candidate refresh and patient-choice expiry
   - pharmacy proof deadlines and bounce-back rules
   - session, grant, and recovery expiry where backend-owned timers are required
8. Define the projection baseline. Make explicit:
   - projection contract versioning
   - rebuild from raw event history
   - projection compatibility digests
   - read-path compatibility windows
   - which routes may degrade to summary-only / recovery-only / blocked during rebuild or cutover
9. Generate a browser-viewable runtime atlas that shows services, event flow, stores, proof classes, and timer families without replacing the machine-readable architecture artifacts.

Mandatory gap closures you must perform:
- Close the “Vecells-first domain truth vs raw FHIR storage” gap by making the domain / FHIR boundary explicit and one-way through published mapping contracts.
- Close the “transport acceptance can look like business truth” gap by encoding proof classes and settlement upgrades explicitly.
- Close the “outbox/inbox/callback replay is implied rather than baseline law” gap.
- Close the “timers and long-running waits can hide inside request handlers or local workers” gap.
- Close the “projection rows imply truth” gap by keeping browser reads projection-first but never projection-authoritative.
- Close the “event naming can drift by producer” gap through canonical namespace and normalization rules.
- Close the “feature store exists in narrative but may silently become core runtime truth” gap by marking it baseline or deferred explicitly and bounding its ownership.
- Close the “unknown namespace or bad producer can blind all observability” gap by preserving slice-bounded trust and quarantine behavior.

Non-negotiable backend rules you must encode:
- Vecells domain aggregates remain the source of truth; FHIR rows are derived representations.
- Event schemas are backward-compatible by default; true breaks require new published contracts and replay proof.
- Every external effect must originate from durable outbox or equivalent durable queue position and must link back to canonical action records.
- Transport success, queue dequeue, webhook arrival, or provider ack alone are never terminal user-visible success.
- Every projection exposed to the browser must be audience-safe and contract-versioned.
- No browser read may join raw stores client-side.
- No timer-driven decision may silently mutate truth without a governed action / settlement chain where the corpus requires it.
- Raw PHI, transcripts, message bodies, phone numbers, and binary contents must move by governed artifact references or masked descriptors, not casual event payloads.
- Rebuild, backfill, migration, and rollback must stay contract-aware and route-aware.

Validation rules:
- Every endpoint from task 005 must map to at least one owning service, one authoritative store class, and one proof class.
- Every external dependency from task 008 must map to outbox / inbox / receipt-checkpoint rules or be explicitly non-applicable.
- Every canonical event namespace in Phase 0 must map to an ownership row and compatibility posture.
- Any store or service that mixes authoritative domain truth with audience projections or raw FHIR writes without a contract boundary must fail validation.
- Any timer or workflow state that can change user-visible posture but lacks an owning checkpoint / settlement / proof class must fail validation.
- Any event payload carrying raw sensitive content beyond the data-classification model from task 010 must fail validation.

Browser-viewable atlas requirements:
Build `13_backend_runtime_topology_atlas.html` and validate it with `Playwright_or_other_appropriate_tooling`.

Atlas composition:
- left rail: service family, bounded context, event namespace, store class, proof class, and timer-family filters
- hero strip: counts for services, stores, queues, canonical event namespaces, timer families, and unresolved gaps
- center flow canvas: command → event → projection → external proof → settlement pipeline view
- service inspector: selected service responsibilities, contracts, stores, event families, and trust zone
- store inspector: selected store class, owner, retention, backup, and data classification summary
- timer panel: checkpoints, deadlines, owning services, and degraded fallback behavior

Unique design requirements:
- this page should feel like a “runtime evidence map”, not a Kubernetes diagram
- keep the visual hierarchy summary-first and causal
- use arrows, sequence rails, and evidence chips rather than decorative infrastructure icons
- no vendor logo walls

Playwright validation must cover:
- flow canvas render
- inspector interactions
- filter/search behavior
- keyboard navigation
- responsive layout
- accessibility smoke checks
- stable markers for selected service, store class, proof class, and timer family

Definition of done:
- Vecells now has one backend runtime baseline that preserves its event-driven, proof-based architecture.
- Later service implementation can proceed without arguing about where truth lives, how effects settle, or how projections rebuild.
- The baseline is explicit enough to prevent transport-success shortcuts, raw-FHIR-first drift, and hidden timer logic.
```
