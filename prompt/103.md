# Prompt 103 For 103_par_phase0_track_Playwright_or_other_appropriate_tooling_frontend_publish_design_token_foundation

Use the shared operating contract first:
[shared_operating_contract_096_to_105.md](/Users/test/Code/V/prompt/shared_operating_contract_096_to_105.md)

```text
Execute roadmap task 103_par_phase0_track_Playwright_or_other_appropriate_tooling_frontend_publish_design_token_foundation.md.

Inherit the Shared_operating_contract_for_prompts_096_to_105 and consume the validated outputs from tasks `001-102`.

Mission:
Publish the first real, machine-readable Vecells design-token foundation and its premium specimen experience.

This task must turn the design-token blueprint from prose into the canonical token graph used by all later shells, route families, components, accessibility coverage, automation anchors, and runtime publication bundles.

This is frontend and design-system work. You must implement real token sources, exports, profile resolutions, validation, a high-fidelity specimen experience, and Playwright-driven development and regression coverage. Do not generate a generic token JSON dump or a Storybook-themed demo and call it done.

Algorithm grounding you must honor:
- `design-token-foundation.md`, especially:
  - token architecture and inheritance
  - `DesignTokenExportArtifact`
  - `TokenKernelLayeringPolicy`
  - `ProfileSelectionResolution`
  - breakpoint and layout lattice
  - typography, spacing, density, radius, elevation, semantic color, motion, and shell profiles
- `platform-frontend-blueprint.md`, especially:
  - `### 0.1A Canonical design-token and visual-language foundation`
  - shell visual profile law
  - design-contract responsibilities and non-negotiable invariants
  - motion and verification contracts
- `canonical-ui-contract-kernel.md`, especially `VisualTokenProfile` and the color, typography, state, and accessibility floors it binds
- `ux-quiet-clarity-redesign.md` and `uiux-skill.md`, especially the `Signal Atlas Live` and `Quiet Clarity` design philosophy, attention budget, density contracts, and “not a template library” constraints
- `accessibility-and-content-system-contract.md` for contrast floors, target sizes, focus visibility, visualization parity, and responsive constraints
- outputs from tasks `014`, `050`, `052`, `094`, `099`, and `100`
- `forensic-audit-findings.md`, especially Findings `86`, `103`, `118`, and `120`

Parallel-execution note:
This task runs in the active Phase 0 parallel block, but it must publish shared token truth in a way later tasks can consume without redesign. If later tasks own component or shell specialization, publish the canonical token graph and specimen law here and record `FOLLOW_ON_DEPENDENCY_*` rather than inventing local token branches.

Mock_now_execution:
Build the full token foundation now with local and CI-friendly exports.
- publish the same canonical token graph used later in runtime publication
- support local specimen rendering, mode switching, responsive previews, and Playwright regression without needing live backend data
- use mock shell-profile pages and token-driven diagrams that later shells can inherit directly
The mock-now path is the real token foundation. Later live shells must consume the same exports and profile-resolution records.

Actual_production_strategy_later:
When production shells consume the design system, preserve the same token lattice, layer law, mode tuples, profile-selection semantics, and publication digests. Production hardening may add packaging, distribution, or stricter linting, but it must not create route-local token systems, extra layers, or undocumented overrides.

Primary outcome:
The repository gains a canonical design-token source of truth plus a premium specimen experience that makes the token system visually understandable, mechanically exportable, and regression-testable.

Implementation deliverables to create:
1. `docs/architecture/103_design_token_foundation_publication.md`
2. `docs/architecture/103_signal_atlas_live_visual_direction.md`
3. `docs/architecture/103_token_profile_resolution_strategy.md`
4. `docs/architecture/103_design_token_specimen.html`
5. `data/analysis/design_token_export_artifact.json`
6. `data/analysis/profile_selection_resolutions.json`
7. `data/analysis/token_contrast_matrix.csv`
8. `data/analysis/token_mode_coverage_matrix.csv`
9. token source files and export pipeline under the chosen `packages/design-system` layout
10. generated CSS variables, typed token exports, and machine-readable artifact manifests
11. `assets/brand/signal-atlas-live-mark.svg`
12. `tools/analysis/validate_design_token_foundation.(py|ts)`
13. visual, unit, and export tests plus `tests/playwright/design-token-foundation.spec.(ts|js)`

You must publish these token-layer outputs at minimum:
- primitive `ref.*` tokens
- semantic `sys.*` tokens
- component `comp.*` tokens
- profile `profile.*` tokens
- one current `TokenKernelLayeringPolicy`
- one current `DesignTokenExportArtifact`
- current `ProfileSelectionResolution` rows for patient, workspace, hub, support, pharmacy, operations, governance, and embedded shells

Visual and interactive direction you must implement:
1. Design language
- Name the visual language `Signal Atlas Live` with the `Quiet Clarity` overlay.
- The specimen must feel like a premium clinical instrument, not a UI kit catalog.
- Favor structure, proportion, contrast, and calm motion over decoration.
- No default Storybook chrome, no gradient hero wallpaper, no card grid spam, and no consumer-app gloss.

2. Exact light-theme seed values to encode as token primitives now
These are the gap-closing raw token seeds because the corpus defines semantic families and tone law but not final numeric values. Encode them in the token foundation, document them as `GAP_RESOLUTION_TOKEN_SEEDS_*`, and consume only token names downstream:
- `surface.canvas = #F7F8FA`
- `surface.shell = #EEF2F6`
- `surface.panel = #FFFFFF`
- `surface.inset = #E8EEF3`
- `surface.overlay = rgba(10, 18, 28, 0.36)`
- `text.strong = #0F1720`
- `text.default = #24313D`
- `text.muted = #5E6B78`
- `border.subtle = #D6DEE6`
- `border.default = #C2CDD8`
- `accent-active = #2F6FED`
- `accent-review = #B7791F`
- `accent-insight = #5B61F6`
- `accent-success = #117A55`
- `accent-danger = #B42318`
Translate these into OKLCH or equivalent perceptually uniform tokens and prove contrast against the blueprint floors.

3. Exact dark-theme seed values to encode as token primitives now
- `surface.canvas = #0D141B`
- `surface.shell = #111C24`
- `surface.panel = #162430`
- `surface.inset = #0F1821`
- `surface.overlay = rgba(3, 7, 12, 0.62)`
- `text.strong = #F5F7FA`
- `text.default = #D6DEE6`
- `text.muted = #9FB0C1`
- `border.subtle = #273848`
- `border.default = #355066`
- `accent-active = #78A7FF`
- `accent-review = #E0A94A`
- `accent-insight = #9A9BFF`
- `accent-success = #3DB98B`
- `accent-danger = #F06A62`

4. Typography and rhythm
- use the canonical system font stack from the blueprint
- implement the exact role scale: `display 40/48/600`, `headline 32/40/600`, `title 24/32/600`, `section 20/28/600`, `body.lg 18/28/400`, `body 16/24/400`, `body.sm 14/20/400`, `label 12/16/600`, `mono.sm 12/16/500`
- use tabular numerics for counts, dates, SLA values, and IDs in the specimen
- preserve the 4px quantum and 8px structural grid

5. Layout and dimensional constraints
- `xs/sm`: 4 columns, 16px gutters, `mission_stack`
- `md`: 8 columns, 24px gutters
- `lg`: 12 columns, 24px gutters
- `xl/2xl`: 16 columns, 32px gutters
- `rail.collapsed = 72px`, `rail.expanded = 280px`
- `support.peek = 320px`, `support.open = clamp(320px, 28vw, 400px)`
- `drawer.inline = clamp(360px, 33vw, 480px)`
- dialog widths `480/640/800`
- specimen page max width for reading-heavy sections: `1440px`
- reading measure: `60-72ch`; task/spec sections: `36-56ch`

6. Motion
- use 120ms hover/attention, 180ms reveal, 240ms morph/settle, 320ms overlays only
- animate opacity and translation first; scale may only move within `0.98-1.0`
- reduced motion must keep identical state order and focus meaning
- provide a motion lab with visible and static equivalents side by side

7. Unique specimen experience
Build a bespoke token specimen page with these regions:
- a restrained masthead containing the `Signal Atlas Live` monochrome mark, mode toggles, and artifact digest chips
- a “token lattice” diagram showing `ref -> sys -> comp -> profile` lineage
- a shell-profile gallery showing patient, workspace, hub, support, pharmacy, operations, governance, and embedded profiles as quiet posture cards, not screenshots of generic dashboards
- a semantic-state wall that demonstrates neutral, active, review, insight, success, and danger roles with copy, icon, keyline, and focus treatment
- a responsive lattice explorer showing the same specimen at `xs`, `md`, `lg`, and `xl`
- a motion lane showing reveal / morph / settle / degrade / recover cues with reduced-motion equivalent
- a chart-and-table parity sample proving that visual summaries and table fallback share one token-driven vocabulary

8. Logo / mark
Create `Signal Atlas Live` as a subtle SVG mark:
- three quiet concentric orbit lines with one active node and one selected anchor node
- use neutral stroke and one semantic accent-active dot only
- no decorative gradients, mascots, or marketing shine
- it must look appropriate in product documentation and specimen surfaces without becoming a parallel brand system

Implementation instructions:
1. Publish token sources in a deterministic, layered format. The export must preserve alias lineage and mode resolution.
2. Generate CSS variables, typed token exports, and machine-readable publication artifacts from the same source graph.
3. Publish `ProfileSelectionResolution` rows for each shell profile from the same artifact.
4. Never allow these anti-patterns:
   - route-local hex values or CSS variables outside the canonical export
   - shell-local typography or spacing ladders
   - density solved by shrinking type below the canonical scale
   - decorative color use that overrides semantic meaning
5. Use `Playwright_or_other_appropriate_tooling` to drive specimen development from the beginning. Capture screenshot regression for light/dark, standard/high-contrast, relaxed/balanced/compact, and full/reduced/essential_only motion.
6. Make the specimen a production-grade reference artifact. It must have stable automation markers, keyboard support, and reduced-motion equivalence.

Gap handling you must perform during this task:
- Close the exact-token-value gap by publishing the seed palette above as canonical primitive tokens, documenting why these values satisfy the blueprint’s tone and contrast law.
- If additional tones are needed across the tone ladder, derive them systematically and record the derivation method in the design-token publication docs.
- If any shell profile needs a visual specialization not yet captured by `profile.*`, extend the profile layer rather than adding route-local tokens.

Verification requirements:
- token export schema validation
- contrast tests for all critical semantic combinations
- snapshot tests for generated exports
- `Playwright_or_other_appropriate_tooling` tests for the specimen page across all mode tuples and breakpoint classes
- accessibility checks for headings, landmarks, tables, diagrams, focus indicators, and reduced-motion equivalence

Definition of done:
- Vecells has one real token foundation, not a design memo
- the specimen experience is premium, unique, calm, and clearly not a generic UI-kit gallery
- later shells can consume the same exported tokens without redefining color, spacing, type, or motion
- Playwright-driven regression can catch token or mode drift immediately
```
