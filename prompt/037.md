# Prompt 037 For 037_seq_external_identify_pharmacy_directory_and_update_record_access_paths

Use the shared operating contract first:
[shared_operating_contract_036_to_045.md](/Users/test/Code/V/prompt/shared_operating_contract_036_to_045.md)

```text
Execute roadmap task 037_seq_external_identify_pharmacy_directory_and_update_record_access_paths.md.

Inherit the Shared_operating_contract_for_prompts_036_to_045 and consume the validated outputs from tasks `001-036`.

Mission:
Create the authoritative pharmacy access-path pack for Vecells in two explicit parts:

1. a high-fidelity local discovery-and-update-record twin that Vecells can exercise now
2. a gated real-provider route strategy for later live access once MVP, provider onboarding, and assurance prerequisites exist

This task must turn the blueprint’s pharmacy discovery, referral, consent, dispatch, outcome, and GP-visibility abstractions into one concrete access-path matrix without pretending that a current-stage MVP already has every live route enabled.

Algorithm grounding you must honor:
- `blueprint-init.md`, especially:
  - `### 7. Pharmacy First pathway`
  - the rules that Pharmacy First is a structured referral and closure loop, not direct GP record mutation by Vecells
  - the requirement that patient choice, consent, dispatch assurance, bounce-back, and GP visibility remain first-class
- `phase-0-the-foundation-protocol.md`, especially:
  - `1.10B ProviderCapabilitySnapshot`
  - `1.11 PharmacyDispatchEnvelope`
  - `1.11A PharmacyDispatchAcknowledgement`
  - `1.11B PharmacyDispatchExpiry`
  - `1.11C PharmacyOutcomeRecord`
  - `1.11D PharmacyOutcomeMatchAssessment`
  - the rules that weak-source matching cannot auto-close canonical request truth
- `phase-6-the-pharmacy-loop.md`, especially:
  - `6A. Pharmacy contract, case model, and state machine`
  - `6B. Eligibility engine, pathway rules, and versioned policy packs`
  - `6C. Pharmacy discovery, provider choice, and directory abstraction`
  - `6D. Referral pack composer, dispatch adapters, and transport contract`
  - `6E. Patient instructions, referral status, and pharmacy-facing UX logic`
  - `6F. Outcome ingest, Update Record observation, and reconciliation`
  - `6G. Bounce-back, urgent return, and reopen mechanics`
  - `6H. Practice visibility, operations queue, and pharmacy exception handling`
- `phase-5-the-network-horizon.md` where no-slot and fallback rules intersect pharmacy routing
- `patient-account-and-communications-blueprint.md` and `platform-frontend-blueprint.md` where patient continuity, same-shell recovery, and actionability are governed by proof and route-intent controls
- `platform-runtime-and-release-blueprint.md`, especially `AdapterContractProfile`, `DependencyDegradationProfile`, and environment-ring rules
- `forensic-audit-findings.md`, especially findings `35-40`, `45`, `77-79`, and any finding that prohibits generic reconciliation or direct domain-written canonical closure
- outputs from tasks `021-036`, especially provider scorecards, MESH strategy, GP provider paths, and simulator planning

Supplemental external-process mechanics you must respect for the `Actual_provider_strategy_later` section:
Use the current official materials named at minimum:
- `Directory of Healthcare Services (Service Search) API`
- `Directory of Services - Urgent and Emergency Care - REST API`
- `Electronic Prescription Service Directory of Services API`
- `GP Connect: Update Record`
- `GP Connect`
Use them only for current directory/access-route mechanics, version posture, and update-record constraints. They must not override Vecells consent, dispatch-proof, bounce-back, or closure-blocker semantics.

Primary outcome:
A pharmacy access-path matrix that tells Vecells exactly which discovery API(s), transport routes, update-record paths, and manual fallbacks are baseline-now, which are watch-only, what proof is required before claiming support, and how the simulator path mirrors the future live path.

Deliverables to create:
1. `docs/external/37_pharmacy_access_paths_mock_strategy.md`
2. `docs/external/37_pharmacy_access_paths_actual_strategy.md`
3. `docs/external/37_directory_and_update_record_decision_pack.md`
4. `docs/external/37_pharmacy_provider_gap_and_watch_register.md`
5. `docs/external/37_pharmacy_route_observatory.html`
6. `data/analysis/pharmacy_directory_access_matrix.csv`
7. `data/analysis/pharmacy_update_record_path_matrix.csv`
8. `data/analysis/pharmacy_referral_transport_decision_register.json`
9. `data/analysis/pharmacy_provider_assurance_gaps.json`
10. `tools/analysis/validate_pharmacy_access_paths.(py|ts)`
11. `tests/playwright/pharmacy-route-observatory.spec.(ts|js)`
12. `tools/browser-automation/pharmacy-access-path-discovery-dry-run.spec.(ts|js)`

Section A — `Mock_now_execution`
This section is mandatory and executable now.

Build a local high-fidelity pharmacy access-path twin that rehearses how Vecells will reason about discovery, consent, dispatch, GP visibility, and outcome reconciliation before real provider access exists.

The mock must separate these purpose classes explicitly:
- pharmacy discovery / service directory lookup
- patient/provider choice support
- referral dispatch transport
- dispatch acknowledgement / expiry tracking
- outcome ingest / outcome match assessment
- GP visibility observation path
- urgent escalation or referral-back path
- manual fallback path

The mock must represent at minimum these route families:
- `service_search_v3_primary_candidate`
- `dos_urgent_rest_watch_or_supporting_route`
- `eps_dos_supporting_route`
- `gp_update_record_assured_path`
- `mesh_or_transport_observation_dependency`
- `manual_nhsmail_or_phone_fallback`
- `practice_disabled_update_record_fallback`

For each route, classify separately:
- directory/discovery purpose
- eligibility to support patient-visible provider choice
- consent prerequisites
- provider capability snapshot requirements
- dispatch proof requirements
- acknowledgement or expiry semantics
- update-record eligibility and scope
- urgent-referral suitability
- ambiguity mode
- degraded fallback mode
- closure-blocker implications

The mock twin must make it impossible to mistake Update Record for urgent referral, safeguarding, or generic referral transport.

Section B — `Actual_provider_strategy_later`
This section is mandatory but gated.

Create the real-provider route strategy for later use. It must include:
- current route classification for pharmacy discovery APIs, supporting directories, and Update Record
- version and migration posture where relevant
- the list of evidence required before Vecells can claim live support for provider choice, dispatch, GP visibility, or outcome reconciliation behavior
- a dry-run browser-automation harness in `Playwright_or_other_appropriate_tooling` for later route discovery / onboarding preparation
- a live-execution gate that refuses any real-provider progression unless:
  - a credible pharmacy MVP exists
  - consent and dispatch proof models are implemented
  - provider-selection and transport scorecards are approved
  - named approver and target environment exist
  - live mutation flag is enabled
  - the gap/watch register has no unresolved blocker for the attempted route

Execution steps:
1. Start from tasks `021-036`.
2. Extract every pharmacy discovery, dispatch, consent, update-record, and reconciliation rule from the blueprint corpus.
3. Build the mock access-path twin first.
4. Separate strategic discovery routes from transport routes and from GP-visibility observation routes.
5. Build the decision pack and watch register.
6. Build the HTML observatory and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Create the later live dry-run harness from the same route matrix.
8. Validate that every route claim in the mock or actual strategy maps back to a canonical blueprint requirement.

Mandatory gap closures you must perform:
- Close the “discovery route vs dispatch route” confusion gap.
- Close the “Update Record equals referral transport” confusion gap.
- Close the “urgent escalation can travel through any convenient route” gap by separating urgent actions and safeguarding from summary/update flows.
- Close the “patient choice proof” gap by explicitly binding choice support to directory evidence, consent posture, and provider capability snapshots.
- Close the “practice has Update Record disabled” gap by making manual fallback explicit.
- Close the “pharmacy outcome or GP visibility event can auto-close the request” gap by keeping outcome match and closure-blocker semantics explicit.
- Close the “mock route cannot prove live truth semantics” gap by making dispatch acknowledgement, expiry, weak match, and bounce-back first-class in the twin.

Non-negotiable rules:
- Do not let Vecells behave as if it writes directly into the GP record.
- Do not let Update Record be modeled as urgent referral transport or safeguarding communication.
- Do not collapse directory route, dispatch route, and GP-visibility observation into one generic pharmacy integration flag.
- Do not store real provider credentials, ODS values, or pharmacy identifiers in repo fixtures.
- Do not let weakly matched or delayed pharmacy outcomes silently resolve or close the request lineage.

Mandatory UI and interaction specification for `37_pharmacy_route_observatory.html`:
This is a premium internal architecture-and-routing surface. Name the visual mode `Pharmacy_Route_Observatory`.

Brand and posture:
- use a restrained Vecells wordmark plus a compact `PHARM_PATHS` monogram in inline SVG
- tone: calm, high-trust, operationally literate, strongly explicit about consent and proof
- base palette:
  - canvas `#F7FAF8`
  - rail `#EEF5F1`
  - panel `#FFFFFF`
  - inset `#F3F7F5`
  - text strong `#102A23`
  - text default `#1F3A34`
  - text muted `#60736D`
  - border subtle `#DCE7E2`
  - border default `#C7D5CF`
  - primary accent `#147D64`
  - choice accent `#2563EB`
  - watch accent `#C98900`
  - blocked accent `#C24141`
  - reconciliation accent `#7C3AED`
- typography:
  - system sans stack only
  - mono only for route codes, version labels, capability keys, and proof hashes

Layout:
- sticky masthead:
  - discovery routes count
  - transport routes count
  - visibility routes count
  - unresolved blockers count
- left route-family rail (`296px`): route cards grouped by purpose class
- center workspace:
  - decision matrix
  - consent / proof ladder cards
  - one restrained lane diagram showing `Discovery -> Choice -> Dispatch -> Visibility -> Reconciliation`
- right inspector (`360px`): selected route details, official-process notes, watch flags, fallback notes
- lower section:
  - table parity for the matrix
  - gap/watch register strip

Dimensional constraints:
- max width `1440px`
- decision matrix row height `52px`
- route cards `min-height 112px`
- primary controls `44px`
- sticky header height `72px`

Motion:
- `120ms` hover/focus
- `180ms` route-family selection transition
- `240ms` inspector drawer transition
- reduced-motion support mandatory

Interaction requirements:
- filter by route purpose, maturity, and consent dependency
- visible “not for urgent use” and “manual fallback required” chips where applicable
- sortable version and freshness columns
- keyboard-first navigation
- deterministic `data-testid` markers for rail, matrix, proof ladder, inspector, and gap strip

Unique design notes:
- include one precise lane diagram and one compact stacked-bar comparison only if table parity sits immediately below
- avoid generic map pins or consumer-app styling
- choice, consent, and proof should feel like first-class information, not secondary notes

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- route-purpose filtering
- route selection
- version sorting
- consent/proof card rendering
- blocker chips
- keyboard navigation
- responsive behavior
- reduced motion
- accessibility smoke checks
- table parity for diagrams

Definition of done:
- Vecells has one credible pharmacy route matrix aligned to the blueprint and usable now.
- Discovery, dispatch, visibility, urgent fallback, and reconciliation are clearly separated.
- Every route claim is evidence-backed and tied to proof, consent, and degraded-mode defaults.
- Mock and actual-later strategies stay aligned but safely separated.
```
