# Prompt 080 For 080_par_phase0_track_backend_build_identity_repair_orchestrator_and_reachability_governor

Use the shared operating contract first:
[shared_operating_contract_076_to_085.md](/Users/test/Code/V/prompt/shared_operating_contract_076_to_085.md)

```text
Execute roadmap task 080_par_phase0_track_backend_build_identity_repair_orchestrator_and_reachability_governor.md.

Inherit the Shared_operating_contract_for_prompts_076_to_085 and consume the validated outputs from tasks `001-079`.

Mission:
Build the two authority services that own wrong-patient correction and contact-route truth:
- `IdentityRepairOrchestrator`
- `ReachabilityGovernor`

These services must make wrong-patient suspicion, contact-route degradation, access freeze, comms freeze, repair journeys, downstream compensation, and release settlement explicit and lineage-safe. They must not let support notes, provider callbacks, or local UI states rewrite binding or contact truth directly.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.4I IdentityRepairSignal`
  - `#### 1.5 IdentityRepairCase`
  - `#### 1.5A IdentityRepairFreezeRecord`
  - `#### 1.5B IdentityRepairBranchDisposition`
  - `#### 1.5C IdentityRepairReleaseSettlement`
  - `#### 1.8D ContactRouteSnapshot`
  - `#### 1.8E ReachabilityObservation`
  - `#### 1.8F ReachabilityAssessmentRecord`
  - `#### 1.9 ReachabilityDependency`
  - `#### 1.9A ContactRouteRepairJourney`
  - `#### 1.9B ContactRouteVerificationCheckpoint`
  - `#### 2.4A ReachabilityGovernor`
  - `#### 2.5 IdentityRepairOrchestrator`
  - `#### 5.6 Wrong-patient correction algorithm`
  - `#### 7.1.2 Reachability-risk function`
- `phase-2-identity-and-echoes.md`, especially the identity-binding authority, wrong-patient hold, and continuation / session implications
- `patient-account-and-communications-blueprint.md` where patient actionability must degrade into identity hold or contact-route repair under current authority truth
- `callback-and-clinician-messaging-loop.md` where delivery failures, callback repair, and patient-visible repair posture depend on canonical reachability and identity-hold state
- `forensic-audit-findings.md`, especially Findings `66`, `69`, `70`, `89`, `97`, `98`, and `120`
- outputs from tasks `001-007`, `019`, `041-045`, `056-057`, `061`, `068-069`, `072`, `077`, and `078`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Do not wait for later support, callback, or patient-portal implementations. Publish the smallest bounded projection or notification hooks they will need, and record `PARALLEL_INTERFACE_GAP_*` where a seam is missing.

Mock_now_execution:
Build a local repair and route-health simulation harness now. It must cover:
- wrong-patient suspicion opening `IdentityRepairCase`
- branch freeze for patient actions, callback, booking, and pharmacy continuations
- access-grant revocation and route-intent supersession during repair
- repair resolution and governed release settlement
- SMS bounce, phone mismatch, email bounce, opt-out, and manual dispute observations
- OTP or verification checkpoint flows that restore route health
The harness must produce the same signals, assessments, freezes, revocations, repair journeys, and release settlements production will later require.

Actual_production_strategy_later:
When live NHS login, telephony, messaging, callback, notification, and support tools are wired, they must all feed the same canonical repair and reachability services. Live providers may enrich evidence quality and timing, but they may not bypass append-only repair truth, route-snapshot supersession, grant revocation, or repair-journey authority.

Primary outcome:
The repository gains authoritative services for wrong-patient correction and contact-route dependency truth, so patient and staff surfaces can freeze, repair, recover, and resume using the same bounded lineage-safe semantics.

Implementation deliverables to create:
1. `IdentityRepairOrchestrator` service implementation
2. `ReachabilityGovernor` service implementation
3. bounded repair-impact planner for downstream branch disposition and compensation coordination
4. route-verification and repair-journey evaluator utilities
5. hooks for access-grant revocation, route-intent supersession, comms freeze, and projection rebuild requests
6. canonical-event entries for repair opened, repair frozen, branch disposition settled, release settled, route snapshot superseded, reachability changed, repair journey opened, checkpoint verified, and repair journey closed
7. `docs/architecture/80_identity_repair_and_reachability_governor_design.md`
8. `docs/architecture/80_wrong_patient_and_contact_route_repair_rules.md`
9. `docs/architecture/80_identity_repair_reachability_command_center.html`
10. `data/analysis/identity_repair_casebook.json`
11. `data/analysis/reachability_repair_matrix.csv`
12. `data/analysis/route_authority_manifest.json`
13. `tools/analysis/validate_identity_repair_and_reachability.(py|ts)`
14. unit tests, replay tests, repair-freeze tests, route-health tests, and integration tests
15. `tests/playwright/identity-repair-reachability-command-center.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- open and advance `IdentityRepairCase` from append-only repair signals
- settle `IdentityRepairFreezeRecord` and freeze access or outward comms where required
- supersede derivative grants and route intents during repair
- enumerate `IdentityRepairBranchDisposition` for affected downstream branches
- coordinate release settlement and bounded downstream compensation after repair resolution
- freeze `ContactRouteSnapshot` before any promise, delivery, callback, or reminder depends on a route
- append `ReachabilityObservation` and settle one `ReachabilityAssessmentRecord` for each materially changed dependency posture
- open and advance `ContactRouteRepairJourney` and `ContactRouteVerificationCheckpoint` for same-shell recovery

Execution steps:
1. Implement the reachability service so transport acknowledgements, mutable profile rows, or operator notes may inform but never directly replace route-health truth.
2. Implement the identity-repair service so wrong-patient suspicion or correction opens append-only repair truth without rewriting workflow milestones.
3. Integrate repair with access-grant supersession, route-intent invalidation, and outward-comms freeze hooks.
4. Integrate reachability with current dependency posture, patient-visible repair guidance, and command gating.
5. Publish typed repair and route-health outputs so patient, support, callback, booking, and pharmacy surfaces can degrade or recover consistently.
6. Build the command center and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.
7. Write tests for repair opening, repair replay, release settlement, route-health re-evaluation, repeated observations, and verification checkpoint success / expiry.

Mandatory gap closures you must perform:
- Close the “wrong-patient correction rewrites workflow state” gap by attaching repair metadata, freezes, and blockers instead of overloading lifecycle state.
- Close the “transport acceptance equals deliverable route” gap by making `ReachabilityAssessmentRecord` the only current dependency truth.
- Close the “repair or reachability can be hidden in notes” gap by publishing explicit append-only case and assessment records.
- Close the “patient recovery splits into detached shells” gap by driving same-shell repair journeys and verification checkpoints.
- Close the “stale grants and route intents survive wrong-patient suspicion” gap by revoking or superseding them during repair.
- Close the “outward comms continue despite unresolved route or identity danger” gap with authoritative comms-freeze behavior.

Industry-best-practice requirements:
- append-only repair and route-health histories
- explicit compensation and branch-disposition planning
- idempotent observation ingest and replay-safe repair signaling
- no hidden UI or support-tool overrides of canonical identity or route truth
- full audit joins for freezes, revocations, repair transitions, and release settlements
- deterministic tests for replay, duplication, expiry, and supersession

Non-negotiable rules:
- Wrong-patient correction uses `IdentityRepairCase`; it must not rewrite `Request.workflowState`.
- `ReachabilityGovernor` is the sole authority for current route-health and delivery-risk posture.
- Route health cannot be inferred from send success alone.
- Repair must revoke or supersede derivative grants and stale route intents where required.
- Same-shell repair and release posture must preserve lineage continuity and last safe anchor.

Mandatory UI and interaction specification for `80_identity_repair_reachability_command_center.html`:
Name the surface mode `Identity_Repair_Reachability_Command_Center`.

Tone:
- premium investigation and repair surface
- exact, calm, and operationally serious
- should feel like a governed repair instrument, not a support CRM

Branding:
- restrained Vecells wordmark plus inline SVG `IR` monogram
- one repair-branch cascade diagram
- one route-health funnel / dependency chart
- one same-shell repair journey ribbon

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F7`
- inset `#F4F7FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#64748B`
- border subtle `#E2E8F0`
- repair accent `#C24141`
- route accent `#2563EB`
- verification accent `#0EA5A4`
- frozen accent `#7C3AED`
- released accent `#059669`

Typography:
- system sans stack only
- monospaced digest styling for repair IDs, route snapshot IDs, epochs, and disposition hashes

Layout:
- sticky masthead with active repair cases, frozen lineages, blocked routes, and released cases
- left rail (`320px`) with repair-state filters, route-authority filters, dependency filters, and audience filters
- central canvas with:
  - repair-branch cascade diagram
  - route-health funnel / dependency chart
  - repair journey ribbon
- right inspector (`420px`) for selected repair case or route, current freezes, affected branches, and release / verification state
- lower grid with one repair table and one reachability table

Dimensional constraints:
- max width `1560px`
- masthead `76px`
- main visual zone min height `300px`
- inspector sticky top offset `92px`
- control height `44px`

Motion:
- `120ms` hover and focus
- `180ms` filter and table transitions
- `220ms` inspector reveal
- reduced-motion mode mandatory

Interaction requirements:
- filter by repair state, route-health state, dependency class, and audience impact
- selecting a repair or reachability row must synchronize cascade diagram, route funnel, journey ribbon, inspector, and tables
- keyboard navigation across filters, tables, and inspector
- deterministic `data-testid` markers for repair cascade, route funnel, repair ribbon, repair table, reachability table, and inspector
- all diagrams must have adjacent textual parity and tabular fallback

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- filtering and synchronized selection
- keyboard navigation and focus management
- reduced-motion handling
- responsive layout at desktop and tablet widths
- accessibility smoke checks and landmark verification

Definition of done:
- The repository has authoritative identity-repair and reachability services with replay-safe behavior.
- Wrong-patient and route-failure states are explicit, lineage-safe, and auditable.
- Repair can freeze access and comms, revoke stale grants, and drive same-shell recovery or governed release.
- Later patient, support, callback, booking, hub, pharmacy, and ops tasks can consume the same repair and reachability substrate.
```
