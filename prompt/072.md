# Prompt 072 For 072_par_phase0_track_backend_implement_command_settlement_record_and_transition_envelope_library

Use the shared operating contract first:
[shared_operating_contract_066_to_075.md](/Users/test/Code/V/prompt/shared_operating_contract_066_to_075.md)

```text
Execute roadmap task 072_par_phase0_track_backend_implement_command_settlement_record_and_transition_envelope_library.md.

Inherit the Shared_operating_contract_for_prompts_066_to_075 and consume the validated outputs from tasks `001-071`.

Mission:
Implement the authoritative mutation-settlement substrate and the same-shell transition-envelope library built on top of it:
- `CommandSettlementRecord`
- `TransitionEnvelope`
- any minimal settlement-to-envelope mapping utilities, schemas, and validators required to keep pending, external, review, recovery, and calm-return posture truthful.

This task spans backend settlement truth and a shared library consumed by browser surfaces. It must make the four settlement dimensions explicit and impossible to collapse into one generic success token.

Algorithm grounding you must honor:
- `phase-0-the-foundation-protocol.md`, especially:
  - `#### 1.23 CommandSettlementRecord`
  - `#### 1.9 TransitionEnvelope`
  - the command-following and same-shell recovery invariants
- `platform-frontend-blueprint.md`, especially the sections that require same-shell continuity, forbid detached success pages, and bind UI progression to authoritative settlement rather than local ack
- `ux-quiet-clarity-redesign.md`, especially `PendingActionRetention`, `QuietSettlementEnvelope`, `ContinuityRestorePlan`, and the rules that calm success requires authoritative settlement and continuity evidence
- `self-care-content-and-admin-resolution-blueprint.md`, `patient-account-and-communications-blueprint.md`, and `staff-workspace-interface-architecture.md` where pending, review, recovery, and read-only postures must remain in place
- `forensic-audit-findings.md`, especially Findings `91`, `97`, and any correction around route intent, settlement, or same-shell return truth
- outputs from tasks `001-007`, `041-050`, `056`, `061`, `065`, `067`, `071`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. Publish shared settlement and envelope schema sets so sibling tracks can consume them without duplicating truth.

Mock_now_execution:
Build a settlement simulator now. It must cover:
- local ack followed by settled success
- accepted-for-processing followed by pending external confirmation
- projection-visible but not yet authoritative success
- review-required outcome
- stale-recoverable outcome due to tuple drift
- blocked-policy and denied-scope recovery
- settlement superseded by later evidence
The simulator must drive the same `CommandSettlementRecord` and `TransitionEnvelope` library used by future real routes.

Actual_production_strategy_later:
When live providers, live queues, or production shells arrive, they must map their authoritative outcomes into the same settlement dimensions and transition-envelope library. Live external systems may increase the number of evidence sources, but they may not remove the distinction between local acknowledgement, processing acceptance, external observation, and authoritative outcome.

Primary outcome:
The repository gains real authoritative mutation-settlement records and a reusable transition-envelope library that later patient, staff, support, ops, and governance shells can consume without inventing route-local async semantics.

Implementation deliverables to create:
1. domain and persistence code for `CommandSettlementRecord`
2. shared schema and library code for `TransitionEnvelope`
3. settlement revision / supersession utilities and validators
4. settlement-to-envelope mapping functions and shared types consumable by shells
5. `docs/architecture/72_command_settlement_and_transition_envelope_design.md`
6. `docs/architecture/72_settlement_dimension_mapping_rules.md`
7. `docs/architecture/72_settlement_envelope_atlas.html`
8. `data/analysis/command_settlement_manifest.json`
9. `data/analysis/settlement_to_transition_matrix.csv`
10. `data/analysis/settlement_supersession_casebook.json`
11. `packages/shared-contracts/schemas/command-settlement-record.schema.json`
12. `packages/shared-contracts/schemas/transition-envelope.schema.json`
13. `tools/analysis/validate_settlement_envelope_library.(py|ts)`
14. unit tests, library tests, integration tests, and simulation tests
15. `tests/playwright/settlement-envelope-atlas.spec.(ts|js)`

At minimum, implement these authoritative capabilities:
- persist immutable `CommandSettlementRecord` revisions with explicit supersession links
- keep `result`, `processingAcceptanceState`, `externalObservationState`, and `authoritativeOutcomeState` distinct
- require authoritative outcome, proof class, and any required projection / recovery refs before calm success or quiet return
- emit same-shell recovery refs when outcomes are stale, blocked, denied, expired, or recoverable
- map settlement truth into `TransitionEnvelope` without losing the four-dimensional distinction
- keep last safe anchor and allowed summary tier for recoverable chains

Execution steps:
1. Implement `CommandSettlementRecord` exactly from the canonical fields and semantics.
2. Implement monotone settlement revision handling with `supersedesSettlementRef` so later callbacks or evidence can advance the same chain without creating a competing result.
3. Implement the shared `TransitionEnvelope` library and mapping rules from settlement truth to envelope truth.
4. Implement validators that fail when:
   - local ack or processing acceptance is rendered as authoritative success
   - calm return is allowed before `authoritativeOutcomeState = settled` and required proof / recovery refs are present
   - recoverable results do not carry same-shell recovery refs
   - stale tuple drift is folded into the old chain instead of opening stale recovery and a new action record when needed
5. Publish shared schemas and types so sibling tracks can consume exact settlement and envelope contracts.
6. Build the atlas and drive it with `Playwright_or_other_appropriate_tooling` while implementing it.

Mandatory gap closures you must perform:
- Close the “async mutation state is one generic success/pending flag” gap by enforcing four distinct settlement dimensions.
- Close the “local acknowledgement or accepted-for-processing can trigger calm success” gap by requiring authoritative outcome and proof.
- Close the “recoverable results can throw users to detached error or success pages” gap by requiring same-shell recovery refs and transition envelopes.
- Close the “later evidence can create a competing success chain” gap with monotone settlement revisions and supersession.
- Close the “frontend routes invent their own async semantics” gap by publishing shared schemas and mapping library.

Industry-best-practice requirements:
- immutable settlement revisions and explicit supersession
- shared schema set for backend and frontend consumers
- deterministic simulation fixtures for settlement evolution
- no hidden optimistic completion paths outside the published settlement chain
- explicit recovery contracts for stale, blocked, denied, or expired results

Non-negotiable rules:
- `CommandSettlementRecord` is the authoritative mutation outcome substrate.
- `TransitionEnvelope` is the required same-shell bridge for meaningful asynchronous action.
- Local ack, processing acceptance, external observation, and authoritative outcome are distinct.
- Calm success or shell collapse may not occur before authoritative settlement and required continuity proof.
- Recoverable outcomes must stay recoverable in place, not via detached pages.

Mandatory UI and interaction specification for `72_settlement_envelope_atlas.html`:
Name the surface mode `Settlement_Envelope_Atlas`.

Tone:
- premium continuity and settlement atlas
- calm, exact, and quietly technical
- should feel like one polished source of async truth

Branding:
- restrained Vecells wordmark plus inline SVG `SE` monogram
- one four-axis settlement quadrant
- one revision rail
- one same-shell recovery spotlight panel

Palette:
- canvas `#F7F9FC`
- panel `#FFFFFF`
- rail `#EEF2F8`
- inset `#F4F6FB`
- text strong `#0F172A`
- text default `#1E293B`
- text muted `#667085`
- border subtle `#E2E8F0`
- settlement accent `#3559E6`
- pending accent `#0EA5A4`
- recovery accent `#7C3AED`
- warning accent `#C98900`
- blocked accent `#C24141`

Typography:
- system sans stack only
- mono for settlement IDs, revision refs, and causal tokens

Layout:
- masthead with pending, settled, review-required, and recovery-required counts
- left rail (`300px`) for result, authoritative-outcome, and recovery filters
- center canvas with four-axis quadrant, revision rail, and settlement cards
- right inspector (`410px`) for selected settlement and transition-envelope details
- lower region with mapping table and validator table

Dimensional constraints:
- max width `1500px`
- masthead `72px`
- quadrant min height `250px`
- cards min height `170px`
- controls `44px`

Motion:
- `120ms` hover/focus
- `180ms` filter and card changes
- `220ms` inspector transition
- reduced-motion support mandatory

Interaction requirements:
- filter by result, authoritative outcome, and recovery state
- selecting a settlement card must synchronize quadrant, revision rail, inspector, and mapping table
- keyboard navigation across filters, cards, and rows
- deterministic `data-testid` markers for quadrant, revision rail, inspector, mapping table, and validator table
- every chart/diagram needs adjacent text parity

Playwright requirements:
Use `Playwright_or_other_appropriate_tooling` from the start. At minimum cover:
- settlement filtering
- card selection synchronization
- quadrant / table parity
- keyboard navigation
- reduced motion
- responsive layout
- accessibility smoke checks

Definition of done:
- The repository has real authoritative settlement records and a shared transition-envelope library.
- Later shells can consume one truthful async/mutation model rather than inventing their own.
- Calm success, pending, review, and recovery posture are all tied to authoritative settlement law.
```
