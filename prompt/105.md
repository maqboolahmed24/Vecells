# Prompt 105 For 105_par_phase0_track_Playwright_or_other_appropriate_tooling_frontend_build_shared_component_primitives_and_token_bindings

Use the shared operating contract first:
[shared_operating_contract_096_to_105.md](/Users/test/Code/V/prompt/shared_operating_contract_096_to_105.md)

```text
Execute roadmap task 105_par_phase0_track_Playwright_or_other_appropriate_tooling_frontend_build_shared_component_primitives_and_token_bindings.md.

Inherit the Shared_operating_contract_for_prompts_096_to_105 and consume the validated outputs from tasks `001-104`.

Mission:
Build the first real shared component primitive layer for Vecells and bind every primitive to the published token and kernel contracts.

This task must produce a premium, route-safe primitive system that later patient, workspace, support, hub, pharmacy, ops, and governance surfaces can compose without turning into generic card dashboards or local widget collections.

This is frontend and design-system implementation work. You must ship real components, typed APIs, token bindings, kernel bindings, specimen pages, and Playwright-driven visual and behavioral regression. Do not ship an ungoverned component library or a generic storybook gallery.

Algorithm grounding you must honor:
- `design-token-foundation.md` for tokens, density, surface roles, dimensions, and shell profiles
- `canonical-ui-contract-kernel.md` for state semantics, kernel bindings, automation anchors, telemetry bindings, and artifact-mode posture
- `platform-frontend-blueprint.md`, especially `PersistentShell`, `CasePulse`, `StateBraid`, `DecisionDock`, status-strip law, motion law, and same-shell continuity requirements
- `ux-quiet-clarity-redesign.md` and `uiux-skill.md` for calm hierarchy, density budgets, list-first posture, and “one dominant action” behavior
- `accessibility-and-content-system-contract.md` for semantic structure, keyboard, live-announcement, form, chart/table parity, and reduced-motion obligations
- audience shell blueprints where primitive shape matters:
  - `patient-portal-experience-architecture-blueprint.md`
  - `staff-workspace-interface-architecture.md`
  - `operations-console-frontend-blueprint.md`
  - `pharmacy-console-frontend-architecture.md`
  - `governance-admin-console-frontend-blueprint.md`
- outputs from tasks `014`, `050`, `103`, and `104`
- `forensic-audit-findings.md`, especially Findings `86`, `92`, `93`, `97`, `116`, `117`, `118`, and `120`

Parallel-execution note:
This task runs in the active Phase 0 parallel block. It must build shared primitives only. Do not silently jump ahead into full audience-app implementation. If a later task owns a route family or shell specialization, provide the reusable primitive plus specimen composition here and record `FOLLOW_ON_DEPENDENCY_*`.

Mock_now_execution:
Build the full primitive set now against mock route-state inputs and the published token/kernel contracts.
- components must render from real token exports and real kernel bindings
- specimen pages must use mocked patient, workspace, ops, and governance state tuples so the primitives can be verified before live data integration
- DOM markers, telemetry vocabulary, and reduced-motion behavior must already behave as they will in later real shells
The mock-now path is the real primitive layer. Later surface work should mostly compose these primitives rather than rewrite them.

Actual_production_strategy_later:
When production surfaces consume the primitives, preserve the same token bindings, state semantics, DOM-marker law, telemetry vocabulary, and accessibility behavior. Production hardening may add performance tuning or bundle splitting, but it must not alter primitive meaning or create shell-local forks.

Primary outcome:
The repository gains a governed primitive layer with strong token and kernel bindings, plus a premium component atlas that demonstrates how the primitives compose into distinctive Vecells surfaces.

Implementation deliverables to create:
1. `docs/architecture/105_shared_component_primitives_and_token_bindings.md`
2. `docs/architecture/105_component_api_and_surface_role_contracts.md`
3. `docs/architecture/105_component_atlas.html`
4. primitive and shared-composite code under the chosen `packages/design-system` layout
5. `data/analysis/component_binding_matrix.csv`
6. `data/analysis/component_automation_anchor_matrix.json`
7. `data/analysis/component_accessibility_coverage_matrix.csv`
8. `tools/analysis/validate_component_bindings.(py|ts)`
9. unit, component, visual, accessibility, and regression tests plus `tests/playwright/component-atlas.spec.(ts|js)`

You must build at minimum these primitive families:
- shell-plane primitives: `ShellFrame`, `ShellRail`, `ShellHeader`, `SharedStatusStrip`, `PromotedSupportRegionFrame`
- semantic working primitives: `CasePulse`, `StateBraid`, `DecisionDock`, `FreshnessChip`, `AmbientStateRibbon`, `SelectedAnchorStub`
- surface-role primitives: `BoardSurface`, `CardSurface`, `TaskSurface`, `RailSurface`, `DrawerSurface`, `FormSurface`, `ListSurface`, `TableSurface`, `ArtifactSurface`
- controls: quiet primary button, secondary button, inline utility button, segmented tabs, quiet chips, filter pills, input field frame, textarea frame, select frame, checkbox/radio frame
- calm-state primitives: loading skeleton, empty-state frame, stale-state frame, blocked-state frame, recovery-state frame, placeholder artifact frame
- comparison and visualization primitives with mandatory summary and table fallback

Visual and interactive direction you must implement:
1. Overall atlas composition
Create a bespoke “Vecells Component Atlas”, not a generic component inventory.
- left rail with surface-role navigation and shell-profile toggles
- centered live composition stage showing realistic mini-screens built from primitives
- right inspector with token bindings, kernel bindings, automation anchors, and accessibility notes
- one lower strip for motion and state transitions
- use the `Signal Atlas Live` mark subtly in the masthead only

2. Showcase pages you must build
- `Patient_Mission_Frame`: calm section-entry with spotlight card, quiet action row, status strip, record/list excerpt, and one promoted support region
- `Workspace_Quiet_Mission_Control`: compact queue spine, preview pocket, active task surface, decision dock, and interruption digest stub
- `Operations_Control_Room_Preview`: north-star band, one table-first health grid, one bounded chart with summary and table fallback, and intervention workbench stub
- `Governance_Approval_Frame`: scope ribbon, change envelope, impact preview, approval stepper, and evidence rail
These are specimen compositions only, but they must look unique and premium rather than like four rearranged dashboard templates.

3. Exact color and spacing behavior
- consume only the token foundation values from task `103`
- primary background surfaces must stay in quiet neutrals
- only one semantic accent may dominate each primary composition
- card-like surfaces are allowed only when the surface role is actually `card`; boards, tables, rails, drawers, and tasks must look different from one another
- use `radius 8` for cards/tasks, `12` for drawers/dialog-like panes, `0-4` for dense tables and input wells
- section padding defaults: `16/24/32px` depending on surface role and breakpoint

4. Typography
- display only in specimen hero or section headers
- most working surfaces should use `section`, `body`, `body.sm`, and `label`
- do not solve density by shrinking type below the canonical roles

5. Motion
- hover and focus cues: `120ms`
- reveal and same-surface expansion: `180ms`
- state settle / morph: `240ms`
- no bouncing, spring theatrics, or decorative counter animations
- reduced motion must preserve the same causal meaning with static emphasis and focus movement only

6. Interaction constraints
- one dominant action per primary specimen viewport
- one promoted support region maximum unless the specimen intentionally demonstrates a blocker or compare state
- primitives must support keyboard-first interaction and stable focus restore
- status chips, buttons, and tables must not rely on color alone

7. Diagrams and charts
- include one component taxonomy diagram showing surface roles and their primitive families
- include one density posture diagram showing quiet vs dense regions
- any chart used in the atlas must ship with summary text and table fallback from the same data

Implementation instructions:
1. Build primitives on top of the token foundation and kernel bindings. Every primitive must expose or consume the relevant automation anchors and accessibility hooks.
2. Publish typed APIs that make semantic misuse harder. For example, require explicit state props or surface-role props where the kernel needs them.
3. Never allow these anti-patterns:
   - one generic “Card” wrapper used for every surface role
   - hidden state encoded only in CSS class names
   - DOM markers added ad hoc per story or page
   - button styles or focus rings bypassing token values
   - charts without table fallback and summary sentence
4. Create a custom atlas page rather than relying on stock story explorers as the primary artifact. A secondary explorer is acceptable, but the authoritative artifact must be the custom atlas.
5. Use `Playwright_or_other_appropriate_tooling` from the beginning:
   - drive atlas development
   - capture visual baselines
   - assert keyboard and focus behavior
   - assert DOM markers from the automation anchor maps
   - verify reduced-motion and high-contrast modes

Gap handling you must perform during this task:
- If the corpus names a primitive concept but not an exact component API, define the smallest API that preserves the canonical behavior and record `GAP_RESOLUTION_COMPONENT_API_*`.
- If a specimen composition reveals missing surface roles or state variants, extend the token/kernel layers first and record the dependency rather than patching component-local styles.
- If a visualization primitive lacks parity metadata, block it and record `GAP_VISUALIZATION_PARITY_*`.

Verification requirements:
- component unit tests
- token-binding tests
- kernel-binding tests
- accessibility tests
- screenshot regression across shell profiles, modes, and breakpoints
- `Playwright_or_other_appropriate_tooling` tests for the atlas and specimen pages, including keyboard-only journeys, focus order, DOM markers, summary/table parity for charts, and reduced-motion equivalence

Definition of done:
- Vecells has a real primitive layer bound to tokens and kernel law
- specimen compositions look bespoke, premium, and route-safe rather than generic UI-kit demos
- later audience surfaces can compose these primitives without forking visual or semantic truth
- Playwright-driven regression makes component drift immediately visible
```
