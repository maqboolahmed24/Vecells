{
  "name": "@vecells/domain-kernel",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Domain Kernel shared contract package with 1 contract families.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts"
}
