{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "package-api-contracts",
  "sourceRoot": "packages/api-contracts/src",
  "projectType": "library",
  "tags": ["artifact:package", "context:shared_contracts", "class:shared_contract_package"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} lint"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} typecheck"
      }
    }
  }
}
