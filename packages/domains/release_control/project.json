{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "package-domains-release-control",
  "sourceRoot": "packages/domains/release_control/src",
  "projectType": "library",
  "tags": ["artifact:package", "context:release_control", "class:domain_context_package"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} lint"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} typecheck"
      }
    }
  }
}
