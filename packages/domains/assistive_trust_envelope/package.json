{
  "name": "@vecells/domain-assistive-trust-envelope",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Assistive surface binding, provenance, confidence, freeze-frame, and trust-envelope projection for Phase 8.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts"
}
