# Assistive Trust Envelope

Backend package for Phase 8 task 411. It owns same-shell assistive surface binding, presentation contracts, provenance envelopes, conservative confidence digests, freeze frames, and the authoritative `AssistiveCapabilityTrustEnvelope` consumed by later UI and monitoring tasks.

Primary factory: `createAssistiveTrustEnvelopeProjectionPlane`.
