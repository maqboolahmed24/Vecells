{
  "name": "@vecells/domain-hub-coordination",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Hub Coordination Domain context package with 49 canonical object families.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vecells/domain-kernel": "workspace:*",
    "@vecells/event-contracts": "workspace:*",
    "@vecells/authz-policy": "workspace:*",
    "@vecells/observability": "workspace:*"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts"
}
