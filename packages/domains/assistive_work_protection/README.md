# Assistive Work Protection

Backend package for Phase 8 task 412. It owns assistive sessions, session fence validation, explicit draft insertion points, draft patch leases, work-protection leases, deferred delta buffering, and quiet-return targets.

Primary factory: `createAssistiveWorkProtectionPlane`.
