{
  "name": "@vecells/domain-intake-request",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Phase 1 intake request validation and submit-readiness domain package.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src tests --ext .ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vecells/domain-kernel": "workspace:*",
    "@vecells/event-contracts": "workspace:*",
    "@vecells/authz-policy": "workspace:*",
    "@vecells/observability": "workspace:*"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts"
}
