{
  "name": "@vecells/domain-assistive-freeze",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Assistive release freeze, freshness invalidation, recovery disposition, and actionability guard runtime for Phase 8.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts"
}
