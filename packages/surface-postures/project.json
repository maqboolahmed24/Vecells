{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "package-surface-postures",
  "sourceRoot": "packages/surface-postures/src",
  "projectType": "library",
  "tags": ["artifact:package", "context:frontend_runtime", "class:shared_experience_package"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} lint"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} typecheck"
      }
    }
  }
}
