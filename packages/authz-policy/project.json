{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "package-authz-policy",
  "sourceRoot": "packages/authz-policy/src",
  "projectType": "library",
  "tags": ["artifact:package", "context:identity_access", "class:shared_policy_package"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} lint"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} typecheck"
      }
    }
  }
}
