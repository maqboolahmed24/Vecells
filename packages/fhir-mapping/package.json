{
  "name": "@vecells/fhir-mapping",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "FHIR Mapping shared contract package with 1 contract families.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vecells/domain-kernel": "workspace:*",
    "@vecells/event-contracts": "workspace:*"
  },
  "exports": {
    ".": "./src/index.ts",
    "./schemas/fhir-representation-set": "./schemas/fhir-representation-set.schema.json",
    "./schemas/fhir-resource-record": "./schemas/fhir-resource-record.schema.json",
    "./schemas/fhir-exchange-bundle": "./schemas/fhir-exchange-bundle.schema.json"
  },
  "types": "./src/index.ts"
}
