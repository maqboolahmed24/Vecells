{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "package-runtime-secrets",
  "sourceRoot": "packages/runtime-secrets/src",
  "projectType": "library",
  "tags": ["artifact:package", "context:platform_runtime", "class:shared_security_package"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} build"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} lint"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} test"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir {projectRoot} typecheck"
      }
    }
  }
}
