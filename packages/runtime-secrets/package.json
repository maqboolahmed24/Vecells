{
  "name": "@vecells/runtime-secrets",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Manifest-driven runtime secret loading, envelope encryption, and rotation controls.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vecells/domain-kernel": "workspace:*"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts"
}
